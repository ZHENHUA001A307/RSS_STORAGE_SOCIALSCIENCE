<?xml version="1.0" encoding="UTF-8" ?>
<rss version="2.0">
<channel>
    <title>我的每日小说</title>
    <link>https://github.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE</link>
    <description>从 EPUB 自动生成的 RSS 订阅源</description>
        <item>
    <title>Basic Python - 019 - 第 19 章 用户账户.epub</title>
    <description><![CDATA[<p style='color: #888;'>⏱ 预计阅读时间：142 分钟 (28371 字)</p><hr><h1 id="nav_point_442">第 19 章　用户账户</h1>
<blockquote>
<p><img alt="" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/19_Image00001.gif" style="width: 16%" width="16%"/>
 　Web应用程序的核心是让任何用户都能够注册账户并能够使用它，不管用户身处何方。在本章中，我们将创建一些表单，让用户能够添加主题和条目，以及编辑既有的条目。我们还将学习Django如何防范对基于表单的页面发起的常见攻击，从而无须花太多时间考虑确保应用程序安全的问题。</p>
</blockquote>
<p>然后，我们将实现一个用户身份验证系统。首先创建一个注册页面，供用户创建账户，并让有些页面只能供已登录的用户访问。接下来，修改一些视图函数，使得用户只能看到自己的数据。我们还将学习如何确保用户数据的安全。</p>
<h2 id="nav_point_443">19.1　让用户输入数据</h2>
<p>建立用于创建用户账户的身份验证系统之前，我们先来添加几个页面，让用户能够输入数据。我们将让用户添加新主题，添加新条目以及编辑既有条目。</p>
<p>当前，只有超级用户能够通过管理网站输入数据。我们不想让用户与管理网站交互，因此我们将使用Django的表单创建工具来创建让用户能够输入数据的页面。</p>
<h3 id="nav_point_444">19.1.1　添加新主题</h3>
<p>首先来让用户能够添加新主题。创建基于表单的页面的方法几乎与前面创建页面一样：定义URL，编写视图函数并编写一个模板。一个主要差别是，需要导入包含表单的模块forms.py。</p>
<ol>
<li value="1">
<p>
<strong>用于添加主题的表单</strong>
</p>
<p>让用户输入并提交信息的页面都是表单，那怕看起来不像。用户输入信息时，我们需要进行验证，确认提供的信息是正确的数据类型，而不是恶意的信息，如中断服务器的代码。然后，对这些有效信息进行处理，并将其保存到数据库的合适地方。这些工作很多都是由Django自动完成的。</p>
<p>在Diango中，创建表单的最简单方式是使用<code>ModelForm</code>
 ，它根据我们在第18章定义的模型中的信息自动创建表单。请创建一个名为forms.py的文件，将其存储到models.py所在的目录，并在其中编写你的第一个表单：</p>
<p>
<em>forms.py</em>
</p>
<pre class="代码无行号"><code>  from django import forms
  from .models import Topic
❶ class TopicForm(forms.ModelForm):
      class Meta:
❷         model = Topic
❸         fields = ['text']
❹         labels = {'text': ''}</code>
</pre>
<p>首先导入模块<code>forms</code>
 以及要使用的模型<code>Topic</code>
 。在❶处，定义一个名为<code>TopicForm</code>
 的类，它继承了<code>forms.ModelForm</code>
 。</p>
<p>最简单的<code>ModelForm</code>
 版本只包含一个内嵌的<code>Meta</code>
 类，让Django根据哪个模型创建表单以及在表单中包含哪些字段。在❷处，根据模型<code>Topic</code>
 创建表单，其中只包含字段<code>text</code>
 （见❸）。❹处的代码让Django不要为字段<code>text</code>
 生成标签。<br/>
 </p>
</li>
<li value="2">
<p>
<strong>URL模式<code>new_topic</code>
</strong>
</p>
<p>新页面的URL应简短且具有描述性，因此当用户要添加新主题时，我们切换到http://localhost:8000/new_topic/。下面是页面<code>new_topic</code>
 的URL模式，请将其添加到learning_logs/ urls.py中：</p>
<p>
<em>urls.py</em>
</p>
<pre class="代码无行号"><code>--snip--
urlpatterns = [
    --snip--
    # 用于添加新主题的页面。
    path('new_topic/', views.new_topic, name='new_topic'),
]</code>
</pre>
<p>这个URL模式将请求交给视图函数<code>new_topic()</code>
 ，下面来编写这个函数。<br/>
 </p>
</li>
<li value="3">
<p>
<strong>视图函数<code>new_topic()</code>
</strong>
</p>
<p>函数<code>new_topic()</code>
 需要处理两种情形。一是刚进入<code>new_topic</code>
 页面（在这种情况下应显示空表单）；二是对提交的表单数据进行处理，并将用户重定向到页面<code>topics</code>
 ：</p>
<p>
<em>views.py</em>
</p>
<pre class="代码无行号"><code>  from django.shortcuts import render, redirect
  from .models import Topic
  from .forms import TopicForm
  --snip--
  def new_topic(request):
      """添加新主题。"""
❶     if request.method != 'POST':
          # 未提交数据：创建一个新表单。
❷         form = TopicForm()
      else:
          # POST提交的数据：对数据进行处理。
❸         form = TopicForm(data=request.POST)
❹         if form.is_valid():
❺             form.save()
❻             return redirect('learning_logs:topics')
      # 显示空表单或指出表单数据无效。
❼     context = {'form': form}
      return render(request, 'learning_logs/new_topic.html', context)</code>
</pre>
<p>我们导入了函数<code>redirect</code>
 ，用户提交主题后将使用这个函数重定向到页面<code>topics</code>
 。函数<code>redirect</code>
 将视图名作为参数，并将用户重定向到这个视图。我们还导入了刚创建的表单<code>TopicForm</code>
 。<br/>
 </p>
</li>
<li value="4">
<p>
<strong>GET请求和POST请求</strong>
</p>
<p>创建Web应用程序时，将用到的两种主要请求类型是GET请求和POST请求。对于只是从服务器读取数据的页面，使用<strong>GET</strong>
 请求；在用户需要通过表单提交信息时，通常使用<strong>POST</strong>
 请求。处理所有表单时，都将指定使用POST方法。还有一些其他类型的请求，但本项目没有使用。</p>
<p>函数<code>new_topic()</code>
 将请求对象作为参数。用户初次请求该页面时，其浏览器将发送GET请求；用户填写并提交表单时，其浏览器将发送POST请求。根据请求的类型，可确定用户请求的是空表单（GET请求）还是要求对填写好的表单进行处理（POST请求）。</p>
<p>❶处的测试确定请求方法是GET还是POST。如果请求方法不是POST，请求就可能是GET，因此需要返回一个空表单。（即便请求是其他类型的，返回空表单也不会有任何问题。）❷处创建一个<code>TopicForm</code>
 实例，将其赋给变量<code>form</code>
 ，再通过上下文字典将这个表单发送给模板（见❼）。由于实例化<code>TopicForm</code>
 时没有指定任何实参，Django将创建一个空表单，供用户填写。</p>
<p>如果请求方法为POST，将执行<code>else</code>
 代码块，对提交的表单数据进行处理。我们使用用户输入的数据（存储在<code>request.POST</code>
 中）创建一个<code>TopicForm</code>
 实例（见❸），这样对象<code>form</code>
 将包含用户提交的信息。</p>
<p>要将提交的信息保存到数据库，必须先通过检查确定它们是有效的（见❹）。方法<code>is_valid()</code>
 核实用户填写了所有必不可少的字段（表单字段默认都是必不可少的），且输入的数据与要求的字段类型一致（例如，字段<code>text</code>
 少于200字符，这是第18章在models.py中指定的）。这种自动验证避免了我们去做大量的工作。如果所有字段都有效，就可调用<code>save()</code>
 （见❺），将表单中的数据写入数据库。</p>
<p>保存数据后，就可离开这个页面了。为此，使用<code>redirect()</code>
 将用户的浏览器重定向到页面<code>topics</code>
 （见❻）。在页面<code>topics</code>
 中，用户将在主题列表中看到他刚输入的主题。</p>
<p>我们在这个视图函数的末尾定义了变量<code>context</code>
 ，并使用稍后将创建的模板new_topic.html来渲染页面。这些代码不在<code>if</code>
 代码块内，因此无论是用户刚进入<code>new_topic</code>
 页面还是提交的表单数据无效，这些代码都将执行。用户提交的表单数据无效时，将显示一些默认的错误消息，帮助用户提供有效的数据。<br/>
 </p>
</li>
<li value="5">
<p>
<strong>模板<code>new_topic</code>
</strong>
</p>
<p>下面来创建新模板new_topic.html，用于显示刚创建的表单：</p>
<p>
<em>new_topic.html</em>
</p>
<pre class="代码无行号"><code>  {% extends "learning_logs/base.html" %}
  {% block content %}
    &lt;p&gt;Add a new topic:&lt;/p&gt;
❶   &lt;form action="{% url 'learning_logs:new_topic' %}" method='post'&gt;
❷     {% csrf_token %}
❸     {{ form.as_p }}
❹     &lt;button name="submit"&gt;Add topic&lt;/button&gt;
    &lt;/form&gt;
  {% endblock content %}</code>
</pre>
<p>这个模板继承了base.html，因此其基本结构与项目“学习笔记”的其他页面相同。在❶处，定义了一个HTML表单。实参<code>action</code>
 告诉服务器将提交的表单数据发送到哪里。这里将它发回给视图函数<code>new_topic()</code>
 。实参<code>method</code>
 让浏览器以POST请求的方式提交数据。</p>
<p>Django使用模板标签<code>{% csrf_token %}</code>
 （见❷）来防止攻击者利用表单来获得对服务器未经授权的访问（这种攻击称为<strong>跨站请求伪造</strong>
 ）。❸处显示表单，从中可知Django使得完成显示表单等任务有多简单：只需包含模板变量<code>{{ form.as_p }}</code>
 ，就可让Django自动创建显示表单所需的全部字段。修饰符<code>as_p</code>
 让Django以段落格式渲染所有表单元素，这是一种整洁地显示表单的简单方式。</p>
<p>Django不会为表单创建提交按钮，因此我们在❹处定义了一个。<br/>
 </p>
</li>
<li value="6">
<p>
<strong>链接到页面<code>new_topic</code>
</strong>
</p>
<p>下面在页面<code>topics</code>
 中添加到页面<code>new_topic</code>
 的链接：</p>
<p>
<em>topics.html</em>
</p>
<pre class="代码无行号"><code>{% extends "learning_logs/base.html" %}
{% block content %}
  &lt;p&gt;Topics&lt;/p&gt;
  &lt;ul&gt;
    --snip--
  &lt;/ul&gt;
  &lt;a href="{% url 'learning_logs:new_topic' %}"&gt;Add a new topic&lt;/a&gt;
{% endblock content %}</code>
</pre>
<p>这个链接放在既有主题列表的后面。图19-1显示了生成的表单。请使用这个表单来添加几个新主题。</p>
<p class="p-img"><img alt="" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/19_Image00052.gif" style="width: 90%" width="90%"/>
</p>
<p>
<strong>图19-1　用于添加新主题的页面</strong>
</p>
</li>
</ol>
<h3 id="nav_point_445">19.1.2　添加新条目</h3>
<p>可以添加新主题之后，用户还会想添加几个新条目。我们将再次定义URL，编写视图函数和模板，并且链接到添加新条目的页面。但在此之前，需要在forms.py中再添加一个类。</p>
<ol>
<li value="1">
<p>
<strong>用于添加新条目的表单</strong>
</p>
<p>我们需要创建一个与模型<code>Entry</code>
 相关联的表单，但这个表单的定制程度比<code>TopicForm</code>
 更高一些：</p>
<p>
<em>forms.py</em>
</p>
<pre class="代码无行号"><code>  from django import forms
  from .models import Topic, Entry
  class TopicForm(forms.ModelForm):
      --snip--
  class EntryForm(forms.ModelForm):
      class Meta:
          model = Entry
          fields = ['text']
❶         labels = {'text': ' '}
❷         widgets = {'text': forms.Textarea(attrs={'cols': 80})}</code>
</pre>
<p>首先修改<code>import</code>
 语句，使其除导入<code>Topic</code>
 外，还导入<code>Entry</code>
 。新类<code>EntryForm</code>
 继承了<code>forms.ModelForm</code>
 ，它包含的<code>Meta</code>
 类指出了表单基于的模型以及要在表单中包含哪些字段。这里给字段<code>'text'</code>
 指定了标签<code>'Entry:'</code>
 （见❶）。</p>
<p>在❷处，我们定义了属性<code>widgets</code>
 。<strong>小部件</strong>
 （widget）是一个HTML表单元素，如单行文本框、多行文本区域或下拉列表。通过设置属性<code>widgets</code>
 ，可覆盖Django选择的默认小部件。通过让Django使用<code>forms.Textarea</code>
 ，我们定制了字段<code>'text'</code>
 的输入小部件，将文本区域的宽度设置为80列，而不是默认的40列。这给用户提供了足够的空间来编写有意义的条目。<br/>
 </p>
</li>
<li value="2">
<p>
<strong>URL模式<code>new_entry</code>
</strong>
</p>
<p>在用于添加新条目的页面的URL模式中，需要包含实参<code>topic_id</code>
 ，因为条目必须与特定的主题相关联。该URL模式如下，请将它添加到learning_logs/urls.py中：</p>
<p>
<em>urls.py</em>
</p>
<pre class="代码无行号"><code>--snip--
urlpatterns = [
    --snip--
    # 用于添加新条目的页面。
    path('new_entry/&lt;int:topic_id&gt;/', views.new_entry, name='new_entry'),
]</code>
</pre>
<p>这个URL模式与形如<code>http://localhost:8000/new_entry/<i>id</i>
 /</code>
 的URL匹配，其中的<code>
<i>id</i>
</code>
 是一个与主题ID匹配的数。代码<code>&lt;int:topic_id&gt;</code>
 捕获一个数值，并将其赋给变量<code>topic_id</code>
 。请求的URL与这个模式匹配时，Django将请求和主题ID发送给函数<code>new_entry()</code>
 。<br/>
 </p>
</li>
<li value="3">
<p>
<strong>视图函数<code>new_entry()</code>
</strong>
</p>
<p>视图函数<code>new_entry()</code>
 与函数<code>new_topic()</code>
 很像，请在views.py中添加如下代码：</p>
<p>
<em>views.py</em>
</p>
<pre class="代码无行号"><code>  from django.shortcuts import render, redirect
  from .models import Topic
  from .forms import TopicForm, EntryForm
  --snip--
  def new_entry(request, topic_id):
      """在特定主题中添加新条目。"""
❶     topic = Topic.objects.get(id=topic_id)
❷     if request.method != 'POST':
          # 未提交数据：创建一个空表单。
❸         form = EntryForm()
      else:
          # POST提交的数据：对数据进行处理。
❹         form = EntryForm(data=request.POST)
          if form.is_valid():
❺             new_entry = form.save(commit=False)
❻             new_entry.topic = topic
              new_entry.save()
❼             return redirect('learning_logs:topic', topic_id=topic_id)
      # 显示空表单或指出表单数据无效。
      context = {'topic': topic, 'form': form}
      return render(request, 'learning_logs/new_entry.html', context)</code>
</pre>
<p>我们修改<code>import</code>
 语句，在其中包含刚创建的<code>EntryForm</code>
 。<code>new_entry()</code>
 的定义包含形参<code>topic_id</code>
 ，用于存储从URL中获得的值。渲染页面和处理表单数据时，都需要知道针对的是哪个主题，因此使用<code>topic_id</code>
 来获得正确的主题（见❶）。</p>
<p>在❷处，检查请求方法是POST还是GET。如果是GET请求，就执行<code>if</code>
 代码块，创建一个空的<code>EntryForm</code>
 实例（见❸）。如果请求方法为POST，就对数据进行处理：创建一个<code>EntryForm</code>
 实例，使用<code>request</code>
 对象中的POST数据来填充它（见❹）。然后检查表单是否有效。如果有效，就设置条目对象的属性<code>topic</code>
 ，再将条目对象保存到数据库。</p>
<p>调用<code>save()</code>
 时，传递实参<code>commit=False</code>
 （见❺），让Django创建一个新的条目对象，并将其赋给<code>new_entry</code>
 ，但不保存到数据库中。将<code>new_entry</code>
 的属性<code>topic</code>
 设置为在这个函数开头从数据库中获取的主题（见❻），再调用<code>save()</code>
 且不指定任何实参。这将把条目保存到数据库，并将其与正确的主题相关联。</p>
<p>在❼处，调用<code>redirect()</code>
 ，它要求提供两个参数：要重定向到的视图和要给视图函数提供的参数。这里重定向到<code>topic()</code>
 ，而这个视图函数需要参数<code>topic_id</code>
 。视图函数<code>topic()</code>
 渲染新增条目所属主题的页面，其中的条目列表包含新增的条目。</p>
<p>在视图函数<code>new_entry()</code>
 的末尾，我们创建了一个上下文字典，并使用模板new_entry.html渲染页面。这些代码将在用户刚进入页面或提交的表单数据无效时执行。<br/>
 </p>
</li>
<li value="4">
<p>
<strong>模板<code>new_entry</code>
</strong>
</p>
<p>模板<code>new_entry</code>
 类似于模板<code>new_topic</code>
 ，如下面的代码所示：</p>
<p>
<em>new_entry.html</em>
</p>
<pre class="代码无行号"><code>  {% extends "learning_logs/base.html" %}
  {% block content %}
❶   &lt;p&gt;&lt;a href="{% url 'learning_logs:topic' topic.id %}"&gt;{{ topic }}&lt;/a&gt;&lt;/p&gt;
    &lt;p&gt;Add a new entry:&lt;/p&gt;
❷   &lt;form action="{% url 'learning_logs:new_entry' topic.id %}" method='post'&gt;
      {% csrf_token %}
      {{ form.as_p }}
      &lt;button name='submit'&gt;Add entry&lt;/button&gt;
    &lt;/form&gt;
  {% endblock content %}</code>
</pre>
<p>在页面顶端显示主题（见❶），让用户知道自己是在哪个主题中添加条目。该主题名也是一个链接，可用于返回到该主题的主页面。</p>
<p>表单的实参<code>action</code>
 包含URL中的<code>topic_id</code>
 值，让视图函数能够将新条目关联到正确的主题（见❷）。除此之外，这个模板与模板new_topic.html完全相同。<br/>
 </p>
</li>
<li value="5">
<p>
<strong>链接到页面<code>new_entry</code>
</strong>
</p>
<p>接下来，需要在显示特定主题的页面中添加到页面<code>new_entry</code>
 的链接：</p>
<p>
<em>topic.html</em>
</p>
<pre class="代码无行号"><code>{% extends "learning_logs/base.html" %}
{% block content %}
  &lt;p&gt;Topic: {{ topic }}&lt;/p&gt;
  &lt;p&gt;Entries:&lt;/p&gt;
  &lt;p&gt;
    &lt;a href="{% url 'learning_logs:new_entry' topic.id %}"&gt;Add new entry&lt;/a&gt;
  &lt;/p&gt;
  &lt;ul&gt;
  --snip--
  &lt;/ul&gt;
{% endblock content %}</code>
</pre>
<p>我们将这个链接放在条目列表前面，因为在这种页面中，执行的最常见的操作是添加新条目。图19-2显示了页面<code>new_entry</code>
 。现在用户可添加新主题，还可在每个主题中添加任意数量的条目。请在一些主题中添加新条目，尝试使用一下页面<code>new_entry</code>
 。</p>
<p class="p-img"><img alt="" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/19_Image00053.gif" style="width: 90%" width="90%"/>
</p>
<p>
<strong>图19-2　页面<code>new_entry</code>
</strong>
</p>
</li>
</ol>
<h3 id="nav_point_446">19.1.3　编辑条目</h3>
<p>下面来创建让用户能够编辑既有条目的页面。</p>
<ol>
<li value="1">
<p>
<strong>URL模式<code>edit_entry</code>
</strong>
</p>
<p>这个页面的URL需要传递要编辑的条目的ID。修改后的learning_logs/urls.py如下：</p>
<p>
<em>urls.py</em>
</p>
<pre class="代码无行号"><code>--snip--
urlpatterns = [
    --snip--
    # 用于编辑条目的页面。
    path('edit_entry/&lt;int:entry_id&gt;/', views.edit_entry, name='edit_entry'),
]</code>
</pre>
<p>在URL（如http://localhost:8000/edit_entry/1/）中传递的ID存储在形参<code>entry_id</code>
 中。这个URL模式将与其匹配的请求发送给视图函数<code>edit_entry()</code>
 。<br/>
 </p>
</li>
<li value="2">
<p>
<strong>视图函数<code>edit_entry()</code>
</strong>
</p>
<p>页面<code>edit_entry</code>
 收到GET请求时，<code>edit_entry()</code>
 将返回一个表单，让用户能够对条目进行编辑；收到POST请求（条目文本经过修订）时，则将修改后的文本保存到数据库：</p>
<p>
<em>views.py</em>
</p>
<pre class="代码无行号"><code>  from django.shortcuts import render, redirect
  from .models import Topic, Entry
  from .forms import TopicForm, EntryForm
  --snip--
  def edit_entry(request, entry_id):
      """编辑既有条目。"""
❶     entry = Entry.objects.get(id=entry_id)
      topic = entry.topic
      if request.method != 'POST':
          # 初次请求：使用当前条目填充表单。
❷         form = EntryForm(instance=entry)
      else:
          # POST提交的数据：对数据进行处理。
❸         form = EntryForm(instance=entry, data=request.POST)
          if form.is_valid():
❹             form.save()
❺             return redirect('learning_logs:topic', topic_id=topic.id)
      context = {'entry': entry, 'topic': topic, 'form': form}
      return render(request, 'learning_logs/edit_entry.html', context)</code>
</pre>
<p>首先导入模型<code>Entry</code>
 。在❶处，获取用户要修改的条目对象以及与其相关联的主题。在请求方法为GET时将执行的<code>if</code>
 代码块中，使用实参<code>instance=entry</code>
 创建一个<code>EntryForm</code>
 实例（见❷）。这个实参让Django创建一个表单，并使用既有条目对象中的信息填充它。用户将看到既有的数据，并且能够编辑。</p>
<p>处理POST请求时，传递实参<code>instance=entry</code>
 和<code>data=request.POST</code>
 （见❸），让Django根据既有条目对象创建一个表单实例，并根据<code>request.POST</code>
 中的相关数据对其进行修改。然后，检查表单是否有效。如果有效，就调用<code>save()</code>
 且不指定任何实参（见❹），因为条目已关联到特定的主题。然后，重定向到显示条目所属主题的页面（见❺），用户将在其中看到其编辑的条目的新版本。</p>
<p>如果要显示表单让用户编辑条目或者用户提交的表单无效，就创建上下文字典并使用模板edit_entry.html渲染页面。<br/>
 </p>
</li>
<li value="3">
<p>
<strong>模板<code>edit_entry</code>
</strong>
</p>
<p>下面来创建模板edit_entry.html，它与模板new_entry.html类似：</p>
<p>
<em>edit_entry.html</em>
</p>
<pre class="代码无行号"><code>  {% extends "learning_logs/base.html" %}
  {% block content %}
    &lt;p&gt;&lt;a href="{% url 'learning_logs:topic' topic.id %}"&gt;{{ topic }}&lt;/a&gt;&lt;/p&gt;
    &lt;p&gt;Edit entry:&lt;/p&gt;
❶   &lt;form action="{% url 'learning_logs:edit_entry' entry.id %}" method='post'&gt;
      {% csrf_token %}
      {{ form.as_p }}
❷     &lt;button name="submit"&gt;Save changes&lt;/button&gt;
    &lt;/form&gt;
  {% endblock content %}</code>
</pre>
<p>在❶处，实参<code>action</code>
 将表单发送给函数<code>edit_entry()</code>
 处理。在标签<code>{% url %}</code>
 中，将条目ID作为一个实参，让视图函数<code>edit_entry()</code>
 能够修改正确的条目对象。在❷处，将提交按钮的标签设置成Save changes，旨在提醒用户：单击该按钮将保存所做的编辑，而不是创建一个新条目。<br/>
 </p>
</li>
<li value="4">
<p>
<strong>链接到页面<code>edit_entry</code>
</strong>
</p>
<p>现在，需要在显示特定主题的页面中给每个条目添加到页面<code>edit_entry</code>
 的链接：</p>
<p>
<em>topic.html</em>
</p>
<pre class="代码无行号"><code>--snip--
  {% for entry in entries %}
    &lt;li&gt;
      &lt;p&gt;{{ entry.date_added|date:'M d, Y H:i' }}&lt;/p&gt;
      &lt;p&gt;{{ entry.text|linebreaks }}&lt;/p&gt;
      &lt;p&gt;
        &lt;a href="{% url 'learning_logs:edit_entry' entry.id %}"&gt;Edit entry&lt;/a&gt;
      &lt;/p&gt;
    &lt;/li&gt;
--snip--</code>
</pre>
<p>将编辑链接放在了每个条目的日期和文本后面。在循环中，使用模板标签<code>{% url %}</code>
 根据URL模式<code>edit_entry</code>
 和当前条目的ID属性（<code>entry.id</code>
 ）来确定URL。链接文本为Edit entry，它出现在页面中每个条目的后面。图19-3显示了包含这些链接时，显示特定主题的页面是什么样的。</p>
<p class="p-img"><img alt="" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/19_Image00054.jpg" style="width: 90%" width="90%"/>
</p>
<p>
<strong>图19-3　每个条目都有一个用于编辑的链接</strong>
</p>
<p>至此，“学习笔记”已具备了需要的大部分功能。用户可添加主题和条目，还可根据需要查看任何条目。在下一节，我们将实现一个用户注册系统，让任何人都可向“学习笔记”申请账户，并创建自己的主题和条目。</p>
</li>
</ol>
<blockquote>
<p>
<strong>动手试一试</strong>
</p>
<p>
<strong>练习19-1：博客</strong>
 　新建一个Django项目，将其命名为Blog。在这个项目中，创建一个名为<code>blogs</code>
 的应用程序，并在其中创建一个名为<code>BlogPost</code>
 的模型。这个模型应包含<code>title</code>
 、<code>text</code>
 和<code>date_added</code>
 等字段。为这个项目创建一个超级用户，并使用管理网站创建几个简短的帖子。创建一个主页，在其中按时间顺序显示所有的帖子。</p>
<p>创建两个表单，其中一个用于发布新帖子，另一个用于编辑既有的帖子。尝试填写这些表单，确认它们能够正确工作。</p>
</blockquote>
<h2 id="nav_point_447">19.2　创建用户账户</h2>
<p>本节将建立用户注册和身份验证系统，让用户能够注册账户，进而登录和注销。为此，我们将新建一个应用程序，其中包含与处理用户账户相关的所有功能。这个应用程序将尽可能使用Django自带的用户身份验证系统来完成工作。本节还将对模型<code>Topic</code>
 稍做修改，让每个主题都归属于特定用户。</p>
<h3 id="nav_point_448">19.2.1　应用程序<code>users</code>
</h3>
<p>首先使用命令<code>startapp</code>
 创建一个名为<code>users</code>
 的应用程序：</p>
<pre class="代码无行号"><code>  (ll_env)learning_log$ python manage.py startapp users
  (ll_env)learning_log$ ls
❶ db.sqlite3 learning_log learning_logs ll_env manage.py users
  (ll_env)learning_log$ ls users
❷ __init__.py admin.py apps.py migrations models.py tests.py views.py</code>
</pre>
<p>这个命令新建一个名为users的目录（见❶），其结构与应用程序<code>learning_logs</code>
 相同（见❷）。</p>
<h3 id="nav_point_449">19.2.2　将<code>users</code>
 添加到settings.py中</h3>
<p>在settings.py中，需要将这个新的应用程序添加到<code>INSTALLED_APPS</code>
 中，如下所示：</p>
<p>
<em>settings.py</em>
</p>
<pre class="代码无行号"><code>--snip--
INSTALLED_APPS = [
    # 我的应用程序
    'learning_logs',
    'users',
    # Django默认创建的应用程序
    --snip--
]
--snip--</code>
</pre>
<p>这样，Django将把应用程序<code>users</code>
 包含到项目中。</p>
<h3 id="nav_point_450">19.2.3　包含<code>users</code>
 的URL</h3>
<p>接下来，需要修改项目根目录中的urls.py，使其包含将为应用程序<code>users</code>
 定义的URL：</p>
<p>
<em>urls.py</em>
</p>
<pre class="代码无行号"><code>from django.contrib import admin
from django.urls import path, include
urlpatterns = [
    path('admin/', admin.site.urls),
    path('users/', include('users.urls')),
    path('', include('learning_logs.urls')),
]</code>
</pre>
<p>我们添加了一行代码，以包含应用程序<code>users</code>
 中的文件urls.py。这行代码与任何以单词users打头的URL（如http://localhost:8000/users/login/）都匹配。</p>
<h3 id="nav_point_451">19.2.4　登录页面</h3>
<p>首先来实现登录页面。我们将使用Django提供的默认视图<code>login</code>
 ，因此这个应用程序的URL模式稍有不同。在目录learning_log/users/中，新建一个名为urls.py的文件，并在其中添加如下代码：</p>
<p>
<em>urls.py</em>
</p>
<pre class="代码无行号"><code>  """为应用程序users定义URL模式。"""
  from django.urls import path, include
❶ app_name = 'users'
  urlpatterns = [
      # 包含默认的身份验证URL。
❷     path('', include('django.contrib.auth.urls')),
  ]</code>
</pre>
<p>导入函数<code>path</code>
 和<code>include</code>
 ，以便包含Django定义的一些默认的身份验证URL。这些默认的URL包含具名的URL模式，如<code>'login'</code>
 和<code>'logout'</code>
 。我们将变量<code>app_name</code>
 设置成<code>'users'</code>
 ，让Django能够将这些URL与其他应用程序的URL区分开来（见❶）。即便是Django提供的默认URL，将其包含在应用程序<code>users</code>
 的文件中后，也可通过命名空间<code>users</code>
 进行访问。</p>
<p>登录页面的URL模式与URL http://localhost:8000/users/login/匹配（见❷）。这个URL中的单词users让Django在users/urls.py中查找，而单词login让它将请求发送给Django的默认视图<code>login</code>
 。</p>
<ol>
<li value="1">
<p>
<strong>模板login.html</strong>
</p>
<p>用户请求登录页面时，Django将使用一个默认的视图函数，但我们依然需要为这个页面提供模板。默认的身份验证视图在文件夹registration中查找模板，因此我们需要创建这个文件夹。为此，在目录learning_log/users/中新建一个名为templates的目录，再在这个目录中新建一个名为registration的目录。下面是模板login.html，应将其存储到目录learning_log/users/templates/ registration中：</p>
<p>
<em>login.html</em>
</p>
<pre class="代码无行号"><code>  {% extends "learning_logs/base.html" %}
  {% block content %}
❶   {% if form.errors %}
      &lt;p&gt;Your username and password didn't match. Please try again.&lt;/p&gt;
    {% endif %}
❷   &lt;form method="post" action="{% url 'users:login' %}"&gt;
      {% csrf_token %}
❸     {{ form.as_p }}
❹     &lt;button name="submit"&gt;Log in&lt;/button&gt;
❺     &lt;input type="hidden" name="next"
        value="{% url 'learning_logs:index' %}" /&gt;
    &lt;/form&gt;
  {% endblock content %}</code>
</pre>
<p>这个模板继承了base.html，旨在确保登录页面的外观与网站的其他页面相同。请注意，一个应用程序中的模板可继承另一个应用程序中的模板。</p>
<p>如果设置表单的<code>errors</code>
 属性，就显示一条错误消息（见❶），指出输入的用户名密码对与数据库中存储的任何用户名密码对都不匹配。</p>
<p>我们要让登录视图对表单进行处理，因此将实参<code>action</code>
 设置为登录页面的URL（见❷）。登录视图将一个表单发送给模板。在模板中，我们显示这个表单（见❸）并添加一个提交按钮（见❹）。在❺处，包含了一个隐藏的表单元素<code>'next'</code>
 ，其中的实参<code>value</code>
 告诉Django在用户成功登录后将其重定向到什么地方。在本例中，用户将返回主页。<br/>
 </p>
</li>
<li value="2">
<p>
<strong>链接到登录页面</strong>
</p>
<p>下面在base.html中添加到登录页面的链接，让所有页面都包含它。用户已登录时，我们不想显示这个链接，因此将它嵌套在一个<code>{% if %}</code>
 标签中：</p>
<p>
<em>base.html</em>
</p>
<pre class="代码无行号"><code>  &lt;p&gt;
    &lt;a href="{% url 'learning_logs:index' %}"&gt;Learning Log&lt;/a&gt; -
    &lt;a href="{% url 'learning_logs:topics' %}"&gt;Topics&lt;/a&gt; -
❶   {% if user.is_authenticated %}
❷     Hello, {{ user.username }}.
    {% else %}
❸     &lt;a href="{% url 'users:login' %}"&gt;Log in&lt;/a&gt;
    {% endif %}
  &lt;/p&gt;
  {% block content %}{% endblock content %}</code>
</pre>
<p>在Django身份验证系统中，每个模板都可使用变量<code>user</code>
 。这个变量有一个<code>is_authenticated</code>
 属性：如果用户已登录，该属性将为<code>True</code>
 ，否则为<code>False</code>
 。这让你能够向已通过身份验证的用户显示一条消息，而向未通过身份验证的用户显示另一条消息。</p>
<p>这里向已登录的用户显示问候语（见❶）。对于已通过身份验证的用户，还设置了属性<code>username</code>
 。这里使用该属性来个性化问候语，让用户知道自己已登录（见❷）。在❸处，对于尚未通过身份验证的用户，显示到登录页面的链接。<br/>
 </p>
</li>
<li value="3">
<p>
<strong>使用登录页面</strong>
</p>
<p>前面建立了一个用户账户，下面来登录一下，看看登录页面是否管用。请访问http://localhost:8000/admin/，如果你依然是以管理员身份登录的，请在页眉上找到注销链接并单击它。</p>
<p>注销后，访问http://localhost:8000/users/login/将看到类似于图19-4所示的登录页面。输入你在前面设置的用户名和密码，将进入索引页面。在这个主页的页眉中，显示了一条个性化问候语，其中包含你的用户名。</p>
<p class="p-img"><img alt="" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/19_Image00055.gif" style="width: 85%" width="85%"/>
</p>
<p>
<strong>图19-4　登录页面</strong>
</p>
</li>
</ol>
<h3 id="nav_point_452">19.2.5　注销</h3>
<p>现在需要提供一个让用户注销的途径。为此，我们将在base.html中添加一个注销链接。用户单击这个链接时，将进入一个确认其已注销的页面。</p>
<ol>
<li value="1">
<p>
<strong>在base.html中添加注销链接</strong>
</p>
<p>下面在base.html中添加注销链接，让每个页面都包含它。将注销链接放在<code>{% if user.is_authenticated %}</code>
 部分中，这样只有已登录的用户才能看到它：</p>
<p>
<em>base.html</em>
</p>
<pre class="代码无行号"><code>--snip--
  {% if user.is_authenticated %}
    Hello, {{ user.username }}.
    &lt;a href="{% url 'users:logout' %}"&gt;Log out&lt;/a&gt;
  {% else %}
    --snip--</code>
</pre>
<p>默认的具名注销URL模式为<code>'logout'</code>
 。<br/>
 </p>
</li>
<li value="2">
<p>
<strong>注销确认页面</strong>
</p>
<p>成功注销后，用户希望获悉这一点。因此默认的注销视图使用模板logged_out.html渲染注销确认页面，我们现在就来创建该模板。下面这个简单的页面确认用户已注销，请将其存储到目录templates/registration（login.html所在的目录）中：</p>
<p>
<em>logged_out.html</em>
</p>
<pre class="代码无行号"><code>{% extends "learning_logs/base.html" %}
{% block content %}
  &lt;p&gt;You have been logged out. Thank you for visiting!&lt;/p&gt;
{% endblock content %}</code>
</pre>
<p>在这个页面中，不需要提供其他内容，因为base.html提供了到主页和登录页面的链接。</p>
<p>图19-5显示了用户单击Log out链接后出现的注销确认页面。这里的重点是创建能够正确工作的网站，因此几乎没有设置样式。确定所需的功能都能正确运行后，我们将设置这个网站的样式，使其看起来更专业。</p>
<p class="p-img"><img alt="" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/19_Image00056.gif" style="width: 85%" width="85%"/>
</p>
<p>
<strong>图19-5　注销确认页面指出用户已成功注销</strong>
</p>
</li>
</ol>
<h3 id="nav_point_453">19.2.6　注册页面</h3>
<p>下面来创建一个页面供新用户注册。我们将使用Django提供的表单<code>UserCreationForm</code>
 ，但编写自己的视图函数和模板。</p>
<ol>
<li value="1">
<p>
<strong>注册页面的URL模式</strong>
</p>
<p>下面的代码定义了注册页面的URL模式，它也包含在users/urls.py中：</p>
<p>
<em>urls.py</em>
</p>
<pre class="代码无行号"><code>"""为应用程序users定义URL模式。"""
from django.urls import path, include
from . import views
app_name = 'users'
urlpatterns = [
    # 包含默认的身份验证URL。
    path('', include('django.contrib.auth.urls')),
    # 注册页面
    path('register/', views.register, name='register'),
]</code>
</pre>
<p>我们从<code>users</code>
 中导入模块<code>views</code>
 。为何需要这样做呢？因为我们将为注册页面编写视图函数。注册页面的URL模式与URL http://localhost:8000/users/register/匹配，并将请求发送给即将编写的函数<code>register()</code>
 。<br/>
 </p>
</li>
<li value="2">
<p>
<strong>视图函数<code>register()</code>
</strong>
</p>
<p>在注册页面首次被请求时，视图函数<code>register()</code>
 需要显示一个空的注册表单，并在用户提交填写好的注册表单时对其进行处理。如果注册成功，这个函数还需让用户自动登录。请在users/views.py中添加如下代码：</p>
<p>
<em>views.py</em>
</p>
<pre class="代码无行号"><code>  from django.shortcuts import render, redirect
  from django.contrib.auth import login
  from django.contrib.auth.forms import UserCreationForm
  def register(request):
      """注册新用户。"""
      if request.method != 'POST':
          # 显示空的注册表单。
❶         form = UserCreationForm()
      else:
          # 处理填写好的表单。
❷         form = UserCreationForm(data=request.POST)
❸         if form.is_valid():
❹             new_user = form.save()
              # 让用户自动登录，再重定向到主页。
❺         login(request, new_user)
❻         return redirect('learning_logs:index')
      # 显示空表单或指出表单无效。
      context = {'form': form}
      return render(request, 'registration/register.html', context)</code>
</pre>
<p>首先导入函数<code>render()</code>
 和<code>redirect()</code>
 ，然后导入函数<code>login()</code>
 ，以便在用户正确填写了注册信息时让其自动登录。我们还导入了默认表单<code>UserCreationForm</code>
 。在函数<code>register()</code>
 中，检查要响应的是否是POST请求。如果不是，就创建一个<code>UserCreationForm</code>
 实例，且不给它提供任何初始数据（见❶）。</p>
<p>如果响应的是POST请求，就根据提交的数据创建一个<code>UserCreationForm</code>
 实例（见❷），并检查这些数据是否有效（见❸）。就本例而言，有效是指用户名未包含非法字符，输入的两个密码相同，以及用户没有试图做恶意的事情。</p>
<p>如果提交的数据有效，就调用表单的方法<code>save()</code>
 ，将用户名和密码的散列值保存到数据库中（见❹）。方法<code>save()</code>
 返回新创建的用户对象，我们将它赋给了<code>new_user</code>
 。保存用户的信息后，调用函数<code>login()</code>
 并传入对象<code>request</code>
 和<code>new_user</code>
 ，为用户创建有效的会话，从而让其自动登录（见❺）。最后，将用户重定向到主页（见❻），而主页的页眉中显示了一条个性化的问候语，让用户知道注册成功了。</p>
<p>在这个函数的末尾，我们渲染了注册页面：它要么显示一个空表单，要么显示提交的无效表单。<br/>
 </p>
</li>
<li value="3">
<p>
<strong>注册模板</strong>
</p>
<p>下面来创建注册页面的模板，它与登录页面的模板类似。请务必将其保存到login.html所在的目录中：</p>
<p>
<em>register.html</em>
</p>
<pre class="代码无行号"><code>{% extends "learning_logs/base.html" %}
{% block content %}
  &lt;form method="post" action="{% url 'users:register' %}"&gt;
    {% csrf_token %}
    {{ form.as_p }}
    &lt;button name="submit"&gt;Register&lt;/button&gt;
    &lt;input type="hidden" name="next" value="{% url 'learning_logs:index' %}" /&gt;
  &lt;/form&gt;
{% endblock content %}</code>
</pre>
<p>这里也使用了方法<code>as_p</code>
 ，让Django在表单中正确地显示所有的字段，包括错误消息——如果用户没有正确地填写表单。<br/>
 </p>
</li>
<li value="4">
<p>
<strong>链接到注册页面</strong>
</p>
<p>下面来添加一些代码，在用户没有登录时显示到注册页面的链接：</p>
<p>
<em>base.html</em>
</p>
<pre class="代码无行号"><code>--snip--
  {% if user.is_authenticated %}
    Hello, {{ user.username }}.
    &lt;a href="{% url 'users:logout' %}"&gt;Log out&lt;/a&gt;
  {% else %}
    &lt;a href="{% url 'users:register' %}"&gt;Register&lt;/a&gt; -
    &lt;a href="{% url 'users:login' %}"&gt;Log in&lt;/a&gt;
  {% endif %}
--snip--</code>
</pre>
<p>现在，已登录的用户看到的是个性化的问候语和注销链接，而未登录的用户看到的是注册链接和登录链接。请尝试使用注册页面创建几个用户名各不相同的用户账户。</p>
<p>下一节会将一些页面限制为仅让已登录的用户访问，还将确保每个主题都归属于特定用户。</p>
<blockquote>
<p>
<strong>注意</strong>
 　这里的注册系统允许用户创建任意数量的账户。有些系统要求用户确认其身份：发送一封确认邮件，用户回复后账户才生效。通过这样做，这些系统会比本例的简单系统生成更少的垃圾账户。然而，学习创建应用程序时，完全可以像这里所做的那样，使用简单的用户注册系统。</p>
</blockquote>
</li>
</ol>
<p>　</p>
<blockquote>
<p>
<strong>动手试一试</strong>
</p>
<p>
<strong>练习19-2：博客账户</strong>
 　在为完成练习19-1而开发的项目Blog中，添加用户身份验证和注册系统。向已登录的用户显示其用户名，向未注册的用户显示到注册页面的链接。</p>
</blockquote>
<h2 id="nav_point_454">19.3　让用户拥有自己的数据</h2>
<p>用户应该能够输入其专有的数据，因此我们将创建一个系统，确定各项数据所属的用户，再限制对页面的访问，让用户只能使用自己的数据。</p>
<p>本节将修改模型<code>Topic</code>
 ，让每个主题都归属于特定用户。这也将影响条目，因为每个条目都属于特定的主题。我们先来限制对一些页面的访问。</p>
<h3 id="nav_point_455">19.3.1　使用<code>@login_required</code>
 限制访问</h3>
<p>Django提供了装饰器<code>@login_required</code>
 ，让你能够轻松地只允许已登录用户访问某些页面。<strong>装饰器</strong>
 （decorator）是放在函数定义前面的指令，Python在函数运行前根据它来修改函数代码的行为。下面来看一个示例。</p>
<ol>
<li value="1">
<p>
<strong>限制访问显示所有主题的页面</strong>
</p>
<p>每个主题都归特定用户所有，因此应只允许已登录的用户请求显示所有主题的页面。为此，在learning_logs/views.py中添加如下代码：</p>
<p>
<em>views.py</em>
</p>
<pre class="代码无行号"><code>from django.shortcuts import render, redirect
from django.contrib.auth.decorators import login_required
from .models import Topic, Entry
--snip--
@login_required
def topics(request):
    """显示所有的主题。"""
    --snip--</code>
</pre>
<p>首先导入函数<code>login_required()</code>
 。将<code>login_required()</code>
 作为装饰器应用于视图函数<code>topics()</code>
 ——在它前面加上符号<code>@</code>
 和<code>login_required</code>
 ，让Python在运行<code>topics()</code>
 的代码之前运行<code>login_required()</code>
 的代码。</p>
<p>
<code>login_required()</code>
 的代码检查用户是否已登录，仅当用户已登录时，Django才运行<code>topics()</code>
 的代码。如果用户未登录，就重定向到登录页面。</p>
<p>为实现这种重定向，需要修改settings.py，让Django知道到哪里去查找登录页面。请在settings.py末尾添加如下代码：</p>
<p>
<em>settings.py</em>
</p>
<pre class="代码无行号"><code>--snip--
# 我的设置
LOGIN_URL = 'users:login'</code>
</pre>
<p>现在，如果未登录的用户请求装饰器<code>@login_required</code>
 保护的页面，Django将重定向到settings.py中的<code>LOGIN_URL</code>
 指定的URL。</p>
<p>要测试这个设置，可注销并进入主页，再单击链接Topics，这将重定向到登录页面。然后，使用你的账户登录，并再次单击主页中的Topics链接，你将看到显示所有主题的页面。<br/>
 </p>
</li>
<li value="2">
<p>
<strong>全面限制对项目“学习笔记”的访问</strong>
</p>
<p>Django让你能够轻松地限制对页面的访问，但你必须确定要保护哪些页面。最好先确定项目的哪些页面不需要保护，再限制对其他所有页面的访问。你可轻松地修改过于严格的访问限制。比起不限制对敏感页面的访问，这样做的风险更低。</p>
<p>在项目“学习笔记”中，将不限制对主页和注册页面的访问，并限制对其他所有页面的访问。</p>
<p>在下面的learning_logs/views.py中，对除<code>index()</code>
 外的每个视图都应用了装饰器<code>@login_required</code>
 ：</p>
<p>
<em>views.py</em>
</p>
<pre class="代码无行号"><code>--snip--
@login_required
def topics(request):
    --snip--
@login_required
def topic(request, topic_id):
    --snip--
@login_required
def new_topic(request):
    --snip--
@login_required
def new_entry(request, topic_id):
    --snip--
@login_required
def edit_entry(request, entry_id):
    --snip--</code>
</pre>
<p>如果你在未登录的情况下尝试访问这些页面，将被重定向到登录页面。另外，你还不能单击到<code>new_topic</code>
 等页面的链接。如果你输入URL http://localhost:8000/new_topic/，将被重定向到登录页面。对于所有与私有用户数据相关的URL，都应限制访问。</p>
</li>
</ol>
<h3 id="nav_point_456">19.3.2　将数据关联到用户</h3>
<p>现在，需要将数据关联到提交它们的用户。只需将最高层的数据关联到用户，更低层的数据就会自动关联到用户。例如，在项目“学习笔记”中，应用程序的最高层数据是主题，而所有条目都与特定主题相关联。只要每个主题都归属于特定用户，就能确定数据库中每个条目的所有者。</p>
<p>下面来修改模型<code>Topic</code>
 ，在其中添加一个关联到用户的外键。这样做之后，必须对数据库进行迁移。最后，必须修改某些视图，使其只显示与当前登录的用户相关联的数据。</p>
<ol>
<li value="1">
<p>
<strong>修改模型<code>Topic</code>
</strong>
</p>
<p>对models.py的修改只涉及两行代码：</p>
<p>
<em>models.py</em>
</p>
<pre class="代码无行号"><code>from django.db import models
from django.contrib.auth.models import User
class Topic(models.Model):
   """用户学习的主题。"""
    text = models.CharField(max_length=200)
    date_added = models.DateTimeField(auto_now_add=True)
    owner = models.ForeignKey(User, on_delete=models.CASCADE)
    def __str__(self):
        """返回模型的字符串表示。"""
        return self.text
class Entry(models.Model):
  --snip--</code>
</pre>
<p>首先导入<code>django.contrib.auth</code>
 中的模型<code>User</code>
 ，然后在<code>Topic</code>
 中添加字段<code>owner</code>
 ，它建立到模型<code>User</code>
 的外键关系。用户被删除时，所有与之相关联的主题也会被删除。<br/>
 </p>
</li>
<li value="2">
<p>
<strong>确定当前有哪些用户</strong>
</p>
<p>迁移数据库时，Django将对数据库进行修改，使其能够存储主题和用户之间的关联。为执行迁移，Django需要知道该将各个既有主题关联到哪个用户。最简单的办法是，将既有主题都关联到同一个用户，如超级用户。为此，需要知道该用户的ID。</p>
<p>下面来查看已创建的所有用户的ID。为此，启动一个Django shell会话，并执行如下命令：</p>
<pre class="代码无行号"><code>   (ll_env)learning_log$ python manage.py shell
❶ &gt;&gt;&gt; from django.contrib.auth.models import User
❷ &gt;&gt;&gt; User.objects.all()
  &lt;QuerySet [&lt;User: ll_admin&gt;, &lt;User: eric&gt;, &lt;User: willie&gt;]&gt;
❸ &gt;&gt;&gt; for user in User.objects.all():
  ...     print(user.username, user.id)
  ...
  ll_admin 1
  eric 2
  willie 3
  &gt;&gt;&gt;</code>
</pre>
<p>在❶处，在shell会话中导入模型<code>User</code>
 。然后，查看到目前为止都创建了哪些用户（见❷）。输出中列出了三个用户：ll_admin、eric和willie。</p>
<p>在❸处，遍历用户列表并打印每位用户的用户名和ID。Django询问要将既有主题关联到哪个用户时，我们将指定其中一个ID值。<br/>
 </p>
</li>
<li value="3">
<p>
<strong>迁移数据库</strong>
</p>
<p>知道用户ID后，就可迁移数据库了。这样做时，Python将询问你是要暂时将模型<code>Topic</code>
 关联到特定用户，还是在文件models.py中指定默认用户。请选择第一个选项。</p>
<pre class="代码无行号"><code>❶ (ll_env)learning_log$ python manage.py makemigrations learning_logs
❷ You are trying to add a non-nullable field 'owner' to topic without a default;
  we can't do that (the database needs something to populate existing rows).
❸ Please select a fix:
    1) Provide a one-off default now (will be set on all existing rows with a
      null value for this column)
    2) Quit, and let me add a default in models.py
❹ Select an option: 1
❺ Please enter the default value now, as valid Python
  The datetime and django.utils.timezone modules are available, so you can do
  e.g. timezone.now
  Type 'exit' to exit this prompt
❻ &gt;&gt;&gt; 1
  Migrations for 'learning_logs':
    learning_logs/migrations/0003_topic_owner.py
  - Add field owner to topic
  (ll_env)learning_log$</code>
</pre>
<p>首先执行命令<code>makemigrations</code>
 （见❶）。在❷处的输出中，Django指出你试图给既有模型<code>Topic</code>
 添加一个必不可少（不可为空）的字段，而该字段没有默认值。在❸处，Django提供了两种选择：要么现在提供默认值，要么退出并在models.py中添加默认值。在❹处，我们选择了第一个选项，因此Django让我们输入默认值（见❺）。</p>
<p>为将所有既有主题都关联到管理用户ll_admin，我们输入用户ID值1（见❻）。可以使用已创建的任何用户的ID，而非必须是超级用户。接下来，Django使用这个值来迁移数据库，并生成了迁移文件0003_topic_owner.py，它在模型<code>Topic</code>
 中添加字段<code>owner</code>
 。</p>
<p>现在可以执行迁移了。为此，在活动状态的虚拟环境中执行如下命令：</p>
<pre class="代码无行号"><code>  (ll_env)learning_log$ python manage.py migrate
  Operations to perform:
    Apply all migrations: admin, auth, contenttypes, learning_logs, sessions
  Running migrations:
❶   Applying learning_logs.0003_topic_owner... OK
  (ll_env)learning_log$</code>
</pre>
<p>Django应用新的迁移，结果一切顺利（见❶）。</p>
<p>为验证迁移符合预期，可在shell会话中像下面这样做：</p>
<pre class="代码无行号"><code>❶ &gt;&gt;&gt; from learning_logs.models import Topic
❷ &gt;&gt;&gt; for topic in Topic.objects.all():
  ...     print(topic, topic.owner)
  ...
  Chess ll_admin
  Rock Climbing ll_admin
  &gt;&gt;&gt;</code>
</pre>
<p>我们从<code>learning_logs.models</code>
 中导入<code>Topic</code>
 （见❶），再遍历所有的既有主题，并打印每个主题及其所属的用户（见❷）。如你所见，现在每个主题都属于用户ll_admin。如果你在运行这些代码时出错，请尝试退出并重启shell。</p>
<blockquote>
<p>
<strong>注意</strong>
 　你可以重置数据库而不是迁移它，但如果这样做，既有的数据都将丢失。一种不错的做法是，学习如何在迁移数据库的同时确保用户数据的完整性。如果你确实想要一个全新的数据库，可执行命令<code>python manage.py flush</code>
 ，这将重建数据库的结构。如果这样做，就必须重新创建超级用户，且原来的所有数据都将丢失。</p>
</blockquote>
</li>
</ol>
<h3 id="nav_point_457">19.3.3　只允许用户访问自己的主题</h3>
<p>当前，不管以哪个用户的身份登录，都能够看到所有的主题。下面改变这一点，只向用户显示属于其自己的主题。</p>
<p>在views.py中，对函数<code>topics()</code>
 做如下修改：</p>
<p>
<em>views.py</em>
</p>
<pre class="代码无行号"><code>--snip--
@login_required
def topics(request):
    """显示所有的主题。"""
    topics = Topic.objects.filter(owner=request.user).order_by('date_added')
    context = {'topics': topics}
    return render(request, 'learning_logs/topics.html', context)
--snip--</code>
</pre>
<p>用户登录后，<code>request</code>
 对象将有一个user属性，其中存储了有关该用户的信息。查询<code>Topic.objects.filter(owner=request.user)</code>
 让Django只从数据库中获取<code>owner</code>
 属性为当前用户的<code>Topic</code>
 对象。由于没有修改主题的显示方式，无须对显示所有主题的页面的模板做任何修改。</p>
<p>要查看结果，以所有既有主题关联到的用户的身份登录，并访问显示所有主题的页面，你将看到所有的主题。然后，注销并以另一个用户的身份登录，该页面将不列出任何主题。</p>
<h3 id="nav_point_458">19.3.4　保护用户的主题</h3>
<p>我们还没有限制对显示单个主题的页面的访问，因此任何已登录的用户都可输入类似于http://localhost:8000/topics/1/的URL，来访问显示相应主题的页面。</p>
<p>你自己试一试就明白了。以拥有所有主题的用户的身份登录，访问特定的主题，并复制该页面的URL或将其中的ID记录下来。然后，注销并以另一个用户的身份登录，再输入显示前述主题的页面的URL。虽然你是作为另一个用户登录的，但依然能够查看该主题中的条目。</p>
<p>为修复这种问题，我们在视图函数<code>topic()</code>
 获取请求的条目前执行检查：</p>
<p>
<em>views.py</em>
</p>
<pre class="代码无行号"><code>  from django.shortcuts import render, redirect
  from django.contrib.auth.decorators import login_required
❶ from django.http import Http404
  --snip--
  @login_required
  def topic(request, topic_id):
      """显示单个主题及其所有的条目。"""
      topic = Topic.objects.get(id=topic_id)
      # 确认请求的主题属于当前用户。
❷     if topic.owner != request.user:
          raise Http404
      entries = topic.entry_set.order_by('-date_added')
      context = {'topic': topic, 'entries': entries}
      return render(request, 'learning_logs/topic.html', context)
  --snip--</code>
</pre>
<p>服务器上没有请求的资源时，标准的做法是返回404响应。这里导入了异常<code>Http404</code>
 （见❶），并在用户请求其不应查看的主题时引发这个异常。收到主题请求后，在渲染页面前检查该主题是否属于当前登录的用户。如果请求的主题不归当前用户所有，就引发<code>Http404</code>
 异常（见❷），让Django返回一个404错误页面。</p>
<p>现在，如果你试图查看其他用户的主题条目，将看到Django发送的消息Page Not Found。第20章将对这个项目进行配置，让用户看到更合适的错误页面。</p>
<h3 id="nav_point_459">19.3.5　保护页面<code>edit_entry</code>
</h3>
<p>页面<code>edit_entry</code>
 的URL形式为<code>http://localhost:8000/edit_entry/<i>entry_id</i>
 /</code>
 ，其中<code>
<i>entry_id</i>
</code>
 是一个数。下面来保护这种页面，禁止用户通过输入类似于前面的URL来访问其他用户的条目：</p>
<p>
<em>views.py</em>
</p>
<pre class="代码无行号"><code>--snip--
@login_required
def edit_entry(request, entry_id):
    """编辑既有条目。"""
    entry = Entry.objects.get(id=entry_id)
    topic = entry.topic
    if topic.owner != request.user:
        raise Http404
    if request.method != 'POST':
        --snip--</code>
</pre>
<p>我们首先获取指定的条目以及与之相关联的主题，再检查主题的所有者是否是当前登录的用户。如果不是，就引发<code>Http404</code>
 异常。</p>
<h3 id="nav_point_460">19.3.6　将新主题关联到当前用户</h3>
<p>当前，用于添加新主题的页面存在问题——没有将新主题关联到特定用户。如果你尝试添加新主题，将看到错误消息<code>IntegrityError</code>
 ，指出<code>learning_logs_topic.user_id</code>
 不能为<code>NULL</code>
 （<code>NOT NULL constraint failed: learning_logs_topic.owner_id</code>
 ）。Django的意思是说，创建新主题时，必须给<code>owner</code>
 字段指定值。</p>
<p>我们可通过<code>request</code>
 对象获悉当前用户，因此有一个修复该问题的简单方案。请添加下面的代码，将新主题关联到当前用户：</p>
<p>
<em>views.py</em>
</p>
<pre class="代码无行号"><code>  --snip--
  @login_required
  def new_topic(request):
      """添加新主题。"""
      if request.method != 'POST':
          # 没有提交的数据：创建一个空表单。
          form = TopicForm()
      else:
          # POST提交的数据：对数据进行处理。
          form = TopicForm(data=request.POST)
          if form.is_valid():
❶             new_topic = form.save(commit=False)
❷             new_topic.owner = request.user
❸             new_topic.save()
              return redirect('learning_logs:topics')
      # 显示一个空表单或指出表单无效。
      context = {'form': form}
      return render(request, 'learning_logs/new_topic.html', context)
  --snip--</code>
</pre>
<p>首先调用<code>form.save()</code>
 并传递实参<code>commit=False</code>
 （见❶），因为要先修改新主题，再将其保存到数据库。接下来，将新主题的<code>owner</code>
 属性设置为当前用户（见❷）。最后，对刚定义的主题实例调用<code>save()</code>
 （见❸）。现在，主题包含所有必不可少的数据，将被成功保存。</p>
<p>这个项目现在允许任何用户注册，而每个用户想添加多少新主题都可以。每个用户都只能访问自己的数据，无论是查看数据、输入新数据还是修改旧数据时都如此。</p>
<blockquote>
<p>
<strong>动手试一试</strong>
</p>
<p>
<strong>练习19-3：重构</strong>
 　在views.py中，我们在两个地方核实了主题关联到的用户为当前登录的用户。请将执行这种检查的代码放在函数<code>check_topic_owner()</code>
 中，并在这两个地方调用该函数。</p>
<p>
<strong>练习19-4：保护页面<code>new_entry</code>
</strong>
 　一个用户可在另一个用户的学习笔记中添加条目，方法是在URL中指定属于另一个用户的主题的ID。为防范这种攻击，请在保存新条目之前，核实它所属的主题归属于当前用户。</p>
<p>
<strong>练习19-5：受保护的博客</strong>
 　在你创建的项目Blog中，确保每篇博文都与特定用户相关联。确保任何用户都可访问所有的博文，但只有已登录的用户能够发表博文和编辑既有博文。在让用户编辑博文的视图中，在处理表单前确认用户编辑的是其自己发表的博文。</p>
</blockquote>
<h2 id="nav_point_461">19.4　小结</h2>
<p>在本章中，你学习了：如何使用表单来让用户添加新主题、添加新条目以及编辑既有条目；如何实现用户账户，让老用户能够登录和注销，并且使用Django提供的表单<code>UserCreationForm</code>
 让用户创建新账户。</p>
<p>建立简单的用户身份验证和注册系统后，你使用装饰器<code>@login_required</code>
 禁止未登录的用户访问特定页面。然后，你通过使用外键将数据关联到特定用户，还迁移了要求指定默认数据的数据库。</p>
<p>最后，你学习了如何修改视图函数，让用户只能看到属于自己的数据。你使用方法<code>filter()</code>
 来获取合适的数据，并学习了如何将被请求数据的所有者同当前登录的用户进行比较。</p>
<p>该让哪些数据可随便访问，又该对哪些数据进行保护呢？这可能并非总是那么显而易见，但通过不断地练习就能掌握这种技能。我们在本章中就该如何保护用户数据所做的决策表明，与人合作开发项目是个不错的主意：在有人对项目进行检查的情况下，更容易发现其薄弱环节。</p>
<p>至此，我们创建了一个功能齐备的项目，它运行在本地计算机上。在本书的最后一章，我们将设置这个项目的样式，使其更漂亮，还将把它部署到一台服务器上，让任何人都可通过互联网注册并创建账户。</p>
<br style="page-break-after:always"/>
<div></div>
]]></description>
    <pubDate>Fri, 31 Jul 2026 01:23:01 +0000</pubDate>
    <guid isPermaLink="false">19</guid>
</item>
<item>
    <title>Basic Python - 018 - 项目3 Web应用程序.epub</title>
    <description><![CDATA[<p style='color: #888;'>⏱ 预计阅读时间：145 分钟 (28858 字)</p><hr><h1 id="nav_point_415">项目3　Web应用程序</h1>
<br style="page-break-after:always"/>
<div></div>
<h1 id="nav_point_416">第 18 章　从Django入手</h1>
<blockquote>
<p><img alt="" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/18_Image00001.gif" style="width: 16%" width="16%"/>
 　在幕后，当今的网站实际上都是富应用程序（rich application），就像成熟的桌面应用程序一样。Python提供了一组开发Web应用程序的卓越工具。Django是一个<strong>Web框架</strong>
 ，即一套旨在帮助开发交互式网站的工具。本章将介绍如何使用Django来开发一个名为“学习笔记”的项目，这是一个在线日志系统，让你能够记录学习到的有关特定主题的知识。</p>
</blockquote>
<p>我们将为这个项目制定规范，然后为应用程序使用的数据定义模型。我们将使用Django的管理系统来输入一些初始数据，再学习编写视图和模板，让Django能够为我们的网站创建页面。</p>
<p>Django能够响应页面请求，还让你能够更轻松地读写数据库、管理用户，等等。第19章和第20章将改进“学习笔记”项目，再将其部署到活动的服务器，让你和朋友都能够使用它。</p>
<h2 id="nav_point_417">18.1　建立项目</h2>
<p>建立项目时，首先需要以规范的方式对项目进行描述，再建立虚拟环境，以便在其中创建项目。</p>
<h3 id="nav_point_418">18.1.1　制定规范</h3>
<p>完整的规范详细说明了项目的目标，阐述了项目的功能，并讨论了项目的外观和用户界面。与任何良好的项目规划和商业计划书一样，规范应突出重点，帮助避免项目偏离轨道。这里不会制定完整的项目规划，只列出一些明确的目标，以突出开发的重点。我们制定的规范如下：</p>
<blockquote>
<p>我们要编写一个名为“学习笔记”的Web应用程序，让用户能够记录感兴趣的主题，并在学习每个主题的过程中添加日志条目。“学习笔记”的主页对这个网站进行描述，并邀请用户注册或登录。用户登录后，可以创建新主题、添加新条目以及阅读既有的条目。</p>
</blockquote>
<p>学习新的主题时，记录学到的知识可帮助跟踪和复习这些知识。优秀的应用程序让这个记录过程简单易行。</p>
<h3 id="nav_point_419">18.1.2　建立虚拟环境</h3>
<p>要使用Django，首先需要建立一个虚拟工作环境。<strong>虚拟环境</strong>
 是系统的一个位置，可在其中安装包，并将之与其他Python包隔离。将项目的库与其他项目分离是有益的，并且为了在第20章将“学习笔记”部署到服务器，这也是必需的。</p>
<p>为项目新建一个目录，将其命名为learning_log，再在终端中切换到这个目录，并执行如下命令创建一个虚拟环境：</p>
<pre class="代码无行号"><code>learning_log$ python -m venv ll_env
learning_log$</code>
</pre>
<p>这里运行了模块<code>venv</code>
 ，并使用它创建了一个名为ll_env的虚拟环境。（请注意，ll_env的开头是两个小写字母l，而不是数字1。）如果你运行程序或安装包时使用的是命令<strong>
<code>python3</code>
</strong>
 ，这里也务必使用同样的命令。</p>
<h3 id="nav_point_420">18.1.3　激活虚拟环境</h3>
<p>现在需要使用下面的命令激活虚拟环境：</p>
<pre class="代码无行号"><code>  learning_log$ source ll_env/bin/activate
❶ (ll_env)learning_log$</code>
</pre>
<p>这个命令运行ll_env/bin中的脚本activate。环境处于活动状态时，环境名将包含在圆括号内，如❶处所示。在这种情况下，你可以在环境中安装包，并使用已安装的包。在ll_env中安装的包仅在该环境处于活动状态时才可用。</p>
<blockquote>
<p>
<strong>注意</strong>
 　如果你使用的是Windows系统，请使用命令<code>ll_env\Scripts\activate</code>
 （不包含<code>source</code>
 ）来激活这个虚拟环境。如果你使用的是PowerShell，可能需要将<code>Activate</code>
 的首字母大写。</p>
</blockquote>
<p>要停止使用虚拟环境，可执行命令<strong>
<code>deactivate</code>
</strong>
 ：</p>
<pre class="代码无行号"><code>(ll_env)learning_log$ deactivate
learning_log$</code>
</pre>
<p>如果关闭运行虚拟环境的终端，虚拟环境也将不再处于活动状态。</p>
<h3 id="nav_point_421">18.1.4　安装Django</h3>
<p>激活虚拟环境后，执行如下命令安装Django：</p>
<pre class="代码无行号"><code> (ll_env)learning_log$ pip install django
Collecting django
--snip--
Installing collected packages: pytz, django
Successfully installed django-2.2.0 pytz-2018.9 sqlparse-0.2.4
(ll_env)learning_log$</code>
</pre>
<p>由于是在虚拟环境（独立的环境）中工作，在各种系统中安装Django的命令都相同：不需要指定标志<code>--user</code>
 ，也不需要使用像<code>python -m pip install package_name</code>
 这样较长的命令。</p>
<p>别忘了，Django仅在虚拟环境ll_env处于活动状态时才可用。</p>
<blockquote>
<p>
<strong>注意</strong>
 　每隔大约8个月，Django新版本就会发布，因此在你安装Django时，看到的可能是更新的版本。即便你使用的是更新的Django版本，这个项目也可行。如果要使用这里所示的Django版本，请使用命令<code>pip install django==2.2.*</code>
 安装Django 2.2的最新版本。如果你在使用更新的版本时遇到麻烦，请参阅本书的在线配套资源。</p>
</blockquote>
<h3 id="nav_point_422">18.1.5　在Django中创建项目</h3>
<p>在虚拟环境依然处于活动状态的情况下（ll_env包含在圆括号内），执行如下命令新建一个项目：</p>
<pre class="代码无行号"><code>❶ (ll_env)learning_log$ django-admin startproject learning_log .
❷ (ll_env)learning_log$ ls
  learning_log ll_env manage.py
❸ (ll_env)learning_log$ ls learning_log
  __init__.py settings.py urls.py wsgi.py</code>
</pre>
<p>❶处的命令让Django新建一个名为learning_log的项目。这个命令末尾的句点让新项目使用合适的目录结构，这样开发完成后可轻松地将应用程序部署到服务器。</p>
<blockquote>
<p>
<strong>注意</strong>
 　千万别忘了这个句点，否则部署应用程序时将遭遇一些配置问题。如果忘记了这个句点，要删除已创建的文件和文件夹（ll_env除外），再重新运行这个命令。</p>
</blockquote>
<p>在❷处，运行命令<code>ls</code>
 （在Windows系统上为<code>dir</code>
 ），结果表明Django新建了一个名为learning_log的目录，还创建了文件manage.py。后者是一个简单的程序，接受命令并将其交给Django的相关部分运行。我们将使用这些命令来管理使用数据库和运行服务器等任务。</p>
<p>目录learning_log包含4个文件（见❸），最重要的是settings.py、urls.py和wsgi.py。文件settings.py指定Django如何与系统交互以及如何管理项目。在开发项目的过程中，我们将修改其中一些设置，并添加一些设置。文件urls.py告诉Django，应创建哪些页面来响应浏览器请求。文件wsgi.py帮助Django提供它创建的文件，这个文件名是<strong>Web服务器网关接口</strong>
 （Web server gateway interface）的首字母缩写。</p>
<h3 id="nav_point_423">18.1.6　创建数据库</h3>
<p>Django将大部分与项目相关的信息存储在数据库中，因此需要创建一个供Django使用的数据库。为给项目“学习笔记”创建数据库，请在虚拟环境处于活动状态的情况下执行下面的命令：</p>
<pre class="代码无行号"><code>  (ll_env)learning_log$ python manage.py migrate
❶ Operations to perform:
    Apply all migrations: admin, auth, contenttypes, sessions
  Running migrations:
    Applying contenttypes.0001_initial... OK
    Applying auth.0001_initial... OK
    --snip--
    Applying sessions.0001_initial... OK
❷ (ll_env)learning_log$ ls
  db.sqlite3 learning_log ll_env manage.py</code>
</pre>
<p>我们将修改数据库称为<strong>迁移</strong>
 （migrate）数据库。首次执行命令<code>migrate</code>
 时，将让Django确保数据库与项目的当前状态匹配。在使用SQLite（后面将详细介绍）的新项目中首次执行这个命令时，Django将新建一个数据库。在❶处，Django指出它将准备好数据库，用于存储执行管理和身份验证任务所需的信息。</p>
<p>在❷处运行命令<code>ls</code>
 ，其输出表明Django又创建了一个文件db.sqlite3。SQLite是一种使用单个文件的数据库，是编写简单应用程序的理想选择，因为它让你不用太关注数据库管理的问题。</p>
<blockquote>
<p>
<strong>注意</strong>
 　在虚拟环境中运行manage.py时，务必使用命令<code>python</code>
 ，即便你在运行其他程序时使用的是另外的命令，如<code>python3</code>
 。在虚拟环境中，命令<code>python</code>
 指的是在虚拟环境中安装的Python版本。</p>
</blockquote>
<h3 id="nav_point_424">18.1.7　查看项目</h3>
<p>下面来核实Django正确地创建了项目。为此，可使用命令<code>runserver</code>
 查看项目的状态，如下所示：</p>
<pre class="代码无行号"><code>  (ll_env)learning_log$ python manage.py runserver
  Watchman unavailable: pywatchman not installed.
  Watching for file changes with StatReloader
  Performing system checks...
❶ System check identified no issues (0 silenced).
  February 18, 2019 - 16:26:07
❷ Django version 2.2.0, using settings 'learning_log.settings'
❸ Starting development server at http://127.0.0.1:8000/
  Quit the server with CONTROL-C.</code>
</pre>
<p>Django启动了一个名为development server的服务器，让你能够查看系统中的项目，了解其工作情况。如果你在浏览器中输入URL以请求页面，该Django服务器将进行响应：生成合适的页面，并将其发送给浏览器。</p>
<p>Django在❶处通过检查确认正确地创建了项目，在❷处指出使用的Django版本以及当前使用的设置文件的名称，并在❸处指出项目的URL。URL http://127.0.0.1:8000/表明项目将在你的计算机（即localhost）的端口8000上侦听请求。localhost指的是只处理当前系统发出的请求，而不允许其他任何人查看你正在开发的页面的服务器。</p>
<p>现在打开一款Web浏览器，并输入URL http://localhost:8000/（如果这不管用，请输入http://127.0.0.1:8000/）。你将看到类似于图18-1所示的页面。这个页面是Django创建的，让你知道到目前为止一切正常。现在暂时不要关闭这个服务器，等你要关闭这个服务器时，可切换到执行命令<code>runserver</code>
 时所在的终端窗口，再按Ctrl + C。</p>
<p class="图"><img alt="{%}" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/18_Image00047.gif"/>
</p>
<p>
<strong>图18-1　到目前为止一切正常</strong>
</p>
<blockquote>
<p>
<strong>注意</strong>
 　如果出现错误消息That port is already in use（指定端口被占用），请执行命令<code>python manage.py runserver 8001</code>
 ，让Diango使用另一个端口。如果这个端口也不可用，请不断执行上述命令，并逐渐增大其中的端口号，直到找到可用的端口。</p>
</blockquote>
<p>　</p>
<blockquote>
<p>
<strong>动手试一试</strong>
</p>
<p>
<strong>练习18-1：新项目</strong>
 　为深入了解Django都做了些什么，可创建两个空项目，看看Django创建了什么。新建一个文件夹，并给它指定简单的名称，如snap_gram或insta_chat（不要在目录learning_log中新建该文件夹）。在终端中切换到该文件夹，并创建一个虚拟环境。在这个虚拟环境中，安装Django，并执行命令<code>django-admin.py startproject snap_gram .</code>
 （千万不要忘了这个命令末尾的句点）。</p>
<p>看看这个命令创建了哪些文件和文件夹，并与项目“学习笔记”包含的文件和文件夹进行比较。这样多做几次，直到你对Django新建项目时创建的东西了如指掌。然后，将项目目录删除（如果你想这样做的话）。</p>
</blockquote>
<h2 id="nav_point_425">18.2　创建应用程序</h2>
<p>Django<strong>项目</strong>
 由一系列应用程序组成，它们协同工作让项目成为一个整体。本章只创建一个应用程序，它将完成项目的大部分工作。第19章将添加一个管理用户账户的应用程序。</p>
<p>当前，在前面打开的终端窗口中应该还运行着<code>runserver</code>
 。请再打开一个终端窗口（或标签页），并切换到manage.py所在的目录。激活虚拟环境，再执行命令<code>startapp</code>
 ：</p>
<pre class="代码无行号"><code>  learning_log$ source ll_env/bin/activate
  (ll_env)learning_log$ python manage.py startapp learning_logs
❶ (ll_env)learning_log$ ls
  db.sqlite3 learning_log learning_logs ll_env manage.py
❷ (ll_env)learning_log$ ls learning_logs/
  __init__.py admin.py apps.py migrations models.py tests.py views.py</code>
</pre>
<p>命令<code>startapp <i>appname</i>
</code>
 让Django搭建创建应用程序所需的基础设施。如果现在查看项目目录，将看到其中新增了文件夹learning_logs（见❶）。打开这个文件夹，看看Django都创建了什么（见❷），其中最重要的文件是models.py、admin.py和views.py。我们将使用models.py来定义要在应用程序中管理的数据，稍后再介绍admin.py和views.py。</p>
<h3 id="nav_point_426">18.2.1　定义模型</h3>
<p>我们来想想涉及的数据。每位用户都需要在学习笔记中创建很多主题。用户输入的每个条目都与特定主题相关联，这些条目将以文本的方式显示。我们还需要存储每个条目的时间戳，以便告诉用户各个条目都是什么时候创建的。</p>
<p>打开文件models.py，看看它当前包含哪些内容：</p>
<p>
<em>models.py</em>
</p>
<pre class="代码无行号"><code>from django.db import models
# 在这里创建模型。</code>
</pre>
<p>这里导入了模块<code>models</code>
 ，并让我们创建自己的模型。模型告诉Django如何处理应用程序中存储的数据。在代码层面，模型就是一个类，就像前面讨论的每个类一样，包含属性和方法。下面是表示用户将存储的主题的模型：</p>
<pre class="代码无行号"><code>  from django.db import models
  class Topic(models.Model):
      """用户学习的主题。"""
❶     text = models.CharField(max_length=200)
❷     date_added = models.DateTimeField(auto_now_add=True)
❸     def __str__(self):
          """返回模型的字符串表示。"""
          return self.text</code>
</pre>
<p>我们创建了一个名为<code>Topic</code>
 的类，它继承<code>Model</code>
 ，即Django中定义了模型基本功能的类。我们给<code>Topic</code>
 类添加了两个属性：<code>text</code>
 和<code>date_added</code>
 。</p>
<p>属性<code>text</code>
 是一个<code>CharField</code>
 ——由字符组成的数据，即文本（见❶）。需要存储少量文本，如名称、标题或城市时，可使用<code>CharField</code>
 。定义<code>CharField</code>
 属性时，必须告诉Django该在数据库中预留多少空间。这里将<code>max_length</code>
 设置成了200（即200字符），这对存储大多数主题名来说足够了。</p>
<p>属性<code>date_added</code>
 是一个<code>DateTimeField</code>
 ——记录日期和时间的数据（见❷）。我们传递了实参<code>auto_now_add=True</code>
 ，每当用户创建新主题时，Django都会将这个属性自动设置为当前日期和时间。</p>
<blockquote>
<p>
<strong>注意</strong>
 　要获悉可在模型中使用的各种字段，请参阅<em>Django Model Field Reference</em>
 。就当前而言，你无须全面了解其中的所有内容，但自己开发应用程序时，这些内容将提供极大的帮助。</p>
</blockquote>
<p>需要告诉Django，默认使用哪个属性来显示有关主题的信息。Django调用方法<code>__str__()</code>
 来显示模型的简单表示。这里编写了方法<code>__str__()</code>
 ，它返回存储在属性<code>text</code>
 中的字符串（见❸）。</p>
<h3 id="nav_point_427">18.2.2　激活模型</h3>
<p>要使用这些模型，必须让Django将前述应用程序包含到项目中。为此，打开settings.py（它位于目录learning_log/learning_log中），其中有个片段告诉Django哪些应用程序被安装到了项目中并将协同工作：</p>
<p>
<em>settings.py</em>
</p>
<pre class="代码无行号"><code>--snip--
INSTALLED_APPS = [
    'django.contrib.admin',
    'django.contrib.auth',
    'django.contrib.contenttypes',
    'django.contrib.sessions',
    'django.contrib.messages',
    'django.contrib.staticfiles',
]
--snip--</code>
</pre>
<p>请将<code>INSTALLED_APPS</code>
 修改成下面这样，将前面的应用程序添加到这个列表中：</p>
<pre class="代码无行号"><code>--snip--
INSTALLED_APPS = [
    # 我的应用程序
    'learning_logs',
    # 默认添加的应用程序
     'django.contrib.admin',
    --snip--
]
--snip--</code>
</pre>
<p>通过将应用程序编组，在项目不断增大，包含更多的应用程序时，有助于对应用程序进行跟踪。这里新建了一个名为“我的应用程序”的片段，当前它只包含应用程序<code>learning_logs</code>
 。务必将自己创建的应用程序放在默认应用程序前面，这样能够覆盖默认应用程序的行为。</p>
<p>接下来，需要让Django修改数据库，使其能够存储与模型<code>Topic</code>
 相关的信息。为此，在终端窗口中执行下面的命令：</p>
<pre class="代码无行号"><code>(ll_env)learning_log$ python manage.py makemigrations learning_logs
Migrations for 'learning_logs':
  learning_logs/migrations/0001_initial.py
    - Create model Topic
(ll_env)learning_log$</code>
</pre>
<p>命令<code>makemigrations</code>
 让Django确定该如何修改数据库，使其能够存储与前面定义的新模型相关联的数据。输出表明Django创建了一个名为0001_initial.py的迁移文件，这个文件将在数据库中为模型<code>Topic</code>
 创建一个表。</p>
<p>下面应用这种迁移，让Django替我们修改数据库：</p>
<pre class="代码无行号"><code>  (ll_env)learning_log$ python manage.py migrate
  Operations to perform:
    Apply all migrations: admin, auth, contenttypes, learning_logs, sessions
  Running migrations:
❶   Applying learning_logs.0001_initial... OK</code>
</pre>
<p>这个命令的大部分输出与首次执行命令<code>migrate</code>
 的输出相同。需要检查的是❶处的输出行。在这里，Django指出为<code>learning_logs</code>
 应用迁移时一切正常。</p>
<p>每当需要修改“学习笔记”管理的数据时，都采取如下三个步骤：修改models.py，对<code>learning_logs</code>
 调用<code>makemigrations</code>
 ，以及让Django迁移项目。</p>
<h3 id="nav_point_428">18.2.3　Django管理网站</h3>
<p>Django提供的<strong>管理网站</strong>
 （admin site）让你能够轻松地处理模型。网站管理员可以使用管理网站，但普通用户不能使用。本节将建立管理网站，并通过它使用模型<code>Topic</code>
 来添加一些主题。</p>
<ol>
<li value="1">
<p>
<strong>创建超级用户</strong>
</p>
<p>Django允许创建具备所有权限的用户，即<strong>超级用户</strong>
 。<strong>权限</strong>
 决定了用户可执行的操作。最严格的权限设置只允许用户阅读网站的公开信息。注册用户通常可阅读自己的私有数据，还可查看一些只有会员才能查看的信息。为有效地管理Web应用程序，网站所有者通常需要访问网站存储的所有信息。优秀的管理员会小心对待用户的敏感信息，因为用户极其信任自己访问的应用程序。</p>
<p>为在Django中创建超级用户，请执行下面的命令并按提示做：</p>
<pre class="代码无行号"><code>  (ll_env)learning_log$ python manage.py createsuperuser
❶ Username (leave blank to use 'eric'): ll_admin
❷ Email address:
❸ Password:
  Password (again):
  Superuser created successfully.
  (ll_env)learning_log$</code>
</pre>
<p>你执行命令<code>createsuperuser</code>
 时，Django提示输入超级用户的用户名（见❶）。这里输入的是ll_admin，但可输入任何用户名。如果你愿意，可以输入电子邮箱地址，也可让这个字段为空（见❷）。需要输入密码两次（见❸）。</p>
<blockquote>
<p>
<strong>注意</strong>
 　一些敏感信息可能会向网站管理员隐藏。例如，Django并不存储你输入的密码，而是存储从该密码派生出来的一个字符串，称为<strong>散列值</strong>
 。每当你输入密码时，Django都计算其散列值，并将结果与存储的散列值进行比较。如果这两个散列值相同，你就通过了身份验证。由于存储的是散列值，即便黑客获得了网站数据库的访问权，也只能获取其中存储的散列值，无法获得密码。在网站配置正确的情况下，几乎无法根据散列值推导出原始密码。</p>
</blockquote>
<p> </p>
</li>
<li value="2">
<p>
<strong>向管理网站注册模型</strong>
</p>
<p>Django自动在管理网站中添加了一些模型，如<code>User</code>
 和<code>Group</code>
 ，但对于我们创建的模型，必须手工进行注册。</p>
<p>我们创建应用程序<code>learning_logs</code>
 时，Django在models.py所在的目录中创建了一个名为admin.py的文件：</p>
<p>
<em>admin.py</em>
</p>
<pre class="代码无行号"><code>from django.contrib import admin
# 在这里注册你的模型。</code>
</pre>
<p>为向管理网站注册<code>Topic</code>
 ，请输入下面的代码：</p>
<pre class="代码无行号"><code>  from django.contrib import admin
❶ from .models import Topic
❷ admin.site.register(Topic)</code>
</pre>
<p>这些代码首先导入要注册的模型<code>Topic</code>
 （见❶）。<code>models</code>
 前面的句点让Django在admin.py所在的目录中查找models.py。<code>admin.site.register()</code>
 让Django通过管理网站管理模型（见❷）。</p>
<p>现在，使用超级用户账户访问管理网站：访问http://localhost:8000/admin/，并输入刚创建的超级用户的用户名和密码。你将看到类似于图18-2所示的屏幕。这个页面让你能够添加和修改用户和用户组，还可管理与刚才定义的模型<code>Topic</code>
 相关的数据。</p>
<p class="p-img"><img alt="{%}" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/18_Image00048.gif"/>
</p>
<p>
<strong>图18-2　包含模型Topic的管理网站</strong>
</p>
<blockquote>
<p>
<strong>注意</strong>
 　如果在浏览器中看到一条消息，指出访问的网页不可用，请确认在终端窗口中运行着Django服务器。如果没有，请激活虚拟环境，并执行命令<code>python manage.py runserver</code>
 。在开发过程中，如果无法通过浏览器访问项目，首先应采取的故障排除措施是，关闭所有打开的终端，再打开终端并执行命令<code>runserver</code>
 。</p>
</blockquote>
<p> </p>
</li>
<li value="3">
<p>
<strong>添加主题</strong>
</p>
<p>向管理网站注册<code>Topic</code>
 后，我们来添加第一个主题。为此，单击Topics进入主题页面，它几乎是空的，因为还没有添加任何主题。单击Add，将出现一个用于添加新主题的表单。在第一个方框中输入<code>Chess</code>
 ，再单击Save回到主题管理页面，其中包含刚创建的主题。</p>
<p>下面再创建一个主题，以便有更多的数据可供使用。再次单击Add，并输入Rock Climbing，然后单击Save回到主题管理页面。现在，你可以看到其中包含了主题Chess和<code>Rock Climbing</code>
 。</p>
</li>
</ol>
<h3 id="nav_point_429">18.2.4　定义模型<code>Entry</code>
</h3>
<p>要记录学到的国际象棋和攀岩知识，用户必须能够在学习笔记中添加条目。为此，需要定义相关的模型。每个条目都与特定主题相关联，这种关系称为<strong>多对一关系</strong>
 ，即多个条目可关联到同一个主题。</p>
<p>下面是模型<code>Entry</code>
 的代码，请将这些代码放在文件models.py中：</p>
<p>
<em>models.py</em>
</p>
<pre class="代码无行号"><code>  from django.db import models
  class Topic(models.Model):
      --snip--
❶ class Entry(models.Model):
      """学到的有关某个主题的具体知识。"""
❷     topic = models.ForeignKey(Topic, on_delete=models.CASCADE)
❸     text = models.TextField()
      date_added = models.DateTimeField(auto_now_add=True)
❹     class Meta:
          verbose_name_plural = 'entries'
      def __str__(self):
          """"""返回模型的字符串表示。"""
❺         return f"{self.text[:50]}..."</code>
</pre>
<p>像<code>Topic</code>
 一样，<code>Entry</code>
 也继承了Django基类<code>Model</code>
 （见❶）。第一个属性<code>topic</code>
 是个<code>ForeignKey</code>
 实例（见❷）。<strong>外键</strong>
 （foreign key）是一个数据库术语，它指向数据库中的另一条记录，这里是将每个条目关联到特定主题。创建每个主题时，都分配了一个键（ID）。需要在两项数据之间建立联系时，Django使用与每项信息相关联的键。我们稍后将根据这些联系获取与特定主题相关联的所有条目。实参<code>on_delete=models.CASCADE</code>
 让Django在删除主题的同时删除所有与之相关联的条目，这称为<strong>级联删除</strong>
 （cascading delete）。</p>
<p>接下来是属性<code>text</code>
 ，它是一个<code>TextField</code>
 实例（见❸）。这种字段的长度不受限制，因为我们不想限制条目的长度。属性<code>date_added</code>
 让我们能够按创建顺序呈现条目，并在每个条目旁边放置时间戳。</p>
<p>在❹处，我们在<code>Entry</code>
 类中嵌套了<code>Meta</code>
 类。<code>Meta</code>
 存储用于管理模型的额外信息。在这里，它让我们能够设置一个特殊属性，让Django在需要时使用<code>Entries</code>
 来表示多个条目。如果没有这个类，Django将使用<code>Entrys</code>
 来表示多个条目。</p>
<p>方法<code>__str__()</code>
 告诉Django，呈现条目时应显示哪些信息。条目包含的文本可能很长，因此让Django只显示<code>text</code>
 的前50字符（见❺）。我们还添加了一个省略号，指出显示的并非整个条目。</p>
<h3 id="nav_point_430">18.2.5　迁移模型<code>Entry</code>
</h3>
<p>添加新模型后，需要再次迁移数据库。你将慢慢地对这个过程了如指掌：修改models.py，执行命令<code>python manage.py makemigrations <i>app_name</i>
</code>
 ，再执行命令<code>python manage.py migrate</code>
 。</p>
<p>请使用如下命令迁移数据库并查看输出：</p>
<pre class="代码无行号"><code>  (ll_env)learning_log$ python manage.py makemigrations learning_logs
  Migrations for 'learning_logs':
❶   learning_logs/migrations/0002_entry.py
      - Create model Entry
  (ll_env)learning_log$ python manage.py migrate
  Operations to perform:
    --snip--
❷   Applying learning_logs.0002_entry... OK</code>
</pre>
<p>生成了一个新的迁移文件0002_entry.py，它告诉Django如何修改数据库，使其能够存储与模型<code>Entry</code>
 相关的信息（见❶）。在❷处执行命令<code>migrate</code>
 ，我们发现Django应用了该迁移且一切顺利。</p>
<h3 id="nav_point_431">18.2.6　向管理网站注册<code>Entry</code>
</h3>
<p>我们还需要注册模型<code>Entry</code>
 。为此，需要将admin.py修改成类似于下面这样：</p>
<p>
<em>admin.py</em>
</p>
<pre class="代码无行号"><code>from django.contrib import admin
from .models import Topic, Entry
admin.site.register(Topic)
admin.site.register(Entry)</code>
</pre>
<p>返回到http://localhost/admin/，你将看到Learning_Logs下列出了Entries。单击Entries的Add链接，或者单击Entries再选择Add entry，将看到一个下拉列表，供你选择要为哪个主题创建条目，以及一个用于输入条目的文本框。从下拉列表中选择Chess，并添加一个条目。下面是我添加的第一个条目。</p>
<blockquote>
<p>The opening is the first part of the game, roughly the first ten moves or so. In the opening, it's a good idea to do three things— bring out your bishops and knights, try to control the center of the board, and castle your king.（国际象棋的第一个阶段是开局，大致是前10步左右。在开局阶段，最好做三件事情：将象和马调出来，努力控制棋盘的中间区域，以及用车将王护住。）</p>
<p>Of course, these are just guidelines. It will be important to learn when to follow these guidelines and when to disregard these suggestions.（当然，这些只是指导原则。学习什么情况下遵守这些原则、什么情况下不用遵守很重要。）</p>
</blockquote>
<p>当你单击Save时，将返回到主条目管理页面。在这里，你将发现使用<code>text[:50]</code>
 作为条目的字符串表示的好处：在管理界面中只显示了条目的开头部分而不是其所有文本，这使得管理多个条目容易得多。</p>
<p>再来创建一个国际象棋条目，并创建一个攀岩条目，以提供一些初始数据。下面是第二个国际象棋条目。</p>
<blockquote>
<p>In the opening phase of the game, it's important to bring out your bishops and knights. These pieces are powerful and maneuverable enough to play a significant role in the beginning moves of a game.（在国际象棋的开局阶段，将象和马调出来很重要。这些棋子威力大，机动性强，在开局阶段扮演着重要角色。）</p>
</blockquote>
<p>下面是第一个攀岩条目。</p>
<blockquote>
<p>One of the most important concepts in climbing is to keep your weight on your feet as much as possible. There's a myth that climbers can hang all day on their arms. In reality, good climbers have practiced specific ways of keeping their weight over their feet whenever possible.（最重要的攀岩概念之一是尽可能让双脚承受体重。有人误认为攀岩者能依靠手臂的力量坚持一整天。实际上，优秀的攀岩者都经过专门训练，能够尽可能让双脚承受体重。）</p>
</blockquote>
<p>接着往下开发“学习笔记”时，这三个条目提供了可供使用的数据。</p>
<h3 id="nav_point_432">18.2.7　Django shell</h3>
<p>输入一些数据后，就可通过交互式终端会话以编程方式查看这些数据了。这种交互式环境称为Django <strong>shell</strong>
 ，是测试项目和排除故障的理想之地。下面是一个交互式shell会话示例：</p>
<pre class="代码无行号"><code>  (ll_env)learning_log$ python manage.py shell
❶ &gt;&gt;&gt; from learning_logs.models import Topic
  &gt;&gt;&gt; Topic.objects.all()
  &lt;QuerySet [&lt;Topic: Chess&gt;, &lt;Topic: Rock Climbing&gt;]&gt;</code>
</pre>
<p>在活动状态的虚拟环境中执行时，命令<code>python manage.py shell</code>
 启动Python解释器，让你能够探索存储在项目数据库中的数据。这里导入了模块<code>learning_logs.models</code>
 中的模型<code>Topic</code>
 （见❶），再使用方法<code>Topic.objects.all()</code>
 获取模型<code>Topic</code>
 的所有实例，这将返回一个称为<strong>查询集</strong>
 （queryset）的列表。</p>
<p>可以像遍历列表一样遍历查询集。下面演示了如何查看分配给每个主题对象的ID：</p>
<pre class="代码无行号"><code>&gt;&gt;&gt; topics = Topic.objects.all()
&gt;&gt;&gt; for topic in topics:
...   print(topic.id, topic)
...
1 Chess
2 Rock Climbing</code>
</pre>
<p>将返回的查询集存储在<code>topics</code>
 中，再打印每个主题的<code>id</code>
 属性和字符串表示。从输出可知，主题Chess的ID为1，而Rock Climbing的ID为2。</p>
<p>知道主题对象的ID后，就可使用方法<code>Topic.objects.get()</code>
 获取该对象并查看其属性。下面来看看主题Chess的属性<code>text</code>
 和<code>date_added</code>
 的值：</p>
<pre class="代码无行号"><code>&gt;&gt;&gt; t = Topic.objects.get(id=1)
&gt;&gt;&gt; t.text
'Chess'
&gt;&gt;&gt; t.date_added
datetime.datetime(2019, 2, 19, 1, 55, 31, 98500, tzinfo=&lt;UTC&gt;)</code>
</pre>
<p>我们还可以查看与主题相关联的条目。前面给模型<code>Entry</code>
 定义了属性<code>topic</code>
 。这是一个<code>ForeignKey</code>
 ，将条目与主题关联起来。利用这种关联，Django能够获取与特定主题相关联的所有条目，如下所示：</p>
<pre class="代码无行号"><code>❶ &gt;&gt;&gt; t.entry_set.all()
  &lt;QuerySet [&lt;Entry: The opening is the first part of the game, roughly...&gt;,
  &lt;Entry:
  In the opening phase of the game, it's important t...&gt;]&gt;</code>
</pre>
<p>要通过外键关系获取数据，可使用相关模型的小写名称、下划线和单词<code>set</code>
 （见❶）。例如，假设有模型<code>Pizza</code>
 和<code>Topping</code>
 ，而<code>Topping</code>
 通过一个外键关联到<code>Pizza</code>
 。如果有一个名为<code>my_pizza</code>
 的<code>Pizza</code>
 对象，就可使用代码<code>my_pizza.topping_set.all()</code>
 来获取这张比萨的所有配料。</p>
<p>编写用户可请求的页面时，我们将使用这种语法。确认代码能获取所需的数据时，shell很有帮助。如果代码在shell中的行为符合预期，那么它们在项目文件中也能正确地工作。如果代码引发了错误或获取的数据不符合预期，那么在简单的shell环境中排除故障要比在生成页面的文件中排除故障容易得多。我们不会太多地使用shell，但应继续使用它来熟悉对存储在项目中的数据进行访问的Django语法。</p>
<blockquote>
<p>
<strong>注意</strong>
 　每次修改模型后，都需要重启shell，这样才能看到修改的效果。要退出shell会话，可按Ctr + D。如果你使用的是Windows系统，应按Ctr + Z，再按回车键。</p>
</blockquote>
<p>　</p>
<blockquote>
<p>
<strong>动手试一试</strong>
</p>
<p>
<strong>练习18-2：简短的条目</strong>
 　当前，Django在管理网站或shell中显示<code>Entry</code>
 实例时，模型<code>Entry</code>
 的方法<code>__str__()</code>
 都在其末尾加上省略号。请在方法<code>__str__()</code>
 中添加一条<code>if</code>
 语句，以便仅在条目长度超过50字符时才添加省略号。使用管理网站添加一个不超过50字符的条目，并核实显示它时没有省略号。</p>
<p>
<strong>练习18-3：Django API</strong>
 　当你编写访问项目中数据的代码时，实际上编写的是查询。请浏览Django网站中有关如何查询数据的文档<em>Making queries</em>
 ，其中大部分内容是你不熟悉的，但等你自己开发项目时，这些内容会很有用。</p>
<p>
<strong>练习18-4：比萨店</strong>
 　新建一个名为Pizzeria的项目，并在其中添加一个名为<code>pizzas</code>
 的应用程序。定义一个名为<code>Pizza</code>
 的模型，它包含字段<code>name</code>
 ，用于存储比萨名称，如<code>Hawaiian</code>
 和<code>Meat Lovers</code>
 。定义一个名为<code>Topping</code>
 的模型，它包含字段<code>pizza</code>
 和<code>name</code>
 ，其中字段<code>pizza</code>
 是一个关联到<code>Pizza</code>
 的外键，而字段<code>name</code>
 用于存储配料，如<code>pineapple</code>
 、<code>Canadian bacon</code>
 和<code>sausage</code>
 。</p>
<p>向管理网站注册这两个模型，并使用管理网站输入一些比萨名和配料。使用shell来查看你输入的数据。</p>
</blockquote>
<h2 id="nav_point_433">18.3　创建页面：学习笔记主页</h2>
<p>使用Django创建页面的过程分三个阶段：定义URL，编写视图和编写模板。按什么顺序完成这三个阶段无关紧要，但在本项目中，总是先定义URL模式。URL模式描述了URL是如何设计的，让Django知道如何将浏览器请求与网站URL匹配，以确定返回哪个页面。</p>
<p>每个URL都被映射到特定的<strong>视图</strong>
 ——视图函数获取并处理页面所需的数据。视图函数通常使用<strong>模板</strong>
 来渲染页面，而模板定义页面的总体结构。为明白其中的工作原理，我们来创建学习笔记的主页。这包括定义该主页的URL，编写其视图函数并创建一个简单的模板。</p>
<p>我们只是要确保“学习笔记”按要求的那样工作，因此暂时让这个页面尽可能简单。Web应用程序能够正常运行后，设置样式可使其更有趣，但中看不中用的应用程序毫无意义。就目前而言，主页只显示标题和简单的描述。</p>
<h3 id="nav_point_434">18.3.1　映射URL</h3>
<p>用户通过在浏览器中输入URL以及单击链接来请求页面，因此我们要确定项目需要哪些URL。主页的URL最重要，它是用户用来访问项目的基础URL。当前，基础URL（http://localhost: 8000/）返回默认的Django网站，让我们知道正确地建立了项目。下面修改这一点，将这个基础URL映射到“学习笔记”的主页。</p>
<p>打开项目主文件夹learning_log中的文件urls.py，你将看到如下代码：</p>
<p>
<em>urls.py</em>
</p>
<pre class="代码无行号"><code>❶ from django.contrib import admin
  from django.urls import path
❷ urlpatterns = [
❸     path('admin/', admin.site.urls),
  ]</code>
</pre>
<p>前两行导入了一个模块和一个函数，以便对管理网站的URL进行管理（见❶）。这个文件的主体定义了变量<code>urlpatterns</code>
 （见❷）。在这个针对整个项目的urls.py文件中，变量<code>urlpatterns</code>
 包含项目中应用程序的URL。❸处的代码包含模块<code>admin.site.urls</code>
 ，该模块定义了可在管理网站中请求的所有URL。</p>
<p>我们需要包含<code>learning_logs</code>
 的URL，因此添加如下代码：</p>
<pre class="代码无行号"><code>  from django.contrib import admin
  from django.urls import path, include
  urlpatterns = [
      path('admin/', admin.site.urls),
❶     path('', include('learning_logs.urls')),
  ]</code>
</pre>
<p>在❶处，添加一行代码来包含模块<code>learning_logs.urls</code>
 。</p>
<p>默认的urls.py包含在文件夹learning_log中，现在需要在文件夹learning_logs中再创建一个urls.py文件。为此，新建一个文件，使用文件名urls.py将其存储到文件夹learning_logs中，再在这个文件中输入如下代码：</p>
<p>
<em>urls.py</em>
</p>
<pre class="代码无行号"><code>❶ """定义learning_logs的URL模式。"""
❷ from django.urls import path
❸ from . import views
❹ app_name = 'learning_logs'
❺ urlpatterns = [
      # 主页
❻     path('', views.index, name='index'),
  ]</code>
</pre>
<p>为指出当前位于哪个urls.py文件中，在该文件开头添加一个文档字符串（见❶）。接下来，导入了函数<code>path</code>
 ，因为需要使用它将URL映射到视图（见❷）。我们还导入了模块<code>views</code>
 （见❸），其中的句点让Python从当前urls.py模块所在的文件夹导入views.py。变量<code>app_name</code>
 让Django能够将这个urls.py文件同项目内其他应用程序中的同名文件区分开来（见❹）。在这个模块中，变量<code>urlpatterns</code>
 是一个列表，包含可在应用程序<code>learning_logs</code>
 中请求的页面。</p>
<p>实际的URL模式是对函数<code>path()</code>
 的调用，这个函数接受三个实参（见❺）。第一个是一个字符串，帮助Django正确地路由（route）请求。收到请求的URL后，Django力图将请求路由给一个视图。为此，它搜索所有的URL模式，找到与当前请求匹配的那个。Django忽略项目的基础URL（http://localhost:8000/），因此空字符串（<code>''</code>
 ）与基础URL匹配。其他URL都与这个模式不匹配。如果请求的URL与任何既有的URL模式都不匹配，Django将返回一个错误页面。</p>
<p>
<code>path()</code>
 的第二个实参（见❻）指定了要调用view.py中的哪个函数。请求的URL与前述正则表达式匹配时，Django将调用view.py中的函数<code>index()</code>
 （这个视图函数将在下一节编写）。第三个实参将这个URL模式的名称指定为<code>index</code>
 ，让我们能够在代码的其他地方引用它。每当需要提供到这个主页的链接时，都将使用这个名称，而不编写URL。</p>
<h3 id="nav_point_435">18.3.2　编写视图</h3>
<p>视图函数接受请求中的信息，准备好生成页面所需的数据，再将这些数据发送给浏览器——这通常是使用定义页面外观的模板实现的。</p>
<p>learning_logs中的文件views.py是执行命令<code>python manage.py startapp</code>
 时自动生成的，当前其内容如下：</p>
<p>
<em>views.py</em>
</p>
<pre class="代码无行号"><code>from django.shortcuts import render
# 在这里创建视图。</code>
</pre>
<p>当前，这个文件只导入了函数<code>render()</code>
 ，它根据视图提供的数据渲染响应。请在这个文件中添加为主页编写视图的代码，如下所示：</p>
<pre class="代码无行号"><code>from django.shortcuts import render
def index(request):
    """学习笔记的主页。"""
    return render(request, 'learning_logs/index.html')</code>
</pre>
<p>URL请求与刚才定义的模式匹配时，Django将在文件views.py中查找函数<code>index()</code>
 ，再将对象<code>request</code>
 传递给这个视图函数。这里不需要处理任何数据，因此这个函数只包含调用<code>render()</code>
 的代码。这里向函数<code>render()</code>
 提供了两个实参：对象<code>request</code>
 以及一个可用于创建页面的模板。下面来编写这个模板。</p>
<h3 id="nav_point_436">18.3.3　编写模板</h3>
<p>模板定义页面的外观，而每当页面被请求时，Django将填入相关的数据。模板让你能够访问视图提供的任何数据。我们的主页视图没有提供任何数据，因此相应的模板非常简单。</p>
<p>在文件夹learning_logs中新建一个文件夹，并将其命名为templates。在文件夹templates中，再新建一个文件夹，并将其命名为learning_logs。这好像有点多余（在文件夹learning_logs中创建文件夹templates，又在这个文件夹中创建文件夹learning_logs），但是建立了Django能够明确解读的结构，即便项目很大、包含很多应用程序亦如此。在最里面的文件夹learning_logs中，新建一个文件，并将其命名为index.html（这个文件的路径为learning_log/learning_logs/templates/ learning_logs/index.html），再在其中编写如下代码：</p>
<p>
<em>index.html</em>
</p>
<pre class="代码无行号"><code>&lt;p&gt;Learning Log&lt;/p&gt;
&lt;p&gt;Learning Log helps you keep track of your learning, for any topic you're
learning about.&lt;/p&gt;</code>
</pre>
<p>这个文件非常简单。这里向不熟悉HTML的读者解释一下：标签<code>&lt;p&gt;&lt;/p&gt;</code>
 标识段落。标签<code>&lt;p&gt;</code>
 指出段落的开头位置，而标签<code>&lt;/p&gt;</code>
 指出段落的结束位置。这里定义了两个段落：第一个充当标题，第二个阐述了用户可使用“学习笔记”来做什么。</p>
<p>现在，如果请求这个项目的基础URL http://localhost:8000/，将看到刚才创建的页面，而不是默认的Django页面。Django接受请求的URL，发现该URL与模式<code>''</code>
 匹配，因此调用函数<code>views.index()</code>
 。这将使用index.html包含的模板来渲染页面，结果如图18-3所示。</p>
<p class="图"><img alt="" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/18_Image00049.gif" style="width: 85%" width="85%"/>
</p>
<p>
<strong>图18-3　学习笔记的主页</strong>
</p>
<p>创建页面的过程看起来可能很复杂，但将URL、视图和模板分离的效果很好。这让我们能够分别考虑项目的不同方面，在项目很大时，可让各个参与者专注于最擅长的方面。例如，数据库专家专注于模型，程序员专注于视图代码，而Web设计人员专注于模板。</p>
<blockquote>
<p>
<strong>注意</strong>
 　可能出现如下错误消息：</p>
<pre class="代码无行号"><code>ModuleNotFoundError: No module named 'learning_logs.urls'</code>
</pre>
<p>如果确实如此，请在执行命令<code>python manage.py runserver</code>
 的终端窗口中按Ctrl + C停用服务器，再重新执行这个命令。这样做后，应该能够看到主页。每当遇到类似的错误时，都尝试停用并重启服务器，看看是否管用。</p>
</blockquote>
<p>　</p>
<blockquote>
<p>
<strong>动手试一试</strong>
</p>
<p>
<strong>练习18-5：膳食规划程序</strong>
 　假设你要创建一个应用程序，帮助用户规划一周的膳食。为此，新建一个文件夹，并将其命名为meal_planner，再在这个文件夹中新建一个Django项目。然后，新建一个名为<code>meal_plans</code>
 的应用程序，并为这个项目创建一个简单的主页。</p>
<p>
<strong>练习18-6：比萨店主页</strong>
 　在为完成练习18-4而创建的项目Pizzeria中，添加一个主页。</p>
</blockquote>
<h2 id="nav_point_437">18.4　创建其他页面</h2>
<p>制定创建页面的流程后，可以开始扩充“学习笔记”项目了。我们将创建两个显示数据的页面，其中一个列出所有的主题，另一个显示特定主题的所有条目。对于每个页面，我们都将指定URL模式、编写一个视图函数并编写一个模板。但这样做之前，我们先创建一个父模板，项目中的其他模板都将继承它。</p>
<h3 id="nav_point_438">18.4.1　模板继承</h3>
<p>创建网站时，一些通用元素几乎会在所有页面中出现。在这种情况下，可编写一个包含通用元素的父模板，并让每个页面都继承这个模板，而不必在每个页面中重复定义这些通用元素。这种方法能让你专注于开发每个页面的独特方面，还能让修改项目的整体外观容易得多。</p>
<ol>
<li value="1">
<p>
<strong>父模板</strong>
</p>
<p>下面创建一个名为base.html的模板，并将其存储在index.html所在的目录中。这个模板包含所有页面都有的元素，而其他模板都继承它。当前，所有页面都包含的元素只有顶端的标题。因为每个页面都包含这个模板，所以将这个标题设置为到主页的链接：</p>
<p>
<em>base.html</em>
</p>
<pre class="代码无行号"><code>  &lt;p&gt;
❶   &lt;a href="{% url 'learning_logs:index' %}"&gt;Learning Log&lt;/a&gt;
  &lt;/p&gt;
❷ {% block content %}{% endblock content %}</code>
</pre>
<p>这个文件的第一部分创建一个包含项目名的段落，该段落也是到主页的链接。为创建链接，使用了一个<strong>模板标签</strong>
 ，它是用花括号和百分号（<code>{% %}</code>
 ）表示的。模板标签是一小段代码，生成要在页面中显示的信息。这里的模板标签<code>{% url 'learning_logs:index' %}</code>
 生成一个URL，该URL与在learning_logs/urls.py中定义的名为<code>'index'</code>
 的URL模式匹配（见❶）。在本例中，<code>learning_logs</code>
 是一个<strong>命名空间</strong>
 ，而<code>index</code>
 是该命名空间中一个名称独特的URL模式。这个命名空间来自在文件learning_logs/urls.py中赋给<code>app_name</code>
 的值。</p>
<p>在简单的HTML页面中，链接是使用<strong>锚</strong>
 标签<code>&lt;a&gt;</code>
 定义的：</p>
<pre class="代码无行号"><code>&lt;a href="link_url"&gt;link text&lt;/a&gt;</code>
</pre>
<p>通过使用模板标签来生成URL，能很容易地确保链接是最新的：只需修改urls.py中的URL模式，Django就会在页面下次被请求时自动插入修改后的URL。在本项目中，每个页面都将继承base.html，因此从现在开始，每个页面都包含到主页的链接。</p>
<p>在❷处，我们插入了一对<strong>块</strong>
 标签。这个块名为<code>content</code>
 ，是一个占位符，其中包含的信息由子模板指定。</p>
<p>子模板并非必须定义父模板中的每个块，因此在父模板中，可使用任意多个块来预留空间，而子模板可根据需要定义相应数量的块。</p>
<blockquote>
<p>
<strong>注意</strong>
 　在Python代码中，几乎总是缩进四个空格。相比于Python文件，模板文件的缩进层级更多，因此每个层级通常只缩进两个空格。每个层级缩进多少个空格无关紧要，只需确保一致即可。</p>
</blockquote>
<p> </p>
</li>
<li value="2">
<p>
<strong>子模板</strong>
</p>
<p>现在需要重写index.html，使其继承base.html。为此，向index.html添加如下代码：</p>
<p>
<em>index.html</em>
</p>
<pre class="代码无行号"><code>❶ {% extends "learning_logs/base.html" %}
❷ {% block content %}
  &lt;p&gt;Learning Log helps you keep track of your learning, for any topic you're
  learning about.&lt;/p&gt;
❸ {% endblock content %}</code>
</pre>
<p>如果将这些代码与原来的index.html进行比较，将发现标题Learning Log没有了，取而代之的是指定要继承哪个模板的代码（见❶）。子模板的第一行必须包含标签<code>{% extends %}</code>
 ，让Django知道它继承了哪个父模板。文件base.html位于文件夹learning_logs中，因此父模板路径中包含learning_logs。这行代码导入模板base.html的所有内容，让index.html能够指定要在<code>content</code>
 块预留的空间中添加的内容。</p>
<p>在❷处，插入了一个名为<code>content</code>
 的<code>{% block %}</code>
 标签，以定义<code>content</code>
 块。不是从父模板继承的内容都包含在<code>content</code>
 块中，在这里是一个描述项目“学习笔记”的段落。在❸处，使用标签<code>{% endblock content %}</code>
 指出了内容定义的结束位置。在标签<code>{% endblock %}</code>
 中，并非必须指定块名，但如果模板包含多个块，指定块名有助于确定结束的是哪个块。</p>
<p>模板继承的优点开始显现出来了：在子模板中，只需包含当前页面特有的内容。这不仅简化了每个模板，还使得网站修改起来容易得多。要修改很多页面都包含的元素，只需修改父模板即可，所做的修改将传导到继承该父模板的每个页面。在包含数十乃至数百个页面的项目中，这种结构使得网站改进起来更容易、更快捷。</p>
<blockquote>
<p>
<strong>注意</strong>
 　在大型项目中，通常有一个用于整个网站的父模板base.html，且网站的每个主要部分都有一个父模板。每个部分的父模板都继承base.html，而网站的每个页面都继承相应部分的父模板。这让你能够轻松地修改整个网站的外观、网站任何一部分的外观以及任何一个页面的外观。这种配置提供了一种效率极高的工作方式，让你乐意不断地去改进网站。</p>
</blockquote>
</li>
</ol>
<h3 id="nav_point_439">18.4.2　显示所有主题的页面</h3>
<p>有了高效的页面创建方法，就能专注于另外两个页面了：显示所有主题的页面和显示特定主题中条目的页面。前者显示用户创建的所有主题，它是第一个需要使用数据的页面。</p>
<ol>
<li value="1">
<p>
<strong>URL模式</strong>
</p>
<p>首先，定义显示所有主题的页面的URL。我们通常使用一个简单的URL片段来指出页面显示的信息，这里使用单词topics，因此URL http://localhost:8000/topics/将返回显示所有主题的页面。下面演示了该如何修改learning_logs/urls.py：</p>
<p>
<em>urls.py</em>
</p>
<pre class="代码无行号"><code>  """为learning_logs定义URL模式。"""
  --snip--
  urlpatterns = [
      # 主页
      path('', views.index, name='index'),
      # 显示所有的主题。
❶     path('topics/', views.topics, name='topics'),
  ]</code>
</pre>
<p>这里在用于主页URL的字符串参数中添加了<code>topics/</code>
 （见❶）。Django检查请求的URL时，这个模式与如下URL匹配：基础URL后面跟着topics。可在末尾包含斜杠，也可省略，但单词topics后面不能有任何东西，否则就与该模式不匹配。URL与该模式匹配的请求都将交给views.py中的函数<code>topics()</code>
 处理。<br/>
 </p>
</li>
<li value="2">
<p>
<strong>视图</strong>
</p>
<p>函数<code>topics()</code>
 需要从数据库中获取一些数据，并将其交给给模板。需要在views.py中添加的代码如下：</p>
<p>
<em>views.py</em>
</p>
<pre class="代码无行号"><code>  from django.shortcuts import render
❶ from .models import Topic
  def index(request):
      --snip--
❷ def topics(request):
      """显示所有的主题。"""
❸     topics = Topic.objects.order_by('date_added')
❹     context = {'topics': topics}
❺     return render(request, 'learning_logs/topics.html', context)</code>
</pre>
<p>首先导入与所需数据相关联的模型（见❶）。函数<code>topics()</code>
 包含一个形参：Django从服务器那里收到的<code>request</code>
 对象（见❷）。在❸处，查询数据库——请求提供<code>Topic</code>
 对象，并根据属性<code>date_added</code>
 进行排序。返回的查询集被存储在<code>topics</code>
 中。</p>
<p>在❹处，定义一个将发送给模板的上下文。<strong>上下文</strong>
 是一个字典，其中的键是将在模板中用来访问数据的名称，而值是要发送给模板的数据。这里只有一个键值对，包含一组将在页面中显示的主题。创建使用数据的页面时，除了对象<code>request</code>
 和模板的路径外，还将变量<code>context</code>
 传递给<code>render()</code>
 （见❺）。<br/>
 </p>
</li>
<li value="3">
<p>
<strong>模板</strong>
</p>
<p>显示所有主题的页面的模板接受字典<code>context</code>
 ，以便使用<code>topics()</code>
 提供的数据。请创建一个文件，将其命名为topics.html，并存储到index.html所在的目录中。下面演示了如何在这个模板中显示主题：</p>
<p>
<em>topics.html</em>
</p>
<pre class="代码无行号"><code>  {% extends "learning_logs/base.html" %}
  {% block content %}
    &lt;p&gt;Topics&lt;/p&gt;
❶   &lt;ul&gt;
❷     {% for topic in topics %}
❸       &lt;li&gt;{{ topic }}&lt;/li&gt;
❹     {% empty %}
        &lt;li&gt;No topics have been added yet.&lt;/li&gt;
❺     {% endfor %}
❻   &lt;/ul&gt;
  {% endblock content %}</code>
</pre>
<p>就像模板index.html中一样，首先使用标签<code>{% extends %}</code>
 来继承base.html，再开始定义<code>content</code>
 块。这个页面的主体是一个项目列表，其中列出了用户输入的主题。在标准HTML中，项目列表称为<strong>无序列表</strong>
 ，用标签<code>&lt;ul&gt;&lt;/ul&gt;</code>
 表示。包含所有主题的项目列表始于❶处。</p>
<p>在❷处，使用一个相当于<code>for</code>
 循环的模板标签，它遍历字典<code>context</code>
 中的列表<code>topics</code>
 。模板中使用的代码与Python代码存在一些重要差别：Python使用缩进来指出哪些代码行是<code>for</code>
 循环的组成部分；而在模板中，每个<code>for</code>
 循环都必须使用<code>{% endfor %}</code>
 标签来显式地指出其结束位置。因此在模板中，循环类似于下面这样：</p>
<pre class="代码无行号"><code>{% for item in list %}
    do something with each item
{% endfor %}</code>
</pre>
<p>在循环中，要将每个主题转换为项目列表中的一项。要在模板中打印变量，需要将变量名用双花括号括起。这些花括号不会出现在页面中，只是用于告诉Django我们使用了一个模板变量。因此每次循环时，❸处的代码<code>{{ topic }}</code>
 都被替换为<code>topic</code>
 的当前值。HTML标签<code>&lt;li&gt;&lt;/li&gt;</code>
 表示一个<strong>项目列表项</strong>
 。在标签对<code>&lt;ul&gt;&lt;/ul&gt;</code>
 内部，位于标签<code>&lt;li&gt;</code>
 和<code>&lt;/li&gt;</code>
 之间的内容都是一个项目列表项。</p>
<p>在❹处，使用模板标签<code>{% empty %}</code>
 ，它告诉Django在列表<code>topics</code>
 为空时该如何办。这里是打印一条消息，告诉用户还没有添加任何主题。最后两行分别结束<code>for</code>
 循环（见❺）和项目列表（见❻）。</p>
<p>现在需要修改父模板，使其包含到显示所有主题的页面的链接。为此，在其中添加如下代码：</p>
<p>
<em>base.html</em>
</p>
<pre class="代码无行号"><code>  &lt;p&gt;
❶   &lt;a href="{% url 'learning_logs:index' %}"&gt;Learning Log&lt;/a&gt; -
❷   &lt;a href="{% url 'learning_logs:topics' %}"&gt;Topics&lt;/a&gt;
  &lt;/p&gt;
  {% block content %}{% endblock content %}</code>
</pre>
<p>在到主页的链接后面添加一个连字符（见❶），再添加一个到显示所有主题的页面的链接——使用的也是模板标签<code>{% url %}</code>
 （见❷）。这行让Django生成一个链接，它与learning_logs/urls.py中名为<code>'topics'</code>
 的URL模式匹配。</p>
<p>现在如果刷新浏览器中的主页，将看到链接Topics。如果单击这个链接，将看到类似于图18-4所示的页面。</p>
<p class="p-img"><img alt="" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/18_Image00050.gif" style="width: 90%" width="90%"/>
</p>
<p>
<strong>图18-4　显示所有主题的页面</strong>
</p>
</li>
</ol>
<h3 id="nav_point_440">18.4.3　显示特定主题的页面</h3>
<p>接下来，需要创建一个专注于特定主题的页面，它显示该主题的名称以及所有条目。我们同样将定义一个新的URL模式，编写一个视图并创建一个模板。此外，还将修改显示所有主题的页面，让每个项目列表项都变为到相应主题页面的链接。</p>
<ol>
<li value="1">
<p>
<strong>URL模式</strong>
</p>
<p>显示特定主题的页面的URL模式与前面的所有URL模式都稍有不同，因为它使用主题的<code>id</code>
 属性来指出请求的是哪个主题。例如，如果用户要查看主题Chess（其<code>id</code>
 为1）的详细页面，URL将为http://localhost:8000/topics/1/。下面是与这个URL匹配的模式，应将其放在learning_logs/ urls.py中：</p>
<p>
<em>urls.py</em>
</p>
<pre class="代码无行号"><code>--snip--
urlpatterns = [
    --snip--
    # 特定主题的详细页面。
    path('topics/&lt;int:topic_id&gt;/', views.topic, name='topic'),
]</code>
</pre>
<p>我们来详细研究这个URL模式中的字符串<code>'topics/&lt;int:topic_id&gt;/'</code>
 。这个字符串的第一部分让Django查找在基础URL后包含单词topics的URL，第二部分（<code>/&lt;int:topic_id&gt;/</code>
 ）与包含在两个斜杠内的整数匹配，并将这个整数存储在一个名为<code>topic_id</code>
 的实参中。</p>
<p>发现URL与这个模式匹配时，Django将调用视图函数<code>topic()</code>
 ，并将存储在<code>topic_id</code>
 中的值作为实参传递给它。在这个函数中，将使用<code>topic_id</code>
 的值来获取相应的主题。<br/>
 </p>
</li>
<li value="2">
<p>
<strong>视图</strong>
</p>
<p>函数<code>topic()</code>
 需要从数据库中获取指定的主题以及与之相关联的所有条目，如下所示：</p>
<p>
<em>views.py</em>
</p>
<pre class="代码无行号"><code>  --snip--
❶ def topic(request, topic_id):
      """显示单个主题及其所有的条目。"""
❷     topic = Topic.objects.get(id=topic_id)
❸     entries = topic.entry_set.order_by('-date_added')
❹     context = {'topic': topic, 'entries': entries}
❺     return render(request, 'learning_logs/topic.html', context)</code>
</pre>
<p>这是除<code>request</code>
 对象外，第一个还包含另一个形参的视图函数。这个函数接受表达式<code>/&lt;int:topic_id&gt;/</code>
 捕获的值，并将其存储到<code>topic_id</code>
 中（见❶）。在❷处，使用<code>get()</code>
 来获取指定的主题，就像前面在Django shell中所做的那样。在❸处，获取与该主题相关联的条目，并根据<code>date_added</code>
 进行排序：<code>date_added</code>
 前面的减号指定按降序排列，即先显示最近的条目。将主题和条目都存储在字典<code>context</code>
 中（见❹），再将这个字典发送给模板topic.html（见❺）。</p>
<blockquote>
<p>
<strong>注意</strong>
 　❷处和❸处的代码称为<strong>查询</strong>
 ，因为它们向数据库查询了特定的信息。在自己的项目中编写这样的查询时，先在Django shell中进行尝试大有裨益。比起先编写视图和模板、再在浏览器中检查结果，在shell中执行代码可更快获得反馈。</p>
</blockquote>
<p> </p>
</li>
<li value="3">
<p>
<strong>模板</strong>
</p>
<p>这个模板需要显示主题的名称和条目的内容。如果当前主题不包含任何条目，还需向用户指出这一点：</p>
<p>
<em>topic.html</em>
</p>
<pre class="代码无行号"><code>  {% extends 'learning_logs/base.html' %}
  {% block content %}
❶   &lt;p&gt;Topic: {{ topic }}&lt;/p&gt;
    &lt;p&gt;Entries:&lt;/p&gt;
❷   &lt;ul&gt;
❸   {% for entry in entries %}
      &lt;li&gt;
❹       &lt;p&gt;{{ entry.date_added|date:'M d, Y H:i' }}&lt;/p&gt;
❺       &lt;p&gt;{{ entry.text|linebreaks }}&lt;/p&gt;
      &lt;/li&gt;
❻   {% empty %}
      &lt;li&gt;There are no entries for this topic yet.&lt;/li&gt;
    {% endfor %}
    &lt;/ul&gt;
  {% endblock content %}</code>
</pre>
<p>像这个项目的其他页面一样，这里也继承了base.html。接下来，显示当前的主题（见❶），它存储在模板变量<code>{{ topic }}</code>
 中。为什么可以使用变量<code>topic</code>
 呢？因为它包含在字典<code>context</code>
 中。接下来，定义一个显示每个条目的项目列表（见❷），并像前面显示所有主题一样遍历条目（见❸）。</p>
<p>每个项目列表项都将列出两项信息：条目的时间戳和完整的文本。为列出时间戳（见❹），我们显示属性<code>date_added</code>
 的值。在Django模板中，竖线（<code>|</code>
 ）表示模板<strong>过滤器</strong>
 ，即对模板变量的值进行修改的函数。过滤器<code>date: 'M d, Y H:i'</code>
 以类似于这样的格式显示时间戳：January 1, 2018 23:00。接下来的一行显示<code>text</code>
 的完整值，而不仅仅是前50字符。过滤器<code>linebreaks</code>
 （见❺）将包含换行符的长条目转换为浏览器能够理解的格式，以免显示为不间断的文本块。在❻处，使用模板标签<code>{% empty %}</code>
 打印一条消息，告诉用户当前主题还没有条目。<br/>
 </p>
</li>
<li value="4">
<p>
<strong>将显示所有主题的页面中的主题设置为链接</strong>
</p>
<p>在浏览器中查看显示特定主题的页面前，需要修改模板topics.html，让每个主题都链接到相应的页面，如下所示：</p>
<p>
<em>topics.html</em>
</p>
<pre class="代码无行号"><code>--snip--
   {% for topic in topics %}
     &lt;li&gt;
       &lt;a href="{% url 'learning_logs:topic' topic.id %}"&gt;{{ topic }}&lt;/a&gt;
     &lt;/li&gt;
  {% empty %}
--snip--</code>
</pre>
<p>我们使用模板标签<code>url</code>
 根据<code>learning_logs</code>
 中名为<code>'topic'</code>
 的URL模式生成了合适的链接。这个URL模式要求提供实参<code>topic_id</code>
 ，因此在模板标签<code>url</code>
 中添加了属性<code>topic.id</code>
 。现在，主题列表中的每个主题都是链接了，链接到显示相应主题的页面，如http://localhost:8000/ topics/1/。</p>
<p>如果现在刷新显示所有主题的页面，再单击其中的一个主题，将看到类似于图18-5所示的页面。</p>
<p class="p-img"><img alt="{%}" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/18_Image00051.jpg"/>
</p>
<p>
<strong>图18-5　特定主题的详细页面，其中显示了该主题的所有条目</strong>
</p>
<blockquote>
<p>
<strong>注意</strong>
 　topic.id和topic_id之间存在细微而重要的差别。表达式topic.id检查主题并获取其ID值，而在代码中，变量topic_id是指向该ID的引用。使用ID时如果出现错误，请确保正确地使用了这两个表达式。</p>
</blockquote>
</li>
</ol>
<p>　</p>
<blockquote>
<p>
<strong>动手试一试</strong>
</p>
<p>
<strong>练习18-7：模板文档</strong>
 　请浏览Django模板文档。自己开发项目时，你可再回过头来参考该文档。</p>
<p>
<strong>练习18-8：比萨店页面</strong>
 　在练习18-6中开发的项目Pizzeria中，添加一个页面来显示供应的比萨的名称。然后，将每个比萨名都设置成链接，可通过单击来显示一个列出相应配料的页面。请务必使用模板继承来高效地创建页面。</p>
</blockquote>
<h2 id="nav_point_441">18.5　小结</h2>
<p>在本章中，你学习了：如何使用Django框架来创建Web应用程序；制定简要的项目规范，在虚拟环境中安装Django，创建一个项目，并核实该项目已被正确地创建；如何创建应用程序，以及如何定义表示应用程序数据的模型。你了解了数据库，以及在修改模型后，Django可为迁移数据库提供什么帮助。你创建了可访问管理网站的超级用户，并使用管理网站输入了一些初始数据。</p>
<p>你还探索了Django shell，它让你能够在终端会话中处理项目的数据。你学习了如何定义URL、创建视图函数以及编写为网站创建页面的模板。最后，你使用模板继承简化了各个模板的结构，使修改网站变得更容易。</p>
<p>第19章将创建对用户友好而直观的页面，让用户无须通过管理网站就能添加新的主题和条目，以及编辑既有条目。我们还将添加一个用户注册系统，让用户能够创建账户并记录自己的学习笔记。Web应用程序的核心就是，让任意数量的用户都能与之交互。</p>
<br style="page-break-after:always"/>
<div></div>
]]></description>
    <pubDate>Thu, 30 Jul 2026 01:10:08 +0000</pubDate>
    <guid isPermaLink="false">18</guid>
</item>
<item>
    <title>Basic Python - 017 - 第 17 章 使用API.epub</title>
    <description><![CDATA[<p style='color: #888;'>⏱ 预计阅读时间：87 分钟 (17348 字)</p><hr><h1 id="nav_point_399">第 17 章　使用API</h1>
<blockquote>
<p><img alt="" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/17_Image00001.gif" style="width: 16%" width="16%"/>
 　本章介绍如何编写独立的程序，对获取的数据进行可视化。这个程序将使用Web<strong>应用程序编程接口</strong>
 （API）自动请求网站的特定信息而不是整个网页，再对这些信息进行可视化。由于这样编写的程序始终使用最新的数据进行可视化，即便数据瞬息万变，它呈现的信息也是最新的。</p>
</blockquote>
<h2 id="nav_point_400">17.1　使用Web API</h2>
<p>Web API是网站的一部分，用于与使用具体URL请求特定信息的程序交互。这种请求称为<strong>API调用</strong>
 。请求的数据将以易于处理的格式（如JSON或CSV）返回。依赖于外部数据源的大多数应用程序依赖于API调用，如集成社交媒体网站的应用程序。</p>
<h3 id="nav_point_401">17.1.1　Git和GitHub</h3>
<p>本章的可视化基于来自GitHub的信息，这是一个让程序员能够协作开发项目的网站。我们将使用GitHub的API来请求有关该网站中Python项目的信息，再使用Plotly生成交互式可视化图表，呈现这些项目的受欢迎程度。</p>
<p>GitHub的名字源自Git，后者是一个分布式版本控制系统，帮助人们管理为项目所做的工作，避免一个人所做的修改影响其他人所做的修改。在项目中实现新功能时，Git跟踪你对每个文件所做的修改。确定代码可行后，你提交所做的修改，而Git将记录项目最新的状态。如果犯了错，想撤销所做的修改，你可以轻松地返回到以前的任何可行状态。（要更深入地了解如何使用Git进行版本控制，请参阅附录D。）GitHub上的项目都存储在仓库中，后者包含与项目相关联的一切：代码、项目参与者的信息、问题或bug报告，等等。</p>
<p>GitHub用户可以给喜欢的项目加星（star）以表示支持，还可以跟踪自己可能想使用的项目。在本章中，我们将编写一个程序，自动下载GitHub上星级最高的Python项目的信息，并对这些信息进行可视化。</p>
<h3 id="nav_point_402">17.1.2　使用API调用请求数据</h3>
<p>GitHub的API让你能够通过API调用来请求各种信息。要知道API调用是什么样的，请在浏览器的地址栏中输入如下地址并按回车键：</p>
<pre class="代码无行号"><code>https://api.github.com/search/repositories?q=language:python&amp;sort=stars</code>
</pre>
<p>这个调用返回GitHub当前托管了多少个Python项目，以及有关最受欢迎的Python仓库的信息。下面来仔细研究这个调用。开头的<code>https://api.github.com/</code>
 将请求发送到GitHub网站中响应API调用的部分，接下来的<code>search/repositories</code>
 让API搜索GitHub上的所有仓库。</p>
<p>
<code>repositories</code>
 后面的问号指出需要传递一个实参。<code>q</code>
 表示查询，而等号（<code>=</code>
 ）让我们能够开始指定查询。我们使用<code>language:python</code>
 指出只想获取主要语言为Python的仓库的信息。最后的<code>&amp;sort=stars</code>
 指定将项目按星级排序。</p>
<p>下面显示了响应的前几行。</p>
<pre class="代码无行号"><code>  {
❶   "total_count": 3494012,
❷   "incomplete_results": false,
❸   "items": [
      {
        "id": 21289110,
        "node_id": "MDEwOlJlcG9zaXRvcnkyMTI4OTExMA==",
        "name": "awesome-python",
        "full_name": "vinta/awesome-python",
        --snip--</code>
</pre>
<p>从响应可知，该URL并不适合人工输入，因为它采用了适合程序处理的格式。本书编写期间，GitHub总共有3 494 012个Python项目（见❶）。<code>"incomplete_results"</code>
 的值为<code>false</code>
 （见❷），由此知道请求是成功的（并非不完整的）。倘若GitHub无法处理该API，此处返回的值将为<code>true</code>
 。接下来的列表中显示了返回的<code>"items"</code>
 ，其中包含GitHub上最受欢迎的Python项目的详细信息（见❸）。</p>
<h3 id="nav_point_403">17.1.3　安装Requests</h3>
<p>Requests包让Python程序能够轻松地向网站请求信息并检查返回的响应。要安装Requests，可使用<code>pip</code>
 ：</p>
<pre class="代码无行号"><code>$ python -m pip install --user requests</code>
</pre>
<p>这个命令让Python运行模块<code>pip</code>
 ，并在当前用户的Python安装中添加Requests包。如果你运行程序或安装包时使用的是命令<strong>
<code>python3</code>
</strong>
 或其他命令，请务必在这里使用同样的命令。</p>
<blockquote>
<p>
<strong>注意</strong>
 　如果该命令在macOS系统上不管用，可以尝试删除标志<code>--user</code>
 再次运行。</p>
</blockquote>
<h3 id="nav_point_404">17.1.4　处理API响应</h3>
<p>下面来编写一个程序，它自动执行API调用并处理结果，以找出GitHub上星级最高的Python项目：</p>
<p>
<em>python</em>
repos.py</p>
<pre class="代码无行号"><code>❶ import requests
  # 执行API调用并存储响应。
❷ url = 'https://api.github.com/search/repositories?q=language:python&amp;sort=stars'
❸ headers = {'Accept': 'application/vnd.github.v3+json'}
❹ r = requests.get(url, headers=headers)
❺ print(f"Status code: {r.status_code}")
  # 将API响应赋给一个变量。
❻ response_dict = r.json()
  # 处理结果。
  print(response_dict.keys())</code>
</pre>
<p>在❶处，导入模块<code>requests</code>
 。在❷处，存储API调用的URL。最新的GitHub API版本为第3版，因此通过指定<code>headers</code>
 显式地要求使用这个版本的API（见❸），再使用<code>requests</code>
 调用API（见❹）。</p>
<p>我们调用<code>get()</code>
 并将URL传递给它，再将响应对象赋给变量<code>r</code>
 。响应对象包含一个名为<code>status_code</code>
 的属性，指出了请求是否成功（状态码200表示请求成功）。在❺处，打印<code>status_code</code>
 ，核实调用是否成功。</p>
<p>这个API返回JSON格式的信息，因此使用方法<code>json()</code>
 将这些信息转换为一个Python字典（见❻），并将结果存储在<code>response_dict</code>
 中。</p>
<p>最后，打印<code>response_dict</code>
 中的键，输出如下：</p>
<pre class="代码无行号"><code>Status code: 200
dict_keys(['total_count', 'incomplete_results', 'items'])</code>
</pre>
<p>状态码为200，由此知道请求成功了。响应字典只包含三个键：<code>'total_count'</code>
 、<code>'incomplete_results'</code>
 和<code>'items'</code>
 。下面来看看响应字典内部是什么样的。</p>
<blockquote>
<p>
<strong>注意</strong>
 　像这样简单的调用应该会返回完整的结果集，因此完全可以忽略与<code>'incomplete_results'</code>
 关联的值。但在执行更复杂的API调用时，应检查这个值。</p>
</blockquote>
<h3 id="nav_point_405">17.1.5　处理响应字典</h3>
<p>将API调用返回的信息存储到字典后，就可处理其中的数据了。我们来生成一些概述这些信息的输出。这是一种不错的方式，可确认收到了期望的信息，进而开始研究感兴趣的信息：</p>
<p>
<em>python_repos.py</em>
</p>
<pre class="代码无行号"><code>  import requests
  # 执行API调用并存储响应。
  --snip--
  # 将API响应赋给一个变量。
  response_dict = r.json()
❶ print(f"Total repositories: {response_dict['total_count']}")
  # 探索有关仓库的信息。
❷ repo_dicts = response_dict['items']
  print(f"Repositories returned: {len(repo_dicts)}")
  # 研究第一个仓库。
❸ repo_dict = repo_dicts[0]
❹ print(f"\nKeys: {len(repo_dict)}")
❺ for key in sorted(repo_dict.keys()):
      print(key)</code>
</pre>
<p>在❶处，打印与<code>'total_count'</code>
 相关联的值，它指出了GitHub总共包含多少个Python仓库。</p>
<p>与<code>'items'</code>
 关联的值是个列表，其中包含很多字典，而每个字典都包含有关一个Python仓库的信息。在❷处，将这个字典列表存储在<code>repo_dicts</code>
 中。接下来，打印<code>repo_dicts</code>
 的长度，以获悉获得了多少个仓库的信息。</p>
<p>为更深入地了解每个仓库的信息，提取<code>repo_dicts</code>
 中的第一个字典，并将其存储在<code>repo_dict</code>
 中（见❸）。接下来，打印这个字典包含的键数，看看其中有多少信息（见❹）。在❺处，打印这个字典的所有键，看看其中包含哪些信息。</p>
<p>输出让我们对实际包含的数据有了更清晰的认识：</p>
<pre class="代码无行号"><code>  Status code: 200
  Total repositories: 3494030
  Repositories returned: 30
❶ Keys: 73
  archive_url
  archived
  assignees_url
  --snip--
  url
  watchers
  watchers_count</code>
</pre>
<p>GitHub的API返回有关仓库的大量信息：<code>repo_dict</code>
 包含73个键（见❶）。通过仔细查看这些键，可大致知道可提取有关项目的哪些信息。（要准确地获悉API将返回哪些信息，要么阅读文档，要么像这里一样使用代码来查看。）</p>
<p>下面来提取<code>repo_dict</code>
 中与一些键相关联的值：</p>
<p>
<em>python_repos.py</em>
</p>
<pre class="代码无行号"><code>  --snip--
  # 研究有关仓库的信息。
  repo_dicts = response_dict['items']
  print(f"Repositories returned: {len(repo_dicts)}")
  # 研究第一个仓库。
  repo_dict = repo_dicts[0]
  print("\nSelected information about first repository:")
❶ print(f"Name: {repo_dict['name']}")
❷ print(f"Owner: {repo_dict['owner']['login']}")
❸ print(f"Stars: {repo_dict['stargazers_count']}")
  print(f"Repository: {repo_dict['html_url']}")
❹ print(f"Created: {repo_dict['created_at']}")
❺ print(f"Updated: {repo_dict['updated_at']}")
  print(f"Description: {repo_dict['description']}")</code>
</pre>
<p>这里打印的值对应于表示第一个仓库的字典中的很多键。在❶处，打印了项目的名称。项目所有者是由一个字典表示的，因此❷处使用键<code>owner</code>
 来访问表示所有者的字典，再使用键<code>key</code>
 来获取所有者的登录名。在❸处，打印项目获得了多少个星的评级，以及该项目GitHub仓库的URL。接下来，显示项目的创建时间（见❹）和最后一次更新的时间（见❺）。最后，打印仓库的描述。输出类似于下面这样：</p>
<pre class="代码无行号"><code>Status code: 200
Total repositories: 3494032
Repositories returned: 30
Selected information about first repository:
Name: awesome-python
Owner: vinta
Stars: 61549
Repository: https://github.com/vinta/awesome-python
Created: 2014-06-27T21:00:06Z
Updated: 2019-02-17T04:30:00Z
Description: A curated list of awesome Python frameworks, libraries, software
    and resources</code>
</pre>
<p>从上述输出可知，在本书编写期间，GitHub上星级最高的Python项目为awesome-python，其所有者为用户vinta，有60 000多位GitHub用户给这项目加星了。我们可以看到这个项目仓库的URL，其创建时间为2014年6月，且最近更新了。最后，描述指出项目awesome-python包含一系列深受欢迎的Python资源。</p>
<h3 id="nav_point_406">17.1.6　概述最受欢迎的仓库</h3>
<p>对这些数据进行可视化时，我们想涵盖多个仓库。下面就来编写一个循环，打印API调用返回的每个仓库的特定信息，以便能够在可视化中包含所有这些信息：</p>
<p>
<em>python_repos.py</em>
</p>
<pre class="代码无行号"><code>  --snip--
  # 研究有关仓库的信息。
  repo_dicts = response_dict['items']
  print(f"Repositories returned: {len(repo_dicts)}")
❶ print("\nSelected information about each repository:")
❷ for repo_dict in repo_dicts:
      print(f"\nName: {repo_dict['name']}")
      print(f"Owner: {repo_dict['owner']['login']}")
      print(f"Stars: {repo_dict['stargazers_count']}")
      print(f"Repository: {repo_dict['html_url']}")
      print(f"Description: {repo_dict['description']}")</code>
</pre>
<p>在❶处，打印了一条说明性消息。在❷处，遍历<code>repo_dicts</code>
 中的所有字典。在这个循环中，打印每个项目的名称、所有者、星级、在GitHub上的URL以及描述：</p>
<pre class="代码无行号"><code>Status code: 200
Total repositories: 3494040
Repositories returned: 30
Selected information about each repository:
Name: awesome-python
Owner: vinta
Stars: 61549
Repository: https://github.com/vinta/awesome-python
Description: A curated list of awesome Python frameworks, libraries, software
  and resources
Name: system-design-primer
Owner: donnemartin
Stars: 57256
Repository: https://github.com/donnemartin/system-design-primer
Description: Learn how to design large-scale systems. Prep for the system
  design interview. Includes Anki flashcards.
--snip--
Name: python-patterns
Owner: faif
Stars: 19058
Repository: https://github.com/faif/python-patterns
Description: A collection of design patterns/idioms in Python</code>
</pre>
<p>在上述输出中，有些有趣的项目可能值得一看。但不要在这上面花费太多时间，因为即将创建的可视化图表能让你更容易地看清结果。</p>
<h3 id="nav_point_407">17.1.7　监视API的速率限制</h3>
<p>大多数API存在速率限制，也就是说，在特定时间内可执行的请求数存在限制。要获悉是否接近了GitHub的限制，请在浏览器中输入https://api.github.com/rate_limit，你将看到类似于下面的响应：</p>
<pre class="代码无行号"><code>  {
    "resources": {
      "core": {
        "limit": 60,
        "remaining": 58,
        "reset": 1550385312
      },
❶     "search": {
❷       "limit": 10,
❸       "remaining": 8,
❹       "reset": 1550381772
      },
      --snip--</code>
</pre>
<p>我们关心的信息是搜索API的速率限制（见❶）。从❷处可知，极限为每分钟10个请求，而在当前分钟内，还可执行8个请求（见❸）。<code>reset</code>
 值指的是配额将重置的<strong>Unix时间</strong>
 或<strong>新纪元时间</strong>
 （1970年1月1日午夜后多少秒）（见❹）。用完配额后，你将收到一条简单的响应，由此知道已到达API极限。到达极限后，必须等待配额重置。</p>
<blockquote>
<p>
<strong>注意</strong>
 　很多API要求注册获得API密钥后才能执行API调用。本书编写期间，GitHub没有这样的要求，但获得API密钥后，配额将高得多。</p>
</blockquote>
<h2 id="nav_point_408">17.2　使用Plotly可视化仓库</h2>
<p>有了一些有趣的数据后，我们来进行可视化，呈现GitHub上Python项目的受欢迎程度。我们将创建一个交互式条形图：条形的高度表示项目获得了多少颗星。单击条形将带你进入项目在GitHub上的主页。请复制前面编写的python_repos_visual.py，并将副本修改成下面这样：</p>
<p>
<em>python_repos_visual.py</em>
</p>
<pre class="代码无行号"><code>  import requests
❶ from plotly.graph_objs import Bar
  from plotly import offline
❷ # 执行API调用并存储响应。
  url = 'https://api.github.com/search/repositories?q=language:python&amp;sort=stars'
  headers = {'Accept': 'application/vnd.github.v3+json'}
  r = requests.get(url, headers=headers)
  print(f"Status code: {r.status_code}")
  # 处理结果。
  response_dict = r.json()
  repo_dicts = response_dict['items']
❸ repo_names, stars = [], []
  for repo_dict in repo_dicts:
      repo_names.append(repo_dict['name'])
      stars.append(repo_dict['stargazers_count'])
  # 可视化。
❹ data = [{
      'type': 'bar',
      'x': repo_names,
      'y': stars,
  }]
❺ my_layout = {
      'title': 'GitHub上最受欢迎的Python项目',
      'xaxis': {'title': 'Repository'},
      'yaxis': {'title': 'Stars'},
  }
  fig = {'data': data, 'layout': my_layout}
  offline.plot(fig, filename='python_repos.html')</code>
</pre>
<p>在❶处，导入Plotly中的<code>Bar</code>
 类和模块<code>offline</code>
 。像第15章的绘制直方图项目中定义列表<code>data</code>
 那样，这里也使用字典来定义布局，因此不需要导入<code>Layout</code>
 类。这里也打印API响应的状态，以便知道是否出现了问题（见❷）。现在不是探索阶段，早已确定了所需的数据是存在的，因此删除部分处理API响应的代码。</p>
<p>接下来，创建两个空列表，用于存储要在图表中呈现的数据（见❸）。我们需要每个项目的名称，用于给条形添加标签，还需要知道项目获得了多少个星，用于指定条形的高度。在循环中，将每个项目的名称和星级分别附加到这两个列表末尾。</p>
<p>然后，定义列表<code>data</code>
 （见❹）。它像第16章的列表<code>data</code>
 一样包含一个字典，指定了图表的类型，并提供了<img alt="x" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/17_Image00003.gif"/>
 值和<img alt="y" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/17_Image00004.gif"/>
 值：<img alt="x" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/17_Image00003.gif"/>
 值为项目名称，<img alt="y" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/17_Image00004.gif"/>
 值为项目获得了多少个星。</p>
<p>在❺处，使用字典定义图表的布局。这里没有创建<code>Layout</code>
 实例，而是创建了一个包含布局规范的字典，并在其中指定了图表的名称以及每个坐标轴的标签。</p>
<p>生成的图表如图17-1所示。从中可知，前几个项目的受欢迎程度比其他项目高得多，但所有这些项目在Python生态系统中都很重要。</p>
<p class="图"><img alt="" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/17_Image00044.gif" style="width: 85%" width="85%"/>
</p>
<p>
<strong>图17-1　GitHub上最受欢迎的Python项目</strong>
</p>
<h3 id="nav_point_409">17.2.1　改进Plotly图表</h3>
<p>下面来改进这个图表的样式。第16章介绍过，可在<code>data</code>
 和<code>my_layout</code>
 中以键值对的形式指定各种样式。</p>
<p>通过修改<code>data</code>
 ，可定制条形。下面是修改后的<code>data</code>
 ，给条形指定了颜色和边框：</p>
<p>
<em>python_repos_visual.py</em>
</p>
<pre class="代码无行号"><code>--snip--
data = [{
    'type': 'bar',
    'x': repo_names,
    'y': stars,
    'marker': {
        'color': 'rgb(60, 100, 150)',
        'line': {'width': 1.5, 'color': 'rgb(25, 25, 25)'}
    },
    'opacity': 0.6,
}]
--snip--</code>
</pre>
<p>
<code>marker</code>
 设置影响条形设计。我们给条形指定了一种自定义的蓝色，加上了宽1.5像素的深灰色轮廓，还将条形的不透明度设置为0.6，以免图表过于惹眼。</p>
<p>下面来修改<code>my_layout</code>
 ：</p>
<p>
<em>python_repos_visual.py</em>
</p>
<pre class="代码无行号"><code>  --snip--
  my_layout = {
      'title': 'GitHub上最受欢迎的Python项目',
❶     'titlefont': {'size': 28},
❷     'xaxis': {
          'title': 'Repository',
          'titlefont': {'size': 24},
          'tickfont': {'size': 14},
      },
❸     'yaxis': {
          'title': 'Stars',
          'titlefont': {'size': 24},
          'tickfont': {'size': 14},
      },
  }
  --snip--</code>
</pre>
<p>使用键<code>'titlefont'</code>
 指定图表名称的字号（见❶）。在字典<code>'xaxis'</code>
 中，添加指定<img alt="x" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/17_Image00003.gif"/>
 轴标签字号的设置（<code>'titlefont'</code>
 ）和刻度标签字号的设置（<code>'tickfont'</code>
 ）（见❷）。由于这些设置嵌套在字典中，还可以使用相应的键指定坐标轴标签和刻度标签的颜色和字体。在❸处，给<img alt="y" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/17_Image00004.gif"/>
 轴指定类似的设置。</p>
<p>重新设置样式后的图表如图17-2所示。</p>
<p class="图"><img alt="" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/17_Image00045.gif" style="width: 85%" width="85%"/>
</p>
<p>
<strong>图17-2　改进了图表的样式</strong>
</p>
<h3 id="nav_point_410">17.2.2　添加自定义工具提示</h3>
<p>在Plotly中，将鼠标指向条形将显示其表示的信息。这通常称为<strong>工具提示</strong>
 。在本例中，当前显示的是项目获得了多少个星。下面来创建自定义工具提示，以显示项目的描述和所有者。</p>
<p>为生成这样的工具提示，需要再提取一些信息并修改对象<code>data</code>
 ：</p>
<p>
<em>python_repos_visual.py</em>
</p>
<pre class="代码无行号"><code>  --snip--
  # 处理结果。
  response_dict = r.json()
  repo_dicts = response_dict['items']
❶ repo_names, stars, labels = [], [], []
  for repo_dict in repo_dicts:
      repo_names.append(repo_dict['name'])
      stars.append(repo_dict['stargazers_count'])
❷     owner = repo_dict['owner']['login']
      description = repo_dict['description']
❸     label = f"{owner}&lt;br /&gt;{description}"
      labels.append(label)
  # 可视化。
  data = [{
      'type': 'bar',
      'x': repo_names,
      'y': stars,
❹     'hovertext': labels,
      'marker': {
          'color': 'rgb(60, 100, 150)',
          'line': {'width': 1.5, 'color': 'rgb(25, 25, 25)'}
      },
      'opacity': 0.6,
  }]
  --snip--</code>
</pre>
<p>首先新建一个空列表<code>labels</code>
 ，用于存储要给各个项目显示的文本（见❶）。在处理数据的循环中，提取每个项目的所有者和描述（见❷）。Plotly允许在文本元素中使用HTML代码，因此在创建由项目所有者和描述组成的字符串时，我们能够在这两部分之间添加换行符（<code>&lt;br /&gt;</code>
 ）（见❸）。然后，将这个字符串附加到列表<code>labels</code>
 末尾。</p>
<p>在列表<code>data</code>
 包含的字典中，添加了键<code>'hovertext'</code>
 ，并将与之关联的值指定为刚创建的列表（见❹）。Plotly创建每个条形时，将提取这个列表中的文本，并在观察者将鼠标指向条形时显示。这样修改后，生成的图表如图17-3所示。</p>
<p class="图"><img alt="" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/17_Image00046.gif" style="width: 85%" width="85%"/>
</p>
<p>
<strong>图17-3　将鼠标指向条形时，将显示项目的描述和所有者</strong>
</p>
<h3 id="nav_point_411">17.2.3　在图表中添加可单击的链接</h3>
<p>Plotly允许在文本元素中使用HTML，让你能够轻松地在图表中添加链接。下面将<img alt="x" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/17_Image00003.gif"/>
 轴标签作为链接，让观察者能够访问项目在GitHub上的主页。为此，需要提取URL并用其生成<img alt="x" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/17_Image00003.gif"/>
 轴标签：</p>
<p>
<em>python_repos_visual.py</em>
</p>
<pre class="代码无行号"><code>  --snip--
  # 处理结果。
  response_dict = r.json()
  repo_dicts = response_dict['items']
❶ repo_links, stars, labels = [], [], []
  for repo_dict in repo_dicts:
      repo_name = repo_dict['name']
❷     repo_url = repo_dict['html_url']
❸     repo_link = f"&lt;a href='{repo_url}'&gt;{repo_name}&lt;/a&gt;"
      repo_links.append(repo_link)
      stars.append(repo_dict['stargazers_count'])
      --snip--
  # 可视化。
  data = [{
      'type': 'bar',
❹     'x': repo_links,
      'y': stars,
      --snip--
  }]
  --snip--</code>
</pre>
<p>这里修改了列表的名称（从<code>repo_names</code>
 改为<code>repo_links</code>
 ），更准确地指出了要组合的信息（见❶）。接下来，从<code>repo_dict</code>
 中提取项目的URL，并将其赋给临时变量<code>repo_url</code>
 （见❷）。在❸处，创建一个指向项目的链接，为此使用了HTML标记<code>&lt;a&gt;</code>
 ，其格式为<code>&lt;a href='URL'&gt;link text&lt;/a&gt;</code>
 。然后，将这个链接附加到列表<code>repo_links</code>
 末尾。</p>
<p>在❹处，将这个列表用作图表的<img alt="x" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/17_Image00003.gif"/>
 值。生成的图表与前面相同，但观察者可单击图表底端的项目名，以访问项目在GitHub上的主页。至此，我们对API获取的数据生成了可视化图表——它是交互性的，包含丰富的信息！</p>
<h3 id="nav_point_412">17.2.4　深入了解Plotly和GitHub API</h3>
<p>要深入地了解如何生成Plotly图表，有两个不错的地方可以查看。第一个是Plotly User Guide in Python。通过研究该资源，可更深入地了解Plotly是如何使用数据来生成可视化图表的，以及它采取这种做法的原因。</p>
<p>第二个不错的资源是Plotly网站中的Python Figure Reference，其中列出了可用来配置Plotly可视化的所有设置。这里还列出了所有的图表类型，以及在各个配置选项中可设置的属性。</p>
<p>要更深入地了解GitHub API，可参阅其文档。通过阅读文档，你可以知道如何从GitHub提取各种信息。如果有GitHub账户，除了向公众提供的有关仓库的信息外，你还可以提取有关自己的信息。</p>
<h2 id="nav_point_413">17.3　Hacker News API</h2>
<p>为探索如何使用其他网站的API调用，我们来看看Hacker News。在Hacker News网站，用户分享编程和技术方面的文章，并就这些文章展开积极的讨论。Hacker News的API让你能够访问有关该网站所有文章和评论的信息，且不要求通过注册获得密钥。</p>
<p>下面的调用返回本书编写期间最热门的文章的信息：</p>
<pre class="代码无行号"><code>https://hacker-news.firebaseio.com/v0/item/19155826.json</code>
</pre>
<p>如果在浏览器中输入这个URL，将发现响应位于一对花括号内，表明这是一个字典。如果不改进格式，这样的响应难以阅读。下面像第16章的地震地图项目那样，通过方法<code>json.dump()</code>
 来运行这个URL，以便对返回的信息进行探索：</p>
<p>
<em>hn_article.py</em>
</p>
<pre class="代码无行号"><code>import requests
import json
# 执行API调用并存储响应。
url = 'https://hacker-news.firebaseio.com/v0/item/19155826.json'
r = requests.get(url)
print(f"Status code: {r.status_code}")
# 探索数据的结构。
response_dict = r.json()
readable_file = 'data/readable_hn_data.json'
with open(readable_file, 'w') as f:
    json.dump(response_dict, f, indent=4)</code>
</pre>
<p>这里的所有代码都在前两章使用过，你应该不会感到陌生。输出是一个字典，其中包含有关ID为19155826的文章的信息：</p>
<p>
<em>readable_hn_data.json</em>
</p>
<pre class="代码无行号"><code>  {
      "by": "jimktrains2",
❶     "descendants": 220,
      "id": 19155826,
❷     "kids": [
          19156572,
          19158857,
          --snip--
      ],
      "score": 722,
      "time": 1550085414,
❸     "title": "Nasa's Mars Rover Opportunity Concludes a 15-Year Mission",
      "type": "story",
❹     "url": "https://www.nytimes.com/.../mars-opportunity-rover-dead.html"
  }</code>
</pre>
<p>这个字典包含很多键。与键<code>'descendants'</code>
 相关联的值是文章被评论的次数（见❶）。与键<code>'kids'</code>
 相关联的值包含文章所有评论的ID（见❷）。每个评论本身也可能有评论，因此文章的后代（descendant）数量可能比其<code>'kids'</code>
 的数量多。这个字典中还包含当前文章的标题（见❸）和URL（见❹）。</p>
<p>下面的URL返回一个列表，其中包含Hacker News上当前排名靠前的文章的ID：</p>
<pre class="代码无行号"><code>https://hacker-news.firebaseio.com/v0/topstories.json</code>
</pre>
<p>通过使用这个调用，可获悉当前有哪些文章位于主页，再生成一系列类似于前面的API调用。通过使用这种方法，可概述当前位于Hacker News主页的每篇文章：</p>
<p>
<em>hn_submissions.py</em>
</p>
<pre class="代码无行号"><code>  from operator import itemgetter
  import requests
  # 执行API调用并存储响应。
❶ url = 'https://hacker-news.firebaseio.com/v0/topstories.json'
  r = requests.get(url)
  print(f"Status code: {r.status_code}")
  # 处理有关每篇文章的信息。
❷ submission_ids = r.json()
❸ submission_dicts = []
  for submission_id in submission_ids[:30]:
      # 对于每篇文章，都执行一个API调用。
❹     url = f"https://hacker-news.firebaseio.com/v0/item/{submission_id}.json"
      r = requests.get(url)
      print(f"id: {submission_id}\tstatus: {r.status_code}")
      response_dict = r.json()
      # 对于每篇文章，都创建一个字典。
❺     submission_dict = {
          'title': response_dict['title'],
          'hn_link': f"http://news.ycombinator.com/item?id={submission_id}",
          'comments': response_dict['descendants'],
      }
❻     submission_dicts.append(submission_dict)
❼ submission_dicts = sorted(submission_dicts, key=itemgetter('comments'),
                              reverse=True)
❽ for submission_dict in submission_dicts:
      print(f"\nTitle: {submission_dict['title']}")
      print(f"Discussion link: {submission_dict['hn_link']}")
      print(f"Comments: {submission_dict['comments']}")</code>
</pre>
<p>首先，执行一个API调用，并打印响应的状态（见❶）。这个API调用返回一个列表，其中包含Hacker News上当前最热门的500篇文章的ID。接下来，将响应对象转换为一个Python列表（见❷），并将其存储在<code>submission_ids</code>
 中。后面将使用这些ID来创建一系列字典，其中每个字典都存储了一篇文章的信息。</p>
<p>在❸处，创建一个名为<code>submission_dicts</code>
 的空列表，用于存储前面所说的字典。接下来，遍历前30篇文章的ID。对于每篇文章，都执行一个API调用，其中的URL包含<code>submission_id</code>
 的当前值（见❹）。我们打印请求的状态和文章ID，以便知道请求是否成功。</p>
<p>在❺处，为当前处理的文章创建一个字典，并在其中存储文章的标题、讨论页面的链接和评论数。然后，将<code>submission_dict</code>
 附加到<code>submission_dicts</code>
 末尾（见❻）。</p>
<p>Hacker News上的文章是根据总体得分排名的，而总体得分取决于很多因素，包含被推荐的次数、评论数和发表时间。我们要根据评论数对字典列表<code>submission_dicts</code>
 进行排序，为此使用了模块<code>operator</code>
 中的函数<code>itemgetter()</code>
 （见❼）。我们向这个函数传递了键<code>'comments'</code>
 ，因此它从该列表的每个字典中提取与键<code>'comments'</code>
 关联的值。这样，函数<code>sorted()</code>
 将根据这个值对列表进行排序。我们将列表按降序排列，即评论最多的文章位于最前面。</p>
<p>对列表排序后遍历它（见❽），并打印每篇热门文章的三项信息：标题、讨论页面的链接和评论数：</p>
<pre class="代码无行号"><code>Status code: 200
id: 19155826    status: 200
id: 19180181    status: 200
id: 19181473    status: 200
--snip--
Title: Nasa's Mars Rover Opportunity Concludes a 15-Year Mission
Discussion link: http://news.ycombinator.com/item?id=19155826
Comments: 220
Title: Ask HN: Is it practical to create a software-controlled model rocket?
Discussion link: http://news.ycombinator.com/item?id=19180181
Comments: 72
Title: Making My Own USB Keyboard from Scratch
Discussion link: http://news.ycombinator.com/item?id=19181473
Comments: 62
--snip--</code>
</pre>
<p>无论使用哪个API来访问和分析信息，流程都与此类似。有了这些数据后，就可进行可视化，指出最近哪些文章引发了最激烈的讨论。基于这种方式，应用程序可以为用户提供网站（如Hacker News）的定制化阅读体验。要深入了解通过Hacker News API可访问哪些信息，请参阅其文档页面。</p>
<blockquote>
<p>
<strong>动手试一试</strong>
</p>
<p>
<strong>练习17-1：其他语言</strong>
 　修改python_repos.py中的API调用，使其在生成的图表中显示其他语言最受欢迎的项目。请尝试语言JavaScript、Ruby、C、Java、Perl、Haskell和Go。</p>
<p>
<strong>练习17-2：最活跃的讨论</strong>
 　使用hn_submissions.py中的数据，创建一个条形图，显示Hacker News上当前最活跃的讨论。条形的高度应对应于文章的评论数。条形的标签应包含文章的标题，并且充当到文章讨论页面的链接。</p>
<p>
<strong>练习17-3：测试python_repos.py</strong>
 　在python_repos.py中，我们打印了<code>status_code</code>
 的值，以核实API调用是否成功。请编写一个名为test_python_repos.py的程序，它使用单元测试来断言<code>status_code</code>
 的值为200。想想还可做出哪些断言，如返回的条目（item）数符合预期，仓库总数超过特定的值，等等。</p>
<p>
<strong>练习17-4：进一步探索</strong>
 　查看Plotly以及GitHub API或Hacker News API的文档，再根据从中获得的信息来定制本节绘制的图表的样式，或者提取并可视化其他数据。</p>
</blockquote>
<h2 id="nav_point_414">17.4　小结</h2>
<p>在本章中，你学习了：如何使用API来编写独立的程序，以自动采集所需的数据并对其进行可视化；使用GitHub API来探索GitHub上星级最高的Python项目，还大致地了解了Hacker News API；如何使用Requests包来自动执行GitHub API调用，以及如何处理调用的结果。本章还简要介绍了一些Plotly设置，可用其进一步定制生成的图表的外观。</p>
<p>在本书的最后一个项目中，我们将使用Django来创建一个Web应用程序。</p>
<br style="page-break-after:always"/>
<div></div>
]]></description>
    <pubDate>Tue, 28 Jul 2026 01:14:36 +0000</pubDate>
    <guid isPermaLink="false">17</guid>
</item>
<item>
    <title>Basic Python - 016 - 第 16 章 下载数据.epub</title>
    <description><![CDATA[<p style='color: #888;'>⏱ 预计阅读时间：112 分钟 (22223 字)</p><hr><h1 id="nav_point_373">第 16 章　下载数据</h1>
<blockquote>
<p><img alt="" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/16_Image00001.gif" style="width: 16%" width="16%"/>
 　本章将从网上下载数据，并对其进行可视化。网上的数据多得令人难以置信，大多未经仔细检查。如果能够对这些数据进行分析，就能发现别人没有发现的规律和关联。</p>
<p>本章将访问并可视化的数据以两种常见格式存储：CSV和JSON。我们将使用Python模块<code>csv</code>
 来处理以CSV格式存储的天气数据，找出两个地区在一段时间内的最高温度和最低温度。然后，使用Matplotlib根据下载的数据创建一个图表，展示两个不同地区的温度变化：阿拉斯加州锡特卡和加利福尼亚州死亡谷。然后，使用模块<code>json</code>
 访问以JSON格式存储的地震数据，并使用Plotly绘制一幅散点图，展示这些地震的位置和震级。</p>
<p>阅读本章后，你将能够处理各种类型和格式的数据集，并对如何创建复杂的图表有深入的认识。要处理各种真实的数据集，必须能够访问并可视化各种类型和格式的在线数据。</p>
</blockquote>
<h2 id="nav_point_374">16.1　CSV文件格式</h2>
<p>要在文本文件中存储数据，一个简单方式是将数据作为一系列<strong>以逗号分隔的值</strong>
 （comma-separated values）写入文件。这样的文件称为<strong>CSV</strong>
 文件。例如，下面是一行CSV格式的天气数据：</p>
<pre class="代码无行号"><code>"USW00025333","SITKA AIRPORT, AK US","2018-01-01","0.45",,"48","38"</code>
</pre>
<p>这是阿拉斯加州锡特卡2018年1月1日的天气数据，其中包含当天的最高温度和最低温度，还有众多其他的数据。CSV文件对人来说阅读起来比较麻烦，但程序可轻松提取并处理其中的值，有助于加快数据分析过程。</p>
<p>我们将首先处理少量CSV格式的锡特卡天气数据，这些数据可在本书的配套资源（ituring.cn/ book/2784）中找到。请将文件sitka_weather_07-2018_simple.csv复制到存储本章程序的文件夹中。（下载本书的配套资源后，就有了这个项目所需的所有文件。）</p>
<blockquote>
<p>
<strong>注意</strong>
 　该项目使用的天气数据来自美国国家海洋与大气管理局（National Oceanic and Atmospheric Administration，NOAA）。</p>
</blockquote>
<h3 id="nav_point_375">16.1.1　分析CSV文件头</h3>
<p>
<code>csv</code>
 模块包含在Python标准库中，可用于分析CSV文件中的数据行，让我们能够快速提取感兴趣的值。先来查看这个文件的第一行，其中的一系列文件头指出了后续各行包含的是什么样的信息：</p>
<p>
<em>sitka_highs.py</em>
</p>
<pre class="代码无行号"><code>  import csv
  filename = 'data/sitka_weather_07-2018_simple.csv'
❶ with open(filename) as f:
❷     reader = csv.reader(f)
❸     header_row = next(reader)
      print(header_row)</code>
</pre>
<p>导入模块<code>csv</code>
 后，将要使用的文件的名称赋给<code>filename</code>
 。接下来，打开这个文件，并将返回的文件对象赋给<code>f</code>
 （见❶）。然后，调用<code>csv.reader()</code>
 并将前面存储的文件对象作为实参传递给它，从而创建一个与该文件相关联的阅读器对象（见❷）。这个阅读器对象被赋给了<code>reader</code>
 。</p>
<p>模块<code>csv</code>
 包含函数<code>next()</code>
 ，调用它并传入阅读器对象时，它将返回文件中的下一行。在上述代码中，只调用了<code>next()</code>
 一次，因此得到的是文件的第一行，其中包含文件头（见❸）。将返回的数据存储到<code>header_row</code>
 中。如你所见，<code>header_row</code>
 包含与天气相关的文件头，指出了每行都包含哪些数据：</p>
<pre class="代码无行号"><code>['STATION', 'NAME', 'DATE', 'PRCP', 'TAVG', 'TMAX', 'TMIN']</code>
</pre>
<p>
<code>reader</code>
 处理文件中以逗号分隔的第一行数据，并将每项数据都作为一个元素存储在列表中。文件头<code>STATION</code>
 表示记录数据的气象站的编码。这个文件头的位置表明，每行的第一个值都是气象站编码。文件头<code>NAME</code>
 指出每行的第二个值都是记录数据的气象站的名称。其他文件头则指出记录了哪些信息。当前，我们最关心的是日期（<code>DATE</code>
 ）、最高温度（<code>TMAX</code>
 ）和最低温度（<code>TMIN</code>
 ）。这是一个简单的数据集，只包含降水量以及与温度相关的数据。你自己下载天气数据时，可选择涵盖众多测量值，如风速、风向以及详细的降水量数据。</p>
<h3 id="nav_point_376">16.1.2　打印文件头及其位置</h3>
<p>为了让文件头数据更容易理解，将列表中的每个文件头及其位置打印出来：</p>
<p>
<em>sitka_highs.py</em>
</p>
<pre class="代码无行号"><code>  --snip--
  with open(filename) as f:
      reader = csv.reader(f)
      header_row = next(reader)
❶     for index, column_header in enumerate(header_row):
          print(index, column_header)</code>
</pre>
<p>在循环中，对列表调用了<code>enumerate()</code>
 （见❶）来获取每个元素的索引及其值。（请注意，我们删除了代码行<code>print(header_row)</code>
 ，转而显示这个更详细的版本。）</p>
<p>输出如下，指出了每个文件头的索引：</p>
<pre class="代码无行号"><code>0 STATION
1 NAME
2 DATE
3 PRCP
4 TAVG
5 TMAX
6 TMIN</code>
</pre>
<p>从中可知，日期和最高温度分别存储在第三列和第六列。为研究这些数据，我们将处理sitka_weather_07-2018_simple.csv中的每行数据，并提取其中索引为2和5的值。</p>
<h3 id="nav_point_377">16.1.3　提取并读取数据</h3>
<p>知道需要哪些列中的数据后，我们来读取一些数据。首先，读取每天的最高温度：</p>
<p>
<em>sitka_highs.py</em>
</p>
<pre class="代码无行号"><code>  --snip--
  with open(filename) as f:
      reader = csv.reader(f)
      header_row = next(reader)
      # 从文件中获取最高温度。
❶     highs = []
❷     for row in reader:
❸         high = int(row[5])
          highs.append(high)
  print(highs)</code>
</pre>
<p>创建一个名为<code>highs</code>
 的空列表（见❶），再遍历文件中余下的各行（见❷）。阅读器对象从其停留的地方继续往下读取CSV文件，每次都自动返回当前所处位置的下一行。由于已经读取了文件头行，这个循环将从第二行开始——从这行开始包含的是实际数据。每次执行循环时，都将索引5处（<code>TMAX</code>
 列）的数据附加到<code>highs</code>
 末尾（见❸）。在文件中，这项数据是以字符串格式存储的，因此在附加到<code>highs</code>
 末尾前，使用函数<code>int()</code>
 将其转换为数值格式，以便使用。</p>
<p>
<code>highs</code>
 现在存储的数据如下：</p>
<pre class="代码无行号"><code>[62, 58, 70, 70, 67, 59, 58, 62, 66, 59, 56, 63, 65, 58, 56, 59, 64, 60, 60,
 61, 65, 65, 63, 59, 64, 65, 68, 66, 64, 67, 65]</code>
</pre>
<p>提取每天的最高温度并将其存储到列表中之后，就可以可视化这些数据了。</p>
<h3 id="nav_point_378">16.1.4　绘制温度图表</h3>
<p>为可视化这些温度数据，首先使用Matplotlib创建一个显示每日最高温度的简单图形，如下所示：</p>
<p>
<em>sitka_highs.py</em>
</p>
<pre class="代码无行号"><code>  import csv
  import matplotlib.pyplot as plt
  filename = 'data/sitka_weather_07-2018_simple.csv'
  with open(filename) as f:
      --_snip_—
  # 根据最高温度绘制图形。
  plt.style.use('seaborn')
  fig, ax = plt.subplots()
❶ ax.plot(highs, c='red')
  # 设置图形的格式。
❷ ax.set_title("2018年7月每日最高温度", fontsize=24)
❸ ax.set_xlabel('', fontsize=16)
  ax.set_ylabel("温度 (F)", fontsize=16)
  ax.tick_params(axis='both', which='major', labelsize=16)
  plt.show()</code>
</pre>
<p>将最高温度列表传给<code>plot()</code>
 （见❶），并传递<code>c='red'</code>
 以便将数据点绘制为红色。（这里使用红色显示最高温度，用蓝色显示最低温度。）接下来，设置了一些其他的格式，如名称和字号（见❷），这些都在第15章介绍过。鉴于还没有添加日期，因此没有给<img alt="x" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/16_Image00003.gif"/>
 轴添加标签，但<code>ax.set_xlabel()</code>
 确实修改了字号，让默认标签更容易看清❸。图16-1显示了绘制的图表：一个简单的折线图，显示了阿拉斯加州锡特卡2018年7月的每日最高温度。</p>
<p class="图"><img alt="" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/16_Image00034.gif" style="width: 82%" width="82%"/>
</p>
<p>
<strong>图16-1　阿拉斯加州锡特卡2018年7月每日最高温度折线图</strong>
</p>
<h3 id="nav_point_379">16.1.5　模块<code>datetime</code>
</h3>
<p>下面在图表中添加日期，使其更有用。在天气数据文件中，第一个日期在第二行：</p>
<pre class="代码无行号"><code>"USW00025333","SITKA AIRPORT, AK US","2018-07-01","0.25",,"62","50"</code>
</pre>
<p>读取该数据时，获得的是一个字符串，因此需要想办法将字符串<code>"2018-7-1"</code>
 转换为一个表示相应日期的对象。为创建一个表示2018年7月1日的对象，可使用模块<code>datetime</code>
 中的方法<code>strptime()</code>
 。我们在终端会话中看看<code>strptime()</code>
 的工作原理：</p>
<pre class="代码无行号"><code>&gt;&gt;&gt; from datetime import datetime
&gt;&gt;&gt; first_date = datetime.strptime('2018-07-01', '%Y-%m-%d')
&gt;&gt;&gt; print(first_date)
2018-07-01 00:00:00</code>
</pre>
<p>首先导入模块<code>datetime</code>
 中的<code>datetime</code>
 类，再调用方法<code>strptime()</code>
 ，并将包含所需日期的字符串作为第一个实参。第二个实参告诉Python如何设置日期的格式。在这里，<code>'%Y-'</code>
 让Python将字符串中第一个连字符前面的部分视为四位的年份，<code>'%m-'</code>
 让Python将第二个连字符前面的部分视为表示月份的数，<code>'%d'</code>
 让Python将字符串的最后一部分视为月份中的一天（1～31）。</p>
<p>方法<code>strptime()</code>
 可接受各种实参，并根据它们来决定如何解读日期。表16-1列出了这样的一些实参。</p>
<p>
<strong>表16-1　模块<code>datetime</code>
 中设置日期和时间格式的实参</strong>
</p>
<table border="1" width="90%">
<thead>
<tr>
<th>
<p class="表头单元格">实参</p>
</th>
<th>
<p class="表头单元格">含义</p>
</th>
</tr>
</thead>
<tbody>
<tr>
<td>
<p class="表格单元格">
<code>%A</code>
</p>
</td>
<td>
<p class="表格单元格">星期几，如Monday</p>
</td>
</tr>
<tr>
<td>
<p class="表格单元格">
<code>%B</code>
</p>
</td>
<td>
<p class="表格单元格">月份名，如January</p>
</td>
</tr>
<tr>
<td>
<p class="表格单元格">
<code>%m</code>
</p>
</td>
<td>
<p class="表格单元格">用数表示的月份（01～12）</p>
</td>
</tr>
<tr>
<td>
<p class="表格单元格">
<code>%d</code>
</p>
</td>
<td>
<p class="表格单元格">用数表示的月份中的一天（01～31）</p>
</td>
</tr>
<tr>
<td>
<p class="表格单元格">
<code>%Y</code>
</p>
</td>
<td>
<p class="表格单元格">四位的年份，如2019</p>
</td>
</tr>
<tr>
<td>
<p class="表格单元格">
<code>%y</code>
</p>
</td>
<td>
<p class="表格单元格">两位的年份，如19</p>
</td>
</tr>
<tr>
<td>
<p class="表格单元格">
<code>%H</code>
</p>
</td>
<td>
<p class="表格单元格">24小时制的小时数（00～23）</p>
</td>
</tr>
<tr>
<td>
<p class="表格单元格">
<code>%I</code>
</p>
</td>
<td>
<p class="表格单元格">12小时制的小时数（01～12）</p>
</td>
</tr>
<tr>
<td>
<p class="表格单元格">
<code>%p</code>
</p>
</td>
<td>
<p class="表格单元格">am或pm</p>
</td>
</tr>
<tr>
<td>
<p class="表格单元格">
<code>%M</code>
</p>
</td>
<td>
<p class="表格单元格">分钟数（00～59）</p>
</td>
</tr>
<tr>
<td>
<p class="表格单元格">
<code>%S</code>
</p>
</td>
<td>
<p class="表格单元格">秒数（00～61）</p>
</td>
</tr>
</tbody>
</table>
<h3 id="nav_point_380">16.1.6　在图表中添加日期</h3>
<p>现在，可以通过提取日期和最高温度并将其传递给<code>plot()</code>
 ，对温度图形进行改进，如下所示：</p>
<p>
<em>sitka_highs.py</em>
</p>
<pre class="代码无行号"><code>  import csv
  from datetime import datetime
  import matplotlib.pyplot as plt
  filename = 'data/sitka_weather_07-2018_simple.csv'
  with open(filename) as f:
      reader = csv.reader(f)
      header_row = next(reader)
      # 从文件中获取日期和最高温度。
❶     dates, highs = [], []
      for row in reader:
❷         current_date = datetime.strptime(row[2], '%Y-%m-%d')
          high = int(row[5])
          dates.append(current_date)
          highs.append(high)
  # 根据最高温度绘制图形。
  plt.style.use('seaborn')
  fig, ax = plt.subplots()
❸ ax.plot(dates, highs, c='red')
  # 设置图形的格式。
  ax.set_title("2018年7月每日最高温度", fontsize=24)
  ax.set_xlabel('', fontsize=16)
❹ fig.autofmt_xdate()
  ax.set_ylabel("温度 (F)", fontsize=16)
  ax.tick_params(axis='both', which='major', labelsize=16)
  plt.show()</code>
</pre>
<p>我们创建了两个空列表，用于存储从文件中提取的日期和最高温度（见❶）。然后，将包含日期信息的数据（<code>row[0]</code>
 ）转换为<code>datetime</code>
 对象（见❷），并将其附加到列表<code>dates</code>
 末尾。在❸处，将日期和最高温度值传递给<code>plot()</code>
 。在❹处，调用<code>fig.autofmt_xdate()</code>
 来绘制倾斜的日期标签，以免其彼此重叠。图16-2显示了改进后的图表。</p>
<p class="图"><img alt="" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/16_Image00035.gif" style="width: 82%" width="82%"/>
</p>
<p>
<strong>图16-2　现在图表的<img alt="x" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/16_Image00003.gif"/>
 轴上有日期，含义更丰富</strong>
</p>
<h3 id="nav_point_381">16.1.7　涵盖更长的时间</h3>
<p>设置好图表后，我们来添加更多的数据，生成一幅更复杂的锡特卡天气图。请将文件sitka_weather_2018_simple.csv复制到本章程序所在的文件夹，该文件包含整年的锡特卡天气数据。</p>
<p>现在可创建覆盖整年的天气图了：</p>
<p>
<em>sitka_highs.py</em>
</p>
<pre class="代码无行号"><code>  --snip--
❶ filename = 'data/sitka_weather_2018_simple.csv'
  with open(filename) as f:
  --snip--
  # 设置图形的格式。
❷ ax.set_title("2018年每日最高温度", fontsize=24)
  ax.set_xlabel('', fontsize=16)
  --snip--</code>
</pre>
<p>这里修改了文件名，以使用数据文件sitka_weather_2018_simple.csv（见❶），还修改了图表的标题，以反映其内容的变化（见❷）。图16-3显示了生成的图形。</p>
<p class="图"><img alt="" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/16_Image00036.gif" style="width: 82%" width="82%"/>
</p>
<p>
<strong>图16-3　一年的天气数据</strong>
</p>
<h3 id="nav_point_382">16.1.8　再绘制一个数据系列</h3>
<p>虽然改进后的图表已经显示了丰富的数据，但是还能再添加最低温度数据，使其更有用。为此，需要从数据文件中提取最低温度，并将它们添加到图表中，如下所示：</p>
<p>
<em>sitka_highs_lows.py</em>
</p>
<pre class="代码无行号"><code>  --snip--
  filename = 'sitka_weather_2018_simple.csv'
  with open(filename) as f:
      reader = csv.reader(f)
      header_row = next(reader)
      # 从文件中获取日期、最高温度和最低温度。
❶     dates, highs, lows = [], [], []
      for row in reader:
          current_date = datetime.strptime(row[2], '%Y-%m-%d')
          high = int(row[5])
❷         low = int(row[6])
          dates.append(current_date)
          highs.append(high)
          lows.append(low)
  # 根据最高温度和最低温度绘制图形。
  plt.style.use('seaborn')
  fig, ax = plt.subplots()
  ax.plot(dates, highs, c='red')
❸ ax.plot(dates, lows, c='blue')
  # 设置图形的格式。
❹ ax.set_title("2018年每日最高温度", fontsize=24)
  --snip--</code>
</pre>
<p>在❶处，添加空列表<code>lows</code>
 ，用于存储最低温度。接下来，从每行的第七列（<code>row[6]</code>
 ）提取最低温度并存储（见❷）。在❸处，添加调用<code>plot()</code>
 的代码，以使用蓝色绘制最低温度。最后，修改标题（见❹）。图16-4显示了这样绘制出来的图表。</p>
<p class="图"><img alt="" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/16_Image00037.gif" style="width: 82%" width="82%"/>
</p>
<p>
<strong>图16-4　在一个图表中包含两个数据系列</strong>
</p>
<h3 id="nav_point_383">16.1.9　给图表区域着色</h3>
<p>添加两个数据系列后，就可以知道每天的温度范围了。下面来给这个图表做最后的修饰，通过着色来呈现每天的温度范围。为此，将使用方法<code>fill_between()</code>
 。它接受一个<img alt="x" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/16_Image00003.gif"/>
 值系列和两个<img alt="y" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/16_Image00004.gif"/>
 值系列，并填充两个<img alt="y" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/16_Image00004.gif"/>
 值系列之间的空间：</p>
<p>
<em>sitka_highs_lows.py</em>
</p>
<pre class="代码无行号"><code>  --snip--
  # 根据最低温度和最高温度绘制图形。
  plt.style.use('seaborn')
  fig, ax = plt.subplots()
❶ ax.plot(dates, highs, c='red', alpha=0.5)
  ax.plot(dates, lows, c='blue', alpha=0.5)
❷ ax.fill_between(dates, highs, lows, facecolor='blue', alpha=0.1)
  --snip--</code>
</pre>
<p>❶处的实参<code>alpha</code>
 指定颜色的透明度。<code>alpha</code>
 值为0表示完全透明，为1（默认设置）表示完全不透明。通过将<code>alpha</code>
 设置为0.5，可让红色和蓝色折线的颜色看起来更浅。</p>
<p>在❷处，向<code>fill_between()</code>
 传递一个<img alt="x" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/16_Image00003.gif"/>
 值系列（列表<code>dates</code>
 ），以及两个<img alt="y" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/16_Image00004.gif"/>
 值系列（<code>highs</code>
 和<code>lows</code>
 ）。实参facecolor指定填充区域的颜色，还将<code>alpha</code>
 设置成了较小的值0.1，让填充区域将两个数据系列连接起来的同时不分散观察者的注意力。图16-5显示了最高温度和最低温度之间的区域被填充后的图表。</p>
<p class="图"><img alt="" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/16_Image00038.gif" style="width: 82%" width="82%"/>
</p>
<p>
<strong>图16-5　给两个数据集之间的区域着色</strong>
</p>
<p>着色让两个数据集之间的区域变得更显眼了。</p>
<h3 id="nav_point_384">16.1.10　错误检查</h3>
<p>我们应该能够使用任何地方的天气数据来运行sitka_highs_lows.py中的代码，但有些气象站收集的数据种类不同，有些气象站会偶尔出现故障，未能收集部分或全部应收集的数据。缺失数据可能引发异常，如果不妥善处理，可能导致程序崩溃。</p>
<p>例如，来看看生成加利福尼亚州死亡谷的温度图时出现的情况。请将文件death_valley_2018_simple.csv复制到本章程序所在的文件夹。</p>
<p>首先通过编写代码来查看这个数据文件包含的文件头：</p>
<p>
<em>death_valley_highs</em>
</p>
<pre class="代码无行号"><code>import csvlows.py_
filename = 'data/death_valley_2018_simple.csv'
with open(filename) as f:
    reader = csv.reader(f)
    header_row = next(reader)
    for index, column_header in enumerate(header_row):
        print(index, column_header)</code>
</pre>
<p>输出如下：</p>
<pre class="代码无行号"><code>0 STATION
1 NAME
2 DATE
3 PRCP
4 TMAX
5 TMIN
6 TOBS</code>
</pre>
<p>与前面一样，日期也在索引2处，但最高温度和最低温度分别在索引4和索引5处，因此需要修改代码中的索引，以反映这一点。另外，这个气象站没有记录平均温度，而记录了<code>TOBS</code>
 ，即特定时点的温度。</p>
<p>为演示缺失数据时将出现的状况，我故意从这个文件中删除了一项温度数据。下面来修改sitka_highs_lows.py，使用前面所说的索引来生成死亡谷的天气图，看看将出现什么状况：</p>
<p>
<em>death_valley_highs_lows.py</em>
</p>
<pre class="代码无行号"><code>  --snip--
  filename = 'data/death_valley_2018_simple.csv'
  with open(filename) as f:
      --snip--
      # 从文件中获取日期、最高温度和最低温度。
      dates, highs, lows = [], [], []
      for row in reader:
          current_date = datetime.strptime(row[2], '%Y-%m-%d')
❶     high = int(row[4])
      low = int(row[5])
      dates.append(current_date)
  --snip--</code>
</pre>
<p>在❶处，修改索引，使其对应于这个文件中<code>TMAX</code>
 和<code>TMIN</code>
 的位置。</p>
<p>运行这个程序时出现了错误，如下述输出的最后一行所示：</p>
<pre class="代码无行号"><code>Traceback (most recent call last):
  File "death_valley_highs_lows.py", line 15, in &lt;module&gt;
    high = int(row[4])
ValueError: invalid literal for int() with base 10: ''</code>
</pre>
<p>该traceback指出，Python无法处理其中一天的最高温度，因为无法将空字符串（<code>''</code>
 ）转换为整数。我们只要看一下文件death_valley_2018_simple.csv，就知道缺失了哪项数据，但这里不这样做，而是直接对缺失数据的情形进行处理。</p>
<p>为此，在从CSV文件中读取值时执行错误检查代码，对可能出现的异常进行处理，如下所示：</p>
<p>
<em>death_valley_highs_lows.py</em>
</p>
<pre class="代码无行号"><code>  --snip--
  filename = 'data/death_valley_2018_simple.csv'
  with open(filename) as f:
      --snip--
      for row in reader:
              current_date = datetime.strptime(row[2], '%Y-%m-%d')
❶             try:
                  high = int(row[4])
                  low = int(row[5])
              except ValueError:
❷                 print(f"Missing data for {current_date}")
❸             else:
                  dates.append(current_date)
                  highs.append(high)
                  lows.append(low)
  # 根据最高温度和最低温度绘制图形。
  --snip--
  # 设置图形的格式。
❹ title = "2018年每日最高温度和最低温度\n美国加利福尼亚州死亡谷"
  ax.set_title(title, fontsize=20)
  ax.set_xlabel('', fontsize=16)
  --snip--</code>
</pre>
<p>对于每一行，都尝试从中提取日期、最高温度和最低温度（见❶）。只要缺失其中一项数据，Python就会引发<code>ValueError</code>
 异常。我们这样进行处理：打印一条错误消息，指出缺失数据的日期（见❷）。打印错误消息后，循环将接着处理下一行。如果获取特定日期的所有数据时没有发生错误，就运行<code>else</code>
 代码块，将数据附加到相应列表的末尾（见❸）。这里绘图时使用的是有关另一个地方的信息，因此修改标题以指出这个地方。因为标题更长，所以我们缩小了字号（见❹）。</p>
<p>如果现在运行death_valley_highs_lows.py，将发现缺失数据的日期只有一个：</p>
<pre class="代码无行号"><code>Missing data for 2018-02-18 00:00:00</code>
</pre>
<p>妥善地处理错误后，代码能够生成图形并忽略缺失数据的那天。图16-6显示了绘制出的图形。</p>
<p class="图"><img alt="" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/16_Image00039.gif" style="width: 82%" width="82%"/>
</p>
<p>
<strong>图16-6　死亡谷每天的最高温度和最低温度</strong>
</p>
<p>将这个图表与锡特卡的图表进行比较可知，总体而言，死亡谷比阿拉斯加东南部暖和，这符合预期。同时，死亡谷沙漠中每天的温差也更大——从着色区域的高度可以看出这一点。</p>
<p>你使用的很多数据集都可能缺失数据、格式不正确或数据本身不正确。对于这样的情形，可使用本书前半部分介绍的工具来处理。在这里，使用了一个<code>try-except-else</code>
 代码块来处理数据缺失的问题。在有些情况下，需要使用<code>continue</code>
 来跳过一些数据，或者使用<code>remove()</code>
 或<code>del</code>
 将已提取的数据删除。只要能进行精确而有意义的可视化，采用任何管用的方法都是可以的。</p>
<h3 id="nav_point_385">16.1.11　自己动手下载数据</h3>
<p>如果你想自己下载天气数据，可采取如下步骤。</p>
<p>(1) 访问网站NOAA Climate Data Online。在Discover Data By部分，单击Search Tool。在下拉列表Select a Dataset中，选择Daily Summaries。</p>
<p>(2) 选择一个日期范围，在Search For下拉列表中ZIP Codes，输入你感兴趣地区的邮政编码，再单击Search按钮。</p>
<p>(3) 在下一个页面中，你将看到指定地区的地图和相关信息。单击地区名下方的View Full Details或单击地图再单击Full Details。</p>
<p>(4) 向下滚动并单击Station List，以显示该地区的气象站，再选择一个气象站并单击Add to Cart。虽然这个网站使用了购物车图标，但提供的数据是免费的。单击右上角的购物车。</p>
<p>(5) 在Select the Output中选择Custom GHCN-Daily CSV。确认日期范围无误后单击Continue。</p>
<p>(6) 在下一个页面中，可选择要下载的数据类型。可以只下载一种数据（如气温），也可以下载该气象站提供的所有数据。做出选择后单击Continue。</p>
<p>(7) 在最后一个页面，你将看到订单小结。请输入你的电子邮箱地址，再单击Submit Order。你将收到一封确认邮件，指出收到了你的订单。几分钟后，你将收到另一封邮件，其中包含用于下载数据的链接。</p>
<p>你下载的数据与本节处理的数据有类似的结构，但包含的文件头可能不同。然而，只要按本节介绍的步骤做，就能对你感兴趣的数据进行可视化。</p>
<blockquote>
<p>
<strong>动手试一试</strong>
</p>
<p>
<strong>练习16-1：锡特卡的降雨量</strong>
 　锡特卡属于温带雨林，降水量非常丰富。在数据文件sitka_weather_2018_simple.csv中，文件头PRCP表示的是每日降水量。请对这列数据进行可视化。如果你想知道沙漠的降水量有多低，可针对死亡谷完成同样的练习。</p>
<p>
<strong>练习16-2：比较锡特卡和死亡谷的温度</strong>
 　在有关锡特卡和死亡谷的图表中，温度刻度反映了数据范围的不同。为准确比较锡特卡和死亡谷的温度范围，需要在<img alt="y" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/16_Image00004.gif"/>
 轴上使用相同的刻度。为此，请修改图16-5和图16-6所示图表的<img alt="y" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/16_Image00004.gif"/>
 轴设置，对锡特卡和死亡谷的温度范围进行直接比较（也可对任何两个地方的温度范围进行比较）。</p>
<p>
<strong>练习16-3：旧金山</strong>
 　旧金山的温度更接近锡特卡还是死亡谷呢？为进行比较，可下载一些有关旧金山的温度数据，并据此生成包含最高温度和最低温度的图表。</p>
<p>
<strong>练习16-4：自动索引</strong>
 　本节以硬编码的方式指定了<code>TMIN</code>
 和<code>TMAX</code>
 列的索引。请根据文件头行确定这些列的索引，让程序同时适用于锡特卡和死亡谷。另外，请根据气象站的名称自动生成图表的标题。</p>
<p>
<strong>练习16-5：探索</strong>
 　生成一些图表，对你好奇的任何地方的其他天气数据进行研究。</p>
</blockquote>
<h2 id="nav_point_386">16.2　制作全球地震散点图：JSON格式</h2>
<p>在本节中<span class="注释编号">1</span>
 ，你将下载一个数据集，其中记录了一个月内全球发生的所有地震，再制作一幅散点图来展示这些地震的位置和震级。这些数据是以JSON格式存储的，因此要使用模块<code>json</code>
 来处理。Plotly提供了根据位置数据绘制地图的工具，适合初学者使用。你将使用它来进行可视化并指出全球的地震分布情况。</p>
<p class="注释内容">
<span class="注释编号下">1</span>
 本节为陶俊杰根据原作编写。——编者注</p>
<h3 id="nav_point_387">16.2.1　地震数据</h3>
<p>请将文件eq_data_1_day_m1.json复制到存储本章程序的文件夹中。地震是以里氏震级度量的，而该文件记录了（截至写作本节时）最近24小时内全球发生的所有不低于1级的地震。</p>
<h3 id="nav_point_388">16.2.2　查看JSON数据</h3>
<p>如果打开文件eq_data_1_day_m1.json，你将发现其内容密密麻麻，难以阅读：</p>
<pre class="代码无行号"><code>{"type":"FeatureCollection","metadata":{"generated":1550361461000,...
{"type":"Feature","properties":{"mag":1.2,"place":"11km NNE of Nor...
{"type":"Feature","properties":{"mag":4.3,"place":"69km NNW of Ayn...
{"type":"Feature","properties":{"mag":3.6,"place":"126km SSE of Co...
{"type":"Feature","properties":{"mag":2.1,"place":"21km NNW of Teh...
{"type":"Feature","properties":{"mag":4,"place":"57km SSW of Kakto...
--snip--</code>
</pre>
<p>这些数据适合机器而不是人来读取。不过可以看到，这个文件包含一些字典，还有一些我们感兴趣的信息，如震级和位置。</p>
<p>模块<code>json</code>
 提供了各种探索和处理JSON数据的工具，其中一些有助于重新设置这个文件的格式，让我们能够更清楚地查看原始数据，继而决定如何以编程的方式来处理。</p>
<p>我们先加载这些数据并将其以易于阅读的方式显示出来。这个数据文件很长，因此不打印出来，而是将数据写入另一个文件，再打开该文件并轻松地在数据中导航：</p>
<p>
<em>eq_explore_data.py</em>
</p>
<pre class="代码无行号"><code>  import json
  # 探索数据的结构。
  filename = 'data/eq_data_1_day_m1.json'
  with open(filename) as f:
❶     all_eq_data = json.load(f)
❷ readable_file = 'data/readable_eq_data.json'
  with open(readable_file, 'w') as f:
❸     json.dump(all_eq_data, f, indent=4)</code>
</pre>
<p>首先导入模块<code>json</code>
 ，以便恰当地加载文件中的数据，并将其存储到<code>all_eq_data</code>
 中（见❶）。函数<code>json.load()</code>
 将数据转换为Python能够处理的格式，这里是一个庞大的字典。在❷处，创建一个文件，以便将这些数据以易于阅读的方式写入其中。函数<code>json.dump()</code>
 接受一个JSON数据对象和一个文件对象，并将数据写入这个文件中（见❸）。参数<code>indent=4</code>
 让<code>dump()</code>
 使用与数据结构匹配的缩进量来设置数据的格式。</p>
<p>如果你现在查看目录data并打开其中的文件readable_eq_data.json，将发现其开头部分像下面这样：</p>
<p>
<em>readable_eq_data.json</em>
</p>
<pre class="代码无行号"><code>  {
      "type": "FeatureCollection",
❶     "metadata": {
          "generated": 1550361461000,
          "url": "https://earthquake.usgs.gov/earthquakes/.../1.0_day.geojson",
          "title": "USGS Magnitude 1.0+ Earthquakes, Past Day",
          "status": 200,
          "api": "1.7.0",
          "count": 158
      },
❷     "features": [
      --snip--</code>
</pre>
<p>这个文件的开头是一个键为<code>"metadata"</code>
 的片段（见❶），指出了这个数据文件是什么时候生成的，以及能够在网上的什么地方找到。它还包含适合人类阅读的标题以及文件中记录了多少次地震：在过去的24小时内，发生了158次地震。</p>
<p>这个geoJSON文件的结构适合存储基于位置的数据。数据存储在一个与键<code>"features"</code>
 相关联的列表中（见❷）。这个文件包含的是地震数据，因此列表的每个元素都对应一次地震。这种结构可能有点令人迷惑，但很有用，让地质学家能够将有关每次地震的任意数量信息存储在一个字典中，再将这些字典放在一个大型列表中。</p>
<p>我们来看看表示特定地震的字典：</p>
<p>
<em>readable_eq_data.json</em>
</p>
<pre class="代码无行号"><code>  --snip--
      {
          "type": "Feature",
❶         "properties": {
              "mag": 0.96,
              --snip--
❷             "title": "M 1.0 - 8km NE of Aguanga, CA"
           },
❸          "geometry": {
               "type": "Point",
               "coordinates": [
❹                 -116.7941667,
❺                 33.4863333,
                  3.22
               ]
          },
          "id": "ci37532978"
      },</code>
</pre>
<p>键<code>"properties"</code>
 关联到了与特定地震相关的大量信息（见❶）。我们关心的主要是与键<code>"mag"</code>
 相关联的地震震级以及地震的标题，因为后者很好地概述了地震的震级和位置（见❷）。</p>
<p>键<code>"geometry"</code>
 指出了地震发生在什么地方（见❸），我们需要根据这项信息将地震在散点图上标出来。在与键<code>"coordinates"</code>
 相关联的列表中，可找到地震发生位置的经度（见❹）和纬度（见❺）。</p>
<p>这个文件的嵌套层级比我们编写的代码多。如果这让你感到迷惑，也不用担心，Python将替你处理大部分复杂的工作。我们每次只会处理一两个嵌套层级。我们将首先提取过去24小时内发生的每次地震对应的字典。</p>
<blockquote>
<p>
<strong>注意</strong>
 　说到位置时，我们通常先说纬度、再说经度，这种习惯形成的原因可能是人类先发现了纬度，很久后才有经度的概念。然而，很多地质学框架都先列出经度、后列出纬度，因为这与数学约定<img alt="(x,y)" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/16_Image00040.gif"/>
 一致。geoJSON格式遵循(经度, 纬度)的约定，但在使用其他框架时，获悉其遵循的约定很重要。</p>
</blockquote>
<h3 id="nav_point_389">16.2.3　创建地震列表</h3>
<p>首先，创建一个列表，其中包含所有地震的各种信息：</p>
<p>
<em>eq_explore_data.py</em>
</p>
<pre class="代码无行号"><code>import json
# 探索数据的结构。
filename = 'data/eq_data_1_day_m1.json'
with open(filename) as f:
    all_eq_data = json.load(f)
all_eq_dicts = all_eq_data['features']
print(len(all_eq_dicts))</code>
</pre>
<p>我们提取与键<code>'features'</code>
 相关联的数据，并将其存储到<code>all_eq_dicts</code>
 中。我们知道，这个文件记录了158次地震。下面的输出表明，我们提取了这个文件记录的所有地震：</p>
<pre class="代码无行号"><code>158</code>
</pre>
<p>注意，我们编写的代码很短。格式良好的文件readable_eq_data.json包含超过6000行内容，但只需几行代码，就可读取所有的数据并将其存储到一个Python列表中。下面将提取所有地震的震级。</p>
<h3 id="nav_point_390">16.2.4　提取震级</h3>
<p>有了包含所有地震数据的列表后，就可遍历这个列表，从中提取所需的数据。下面来提取每次地震的震级：</p>
<p>
<em>eq_explore_data.py</em>
</p>
<pre class="代码无行号"><code>  --snip--
  all_eq_dicts = all_eq_data['features']
❶ mags = []
  for eq_dict in all_eq_dicts:
❷     mag = eq_dict['properties']['mag']
      mags.append(mag)
  print(mags[:10])</code>
</pre>
<p>我们创建了一个空列表，用于存储地震震级，再遍历列表all_eq_dicts（见❶）。每次地震的震级都存储在相应字典的<code>'properties'</code>
 部分的<code>'mag'</code>
 键下（见❷）。我们依次将地震震级赋给变量<code>mag</code>
 ，再将这个变量附加到列表<code>mags</code>
 末尾。</p>
<p>为确定提取的数据是否正确，打印前10次地震的震级：</p>
<pre class="代码无行号"><code>[0.96, 1.2, 4.3, 3.6, 2.1, 4, 1.06, 2.3, 4.9, 1.8]</code>
</pre>
<p>接下来，我们将提取每次地震的位置信息，然后就可以绘制地震散点图了。</p>
<h3 id="nav_point_391">16.2.5　提取位置数据</h3>
<p>位置数据存储在<code>"geometry"</code>
 键下。在<code>"geometry"</code>
 键关联的字典中，有一个<code>"coordinates"</code>
 键，它关联到一个列表，而列表中的前两个值为经度和纬度。下面演示了如何提取位置数据：</p>
<p>
<em>eq_explore_data.py</em>
</p>
<pre class="代码无行号"><code>  --snip--
  all_eq_dicts = all_eq_data['features']
  mags, titles, lons, lats = [], [], [], []
  for eq_dict in all_eq_dicts:
      mag = eq_dict['properties']['mag']
❶     title = eq_dict['properties']['title']
❷     lon = eq_dict['geometry']['coordinates'][0]
      lat = eq_dict['geometry']['coordinates'][1]
      mags.append(mag)
      titles.append(title)
      lons.append(lon)
      lats.append(lat)
  print(mags[:10])
  print(titles[:2])
  print(lons[:5])
  print(lats[:5])</code>
</pre>
<p>我们创建了用于存储位置标题的列表<code>titles</code>
 ，来提取字典<code>'properties'</code>
 里<code>'title'</code>
 键对应的值（见❶），以及用于存储经度和纬度的列表。代码<code>eq_dict['geometry']</code>
 访问与<code>"geometry"</code>
 键相关联的字典（见❷）。第二个键（<code>'coordinates'</code>
 ）提取与<code>"coordinates"</code>
 相关联的列表，而索引0提取该列表中的第一个值，即地震发生位置的经度。</p>
<p>打印前5个经度和纬度时，输出表明提取的数据是正确的：</p>
<pre class="代码无行号"><code>[0.96, 1.2, 4.3, 3.6, 2.1, 4, 1.06, 2.3, 4.9, 1.8]
['M 1.0 - 8km NE of Aguanga, CA', 'M 1.2 - 11km NNE of North Nenana, Alaska']
[-116.7941667, -148.9865, -74.2343, -161.6801, -118.5316667]
[33.4863333, 64.6673, -12.1025, 54.2232, 35.3098333]</code>
</pre>
<p>有了这些数据，就可以绘制地震散点图了。</p>
<h3 id="nav_point_392">16.2.6　绘制震级散点图</h3>
<p>有了前面提取的数据，就可以绘制可视化图了。首先要实现一个简单的震级散点图，在确保显示的信息正确无误之后，我们再将注意力转向样式和外观方面。绘制初始散点图的代码如下：</p>
<p>
<em>eq_world_map.py</em>
</p>
<pre class="代码无行号"><code>❶ import plotly.express as px
  fig = px.scatter(
      x=lons,
      y=lats,
      labels={"x": "经度", "y": "纬度"},
      range_x=[-200, 200],
      range_y=[-90, 90],
      width=800,
      height=800,
      title="全球地震散点图",
❷ )
❸ fig.write_html("global_earthquakes.html")
❹ fig.show()</code>
</pre>
<p>首先，导入<code>plotly.express</code>
 ，用别名<code>px</code>
 表示。Plotly Express是Plotly的高级接口，简单易用，语法与Matplotlib类似（见❶）。然后，调用<code>px.scatter</code>
 函数配置参数创建一个<code>fig</code>
 实例，分别设置<img alt="x" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/16_Image00003.gif"/>
 轴为经度［范围是<code>[-200, 200]</code>
 （扩大空间，以便完整显示东西经180°附近的地震散点）］、<img alt="y" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/16_Image00004.gif"/>
 轴为纬度［范围是<code>[-90, 90]</code>
 ］，设置散点图显示的宽度和高度均为800像素，并设置标题为“全球地震散点图”（见❷）。</p>
<p>只用14行代码，简单的散点图就配置完成了，这返回了一个<code>fig</code>
 对象。<code>fig.write_html</code>
 方法可以将可视化图保存为html文件。在文件夹中找到global_earthquakes.html文件，用浏览器打开即可（见❸）。另外，如果使用Jupyter Notebook，可以直接使用<code>fig.show</code>
 方法直接在notebook单元格显示散点图（见❹）。</p>
<p>局部效果如下图所示：</p>
<p class="图"><img alt="{%}" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/16_Image00041.gif"/>
</p>
<p>
<strong>图16-7　显示24小时内所有地震的简单散点图</strong>
</p>
<p>可对这幅散点图做大量修改，使其更有意义、更好懂。下面就来做些这样的修改。</p>
<h3 id="nav_point_393">16.2.7　另一种指定图表数据的方式</h3>
<p>配置这个图表前，先来看看另一种稍微不同的指定Plotly 图表数据的方式。当前，经纬度数据是手动配置的：</p>
<pre class="代码无行号"><code>--snip--
    x=lons,
    y=lats,
    labels={"x": "经度", "y": "纬度"},
--snip--</code>
</pre>
<p>这是在Plotly Express中给图表定义数据的最简单方式之一，但在数据处理中并不是最佳的。下面是另一种给图表定义数据的等效方式，需要使用pandas数据分析工具。首先创建一个<code>DataFrame</code>
 ，将需要的数据封装起来：</p>
<pre class="代码无行号"><code>import pandas as pd
data = pd.DataFrame(
    data=zip(lons, lats, titles, mags), columns=["经度", "纬度", "位置", "震级"]
)
data.head()</code>
</pre>
<p>然后，参数配置方式可以变更为：</p>
<pre class="代码无行号"><code>--snip--
    data,
    x="经度",
    y="纬度",
--snip--</code>
</pre>
<p>在这种方式中，所有有关数据的信息都以键值对的形式放在一个字典中。如果在eq_plot.py中使用这些代码，生成的图表是一样的。相比于前一种格式，这种格式让我们能够无缝衔接数据分析，并且更轻松地进行定制。</p>
<h3 id="nav_point_394">16.2.8　定制标记的尺寸</h3>
<p>确定如何改进散点图的样式时，应着重于让要传达的信息更清晰。当前的散点图显示了每次地震的位置，但没有指出震级。我们要让观察者迅速获悉最严重的地震发生在什么地方。</p>
<p>为此，根据地震的震级设置其标记的尺寸：</p>
<p>
<em>eq_world_map.py</em>
</p>
<pre class="代码无行号"><code>  fig = px.scatter(
      data,
      x="经度",
      y="纬度",
      range_x=[-200, 200],
      range_y=[-90, 90],
      width=800,
      height=800,
      title="全球地震散点图",
❶     size="震级",
❷     size_max=10,
  )
  fig.write_html("global_earthquakes.html")
  fig.show()</code>
</pre>
<p>Plotly Express支持对数据系列进行定制，这些定制都以参数表示。这里使用了<code>size</code>
 参数来指定散点图中每个标记的尺寸，我们只需要将前面<code>data</code>
 中的<code>"震级"</code>
 字段提供给<code>size</code>
 参数即可（见❶）。另外，标记尺寸默认为20像素，还可以通过<code>size_max=10</code>
 将最大显示尺寸缩放到10（见❷）。</p>
<p>如果运行这些代码，将看到类似于图16-8所示的散点图。这比前面的散点图好多了，但还有很大的改进空间。</p>
<p class="图"><img alt="{%}" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/16_Image00042.gif"/>
</p>
<p>
<strong>图16-8　现在散点图显示了地震的震级</strong>
</p>
<h3 id="nav_point_395">16.2.9　定制标记的颜色</h3>
<p>我们还可以定制标记的颜色，以呈现地震的严重程度。执行这些修改前，将文件eq_data_30_day_m1.json复制到你的数据目录中，它包含30天内的地震数据。通过使用这个更大的数据集，绘制出来的地震散点图将有趣得多。</p>
<p>下面演示了如何使用渐变来呈现地震震级：</p>
<p>
<em>eq_world_map.py</em>
</p>
<pre class="代码无行号"><code>❶ filename = 'data/eq_data_30_day_m1.json'
  --snip--
  fig = px.scatter(
      data,
      x="经度",
      y="纬度",
      range_x=[-200, 200],
      range_y=[-90, 90],
      width=800,
      height=800,
      title="全球地震散点图",
      size="震级",
      size_max=10,
❷     color="震级",
  )
  --snip--</code>
</pre>
<p>首先修改文件名，以使用30天的数据集（见❶）。为了让标记的震级按照不同的颜色显示，只需要配置<code>color="震级"</code>
 即可。默认的视觉映射图例渐变色范围是从蓝到红再到黄，数值越小则标记越蓝，而数值越大则标记越黄（见❷）。</p>
<p>如果现在运行这个程序，你看到的散点图将漂亮得多。如图16-9所示，渐变的颜色指出了地震的严重程度。通过在散点图上显示大量的地震，可将板块边界大致呈现出来！</p>
<p class="图"><img alt="{%}" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/16_Image00043.gif"/>
</p>
<p>
<strong>图16-9　使用颜色和尺寸呈现震级的30天地震散点图</strong>
</p>
<h3 id="nav_point_396">16.2.10　其他渐变</h3>
<p>Plotly Express有大量的渐变可供选择。要获悉有哪些渐变可供使用，请使用文件名show_color_scales.py保存下面这个简短的程序：</p>
<p>
<em>show_color_scales.py</em>
</p>
<pre class="代码无行号"><code>import plotly.express as px
for key in px.colors.named_colorscales():
    print(key)</code>
</pre>
<p>Plotly Express将渐变存储在模块<code>colors</code>
 中。这些渐变是在列表<code>px.colors.named_colorscales()</code>
 中定义的。下面的输出列出了可供你使用的所有渐变：</p>
<pre class="代码无行号"><code>--snip--
greys
hot
inferno
jet
magenta
magma
--snip--</code>
</pre>
<p>请尝试使用这些渐变其实映射到一个颜色列表。使用<code>px.colors.diverging.RdYlGn[::-1]</code>
 可以将对应颜色的配色列表反转。</p>
<blockquote>
<p>
<strong>注意</strong>
 　Plotly除了有<code>px.colors.diverging</code>
 表示连续变量的配色方案，还有<code>px.colors.sequential</code>
 和<code>px.colors.qualitative</code>
 表示离散变量。随便挑一种配色，例如<code>px.colors.qualitative.Alphabet</code>
 ，你将看到渐变是如何定义的。每个渐变都有起始色和终止色，有些渐变还定义了一个或多个中间色。Plotly会在这些定义好的颜色之间插入颜色。</p>
</blockquote>
<h3 id="nav_point_397">16.2.11　添加鼠标指向时显示的文本</h3>
<p>为完成这幅散点图的绘制，我们将添加一些说明性文本，在你将鼠标指向表示地震的标记时显示出来。除了默认显示的经度和纬度外，还将显示震级以及地震的大致位置：</p>
<p>
<em>eq_world_map.py</em>
</p>
<pre class="代码无行号"><code>fig = px.scatter(
    data,
    x="经度",
    y="纬度",
    range_x=[-200, 200],
    range_y=[-90, 90],
    width=800,
    height=800,
    title="全球地震散点图",
    size="震级",
    size_max=10,
    color="震级",
   hover_name="位置",
)
fig.write_html("global_earthquakes.html")
fig.show()
--snip--</code>
</pre>
<p>Plotly Express的操作非常简单，只需要将<code>hover_name</code>
 参数配置为<code>data</code>
 的<code>"位置"</code>
 字段即可。</p>
<p>太令人震惊了！通过编写大约40行代码，我们就绘制了一幅漂亮的全球地震活动散点图，并通过30天地震数据大致展示了地球的板块结构。Plotly Express提供了众多定制可视化外观和行为的方式。使用它提供的众多选项，可让图表和散点图准确地显示你所需的信息。</p>
<blockquote>
<p>
<strong>动手试一试</strong>
</p>
<p>
<strong>练习16-6：重构</strong>
 　在从<code>all_eq_dicts</code>
 中提取数据的循环中，使用了变量来指向震级、经度、纬度和标题，再将这些值分别附加到相应列表的末尾。这旨在清晰地演示如何从JSON文件中提取数据，但并非必须这样做。你也可以不使用这些临时变量，而是直接从<code>eq_dict</code>
 中提取这些值，并将其附加到相应的列表末尾。这样做将缩短这个循环的循环体，使其只包含4行代码。</p>
<p>
<strong>练习16-7：自动生成标题</strong>
 　本节定义<code>my_layout</code>
 时以手工方式指定标题，这意味着每次变更源文件时，都需要修改标题。你可以不这样做，而是使用JSON文件中元数据（metadata）部分的数据集标题。为此，可提取这个值，将其赋给一个变量，并在定义<code>my_layout</code>
 时使用这个变量来指定散点图的标题。</p>
<p>
<strong>练习16-8：最近发生的地震</strong>
 　请在本书配套资源中找到关于最近1小时、1天、7天和30天内地震信息的数据文件（截至本书出版时，参见文件夹chapter_16/Excercise16-8）。请使用其中一个数据集，绘制一幅散点图来展示最近发生的地震。</p>
<p>
<strong>练习16-9：全球火灾</strong>
 　在本章的配套资源中，有一个名为world_fires_1_day.csv的文件。它包含了有关全球各地发生的火灾信息，包括经度、纬度和火灾强度（brightness）。使用16.1节介绍的数据处理技术以及16.2节介绍的散点图绘制技术，绘制一幅散点图来展示全球哪些地方发生了火灾。</p>
</blockquote>
<h2 id="nav_point_398">16.3　小结</h2>
<p>在本章中，你学习了：如何使用现实世界中的数据集；如何处理CSV和JSON文件，以及如何提取感兴趣的数据；如何使用Matplotlib来处理以往的天气数据，包括如何使用模块<code>datetime</code>
 和如何在同一个图表中绘制多个数据系列；如何使用Plotly绘制呈现地质数据的散点图；以及如何设置Plotly散点图和图表的样式。</p>
<p>有了使用CSV和JSON文件的经验后，你将能够处理几乎任何要分析的数据。大多数在线数据集能以这两种格式中的一种或两种下载。熟悉了这两种格式，再学习使用其他格式的数据就会更简单。</p>
<p>在下一章，你将编写自动从网上采集数据并对其进行可视化的程序。如果你只是将编程作为业余爱好，学会这些技能可以增加乐趣；如果你有志于成为专业程序员，就必须掌握这些技能。</p>
<br style="page-break-after:always"/>
<div></div>
]]></description>
    <pubDate>Mon, 27 Jul 2026 01:26:45 +0000</pubDate>
    <guid isPermaLink="false">16</guid>
</item>
<item>
    <title>Basic Python - 015 - 项目2 数据可视化.epub</title>
    <description><![CDATA[<p style='color: #888;'>⏱ 预计阅读时间：108 分钟 (21413 字)</p><hr><h1 id="nav_point_345">项目2　数据可视化</h1>
<br style="page-break-after:always"/>
<div></div>
<h1 id="nav_point_346">第 15 章　生成数据</h1>
<blockquote>
<p><img alt="" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/15_Image00001.gif" style="width: 16%" width="16%"/>
 　<strong>数据可视化</strong>
 指的是通过可视化表示来探索数据。它与<strong>数据分析</strong>
 紧密相关，而数据分析指的是使用代码来探索数据集的规律和关联。数据集可以是用一行代码就能表示的小型数字列表，也可以是数千兆字节的数据。</p>
<p>漂亮地呈现数据并非仅仅关乎漂亮的图片。通过以引人注目的简单方式呈现数据，能让观看者明白其含义：发现数据集中原本未知的规律和意义。</p>
<p>所幸即便没有超级计算机，你也能够可视化复杂的数据。鉴于Python的高效性，使用它在笔记本电脑上就能快速地探索由数百万个数据点组成的数据集。数据点并非必须是数。利用本书前半部分介绍的基本知识，也可对非数值数据进行分析。</p>
<p>在基因研究、天气研究、政治经济分析等众多领域，人们常常使用Python来完成数据密集型工作。数据科学家使用Python编写了一系列优秀的可视化和分析工具，其中很多可供你使用。最流行的工具之一是Matplotlib，它是一个数学绘图库，我们将使用它来制作简单的图表，如折线图和散点图。然后，我们将基于随机漫步概念生成一个更有趣的数据集——根据一系列随机决策生成的图表。</p>
<p>本章还将使用Plotly包，它生成的图表非常适合在数字设备上显示。Plotly生成的图表可根据显示设备的尺寸自动调整大小，还具备众多交互特性，如在用户将鼠标指向图表的不同部分时突出数据集的特定方面。本章将使用Plotly来分析掷骰子的结果。</p>
</blockquote>
<h2 id="nav_point_347">15.1　安装Matplotlib</h2>
<p>本章将首先使用Matplotlib来生成几个图表，为此需要使用<code>pip</code>
 来安装它。<code>pip</code>
 是一个可用于下载并安装Python包的模块。请在终端提示符下执行如下命令：</p>
<pre class="代码无行号"><code>$ python -m pip install --user matplotlib</code>
</pre>
<p>这个命令让Python运行模块<code>pip</code>
 ，并将<code>matplotlib</code>
 包添加到当前用户的Python安装中。在你的系统中，如果运行程序或启动终端会话时使用的命令不是<code>python</code>
 ，而是<code>python3</code>
 ，应使用类似于下面的命令：</p>
<pre class="代码无行号"><code>$ python3 -m pip install --user matplotlib</code>
</pre>
<blockquote>
<p>
<strong>注意</strong>
 　在macOS系统中，如果这样不管用，请尝试在不指定标志<code>--user</code>
 的情况下再次执行该命令。</p>
</blockquote>
<p>要查看使用Matplotlib可制作的各种图表，请访问其官方网站，浏览示例画廊。通过单击画廊中的图表，可查看生成它们的代码。</p>
<h2 id="nav_point_348">15.2　绘制简单的折线图</h2>
<p>下面使用Matplotlib绘制一个简单的折线图，再对其进行定制，以实现信息更丰富的数据可视化效果。我们将使用平方数序列1、4、9、16和25来绘制这个图表。</p>
<p>只需提供如下的数，Matplotlib将完成其他工作：</p>
<p>
<em>mpl_squares.py</em>
</p>
<pre class="代码无行号"><code>  import matplotlib.pyplot as plt
  squares = [1, 4, 9, 16, 25]
❶ fig, ax = plt.subplots()
  ax.plot(squares)
  plt.show()</code>
</pre>
<p>首先导入模块<code>pyplot</code>
 ，并为其指定别名<code>plt</code>
 ，以免反复输入<code>pyplot</code>
 。（在线示例大多这样做，这里也不例外。）模块<code>pyplot</code>
 包含很多用于生成图表的函数。</p>
<p>我们创建了一个名为<code>squares</code>
 的列表，在其中存储要用来制作图表的数据。然后，采取了另一种常见的Matplotlib做法——调用函数<code>subplots()</code>
 （见❶）。这个函数可在一张图片中绘制一个或多个图表。变量<code>fig</code>
 表示整张图片。变量<code>ax</code>
 表示图片中的各个图表，大多数情况下要使用它。</p>
<p>接下来调用方法<code>plot()</code>
 ，它尝试根据给定的数据以有意义的方式绘制图表。函数<code>plt.show()</code>
 打开Matplotlib查看器并显示绘制的图表，如图15-1所示。在查看器中，你可缩放和导航图形，还可单击磁盘图标将图表保存起来。</p>
<p class="图"><img alt="{%}" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/15_Image00020.gif"/>
</p>
<p>
<strong>图15-1　使用Matplotlib可制作的最简单的图表</strong>
</p>
<h3 id="nav_point_349">15.2.1　修改标签文字和线条粗细</h3>
<p>如图15-1所示的图形表明数是越来越大的，但标签文字太小、线条太细，难以看清楚。所幸Matplotlib让你能够调整可视化的各个方面。</p>
<p>下面通过一些定制来改善这个图表的可读性，如下所示：</p>
<p>
<em>mpl_squares.py</em>
</p>
<pre class="代码无行号"><code>  import matplotlib.pyplot as plt
  squares = [1, 4, 9, 16, 25]
  fig, ax = plt.subplots()
❶ ax.plot(squares, linewidth=3)
  # 设置图表标题并给坐标轴加上标签。
❷ ax.set_title("平方数", fontsize=24)
❸ ax.set_xlabel("值", fontsize=14)
  ax.set_ylabel("值的平方", fontsize=14)
  # 设置刻度标记的大小。
❹ ax.tick_params(axis='both', labelsize=14)
  plt.show()</code>
</pre>
<p>参数<code>linewidth</code>
 （见❶）决定了<code>plot()</code>
 绘制的线条粗细。方法<code>set_title()</code>
 （见❷）给图表指定标题。在上述代码中，出现多次的参数<code>fontsize</code>
 指定图表中各种文字的大小。</p>
<p>方法<code>set_xlabel()</code>
 和<code>set_ylabel()</code>
 让你能够为每条轴设置标题（见❸）。方法<code>tick_params()</code>
 设置刻度的样式（见❹），其中指定的实参将影响<img alt="x" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/15_Image00003.gif"/>
 轴和<img alt="y" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/15_Image00004.gif"/>
 轴上的刻度（<code>axes='both'</code>
 ），并将刻度标记的字号设置为14（<code>labelsize=14</code>
 ）。</p>
<p>最终的图表阅读起来容易得多，如图15-2所示：标签文字更大，线条也更粗了。通常，需要尝试不同的值，才能确定什么样的设置生成的图表最合适。</p>
<p class="图"><img alt="" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/15_Image00021.gif" style="width: 85%" width="85%"/>
</p>
<p>
<strong>图15-2　现在图表阅读起来容易得多</strong>
</p>
<h3 id="nav_point_350">15.2.2　校正图形</h3>
<p>图形更容易看清后，我们发现没有正确地绘制数据：折线图的终点指出4.0的平方为25！下面来修复这个问题。</p>
<p>向<code>plot()</code>
 提供一系列数时，它假设第一个数据点对应的<img alt="x" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/15_Image00003.gif"/>
 坐标值为0，但这里第一个点对应的<img alt="x" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/15_Image00003.gif"/>
 值为1。为改变这种默认行为，可向<code>plot()</code>
 同时提供输入值和输出值：</p>
<p>
<em>mpl_squares.py</em>
</p>
<pre class="代码无行号"><code>import matplotlib.pyplot as plt
input_values = [1, 2, 3, 4, 5]
squares = [1, 4, 9, 16, 25]
fig, ax = plt.subplots()
ax.plot(input_values, squares, linewidth=3)
# 设置图表标题并给坐标轴加上标签。
--snip--</code>
</pre>
<p>现在<code>plot()</code>
 将正确地绘制数据，因为同时提供了输入值和输出值，<code>plot()</code>
 无须对输出值的生成方式做出假设。最终的图形是正确的，如图15-3所示。</p>
<p class="图"><img alt="" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/15_Image00022.gif" style="width: 85%" width="85%"/>
</p>
<p>
<strong>图15-3　根据数据正确地绘制了图形</strong>
</p>
<p>使用<code>plot()</code>
 时可指定各种实参，还可使用众多函数对图形进行定制。本章后面处理更有趣的数据集时，将继续探索这些定制函数。</p>
<h3 id="nav_point_351">15.2.3　使用内置样式</h3>
<p>Matplotlib提供了很多已经定义好的样式，它们使用的背景色、网格线、线条粗细、字体、字号等设置很不错，让你无须做太多定制就可生成引人瞩目的可视化效果。要获悉在你的系统中可使用哪些样式，可在终端会话中执行如下命令：</p>
<pre class="代码无行号"><code>&gt;&gt;&gt; import matplotlib.pyplot as plt
&gt;&gt;&gt; plt.style.available
['seaborn-dark', 'seaborn-darkgrid', 'seaborn-ticks', 'fivethirtyeight',
--snip--</code>
</pre>
<p>要使用这些样式，可在生成图表的代码前添加如下代码行：</p>
<p>
<em>mpl_squares.py</em>
</p>
<pre class="代码无行号"><code>import matplotlib.pyplot as plt
input_values = [1, 2, 3, 4, 5]
squares = [1, 4, 9, 16, 25]
plt.style.use('seaborn')
fig, ax = plt.subplots()
--snip--</code>
</pre>
<p>这些代码生成的图表如图15-4所示。可供使用的内置样式有很多，请尝试使用它们，找出你喜欢的。</p>
<p class="图"><img alt="" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/15_Image00023.gif" style="width: 85%" width="85%"/>
</p>
<p>
<strong>图15-4　内置样式seaborn</strong>
</p>
<h3 id="nav_point_352">15.2.4　使用<code>scatter()</code>
 绘制散点图并设置样式</h3>
<p>有时候，绘制散点图并设置各个数据点的样式很有用。例如，你可能想以一种颜色显示较小的值，用另一种颜色显示较大的值。绘制大型数据集时，还可对每个点都设置同样的样式，再使用不同的样式选项重新绘制某些点以示突出。</p>
<p>要绘制单个点，可使用方法<code>scatter()</code>
 。向它传递一对<img alt="x" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/15_Image00003.gif"/>
 坐标和<img alt="y" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/15_Image00004.gif"/>
 坐标，它将在指定位置绘制一个点：</p>
<p>
<em>catter_squares.py</em>
</p>
<pre class="代码无行号"><code>import matplotlib.pyplot as plt
plt.style.use('seaborn')
fig, ax = plt.subplots()
ax.scatter(2, 4)
plt.show()</code>
</pre>
<p>下面来设置图表的样式，使其更有趣。我们将添加标题，给坐标轴加上标签，并且确保所有文本都大到能够看清：</p>
<pre class="代码无行号"><code>  import matplotlib.pyplot as plt
  plt.style.use('seaborn')
  fig, ax = plt.subplots()
❶ ax.scatter(2, 4, s=200)
  # 设置图表标题并给坐标轴加上标签。
  ax.set_title("平方数", fontsize=24)
  ax.set_xlabel("值", fontsize=14)
  ax.set_ylabel("值的平方", fontsize=14)
  # 设置刻度标记的大小。
  ax.tick_params(axis='both', which='major', labelsize=14)
  plt.show()</code>
</pre>
<p>在❶处，调用<code>scatter()</code>
 并使用参数<code>s</code>
 设置绘制图形时使用的点的尺寸。如果此时运行scatter_squares.py，将在图表中央看到一个点，如图15-5所示。</p>
<p class="图"><img alt="" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/15_Image00024.gif" style="width: 85%" width="85%"/>
</p>
<p>
<strong>图15-5　绘制单个点</strong>
</p>
<h3 id="nav_point_353">15.2.5　使用<code>scatter()</code>
 绘制一系列点</h3>
<p>要绘制一系列的点，可向<code>scatter()</code>
 传递两个分别包含<img alt="x" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/15_Image00003.gif"/>
 值和<img alt="y" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/15_Image00004.gif"/>
 值的列表，如下所示：</p>
<p>
<em>scatter_squares.py</em>
</p>
<pre class="代码无行号"><code>import matplotlib.pyplot as plt
x_values = [1, 2, 3, 4, 5]
y_values = [1, 4, 9, 16, 25]
plt.style.use('seaborn')
fig, ax = plt.subplots()
ax.scatter(x_values, y_values, s=100)
# 设置图表标题并给坐标轴指定标签。
--snip--</code>
</pre>
<p>列表<code>x_values</code>
 包含要计算平方值的数，列表<code>y_values</code>
 包含前述数的平方值。将这些列表传递给<code>scatter()</code>
 时，Matplotlib依次从每个列表中读取一个值来绘制一个点。要绘制的点的坐标分别为 (1, 1)、(2, 4)、(3, 9)、(4, 16)和(5, 25)，最终的结果如图15-6所示。</p>
<p class="图"><img alt="" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/15_Image00025.gif" style="width: 85%" width="85%"/>
</p>
<p>
<strong>图15-6　由多个点组成的散点图</strong>
</p>
<h3 id="nav_point_354">15.2.6　自动计算数据</h3>
<p>手工计算列表要包含的值可能效率低下，需要绘制的点很多时尤其如此。我们不必手工计算包含点坐标的列表，可以用Python循环来完成。</p>
<p>下面是绘制1000个点的代码：</p>
<p>
<em>scatter_squares.py</em>
</p>
<pre class="代码无行号"><code>  import matplotlib.pyplot as plt
❶ x_values = range(1, 1001)
  y_values = [x**2 for x in x_values]
  plt.style.use('seaborn')
  fig, ax = plt.subplots()
❷ ax.scatter(x_values, y_values, s=10)
  # 设置图表标题并给坐标轴加上标签。
  --snip--
  # 设置每个坐标轴的取值范围。
❸ ax.axis([0, 1100, 0, 1100000])
  plt.show()</code>
</pre>
<p>首先创建了一个包含<img alt="x" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/15_Image00003.gif"/>
 值的列表，其中包含数1～1000（见❶）。接下来，是一个生成<img alt="y" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/15_Image00004.gif"/>
 值的列表解析，它遍历<img alt="x" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/15_Image00003.gif"/>
 值（<code>for x in x_values</code>
 ），计算其平方值（<code>x**2</code>
 ），并将结果存储到列表<code>y_values</code>
 中。然后，将输入列表和输出列表传递给<code>scatter()</code>
 （见❷）。这个数据集较大，因此将点设置得较小。</p>
<p>在❸处，使用方法<code>axis()</code>
 指定了每个坐标轴的取值范围。方法<code>axis()</code>
 要求提供4个值：<img alt="x" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/15_Image00003.gif"/>
 和<img alt="y" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/15_Image00004.gif"/>
 坐标轴的最小值和最大值。这里将<img alt="x" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/15_Image00003.gif"/>
 坐标轴的取值范围设置为0～1100，并将<img alt="y" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/15_Image00004.gif"/>
 坐标轴的取值范围设置为0～1 100 000。结果如图15-7所示。</p>
<p class="图"><img alt="" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/15_Image00026.gif" style="width: 85%" width="85%"/>
</p>
<p>
<strong>图15-7　对Python来说，绘制1000个点与绘制5个点一样容易</strong>
</p>
<h3 id="nav_point_355">15.2.7　自定义颜色</h3>
<p>要修改数据点的颜色，可向<code>scatter()</code>
 传递参数<code>c</code>
 ，并将其设置为要使用的颜色的名称（放在引号内），如下所示：</p>
<pre class="代码无行号"><code>ax.scatter(x_values, y_values, c='red', s=10)</code>
</pre>
<p>还可使用RGB颜色模式自定义颜色。要指定自定义颜色，可传递参数<code>c</code>
 ，并将其设置为一个元组，其中包含三个0～1的小数值，分别表示红色、绿色和蓝色的分量。例如，下面的代码行创建一个由淡绿色点组成的散点图：</p>
<pre class="代码无行号"><code>ax.scatter(x_values, y_values, c=(0, 0.8, 0), s=10)</code>
</pre>
<p>值越接近0，指定的颜色越深；值越接近1，指定的颜色越浅。</p>
<h3 id="nav_point_356">15.2.8　使用颜色映射</h3>
<p>
<strong>颜色映射</strong>
 （colormap）是一系列颜色，从起始颜色渐变到结束颜色。在可视化中，颜色映射用于突出数据的规律。例如，你可能用较浅的颜色来显示较小的值，并使用较深的颜色来显示较大的值。</p>
<p>模块<code>pyplot</code>
 内置了一组颜色映射。要使用这些颜色映射，需要告诉<code>pyplot</code>
 该如何设置数据集中每个点的颜色。下面演示了如何根据每个点的<img alt="y" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/15_Image00004.gif"/>
 值来设置其颜色：</p>
<p>
<em>scatter_squares.py</em>
</p>
<pre class="代码无行号"><code>import matplotlib.pyplot as plt
x_values = range(1, 1001)
y_values = [x**2 for x in x_values]
ax.scatter(x_values, y_values, c=y_values, cmap=plt.cm.Blues, s=10)
# 设置图表标题并给坐标轴加上标签。
--snip--</code>
</pre>
<p>我们将参数<code>c</code>
 设置成了一个<img alt="y" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/15_Image00004.gif"/>
 值列表，并使用参数<code>cmap</code>
 告诉<code>pyplot</code>
 使用哪个颜色映射。这些代码将<img alt="y" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/15_Image00004.gif"/>
 值较小的点显示为浅蓝色，并将<img alt="y" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/15_Image00004.gif"/>
 值较大的点显示为深蓝色，结果如图15-8所示。</p>
<p class="图"><img alt="" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/15_Image00027.gif" style="width: 85%" width="85%"/>
</p>
<p>
<strong>图15-8　使用颜色映射Blues的图表</strong>
</p>
<blockquote>
<p>
<strong>注意</strong>
 　要了解<code>pyplot</code>
 中所有的颜色映射，请访问Matplotlib网站主页，单击Examples，向下滚动到Color，再单击Colormaps reference。</p>
</blockquote>
<h3 id="nav_point_357">15.2.9　自动保存图表</h3>
<p>要让程序自动将图表保存到文件中，可将调用<code>plt.show()</code>
 替换为调用<code>plt.savefig()</code>
 ：</p>
<pre class="代码无行号"><code>plt.savefig('squares_plot.png', bbox_inches='tight')</code>
</pre>
<p>第一个实参指定要以什么文件名保存图表，这个文件将存储到scatter_squares.py所在的目录。第二个实参指定将图表多余的空白区域裁剪掉。如果要保留图表周围多余的空白区域，只需省略这个实参即可。</p>
<blockquote>
<p>
<strong>动手试一试</strong>
</p>
<p>
<strong>练习15-1：立方</strong>
 　数的三次方称为<strong>立方</strong>
 。请绘制一个图形，显示前5个整数的立方值。再绘制一个图形，显示前5000个整数的立方值。</p>
<p>
<strong>练习15-2：彩色立方</strong>
 　给前面绘制的立方图指定颜色映射。</p>
</blockquote>
<h2 id="nav_point_358">15.3　随机漫步</h2>
<p>本节将使用Python来生成随机漫步数据，再使用Matplotlib以引人瞩目的方式将这些数据呈现出来。<strong>随机漫步</strong>
 是这样行走得到的路径：每次行走都是完全随机的、没有明确的方向，结果是由一系列随机决策决定的。你可以将随机漫步看作蚂蚁在晕头转向的情况下，每次都沿随机的方向前行所经过的路径。</p>
<p>在自然界、物理学、生物学、化学和经济领域，随机漫步都有其实际用途。例如，漂浮在水滴上的花粉因不断受到水分子的挤压而在水面上移动。水滴中的分子运动是随机的，因此花粉在水面上的运动路径犹如随机漫步。我们稍后编写的代码将模拟现实世界的很多情形。</p>
<h3 id="nav_point_359">15.3.1　创建<code>RandomWalk</code>
 类</h3>
<p>为模拟随机漫步，将创建一个名为<code>RandomWalk</code>
 的类，它随机地选择前进方向。这个类需要三个属性：一个是存储随机漫步次数的变量，其他两个是列表，分别存储随机漫步经过的每个点的<img alt="x" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/15_Image00003.gif"/>
 坐标和<img alt="y" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/15_Image00004.gif"/>
 坐标。</p>
<p>
<code>RandomWalk</code>
 类只包含两个方法：方法<code>__init___()</code>
 和<code>fill_walk()</code>
 ，后者计算随机漫步经过的所有点。先来看看<code>__init__()</code>
 ，如下所示：</p>
<p>
<em>random_walk.py</em>
</p>
<pre class="代码无行号"><code>❶ from random import choice
  class RandomWalk:
      """一个生成随机漫步数据的类。"""
❷     def __init__(self, num_points=5000):
          """初始化随机漫步的属性。"""
          self.num_points = num_points
          #所有随机漫步都始于(0, 0)。
❸         self.x_values = [0]
          self.y_values = [0]</code>
</pre>
<p>为做出随机决策，将所有可能的选择都存储在一个列表中，并在每次决策时都使用模块<code>random</code>
 中的<code>choice()</code>
 来决定使用哪种选择（见❶）。接下来，将随机漫步包含的默认点数设置为5000。这个数大到足以生成有趣的模式，又小到可确保能够快速地模拟随机漫步（见❷）。然后，在❸处创建两个用于存储<img alt="x" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/15_Image00003.gif"/>
 值和<img alt="y" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/15_Image00004.gif"/>
 值的列表，并让每次漫步都从点(0, 0)出发。</p>
<h3 id="nav_point_360">15.3.2　选择方向</h3>
<p>我们将使用方法<code>fill_walk()</code>
 来生成漫步包含的点并决定每次漫步的方向，如下所示。请将这个方法添加到random_walk.py中：</p>
<p>
<em>random_walk.py</em>
</p>
<pre class="代码无行号"><code>      def fill_walk(self):
          """计算随机漫步包含的所有点。"""
          # 不断漫步，直到列表达到指定的长度。
❶         while len(self.x_values) &lt; self.num_points:
              # 决定前进方向以及沿这个方向前进的距离。
❷             x_direction = choice([1, -1])
              x_distance = choice([0, 1, 2, 3, 4])
❸             x_step = x_direction * x_distance
              y_direction = choice([1, -1])
              y_distance = choice([0, 1, 2, 3, 4])
❹             y_step = y_direction * y_distance
              # 拒绝原地踏步。
❺             if x_step == 0 and y_step == 0:
                  continue
              # 计算下一个点的x值和y值。
❻             x = self.x_values[-1] + x_step
              y = self.y_values[-1] + y_step
              self.x_values.append(x)
              self.y_values.append(y)</code>
</pre>
<p>❶处建立了一个循环，它不断运行，直到漫步包含所需的点数。方法<code>fill_walk()</code>
 的主要部分告诉Python如何模拟四种漫步决定：向右走还是向左走？沿指定的方向走多远？向上走还是向下走？沿选定的方向走多远？</p>
<p>使用<code>choice([1, -1])</code>
 给<code>x_direction</code>
 选择一个值，结果要么是表示向右走的1，要么是表示向左走的-1（见❷）。接下来，<code>choice([0, 1, 2, 3, 4])</code>
 随机地选择一个0～4的整数，告诉Python 沿指定的方向走多远（<code>x_distance</code>
 ）。通过包含0，不仅能够同时沿两个轴移动，还能够只沿一个轴移动。</p>
<p>在❸和❹处，将移动方向乘以移动距离，确定沿<img alt="x" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/15_Image00003.gif"/>
 轴和<img alt="y" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/15_Image00004.gif"/>
 轴移动的距离。如果<code>x_step</code>
 为正将向右移动，为负将向左移动，为零将垂直移动；如果<code>y_step</code>
 为正将向上移动，为负将向下移动，为零将水平移动。如果<code>x_step</code>
 和<code>y_step</code>
 都为零，则意味着原地踏步。我们拒绝这样的情况，接着执行下一次循环（见❺）。</p>
<p>为获取漫步中下一个点的<img alt="x" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/15_Image00003.gif"/>
 值，将<code>x_step</code>
 与<code>x_values</code>
 中的最后一个值相加（见❻），对<img alt="y" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/15_Image00004.gif"/>
 值也做相同的处理。获得下一个点的<img alt="x" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/15_Image00003.gif"/>
 值和<img alt="y" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/15_Image00004.gif"/>
 值后，将它们分别附加到列表<code>x_values</code>
 和<code>y_values</code>
 的末尾。</p>
<h3 id="nav_point_361">15.3.3　绘制随机漫步图</h3>
<p>下面的代码将随机漫步的所有点都绘制出来：</p>
<p>
<em>rw_visual.py</em>
</p>
<pre class="代码无行号"><code>  import matplotlib.pyplot as plt
  from random_walk import RandomWalk
  # 创建一个RandomWalk实例。
❶ rw = RandomWalk()
  rw.fill_walk()
  # 将所有的点都绘制出来。
  plt.style.use('classic')
  fig, ax = plt.subplots()
❷ ax.scatter(rw.x_values, rw.y_values, s=15)
  plt.show()</code>
</pre>
<p>首先导入模块<code>pyplot</code>
 和<code>RandomWalk</code>
 类，再创建一个<code>RandomWalk</code>
 实例并将其存储到<code>rw</code>
 中（见❶），并且调用<code>fill_walk()</code>
 。在❷处，将随机漫步包含的<img alt="x" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/15_Image00003.gif"/>
 值和<img alt="y" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/15_Image00004.gif"/>
 值传递给<code>scatter()</code>
 ，并选择合适的点尺寸。图15-9显示了包含5000个点的随机漫步图。（本节的示意图未包含Matplotlib查看器的界面，但你运行rw_visual.py时会看到。）</p>
<p class="图"><img alt="{%}" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/15_Image00028.gif"/>
</p>
<p>
<strong>图15-9　包含5000个点的随机漫步</strong>
</p>
<h3 id="nav_point_362">15.3.4　模拟多次随机漫步</h3>
<p>每次随机漫步都不同，因此探索可能生成的各种模式很有趣。要在不多次运行程序的情况下使用前面的代码模拟多次随机漫步，一种办法是将这些代码放在一个<code>while</code>
 循环中，如下所示：</p>
<p>
<em>rw_visual.py</em>
</p>
<pre class="代码无行号"><code>import matplotlib.pyplot as plt
from random_walk import RandomWalk
# 只要程序处于活动状态，就不断地模拟随机漫步。
while True:
    # 创建一个RandomWalk实例。
    rw = RandomWalk()
    rw.fill_walk()
    # 将所有的点都绘制出来。
    plt.style.use('classic')
    fig, ax = plt.subplots()
    ax.scatter(rw.x_values, rw.y_values, s=15)
    plt.show()
    keep_running = input("Make another walk? (y/n): ")
    if keep_running == 'n':
        break</code>
</pre>
<p>这些代码模拟一次随机漫步，在Matplotlib查看器中显示结果，再在不关闭查看器的情况下暂停。如果关闭查看器，程序将询问是否要再模拟一次随机漫步。如果输入<strong>
<code>y</code>
</strong>
 ，可模拟在起点附近进行的随机漫步、大多沿特定方向偏离起点的随机漫步、漫步点分布不均匀的随机漫步，等等。要结束程序，请输入<strong>
<code>n</code>
</strong>
 。</p>
<h3 id="nav_point_363">15.3.5　设置随机漫步图的样式</h3>
<p>本节将定制图表，以突出每次漫步的重要特征，并让分散注意力的元素不那么显眼。为此，我们确定要突出的元素，如漫步的起点、终点和经过的路径。接下来确定要使其不那么显眼的元素，如刻度标记和标签。最终的结果是简单的可视化表示，清楚地指出了每次漫步经过的路径。</p>
<ol>
<li value="1">
<p>
<strong>给点着色</strong>
</p>
<p>我们将使用颜色映射来指出漫步中各点的先后顺序，并删除每个点的黑色轮廓，让其颜色更为明显。为根据漫步中各点的先后顺序来着色，传递参数<code>c</code>
 ，并将其设置为一个列表，其中包含各点的先后顺序。这些点是按顺序绘制的，因此给参数<code>c</code>
 指定的列表只需包含数0～4999，如下所示：</p>
<p>
<em>rw_visual.py</em>
</p>
<pre class="代码无行号"><code>  --snip--
  while True:
      # 创建一个RandomWalk实例。
      rw = RandomWalk()
      rw.fill_walk()
      # 将所有的点都绘制出来。
      plt.style.use('classic')
      fig, ax = plt.subplots()
❶     point_numbers = range(rw.num_points)
      ax.scatter(rw.x_values, rw.y_values, c=point_numbers, cmap=plt.cm.Blues,
          edgecolors='none', s=15)
      plt.show()
      keep_running = input("Make another walk? (y/n): ")
      --snip--</code>
</pre>
<p>在❶处，使用<code>range()</code>
 生成了一个数字列表，其中包含的数与漫步包含的点数量相同。接下来，将这个列表存储在<code>point_numbers</code>
 中，以便后面使用它来设置每个漫步点的颜色。将参数<code>c</code>
 设置为<code>point_numbers</code>
 ，指定使用颜色映射<code>Blues</code>
 ，并传递实参<code>edgecolors='none'</code>
 以删除每个点周围的轮廓。最终的随机漫步图从浅蓝色渐变为深蓝色，如图15-10所示。</p>
<p class="p-img"><img alt="{%}" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/15_Image00029.jpg"/>
</p>
<p>
<strong>图15-10　使用颜色映射Blues着色的随机漫步图</strong>
<br/>
 </p>
</li>
<li value="2">
<p>
<strong>重新绘制起点和终点</strong>
</p>
<p>除了给随机漫步的各个点着色，以指出其先后顺序外，如果还能呈现随机漫步的起点和终点就好了。为此，可在绘制随机漫步图后重新绘制起点和终点。这里让起点和终点更大并显示为不同的颜色，以示突出，如下所示：</p>
<p>
<em>rw_visual.py</em>
</p>
<pre class="代码无行号"><code>--snip--
while True:
    --snip--
    ax.scatter(rw.x_values, rw.y_values, c=point_numbers, cmap=plt.cm.Blues,
        edgecolors='none', s=15)
    # 突出起点和终点。
    ax.scatter(0, 0, c='green', edgecolors='none', s=100)
    ax.scatter(rw.x_values[-1], rw.y_values[-1], c='red', edgecolors='none',
        s=100)
    plt.show()
    --snip--</code>
</pre>
<p>为突出起点，使用绿色绘制点(0, 0)，并使其比其他点大（<code>s=100</code>
 ）。为突出终点，在漫步包含的最后一个<img alt="x" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/15_Image00003.gif"/>
 值和<img alt="y" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/15_Image00004.gif"/>
 值处绘制一个点，将其颜色设置为红色，并将尺寸设置为100。务必将这些代码放在调用<code>plt.show()</code>
 的代码前面，确保在其他点之上绘制起点和终点。</p>
<p>如果现在运行这些代码，将能准确地知道每次随机漫步的起点和终点。（如果起点和终点不明显，请调整颜色和大小，直到明显为止。）<br/>
 </p>
</li>
<li value="3">
<p>
<strong>隐藏坐标轴</strong>
</p>
<p>下面来隐藏这个图表的坐标轴，以免分散观察者对随机漫步路径的注意力。要隐藏坐标轴，可使用如下代码：</p>
<p>
<em>rw_visual.py</em>
</p>
<pre class="代码无行号"><code>  --snip--
  while True:
      --snip--
      ax.scatter(rw.x_values[-1], rw.y_values[-1], c='red', edgecolors='none',
          s=100)
      # 隐藏坐标轴。
❶     ax.get_xaxis().set_visible(False)
      ax.get_yaxis().set_visible(False)
      plt.show()
      --snip--</code>
</pre>
<p>为修改坐标轴，使用方法<code>ax.get_xaxis()</code>
 和<code>ax.get_yaxis()</code>
 （见❶）将每条坐标轴的可见性都设置为<code>False</code>
 。随着对数据可视化的不断学习和实践，你会经常看到这种串接方法的方式。</p>
<p>如果现在运行rw_visual.py，你将看到一系列图形，但看不到坐标轴。<br/>
 </p>
</li>
<li value="4">
<p>
<strong>增加点数</strong>
</p>
<p>下面来增加点数，以提供更多数据。为此，在创建<code>RandomWalk</code>
 实例时增大<code>num_points</code>
 的值，并在绘图时调整每个点的大小，如下所示：</p>
<p>
<em>rw_visual.py</em>
</p>
<pre class="代码无行号"><code>--snip--
while True:
    # 创建一个RandomWalk实例。
    rw = RandomWalk(50_000)
    rw.fill_walk()
    # 将所有的点都绘制出来。
    plt.style.use('classic')
    fig, ax = plt.subplots()
    point_numbers = range(rw.num_points)
    ax.scatter(rw.x_values, rw.y_values, c=point_numbers, cmap=plt.cm.Blues,
        edgecolor='none', s=1)
    --snip--</code>
</pre>
<p>这个示例模拟了一次包含50 000个点的随机漫步（以模拟现实情况），并将每个点的大小都设置为1。最终的随机漫步图更稀疏，犹如云朵，如图15-11所示。如你所见，我们使用简单的散点图制作出了一件艺术品！</p>
<p class="p-img"><img alt="{%}" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/15_Image00030.gif"/>
</p>
<p>
<strong>图15-11　包含50 000个点的随机漫步</strong>
</p>
<p>请尝试修改上述代码，看看将漫步包含的点数增加到多少后，程序的运行速度变得极其缓慢或绘制出的图形变得很难看。<br/>
 </p>
</li>
<li value="5">
<p>
<strong>调整尺寸以适合屏幕</strong>
</p>
<p>图表适合屏幕大小时，更能有效地将数据中的规律呈现出来。为让绘图窗口更适合屏幕大小，可以像下面这样调整Matplotlib输出的尺寸：</p>
<p>
<em>rw_visual.py</em>
</p>
<pre class="代码无行号"><code>--snip--
while True:
    # 创建一个RandomWalk实例。
    rw = RandomWalk(50_000)
    rw.fill_walk()
    # 将所有的点都绘制出来。
    plt.style.use('classic')
    fig, ax = plt.subplots(figsize=(15, 9))
    --snip--</code>
</pre>
<p>创建图表时，可传递参数<code>figsize</code>
 以指定生成的图形的尺寸。需要给参数<code>figsize</code>
 指定一个元组，向Matplotlib指出绘图窗口的尺寸，单位为英寸<span class="注释编号">1</span>
 。</p>
<p>Matplotlib假定屏幕分辨率为100像素/英寸。如果上述代码指定的图表尺寸不合适，可根据需要调整数字。如果知道当前系统的分辨率，可通过参数<code>dpi</code>
 向<code>plt.subplots()</code>
 传递该分辨率，以有效利用可用的屏幕空间，如下所示：</p>
<pre class="代码无行号"><code>fig, ax = plt.subplots(figsize=(10, 6), dpi=128)</code>
</pre>
</li>
</ol>
<p class="注释内容">
<span class="注释编号下">1</span>
 1英寸=2.54 厘米。——编者注</p>
<blockquote>
<p>
<strong>动手试一试</strong>
</p>
<p>
<strong>练习15-3：分子运动</strong>
 　修改rw_visual.py，将其中的<code>ax.scatter()</code>
 替换为<code>ax.plot()</code>
 。为模拟花粉在水滴表面的运动路径，向<code>plt.plot()</code>
 传递<code>rw.x_values</code>
 和<code>rw.y_values</code>
 ，并指定实参<code>linewidth</code>
 。请使用5000个点而不是50 000个点。</p>
<p>
<strong>练习15-4：改进的随机漫步</strong>
 　在类<code>RandomWalk</code>
 中，<code>x_step</code>
 和<code>y_step</code>
 是根据相同的条件生成的：从列表<code>[1, -1]</code>
 中随机选择方向，并从列表<code>[0, 1, 2, 3, 4]</code>
 中随机选择距离。请修改这些列表中的值，看看对随机漫步路径有何影响。尝试使用更长的距离选择列表（如0～8），或者将-1从<img alt="x" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/15_Image00003.gif"/>
 或<img alt="y" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/15_Image00004.gif"/>
 方向列表中删除。</p>
<p>
<strong>练习15-5：重构</strong>
 　方法<code>fill_walk()</code>
 很长。请新建一个名为<code>get_step()</code>
 的方法，用于确定每次漫步的距离和方向，并计算每一步。然后，在<code>fill_walk()</code>
 中调用<code>get_step()</code>
 两次：</p>
<pre class="代码无行号"><code>x_step = self.get_step()
y_step = self.get_step()</code>
</pre>
<p>通过这样的重构，可缩小方法<code>fill_walk()</code>
 ，让它阅读和理解起来更容易。</p>
</blockquote>
<h2 id="nav_point_364">15.4　使用Plotly模拟掷骰子</h2>
<p>本节将使用Python包Plotly来生成交互式图表。需要创建在浏览器中显示的图表时，Plotly很有用，因为它生成的图表将自动缩放以适合观看者的屏幕。Plotly生成的图表还是交互式的：用户将鼠标指向特定元素时，将突出显示有关该元素的信息。</p>
<p>在这个项目中，我们将对掷骰子的结果进行分析。抛掷一个6面的常规骰子时，可能出现的结果为1～6点，且出现每种结果的可能性相同。然而，如果同时掷两个骰子，某些点数出现的可能性将比其他点数大。为确定哪些点数出现的可能性最大，将生成一个表示掷骰子结果的数据集，并根据结果绘制一个图形。</p>
<p>在数学领域，掷骰子常被用来解释各种数据分析类型，而它在赌场和其他博弈场景中也有实际应用，在游戏《大富翁》以及众多角色扮演游戏中亦如此。</p>
<h3 id="nav_point_365">15.4.1　安装Plotly</h3>
<p>要安装Plotly，可像本章前面安装Matplotlib那样使用<code>pip</code>
 ：</p>
<pre class="代码无行号"><code>$ python -m pip install --user plotly</code>
</pre>
<p>在前面安装Matplotlib时，如果使用了<strong>
<code>python3</code>
</strong>
 之类的命令，这里也要使用同样的命令。</p>
<p>要了解使用Plotly可创建什么样的图表，请在其官方网站查看图表类型画廊。每个示例都包含源代码，让你知道这些图表是如何生成的。</p>
<h3 id="nav_point_366">15.4.2　创建<code>Die</code>
 类</h3>
<p>为模拟掷一个骰子的情况，我们创建下面的类：</p>
<p>
<em>die.py</em>
</p>
<pre class="代码无行号"><code>  from random import randint
  class Die:
      """表示一个骰子的类。"""
❶     def __init__(self, num_sides=6):
          """骰子默认为6面。"""
          self.num_sides = num_sides
      def roll(self):
          """"返回一个位于1和骰子面数之间的随机值。"""
❷         return randint(1, self.num_sides)</code>
</pre>
<p>方法<code>__init__()</code>
 接受一个可选参数。创建这个类的实例时，如果没有指定任何实参，面数默认为6；如果指定了实参，这个值将用于设置骰子的面数（见❶）。骰子是根据面数命名的，6面的骰子名为D6，8面的骰子名为D8，依此类推。</p>
<p>方法<code>roll()</code>
 使用函数<code>randint()</code>
 来返回一个1和面数之间的随机数（见❷）。这个函数可能返回起始值1、终止值<code>num_sides</code>
 或这两个值之间的任何整数。</p>
<h3 id="nav_point_367">15.4.3　掷骰子</h3>
<p>使用这个类来创建图表前，先来掷D6，将结果打印出来，并确认结果是合理的：</p>
<p>
<em>die_visual.py</em>
</p>
<pre class="代码无行号"><code>  from die import Die
  # 创建一个D6。
❶ die = Die()
  # 掷几次骰子并将结果存储在一个列表中。
  results = []
❷ for roll_num in range(100):
      result = die.roll()
      results.append(result)
  print(results)</code>
</pre>
<p>在❶处，创建一个<code>Die</code>
 实例，其面数为默认值6。在❷处，掷骰子100次，并将每次的结果都存储在列表<code>results</code>
 中。下面是一个示例结果集：</p>
<pre class="代码无行号"><code>[4, 6, 5, 6, 1, 5, 6, 3, 5, 3, 5, 3, 2, 2, 1, 3, 1, 5, 3, 6, 3, 6, 5, 4,
 1, 1, 4, 2, 3, 6, 4, 2, 6, 4, 1, 3, 2, 5, 6, 3, 6, 2, 1, 1, 3, 4, 1, 4,
 3, 5, 1, 4, 5, 5, 2, 3, 3, 1, 2, 3, 5, 6, 2, 5, 6, 1, 3, 2, 1, 1, 1, 6,
 5, 5, 2, 2, 6, 4, 1, 4, 5, 1, 1, 1, 4, 5, 3, 3, 1, 3, 5, 4, 5, 6, 5, 4,
 1, 5, 1, 2]</code>
</pre>
<p>通过快速浏览这些结果可知，<code>Die</code>
 类似乎没有问题。我们见到了值1和6，表明返回了最大和最小的可能值；没有见到0或7，表明结果都在正确的范围内；还看到了1～6的所有数字，表明所有可能的结果都出现了。下面来确定各个点数都出现了多少次。</p>
<h3 id="nav_point_368">15.4.4　分析结果</h3>
<p>为分析掷一个D6的结果，计算每个点数出现的次数：</p>
<p>
<em>die_visual.py</em>
</p>
<pre class="代码无行号"><code>  --snip--
  # 掷几次骰子并将结果存储在一个列表中。
  results = []
❶ for roll_num in range(1000):
      result = die.roll()
      results.append(result)
  # 分析结果。
  frequencies = []
❷ for value in range(1, die.num_sides+1):
❸     frequency = results.count(value)
❹     frequencies.append(frequency)
  print(frequencies)</code>
</pre>
<p>由于将使用Plotly来分析，而不是将结果打印出来，因此可将模拟掷骰子的次数增加到1000（见❶）。为分析结果，我们创建空列表<code>frequencies</code>
 ，用于存储每种点数出现的次数。在❷处，遍历可能的点数（这里为1～6），计算每种点数在<code>results</code>
 中出现了多少次（见❸），并将这个值附加到列表<code>frequencies</code>
 的末尾（见❹）。接下来，在可视化之前将这个列表打印出来：</p>
<pre class="代码无行号"><code>[155, 167, 168, 170, 159, 181]</code>
</pre>
<p>结果看起来是合理的：有6个值，对应掷D6时可能出现的每个点数；另外，没有任何点数出现的频率比其他点数高很多。下面来可视化这些结果。</p>
<h3 id="nav_point_369">15.4.5　绘制直方图</h3>
<p>有了频率列表，就可以绘制一个表示结果的直方图了。<strong>直方图</strong>
 是一种条形图，指出了各种结果出现的频率。创建这种直方图的代码如下：</p>
<p>
<em>die_visual.py</em>
</p>
<pre class="代码无行号"><code>  from plotly.graph_objs import Bar, Layout
  from plotly import offline
  from die import Die
  --snip--
  # 分析结果。
  frequencies = []
  for value in range(1, die.num_sides+1):
      frequency = results.count(value)
      frequencies.append(frequency)
  # 对结果进行可视化。
❶ x_values = list(range(1, die.num_sides+1))
❷ data = [Bar(x=x_values, y=frequencies)]
❸ x_axis_config = {'title': '结果'}
  y_axis_config = {'title': '结果的频率'}
❹ my_layout = Layout(title='掷一个D6 1000次的结果',
          xaxis=x_axis_config, yaxis=y_axis_config)
❺ offline.plot({'data': data, 'layout': my_layout}, filename='d6.html')</code>
</pre>
<p>为创建直方图，需要为每个可能出现的点数生成一个条形。我们将可能出现的点数（1到骰子的面数）存储在一个名为<code>x_values</code>
 的列表中（见❶）。Plotly不能直接接受函数<code>range()</code>
 的结果，因此需要使用函数<code>list()</code>
 将其转换为列表。Plotly类<code>Bar()</code>
 表示用于绘制条形图的数据集（见❷），需要一个存储<img alt="x" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/15_Image00003.gif"/>
 值的列表和一个存储<img alt="y" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/15_Image00004.gif"/>
 值的列表。这个类必须放在方括号内，因为数据集可能包含多个元素。</p>
<p>每个坐标轴都能以不同的方式进行配置，而每个配置选项都是一个字典元素。这里只设置了坐标轴标签（见❸）。类<code>Layout()</code>
 返回一个指定图表布局和配置的对象（见❹）。这里设置了图表名称，并传入了<img alt="x" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/15_Image00003.gif"/>
 轴和<img alt="y" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/15_Image00004.gif"/>
 轴的配置字典。</p>
<p>为生成图表，我们调用了函数<code>offline.plot()</code>
 （见❺）。这个函数需要一个包含数据和布局对象的字典，还接受一个文件名，指定要将图表保存到哪里。这里将输出存储到文件d6.html。</p>
<p>运行程序die_visual.py时，可能打开浏览器并显示文件d6.html。如果没有自动显示d6.html，可在任意Web浏览器中新建一个标签页，再在其中打开文件d6.html（它位于die_visual.py所在的文件夹中）。你将看到一个类似于图15-12所示的图表。（为方便印刷，我稍微修改了这个图表。在默认情况下，Plotly所生成图表的文本比图15-12所示的要小。）</p>
<p class="图"><img alt="" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/15_Image00031.gif" style="width: 90%" width="90%"/>
</p>
<p>
<strong>图15-12　使用Plotly创建的简单条形图</strong>
</p>
<p>注意，Plotly让这个图表具有交互性：如果将鼠标指向其中的任意条形，就能看到与之相关联的数据。在同一个图表中绘制多个数据集时，这项功能特别有用。另外，注意到右上角有一些图标，让你能够平移和缩放图表以及将其保存为图像。</p>
<h3 id="nav_point_370">15.4.6　同时掷两个骰子</h3>
<p>同时掷两个骰子时，得到的点数更多，结果分布情况也不同。下面来修改前面的代码，创建两个D6以模拟同时掷两个骰子的情况。每次掷两个骰子时，都将两个骰子的点数相加，并将结果存储在<code>results</code>
 中。请复制die_visual.py并将其保存为dice_visual.py，再做如下修改：</p>
<p>
<em>dice_visual.py</em>
</p>
<pre class="代码无行号"><code>  from plotly.graph_objs import Bar, Layout
  from plotly import offline
  from die import Die
  # 创建两个D6。
  die_1 = Die()
  die_2 = Die()
  # 掷几次骰子并将结果存储在一个列表中。
  results = []
  for roll_num in range(1000):
❶     result = die_1.roll() + die_2.roll()
      results.append(result)
  # 分析结果。
  frequencies = []
❷ max_result = die_1.num_sides + die_2.num_sides
❸ for value in range(2, max_result+1):
      frequency = results.count(value)
      frequencies.append(frequency)
  # 可视化结果。
  x_values = list(range(2, max_result+1))
  data = [Bar(x=x_values, y=frequencies)]
❹ x_axis_config = {'title': '结果', 'dtick': 1}
  y_axis_config = {'title': '结果的频率'}
  my_layout = Layout(title='掷两个D6 1000次的结果',
          xaxis=x_axis_config, yaxis=y_axis_config)
  offline.plot({'data': data, 'layout': my_layout}, filename='d6_d6.html')</code>
</pre>
<p>创建两个<code>Die</code>
 实例后，掷骰子多次，并计算每次的总点数（见❶）。可能出现的最大点数为两个骰子的最大可能点数之和（12），这个值存储在<code>max_result</code>
 中（见❷）。可能出现的最小总点数为两个骰子的最小可能点数之和（2）。分析结果时，计算2到<code>max_result</code>
 的各种点数出现的次数（见❸）。（我们原本可以使用<code>range(2, 13)</code>
 ，但这只适用于两个D6。模拟现实世界的情形时，最好编写可轻松模拟各种情形的代码。前面的代码让我们能够模拟掷任意两个骰子的情形，不管这些骰子有多少面。）</p>
<p>创建图表时，在字典<code>x_axis_config</code>
 中使用了<code>dtick</code>
 键（见❹）。这项设置指定了<img alt="x" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/15_Image00003.gif"/>
 轴显示的刻度间距。这里绘制的直方图包含的条形更多，Plotly默认只显示某些刻度值，而设置<code>'dtick': 1</code>
 让Plotly显示每个刻度值。另外，我们还修改了图表名称及输出文件名。</p>
<p>运行这些代码后，你将看到如图15-13所示的图表。</p>
<p class="图"><img alt="" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/15_Image00032.gif" style="width: 90%" width="90%"/>
</p>
<p>
<strong>图15-13　模拟同时掷两个6面骰子1000次的结果</strong>
</p>
<p>这个图表显示了掷两个D6时得到的大致结果。如你所见，总点数为2或12的可能性最小，而总点数为7的可能性最大。这是因为在下面6种情况下得到的总点数都为7：1和6、2和5、3和4、4和3、5和2以及6和1。</p>
<h3 id="nav_point_371">15.4.7　同时掷两个面数不同的骰子</h3>
<p>下面来创建一个6面骰子和一个10面骰子，看看同时掷这两个骰子50 000次的结果如何：</p>
<p>
<em>dice_visual.py</em>
</p>
<pre class="代码无行号"><code>  from plotly.graph_objs import Bar, Layout
  from plotly import offline
  from die import Die
  # 创建一个D6和一个D10。
  die_1 = Die()
❶ die_2 = Die(10)
  # 掷几次骰子并将结果存储在一个列表中。
  results = []
  for roll_num in range(50_000):
      result = die_1.roll() + die_2.roll()
      results.append(result)
  # 分析结果。
  --snip--
  # 可视化结果。
  x_values = list(range(2, max_result+1))
  data = [Bar(x=x_values, y=frequencies)]
  x_axis_config = {'title': '结果', 'dtick': 1}
  y_axis_config = {'title': '结果的频率'}
❷ my_layout = Layout(title='掷一个D6和一个D10 50000次的结果',
      xaxis=x_axis_config, yaxis=y_axis_config)
  offline.plot({'data': data, 'layout': my_layout}, filename='d6_d10.html')</code>
</pre>
<p>为创建D10，我们在创建第二个<code>Die</code>
 实例时传递了实参<code>10</code>
 （见❶）；修改了第一个循环，以模拟掷骰子50 000而不是1000次；还修改了图表名称和输出文件名（见❷）。</p>
<p>图15-14显示了最终的图表。可能性最大的点数不止一个，而是有5个。这是因为导致出现最小点数和最大点数的组合都只有一种（1和1以及6和10），但面数较小的骰子限制了得到中间点数的组合数：得到总点数7、8、9、10和11的组合数都是6种。因此，这些总点数是最常见的结果，它们出现的可能性相同。</p>
<p class="图"><img alt="" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/15_Image00033.gif" style="width: 90%" width="90%"/>
</p>
<p>
<strong>图15-14　同时掷6面骰子和10面骰子50 000次的结果</strong>
</p>
<p>通过使用Plotly模拟掷骰子的结果，我们能够非常自由地探索这种现象。只需几分钟，就可模拟掷各种骰子很多次。</p>
<blockquote>
<p>
<strong>动手试一试</strong>
</p>
<p>
<strong>练习15-6：两个D8</strong>
 　编写一个程序，模拟同时掷两个8面骰子1000次的结果。先想象一下结果会是什么样的，再运行这个程序，看看你的直觉准不准。逐渐增加掷骰子的次数，直到系统不堪重负为止。</p>
<p>
<strong>练习15-7：同时掷三个骰子</strong>
 　同时掷三个D6时，可能得到的最小点数为3，最大点数为18。请通过可视化展示同时掷三个D6的结果。</p>
<p>
<strong>练习15-8：将点数相乘</strong>
 　同时掷两个骰子时，通常将其点数相加。请通过可视化展示将两个骰子的点数相乘的结果。</p>
<p>
<strong>练习15-9：改用列表解析</strong>
 　为清晰起见，本节模拟掷骰子的结果时，使用的是较长的<code>for</code>
 循环。如果你熟悉列表解析，尝试将这些程序中的一个或两个<code>for</code>
 循环改为列表解析。</p>
<p>
<strong>练习15-10 使用两个库</strong>
 ：尝试使用Matplotlib通过可视化来模拟掷骰子的情况，并尝试使用Plotly通过可视化来模拟随机漫步的情况。要完成这个练习，需要查看这些库的文档。</p>
</blockquote>
<h2 id="nav_point_372">15.5　小结</h2>
<p>在本章中，你学习了：如何生成数据集以及如何对其进行可视化；如何使用Matplotlib创建简单的图表，以及如何使用散点图来探索随机漫步过程；如何使用Plotly来创建直方图，以及如何使用直方图来探索同时掷两个面数不同的骰子的结果。</p>
<p>使用代码生成数据集是一种有趣而强大的方式，可用于模拟和探索现实世界的各种情形。完成后面的数据可视化项目时，请注意可使用代码模拟哪些情形。请研究新闻媒体中的可视化，看看其中是否有图表是以你在这些项目中学到的类似方式生成的。</p>
<p>在第16章中，我们将从网上下载数据，并继续使用Matplotlib和Plotly来探索这些数据。</p>
<br style="page-break-after:always"/>
<div></div>
]]></description>
    <pubDate>Fri, 24 Jul 2026 01:18:57 +0000</pubDate>
    <guid isPermaLink="false">15</guid>
</item>
<item>
    <title>Basic Python - 014 - 第 14 章 记分.epub</title>
    <description><![CDATA[<p style='color: #888;'>⏱ 预计阅读时间：125 分钟 (24959 字)</p><hr><h1 id="nav_point_322">第 14 章　记分</h1>
<blockquote>
<p><img alt="" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/14_Image00001.gif" style="width: 16%" width="16%"/>
 　本章将结束游戏《外星人入侵》的开发。我们会添加一个Play按钮，用于根据需要启动游戏以及在游戏结束后重启游戏，还会修改这个游戏，使其随玩家等级提高而加快节奏，并实现一个记分系统。阅读本章后，你将掌握足够多的知识，能够开始编写随玩家等级提高而加大难度以及显示得分的游戏。</p>
</blockquote>
<h2 id="nav_point_323">14.1　添加Play按钮</h2>
<p>本节将添加一个Play按钮，它在游戏开始前出现，并在游戏结束后再次出现，让玩家能够开始新游戏。</p>
<p>当前，这个游戏在玩家运行alien_invasion.py时就开始了。下面让游戏一开始处于非活动状态，并提示玩家单击Play按钮来开始游戏。为此，像下面这样修改<code>GameStats</code>
 类的方法<code>__init__()</code>
 ：</p>
<p>
<em>game_stats.py</em>
</p>
<pre class="代码无行号"><code>    def __init__(self, ai_game):
        """初始化统计信息。"""
        self.settings = ai_game.settings
        self.reset_stats()
        # 让游戏一开始处于非活动状态。
        self.game_active = False</code>
</pre>
<p>现在，游戏一开始将处于非活动状态，待创建Play按钮后，玩家才能开始游戏。</p>
<h3 id="nav_point_324">14.1.1　创建<code>Button</code>
 类</h3>
<p>由于Pygame没有内置创建按钮的方法，我们将编写一个<code>Button</code>
 类，用于创建带标签的实心矩形。你可在游戏中使用这些代码来创建任何按钮。下面是<code>Button</code>
 类的第一部分，请将这个类保存为文件button.py：</p>
<p>
<em>button.py</em>
</p>
<pre class="代码无行号"><code>  import pygame.font
  class Button:
❶     def __init__(self, ai_game, msg):
          """初始化按钮的属性。"""
          self.screen = ai_game.screen
          self.screen_rect = self.screen.get_rect()
          # 设置按钮的尺寸和其他属性。
❷         self.width, self.height = 200, 50
          self.button_color = (0, 255, 0)
          self.text_color = (255, 255, 255)
❸         self.font = pygame.font.SysFont(None, 48)
          # 创建按钮的rect对象，并使其居中。
❹         self.rect = pygame.Rect(0, 0, self.width, self.height)
          self.rect.center = self.screen_rect.center
          # 按钮的标签只需创建一次。
❺         self._prep_msg(msg)</code>
</pre>
<p>首先，导入模块<code>pygame.font</code>
 ，它让Pygame能够将文本渲染到屏幕上。方法<code>__init__()</code>
 接受参数<code>self</code>
 、对象<code>ai_game</code>
 和<code>msg</code>
 ，其中<code>msg</code>
 是要在按钮中显示的文本（见❶）。设置按钮的尺寸（见❷），再通过设置<code>button_color</code>
 ，让按钮的<code>rect</code>
 对象为亮绿色，并通过设置<code>text_color</code>
 让文本为白色。</p>
<p>在❸处，指定使用什么字体来渲染文本。实参<code>None</code>
 让Pygame使用默认字体，而<code>48</code>
 指定了文本的字号。为让按钮在屏幕上居中，创建一个表示按钮的<code>rect</code>
 对象（见❹），并将其<code>center</code>
 属性设置为屏幕的<code>center</code>
 属性。</p>
<p>Pygame处理文本的方式是，将要显示的字符串渲染为图像。在❺处，调用了<code>_prep_msg()</code>
 来处理这样的渲染。</p>
<p>
<code>_prep_msg()</code>
 的代码如下：</p>
<p>
<em>button.py</em>
</p>
<pre class="代码无行号"><code>      def _prep_msg(self, msg):
          """将msg渲染为图像，并使其在按钮上居中。"""
❶         self.msg_image = self.font.render(msg, True, self.text_color,
                  self.button_color)
❷         self.msg_image_rect = self.msg_image.get_rect()
          self.msg_image_rect.center = self.rect.center</code>
</pre>
<p>方法<code>_prep_msg()</code>
 接受实参<code>self</code>
 以及要渲染为图像的文本（<code>msg</code>
 ）。调用<code>font.render()</code>
 将存储在<code>msg</code>
 中的文本转换为图像，再将该图像存储在<code>self.msg_image</code>
 中（见❶）。方法<code>font.render()</code>
 还接受一个布尔实参，该实参指定开启还是关闭反锯齿功能（反锯齿让文本的边缘更平滑）。余下的两个实参分别是文本颜色和背景色。我们启用了反锯齿功能，并将文本的背景色设置为按钮的颜色。（如果没有指定背景色，Pygame渲染文本时将使用透明背景。）</p>
<p>在❷处，让文本图像在按钮上居中：根据文本图像创建一个<code>rect</code>
 ，并将其<code>center</code>
 属性设置为按钮的<code>center</code>
 属性。</p>
<p>最后，创建方法<code>draw_button()</code>
 ，用于将这个按钮显示到屏幕上：</p>
<p>
<em>button.py</em>
</p>
<pre class="代码无行号"><code>    def draw_button(self):
        # 绘制一个用颜色填充的按钮，再绘制文本。
        self.screen.fill(self.button_color, self.rect)
        self.screen.blit(self.msg_image, self.msg_image_rect)</code>
</pre>
<p>我们调用<code>screen.fill()</code>
 来绘制表示按钮的矩形，再调用<code>screen.blit()</code>
 并向它传递一幅图像以及与该图像相关联的<code>rect</code>
 ，从而在屏幕上绘制文本图像。至此，<code>Button</code>
 类便创建好了。</p>
<h3 id="nav_point_325">14.1.2　在屏幕上绘制按钮</h3>
<p>我们将在<code>AlienInvasion</code>
 中使用<code>Button</code>
 类来创建一个Play按钮。首先，更新<code>import</code>
 语句：</p>
<p>
<em>alien_invasion.py</em>
</p>
<pre class="代码无行号"><code>--snip--
from game_stats import GameStats
from button import Button</code>
</pre>
<p>只需要一个Play按钮，因此在<code>AlienInvasion</code>
 类的方法<code>__init__()</code>
 中创建它。可将这些代码放在方法<code>__init__()</code>
 的末尾：</p>
<p>
<em>alien_invasion.py</em>
</p>
<pre class="代码无行号"><code>    def __init__(self):
        --snip--
        self._create_fleet()
        # 创建Play按钮。
        self.play_button = Button(self, "Play")</code>
</pre>
<p>这些代码创建一个标签为Play的<code>Button</code>
 实例，但没有将它显示到屏幕上。为显示该按钮，在<code>_update_screen()</code>
 对其调用方法<code>draw_button()</code>
 ：</p>
<p>
<em>alien_invasion.py</em>
</p>
<pre class="代码无行号"><code>    def _update_screen(self):
        --snip--
        self.aliens.draw(self.screen)
        # 如果游戏处于非活动状态，就绘制Play按钮。
        if not self.stats.game_active:
            self.play_button.draw_button()
        pygame.display.flip()</code>
</pre>
<p>为让Play按钮位于其他所有屏幕元素上面，在绘制其他所有游戏元素后再绘制这个按钮，然后切换到新屏幕。将这些代码放在一个<code>if</code>
 代码块中，让按钮仅在游戏出于非活动状态时才出现。</p>
<p>如果现在运行这个游戏，将在屏幕中央看到一个Play按钮，如图14-1所示。</p>
<p class="图"><img alt="{%}" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/14_Image00014.gif"/>
</p>
<p>
<strong>图14-1　游戏处于非活动状态时出现的Play按钮</strong>
</p>
<h3 id="nav_point_326">14.1.3　开始游戏</h3>
<p>为在玩家单击Play按钮时开始新游戏，在<code>_check_events()</code>
 末尾添加如下<code>elif</code>
 代码块，以监视与该按钮相关的鼠标事件：</p>
<p>
<em>alien_invasion.py</em>
</p>
<pre class="代码无行号"><code>      def _check_events(self):
          """响应按键和鼠标事件。"""
          for event in pygame.event.get():
              if event.type == pygame.QUIT:
                  --snip--
❶             elif event.type == pygame.MOUSEBUTTONDOWN:
❷                 mouse_pos = pygame.mouse.get_pos()
❸                 self._check_play_button(mouse_pos)</code>
</pre>
<p>无论玩家单击屏幕的什么地方，Pygame都将检测到一个<code>MOUSEBUTTONDOWN</code>
 事件（见❶），但我们只想让这个游戏在玩家用鼠标单击Play按钮时做出响应。为此，使用了<code>pygame.mouse.get_pos()</code>
 ，它返回一个元组，其中包含玩家单击时鼠标的<img alt="x" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/14_Image00003.gif"/>
 坐标和<img alt="y" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/14_Image00004.gif"/>
 坐标（见❷）。我们将这些值传递给新方法<code>_check_play_button()</code>
 （见❸）。</p>
<p>方法<code>_check_play_button()</code>
 的代码如下，将它放在<code>_check_events()</code>
 后面：</p>
<p>
<em>alien_invasion.py</em>
</p>
<pre class="代码无行号"><code>      def _check_play_button(self, mouse_pos):
          """在玩家单击Play按钮时开始新游戏。"""
❶         if self.play_button.rect.collidepoint(mouse_pos):
              self.stats.game_active = True</code>
</pre>
<p>这里使用了<code>rect</code>
 的方法<code>collidepoint()</code>
 检查鼠标单击位置是否在Play按钮的<code>rect</code>
 内（见❶）。如果是，就将<code>game_active</code>
 设置为<code>True</code>
 ，让游戏开始！</p>
<p>至此，现在应该能够开始这个游戏了。游戏结束时，应将<code>game_active</code>
 设置为<code>False</code>
 ，并重新显示Play按钮。</p>
<h3 id="nav_point_327">14.1.4　重置游戏</h3>
<p>前面编写的代码只处理了玩家第一次单击Play按钮的情况，而没有处理游戏结束的情况，因为没有重置导致游戏结束的条件。</p>
<p>为在玩家每次单击Play按钮时都重置游戏，需要重置统计信息、删除现有的外星人和子弹、创建一群新的外星人并让飞船居中，如下所示：</p>
<p>
<em>alien_invasion.py</em>
</p>
<pre class="代码无行号"><code>      def _check_play_button(self, mouse_pos):
          """在玩家单击Play按钮时开始新游戏。"""
          if self.play_button.rect.collidepoint(mouse_pos):
              # 重置游戏统计信息。
❶             self.stats.reset_stats()
              self.stats.game_active = True
              # 清空余下的外星人和子弹。
❷             self.aliens.empty()
              self.bullets.empty()
              # 创建一群新的外星人并让飞船居中。
❸             self._create_fleet()
              self.ship.center_ship()</code>
</pre>
<p>在❶处，重置游戏统计信息，给玩家提供三艘新飞船。接下来，将<code>game_active</code>
 设置为<code>True</code>
 。这样，这个方法的代码执行完毕后，游戏就将开始。清空编组<code>aliens</code>
 和<code>bullets</code>
 （见❷），然后创建一群新的外星人并将飞船居中（见❸）。</p>
<p>现在，每当玩家单击Play按钮时，这个游戏都将正确地重置，让玩家想玩多少次就玩多少次！</p>
<h3 id="nav_point_328">14.1.5　将Play按钮切换到非活动状态</h3>
<p>当前存在一个问题：即便Play按钮不可见，玩家单击其所在的区域时，游戏依然会做出响应。游戏开始后，如果玩家不小心单击了Play按钮所处的区域，游戏将重新开始！</p>
<p>为修复这个问题，可让游戏仅在<code>game_active</code>
 为<code>False</code>
 时才开始：</p>
<p>
<em>alien_invasion.py</em>
</p>
<pre class="代码无行号"><code>      def _check_play_button(self, mouse_pos):
          """玩家单击Play按钮时开始新游戏。"""
❶         button_clicked = self.play_button.rect.collidepoint(mouse_pos)
❷         if button_clicked and not self.stats.game_active:
              # 重置游戏统计信息。
              self.stats.reset_stats()
              --snip--</code>
</pre>
<p>标志<code>button_clicked</code>
 的值为<code>True</code>
 或<code>False</code>
 （见❶）。仅当玩家单击了Play按钮<strong>且</strong>
 游戏当前处于非活动状态时，游戏才重新开始（见❷）。要测试这种行为，可开始新游戏，并不断单击Play按钮所在的区域。如果一切都像预期的那样工作，单击Play按钮所处的区域应该没有任何影响。</p>
<h3 id="nav_point_329">14.1.6　隐藏鼠标光标</h3>
<p>为让玩家能够开始游戏，要让鼠标光标可见，但游戏开始后，光标只会添乱。为修复这种问题，需要在游戏处于活动状态时让光标不可见。可在方法<code>_check_play_button()</code>
 末尾的<code>if</code>
 代码块中完成这项任务：</p>
<p>
<em>alien_invasion.py</em>
</p>
<pre class="代码无行号"><code>    def _check_play_button(self, mouse_pos):
        """在玩家单击Play按钮时开始新游戏。"""
        button_clicked = self.play_button.rect.collidepoint(mouse_pos)
        if button_clicked and not self.stats.game_active:
            --snip--
            # 隐藏鼠标光标。
            pygame.mouse.set_visible(False)</code>
</pre>
<p>通过向<code>set_visible()</code>
 传递<code>False</code>
 ，让Pygame在光标位于游戏窗口内时将其隐藏起来。</p>
<p>游戏结束后，将重新显示光标，让玩家能够单击Play按钮来开始新游戏。相关的代码如下：</p>
<p>
<em>alien_invasion.py</em>
</p>
<pre class="代码无行号"><code>    def _ship_hit(self):
        """响应飞船被外星人撞到。"""
        if self.stats.ships_left &gt; 0:
            --snip--
        else:
            self.stats.game_active = False
            pygame.mouse.set_visible(True)</code>
</pre>
<p>在<code>_ship_hit()</code>
 中，在游戏进入非活动状态后，立即让光标可见。关注这样的细节让游戏显得更专业，也让玩家能够专注于玩游戏而不是去费力理解用户界面。</p>
<blockquote>
<p>
<strong>动手试一试</strong>
</p>
<p>
<strong>练习14-1：按P键开始新游戏</strong>
 　鉴于游戏《外星人入侵》使用键盘来控制飞船，最好也能够让玩家通过按键来开始游戏。请添加在玩家按P键时开始游戏的代码。也许这样做会有所帮助：将<code>_check_play_button()</code>
 的一些代码提取出来，放到一个名为<code>_start_game()</code>
 的方法中，并在<code>_check_play_button()</code>
 和<code>_check_keydown_events()</code>
 中调用这个方法。</p>
<p>
<strong>练习14-2：射击练习</strong>
 　创建一个矩形，让它在屏幕右边缘以固定的速度上下移动。然后，在屏幕左边缘创建一艘飞船，玩家可上下移动它来射击前述矩形标靶。添加一个用于开始游戏的Play按钮，在玩家三次未击中目标时结束游戏，并重新显示Play按钮，让玩家能够单击该按钮来重新开始游戏。</p>
</blockquote>
<h2 id="nav_point_330">14.2　提高等级</h2>
<p>当前，将整群外星人消灭干净后，玩家将提高一个等级，但游戏的难度没变。下面来增加一点趣味性：每当玩家将屏幕上的外星人消灭干净后，都加快游戏的节奏，让游戏玩起来更难。</p>
<h3 id="nav_point_331">14.2.1　修改速度设置</h3>
<p>首先重新组织<code>Settings</code>
 类，将游戏设置划分成静态和动态两组。对于随着游戏进行而变化的设置，还要确保在开始新游戏时进行重置。settings.py的方法<code>__init__()</code>
 如下：</p>
<p>
<em>settings.py</em>
</p>
<pre class="代码无行号"><code>      def __init__(self):
          """初始化游戏的静态设置。"""
          #屏幕设置
          self.screen_width = 1200
          self.screen_height = 800
          self.bg_color = (230, 230, 230)
          # 飞船设置
          self.ship_limit = 3
          # 子弹设置
          self.bullet_width = 3
          self.bullet_height = 15
          self.bullet_color = 60, 60, 60
          self.bullets_allowed = 3
          # 外星人设置
          self.fleet_drop_speed = 10
          # 加快游戏节奏的速度。
❶         self.speedup_scale = 1.1
❷         self.initialize_dynamic_settings()</code>
</pre>
<p>依然在<code>__init__()</code>
 中初始化静态设置。在❶处，添加设置<code>speedup_scale</code>
 ，用于控制游戏节奏的加快速度：2表示玩家每提高一个等级，游戏的节奏就翻一倍；1表示游戏节奏始终不变。将其设置为1.1能够将游戏节奏提高到足够快，让游戏既有难度又并非不可完成。最后，调用<code>initialize_dynamic_settings()</code>
 初始化随游戏进行而变化的属性（见❷）。</p>
<p>
<code>initialize_dynamic_settings()</code>
 的代码如下：</p>
<p>
<em>settings.py</em>
</p>
<pre class="代码无行号"><code>    def initialize_dynamic_settings(self):
        """初始化随游戏进行而变化的设置。"""
        self.ship_speed = 1.5
        self.bullet_speed = 3.0
        self.alien_speed = 1.0
        # fleet_direction为1表示向右，为-1表示向左。
        self.fleet_direction = 1</code>
</pre>
<p>这个方法设置飞船、子弹和外星人的初始速度。随着游戏的进行，将提高这些速度。每当玩家开始新游戏时，都将重置这些速度。在这个方法中，还设置了<code>fleet_direction</code>
 ，使得游戏刚开始时，外星人总是向右移动。不需要增大<code>fleet_drop_speed</code>
 的值，因为外星人移动的速度越快，到达屏幕底端所需的时间越短。</p>
<p>为在玩家的等级提高时提高飞船、子弹和外星人的速度，编写一个名为<code>increase_speed()</code>
 的新方法：</p>
<p>
<em>settings.py</em>
</p>
<pre class="代码无行号"><code>    def increase_speed(self):
        """提高速度设置"""
        self.ship_speed *= self.speedup_scale
        self.bullet_speed *= self.speedup_scale
        self.alien_speed *= self.speedup_scale</code>
</pre>
<p>为提高这些游戏元素的速度，将每个速度设置都乘以<code>speedup_scale</code>
 的值。</p>
<p>在<code>_check_bullet_alien_collisions()</code>
 中，在整群外星人都被消灭后调用<code>increase_speed()</code>
 来加快游戏的节奏：</p>
<p>
<em>alien_invasion.py</em>
</p>
<pre class="代码无行号"><code>    def _check_bullet_alien_collisions(self):
        --snip--
        if not self.aliens:
            # 删除现有的子弹并创建一群新的外星人。
            self.bullets.empty()
            self._create_fleet()
            self.settings.increase_speed()</code>
</pre>
<p>通过修改速度设置<code>ship_speed</code>
 、<code>alien_speed</code>
 和<code>bullet_speed</code>
 的值，足以加快整个游戏的节奏！</p>
<h3 id="nav_point_332">14.2.2　重置速度</h3>
<p>每当玩家开始新游戏时，都需要将发生了变化的设置重置为初始值，否则新游戏开始时，速度设置将为前一次提高后的值：</p>
<p>
<em>alien_invasion.py</em>
</p>
<pre class="代码无行号"><code>    def _check_play_button(self, mouse_pos):
        """在玩家单击Play按钮时开始新游戏。"""
        button_clicked = self.play_button.rect.collidepoint(mouse_pos)
        if button_clicked and not self.stats.game_active:
            # 重置游戏设置。
            self.settings.initialize_dynamic_settings()
            --snip--</code>
</pre>
<p>现在，游戏《外星人入侵》玩起来更有趣，也更有挑战性了。每当玩家将屏幕上的外星人消灭干净后，游戏都将加快节奏，因此难度更大。如果游戏的难度提高得太快，可降低<code>settings.speedup_scale</code>
 的值；如果游戏的挑战性不足，可稍微提高这个设置的值。找出这个设置的最佳值，让难度的提高速度相对合理：一开始的几群外星人很容易消灭干净，接下来的几群消灭起来有一定难度，但也不是不可能，而要将之后的外星人群消灭干净几乎不可能。</p>
<blockquote>
<p>
<strong>动手试一试</strong>
</p>
<p>
<strong>练习14-3：有一定难度的射击练习</strong>
 　以你为完成练习14-2而做的工作为基础，让标靶的移动速度随游戏进行而加快，并在玩家单击Play按钮时将其重置为初始值。</p>
<p>
<strong>练习14-4：难度等级</strong>
 　在游戏《外星人入侵》中创建一组按钮，让玩家选择起始难度等级。每个按钮都给<code>Settings</code>
 中的属性指定合适的值，以实现相应的难度等级。</p>
</blockquote>
<h2 id="nav_point_333">14.3　记分</h2>
<p>下面来实现一个记分系统，以实时跟踪玩家的得分，并显示最高得分、等级和余下的飞船数。</p>
<p>得分是游戏的一项统计信息，因此在<code>GameStats</code>
 中添加一个<code>score</code>
 属性：</p>
<p>
<em>game_stats.py</em>
</p>
<pre class="代码无行号"><code>class GameStats:
    --snip--
    def reset_stats(self):
        """初始化随游戏进行可能变化的统计信息。"""
        self.ships_left = self.ai_settings.ship_limit
        self.score = 0</code>
</pre>
<p>为在每次开始游戏时都重置得分，我们在<code>reset_stats()</code>
 而不是<code>__init__()</code>
 中初始化<code>score</code>
 。</p>
<h3 id="nav_point_334">14.3.1　显示得分</h3>
<p>为在屏幕上显示得分，首先创建一个新类<code>Scoreboard</code>
 。当前，这个类只显示当前得分，但后面也将使用它来显示最高得分、等级和余下的飞船数。下面是这个类的前半部分，被保存为文件scoreboard.py：</p>
<p>
<em>scoreboard.py</em>
</p>
<pre class="代码无行号"><code>  import pygame.font
  class Scoreboard:
      """显示得分信息的类。"""
❶     def __init__(self, ai_game):
          """初始化显示得分涉及的属性。"""
          self.screen = ai_game.screen
          self.screen_rect = self.screen.get_rect()
          self.settings = ai_game.settings
          self.stats = ai_game.stats
          # 显示得分信息时使用的字体设置。
❷         self.text_color = (30, 30, 30)
❸         self.font = pygame.font.SysFont(None, 48)
          # 准备初始得分图像。
❹         self.prep_score()</code>
</pre>
<p>由于<code>Scoreboard</code>
 在屏幕上显示文本，首先导入模块<code>pygame.font</code>
 。接下来，在<code>__init__()</code>
 中包含形参<code>ai_game</code>
 ，以便访问报告跟踪的值所需的对象<code>settings</code>
 、<code>screen</code>
 和<code>stats</code>
 （见❶）。然后，设置文本颜色（见❷）并实例化一个字体对象（见❸）。</p>
<p>为将要显示的文本转换为图像，调用<code>prep_score()</code>
 （见❹），其定义如下：</p>
<p>
<em>scoreboard.py</em>
</p>
<pre class="代码无行号"><code>      def prep_score(self):
           """将得分转换为一幅渲染的图像。"""
❶         score_str = str(self.stats.score)
❷         self.score_image = self.font.render(score_str, True,
                  self.text_color, self.settings.bg_color)
          # 在屏幕右上角显示得分。
❸         self.score_rect = self.score_image.get_rect()
❹         self.score_rect.right = self.screen_rect.right - 20
❺         self.score_rect.top = 20</code>
</pre>
<p>在<code>prep_score()</code>
 中，将数值<code>stats.score</code>
 转换为字符串（见❶），再将这个字符串传递给创建图像的<code>render()</code>
 （见❷）。为在屏幕上清晰地显示得分，向<code>render()</code>
 传递屏幕背景色和文本颜色。</p>
<p>将得分放在屏幕右上角，并在得分增大导致数变宽时让其向左延伸。为确保得分始终锚定在屏幕右边，创建一个名为<code>score_rect</code>
 的<code>rect</code>
 （见❸），让其右边缘与屏幕右边缘相距20像素（见❹），并让其上边缘与屏幕上边缘也相距20像素（见❺）。</p>
<p>接下来，创建方法<code>show_score()</code>
 ，用于显示渲染好的得分图像：</p>
<p>
<em>scoreboard.py</em>
</p>
<pre class="代码无行号"><code>    def show_score(self):
         """在屏幕上显示得分。"""
         self.screen.blit(self.score_image, self.score_rect)</code>
</pre>
<p>这个方法在屏幕上显示得分图像，并将其放在<code>score_rect</code>
 指定的位置。</p>
<h3 id="nav_point_335">14.3.2　创建记分牌</h3>
<p>为显示得分，在<code>AlienInvasion</code>
 中创建一个<code>Scoreboard</code>
 实例。先来更新<code>import</code>
 语句：</p>
<p>
<em>alien_invasion.py</em>
</p>
<pre class="代码无行号"><code>--snip--
from game_stats import GameStats
from scoreboard import Scoreboard
--snip--</code>
</pre>
<p>接下来，在方法<code>__init__()</code>
 中创建一个<code>Scoreboard</code>
 实例：</p>
<p>
<em>alien_invasion.py</em>
</p>
<pre class="代码无行号"><code>    def __init__(self):
        --snip--
        pygame.display.set_caption("Alien Invasion")
        # 创建存储游戏统计信息的实例，
        #    并创建记分牌。
        self.stats = GameStats(self)
        self.sb = Scoreboard(self)
        --snip--</code>
</pre>
<p>然后，在<code>_update_screen()</code>
 中将记分牌绘制到屏幕上：</p>
<p>
<em>alien_invasion.py</em>
</p>
<pre class="代码无行号"><code>    def _update_screen(self):
        --snip--
        self.aliens.draw(self.screen)
        #显示得分。
        self.sb.show_score()
        # 如果游戏处于非活动状态，就显示Play按钮。
        --snip--</code>
</pre>
<p>在显示Play按钮前调用<code>show_score()</code>
 。</p>
<p>如果现在运行这个游戏，将在屏幕右上角看到0。（当前，我们只想在进一步开发记分系统前确认得分出现在正确的地方。）图14-2显示了游戏开始前的得分。</p>
<p class="图"><img alt="{%}" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/14_Image00015.gif"/>
</p>
<p>
<strong>图14-2　得分出现在屏幕右上角</strong>
</p>
<p>下面来指定每个外星人值多少分！</p>
<h3 id="nav_point_336">14.3.3　在外星人被消灭时更新得分</h3>
<p>为在屏幕上实时显示得分，每当有外星人被击中时，都更新<code>stats.score</code>
 的值，再调用<code>prep_score()</code>
 更新得分图像。但在此之前，需要指定玩家每击落一个外星人将得到多少分：</p>
<p>
<em>settings.py</em>
</p>
<pre class="代码无行号"><code>    def initialize_dynamic_settings(self):
        --snip--
        # 记分
        self.alien_points = 50</code>
</pre>
<p>随着游戏的进行，将提高每个外星人的分数。为确保每次开始新游戏时这个值都会被重置，我们在<code>initialize_dynamic_settings()</code>
 中设置它。</p>
<p>在<code>_check_bullet_alien_collisions()</code>
 中，每当有外星人被击落时，都更新得分：</p>
<p>
<em>alien_invasion.py</em>
</p>
<pre class="代码无行号"><code>    def _check_bullet_alien_collisions(self):
        """响应子弹和外星人发生碰撞。"""
        # 删除彼此碰撞的子弹和外星人。
        collisions = pygame.sprite.groupcollide(
                self.bullets, self.aliens, True, True)
        if collisions:
            self.stats.score += self.settings.alien_points
            self.sb.prep_score()
        --snip--</code>
</pre>
<p>有子弹击中外星人时，Pygame返回一个字典（<code>collisions</code>
 ）。我们检查这个字典是否存在，如果存在，就将得分加上一个外星人的分数（见❶）。接下来，调用<code>prep_score()</code>
 来创建一幅包含最新得分的新图像。</p>
<p>如果现在运行这个游戏，得分将不断增加！</p>
<h3 id="nav_point_337">14.3.4　重置得分</h3>
<p>当前，仅在有外星人被射杀<strong>之后</strong>
 生成得分。这在大多数情况下可行，但从开始新游戏到有外星人被射杀之间，显示的是上一次的得分。</p>
<p>为修复这个问题，可在开始新游戏时生成得分：</p>
<p>
<em>alien_invasion.py</em>
</p>
<pre class="代码无行号"><code>    def _check_play_button(self, mouse_pos):
        --snip--
        if button_clicked and not self.stats.game_active:
            --snip--
            # 重置游戏统计信息。
            self.stats.reset_stats()
            self.stats.game_active = True
            self.sb.prep_score()
            --snip--</code>
</pre>
<p>开始新游戏时，我们重置游戏统计信息再调用<code>prep_score()</code>
 。此时生成的记分牌上显示的得分为零。</p>
<h3 id="nav_point_338">14.3.5　将消灭的每个外星人都计入得分</h3>
<p>当前的代码可能会遗漏一些被消灭的外星人。例如，如果在一次循环中，有两颗子弹击中了外星人，或者因子弹较宽而同时击中了多个外星人，玩家将只能得到一个外星人的分数。为修复这种问题，我们来调整检测子弹和外星人碰撞的方式。</p>
<p>在<code>_check_bullet_alien_collisions()</code>
 中，与外星人碰撞的子弹都是字典<code>collisions</code>
 中的一个键，而与每颗子弹相关的值都是一个列表，其中包含该子弹击中的外星人。我们遍历字典<code>collisions</code>
 ，确保将消灭的每个外星人都计入得分：</p>
<p>
<em>alien_invasion.py</em>
</p>
<pre class="代码无行号"><code>  def _check_bullet_alien_collisions(self):
      --snip--
      if collisions:
❶         for aliens in collisions.values():
              self.stats.score += self.settings.alien_points * len(aliens)
          self.sb.prep_score()
      --snip--</code>
</pre>
<p>如果字典<code>collisions</code>
 存在，就遍历其中的所有值。别忘了，每个值都是一个列表，包含被同一颗子弹击中的所有外星人。对于每个列表，都将其包含的外星人数量乘以一个外星人的分数，并将结果加入当前得分。为测试这一点，请将子弹宽度改为300像素，并核实得到了其击中的每个外星人的分数，再将子弹宽度恢复正常值。</p>
<h3 id="nav_point_339">14.3.6　提高分数</h3>
<p>鉴于玩家每提高一个等级，游戏都变得更难，因此处于较高的等级时，外星人的分数应更高。为实现这种功能，需要编写在游戏节奏加快时提高分数的代码：</p>
<p>
<em>settings.py</em>
</p>
<pre class="代码无行号"><code>  class Settings:
      """存储游戏《外星人入侵》的所有设置的类。"""
      def __init__(self):
          --snip--
          # 加快游戏节奏的速度。
          self.speedup_scale = 1.1
          # 外星人分数的提高速度。
❶         self.score_scale = 1.5
          self.initialize_dynamic_settings()
      def initialize_dynamic_settings(self):
          --snip--
      def increase_speed(self):
          """提高速度设置和外星人分数。"""
          self.ship_speed *= self.speedup_scale
          self.bullet_speed *= self.speedup_scale
          self.alien_speed *= self.speedup_scale
❷         self.alien_points = int(self.alien_points * self.score_scale)</code>
</pre>
<p>我们定义了分数的提高速度，并称之为<code>score_scale</code>
 （见❶）。较低的节奏加快速度（1.1）让游戏很快变得极具挑战性，但为了让记分发生显著的变化，需要将分数的提高速度设置为更大的值（1.5）。现在，在加快游戏节奏的同时，提高了每个外星人的分数（见❷）。为让分数为整数，使用了函数<code>int()</code>
 。</p>
<p>为显示外星人的分数，在<code>Settings</code>
 的方法<code>increase_speed()</code>
 中调用函数<code>print()</code>
 ：</p>
<p>
<em>settings.py</em>
</p>
<pre class="代码无行号"><code>    def increase_speed(self):
        --snip--
        self.alien_points = int(self.alien_points * self.score_scale)
        print(self.alien_points)</code>
</pre>
<p>现在每当提高一个等级时，你都将在终端窗口看到新的分数值。</p>
<blockquote>
<p>
<strong>注意</strong>
 　确认分数在不断增加后，一定要删除调用函数<code>print()</code>
 的代码，否则可能影响游戏的性能，分散玩家的注意力。</p>
</blockquote>
<h3 id="nav_point_340">14.3.7　舍入得分</h3>
<p>大多数街机风格的射击游戏将得分显示为10的整数倍，下面让记分系统遵循这个原则。我们还将设置得分的格式，在大数中添加用逗号表示的千位分隔符。在<code>Scoreboard</code>
 中执行这种修改：</p>
<p>
<em>scoreboard.py</em>
</p>
<pre class="代码无行号"><code>      def prep_score(self):
          """将得分转换为渲染的图像。"""
❶         rounded_score = round(self.stats.score, -1)
❷         score_str = "{:,}".format(rounded_score)
          self.score_image = self.font.render(score_str, True,
                  self.text_color, self.settings.bg_color)
          --snip--</code>
</pre>
<p>函数<code>round()</code>
 通常让小数精确到小数点后某一位，其中小数位数是由第二个实参指定的。然而，如果将第二个实参指定为负数，<code>round()</code>
 将舍入到最近的10的整数倍，如10、100、1000等。❶处的代码让Python将<code>stats.score</code>
 的值舍入到最近的10的整数倍，并将结果存储到<code>rounded_score</code>
 中。</p>
<p>在❷处，使用一个字符串格式设置指令，让Python将数值转换为字符串时在其中插入逗号。例如，输出为<code>1,000,000</code>
 而不是<code>1000000</code>
 。如果现在运行这个游戏，看到的得分将是10的整数倍，即便得分很高亦如此，如图14-3所示。</p>
<p class="图"><img alt="{%}" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/14_Image00016.gif"/>
</p>
<p>
<strong>图14-3　得分为10的整数倍，并将逗号用作千分位分隔符</strong>
</p>
<h3 id="nav_point_341">14.3.8　最高得分</h3>
<p>每个玩家都想超过游戏的最高得分纪录。下面来跟踪并显示最高得分，给玩家提供要超越的目标。我们将最高得分存储在<code>GameStats</code>
 中：</p>
<p>
<em>game_stats.py</em>
</p>
<pre class="代码无行号"><code>    def __init__(self, ai_game):
        --snip--
        # 任何情况下都不应重置最高得分。
        self.high_score = 0</code>
</pre>
<p>因为在任何情况下都不会重置最高得分，所以在<code>__init__()</code>
 而不是<code>reset_stats()</code>
 中初始化<code>high_score</code>
 。</p>
<p>下面来修改<code>Scoreboard</code>
 以显示最高得分。先来修改方法<code>__init__()</code>
 ：</p>
<p>
<em>scoreboard.py</em>
</p>
<pre class="代码无行号"><code>      def __init__(self, ai_game):
          --snip--
          # 准备包含最高得分和当前得分的图像。
          self.prep_score()
❶         self.prep_high_score()</code>
</pre>
<p>最高得分将与当前得分分开显示，因此需要编写一个新方法<code>prep_high_score()</code>
 ，用于准备包含最高得分的图像（见❶）。</p>
<p>方法<code>prep_high_score()</code>
 的代码如下：</p>
<p>
<em>scoreboard.py</em>
</p>
<pre class="代码无行号"><code>      def prep_high_score(self):
          """将最高得分转换为渲染的图像。"""
❶         high_score = round(self.stats.high_score, -1)
          high_score_str = "{:,}".format(high_score)
❷         self.high_score_image = self.font.render(high_score_str, True,
                   self.text_color, self.settings.bg_color)
           # 将最高得分放在屏幕顶部中央。
           self.high_score_rect = self.high_score_image.get_rect()
❸          self.high_score_rect.centerx = self.screen_rect.centerx
❹          self.high_score_rect.top = self.score_rect.top</code>
</pre>
<p>将最高得分舍入到最近的10的整数倍，并添加用逗号表示的千分位分隔符（见❶）。然后，根据最高得分生成一幅图像（见❷），使其水平居中（见❸），并将其<code>top</code>
 属性设置为当前得分图像的<code>top</code>
 属性（见❹）。</p>
<p>现在，方法<code>show_score()</code>
 需要在屏幕右上角显示当前得分，并在屏幕顶部中央显示最高得分：</p>
<p>
<em>scoreboard.py</em>
</p>
<pre class="代码无行号"><code>    def show_score(self):
        """在屏幕上显示得分。"""
        self.screen.blit(self.score_image, self.score_rect)
        self.screen.blit(self.high_score_image, self.high_score_rect)</code>
</pre>
<p>为检查是否诞生了新的最高得分，在<code>Scoreboard</code>
 中添加一个新方法<code>check_high_score()</code>
 ：</p>
<p>
<em>scoreboard.py</em>
</p>
<pre class="代码无行号"><code>    def check_high_score(self):
        """检查是否诞生了新的最高得分。"""
        if self.stats.score &gt; self.stats.high_score:
            self.stats.high_score = self.stats.score
            self.prep_high_score()</code>
</pre>
<p>方法<code>check_high_score()</code>
 比较当前得分和最高得分。如果当前得分更高，就更新<code>high_score</code>
 的值，并调用<code>prep_high_score()</code>
 来更新包含最高得分的图像。</p>
<p>在<code>_check_bullet_alien_collisions()</code>
 中，每当有外星人被消灭时，都需要在更新得分后调用<code>check_high_score()</code>
 ：</p>
<p>
<em>alien_invasion.py</em>
</p>
<pre class="代码无行号"><code>    def _check_bullet_alien_collisions(self):
        --snip--
        if collisions:
            for aliens in collisions.values():
                self.stats.score += self.settings.alien_points * len(aliens)
            self.sb.prep_score()
            self.sb.check_high_score()
        --snip--</code>
</pre>
<p>如果字典<code>collisions</code>
 存在，就根据消灭了多少外星人更新得分，再调用<code>check_high_score()</code>
 。</p>
<p>第一次玩这个游戏时，当前得分就是最高得分，因此两个地方显示的都是当前得分。但再次开始该游戏时，最高得分会出现在中央，而当前得分则出现在右边，如图14-4所示。</p>
<p class="图"><img alt="" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/14_Image00017.gif" style="width: 80%" width="80%"/>
</p>
<p>
<strong>图14-4　最高得分显示在屏幕顶部中央</strong>
</p>
<h3 id="nav_point_342">14.3.9　显示等级</h3>
<p>为在游戏中显示玩家的等级，首先需要在<code>GameStats</code>
 中添加一个表示当前等级的属性。为确保每次开始新游戏时都重置等级，在<code>reset_stats()</code>
 中初始化它：</p>
<p>
<em>game_stats.py</em>
</p>
<pre class="代码无行号"><code>    def reset_stats(self):
        """初始化随游戏进行可能变化的统计信息。"""
        self.ships_left = self.settings.ship_limit
        self.score = 0
        self.level = 1</code>
</pre>
<p>为了让<code>Scoreboard</code>
 显示当前等级，在<code>__init__()</code>
 中调用一个新方法<code>prep_level()</code>
 ：</p>
<p>
<em>scoreboard.py</em>
</p>
<pre class="代码无行号"><code>    def __init__(self, ai_game):
        --snip--
        self.prep_high_score()
        self.prep_level()</code>
</pre>
<p>
<code>prep_level()</code>
 的代码如下：</p>
<p>
<em>scoreboard.py</em>
</p>
<pre class="代码无行号"><code>      def prep_level(self):
          """将等级转换为渲染的图像。"""
          level_str = str(self.stats.level)
❶         self.level_image = self.font.render(level_str, True,
                  self.text_color, self.settings.bg_color)
          # 将等级放在得分下方。
          self.level_rect = self.level_image.get_rect()
❷         self.level_rect.right = self.score_rect.right
❸         self.level_rect.top = self.score_rect.bottom + 10</code>
</pre>
<p>方法<code>prep_level()</code>
 根据存储在<code>stats.level</code>
 中的值创建一幅图像（见❶），并将其<code>right</code>
 属性设置为得分的<code>right</code>
 属性（见❷）。然后，将<code>top</code>
 属性设置为比得分图像的<code>bottom</code>
 属性大10像素，以便在得分和等级之间留出一定的空间（见❸）。</p>
<p>还需要更新<code>show_score()</code>
 ：</p>
<p>
<em>scoreboard.py</em>
</p>
<pre class="代码无行号"><code>    def show_score(self):
        """在屏幕上显示得分和等级。"""
        self.screen.blit(self.score_image, self.score_rect)
        self.screen.blit(self.high_score_image, self.high_score_rect)
        self.screen.blit(self.level_image, self.level_rect)</code>
</pre>
<p>新增的代码行在屏幕上显示等级图像。</p>
<p>我们在<code>_check_bullet_alien_collisions()</code>
 中提高等级并更新等级图像：</p>
<p>
<em>alien_invasion.py</em>
</p>
<pre class="代码无行号"><code>    def _check_bullet_alien_collisions(self):
        --snip--
        if not self.aliens:
            # 删除现有的子弹并新建一群外星人。
            self.bullets.empty()
            self._create_fleet()
            self.settings.increase_speed()
            # 提高等级。
            self.stats.level += 1
            self.sb.prep_level()</code>
</pre>
<p>如果整群外星人都被消灭，就将<code>stats.level</code>
 的值加1，并调用<code>prep_level()</code>
 确保正确地显示了新等级。</p>
<p>为确保在开始新游戏时更新等级图像，还需在玩家单击按钮Play时调用<code>prep_level()</code>
 ：</p>
<p>
<em>alien_invasion.py</em>
</p>
<pre class="代码无行号"><code>    def _check_play_button(self, mouse_pos):
        --snip--
         if button_clicked and not self.stats.game_active:
             --snip--
             self.sb.prep_score()
             self.sb.prep_level()
             --snip--</code>
</pre>
<p>这里在调用<code>prep_score()</code>
 后立即调用<code>prep_level()</code>
 。</p>
<p>现在可以知道到了多少级，如图14-5所示。</p>
<p class="图"><img alt="" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/14_Image00018.gif" style="width: 80%" width="80%"/>
</p>
<p>
<strong>图14-5　当前等级显示在当前得分的正下方</strong>
</p>
<blockquote>
<p>
<strong>注意</strong>
 　在一些经典游戏中，得分带有标签，如Score、High Score和Level。这里没有显示这些标签，游戏开始后，每个数的含义将一目了然。要包含这些标签，只需在<code>Scoreboard</code>
 中调用<code>font.render()</code>
 前，将它们添加到得分字符串中。</p>
</blockquote>
<h3 id="nav_point_343">14.3.10　显示余下的飞船数</h3>
<p>最后来显示玩家还有多少艘飞船，但使用图形而不是数字。为此，在屏幕左上角绘制飞船图像来指出还余下多少艘飞船，就像众多经典的街机游戏中那样。</p>
<p>首先，需要让<code>Ship</code>
 继承<code>Sprite</code>
 ，以便创建飞船编组：</p>
<p>
<em>ship.py</em>
</p>
<pre class="代码无行号"><code>  import pygame
  from pygame.sprite import Sprite
❶ class Ship(Sprite):
      """管理飞船的类。"""
      def __init__(self, ai_game):
          """初始化飞船并设置其起始位置。"""
❷         super().__init__()
          --snip--</code>
</pre>
<p>这里导入了<code>Sprite</code>
 ，让<code>Ship</code>
 继承<code>Sprite</code>
 （见❶），并在<code>__init__()</code>
 的开头调用<code>super()</code>
 （见❷）。</p>
<p>接下来，需要修改<code>Scoreboard</code>
 ，以创建可供显示的飞船编组。下面是其中的<code>import</code>
 语句：</p>
<p>
<em>scoreboard.py</em>
</p>
<pre class="代码无行号"><code>import pygame.font
from pygame.sprite import Group
from ship import Ship</code>
</pre>
<p>鉴于需要创建飞船编组，导入<code>Group</code>
 和<code>Ship</code>
 类。</p>
<p>下面是方法<code>__init__()</code>
 ：</p>
<p>
<em>scoreboard.py</em>
</p>
<pre class="代码无行号"><code>    def __init__(self, ai_game):
        """初始化记录得分的属性。"""
        self.ai_game = ai_game
        self.screen = ai_game.screen
        --snip--
        self.prep_level()
        self.prep_ships()</code>
</pre>
<p>我们将游戏实例赋给一个属性，因为创建飞船时需要用到它。在调用<code>prep_level()</code>
 后调用了<code>prep_ships()</code>
 。</p>
<p>
<code>prep_ships()</code>
 的代码如下：</p>
<p>
<em>scoreboard.py</em>
</p>
<pre class="代码无行号"><code>      def prep_ships(self):
          """显示还余下多少艘飞船。"""
❶         self.ships = Group()
❷         for ship_number in range(self.stats.ships_left):
              ship = Ship(self.ai_game)
❸             ship.rect.x = 10 + ship_number * ship.rect.width
❹             ship.rect.y = 10
❺             self.ships.add(ship)</code>
</pre>
<p>方法<code>prep_ships()</code>
 创建一个空编组<code>self.ships</code>
 ，用于存储飞船实例（见❶）。为填充这个编组，根据玩家还有多少艘飞船以相应的次数运行一个循环（见❷）。在这个循环中，创建新飞船并设置其<img alt="x" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/14_Image00003.gif"/>
 坐标，让整个飞船编组都位于屏幕左边，且每艘飞船的左边距都为10像素（见❸）。还将<img alt="y" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/14_Image00004.gif"/>
 坐标设置为离屏幕上边缘10像素，让所有飞船都出现在屏幕左上角（见❹）。最后，将每艘新飞船都添加到编组<code>ships</code>
 中（见❺）。</p>
<p>现在需要在屏幕上绘制飞船了：</p>
<p>
<em>scoreboard.py</em>
</p>
<pre class="代码无行号"><code>    def show_score(self):
        """在屏幕上绘制得分、等级和余下的飞船数。"""
        self.screen.blit(self.score_image, self.score_rect)
        self.screen.blit(self.high_score_image, self.high_score_rect)
        self.screen.blit(self.level_image, self.level_rect)
        self.ships.draw(self.screen)</code>
</pre>
<p>为在屏幕上显示飞船，对编组调用<code>draw()</code>
 。Pygame将绘制每艘飞船。</p>
<p>为在游戏开始时让玩家知道自己有多少艘飞船，在开始新游戏时调用<code>prep_ships()</code>
 。这是在<code>AlienInvasion</code>
 的<code>_check_play_button()</code>
 中进行的：</p>
<p>
<em>alien_invasion.py</em>
</p>
<pre class="代码无行号"><code>    def _check_play_button(self, mouse_pos):
        --snip--
     if button_clicked and not self.stats.game_active:
         --snip--
         self.sb.prep_score()
         self.sb.prep_level()
         self.sb.prep_ships()
         --snip--</code>
</pre>
<p>还要在飞船被外星人撞到时调用<code>prep_ships()</code>
 ，从而在玩家损失飞船时更新飞船图像：</p>
<p>
<em>alien_invasion.py</em>
</p>
<pre class="代码无行号"><code>    def _ship_hit(self):
        """响应飞船被外星人撞到。"""
        if self.stats.ships_left &gt; 0:
            # 将ships_left减1并更新记分牌。
            self.stats.ships_left -= 1
            self.sb.prep_ships()
            --snip--</code>
</pre>
<p>这里在将<code>ships_left</code>
 的值减1后调用<code>prep_ships()</code>
 。这样每次损失飞船后，显示的飞船数都是正确的。</p>
<p>图14-6显示了完整的记分系统，它在屏幕左上角指出还余下多少艘飞船。</p>
<p class="图"><img alt="" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/14_Image00019.gif" style="width: 80%" width="80%"/>
</p>
<p>
<strong>图14-6　游戏《外星人入侵》的完整记分系统</strong>
</p>
<blockquote>
<p>
<strong>动手试一试</strong>
</p>
<p>
<strong>练习14-5：空前的最高分</strong>
 　每当玩家关闭并重新开始游戏《外星人入侵》时，最高分都将被重置。请这样修复该问题：调用<code>sys.exit()</code>
 前将最高分写入文件，并在<code>GameStats</code>
 中初始化最高分时从文件中读取它。</p>
<p>
<strong>练习14-6：重构</strong>
 　找出执行多项任务的方法，对其进行重构，让代码高效而有序。例如，对于<code>_check_bullet_alien_collisions()</code>
 ，将在外星人群被消灭干净时开始新等级的代码移到一个名为<code>start_new_level()</code>
 的方法中。再例如，对于<code>Scoreboard</code>
 的方法<code>__init__()</code>
 ，将其中调用四个不同方法的代码移到一个名为<code>prep_images()</code>
 的方法中，以缩短方法<code>__init__()</code>
 。如果你重构了<code>_check_play_button()</code>
 ，方法<code>prep_images()</code>
 也可简化<code>_check_play_button()</code>
 和<code>_start_game()</code>
 。</p>
<blockquote>
<p>
<strong>注意</strong>
 　重构项目前，请阅读附录D，了解如果重构时引入了bug，如何将项目恢复到可正确运行的状态。</p>
</blockquote>
<p>
<strong>练习14-7：扩展游戏《外星人入侵》</strong>
 　想想如何扩展游戏《外星人入侵》。例如，可以让外星人也能够向飞船射击，也可以为飞船添加盾牌，使得只有从两边射来的子弹才能摧毁飞船。另外，还可以使用像<code>pygame.mixer</code>
 这样的模块来添加声音效果，如爆炸声和射击声。</p>
<p>
<strong>练习14-8：终极版侧面射击</strong>
 　模仿项目“外星人入侵”继续开发“侧面射击”。添加一个Play按钮，在适合的情况下加快游戏的节奏，并开发一个记分系统。在开发过程中，务必重构代码，并寻找机会以本章没有介绍的方式定制该游戏。</p>
</blockquote>
<h2 id="nav_point_344">14.4　小结</h2>
<p>在本章中，你学习了如何创建用于开始新游戏的Play按钮，如何检测鼠标事件，以及在游戏处于活动状态时如何隐藏鼠标光标。你可以利用学到的知识在游戏中创建其他按钮，如用于显示玩法说明的Help按钮。你还学习了如何随游戏的进行调整其节奏，如何实现记分系统，以及如何以文本和非文本方式显示信息。</p>
<br style="page-break-after:always"/>
<div></div>
]]></description>
    <pubDate>Thu, 23 Jul 2026 01:22:56 +0000</pubDate>
    <guid isPermaLink="false">14</guid>
</item>
<item>
    <title>Basic Python - 013 - 第 13 章 外星人来了.epub</title>
    <description><![CDATA[<p style='color: #888;'>⏱ 预计阅读时间：113 分钟 (22527 字)</p><hr><h1 id="nav_point_294">第 13 章　外星人来了</h1>
<blockquote>
<p><img alt="" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/13_Image00001.gif" style="width: 16%" width="16%"/>
 　本章将在游戏《外星人入侵》中添加外星人。我们将首先在屏幕上边缘附近添加一个外星人，再生成一群外星人。然后让这群外星人向两边和下面移动，并删除被子弹击中的外星人。最后，显示玩家拥有的飞船数量，并在玩家的飞船用完后结束游戏。</p>
<p>通过阅读本章，你将更深入地了解Pygame和大型项目管理，还将学习如何检测游戏对象之间的碰撞，如子弹和外星人之间的碰撞。检测碰撞有助于定义游戏元素之间的交互。例如，可以将角色限定在迷宫墙壁之内，或者在两个角色之间传球。我们将不时查看游戏开发计划，确保编程工作不偏离轨道。</p>
<p>着手编写在屏幕上添加一群外星人的代码前，先来回顾一下这个项目，并更新开发计划。</p>
</blockquote>
<h2 id="nav_point_295">13.1　项目回顾</h2>
<p>开发大型项目时，要在进入每个开发阶段之前回顾一下开发计划，搞清楚接下来要通过编写代码来完成哪些任务。本章涉及以下内容。</p>
<ul>
<li class="第1级无序列表">研究既有代码，确定实现新功能前是否要重构。</li>
<li class="第1级无序列表">在屏幕左上角添加一个外星人，并指定合适的边距。</li>
<li class="第1级无序列表">根据第一个外星人的边距和屏幕尺寸计算屏幕上可容纳多少个外星人。编写一个循环来创建一系列外星人，使其填满屏幕的上半部分。</li>
<li class="第1级无序列表">让外星人群向两边和下方移动，直到外星人被全部击落、有外星人撞到飞船或有外星人抵达屏幕底端。如果整群外星人都被击落，将再创建一群外星人。如果有外星人撞到了飞船或抵达屏幕底端，将销毁飞船并再创建一群外星人。</li>
<li class="第1级无序列表">限制玩家可用的飞船数量。当配给的飞船用完之后，游戏将结束。</li>
</ul>
<p>我们将在实现功能的同时完善这个计划，但就目前而言，该计划已足够详尽。</p>
<p>在项目中添加新功能前，还应审核既有代码。每进入一个新阶段，项目通常会更复杂，因此最好对混乱或低效的代码进行清理。我们一直在不断重构，因此当前没有需要重构的代码。</p>
<h2 id="nav_point_296">13.2　创建第一个外星人</h2>
<p>在屏幕上放置外星人与放置飞船类似。每个外星人的行为都由<code>Alien</code>
 类控制，我们将像创建<code>Ship</code>
 类那样创建这个类。出于简化考虑，也将使用位图来表示外星人。你可以自己寻找表示外星人的图像，也可以使用如图13-1所示的图像，它可在本书配套资源（ituring.cn/book/2784）中找到。这幅图像的背景为灰色，与屏幕背景色一致。请务必将选择的图像文件保存到文件夹images中。</p>
<p class="图"><img alt="" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/13_Image00008.gif" style="width: 40%" width="40%"/>
</p>
<p>
<strong>图13-1　用来创建外星人群的外星人图像</strong>
</p>
<h3 id="nav_point_297">13.2.1　创建<code>Alien</code>
 类</h3>
<p>下面来编写<code>Alien</code>
 类并将其保存为文件alien.py：</p>
<p>
<em>alien.py</em>
</p>
<pre class="代码无行号"><code>  import pygame
  from pygame.sprite import Sprite
  class Alien(Sprite):
      """表示单个外星人的类。"""
      def __init__(self, ai_game):
          """初始化外星人并设置其起始位置。"""
          super().__init__()
          self.screen = ai_game.screen
          # 加载外星人图像并设置其rect属性
          self.image = pygame.image.load('images/alien.bmp')
          self.rect = self.image.get_rect()
          # 每个外星人最初都在屏幕左上角附近。
❶         self.rect.x = self.rect.width
          self.rect.y = self.rect.height
          # 存储外星人的精确水平位置。
❷         self.x = float(self.rect.x)</code>
</pre>
<p>除位置不同外，这个类的大部分代码与<code>Ship</code>
 类相似。每个外星人最初都位于屏幕左上角附近。将每个外星人的左边距都设置为外星人的宽度，并将上边距设置为外星人的高度（见❶），这样更容易看清。我们主要关心的是外星人的水平速度，因此精确地记录了每个外星人的水平位置（见❷）。</p>
<p>
<code>Alien</code>
 类不需要一个在屏幕上绘制外星人的方法，因为我们将使用一个Pygame编组方法，自动在屏幕上绘制编组中的所有元素。</p>
<h3 id="nav_point_298">13.2.2　创建<code>Alien</code>
 实例</h3>
<p>要让第一个外星人在屏幕上现身，需要创建一个<code>Alien</code>
 实例。这属于设置工作，因此将把这些代码放在<code>AlienInvasion</code>
 类的方法<code>__init__()</code>
 末尾。我们最终会创建一群外星人，涉及的工作量不少，因此将新建一个名为<code>_create_fleet()</code>
 的辅助方法。</p>
<p>在类中，方法的排列顺序无关紧要，只要按统一的标准排列就行。我们将把<code>_create_fleet()</code>
 放在<code>_update_screen()</code>
 前面，不过放在<code>AlienInvasion</code>
 类的任何地方其实都可行。首先，需要导入<code>Alien</code>
 类。</p>
<p>下面是alien_invasion.py中修改后的<code>import</code>
 语句：</p>
<p>
<em>alien_invasion.py</em>
</p>
<pre class="代码无行号"><code>--snip--
from bullet import Bullet
from alien import Alien</code>
</pre>
<p>下面是修改后的方法<code>__init__()</code>
 ：</p>
<p>
<em>alien_invasion.py</em>
</p>
<pre class="代码无行号"><code>    def __init__(self):
        --snip--
        self.ship = Ship(self)
        self.bullets = pygame.sprite.Group()
        self.aliens = pygame.sprite.Group()
        self._create_fleet()</code>
</pre>
<p>创建了一个用于存储外星人群的编组，还调用了接下来将编写的方法<code>_create_fleet()</code>
 。</p>
<p>下面是新编写的方法<code>_create_fleet()</code>
 ：</p>
<p>
<em>alien_invasion.py</em>
</p>
<pre class="代码无行号"><code>    def _create_fleet(self):
        """创建外星人群。"""
        # 创建一个外星人。
        alien = Alien(self)
        self.aliens.add(alien)</code>
</pre>
<p>在这个方法中，创建了一个<code>Alien</code>
 实例，再将其添加到用于存储外星人群的编组中。外星人默认放在屏幕左上角附近，对第一个外星人来说，这样的位置非常合适。</p>
<p>要让外星人现身，需要在<code>_update_screen()</code>
 中对外星人编组调用方法<code>draw()</code>
 ：</p>
<p>
<em>alien_invasion.py</em>
</p>
<pre class="代码无行号"><code>    def _update_screen(self):
        --snip--
        for bullet in self.bullets.sprites():
            bullet.draw_bullet()
        self.aliens.draw(self.screen)
        pygame.display.flip()</code>
</pre>
<p>对编组调用<code>draw()</code>
 时，Pygame将把编组中的每个元素绘制到属性<code>rect</code>
 指定的位置。方法<code>draw()</code>
 接受一个参数，这个参数指定了要将编组中的元素绘制到哪个surface上。图13-2显示了在屏幕上现身的第一个外星人。</p>
<p class="图"><img alt="{%}" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/13_Image00009.gif"/>
</p>
<p>
<strong>图13-2　第一个外星人现身</strong>
</p>
<p>第一个外星人正确地现身了，下面来编写绘制一群外星人的代码。</p>
<h2 id="nav_point_299">13.3　创建一群外星人</h2>
<p>要绘制一群外星人，需要确定一行能容纳多少个外星人以及要绘制多少行。我们将首先计算外星人的水平间距并创建一行外星人，再确定可用的垂直空间并创建整群外星人。</p>
<h3 id="nav_point_300">13.3.1　确定一行可容纳多少个外星人</h3>
<p>为确定一行可容纳多少个外星人，来看看可用的水平空间有多大。屏幕宽度存储在<code>settings.screen_width</code>
 中，但需要在屏幕两边都留下一定的边距（将其设置为外星人的宽度）。因为有两个边距，所以可用于放置外星人的水平空间为屏幕宽度减去外星人宽度的两倍：</p>
<pre class="代码无行号"><code>available_space_x = settings.screen_width – (2 * alien_width)</code>
</pre>
<p>还需要在外星人之间留出一定的空间，不妨将其定为外星人的宽度。因此，显示一个外星人所需的水平空间为外星人宽度的两倍：一个宽度用于放置外星人，另一个宽度为外星人右边的空白区域。为确定一行可容纳多少个外星人，将可用空间除以外星人宽度的两倍。我们使用<strong>整除</strong>
 （floor division）运算符<code>//</code>
 ，它将两个数相除并丢弃余数，让我们得到一个表示外星人个数的整数。</p>
<pre class="代码无行号"><code>number_aliens_x = available_space_x // (2 * alien_width)</code>
</pre>
<p>我们将在创建外星人群时使用这些公式。</p>
<blockquote>
<p>
<strong>注意</strong>
 　令人欣慰的是，在程序中执行计算时，无须在一开始确定公式是正确的，而是可以尝试运行程序，看看结果是否符合预期。即便是在最坏的情况下，也只是屏幕上显示的外星人太多或太少。随后可根据在屏幕上看到的情况调整计算公式。</p>
</blockquote>
<h3 id="nav_point_301">13.3.2　创建一行外星人</h3>
<p>现在可以创建整行外星人了。由于创建单个外星人的代码管用，我们重写<code>_create_fleet()</code>
 使其创建一行外星人：</p>
<p>
<em>alien_invasion.py</em>
</p>
<pre class="代码无行号"><code>      def _create_fleet(self):
          """创建外星人群。"""
          # 创建一个外星人并计算一行可容纳多少个外星人。
          # 外星人的间距为外星人宽度。
❶         alien = Alien(self)
❷         alien_width = alien.rect.width
❸         available_space_x = self.settings.screen_width - (2 * alien_width)
          number_aliens_x = available_space_x // (2 * alien_width)
          # 创建第一行外星人。
❹         for alien_number in range(number_aliens_x):
              # 创建一个外星人并将其加入当前行。
              alien = Alien(self)
❺             alien.x = alien_width + 2 * alien_width * alien_number
              alien.rect.x = alien.x
              self.aliens.add(alien)</code>
</pre>
<p>这些代码大多在前面详细介绍过。为放置外星人，需要知道外星人的宽度和高度，因此在执行计算前，创建一个外星人（见❶）。这个外星人不是外星人群的成员，因此没有将其加入编组<code>aliens</code>
 中。在❷处，从外星人的<code>rect</code>
 属性中获取外星人宽度，并将这个值存储到<code>alien_width</code>
 中，以免反复访问属性<code>rect</code>
 。在❸处，计算可用于放置外星人的水平空间以及其中可容纳多少个外星人。</p>
<p>接下来，编写一个循环，从零数到要创建的外星人数（见❹）。在这个循环中，创建一个新的外星人，并通过设置<img alt="x" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/13_Image00003.gif"/>
 坐标将其加入当前行（见❺）。将每个外星人都往右推一个外星人宽度。接下来，将外星人宽度乘以2，得到每个外星人占据的空间（其中包括右边的空白区域），再据此计算当前外星人在当前行的位置。我们使用外星人的属性<code>x</code>
 来设置其<code>rect</code>
 的位置。最后，将每个新创建的外星人都添加到编组<code>aliens</code>
 中。</p>
<p>如果现在运行这个游戏，将看到第一行外星人，如图13-3所示。</p>
<p class="图"><img alt="{%}" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/13_Image00010.gif"/>
</p>
<p>
<strong>图13-3　第一行外星人</strong>
</p>
<p>这行外星人在屏幕上稍微偏向了左边、这实际上是有好处的，因为后面将让外星人群往右移，触及屏幕边缘后稍微往下移，再往左移，依此类推。就像经典游戏《太空入侵者》，相比于只往下移，这种移动方式更为有趣。我们将让外星人群不断这样移动，直到所有外星人都被击落，或者有外星人撞上飞船或抵达屏幕底端。</p>
<blockquote>
<p>
<strong>注意</strong>
 　根据所选择的屏幕宽度，在你的系统中，第一个外星人的位置可能稍有不同。</p>
</blockquote>
<h3 id="nav_point_302">13.3.3　重构<code>_create_fleet()</code>
</h3>
<p>倘若只需使用前面的代码就能创建外星人群，也许应该让<code>_create_fleet()</code>
 保持原样，但鉴于创建外星人群的工作还未完成，我们稍微整理一下这个方法。为此，添加辅助方法<code>_create_alien()</code>
 ，并在<code>_create_fleet()</code>
 中调用它：</p>
<p>
<em>alien_invasion.py</em>
</p>
<pre class="代码无行号"><code>    def _create_fleet(self):
        --snip--
        # 创建第一行外星人。
        for alien_number in range(number_aliens_x):
            self._create_alien(alien_number)
    def _create_alien(self, alien_number):
        """创建一个外星人并将其放在当前行。"""
        alien = Alien(self)
        alien_width = alien.rect.width
        alien.x = alien_width + 2 * alien_width * alien_number
        alien.rect.x = alien.x
        self.aliens.add(alien)</code>
</pre>
<p>除<code>self</code>
 外，方法<code>_create_alien()</code>
 还接受另一个参数，即要创建的外星人的编号。该方法的代码与<code>_create_fleet()</code>
 相同，但在内部获取外星人宽度，而不是将其作为参数传入。这样重构后，添加新行进而创建整群外星人将更容易。</p>
<h3 id="nav_point_303">13.3.4　添加行</h3>
<p>要创建外星人群，需要计算屏幕可容纳多少行，并将创建一行外星人的循环重复执行相应的次数。为计算可容纳的行数，要先计算可用的垂直空间：用屏幕高度减去第一行外星人的上边距（外星人高度）、飞船的高度以及外星人群最初与飞船之间的距离（外星人高度的两倍）：</p>
<pre class="代码无行号"><code>available_space_y = settings.screen_height – (3 * alien_height) – ship_height</code>
</pre>
<p>这将在飞船上方留出一定的空白区域，给玩家留出射杀外星人的时间。</p>
<p>每行下方都要留出一定的空白区域，不妨将其设置为外星人的高度。为计算可容纳的行数，将可用的垂直空间除以外星人高度的两倍。我们使用整除，因为行数只能是整数。（同样，如果这样的计算不对，我们马上就能发现，继而将间距调整为合理的值。）</p>
<pre class="代码无行号"><code>number_rows = available_space_y // (2 * alien_height)</code>
</pre>
<p>知道可容纳多少行之后，便可重复执行创建一行外星人的代码了：</p>
<p>
<em>alien_invasion.py</em>
</p>
<pre class="代码无行号"><code>      def _create_fleet(self):
          --snip--
          alien = Alien(self)
❶         alien_width, alien_height = alien.rect.size
          available_space_x = self.settings.screen_width - (2 * alien_width)
          number_aliens_x = available_space_x // (2 * alien_width)
          #计算屏幕可容纳多少行外星人。
          ship_height = self.ship.rect.height
❷         available_space_y = (self.settings.screen_height -
                                   (3 * alien_height) - ship_height)
          number_rows = available_space_y // (2 * alien_height)
          # 创建外星人群。
❸         for row_number in range(number_rows):
              for alien_number in range(number_aliens_x):
                  self._create_alien(alien_number, row_number)
      def _create_alien(self, alien_number, row_number):
          """创建一个外星人，并将其放在当前行。"""
          alien = Alien(self)
          alien_width, alien_height = alien.rect.size
          alien.x = alien_width + 2 * alien_width * alien_number
          alien.rect.x = alien.x
❹         alien.rect.y = alien.rect.height + 2 * alien.rect.height * row_number
          self.aliens.add(alien)</code>
</pre>
<p>需要知道外星人的宽度和高度，因此在❶处使用了属性<code>size</code>
 。该属性是一个元组，包含<code>rect</code>
 对象的宽度和高度。为计算屏幕可容纳多少行外星人，在计算<code>available_space_x</code>
 的代码后面添加了计算<code>available_space_y</code>
 的代码（见❷）。此处将计算公式用圆括号括起来，以便将代码分成两行，遵循每行不超过79字符的建议。</p>
<p>为创建多行外星人，使用了两个嵌套在一起的循环：一个外部循环和一个内部循环（见❸）。内部循环创建一行外星人，而外部循环从零数到要创建的外星人行数：Python将重复执行创建单行外星人的代码，重复次数为<code>number_rows</code>
 。</p>
<p>为嵌套循环，编写了一个新的<code>for</code>
 循环，并缩进了要重复执行的代码。（在大多数文本编辑器中，缩进代码块和取消缩进都很容易，详情请参阅附录B）。现在调用<code>_create_alien()</code>
 时，传递了一个表示行号的实参，将每行都沿屏幕依次向下放置。</p>
<p>在<code>_create_alien()</code>
 的定义中，需要一个用于存储行号的形参。在<code>_create_alien()</code>
 中，修改外星人的<img alt="y" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/13_Image00004.gif"/>
 坐标（见❹）并在第一行外星人上方留出与外星人等高的空白区域。相邻外星人行的<img alt="y" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/13_Image00004.gif"/>
 坐标相差外星人高度的两倍，因此将外星人高度乘以2，再乘以行号。第一行的行号为0，因此第一行的垂直位置不变，而其他行都沿屏幕依次向下放置。</p>
<p>如果现在运行这个游戏，将看到一群外星人，如图13-4所示。</p>
<p class="图"><img alt="{%}" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/13_Image00011.gif"/>
</p>
<p>
<strong>图13-4　整群外星人都现身了</strong>
</p>
<p>下一节将让外星人群动起来！</p>
<blockquote>
<p>
<strong>动手试一试</strong>
</p>
<p>
<strong>练习13-1：星星</strong>
 　找一幅星星图像，并在屏幕上显示一系列整齐排列的星星。</p>
<p>
<strong>练习13-2：更逼真的星星</strong>
 　为让星星的分布更逼真，可随机地放置星星。本书前面说过，可像下面这样来生成随机数：</p>
<pre class="代码无行号"><code>from random import randint
random_number = randint(-10, 10)</code>
</pre>
<p>上述代码返回一个-10和10之间的随机整数。在为完成练习13-1而编写的程序中，随机地调整每颗星星的位置。</p>
</blockquote>
<h2 id="nav_point_304">13.4　让外星人群移动</h2>
<p>下面来让外星人群在屏幕上向右移动，撞到屏幕边缘后下移一定的量，再沿相反的方向移动。我们将不断移动所有的外星人，直到外星人被全部消灭，或者有外星人撞上飞船或抵达屏幕底端。下面先让外星人向右移动。</p>
<h3 id="nav_point_305">13.4.1　向右移动外星人群</h3>
<p>为移动外星人群，将使用alien.py中的方法<code>update()</code>
 。对于外星人群中的每个外星人，都要调用它。首先，添加一个控制外星人速度的设置：</p>
<p>
<em>settings.py</em>
</p>
<pre class="代码无行号"><code>    def __init__(self):
        --snip--
        # 外星人设置
        self.alien_speed = 1.0</code>
</pre>
<p>再使用这个设置来实现<code>update()</code>
 ：</p>
<p>
<em>alien.py</em>
</p>
<pre class="代码无行号"><code>      def __init__(self, ai_game):
          """初始化外星人并设置其初始位置。"""
          super().__init__()
          self.screen = ai_game.screen
          self.settings = ai_game.settings
          --snip--
      def update(self):
          """向右移动外星人。"""
❶         self.x += self.settings.alien_speed
❷         self.rect.x = self.x</code>
</pre>
<p>在<code>__init__()</code>
 中添加了属性<code>settings</code>
 ，以便能够在<code>update()</code>
 中访问外星人的速度。每次更新外星人时，都将它向右移动，移动量为<code>alien_speed</code>
 的值。我们使用属性<code>self.x</code>
 跟踪每个外星人的准确位置，该属性可存储小数值（见❶）。然后，使用<code>self.x</code>
 的值来更新外星人的<code>rect</code>
 的位置（见❷）。</p>
<p>主<code>while</code>
 循环中已调用了更新飞船和子弹的方法，现在还需更调用更新每个外星人位置的方法：</p>
<p>
<em>alien_invasion.py</em>
</p>
<pre class="代码无行号"><code>        while True:
            self._check_events()
            self.ship.update()
            self._update_bullets()
            self._update_aliens()
            self._update_screen()</code>
</pre>
<p>需要编写一些代码来管理外星人群的移动，因此新建一个名为<code>_update_aliens()</code>
 的方法。我们在更新子弹后再更新外星人的位置，因为稍后要检查是否有子弹击中了外星人。</p>
<p>将这个方法放在模块的什么地方都无关紧要，但为确保代码组织有序，我将它放在方法<code>_update_bullets()</code>
 的后面，以便与<code>while</code>
 循环中的调用顺序一致。下面是<code>_update_aliens()</code>
 的第一版：</p>
<p>
<em>alien_invasion.py</em>
</p>
<pre class="代码无行号"><code>    def _update_aliens(self):
        """更新外星人群中所有外星人的位置。"""
        self.aliens.update()</code>
</pre>
<p>对编组调用方法<code>update()</code>
 ，这将自动对每个外星人调用方法<code>update()</code>
 。如果现在运行这个游戏，你将看到外星人群向右移动，并在屏幕右边缘消失。</p>
<h3 id="nav_point_306">13.4.2　创建表示外星人移动方向的设置</h3>
<p>下面来创建让外星人撞到屏幕右边缘后向下移动、再向左移动的设置。实现这种行为的代码如下：</p>
<p>
<em>settings.py</em>
</p>
<pre class="代码无行号"><code>         # 外星人设置
         self.alien_speed = 1.0
         self.fleet_drop_speed = 10
         # fleet_direction为1表示向右移，为-1表示向左移。
         self.fleet_direction = 1</code>
</pre>
<p>设置<code>fleet_drop_speed</code>
 指定有外星人撞到屏幕边缘时，外星人群向下移动的速度。将这个速度与水平速度分开是有好处的，便于分别调整这两个速度。</p>
<p>要实现设置<code>fleet_direction</code>
 ，可将其设置为文本值，如<code>'left'</code>
 或<code>'right'</code>
 ，但这样就必须编写<code>if-elif</code>
 语句来检查外星人群的移动方向。鉴于只有两个可能的方向，我们使用值1和-1来表示，并在外星人群改变方向时在这两个值之间切换。（向右移时需要增大每个外星人的<img alt="x" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/13_Image00003.gif"/>
 坐标，而向左移时需要减小每个外星人的<img alt="x" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/13_Image00003.gif"/>
 坐标，因此使用数字来表示方向十分合理。）</p>
<h3 id="nav_point_307">13.4.3　检查外星人是否撞到了屏幕边缘</h3>
<p>现在需要编写一个方法来检查外星人是否撞到了屏幕边缘，还需修改<code>update()</code>
 让每个外星人都沿正确的方向移动。这些代码位于<code>Alien</code>
 类中：</p>
<p>
<em>alien.py</em>
</p>
<pre class="代码无行号"><code>      def check_edges(self):
          """如果外星人位于屏幕边缘，就返回True。"""
          screen_rect = self.screen.get_rect()
❶         if self.rect.right &gt;= screen_rect.right or self.rect.left &lt;= 0:
                return True
      def update(self):
          """向左或向右移动外星人。"""
❷         self.x += (self.settings.alien_speed *
                          self.settings.fleet_direction)
          self.rect.x = self.x</code>
</pre>
<p>可对任意外星人调用新方法<code>check_edges()</code>
 ，看其是否位于屏幕左边缘或右边缘。如果外星人的<code>rect</code>
 的属性<code>right</code>
 大于或等于屏幕的<code>rect</code>
 的<code>right</code>
 属性，就说明外星人位于屏幕右边缘；如果外星人的<code>rect</code>
 的<code>left</code>
 属性小于或等于0，就说明外星人位于屏幕左边缘（见❶）。</p>
<p>我们修改方法<code>update()</code>
 ，将移动量设置为外星人速度和<code>fleet_direction</code>
 的乘积，让外星人向左或向右移动（见❷）。如果<code>fleet_direction</code>
 为1，就将外星人的当前<img alt="x" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/13_Image00003.gif"/>
 坐标增大<code>alien_speed</code>
 ，从而将外星人向右移；如果<code>fleet_direction</code>
 为-1，就将外星人的当前<img alt="x" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/13_Image00003.gif"/>
 坐标减去<code>alien_speed</code>
 ，从而将外星人向左移。</p>
<h3 id="nav_point_308">13.4.4　向下移动外星人群并改变移动方向</h3>
<p>有外星人到达屏幕边缘时，需要将整群外行星下移，并改变它们的移动方向。为此，需要在<code>AlienInvasion</code>
 中添加一些代码，因为要在这里检查是否有外星人到达了左边缘或右边缘。我们编写方法<code>_check_fleet_edges()</code>
 和<code>_change_fleet_direction()</code>
 ，并且修改<code>_update_aliens()</code>
 。这些新方法将放在<code>_create_alien()</code>
 后面，但其实放在<code>AlienInvasion</code>
 类中的什么位置都无关紧要：</p>
<p>
<em>alien_invasion.py</em>
</p>
<pre class="代码无行号"><code>      def _check_fleet_edges(self):
          """有外星人到达边缘时采取相应的措施。"""
❶         for alien in self.aliens.sprites():
              if alien.check_edges():
❷                 self._change_fleet_direction()
                  break
      def _change_fleet_direction(self):
          """将整群外星人下移，并改变它们的方向。"""
          for alien in self.aliens.sprites():
❸             alien.rect.y += self.settings.fleet_drop_speed
          self.settings.fleet_direction *= -1</code>
</pre>
<p>在<code>_check_fleet_edges()</code>
 中，遍历外星人群并对其中的每个外星人调用<code>check_edges()</code>
 （见❶）。如果<code>check_edges()</code>
 返回<code>True</code>
 ，就表明相应的外星人位于屏幕边缘，需要改变外星人群的方向，因此调用<code>_change_fleet_direction()</code>
 并退出循环（见❷）。在<code>_change_fleet_direction()</code>
 中，遍历所有外星人，将每个外星人下移设置<code>fleet_drop_speed</code>
 的值（见❸）。然后，将<code>fleet_direction</code>
 的值改为其当前值与-1的乘积。调整外星人群移动方向的代码行没有包含在<code>for</code>
 循环中，因为我们要调整每个外星人的垂直位置，但只想调整外星人群移动方向一次。</p>
<p>下面显示了对<code>_update_aliens()</code>
 所做的修改：</p>
<p>
<em>alien_invasion.py</em>
</p>
<pre class="代码无行号"><code>    def _update_aliens(self):
        """
        检查是否有外星人位于屏幕边缘，
        并更新整群外星人的位置。
        """
        self._check_fleet_edges()
        self.aliens.update()</code>
</pre>
<p>我们将方法<code>_update_aliens()</code>
 修改成了先调用<code>_check_fleet_edges()</code>
 ，再更新每个外星人的位置。</p>
<p>如果现在运行这个游戏，外星人群将在屏幕上来回移动，并在抵达屏幕边缘后向下移动。现在可以开始射杀外星人，并检查是否有外星人撞到飞船或抵达了屏幕底端。</p>
<blockquote>
<p>
<strong>动手试一试</strong>
</p>
<p>
<strong>练习13-3：雨滴</strong>
 　寻找一幅雨滴图像，并创建一系列整齐排列的雨滴。让这些雨滴往下落，直到到达屏幕底端后消失。</p>
<p>
<strong>练习13-4：连绵细雨</strong>
 　修改为完成练习11-3而编写的代码，使得一行雨滴消失在屏幕底端后，屏幕顶端又出现一行新雨滴并开始往下落。</p>
</blockquote>
<h2 id="nav_point_309">13.5　射杀外星人</h2>
<p>我们创建了飞船和外星人群，但子弹击中外星人时将穿过外星人，因为还没有检查碰撞。在游戏编程中，<strong>碰撞</strong>
 指的是游戏元素重叠在一起。要让子弹能够击落外星人，我们将使用<code>sprite.groupcollide()</code>
 检测两个编组的成员之间的碰撞。</p>
<h3 id="nav_point_310">13.5.1　检测子弹与外星人的碰撞</h3>
<p>子弹击中外星人时，我们需要马上知道，以便碰撞发生后让子弹立即消失。为此，我们将在更新子弹的位置后立即检测碰撞。</p>
<p>函数<code>sprite.groupcollide()</code>
 将一个编组中每个元素的<code>rect</code>
 同另一个编组中每个元素的<code>rect</code>
 进行比较。在这里，是将每颗子弹的<code>rect</code>
 同每个外星人的<code>rect</code>
 进行比较，并返回一个字典，其中包含发生了碰撞的子弹和外星人。在这个字典中，每个键都是一颗子弹，而关联的值是被该子弹击中的外星人（第14章实现记分系统时，也将使用该字典）。</p>
<p>在方法<code>_update_bullets()</code>
 末尾，添加如下检查子弹和外星人碰撞的代码：</p>
<p>
<em>alien_invasion.py</em>
</p>
<pre class="代码无行号"><code>    def _update_bullets(self):
        """更新子弹的位置，并删除消失的子弹。"""
        --snip--
        # 检查是否有子弹击中了外星人。
        #  如果是，就删除相应的子弹和外星人。
        collisions = pygame.sprite.groupcollide(
                self.bullets, self.aliens, True, True)</code>
</pre>
<p>这些新增的代码将<code>self.bullets</code>
 中所有的子弹都与<code>self.aliens</code>
 中所有的外星人进行比较，看它们是否重叠在一起。每当有子弹和外星人的<code>rect</code>
 重叠时，<code>groupcollide()</code>
 就在它返回的字典中添加一个键值对。两个实参<code>True</code>
 让Pygame删除发生碰撞的子弹和外星人。（要模拟能够飞行到屏幕顶端、消灭击中的每个外星人的高能子弹，可将第一个布尔实参设置为<code>False</code>
 ，并保留第二个布尔参数为<code>True</code>
 。这样被击中的外星人将消失，但所有的子弹都始终有效，直到抵达屏幕顶端后消失。）</p>
<p>如果此时运行这个游戏，被击中的外星人将消失。如图13-5所示，有些外星人被射杀了。</p>
<p class="图"><img alt="{%}" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/13_Image00012.gif"/>
</p>
<p>
<strong>图13-5　可以射杀外星人了</strong>
</p>
<h3 id="nav_point_311">13.5.2　为测试创建大子弹</h3>
<p>只需运行这个游戏就可测试很多功能，但有些功能在正常情况下测试起来比较烦琐。例如，要测试代码能否正确处理外星人编组为空的情形，需要花很长时间将屏幕上的外星人全部射杀。</p>
<p>测试有些功能时，可以修改游戏的某些设置，以便能够专注于游戏的特定方面。例如，可以缩小屏幕以减少需要射杀的外星人数量，也可以提高子弹的速度，以便能够在单位时间内发射大量子弹。</p>
<p>测试这个游戏时，我喜欢做的一项修改是，增大子弹的尺寸并使其在击中外星人后依然有效，如图13-6所示。请尝试将<code>bullet_width</code>
 设置为300乃至3000，看看将所有外星人全部射杀有多快！</p>
<p class="图"><img alt="{%}" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/13_Image00013.gif"/>
</p>
<p>
<strong>图13-6　威力超强的子弹让游戏的有些方法测试起来更容易</strong>
</p>
<p>这样的修改可提高测试效率，还可能激发出如何赋予玩家更大威力的思想火花。（完成测试后，别忘了将设置恢复正常。）</p>
<h3 id="nav_point_312">13.5.3　生成新的外星人群</h3>
<p>这个游戏的一个重要特点是，外星人无穷无尽：一群外星人被消灭后，又会出现另一群外星人。</p>
<p>要在一群外星人被消灭后再显示一群外星人，首先需要检查编组<code>aliens</code>
 是否为空。如果是，就调用<code>_create_fleet()</code>
 。我们将在<code>_update_bullets()</code>
 末尾执行这项任务，因为外星人都是在这里被消灭的：</p>
<p>
<em>alien_invasion.py</em>
</p>
<pre class="代码无行号"><code>      def _update_bullets(self):
          --snip--
❶         if not self.aliens:
              # 删除现有的子弹并新建一群外星人。
❷             self.bullets.empty()
              self._create_fleet()</code>
</pre>
<p>在❶处，检查编组<code>aliens</code>
 是否为空。空编组相当于<code>False</code>
 ，因此这是一种检查编组是否为空的简单方式。如果编组<code>aliens</code>
 为空，就使用方法<code>empty()</code>
 删除编组中余下的所有精灵，从而删除现有的所有子弹（见❷）。我们还调用了<code>_create_fleet()</code>
 ，在屏幕上重新显示一群外星人。</p>
<p>现在，当前这群外星人被消灭干净后，将立刻出现一群新的外星人。</p>
<h3 id="nav_point_313">13.5.4　提高子弹的速度</h3>
<p>如果现在尝试在游戏中射杀外星人，可能会发现子弹的速度不太合适（有点快或有点慢），游戏感不好。当前，可通过修改设置让这款游戏更有意思、更好玩。</p>
<p>要修改子弹的速度，可调整settings.py中<code>bullet_speed</code>
 的值。在我的系统中，我把<code>bullet_speed</code>
 的值调整到1.5，让子弹的速度快些：</p>
<p>
<em>settings.py</em>
</p>
<pre class="代码无行号"><code>         # 子弹设置
         self.bullet_speed = 1.5
         self.bullet_width = 3
         --snip--</code>
</pre>
<p>这项设置的最佳值取决于你使用的系统的速度，请找出适合自己的值。你也可以调整其他设置。</p>
<h3 id="nav_point_314">13.5.5　重构<code>_update_bullets()</code>
</h3>
<p>下面来重构<code>_update_bullets()</code>
 ，使其不再执行那么多任务。为此，将处理子弹和外星人碰撞的代码移到一个独立的方法中：</p>
<p>
<em>alien_invasion.py</em>
</p>
<pre class="代码无行号"><code>    def _update_bullets(self):
        --snip--
        # 删除消失的子弹。
        for bullet in self.bullets.copy():
            if bullet.rect.bottom &lt;= 0:
                self.bullets.remove(bullet)
        self._check_bullet_alien_collisions()
    def _check_bullet_alien_collisions(self):
        """响应子弹和外星人碰撞。"""
        # 删除发生碰撞的子弹和外星人。
        collisions = pygame.sprite.groupcollide(
                self.bullets, self.aliens, True, True)
        if not self.aliens:
            # 删除现有的所有子弹，并创建一群新的外星人。
            self.bullets.empty()
            self._create_fleet()</code>
</pre>
<p>我们创建了一个新方法<code>_check_bullet_alien_collisions()</code>
 ，用于检测子弹和外星人之间的碰撞，并在整群外星人被消灭干净时采取相应的措施。这能避免<code>_update_bullets()</code>
 过长，简化了后续开发工作。</p>
<blockquote>
<p>
<strong>动手试一试</strong>
</p>
<p>
<strong>练习13-5：侧面射击2</strong>
 　完成练习12-6之后，我们给游戏《外星人入侵》添加了很多功能。在本练习中，请尝试让练习12-6中飞船的功能与当前《外星人入侵》中的类似。在屏幕右侧添加一群外星人（或让外星人的位置随机），并让其向飞船移动。另外，编写代码让被子弹击中的外星人消失。</p>
</blockquote>
<h2 id="nav_point_315">13.6　结束游戏</h2>
<p>如果玩家根本不会输，游戏还有什么趣味和挑战性可言？如果玩家没能在足够短的时间内将整群外星人消灭干净，导致有外星人撞到了飞船或抵达屏幕底端，飞船将被摧毁。与此同时，限制玩家可使用的飞船数，在玩家用光所有的飞船后，游戏将结束。</p>
<h3 id="nav_point_316">13.6.1　检测外星人和飞船碰撞</h3>
<p>首先检查外星人和飞船之间的碰撞，以便在外星人撞上飞船时做出合适的响应。为此，在<code>AlienInvasion</code>
 中更新每个外星人的位置后，立即检测外星人和飞船之间的碰撞：</p>
<p>
<em>alien_invasion.py</em>
</p>
<pre class="代码无行号"><code>      def _update_aliens(self):
          --snip--
           self.aliens.update()
           # 检测外星人和飞船之间的碰撞。
❶         if pygame.sprite.spritecollideany(self.ship, self.aliens):
❷             print("Ship hit!!!")</code>
</pre>
<p>函数<code>spritecollideany()</code>
 接受两个实参：一个精灵和一个编组。它检查编组是否有成员与精灵发生了碰撞，并在找到与精灵发生碰撞的成员后停止遍历编组。在这里，它遍历编组<code>aliens</code>
 ，并返回找到的第一个与飞船发生碰撞的外星人。</p>
<p>如果没有发生碰撞，<code>spritecollideany()</code>
 将返回<code>None</code>
 ，因此❶处的<code>if</code>
 代码块不会执行。如果找到了与飞船发生碰撞的外星人，它就返回这个外星人，因此<code>if</code>
 代码块将执行：打印“Ship hit!!!”（见❷）。有外星人撞到飞船时，需要执行很多任务：删除余下的外星人和子弹，让飞船重新居中，以及创建一群新的外星人。编写完成这些任务的代码之前，需要确定检测外星人和飞船碰撞的方法是否可行。为此，最简单的方式就是调用函数<code>print()</code>
 。</p>
<p>现在如果运行这个游戏，则每当有外星人撞到飞船时，终端窗口都将显示“Ship hit!!!”。测试这项功能时，请将<code>alien_drop_speed</code>
 设置为较大的值，如50或100，这样外星人将更快地撞到飞船。</p>
<h3 id="nav_point_317">13.6.2　响应外星人和飞船碰撞</h3>
<p>现在需要确定当外星人与飞船发生碰撞时该做些什么。我们不销毁<code>Ship</code>
 实例并创建新的，而是通过跟踪游戏的统计信息来记录飞船被撞了多少次（跟踪统计信息还有助于记分）。</p>
<p>下面来编写一个用于跟踪游戏统计信息的新类<code>GameStats</code>
 ，并将其保存为文件game_stats.py：</p>
<p>
<em>game_stats.py</em>
</p>
<pre class="代码无行号"><code>class GameStats:
    """跟踪游戏的统计信息。"""
    def __init__(self, ai_game):
        """初始化统计信息。"""
        self.settings = ai_game.settings
        self.reset_stats()
    def reset_stats(self):
        """初始化在游戏运行期间可能变化的统计信息。"""
        self.ships_left = self.settings.ship_limit</code>
</pre>
<p>在游戏运行期间，只创建一个<code>GameStats</code>
 实例，但每当玩家开始新游戏时，需要重置一些统计信息。为此，在方法<code>reset_stats()</code>
 中初始化大部分统计信息，而不是在<code>__init__()</code>
 中直接初始化。我们在<code>__init__()</code>
 中调用这个方法，这样创建<code>GameStats</code>
 实例时将妥善地设置这些统计信息，在玩家开始新游戏时也能调用<code>reset_stats()</code>
 。</p>
<p>当前，只有一项统计信息<code>ships_left</code>
 ，其值在游戏运行期间不断变化。一开始玩家拥有的飞船数存储在settings.py的<code>ship_limit</code>
 中：</p>
<p>
<em>settings.py</em>
</p>
<pre class="代码无行号"><code>        # 飞船设置
        self.ship_speed = 1.5
        self.ship_limit = 3</code>
</pre>
<p>还需对alien_invasion.py做些修改，以创建一个<code>GameStats</code>
 实例。首先，更新这个文件开头的<code>import</code>
 语句：</p>
<p>
<em>alien_invasion.py</em>
</p>
<pre class="代码无行号"><code>import sys
from time import sleep
import pygame
from settings import Settings
from game_stats import GameStats
from ship import Ship
--snip--</code>
</pre>
<p>从Python标准库的模块<code>time</code>
 中导入函数<code>sleep()</code>
 ，以便在飞船被外星人撞到后让游戏暂停片刻。我们还导入了<code>GameStats</code>
 。</p>
<p>接下来，在<code>__init__()</code>
 中创建一个<code>GameStats</code>
 实例：</p>
<p>
<em>alien_invasion.py</em>
</p>
<pre class="代码无行号"><code>    def __init__(self):
        --snip--
        self.screen = pygame.display.set_mode(
            (self.settings.screen_width, self.settings.screen_height))
        pygame.display.set_caption("Alien Invasion")
        # 创建一个用于存储游戏统计信息的实例。
        self.stats = GameStats(self)
        self.ship = Ship(self)
        --snip--</code>
</pre>
<p>在创建游戏窗口后、定义诸如飞船等其他游戏元素前，创建一个<code>GameStats</code>
 实例。</p>
<p>有外星人撞到飞船时，将余下的飞船数减1，创建一群新的外星人，并将飞船重新放到屏幕底端的中央。另外，让游戏暂停片刻，让玩家在新外星人群出现前注意到发生了碰撞并将重新创建外星人群。</p>
<p>下面将实现这些功能的大部分代码放到新方法<code>_ship_hit()</code>
 中。在<code>_update_aliens()</code>
 中，将在有外星人撞到飞船时调用这个方法：</p>
<p>
<em>alien_invasion.py</em>
</p>
<pre class="代码无行号"><code>      def _ship_hit(self):
          """响应飞船被外星人撞到。"""
          # 将ships_left减1。
❶         self.stats.ships_left -= 1
          # 清空余下的外星人和子弹。
❷         self.aliens.empty()
          self.bullets.empty()
          # 创建一群新的外星人，并将飞船放到屏幕底端的中央。
❸         self._create_fleet()
          self.ship.center_ship()
          # 暂停。
❹         sleep(0.5)</code>
</pre>
<p>新方法<code>_ship_hit()</code>
 在飞船被外星人撞到时做出响应。在这个方法中，将余下的飞船数减1（见❶），再清空编组<code>aliens</code>
 和<code>bullets</code>
 （见❷）。</p>
<p>接下来，创建一群新的外星人，并将飞船居中（见❸）。（稍后将在<code>Ship</code>
 类中添加方法<code>center_ship()</code>
 。）最后，在更新所有元素后（但在将修改显示到屏幕前）暂停，让玩家知道飞船被撞到了（见❹）。这里的函数调用<code>sleep()</code>
 让游戏暂停半秒钟，让玩家能够看到外星人撞到了飞船。函数<code>sleep()</code>
 执行完毕后，将接着执行方法<code>_update_screen()</code>
 ，将新的外星人群绘制到屏幕上。</p>
<p>在<code>_update_aliens()</code>
 中，当有外星人撞到飞船时，不调用函数<code>print()</code>
 ，而调用<code>_ship_hit()</code>
 ：</p>
<p>
<em>alien_invasion.py</em>
</p>
<pre class="代码无行号"><code>    def _update_aliens(self):
        --snip--
        if pygame.sprite.spritecollideany(self.ship, self.aliens):
            self._ship_hit()</code>
</pre>
<p>下面是新方法<code>center_ship()</code>
 ，请将其添加到ship.py的末尾：</p>
<p>
<em>ship.py</em>
</p>
<pre class="代码无行号"><code>    def center_ship(self):
        """让飞船在屏幕底端居中。"""
        self.rect.midbottom = self.screen_rect.midbottom
        self.x = float(self.rect.x)</code>
</pre>
<p>这里像<code>__init__()</code>
 中那样让飞船在屏幕底端居中。让飞船在屏幕底端居中后，重置用于跟踪飞船确切位置的属性<code>self.x</code>
 。</p>
<blockquote>
<p>
<strong>注意</strong>
 　我们根本没有创建多艘飞船。在整个游戏运行期间，只创建了一个飞船实例，并在该飞船被撞到时将其居中。统计信息<code>ships_left</code>
 指出玩家是否用完了所有的飞船。</p>
</blockquote>
<p>请运行这个游戏，射杀几个外星人，并让一个外星人撞到飞船。游戏暂停片刻后，将出现一群新的外星人，而飞船将在屏幕底端居中。</p>
<h3 id="nav_point_318">13.6.3　有外星人到达屏幕底端</h3>
<p>如果有外星人到达屏幕底端，我们将像有外星人撞到飞船那样做出响应。为检测这种情况，在alien_invasion.py中添加一个新方法：</p>
<p>
<em>alien_invasion.py</em>
</p>
<pre class="代码无行号"><code>      def _check_aliens_bottom(self):
          """检查是否有外星人到达了屏幕底端。"""
          screen_rect = self.screen.get_rect()
          for alien in self.aliens.sprites():
❶             if alien.rect.bottom &gt;= screen_rect.bottom:
                  # 像飞船被撞到一样处理。
                  self._ship_hit()
                  break</code>
</pre>
<p>方法<code>_check_aliens_bottom()</code>
 检查是否有外星人到达了屏幕底端。到达屏幕底端后，外星人的属性<code>rect.bottom</code>
 大于或等于屏幕的属性<code>rect.bottom</code>
 （见❶）。如果有外星人到达屏幕底端，就调用<code>_ship_hit()</code>
 。只要检测到一个外星人到达屏幕底端，就无须检查其他外星人了，因此在调用<code>_ship_hit()</code>
 后退出循环。</p>
<p>我们在<code>_update_aliens()</code>
 中调用<code>_check_aliens_bottom()</code>
 ：</p>
<p>
<em>alien_invasion.py</em>
</p>
<pre class="代码无行号"><code>    def _update_aliens(self):
        --snip--
        # 检查是否有外星人撞到飞船。
        if pygame.sprite.spritecollideany(self.ship, self.aliens):
            self._ship_hit()
        # 检查是否有外星人到达了屏幕底端。
        self._check_aliens_bottom()</code>
</pre>
<p>在更新所有外星人的位置并检测是否有外星人和飞船发生碰撞后调用<code>_check_aliens_bottom()</code>
 。现在，每当有外星人撞到飞船或抵达屏幕底端时，都将出现一群新的外星人。</p>
<h3 id="nav_point_319">13.6.4　游戏结束</h3>
<p>现在这个游戏看起来更完整了，但它永远都不会结束，只是<code>ships_left</code>
 不断变成越来越小的负数。下面在<code>GameStats</code>
 中添加一个作为标志的属性<code>game_active</code>
 ，以便在玩家的飞船用完后结束游戏。首先，在<code>GameStats</code>
 类的方法<code>__init__()</code>
 末尾设置这个标志：</p>
<p>
<em>game_stats.py</em>
</p>
<pre class="代码无行号"><code>    def __init__(self, ai_game):
        --snip--
        # 游戏刚启动时处于活动状态。
        self.game_active = True</code>
</pre>
<p>接下来在<code>_ship_hit()</code>
 中添加代码，在玩家的飞船用完后将<code>game_active</code>
 设置为<code>False</code>
 ：</p>
<p>
<em>alien_invasion.py</em>
</p>
<pre class="代码无行号"><code>    def _ship_hit(self):
        """响应飞船被外星人撞到。"""
        if self.stats.ships_left &gt; 0:
            # 将ships_left减1。
            self.stats.ships_left -= 1
          --snip--
            # 暂停。
            sleep(0.5)
        else:
            self.stats.game_active = False</code>
</pre>
<p>
<code>_ship_hit()</code>
 的大部分代码没有变。我们将原来的代码都移到了一个<code>if</code>
 语句块中，它检查玩家是否至少还有一艘飞船。如果是，就创建一群新的外星人，暂停片刻，再接着往下执行。如果玩家没有了飞船，就将<code>game_active</code>
 设置为<code>False</code>
 。</p>
<h2 id="nav_point_320">13.7　确定应运行游戏的哪些部分</h2>
<p>我们需要确定游戏的哪些部分在任何情况下都应运行，哪些部分仅在游戏处于活动状态时才运行：</p>
<p>
<em>alien_invasion.py</em>
</p>
<pre class="代码无行号"><code>    def run_game(self):
        """开始游戏主循环。"""
        while True:
            self._check_events()
            if self.stats.game_active:
                self.ship.update()
                self._update_bullets()
                self._update_aliens()
            self._update_screen()</code>
</pre>
<p>在主循环中，在任何情况下都需要调用<code>_check_events()</code>
 ，即便游戏处于非活动状态。例如，我们需要知道玩家是否按了<code>Q</code>
 键以退出游戏，或者是否单击了关闭窗口的按钮。我们还需要不断更新屏幕，以便在等待玩家是否选择开始新游戏时修改屏幕。其他的函数仅在游戏处于活动状态时才需要调用，因为游戏处于非活动状态时，不用更新游戏元素的位置。</p>
<p>现在运行这个游戏，它将在飞船用完后停止不动。</p>
<blockquote>
<p>
<strong>动手试一试</strong>
</p>
<p>
<strong>练习13-6：游戏结束</strong>
 　在为完成练习13-5而编写的游戏中，记录飞船被撞到了多少次以及有多少外星人被射杀。确定合适的游戏结束条件，并在满足该条件后结束游戏。</p>
</blockquote>
<h2 id="nav_point_321">13.8　小结</h2>
<p>在本章中，你学习了：如何在游戏中添加大量相同的元素，如创建一群外星人；如何使用嵌套循环来创建元素网格，还通过调用每个元素的方法<code>update()</code>
 移动了大量元素；如何控制对象在屏幕上移动的方向，以及如何响应事件，如有外星人到达屏幕边缘；如何检测和响应子弹和外星人碰撞以及外星人和飞船碰撞；如何在游戏中跟踪统计信息，以及如何使用标志<code>game_active</code>
 来判断游戏是否结束。</p>
<p>在与这个项目相关的最后一章中，我们将添加一个Play按钮，让玩家能够开始游戏，以及在游戏结束后重玩。每当玩家消灭一群外星人后，我们都将加快游戏的节奏，并添加一个记分系统，得到一个极具可玩性的游戏！</p>
<br style="page-break-after:always"/>
<div></div>
]]></description>
    <pubDate>Tue, 21 Jul 2026 01:17:00 +0000</pubDate>
    <guid isPermaLink="false">13</guid>
</item>
<item>
    <title>Basic Python - 012 - 第二部分 项目.epub</title>
    <description><![CDATA[<p style='color: #888;'>⏱ 预计阅读时间：146 分钟 (29071 字)</p><hr><h1 id="nav_point_257">第二部分　项目</h1>
<p>祝贺你！你现在已经对Python有足够的认识，可以开始开发有意思的交互式项目了。通过动手开发项目，你能够学到新技能，并更深入理解第一部分介绍的概念。</p>
<p>第二部分包含三个不同类型的项目，你可以选择完成其中的任意或全部项目，完成的顺序无关紧要。下面简要描述每个项目，帮助你决定先去完成哪个。</p>
<p>
<strong>外星人入侵：使用Python开发游戏</strong>
</p>
<p>在项目“外星人入侵”（第12章～第14章）中，你将使用Pygame包来开发一款2D游戏。它在玩家每消灭一群向下移动的外星人后，将玩家提高一个等级。等级越高，游戏的节奏越快，难度越大。完成这个项目后，你将获得自己动手使用Pygame开发2D游戏所需的技能。</p>
<p>
<strong>数据可视化</strong>
</p>
<p>“数据可视化”项目始于第15章，你将在这一章学习如何使用Matplotlib和Plotly来生成数据，以及根据这些数据创建实用而漂亮的图表。第16章介绍如何从网上获取数据，并将其提供给可视化包以创建天气图和世界地震活动散点图。最后，第17章介绍如何编写自动下载数据并对其进行可视化的程序。学习可视化让你能够探索数据挖掘领域，这是当前在全球都非常热门的技能。</p>
<p>
<strong>Web应用程序</strong>
</p>
<p>在“Web应用程序”项目（第18章～第20章）中，你将使用Django包来创建一个简单的Web应用程序，让用户能够记录任意数量的学习主题。用户将通过指定用户名和密码来创建账户，输入主题，并编写条目来记录学习的内容。你还将学习如何部署应用程序，让任何人都能够访问它。</p>
<p>完成这个项目后，你将能够自己动手创建简单的Web应用程序，并能够深入学习其他有关如何使用Django开发应用程序的资料。</p>
<br style="page-break-after:always"/>
<div></div>
<h1 id="nav_point_258">项目1　外星人入侵</h1>
<br style="page-break-after:always"/>
<div></div>
<h1 id="nav_point_259">第 12 章　武装飞船</h1>
<blockquote>
<p><img alt="" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/12_Image00001.gif" style="width: 16%" width="16%"/>
 　我们来开发一个名为《外星人入侵》的游戏吧！为此将使用Pygame，这是一组功能强大而有趣的模块，可用于管理图形、动画乃至声音，让你能够更轻松地开发复杂的游戏。通过使用Pygame来处理在屏幕上绘制图像等任务，可将重点放在程序的高级逻辑上。</p>
<p>在本章中，你将安装Pygame，再创建一艘能够根据用户输入左右移动和射击的飞船。在接下来的两章，你将创建一群作为射杀目标的外星人，并改进该游戏：限制可供玩家使用的飞船数，并且添加记分牌。</p>
<p>在开发这款游戏的过程中，你还将学习如何管理包含多个文件的项目。你将重构很多代码并管理文件的内容，以确保项目组织有序以及提高效率。</p>
<p>开发游戏是趣学语言的理想方式。看别人玩你编写的游戏能获得满足感，而编写简单的游戏有助于你明白专业级游戏是怎么编写出来的。在阅读本章的过程中，请动手输入并运行代码，以明白各个代码块对整个游戏所做的贡献，并且尝试不同的值和设置，以对如何改进游戏的交互性有更深入的认识。</p>
</blockquote>
<p>　</p>
<blockquote>
<p>
<strong>注意</strong>
 　游戏《外星人入侵》将包含很多不同的文件，因此请在系统中新建一个名为alien_invasion的文件夹，并将该项目的所有文件都存储到该文件夹中，这样相关的<code>import</code>
 语句才能正确工作。</p>
<p>另外，如果你熟悉版本控制，可能想将其用于这个项目；如果你没有使用过版本控制，请参阅附录D的概述。</p>
</blockquote>
<h2 id="nav_point_260">12.1　规划项目</h2>
<p>开发大型项目时，制定好规划后再动手编写代码很重要。规划可确保你不偏离轨道，从而提高项目成功的可能性。</p>
<p>下面来编写有关游戏《外星人入侵》的描述，其中虽然没有涵盖这款游戏的所有细节，但能让你清楚地知道该如何动手开发。</p>
<blockquote>
<p>在游戏《外星人入侵》中，玩家控制一艘最初出现在屏幕底部中央的飞船。玩家可以使用箭头键左右移动飞船，还可使用空格键射击。游戏开始时，一群外星人出现在天空中，并向屏幕下方移动。玩家的任务是射杀这些外星人。玩家将所有外星人都消灭干净后，将出现一群新的外星人，其移动速度更快。只要有外星人撞到玩家的飞船或到达屏幕底部，玩家就损失一艘飞船。玩家损失三艘飞船后，游戏结束。</p>
</blockquote>
<p>开发的第一个阶段将创建一艘飞船，它可左右移动，并且能在用户按空格键时开火。设置好这种行为后，就可以创建外星人并提高游戏的可玩性了。</p>
<h2 id="nav_point_261">12.2　安装Pygame</h2>
<p>开始编码前，先来安装Pygame。可使用<code>pip</code>
 模块来帮助下载并安装Python包。要安装Pygame，在终端提示符下执行如下命令：</p>
<pre class="代码无行号"><code>$ python -m pip install --user pygame</code>
</pre>
<p>这个命令让Python运行<code>pip</code>
 模块，将<code>pygame</code>
 包添加到当前用户的Python安装中。如果你运行程序或启动终端会话时使用的命令不是<code>python</code>
 ，而是<code>python3</code>
 ，请执行如下命令来安装Pygame：</p>
<pre class="代码无行号"><code>$ python3 -m pip install --user pygame</code>
</pre>
<blockquote>
<p>
<strong>注意</strong>
 　如果该命令在macOS系统中不管用，请尝试在不指定标志<code>--user</code>
 的情况下再次执行。</p>
</blockquote>
<h2 id="nav_point_262">12.3　开始游戏项目</h2>
<p>开始开发游戏《外星人入侵》吧。首先要创建一个空的Pygame窗口，供之后用来绘制游戏元素，如飞船和外星人。我们还将让这个游戏响应用户输入，设置背景色，以及加载飞船图像。</p>
<h3 id="nav_point_263">12.3.1　创建Pygame窗口及响应用户输入</h3>
<p>下面创建一个表示游戏的类，以创建空的Pygame窗口。为此，在文本编辑器中新建一个文件，将其保存为alien_invasion.py，再在其中输入如下代码：</p>
<p>
<em>alien_invasion.py</em>
</p>
<pre class="代码无行号"><code>  import sys
  import pygame
  class AlienInvasion:
      """管理游戏资源和行为的类"""
      def __init__(self):
          """初始化游戏并创建游戏资源。"""
❶         pygame.init()
❷         self.screen = pygame.display.set_mode((1200, 800))
          pygame.display.set_caption("Alien Invasion")
      def run_game(self):
          """开始游戏的主循环"""
❸         while True:
              # 监视键盘和鼠标事件。
❹             for event in pygame.event.get():
❺                 if event.type == pygame.QUIT:
                      sys.exit()
              # 让最近绘制的屏幕可见。
❻             pygame.display.flip()
  if __name__ == '__main__':
      # 创建游戏实例并运行游戏。
      ai = AlienInvasion()
      ai.run_game()</code>
</pre>
<p>首先，导入模块<code>sys</code>
 和<code>pygame</code>
 。模块<code>pygame</code>
 包含开发游戏所需的功能。玩家退出时，我们将使用模块<code>sys</code>
 中的工具来退出游戏。</p>
<p>为开发游戏《外星人入侵》，我们创建了一个表示它的类，名为<code>AlienInvasion</code>
 。在这个类的方法<code>__init__()</code>
 中，调用函数<code>pygame.init()</code>
 来初始化背景设置，让Pygame能够正确地工作（见❶）。在❷处，调用<code>pygame.display.set_mode()</code>
 来创建一个显示窗口，游戏的所有图形元素都将在其中绘制。实参<code>(1200, 800)</code>
 是一个元组，指定了游戏窗口的尺寸——宽1200像素、高800像素（你可以根据自己的显示器尺寸调整这些值）。将这个显示窗口赋给属性<code>self.screen</code>
 ，让这个类中的所有方法都能够使用它。</p>
<p>赋给属性<code>self.screen</code>
 的对象是一个<strong>surface</strong>
 。在Pygame中，surface是屏幕的一部分，用于显示游戏元素。在这个游戏中，每个元素（如外星人或飞船）都是一个surface。<code>display.set_mode()</code>
 返回的surface表示整个游戏窗口。激活游戏的动画循环后，每经过一次循环都将自动重绘这个surface，将用户输入触发的所有变化都反映出来。</p>
<p>这个游戏由方法<code>run_game()</code>
 控制。该方法包含一个不断运行的<code>while</code>
 循环（见❸），而这个循环包含一个事件循环以及管理屏幕更新的代码。<strong>事件</strong>
 是用户玩游戏时执行的操作，如按键或移动鼠标。为程序响应事件，可编写一个<strong>事件循环</strong>
 ，以<strong>侦听</strong>
 事件并根据发生的事件类型执行合适的任务。❹处的<code>for</code>
 循环就是一个事件循环。</p>
<p>为访问Pygame检测到的事件，我们使用了函数<code>pygame.event.get()</code>
 。这个函数返回一个列表，其中包含它在上一次被调用后发生的所有事件。所有键盘和鼠标事件都将导致这个<code>for</code>
 循环运行。在这个循环中，我们将编写一系列<code>if</code>
 语句来检测并响应特定的事件。例如，当玩家单击游戏窗口的关闭按钮时，将检测到<code>pygame.QUIT</code>
 事件，进而调用<code>sys.exit()</code>
 来退出游戏（见❺）。</p>
<p>❻处调用了<code>pygame.display.flip()</code>
 ，命令Pygame让最近绘制的屏幕可见。在这里，它在每次执行<code>while</code>
 循环时都绘制一个空屏幕，并擦去旧屏幕，使得只有新屏幕可见。我们移动游戏元素时，<code>pygame.display.flip()</code>
 将不断更新屏幕，以显示元素的新位置，并且在原来的位置隐藏元素，从而营造平滑移动的效果。</p>
<p>在这个文件末尾，创建一个游戏实例并调用<code>run_game()</code>
 。这些代码放在一个<code>if</code>
 代码块中，仅当直接运行该文件时，它们才会执行。如果此时运行alien_invasion.py，将看到一个空的Pygame窗口。</p>
<h3 id="nav_point_264">12.3.2　设置背景色</h3>
<p>Pygame默认创建一个黑色屏幕，这太乏味了。下面来将背景设置为另一种颜色，这是在方法<code>__init__()</code>
 末尾进行的：</p>
<p>
<em>alien_invasion.py</em>
</p>
<pre class="代码无行号"><code>      def __init__(self):
          --snip--
          pygame.display.set_caption("Alien Invasion")
          # 设置背景色。
❶         self.bg_color = (230, 230, 230)
      def run_game(self):
          --snip--
              for event in pygame.event.get():
                  if event.type == pygame.QUIT:
                      sys.exit()
              # 每次循环时都重绘屏幕。
❷             self.screen.fill(self.bg_color)
              # 让最近绘制的屏幕可见。
              pygame.display.flip()</code>
</pre>
<p>在Pygame中，颜色是以RGB值指定的。这种颜色由红色、绿色和蓝色值组成，其中每个值的可能取值范围都是0～255。颜色值(255, 0, 0)表示红色，(0, 255, 0)表示绿色，而(0, 0, 255)表示蓝色。通过组合不同的RGB值，可创建1600万种颜色。在颜色值(230, 230, 230)中，红色、绿色和蓝色的量相同，它生成一种浅灰色。我们将这种颜色赋给了<code>self.bg_color</code>
 （见❶）。</p>
<p>在❷处，调用方法<code>fill()</code>
 用这种背景色填充屏幕。方法<code>fill()</code>
 用于处理surface，只接受一个实参：一种颜色。</p>
<h3 id="nav_point_265">12.3.3　创建设置类</h3>
<p>每次给游戏添加新功能时，通常也将引入一些新设置。下面来编写一个名为<code>settings</code>
 的模块，在其中包含一个名为<code>Settings</code>
 的类，用于将所有设置都存储在一个地方，以免在代码中到处添加设置。这样，每当需要访问设置时，只需使用一个设置对象。另外，在项目增大时，这使得修改游戏的外观和行为更容易：要修改游戏，只需修改（接下来将创建的）settings.py中的一些值，而无须查找散布在项目中的各种设置。</p>
<p>在文件夹alien_invasion中，新建一个名为settings.py的文件，并在其中添加如下<code>Settings</code>
 类：</p>
<p>
<em>settings.py</em>
</p>
<pre class="代码无行号"><code>class Settings:
    """存储游戏《外星人入侵》中所有设置的类"""
    def __init__(self):
        """初始化游戏的设置。"""
        # 屏幕设置
        self.screen_width = 1200
        self.screen_height = 800
        self.bg_color = (230, 230, 230)</code>
</pre>
<p>为在项目中创建<code>Settings</code>
 实例并用它来访问设置，需要将alien_invasion.py修改成下面这样：</p>
<p>
<em>alien_invasion.py</em>
</p>
<pre class="代码无行号"><code>  --snip--
  import pygame
  from settings import Settings
  class AlienInvasion:
      """管理游戏资源和行为的类"""
      def __init__(self):
          """初始化游戏并创建游戏资源。"""
          pygame.init()
❶         self.settings = Settings()
❷         self.screen = pygame.display.set_mode(
              (self.settings.screen_width, self.settings.screen_height))
          pygame.display.set_caption("Alien Invasion")
      def run_game(self):
              --snip--
              # 每次循环时都重绘屏幕。
❸             self.screen.fill(self.settings.bg_color)
              # 让最近绘制的屏幕可见。
              pygame.display.flip()
  --snip--</code>
</pre>
<p>在主程序文件中，导入<code>Settings</code>
 类，调用<code>pygame.init()</code>
 ，再创建一个<code>Settings</code>
 实例并将其赋给<code>self.settings</code>
 （见❶）。创建屏幕时（见❷），使用了<code>self.settings</code>
 的属性<code>screen_width</code>
 和<code>screen_height</code>
 。接下来填充屏幕时，也使用了<code>self.settings</code>
 来访问背景色（见❸）。</p>
<p>如果此时运行alien_invasion.py，结果不会有任何不同，因为我们只是将设置移到了不同的地方。现在可以在屏幕上添加新元素了。</p>
<h2 id="nav_point_266">12.4　添加飞船图像</h2>
<p>下面将飞船加入游戏中。为了在屏幕上绘制玩家的飞船，我们将加载一幅图像，再使用Pygame方法<code>blit()</code>
 绘制它。</p>
<p>为游戏选择素材时，务必要注意许可。最安全、最不费钱的方式是使用Pixabay等网站提供的免费图形，无须授权许可即可使用并修改。</p>
<p>在游戏中几乎可以使用任何类型的图像文件，但使用位图（.bmp）文件最为简单，因为Pygame默认加载位图。虽然可配置Pygame以使用其他文件类型，但有些文件类型要求你在计算机上安装相应的图像库。大多数图像为.jpg、.png或.gif格式，但可使用Photoshop、GIMP和Paint等工具将其转换为位图。</p>
<p>选择图像时，要特别注意背景色。请尽可能选择背景为透明或纯色的图像，便于使用图像编辑器将其背景替换为任意颜色。图像的背景色与游戏的背景色匹配时，游戏看起来最漂亮。你也可以将游戏的背景色设置成图像的背景色。</p>
<p>就游戏《外星人入侵》而言，可使用文件ship.bmp（如图12-1所示），该文件可在本书主页（ituring.cn/book/2784）的“随书下载”中找到。这个文件的背景色与项目使用的设置相同。请在项目文件夹（alien_invasion）中新建一个名为images的文件夹，并将文件ship.bmp保存在其中。</p>
<p class="图"><img alt="" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/12_Image00005.gif" style="width: 50%" width="50%"/>
</p>
<p>
<strong>图12-1　游戏《外星人入侵》中的飞船</strong>
</p>
<h3 id="nav_point_267">12.4.1　创建<code>Ship</code>
 类</h3>
<p>选择用于表示飞船的图像后，需要将其显示到屏幕上。我们创建一个名为<code>ship</code>
 的模块，其中包含<code>Ship</code>
 类，负责管理飞船的大部分行为。</p>
<p>
<em>ship.py</em>
</p>
<pre class="代码无行号"><code>  import pygame
  class Ship:
      """管理飞船的类"""
      def __init__(self, ai_game):
          """初始化飞船并设置其初始位置。"""
❶         self.screen = ai_game.screen
❷         self.screen_rect = ai_game.screen.get_rect()
          # 加载飞船图像并获取其外接矩形。
❸         self.image = pygame.image.load('images/ship.bmp')
          self.rect = self.image.get_rect()
          # 对于每艘新飞船，都将其放在屏幕底部的中央。
❹         self.rect.midbottom = self.screen_rect.midbottom
❺     def blitme(self):
          """在指定位置绘制飞船。"""
          self.screen.blit(self.image, self.rect)</code>
</pre>
<p>Pygame之所以高效，是因为它让你能够像处理矩形（<code>rect</code>
 对象）一样处理所有的游戏元素，即便其形状并非矩形。像处理矩形一样处理游戏元素之所以高效，是因为矩形是简单的几何形状。例如，通过将游戏元素视为矩形，Pygame能够更快地判断出它们是否发生了碰撞。这种做法的效果通常很好，游戏玩家几乎注意不到我们处理的并不是游戏元素的实际形状。在这个类中，我们将把飞船和屏幕作为矩形进行处理。</p>
<p>定义这个类之前，导入了模块<code>pygame</code>
 。<code>Ship</code>
 的方法<code>__init__()</code>
 接受两个参数：引用<code>self</code>
 和指向当前<code>AlienInvasion</code>
 实例的引用。这让<code>Ship</code>
 能够访问<code>AlienInvasion</code>
 中定义的所有游戏资源。在❶处，将屏幕赋给了<code>Ship</code>
 的一个属性，以便在这个类的所有方法中轻松访问。在❷处，使用方法<code>get_rect()</code>
 访问屏幕的属性<code>rect</code>
 ，并将其赋给了<code>self.screen_rect</code>
 ，这让我们能够将飞船放到屏幕的正确位置。</p>
<p>调用<code>pygame.image.load()</code>
 加载图像，并将飞船图像的位置传递给它（见❸）。该函数返回一个表示飞船的surface，而我们将这个surface赋给了<code>self.image</code>
 。加载图像后，使用<code>get_rect()</code>
 获取相应surface的属性<code>rect</code>
 ，以便后面能够使用它来指定飞船的位置。</p>
<p>处理<code>rect</code>
 对象时，可使用矩形四角和中心的<img alt="x" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/12_Image00003.gif"/>
 坐标和<img alt="y" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/12_Image00004.gif"/>
 坐标。可通过设置这些值来指定矩形的位置。要让游戏元素居中，可设置相应<code>rect</code>
 对象的属性<code>center</code>
 、<code>centerx</code>
 或<code>centery</code>
 ；要让游戏元素与屏幕边缘对齐，可使用属性<code>top</code>
 、<code>bottom</code>
 、<code>left</code>
 或<code>right</code>
 。除此之外，还有一些组合属性，如<code>midbottom</code>
 、<code>midtop</code>
 、<code>midleft</code>
 和<code>midright</code>
 。要调整游戏元素的水平或垂直位置，可使用属性<code>x</code>
 和<code>y</code>
 ，分别是相应矩形左上角的<img alt="x" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/12_Image00003.gif"/>
 坐标和<img alt="y" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/12_Image00004.gif"/>
 坐标。这些属性让你无须做游戏开发人员原本需要手工完成的计算，因此会经常用到。</p>
<blockquote>
<p>
<strong>注意</strong>
 　在Pygame中，原点(0, 0)位于屏幕左上角，向右下方移动时，坐标值将增大。在1200 × 800的屏幕上，原点位于左上角，而右下角的坐标为(1200, 800)。这些坐标对应的是游戏窗口，而不是物理屏幕。</p>
</blockquote>
<p>我们要将飞船放在屏幕底部的中央。为此，将<code>self.rect.midbottom</code>
 设置为表示屏幕的矩形的属性<code>midbottom</code>
 （见❹）。Pygame使用这些<code>rect</code>
 属性来放置飞船图像，使其与屏幕下边缘对齐并水平居中。</p>
<p>在❺处，定义了方法<code>blitme()</code>
 ，它将图像绘制到<code>self.rect</code>
 指定的位置。</p>
<h3 id="nav_point_268">12.4.2　在屏幕上绘制飞船</h3>
<p>下面更新alien_invasion.py，创建一艘飞船并调用其方法<code>blitme()</code>
 ：</p>
<p>
<em>alien_invasion.py</em>
</p>
<pre class="代码无行号"><code>  --snip--
  from settings import Settings
  from ship import Ship
  class AlienInvasion:
      """管理游戏资源和行为的类"""
      def __init__(self):
          --snip--
          pygame.display.set_caption("Alien Invasion")
❶         self.ship = Ship(self)
      def run_game(self):
              --snip--
              # 每次循环时都重绘屏幕。
              self.screen.fill(self.settings.bg_color)
❷             self.ship.blitme()
              # 让最近绘制的屏幕可见。
              pygame.display.flip()
  --snip--</code>
</pre>
<p>导入<code>Ship</code>
 类，并在创建屏幕后创建一个<code>Ship</code>
 实例（见❶）。调用<code>Ship()</code>
 时，必须提供一个参数：一个<code>AlienInvasion</code>
 实例。在这里，<code>self</code>
 指向的是当前<code>AlienInvasion</code>
 实例。这个参数让<code>Ship</code>
 能够访问游戏资源，如对象<code>screen</code>
 。我们将这个<code>Ship</code>
 实例赋给了<code>self.ship</code>
 。</p>
<p>填充背景后，调用<code>ship.blitme()</code>
 将飞船绘制到屏幕上，确保它出现在背景前面（见❷）。</p>
<p>现在如果运行alien_invasion.py，将看到飞船位于空游戏屏幕底部的中央，如图12-2所示。</p>
<p class="图"><img alt="{%}" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/12_Image00006.gif"/>
</p>
<p>
<strong>图12-2　游戏《外星人入侵》屏幕底部的中央有一艘飞船</strong>
</p>
<h2 id="nav_point_269">12.5　重构：方法<code>_check_events()</code>
 和<code>__update_screen()</code>
</h2>
<p>在大型项目中，经常需要在添加新代码前重构既有代码。重构旨在简化既有代码的结构，使其更容易扩展。本节将把越来越长的方法<code>run_game()</code>
 拆分成两个辅助方法（helper method）。<strong>辅助方法</strong>
 在类中执行任务，但并非是通过实例调用的。在Python中，辅助方法的名称以单个下划线打头。</p>
<h3 id="nav_point_270">12.5.1　方法<code>_check_events()</code>
</h3>
<p>我们将把管理事件的代码移到一个名为<code>_check_events()</code>
 的方法中，以简化<code>run_game()</code>
 并隔离事件管理循环。通过隔离事件循环，可将事件管理与游戏的其他方面（如更新屏幕）分离。</p>
<p>下面是新增方法<code>_check_events()</code>
 后的<code>AlienInvasion</code>
 类，只有<code>run_game()</code>
 的代码受到影响：</p>
<p>
<em>alien_invasion.py</em>
</p>
<pre class="代码无行号"><code>      def run_game(self):
          """开始游戏主循环。"""
          while True:
❶             self._check_events()
              # 每次循环时都重绘屏幕。
              --snip--
❷     def _check_events(self):
          """响应按键和鼠标事件。"""
          for event in pygame.event.get():
              if event.type == pygame.QUIT:
                  sys.exit()</code>
</pre>
<p>新增方法<code>_check_events()</code>
 （见❷），并将检查玩家是否单击了关闭窗口按钮的代码移到该方法中。</p>
<p>要调用当前类的方法，可使用句点表示法，并指定变量名<code>self</code>
 和要调用的方法的名称（见❶）。我们在<code>run_game()</code>
 的<code>while</code>
 循环中调用这个新增的方法。</p>
<h3 id="nav_point_271">12.5.2　方法<code>_update_screen()</code>
</h3>
<p>为进一步简化<code>run_game()</code>
 ，将更新屏幕的代码移到一个名为<code>_update_screen()</code>
 的方法中：</p>
<p>
<em>alien_invasion.py</em>
</p>
<pre class="代码无行号"><code>      def run_game(self):
          """开始游戏主循环。"""
          while True:
              self._check_events()
              self._update_screen()
      def _check_events(self):
          --snip--
      def _update_screen(self):
          """更新屏幕上的图像，并切换到新屏幕。"""
          self.screen.fill(self.settings.bg_color)
          self.ship.blitme()
          pygame.display.flip()</code>
</pre>
<p>我们将绘制背景和飞船以及切换屏幕的代码移到了方法<code>_update_screen()</code>
 中。现在，<code>run_game()</code>
 中的主循环简单多了，很容易看出在每次循环中都检测了新发生的事件并更新了屏幕。</p>
<p>如果你开发过大量的游戏，可能早就开始像这样将代码放到不同的方法中了。不过如果你从未开发过这样的项目，可能不知道如何组织代码。这里采用的做法是，先编写可行的代码，等代码越来越复杂时再进行重构，以向你展示真正的开发过程：先编写尽可能简单的代码，等项目越来越复杂后对其进行重构。</p>
<p>对代码进行重构使其更容易扩展后，可以开始处理游戏的动态方面了！</p>
<blockquote>
<p>
<strong>动手试一试</strong>
</p>
<p>
<strong>练习12-1：蓝色天空</strong>
 　创建一个背景为蓝色的Pygame窗口。</p>
<p>
<strong>练习12-2：游戏角色</strong>
 　找一幅你喜欢的游戏角色位图图像或将一幅图像转换为位图。创建一个类，将该角色绘制到屏幕中央，并将该图像的背景色设置为屏幕背景色，或者将屏幕背景色设置为该图像的背景色。</p>
</blockquote>
<h2 id="nav_point_272">12.6　驾驶飞船</h2>
<p>下面来让玩家能够左右移动飞船。我们将编写代码，在用户按左或右箭头键时做出响应。我们将首先专注于向右移动，再使用同样的原理来控制向左移动。通过这样做，你将学会如何控制屏幕图像的移动。</p>
<h3 id="nav_point_273">12.6.1　响应按键</h3>
<p>每当用户按键时，都将在Pygame中注册一个事件。事件都是通过方法<code>pygame.event.get()</code>
 获取的，因此需要在方法<code>_check_events()</code>
 中指定要检查哪些类型的事件。每次按键都被注册为一个<code>KEYDOWN</code>
 事件。</p>
<p>Pygame检测到<code>KEYDOWN</code>
 事件时，需要检查按下的是否是触发行动的键。例如，如果玩家按下的是右箭头键，就增大飞船的<code>rect.centerx</code>
 值，将飞船向右移动：</p>
<p>
<em>alien_invasion.py</em>
</p>
<pre class="代码无行号"><code>      def _check_events(self):
          """响应按键和鼠标事件。"""
          for event in pygame.event.get():
              if event.type == pygame.QUIT:
                  sys.exit()
❶             elif event.type == pygame.KEYDOWN:
❷                 if event.key == pygame.K_RIGHT:
                      # 向右移动飞船。
❸                     self.ship.rect.x += 1</code>
</pre>
<p>在方法<code>_check_events()</code>
 中，为事件循环添加一个<code>elif</code>
 代码块，以便在Pygame检测到<code>KEYDOWN</code>
 事件时做出响应（见❶）。我们检查按下键（<code>event.key</code>
 ）是否是右箭头键（<code>pygame.K_RIGHT</code>
 ）（见❷）。如果是，就将<code>self.ship.rect.centerx</code>
 的值加1，从而将飞船向右移动（见❸）。</p>
<p>如果现在运行alien_invasion.py，则每按右箭头键一次，飞船都将向右移动1像素。这是一个开端，但并非控制飞船的高效方式。下面来改进控制方式，允许持续移动。</p>
<h3 id="nav_point_274">12.6.2　允许持续移动</h3>
<p>玩家按住右箭头键不放时，我们希望飞船不断向右移动，直到玩家松开为止。我们将让游戏检测<code>pygame.KEYUP</code>
 事件，以便知道玩家何时松开右箭头键。然后，结合使用<code>KEYDOWN</code>
 和<code>KEYUP</code>
 事件以及一个名为<code>moving_right</code>
 的标志来实现持续移动。</p>
<p>当标志<code>moving_right</code>
 为<code>False</code>
 时，飞船不会移动。玩家按下右箭头键时，我们将该标志设置为<code>True</code>
 ，在玩家松开时将该标志重新设置为<code>False</code>
 。</p>
<p>飞船的属性都由<code>Ship</code>
 类控制，因此要给这个类添加一个名为<code>moving_right</code>
 的属性和一个名为<code>update()</code>
 的方法。方法<code>update()</code>
 检查标志<code>moving_right</code>
 的状态。如果该标志为<code>True</code>
 ，就调整飞船的位置。我们将在<code>while</code>
 循环中调用这个方法，以调整飞船的位置。</p>
<p>下面是对<code>Ship</code>
 类所做的修改：</p>
<p>
<em>ship.py</em>
</p>
<pre class="代码无行号"><code>  class Ship:
      """管理飞船的类"""
      def __init__(self, ai_game):
          --snip--
          # 对于每艘新飞船，都将其放在屏幕底部的中央。
          self.rect.midbottom = self.screen_rect.midbottom
          # 移动标志。
❶         self.moving_right = False
❷     def update(self):
          """根据移动标志调整飞船的位置。"""
          if self.moving_right:
              self.rect.x += 1
      def blitme(self):
          --snip--</code>
</pre>
<p>在方法<code>__init__()</code>
 中，添加属性<code>self.moving_right</code>
 ，并将其初始值设置为<code>False</code>
 （见❶）。接下来，添加方法<code>update()</code>
 ，在前述标志为<code>True</code>
 时向右移动飞船（见❷）。方法<code>update()</code>
 将通过<code>Ship</code>
 实例来调用，因此不是辅助方法。</p>
<p>接下来，需要修改<code>_check_events()</code>
 ，使其在玩家按下右箭头键时将<code>moving_right</code>
 设置为<code>True</code>
 ，并在玩家松开时将<code>moving_right</code>
 设置为<code>False</code>
 ：</p>
<p>
<em>alien_invasion.py</em>
</p>
<pre class="代码无行号"><code>      def _check_events(self):
          """响应按键和鼠标事件。"""
          for event in pygame.event.get():
              --snip--
              elif event.type == pygame.KEYDOWN:
                  if event.key == pygame.K_RIGHT:
❶                     self.ship.moving_right = True
❷             elif event.type == pygame.KEYUP:
                  if event.key == pygame.K_RIGHT:
                      self.ship.moving_right = False</code>
</pre>
<p>在❶处，修改游戏在玩家按下右箭头键时响应的方式：不直接调整飞船的位置，而只是将<code>moving_right</code>
 设置为<code>True</code>
 。在❷处，添加一个新的<code>elif</code>
 代码块，用于响应<code>KEYUP</code>
 事件：玩家松开右箭头键（<code>K_RIGHT</code>
 ）时，将<code>moving_right</code>
 设置为<code>False</code>
 。</p>
<p>最后，需要修改<code>run_game()</code>
 中的<code>while</code>
 循环，以便每次执行循环时都调用飞船的方法<code>update()</code>
 ：</p>
<p>
<em>alien_invasion.py</em>
</p>
<pre class="代码无行号"><code>    def run_game(self):
        """开始游戏主循环。"""
        while True:
            self._check_events()
            self.ship.update()
            self._update_screen()</code>
</pre>
<p>飞船的位置将在检测到键盘事件后（但在更新屏幕前）更新。这样，玩家输入时，飞船的位置将更新，从而确保使用更新后的位置将飞船绘制到屏幕上。</p>
<p>如果现在运行alien_invasion.py并按住右箭头键，飞船将持续向右移动，直到松开为止。</p>
<h3 id="nav_point_275">12.6.3　左右移动</h3>
<p>现在飞船能够持续向右移动了，添加向左移动的逻辑也很容易。我们将再次修改<code>Ship</code>
 类和方法<code>_check_events()</code>
 。下面显示了对<code>Ship</code>
 类的方法<code>__init__()</code>
 和<code>update()</code>
 所做的相关修改：</p>
<p>
<em>ship.py</em>
</p>
<pre class="代码无行号"><code>    def __init__(self, ai_game):
        --snip--
        # 移动标志
        self.moving_right = False
        self.moving_left = False
    def update(self):
        """根据移动标志调整飞船的位置。"""
        if self.moving_right:
            self.rect.x += 1
        if self.moving_left:
            self.rect.x -= 1</code>
</pre>
<p>在方法<code>__init__()</code>
 中，添加标志<code>self.moving_left</code>
 。在方法<code>update()</code>
 中，添加一个<code>if</code>
 代码块而不是<code>elif</code>
 代码块，这样如果玩家同时按下了左右箭头键，将先增加再减少飞船的<code>rect.x</code>
 值，即飞船的位置保持不变。如果使用一个<code>elif</code>
 代码块来处理向左移动的情况，右箭头键将始终处于优先地位。从向左移动切换到向右移动时，玩家可能同时按住左右箭头键，此时前面的做法让移动更准确。</p>
<p>还需对<code>_check_events()</code>
 做两方面的调整：</p>
<p>
<em>alien_invasion.py</em>
</p>
<pre class="代码无行号"><code>    def _check_events(self):
        """响应按键和鼠标事件。"""
        for event in pygame.event.get():
            --snip--
            elif event.type == pygame.KEYDOWN:
                if event.key == pygame.K_RIGHT:
                    self.ship.moving_right = True
                elif event.key == pygame.K_LEFT:
                    self.ship.moving_left = True
            elif event.type == pygame.KEYUP:
                if event.key == pygame.K_RIGHT:
                    self.ship.moving_right = False
                elif event.key == pygame.K_LEFT:
                    self.ship.moving_left = False</code>
</pre>
<p>如果因玩家按下<code>K_LEFT</code>
 键而触发了<code>KEYDOWN</code>
 事件，就将<code>moving_left</code>
 设置为<code>True</code>
 。如果因玩家松开<code>K_LEFT</code>
 而触发了<code>KEYUP</code>
 事件，就将<code>moving_left</code>
 设置为<code>False</code>
 。这里之所以可以使用<code>elif</code>
 代码块，是因为每个事件都只与一个键相关联。如果玩家同时按下左右箭头键，将检测到两个不同的事件。</p>
<p>如果此时运行alien_invasion.py，将能够持续左右移动飞船。如果同时按下左右箭头键，飞船将纹丝不动。</p>
<p>下面来进一步优化飞船的移动方式：调整飞船的速度，以及限制飞船的移动距离，以免其消失在屏幕之外。</p>
<h3 id="nav_point_276">12.6.4　调整飞船的速度</h3>
<p>当前，每次执行<code>while</code>
 循环时，飞船最多移动1像素，但可在<code>Settings</code>
 类中添加属性<code>ship_speed</code>
 ，用于控制飞船的速度。我们将根据这个属性决定飞船在每次循环时最多移动多远。下面演示了如何在settings.py中添加这个新属性：</p>
<p>
<em>settings.py</em>
</p>
<pre class="代码无行号"><code>class Settings:
    """存储游戏《外星人入侵》中所有设置的类。"""
    def __init__(self):
        --snip--
        # 飞船设置
        self.ship_speed = 1.5</code>
</pre>
<p>将<code>ship_speed</code>
 的初始值设置为<code>1.5</code>
 。现在需要移动飞船时，每次循环将移动1.5像素而不是1像素。</p>
<p>通过将速度设置指定为小数值，可在后面加快游戏节奏时更细致地控制飞船的速度。然而，<code>rect</code>
 的<code>x</code>
 等属性只能存储整数值，因此需要对<code>Ship</code>
 类做些修改：</p>
<p>
<em>ship.py</em>
</p>
<pre class="代码无行号"><code>  class Ship:
      """管理飞船的类"""
❶     def __init__(self, ai_game):
          """初始化飞船并设置其初始位置。"""
          self.screen = ai_game.screen
          self.settings = ai_game.settings
          --snip--
          # 对于每艘新飞船，都将其放在屏幕底部的中央。
          --snip--
          # 在飞船的属性x中存储小数值。
❷         self.x = float(self.rect.x)
          # 移动标志
          self.moving_right = False
          self.moving_left = False
      def update(self):
          """根据移动标志调整飞船的位置。"""
          # 更新飞船而不是rect对象的x值。
          if self.moving_right:
❸             self.x += self.settings.ship_speed
          if self.moving_left:
              self.x -= self.settings.ship_speed
          # 根据self.x更新rect对象。
❹         self.rect.x = self.x
      def blitme(self):
          --snip--</code>
</pre>
<p>在❶处，给<code>Ship</code>
 类添加属性<code>settings</code>
 ，以便能够在<code>update()</code>
 中使用它。鉴于现在调整飞船的位置时，将增减一个单位为像素的小数值，因此需要将位置赋给一个能够存储小数值的变量。可使用小数来设置<code>rect</code>
 的属性，但<code>rect</code>
 将只存储这个值的整数部分。为准确存储飞船的位置，定义一个可存储小数值的新属性<code>self.x</code>
 （见❷）。使用函数<code>float()</code>
 将<code>self.rect.x</code>
 的值转换为小数，并将结果赋给<code>self.x</code>
 。</p>
<p>现在在<code>update()</code>
 中调整飞船的位置时，将<code>self.x</code>
 的值增减<code>settings.ship_speed</code>
 的值（见❸）。更新<code>self.x</code>
 后，再根据它来更新控制飞船位置的<code>self.rect.x</code>
 （见❹）。<code>self.rect.x</code>
 只存储<code>self.x</code>
 的整数部分，但对显示飞船而言，这问题不大。</p>
<p>现在可以修改<code>ship_speed</code>
 的值了。只要它的值大于1，飞船的移动速度就会比以前更快。这有助于让飞船的反应速度足够快，以便射杀外星人，还让我们能够随着游戏的进行加快游戏的节奏。</p>
<blockquote>
<p>
<strong>注意</strong>
 　如果你使用的是macOS，可能发现即便<code>ship_speed</code>
 的值很大，飞船的移动速度还是很慢。要修复这种问题，可在全屏模式下运行游戏，我们稍后就将实现这种功能。</p>
</blockquote>
<h3 id="nav_point_277">12.6.5　限制飞船的活动范围</h3>
<p>当前，如果玩家按住箭头键的时间足够长，飞船将飞到屏幕之外，消失得无影无踪。下面来修复这种问题，让飞船到达屏幕边缘后停止移动。为此，将修改<code>Ship</code>
 类的方法<code>update()</code>
 ：</p>
<p>
<em>ship.py</em>
</p>
<pre class="代码无行号"><code>      def update(self):
          """根据移动标志调整飞船的位置。"""
          # 更新飞船而不是rect对象的x值。
❶         if self.moving_right and self.rect.right &lt; self.screen_rect.right:
              self.x += self.settings.ship_speed
❷         if self.moving_left and self.rect.left &gt; 0:
              self.x -= self.settings.ship_speed
          # 根据self.x更新rect对象。
          self.rect.x = self.x</code>
</pre>
<p>上述代码在修改<code>self.x</code>
 的值之前检查飞船的位置。<code>self.rect.right</code>
 返回飞船外接矩形右边缘的<img alt="x" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/12_Image00003.gif"/>
 坐标。如果这个值小于<code>self.screen_rect.right</code>
 的值，就说明飞船未触及屏幕右边缘（见❶）。左边缘的情况与此类似：如果<code>rect</code>
 左边缘的<img alt="x" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/12_Image00003.gif"/>
 坐标大于零，就说明飞船未触及屏幕左边缘（见❷）。这确保仅当飞船在屏幕内时，才调整<code>self.x</code>
 的值。</p>
<p>如果此时运行alien_invasion.py，飞船将在触及屏幕左边缘或右边缘后停止移动。真是太神奇了！只在<code>if</code>
 语句中添加一个条件测试，就让飞船在到达屏幕左右边缘时像被墙挡住了一样。</p>
<h3 id="nav_point_278">12.6.6　重构<code>_check_events()</code>
</h3>
<p>随着游戏的开发，方法<code>_check_events()</code>
 将越来越长。因此将其部分代码放在两个方法中，其中一个处理<code>KEYDOWN</code>
 事件，另一个处理<code>KEYUP</code>
 事件：</p>
<p>
<em>alien_invasion.py</em>
</p>
<pre class="代码无行号"><code>    def _check_events(self):
        """响应鼠标和按键事件。"""
        for event in pygame.event.get():
            if event.type == pygame.QUIT:
                sys.exit()
            elif event.type == pygame.KEYDOWN:
                self._check_keydown_events(event)
            elif event.type == pygame.KEYUP:
                self._check_keyup_events(event)
    def _check_keydown_events(self, event):
        """响应按键。"""
        if event.key == pygame.K_RIGHT:
            self.ship.moving_right = True
        elif event.key == pygame.K_LEFT:
            self.ship.moving_left = True
    def _check_keyup_events(self, event):
        """响应松开。"""
        if event.key == pygame.K_RIGHT:
            self.ship.moving_right = False
        elif event.key == pygame.K_LEFT:
            self.ship.moving_left = False</code>
</pre>
<p>我们创建了两个新的辅助方法：<code>_check_keydown_events()</code>
 和<code>_check_keyup_events()</code>
 。它们都包含形参<code>self</code>
 和<code>event</code>
 。这两个方法的代码是从<code>_check_events()</code>
 中复制而来的，因此将方法<code>_check_events()</code>
 中相应的代码替换成了对这两个新方法的调用。现在，方法<code>_check_events()</code>
 更简单，代码结构也更清晰，在其中响应玩家输入时将更容易。</p>
<h3 id="nav_point_279">12.6.7　按Q键退出</h3>
<p>能够高效地响应按键后，我们来添加另一种退出游戏的方式。当前，每次测试新功能时，都需要单击游戏窗口顶部的X按钮来结束游戏，实在是太麻烦了。因此，我们来添加一个结束游戏的键盘快捷键—— Q键：</p>
<p>
<em>alien_invasion.py</em>
</p>
<pre class="代码无行号"><code>    def _check_keydown_events(self, event):
        --snip--
        elif event.key == pygame.K_LEFT:
            self.ship.moving_left = True
        elif event.key == pygame.K_q:
            sys.exit()</code>
</pre>
<p>在<code>_check_keydown_events()</code>
 中，添加一个代码块，用于在玩家按Q键时结束游戏。现在测试该游戏时，你可按Q键来结束游戏，而无须使用鼠标将窗口关闭。</p>
<h3 id="nav_point_280">12.6.8　在全屏模式下运行游戏</h3>
<p>Pygame支持全屏模式，你可能会更喜欢在这种模式下而非常规窗口中运行游戏。有些游戏在全屏模式下看起来更舒服，而在macOS系统中用全屏模式运行会提升性能。</p>
<p>要在全屏模式下运行该游戏，可在<code>__init__()</code>
 中做如下修改：</p>
<p>
<em>alien_invasion.py</em>
</p>
<pre class="代码无行号"><code>      def __init__(self):
          """初始化游戏并创建游戏资源。"""
          pygame.init()
          self.settings = Settings()
❶         self.screen = pygame.display.set_mode((0, 0), pygame.FULLSCREEN)
❷         self.settings.screen_width = self.screen.get_rect().width
          self.settings.screen_height = self.screen.get_rect().height
          pygame.display.set_caption("Alien Invasion")</code>
</pre>
<p>创建屏幕时，传入了尺寸<code>(0, 0)</code>
 以及参数<code>pygame.FULLSCREEN</code>
 （见❶）。这让Pygame生成一个覆盖整个显示器的屏幕。由于无法预先知道屏幕的宽度和高度，要在创建屏幕后更新这些设置（见❷）：使用屏幕的<code>rect</code>
 的属性<code>width</code>
 和<code>height</code>
 来更新对象<code>settings</code>
 。</p>
<p>如果你喜欢这款游戏在全屏模式下的外观和行为，请保留这些设置。如果你更喜欢这款游戏在独立的窗口中运行，可恢复到原来采用的方法——将屏幕尺寸设置为特定的值。</p>
<blockquote>
<p>
<strong>注意</strong>
 　在全屏模式下运行这款游戏之前，请确认能够按Q键退出，因为Pygame默认不提供在全屏模式下退出游戏的方式。</p>
</blockquote>
<h2 id="nav_point_281">12.7　简单回顾</h2>
<p>下一节将添加射击功能，为此需要新增一个名为bullet.py的文件，并修改一些既有文件。当前有三个文件，其中包含很多类和方法。添加其他功能之前，先来回顾一下这些文件，让你清楚这个项目的组织结构。</p>
<h3 id="nav_point_282">12.7.1　alien_invasion.py</h3>
<p>主文件alien_invasion.py包含<code>AlienInvasion</code>
 类。这个类创建一系列贯穿整个游戏都要用到的属性：赋给<code>self.settings</code>
 的设置，赋给<code>screen</code>
 中的主显示surface，以及一个飞船实例。这个模块还包含游戏的主循环，即一个调用<code>_check_events()</code>
 、<code>ship.update()</code>
 和<code>_update_screen()</code>
 的<code>while</code>
 循环。</p>
<p>方法<code>_check_events()</code>
 检测相关的事件（如按下和松开键盘），并通过调用方法<code>_check_keydown_events()</code>
 和<code>_check_keyup_events()</code>
 处理这些事件。当前，这些方法负责管理飞船的移动。<code>AlienInvasion</code>
 类还包含方法<code>_update_screen()</code>
 ，该方法在每次主循环中重绘屏幕。</p>
<p>要玩游戏《外星人入侵》，只需运行文件alien_invasion.py，其他文件（settings.py和ship.py）包含的代码会被导入这个文件中。</p>
<h3 id="nav_point_283">12.7.2　settings.py</h3>
<p>文件settings.py包含<code>Settings</code>
 类，这个类只包含方法<code>__init__()</code>
 ，用于初始化控制游戏外观和飞船速度的属性。</p>
<h3 id="nav_point_284">12.7.3　ship.py</h3>
<p>文件ship.py包含<code>Ship</code>
 类，这个类包含方法<code>__init__()</code>
 、管理飞船位置的方法<code>update()</code>
 和在屏幕上绘制飞船的方法<code>blitme()</code>
 。表示飞船的图像存储在文件夹images下的文件ship.bmp中。</p>
<blockquote>
<p>
<strong>动手试一试</strong>
</p>
<p>
<strong>练习12-3：Pygame文档</strong>
 　你在编写游戏的道路上走了很远，可能想看看Pygame文档。目前，只需大致浏览一下文档即可。在完成本章项目的过程中，不需要参阅这些文档，但如果你想修改游戏《外星人入侵》或编写自己的游戏，这些文档将会有所帮助。</p>
<p>
<strong>练习12-4：火箭</strong>
 　编写一个游戏，它在屏幕中央显示一个火箭，而玩家可使用四个方向键上下左右移动火箭。请务必确保火箭不会移到屏幕外面。</p>
<p>
<strong>练习12-5：按键</strong>
 　创建一个程序，它显示一个空屏幕。在事件循环中，每当检测到<code>pygame.KEYDOWN</code>
 事件时都打印属性<code>event.key</code>
 。运行这个程序并按各种键，看看Pygame如何响应。</p>
</blockquote>
<h2 id="nav_point_285">12.8　射击</h2>
<p>下面来添加射击功能。我们将编写在玩家按空格键时发射子弹（用小矩形表示）的代码。子弹将在屏幕中向上飞行，抵达屏幕上边缘后消失。</p>
<h3 id="nav_point_286">12.8.1　添加子弹设置</h3>
<p>首先，更新settings.py，在方法<code>__init__()</code>
 末尾存储新类<code>Bullet</code>
 所需的值：</p>
<p>
<em>settings.py</em>
</p>
<pre class="代码无行号"><code>    def __init__(self):
        --snip--
        # 子弹设置
        self.bullet_speed = 1.0
        self.bullet_width = 3
        self.bullet_height = 15
        self.bullet_color = (60, 60, 60)</code>
</pre>
<p>这些设置创建宽3像素、高15像素的深灰色子弹。子弹的速度比飞船稍低。</p>
<h3 id="nav_point_287">12.8.2　创建<code>Bullet</code>
 类</h3>
<p>下面来创建存储<code>Bullet</code>
 类的文件bullet.py，其前半部分如下：</p>
<p>
<em>bullet.py</em>
</p>
<pre class="代码无行号"><code>  import pygame
  from pygame.sprite import Sprite
  class Bullet(Sprite):
      """管理飞船所发射子弹的类"""
      def __init__(self, ai_game):
          """在飞船当前位置创建一个子弹对象。"""
          super().__init__()
          self.screen = ai_game.screen
          self.settings = ai_game.settings
          self.color = self.settings.bullet_color
          #在(0,0)处创建一个表示子弹的矩形，再设置正确的位置。
❶         self.rect = pygame.Rect(0, 0, self.settings.bullet_width,
              self.settings.bullet_height)
❷         self.rect.midtop = ai_game.ship.rect.midtop
          # 存储用小数表示的子弹位置。
❸         self.y = float(self.rect.y)</code>
</pre>
<p>
<code>Bullet</code>
 类继承了从模块<code>pygame.sprite</code>
 导入的<code>Sprite</code>
 类。通过使用精灵（sprite），可将游戏中相关的元素编组，进而同时操作编组中的所有元素。为创建子弹实例，<code>__init__()</code>
 需要当前的<code>AlienInvasion</code>
 实例，我们还调用了<code>super()</code>
 来继承<code>Sprite</code>
 。另外，我们还定义了用于存储屏幕以及设置对象和子弹颜色的属性。</p>
<p>在❶处，创建子弹的属性<code>rect</code>
 。子弹并非基于图像，因此必须使用<code>pygame.Rect()</code>
 类从头开始创建一个矩形。创建这个类的实例时，必须提供矩形左上角的<img alt="x" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/12_Image00003.gif"/>
 坐标和<img alt="y" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/12_Image00004.gif"/>
 坐标，以及矩形的宽度和高度。我们在(0, 0)处创建这个矩形，但下一行代码将其移到了正确的位置，因为子弹的初始位置取决于飞船当前的位置。子弹的宽度和高度是从<code>self.settings</code>
 中获取的。</p>
<p>在❷处，将子弹的<code>rect.midtop</code>
 设置为飞船的<code>rect.midtop</code>
 。这样子弹将从飞船顶部出发，看起来像是从飞船中射出的。我们将子弹的<img alt="y" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/12_Image00004.gif"/>
 坐标存储为小数值，以便能够微调子弹的速度（见❸）。</p>
<p>下面是bullet.py的第二部分，包括方法<code>update()</code>
 和<code>draw_bullet()</code>
 ：</p>
<p>
<em>bullet.py</em>
</p>
<pre class="代码无行号"><code>      def update(self):
          """向上移动子弹。"""
          #更新表示子弹位置的小数值。
❶         self.y -= self.settings.bullet_speed
          # 更新表示子弹的rect的位置。
❷         self.rect.y = self.y
      def draw_bullet(self):
          """在屏幕上绘制子弹。"""
❸         pygame.draw.rect(self.screen, self.color, self.rect)</code>
</pre>
<p>方法<code>update()</code>
 管理子弹的位置。发射出去后，子弹向上移动，意味着其<img alt="y" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/12_Image00004.gif"/>
 坐标将不断减小。为更新子弹的位置，从<code>self.y</code>
 中减去<code>settings .bullet_speed</code>
 的值（见❶）。接下来，将<code>self.rect.y</code>
 设置为<code>self.y</code>
 的值（见❷）。</p>
<p>属性<code>bullet_speed</code>
 让我们能够随着游戏的进行或根据需要提高子弹的速度，以调整游戏的行为。子弹发射后，其<img alt="x" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/12_Image00003.gif"/>
 坐标始终不变，因此子弹将沿直线垂直向上飞行。</p>
<p>需要绘制子弹时，我们调用<code>draw_bullet()</code>
 。<code>draw.rect()</code>
 函数使用存储在<code>self.color</code>
 中的颜色填充表示子弹的<code>rect</code>
 占据的屏幕部分（见❸）。</p>
<h3 id="nav_point_288">12.8.3　将子弹存储到编组中</h3>
<p>定义<code>Bullet</code>
 类和必要的设置后，便可编写代码在玩家每次按空格键时都射出一发子弹了。我们将在<code>AlienInvasion</code>
 中创建一个编组（group），用于存储所有有效的子弹，以便管理发射出去的所有子弹。这个编组是<code>pygame.sprite.Group</code>
 类的一个实例。<code>pygame.sprite.Group</code>
 类似于列表，但提供了有助于开发游戏的额外功能。在主循环中，将使用这个编组在屏幕上绘制子弹以及更新每颗子弹的位置。</p>
<p>首先，在<code>__init__()</code>
 中创建用于存储子弹的编组：</p>
<p>
<em>alien_invasion.py</em>
</p>
<pre class="代码无行号"><code>    def __init__(self):
        --snip--
        self.ship = Ship(self)
        self.bullets = pygame.sprite.Group()</code>
</pre>
<p>然后在<code>while</code>
 循环中更新子弹的位置：</p>
<p>
<em>alien_invasion.py</em>
</p>
<pre class="代码无行号"><code>      def run_game(self):
          """开始游戏主循环。"""
          while True:
              self._check_events()
              self.ship.update()
❶             self.bullets.update()
              self._update_screen()</code>
</pre>
<p>对编组调用<code>update()</code>
 时（见❶），编组自动对其中的每个精灵调用<code>update()</code>
 。因此代码行<code>bullets.update()</code>
 将为编组<code>bullets</code>
 中的每颗子弹调用<code>bullet.update()</code>
 。</p>
<h3 id="nav_point_289">12.8.4　开火</h3>
<p>在<code>AlienInvasion</code>
 中，需要修改<code>_check_keydown_events()</code>
 ，以便在玩家按空格键时发射一颗子弹。无须修改<code>_check_keyup_events()</code>
 ，因为玩家松开空格键时什么都不会发生。还需要修改<code>_update_screen()</code>
 ，确保在调用<code>flip()</code>
 前在屏幕上重绘每颗子弹。</p>
<p>为发射子弹，需要做的工作不少，因此编写一个新方法<code>_fire_bullet()</code>
 来完成这项任务：</p>
<p>
<em>alien_invasion.py</em>
</p>
<pre class="代码无行号"><code>  --snip--
  from ship import Ship
❶ from bullet import Bullet
  class AlienInvasion:
      --snip--
      def _check_keydown_events(self, event):
          --snip--
          elif event.key == pygame.K_q:
              sys.exit()
❷         elif event.key == pygame.K_SPACE:
              self._fire_bullet()
      def _check_keyup_events(self, event):
          --snip--
      def _fire_bullet(self):
          """创建一颗子弹，并将其加入编组bullets中。"""
❸         new_bullet = Bullet(self)
❹         self.bullets.add(new_bullet)
      def _update_screen(self):
          """更新屏幕上的图像，并切换到新屏幕。"""
          self.screen.fill(self.settings.bg_color)
          self.ship.blitme()
❺         for bullet in self.bullets.sprites():
              bullet.draw_bullet()
          pygame.display.flip()
  --snip--</code>
</pre>
<p>首先导入<code>Bullet</code>
 类（见❶），再在玩家按空格键时调用<code>_fire_bullet()</code>
 （见❷）。在<code>_fire_bullet()</code>
 中，创建一个<code>Bullet</code>
 实例并将其赋给<code>new_bullet</code>
 （见❸），再使用方法<code>add()</code>
 将其加入编组<code>bullets</code>
 中（见❹）。方法<code>add()</code>
 类似于<code>append()</code>
 ，不过是专门为Pygame编组编写的。</p>
<p>方法<code>bullets.sprites()</code>
 返回一个列表，其中包含编组<code>bullets</code>
 中的所有精灵。为在屏幕上绘制发射的所有子弹，遍历编组<code>bullets</code>
 中的精灵，并对每个精灵调用<code>draw_bullet()</code>
 （见❺）。</p>
<p>如果此时运行alien_invasion.py，将能够左右移动飞船，并发射任意数量的子弹。子弹在屏幕上向上飞行，抵达屏幕顶部后消失得无影无踪，如图12-3所示。你可在settings.py中修改子弹的尺寸、颜色和速度。</p>
<p class="图"><img alt="{%}" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/12_Image00007.gif"/>
</p>
<p>
<strong>图12-3　飞船发射一系列子弹后的《外星人入侵》游戏</strong>
</p>
<h3 id="nav_point_290">12.8.5　删除消失的子弹</h3>
<p>当前，子弹在抵达屏幕顶端后消失，但这仅仅是因为Pygame无法在屏幕外面绘制它们。这些子弹实际上依然存在，其<img alt="y" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/12_Image00004.gif"/>
 坐标为负数且越来越小。这是个问题，因为它们将继续消耗内存和处理能力。</p>
<p>需要将这些消失的子弹删除，否则游戏所做的无谓工作将越来越多，进而变得越来越慢。为此，需要检测表示子弹的<code>rect</code>
 的<code>bottom</code>
 属性是否为零。如果是，则表明子弹已飞过屏幕顶端：</p>
<p>
<em>alien_invasion.py</em>
</p>
<pre class="代码无行号"><code>      def run_game(self):
          """开始游戏主循环。"""
          while True:
              self._check_events()
              self.ship.update()
              self.bullets.update()
              # 删除消失的子弹。
❶             for bullet in self.bullets.copy():
❷               if bullet.rect.bottom &lt;= 0:
❸                     self.bullets.remove(bullet)
❹             print(len(self.bullets))
              self._update_screen()</code>
</pre>
<p>使用<code>for</code>
 循环遍历列表（或Pygame编组）时，Python要求该列表的长度在整个循环中保持不变。因为不能从<code>for</code>
 循环遍历的列表或编组中删除元素，所以必须遍历编组的副本。我们使用方法<code>copy()</code>
 来设置<code>for</code>
 循环（见❶），从而能够在循环中修改<code>bullets</code>
 。我们检查每颗子弹，看看它是否从屏幕顶端消失（见❷）。如果是，就将其从<code>bullets</code>
 中删除（见❸）。在❹处，使用函数调用<code>print()</code>
 显示当前还有多少颗子弹，以核实确实删除了消失的子弹。</p>
<p>如果这些代码没有问题，我们发射子弹后查看终端窗口时，将发现随着子弹一颗颗地在屏幕顶端消失，子弹数将逐渐降为零。运行该游戏并确认子弹被正确删除后，请将这个函数调用<code>print()</code>
 删除。如果不删除，游戏的速度将大大降低，因为将输出写入终端花费的时间比将图形绘制到游戏窗口花费的时间还要多。</p>
<h3 id="nav_point_291">12.8.6　限制子弹数量</h3>
<p>很多射击游戏对可同时出现在屏幕上的子弹数量进行了限制，以鼓励玩家有目标地射击。下面在游戏《外星人入侵》中做这样的限制。</p>
<p>首先，在settings.py中存储最大子弹数：</p>
<p>
<em>settings.py</em>
</p>
<pre class="代码无行号"><code>        # 子弹设置
        --snip--
        self.bullet_color = (60, 60, 60)
        self.bullets_allowed = 3</code>
</pre>
<p>这将未消失的子弹数限制为三颗。在<code>AlienInvasion</code>
 的<code>_fire_bullet()</code>
 中，在创建新子弹前检查未消失的子弹数是否小于该设置：</p>
<p>
<em>alien_invasion.py</em>
</p>
<pre class="代码无行号"><code>    def _fire_bullet(self):
        """创建新子弹并将其加入编组bullets中。"""
        if len(self.bullets) &lt; self.settings.bullets_allowed:
            new_bullet = Bullet(self)
            self.bullets.add(new_bullet)</code>
</pre>
<p>玩家按空格键时，我们检查<code>bullets</code>
 的长度。如果<code>len(bullets)</code>
 小于3，就创建一颗新子弹；但如果有三颗未消失的子弹，则玩家按空格键时什么都不会发生。如果现在运行这个游戏，屏幕上最多只能有三颗子弹。</p>
<h3 id="nav_point_292">12.8.7　创建方法<code>_update_bullets()</code>
</h3>
<p>编写并检查子弹管理代码后，可将其移到一个独立的方法中，确保<code>AlienInvasion</code>
 类组织有序。为此，创建一个名为<code>_update_bullets()</code>
 的新方法，并将其放在<code>_update_screen()</code>
 前面：</p>
<p>
<em>alien_invasion.py</em>
</p>
<pre class="代码无行号"><code>    def _update_bullets(self):
        """更新子弹的位置并删除消失的子弹。"""
        # 更新子弹的位置。
        self.bullets.update()
        # 删除消失的子弹。
        for bullet in self.bullets.copy():
            if bullet.rect.bottom &lt;= 0:
                 self.bullets.remove(bullet)</code>
</pre>
<p>
<code>_update_bullets()</code>
 的代码是从<code>run_game()</code>
 剪切并粘贴而来的，这里只是让注释更清晰了。</p>
<p>
<code>run_game()</code>
 中的<code>while</code>
 循环又变得简单了：</p>
<p>
<em>alien_invasion.py</em>
</p>
<pre class="代码无行号"><code>      while True:
          self._check_events()
          self.ship.update()
          self._update_bullets()
          self._update_screen()</code>
</pre>
<p>我们让主循环包含尽可能少的代码，这样只要看方法名就能迅速知道游戏中发生的情况。主循环检查玩家的输入，并更新飞船的位置和所有未消失子弹的位置。然后，使用更新后的位置来绘制新屏幕。</p>
<p>请再次运行alien_invasion.py，确认发射子弹时没有错误。</p>
<blockquote>
<p>
<strong>动手试一试</strong>
</p>
<p>
<strong>练习12-6：侧面射击</strong>
 　编写一个游戏，将一艘飞船放在屏幕左侧，并允许玩家上下移动飞船。在玩家按空格键时，让飞船发射一颗在屏幕中向右飞行的子弹，并在子弹从屏幕中消失后将其删除。</p>
</blockquote>
<h2 id="nav_point_293">12.9　小结</h2>
<p>在本章中，你学习了：游戏开发计划的制定，以及使用Pygame编写的游戏的基本结构；如何设置背景色，以及如何将设置存储在独立的类中，以便轻松调整；如何在屏幕上绘制图像，以及如何让玩家控制游戏元素的移动；创建自动移动的元素，如在屏幕中向上飞行的子弹，以及删除不再需要的对象；如何定期重构项目的代码，为后续开发提供便利。</p>
<p>在第13章中，我们将在游戏《外星人入侵》中添加外星人。到第13章结束时，你将能够击落外星人——但愿是在其撞到飞船之前！</p>
<br style="page-break-after:always"/>
<div></div>
]]></description>
    <pubDate>Mon, 20 Jul 2026 03:11:34 +0000</pubDate>
    <guid isPermaLink="false">12</guid>
</item>
<item>
    <title>Basic Python - 011 - 第 11 章 测试代码.epub</title>
    <description><![CDATA[<p style='color: #888;'>⏱ 预计阅读时间：81 分钟 (16187 字)</p><hr><h1 id="nav_point_244">第 11 章　测试代码</h1>
<blockquote>
<p><img alt="" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/11_Image00001.gif" style="width: 16%" width="16%"/>
 　编写函数或类时，还可为其编写测试。通过测试，可确定代码面对各种输入都能够按要求的那样工作。测试让你深信，即便有更多人使用你的程序，它也能正确地工作。在程序中添加新代码时，也可以对其进行测试，确认不会破坏程序既有的行为。程序员都会犯错，因此每个程序员都必须经常测试其代码，在用户发现问题前找出它们。</p>
<p>在本章中，你将学习如何使用Python模块<code>unittest</code>
 中的工具来测试代码，还将学习编写测试用例，核实一系列输入都将得到预期的输出。你将看到测试通过了是什么样子，测试未通过又是什么样子，还将知道测试未通过如何有助于改进代码。你将学习如何测试函数和类，并将知道该为项目编写多少个测试。</p>
</blockquote>
<h2 id="nav_point_245">11.1　测试函数</h2>
<p>要学习测试，必须有要测试的代码。下面是一个简单的函数，它接受名和姓并返回整洁的姓名：</p>
<p>
<em>name_function.py</em>
</p>
<pre class="代码无行号"><code>def get_formatted_name(first, last):
    """生成整洁的姓名。"""
    full_name = f"{first} {last}"
    return full_name.title()</code>
</pre>
<p>函数<code>get_formatted_name()</code>
 将名和姓合并成姓名：在名和姓之间加上一个空格并将其首字母大写，再返回结果。为核实<code>get_formatted_name()</code>
 像期望的那样工作，我们来编写一个使用该函数的程序。程序names.py让用户输入名和姓，并显示整洁的姓名：</p>
<p>
<em>names.py</em>
</p>
<pre class="代码无行号"><code>from name_function import get_formatted_name
print("Enter 'q' at any time to quit.")
while True:
    first = input("\nPlease give me a first name: ")
    if first == 'q':
        break
    last = input("Please give me a last name: ")
    if last == 'q':
        break
    formatted_name = get_formatted_name(first, last)
    print(f"\tNeatly formatted name: {formatted_name}.")</code>
</pre>
<p>这个程序从name_function.py中导入<code>get_formatted_name()</code>
 。用户可输入一系列名和姓，并看到格式整洁的姓名：</p>
<pre class="代码无行号"><code>Enter 'q' at any time to quit.
Please give me a first name: janis
Please give me a last name: joplin
        Neatly formatted name: Janis Joplin.
Please give me a first name: bob
Please give me a last name: dylan
        Neatly formatted name: Bob Dylan.
Please give me a first name: q</code>
</pre>
<p>从上述输出可知，合并得到的姓名正确无误。现在假设要修改<code>get_formatted_name()</code>
 ，使其还能够处理中间名。这样做时，要确保不破坏这个函数处理只含有名和姓的姓名的方式。为此，可在每次修改<code>get_formatted_name()</code>
 后都进行测试：运行程序names.py，并输入像Janis Joplin这样的姓名。不过这太烦琐了。所幸Python提供了一种自动测试函数输出的高效方式。倘若对<code>get_formatted_name()</code>
 进行自动测试，就能始终确信当提供测试过的姓名时，该函数都能正确工作。</p>
<h3 id="nav_point_246">11.1.1　单元测试和测试用例</h3>
<p>Python标准库中的模块<code>unittest</code>
 提供了代码测试工具。<strong>单元测试</strong>
 用于核实函数的某个方面没有问题。<strong>测试用例</strong>
 是一组单元测试，它们一道核实函数在各种情形下的行为都符合要求。良好的测试用例考虑到了函数可能收到的各种输入，包含针对所有这些情形的测试。<strong>全覆盖</strong>
 的测试用例包含一整套单元测试，涵盖了各种可能的函数使用方式。对于大型项目，要进行全覆盖测试可能很难。通常，最初只要针对代码的重要行为编写测试即可，等项目被广泛使用时再考虑全覆盖。</p>
<h3 id="nav_point_247">11.1.2　可通过的测试</h3>
<p>你需要一段时间才能习惯创建测试用例的语法，但创建测试用例之后，再添加针对函数的单元测试就很简单了。要为函数编写测试用例，可先导入模块<code>unittest</code>
 和要测试的函数，再创建一个继承<code>unittest.TestCase</code>
 的类，并编写一系列方法对函数行为的不同方面进行测试。</p>
<p>下面的测试用例只包含一个方法，它检查函数<code>get_formatted_name()</code>
 在给定名和姓时能否正确工作：</p>
<p>
<em>test_name_function.py</em>
</p>
<pre class="代码无行号"><code>  import unittest
  from name_function import get_formatted_name
❶ class NamesTestCase(unittest.TestCase):
      """测试name_function.py。"""
      def test_first_last_name(self):
          """能够正确地处理像Janis Joplin这样的姓名吗？"""
❷         formatted_name = get_formatted_name('janis', 'joplin')
❸         self.assertEqual(formatted_name, 'Janis Joplin')
❹ if __name__ == '__main__':
      unittest.main()</code>
</pre>
<p>首先，导入了模块<code>unittest</code>
 和要测试的函数<code>get_formatted_name()</code>
 。在❶处，创建了一个名为<code>NamesTestCase</code>
 的类，用于包含一系列针对<code>get_formatted_name()</code>
 的单元测试。这个类可以随意命名，但最好让它看起来与要测试的函数相关并包含<code>Test</code>
 字样。这个类必须继承<code>unittest.TestCase</code>
 类，这样Python才知道如何运行你编写的测试。</p>
<p>
<code>NamesTestCase</code>
 只包含一个方法，用于测试<code>get_formatted_name()</code>
 的一个方面。将该方法命名为<code>test_first_last_name()</code>
 ，因为要核实的是只有名和姓的姓名能否被正确格式化。运行test_name_function.py时，所有以<code>test_</code>
 打头的方法都将自动运行。在这个方法中，调用了要测试的函数。在本例中，使用实参<code>'janis'</code>
 和<code>'joplin'</code>
 调用<code>get_formatted_name()</code>
 ，并将结果赋给变量<code>formatted_name</code>
 （见❷）。</p>
<p>在❸处，使用了<code>unittest</code>
 类最有用的功能之一：<strong>断言</strong>
 方法。断言方法核实得到的结果是否与期望的结果一致。在这里，我们知道<code>get_formatted_name()</code>
 应返回名和姓首字母大写且之间有一个空格的姓名，因此期望<code>formatted_name</code>
 的值为<code>Janis Joplin</code>
 。为检查是否确实如此，我们调用<code>unittest</code>
 的方法<code>assertEqual()</code>
 ，并向它传递<code>formatted_name</code>
 和<code>'Janis Joplin'</code>
 。代码行</p>
<pre class="代码无行号"><code>self.assertEqual(formatted_name, 'Janis Joplin')</code>
</pre>
<p>的意思是：“将<code>formatted_name</code>
 的值与字符串<code>'Janis Joplin'</code>
 比较。如果它们相等，那么万事大吉；如果它们不相等，就告诉我一声！”</p>
<p>我们将直接运行这个文件，但需要指出的是，很多测试框架都会先导入测试文件再运行。导入文件时，解释器将在导入的同时执行它。❹处的<code>if</code>
 代码块检查特殊变量<code>__name__</code>
 ，这个变量是在程序执行时设置的。如果这个文件作为主程序执行，变量<code>__name__</code>
 将被设置为<code>'__main__'</code>
 。在这里，调用<code>unittest.main()</code>
 来运行测试用例。如果这个文件被测试框架导入，变量<code>__name__</code>
 的值将不是<code>'__main__'</code>
 ，因此不会调用<code>unittest.main()</code>
 。</p>
<p>运行test_name_function.py时，得到的输出如下：</p>
<pre class="代码无行号"><code>.
----------------------------------------------------------------------
Ran 1 test in 0.000s
OK</code>
</pre>
<p>第一行的句点表明有一个测试通过了。接下来的一行指出Python运行了一个测试，消耗的时间不到0.001秒。最后的<code>OK</code>
 表明该测试用例中的所有单元测试都通过了。</p>
<p>上述输出表明，给定包含名和姓的姓名时，函数<code>get_formatted_name()</code>
 总是能正确地处理。修改<code>get_formatted_name()</code>
 后，可再次运行这个测试用例。如果它通过了，就表明给定Janis Joplin这样的姓名时，该函数依然能够正确地处理。</p>
<h3 id="nav_point_248">11.1.3　未通过的测试</h3>
<p>测试未通过时结果是什么样的呢？我们来修改<code>get_formatted_name()</code>
 ，使其能够处理中间名，但同时故意让该函数无法正确处理像Janis Joplin这样只有名和姓的姓名。</p>
<p>下面是函数<code>get_formatted_name()</code>
 的新版本，它要求通过一个实参指定中间名：</p>
<p>
<em>name_function.py</em>
</p>
<pre class="代码无行号"><code>def get_formatted_name(first, middle, last):
    """生成整洁的姓名。"""
    full_name = f"{first} {middle} {last}"
    return full_name.title()</code>
</pre>
<p>这个版本应该能够正确处理包含中间名的姓名，但对其进行测试时，我们发现它不再能正确处理只有名和姓的姓名。这次运行程序test_name_function.py时，输出如下：</p>
<pre class="代码无行号"><code>❶ E
  ======================================================================
❷ ERROR: test_first_last_name (__main__.NamesTestCase)
  ----------------------------------------------------------------------
❸ Traceback (most recent call last):
    File "test_name_function.py", line 8, in test_first_last_name
      formatted_name = get_formatted_name('janis', 'joplin')
  TypeError: get_formatted_name() missing 1 required positional argument: 'last'
  ----------------------------------------------------------------------
❹ Ran 1 test in 0.000s
❺ FAILED (errors=1)</code>
</pre>
<p>里面包含很多信息，因为测试未通过时，需要让你知道的事情可能有很多。第一行输出只有一个字母<code>E</code>
 （见❶），指出测试用例中有一个单元测试导致了错误。接下来，我们看到<code>NamesTestCase</code>
 中的<code>test_first_last_name()</code>
 导致了错误（见❷）。测试用例包含众多单元测试时，知道哪个测试未通过至关重要。在❸处，我们看到了一个标准的traceback，指出函数调用<code>get_formatted_name</code>
<code>('janis', 'joplin')</code>
 有问题，因为缺少一个必不可少的位置实参。</p>
<p>我们还看到运行了一个单元测试（见❹）。最后是一条消息，指出整个测试用例未通过，因为运行该测试用例时发生了一个错误（见❺）。这条消息位于输出末尾，让你一眼就能看到。你可不希望为获悉有多少测试未通过而翻阅长长的输出。</p>
<h3 id="nav_point_249">11.1.4　测试未通过时怎么办</h3>
<p>测试未通过时怎么办呢？如果你检查的条件没错，测试通过意味着函数的行为是对的，而测试未通过意味着编写的新代码有错。因此，测试未通过时，不要修改测试，而应修复导致测试不能通过的代码：检查刚刚对函数所做的修改，找出导致函数行为不符合预期的修改。</p>
<p>在本例中，<code>get_formatted_name()</code>
 以前只需要名和姓两个实参，但现在要求提供名、中间名和姓。新增的中间名参数是必不可少的，这导致<code>get_formatted_name()</code>
 的行为不符合预期。就这里而言，最佳的选择是让中间名变为可选的。这样做后，使用类似于Janis Joplin的姓名进行测试时，测试就又能通过了，而且也可以接受中间名。下面来修改<code>get_formatted_name()</code>
 ，将中间名设置为可选的，然后再次运行这个测试用例。如果通过了，就接着确认该函数能够妥善地处理中间名。</p>
<p>要将中间名设置为可选的，可在函数定义中将形参<code>middle</code>
 移到形参列表末尾，并将其默认值指定为一个空字符串。还需要添加一个<code>if</code>
 测试，以便根据是否提供了中间名相应地创建姓名：</p>
<p>
<em>name_function.py</em>
</p>
<pre class="代码无行号"><code>def get_formatted_name(first, last, middle=''):
    """生成整洁的姓名。"""
    if middle:
        full_name = f"{first} {middle} {last}"
    else:
        full_name = f"{first} {last}"
    return full_name.title()</code>
</pre>
<p>在<code>get_formatted_name()</code>
 的这个新版本中，中间名是可选的。如果向该函数传递了中间名，姓名将包含名、中间名和姓，否则姓名将只包含名和姓。现在，对于两种不同的姓名，这个函数都应该能够正确地处理。为确定这个函数依然能够正确处理像Janis Joplin这样的姓名，我们再次运行test_name_function.py：</p>
<pre class="代码无行号"><code>.
----------------------------------------------------------------------
Ran 1 test in 0.000s
OK</code>
</pre>
<p>现在，测试用例通过了。太好了，这意味着这个函数又能正确处理像Janis Joplin这样的姓名了，而且我们无须手工测试这个函数。这个函数之所以很容易修复，是因为未通过的测试让我们得知新代码破坏了函数原来的行为。</p>
<h3 id="nav_point_250">11.1.5　添加新测试</h3>
<p>确定<code>get_formatted_name()</code>
 又能正确处理简单的姓名后，我们再编写一个测试，用于测试包含中间名的姓名。为此，在<code>NamesTestCase</code>
 类中再添加一个方法：</p>
<p>
<em>test_name_function.py</em>
</p>
<pre class="代码无行号"><code>  --snip--
  class NamesTestCase(unittest.TestCase):
      """测试name_function.py。"""
      def test_first_last_name(self):
          --snip--
      def test_first_last_middle_name(self):
          """能够正确地处理像Wolfgang Amadeus Mozart这样的姓名吗？"""
❶         formatted_name = get_formatted_name(
              'wolfgang', 'mozart', 'amadeus')
          self.assertEqual(formatted_name, 'Wolfgang Amadeus Mozart')
  if __name__ == '__main__':
      unittest.main()</code>
</pre>
<p>将该方法命名为<code>test_first_last_middle_name()</code>
 。方法名必须以<code>test_</code>
 打头，这样它才会在我们运行test_name_function.py时自动运行。这个方法名清楚地指出了它测试的是<code>get_formatted_name()</code>
 的哪个行为。这样，如果该测试未通过，我们就能马上知道受影响的是哪种类型的姓名。可以在<code>TestCase</code>
 类中使用很长的方法名，而且这些方法名必须是描述性的，这样你才能看懂测试未通过时的输出。这些方法由Python自动调用，你根本不用编写调用它们的代码。</p>
<p>为测试函数<code>get_formatted_name()</code>
 ，我们使用名、姓和中间名调用它（见❶），再使用<code>assertEqual()</code>
 检查返回的姓名是否与预期的姓名（名、中间名和姓）一致。再次运行test_name_function.py时，两个测试都通过了：</p>
<pre class="代码无行号"><code>..
----------------------------------------------------------------------
Ran 2 tests in 0.000s
OK</code>
</pre>
<p>太好了！现在我们知道，这个函数又能正确地处理像Janis Joplin这样的姓名了，而且深信它也能够正确地处理像Wolfgang Amadeus Mozart这样的姓名。</p>
<blockquote>
<p>
<strong>动手试一试</strong>
</p>
<p>
<strong>练习11-1：城市和国家</strong>
 　编写一个函数，它接受两个形参：一个城市名和一个国家名。这个函数返回一个格式为<code>
<i>City</i>
 , <i>Country</i>
</code>
 的字符串，如<code>Santiago, Chile</code>
 。将这个函数存储在一个名为city_functions.py的模块中。</p>
<p>创建一个名为test_cities.py的程序，对刚才编写的函数进行测试（别忘了，需要导入模块<code>unittest</code>
 和要测试的函数）。编写一个名为<code>test_city_country()</code>
 的方法，核实使用类似于<code>'santiago'</code>
 和<code>'chile'</code>
 这样的值来调用前述函数时，得到的字符串是正确的。运行test_cities.py，确认测试<code>test_city_country()</code>
 通过了。</p>
<p>
<strong>练习11-2：人口数量</strong>
 　修改前面的函数，加上第三个必不可少的形参<code>population</code>
 ，并返回一个格式为<code>
<i>City</i>
 , <i>Country – population xxx</i>
</code>
 的字符串，如<code>Santiago, Chile – population 5000000</code>
 。运行test_cities.py，确认测试<code>test_city_country()</code>
 未通过。</p>
<p>修改上述函数，将形参<code>population</code>
 设置为可选的。再次运行test_cities.py，确认测试<code>test_city_country()</code>
 又通过了。</p>
<p>再编写一个名为<code>test_city_country_population()</code>
 的测试，核实可以使用类似于<code>'santiago'</code>
 、<code>'chile'</code>
 和<code>'population=5000000'</code>
 这样的值来调用这个函数。再次运行test_cities.py，确认测试<code>test_city_country_population()</code>
 通过了。</p>
</blockquote>
<h2 id="nav_point_251">11.2　测试类</h2>
<p>在本章前半部分，你编写了针对单个函数的测试，下面来编写针对类的测试。很多程序中都会用到类，因此证明你的类能够正确工作大有裨益。如果针对类的测试通过了，你就能确信对类所做的改进没有意外地破坏其原有的行为。</p>
<h3 id="nav_point_252">11.2.1　各种断言方法</h3>
<p>Python在<code>unittest.TestCase</code>
 类中提供了很多断言方法。前面说过，断言方法检查你认为应该满足的条件是否确实满足。如果该条件确实满足，你对程序行为的假设就得到了确认，可以确信其中没有错误。如果你认为应该满足的条件实际上并不满足，Python将引发异常。</p>
<p>表11-1描述了6个常用的断言方法。使用这些方法可核实返回的值等于或不等于预期的值，返回的值为<code>True</code>
 或<code>False</code>
 ，以及返回的值在列表中或不在列表中。只能在继承<code>unittest.TestCase</code>
 的类中使用这些方法，随后来看看如何在测试类时使用其中之一。</p>
<p>
<strong>表11-1　unittest模块中的断言方法</strong>
</p>
<table border="1" width="90%">
<thead>
<tr>
<th>
<p class="表头单元格">方法</p>
</th>
<th>
<p class="表头单元格">用途</p>
</th>
</tr>
</thead>
<tbody>
<tr>
<td>
<p class="表格单元格">
<code>assertEqual(a, b)</code>
</p>
</td>
<td>
<p class="表格单元格">核实<code>a == b</code>
</p>
</td>
</tr>
<tr>
<td>
<p class="表格单元格">
<code>assertNotEqual(a, b)</code>
</p>
</td>
<td>
<p class="表格单元格">核实<code>a != b</code>
</p>
</td>
</tr>
<tr>
<td>
<p class="表格单元格">
<code>assertTrue(x)</code>
</p>
</td>
<td>
<p class="表格单元格">核实<code>x</code>
 为<code>True</code>
</p>
</td>
</tr>
<tr>
<td>
<p class="表格单元格">
<code>assertFalse(x)</code>
</p>
</td>
<td>
<p class="表格单元格">核实<code>x</code>
 为<code>False</code>
</p>
</td>
</tr>
<tr>
<td>
<p class="表格单元格">
<code>assertIn(<i>item</i>
 , <i>list</i>
 )</code>
</p>
</td>
<td>
<p class="表格单元格">核实<code>
<i>item</i>
</code>
 在<code>
<i>list</i>
</code>
 中</p>
</td>
</tr>
<tr>
<td>
<p class="表格单元格">
<code>assertNotIn(<i>item</i>
 , <i>list</i>
 )</code>
</p>
</td>
<td>
<p class="表格单元格">核实<code>
<i>item</i>
</code>
 不在<code>
<i>list</i>
</code>
 中</p>
</td>
</tr>
</tbody>
</table>
<h3 id="nav_point_253">11.2.2　一个要测试的类</h3>
<p>类的测试与函数的测试相似，你所做的大部分工作是测试类中方法的行为。不过还是存在一些不同之处，下面编写一个要测试的类。来看一个帮助管理匿名调查的类：</p>
<p>
<em>survey.py</em>
</p>
<pre class="代码无行号"><code>  class AnonymousSurvey:
      """收集匿名调查问卷的答案。"""
❶     def __init__(self, question):
          """存储一个问题，并为存储答案做准备。"""
          self.question = question
          self.responses = []
❷     def show_question(self):
          """显示调查问卷。"""
          print(self.question)
❸     def store_response(self, new_response):
          """存储单份调查答卷。"""
          self.responses.append(new_response)
❹     def show_results(self):
          """显示收集到的所有答卷。"""
          print("Survey results:")
          for response in self.responses:
              print(f"- {response}")</code>
</pre>
<p>这个类首先存储了一个调查问题（见❶），并创建了一个空列表，用于存储答案。这个类包含打印调查问题的方法（见❷），在答案列表中添加新答案的方法（见❸），以及将存储在列表中的答案都打印出来的方法（见❹）。要创建该类的实例，只需提供一个问题即可。有了表示调查的实例后，就可使用<code>show_question()</code>
 来显示其中的问题，使用<code>store_response()</code>
 来存储答案并使用<code>show_results()</code>
 来显示调查结果。</p>
<p>为证明<code>AnonymousSurvey</code>
 类能够正确工作，编写一个使用它的程序：</p>
<p>
<em>language_survey.py</em>
</p>
<pre class="代码无行号"><code>from survey import AnonymousSurvey
# 定义一个问题，并创建一个调查。
question = "What language did you first learn to speak?"
my_survey = AnonymousSurvey(question)
# 显示问题并存储答案。
my_survey.show_question()
print("Enter 'q' at any time to quit.\n")
while True:
    response = input("Language: ")
    if response == 'q':
        break
    my_survey.store_response(response)
# 显示调查结果。
print("\nThank you to everyone who participated in the survey!")
my_survey.show_results()</code>
</pre>
<p>这个程序定义了一个问题（<code>"What language did you first learn to speak? "</code>
 ），并使用该问题创建了一个<code>AnonymousSurvey</code>
 对象。接下来，这个程序调用<code>show_question()</code>
 来显示问题，并提示用户输入答案。在收到每个答案的同时将其存储起来。用户输入所有答案（输入<code>q</code>
 要求退出）后，调用<code>show_results()</code>
 来打印调查结果：</p>
<pre class="代码无行号"><code>What language did you first learn to speak?
Enter 'q' at any time to quit.
Language: English
Language: Spanish
Language: English
Language: Mandarin
Language: q
Thank you to everyone who participated in the survey!
Survey results:
- English
- Spanish
- English
- Mandarin</code>
</pre>
<p>
<code>AnonymousSurvey</code>
 类可用于进行简单的匿名调查。假设我们将它放在了模块<code>survey</code>
 中，并想进行改进：让每位用户都可输入多个答案；编写一个方法，只列出不同的答案并指出每个答案出现了多少次；再编写一个类，用于管理非匿名调查。</p>
<p>进行上述修改存在风险，可能影响<code>AnonymousSurvey</code>
 类的当前行为。例如，允许每位用户输入多个答案时，可能会不小心修改处理单个答案的方式。要确认在开发这个模块时没有破坏既有行为，可以编写针对这个类的测试。</p>
<h3 id="nav_point_254">11.2.3　测试<code>AnonymousSurvey</code>
 类</h3>
<p>下面来编写一个测试，对<code>AnonymousSurvey</code>
 类的行为的一个方面进行验证：如果用户面对调查问题只提供一个答案，这个答案也能被妥善地存储。为此，我们将在这个答案被存储后，使用方法<code>assertIn()</code>
 来核实它确实在答案列表中：</p>
<p>
<em>test_survey.py</em>
</p>
<pre class="代码无行号"><code>  import unittest
  from survey import AnonymousSurvey
❶ class TestAnonymousSurvey(unittest.TestCase):
      """针对AnonymousSurvey类的测试。"""
❷     def test_store_single_response(self):
          """测试单个答案会被妥善地存储。"""
          question = "What language did you first learn to speak?"
❸         my_survey = AnonymousSurvey(question)
          my_survey.store_response('English')
❹         self.assertIn('English', my_survey.responses)
  if __name__ == '__main__':
      unittest.main()</code>
</pre>
<p>首先导入模块<code>unittest</code>
 和要测试的类<code>AnonymousSurvey</code>
 。将测试用例命名为<code>TestAnonymousSurvey</code>
 ，它也继承了<code>unittest.TestCase</code>
 （见❶）。第一个测试方法验证：调查问题的单个答案被存储后，会包含在调查结果列表中。对于这个方法，一个不错的描述性名称是<code>test_store_single_response()</code>
 （见❷）。如果这个测试未通过，我们就能通过输出中的方法名得知，在存储单个调查答案方面存在问题。</p>
<p>要测试类的行为，需要创建其实例。在❸处，使用问题<code>"What language did you first learn to speak?"</code>
 创建一个名为<code>my_survey</code>
 的实例，然后使用方法<code>store_response()</code>
 存储单个答案<code>English</code>
 。接下来，检查<code>English</code>
 是否包含在列表<code>my_survey.responses</code>
 中，以核实这个答案是否被妥善地存储了（见❹）。</p>
<p>当我们运行test_survey.py时，测试通过了：</p>
<pre class="代码无行号"><code>.
----------------------------------------------------------------------
Ran 1 test in 0.001s
OK</code>
</pre>
<p>这很好，但只能收集一个答案的调查用途不大。下面来核实当用户提供三个答案时，它们也将被妥善地存储。为此，在<code>TestAnonymousSurvey</code>
 中再添加一个方法：</p>
<pre class="代码无行号"><code>  import unittest
  from survey import AnonymousSurvey
  class TestAnonymousSurvey(unittest.TestCase):
      """针对AnonymousSurvey类的测试。"""
      def test_store_single_response(self):
          --snip--
      def test_store_three_responses(self):
          """测试三个答案会被妥善地存储。"""
          question = "What language did you first learn to speak?"
          my_survey = AnonymousSurvey(question)
❶         responses = ['English', 'Spanish', 'Mandarin']
          for response in responses:
              my_survey.store_response(response)
❷         for response in responses:
              self.assertIn(response, my_survey.responses)
  if __name__ == '__main__':
      unittest.main()</code>
</pre>
<p>我们将该方法命名为<code>test_store_three_responses()</code>
 ，并像对<code>test_store_single_response()</code>
 所做的一样，在其中创建一个调查对象。定义一个包含三个不同答案的列表（见❶），再对其中每个答案调用<code>store_response()</code>
 。存储这些答案后，使用一个循环来确认每个答案都包含在<code>my_survey.responses</code>
 中（见❷）。</p>
<p>再次运行test_survey.py时，两个测试（针对单个答案的测试和针对三个答案的测试）都通过了：</p>
<pre class="代码无行号"><code>..
----------------------------------------------------------------------
Ran 2 tests in 0.000s
OK</code>
</pre>
<p>前述做法的效果很好，但这些测试有些重复的地方。下面使用<code>unittest</code>
 的另一项功能来提高其效率。</p>
<h3 id="nav_point_255">11.2.4　方法<code>setUp()</code>
</h3>
<p>在前面的test_survey.py中，我们在每个测试方法中都创建了一个<code>AnonymousSurvey</code>
 实例，并在每个方法中都创建了答案。<code>unittest.TestCase</code>
 类包含的方法<code>setUp()</code>
 让我们只需创建这些对象一次，就能在每个测试方法中使用。如果在<code>TestCase</code>
 类中包含了方法<code>setUp()</code>
 ，Python将先运行它，再运行各个以<code>test_</code>
 打头的方法。这样，在你编写的每个测试方法中，都可使用在方法<code>setUp()</code>
 中创建的对象。</p>
<p>下面使用<code>setUp()</code>
 来创建一个调查对象和一组答案，供方法<code>test_store_single_response()</code>
 和<code>test_store_three_responses()</code>
 使用：</p>
<pre class="代码无行号"><code>  import unittest
  from survey import AnonymousSurvey
  class TestAnonymousSurvey(unittest.TestCase):
      """针对AnonymousSurvey类的测试。"""
      def setUp(self):
          """
          创建一个调查对象和一组答案，供使用的测试方法使用。
          """
          question = "What language did you first learn to speak?"
❶         self.my_survey = AnonymousSurvey(question)
❷         self.responses = ['English', 'Spanish', 'Mandarin']
      def test_store_single_response(self):
          """测试单个答案会被妥善地存储。"""
          self.my_survey.store_response(self.responses[0])
          self.assertIn(self.responses[0], self.my_survey.responses)
      def test_store_three_responses(self):
          """测试三个答案会被妥善地存储。"""
          for response in self.responses:
              self.my_survey.store_response(response)
          for response in self.responses:
              self.assertIn(response, self.my_survey.responses)
  if __name__ == '__main__':
      unittest.main()</code>
</pre>
<p>方法<code>setUp()</code>
 做了两件事情：创建一个调查对象（见❶），以及创建一个答案列表（见❷）。存储这两样东西的变量名包含前缀<code>self</code>
 （即存储在属性中），因此可在这个类的任何地方使用。这让两个测试方法都更简单，因为它们都不用创建调查对象和答案了。方法<code>test_store_single_response()</code>
 核实<code>self.responses</code>
 中的第一个答案<code>self.responses[0]</code>
 被妥善地存储，而方法<code>test_store_three_response()</code>
 核实<code>self.responses</code>
 中的全部三个答案都被妥善地存储。</p>
<p>再次运行test_survey.py时，这两个测试也都通过了。如果要扩展<code>AnonymousSurvey</code>
 ，使其允许每位用户输入多个答案，这些测试将很有用。修改代码以接受多个答案后，可运行这些测试，确认存储单个答案或一系列答案的行为未受影响。</p>
<p>测试自己编写的类时，方法<code>setUp()</code>
 让测试方法编写起来更容易：可在<code>setUp()</code>
 方法中创建一系列实例并设置其属性，再在测试方法中直接使用这些实例。相比于在每个测试方法中都创建实例并设置其属性，这要容易得多。</p>
<blockquote>
<p>
<strong>注意</strong>
 　运行测试用例时，每完成一个单元测试，Python都打印一个字符：测试通过时打印一个句点，测试引发错误时打印一个<code>E</code>
 ，而测试导致断言失败时则打印一个<code>F</code>
 。这就是你运行测试用例时，在输出的第一行中看到的句点和字符数量各不相同的原因。如果测试用例包含很多单元测试，需要运行很长时间，就可通过观察这些结果来获悉有多少个测试通过了。</p>
</blockquote>
<p>　</p>
<blockquote>
<p>
<strong>动手试一试</strong>
</p>
<p>
<strong>练习11-3：雇员</strong>
 　编写一个名为<code>Employee</code>
 的类，其方法<code>__init__()</code>
 接受名、姓和年薪，并将它们存储在属性中。编写一个名为<code>give_raise()</code>
 的方法，它默认将年薪增加5000美元，但也能够接受其他的年薪增加量。</p>
<p>为<code>Employee</code>
 编写一个测试用例，其中包含两个测试方法：<code>test_give_default_raise()</code>
 和<code>test_give_custom_raise()</code>
 。使用方法<code>setUp()</code>
 ，以免在每个测试方法中都新建雇员实例。运行这个测试用例，确认两个测试都通过了。</p>
</blockquote>
<h2 id="nav_point_256">11.3　小结</h2>
<p>在本章中，你学习了：如何使用模块<code>unittest</code>
 中的工具来为函数和类编写测试；如何编写继承<code>unittest.TestCase</code>
 的类，以及如何编写测试方法，以核实函数和类的行为符合预期；如何使用方法<code>setUp()</code>
 来根据类高效地创建实例并设置其属性，以便在类的所有测试方法中使用。</p>
<p>测试是很多初学者不熟悉的主题。作为初学者，并非必须为你尝试的所有项目编写测试。然而参与工作量较大的项目时，你应该对自己所编写函数和类的重要行为进行测试。这样你就能够更加确定自己所做的工作不会破坏项目的其他部分，从而自由地改进既有代码。如果不小心破坏了原来的功能，你马上就会知道，而且能够轻松地修复问题。比起等到不满意的用户报告bug后再采取措施，在测试未通过时采取措施要容易得多。</p>
<p>如果你在项目中包含了初步测试，将得到其他程序员的尊敬。他们不仅能够更得心应手地使用你编写的代码，也更愿意与你合作开发项目。如果要跟其他程序员开发的项目共享代码，就必须证明你编写的代码通过了既有测试，通常还需要为你添加的新行为编写测试。</p>
<p>请通过多开展测试来熟悉代码测试过程。对于自己编写的函数和类，请编写针对其重要行为的测试。不过不要在项目早期试图编写全覆盖的测试用例，除非有充分的理由。</p>
<br style="page-break-after:always"/>
<div></div>
]]></description>
    <pubDate>Sat, 18 Jul 2026 01:26:28 +0000</pubDate>
    <guid isPermaLink="false">11</guid>
</item>
<item>
    <title>Basic Python - 010 - 第 10 章 文件和异常.epub</title>
    <description><![CDATA[<p style='color: #888;'>⏱ 预计阅读时间：130 分钟 (25970 字)</p><hr><h1 id="nav_point_216">第 10 章　文件和异常</h1>
<blockquote>
<p><img alt="" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/10_Image00001.gif" style="width: 16%" width="16%"/>
 　至此，你掌握了编写组织有序、易于使用的程序所需的基本技能，该考虑让程序目标更明确、用途更大了。在本章中，你将学习处理文件，让程序能够快速地分析大量数据；你将学习错误处理，避免程序在面对意外情形时崩溃；你将学习<strong>异常</strong>
 ，它们是Python创建的特殊对象，用于管理程序运行时出现的错误；你还将学习模块<code>json</code>
 ，它让你能够保存用户数据，以免在程序停止运行后丢失。</p>
<p>学习处理文件和保存数据可让你的程序使用起来更容易：用户将能够选择输入什么样的数据，以及在什么时候输入；用户使用你的程序做一些工作后，可将程序关闭，以后再接着往下做。学习处理异常可帮助你应对文件不存在的情形，以及处理其他可能导致程序崩溃的问题。这让你的程序在面对错误的数据时更健壮，不管这些错误数据源自无意的错误，还是源自破坏程序的恶意企图。你在本章学习的技能可提高程序的适用性、可用性和稳定性。</p>
</blockquote>
<h2 id="nav_point_217">10.1　从文件中读取数据</h2>
<p>文本文件可存储的数据量多得难以置信：天气数据、交通数据、社会经济数据、文学作品等。每当需要分析或修改存储在文件中的信息时，读取文件都很有用，对数据分析应用程序来说尤其如此。例如，可以编写一个这样的程序：读取一个文本文件的内容，重新设置这些数据的格式并将其写入文件，让浏览器能够显示这些内容。</p>
<p>要使用文本文件中的信息，首先需要将信息读取到内存中。为此，你可以一次性读取文件的全部内容，也可以以每次一行的方式逐步读取。</p>
<h3 id="nav_point_218">10.1.1　读取整个文件</h3>
<p>要读取文件，需要一个包含几行文本的文件。下面首先创建一个文件，它包含精确到小数点后30位的圆周率值，且在小数点后每10位处换行：</p>
<p>
<em>pi_digits.txt</em>
</p>
<pre class="代码无行号"><code>3.1415926535
  8979323846
  2643383279</code>
</pre>
<p>要动手尝试后续示例，可在编辑器中输入这些数据行，再将文件保存为pi_digits.txt，也可从本书主页（ituring.cn/book/2784）下载该文件。请将该文件保存到本章程序所在的目录。</p>
<p>下面的程序打开并读取这个文件，再将其内容显示到屏幕上：</p>
<p>
<em>file_reader.py</em>
</p>
<pre class="代码无行号"><code>with open('pi_digits.txt') as file_object:
    contents = file_object.read()
print(contents)</code>
</pre>
<p>在这个程序中，第一行代码做了大量的工作。我们先来看看函数<code>open()</code>
 。要以任何方式使用文件，那怕仅仅是打印其内容，都得先<strong>打开</strong>
 文件，才能访问它。函数<code>open()</code>
 接受一个参数：要打开的文件的名称。Python在当前执行的文件所在的目录中查找指定的文件。在本例中，当前运行的是file_reader.py，因此Python在file_reader.py所在的目录中查找pi_digits.txt。函数<code>open()</code>
 返回一个表示文件的对象。在这里，<code>open('pi_digits.txt')</code>
 返回一个表示文件pi_digits.txt的对象，Python将该对象赋给<code>file_object</code>
 供以后使用。</p>
<p>关键字<code>with</code>
 在不再需要访问文件后将其关闭。在这个程序中，注意到我们调用了<code>open()</code>
 ，但没有调用<code>close()</code>
 。也可以调用<code>open()</code>
 和<code>close()</code>
 来打开和关闭文件，但这样做时，如果程序存在bug导致方法<code>close()</code>
 未执行，文件将不会关闭。这看似微不足道，但未妥善关闭文件可能导致数据丢失或受损。如果在程序中过早调用<code>close()</code>
 ，你会发现需要使用文件时它已<strong>关闭</strong>
 （无法访问），这会导致更多的错误。并非在任何情况下都能轻松确定关闭文件的恰当时机，但通过使用前面所示的结构，可让Python去确定：你只管打开文件，并在需要时使用它，Python自会在合适的时候自动将其关闭。</p>
<p>有了表示pi_digits.txt的文件对象后，使用方法<code>read()</code>
 （前述程序的第二行）读取这个文件的全部内容，并将其作为一个长长的字符串赋给变量<code>contents</code>
 。这样，通过打印<code>contents</code>
 的值，就可将这个文本文件的全部内容显示出来：</p>
<pre class="代码无行号"><code>3.1415926535
  8979323846
  2643383279</code>
</pre>
<p>相比于原始文件，该输出唯一不同的地方是末尾多了一个空行。为何会多出这个空行呢？因为<code>read()</code>
 到达文件末尾时返回一个空字符串，而将这个空字符串显示出来时就是一个空行。要删除多出来的空行，可在函数调用<code>print()</code>
 中使用<code>rstrip()</code>
 ：</p>
<pre class="代码无行号"><code>with open('pi_digits.txt') as file_object:
    contents = file_object.read()
print(contents.rstrip())</code>
</pre>
<p>本书前面说过，Python方法<code>rstrip()</code>
 删除字符串末尾的空白。现在，输出与原始文件的内容完全相同：</p>
<pre class="代码无行号"><code>3.1415926535
  8979323846
  2643383279</code>
</pre>
<h3 id="nav_point_219">10.1.2　文件路径</h3>
<p>将类似于pi_digits.txt的简单文件名传递给函数<code>open()</code>
 时，Python将在当前执行的文件（即.py程序文件）所在的目录中查找。</p>
<p>根据你组织文件的方式，有时可能要打开不在程序文件所属目录中的文件。例如，你可能将程序文件存储在了文件夹python_work中，而该文件夹中有一个名为text_files的文件夹用于存储程序文件操作的文本文件。虽然文件夹text_files包含在文件夹python_work中，但仅向<code>open()</code>
 传递位于前者中的文件名称也不可行，因为Python只在文件夹python_work中查找，而不会在其子文件夹text_files中查找。要让Python打开不与程序文件位于同一个目录中的文件，需要提供文件路径，让Python到系统的特定位置去查找。</p>
<p>由于文件夹text_files位于文件夹python_work中，可以使用相对文件路径来打开其中的文件。相对文件路径让Python到指定的位置去查找，而该位置是相对于当前运行的程序所在目录的。例如，可这样编写代码：</p>
<pre class="代码无行号"><code>with open('text_files/filename.txt') as file_object:</code>
</pre>
<p>这行代码让Python到文件夹python_work下的文件夹text_files中去查找指定的.txt文件。</p>
<blockquote>
<p>
<strong>注意</strong>
 　显示文件路径时，Windows系统使用反斜杠（<code>\</code>
 ）而不是斜杠（<code>/</code>
 ），但在代码中依然可以使用斜杠。</p>
</blockquote>
<p>还可以将文件在计算机中的准确位置告诉Python，这样就不用关心当前运行的程序存储在什么地方了。这称为<strong>绝对文件路径</strong>
 。在相对路径行不通时，可使用绝对路径。例如，如果text_files并不在文件夹python_work中，而在文件夹other_files中，则向<code>open()</code>
 传递路径<code>'text_files/<i>filename</i>
 .txt'</code>
 行不通，因为Python只在文件夹python_work中查找该位置。为明确指出希望Python到哪里去查找，需要提供完整的路径。</p>
<p>绝对路径通常比相对路径长，因此将其赋给一个变量，再将该变量传递给<code>open()</code>
 会有所帮助：</p>
<pre class="代码无行号"><code>file_path = '/home/ehmatthes/other_files/text_files/_filename_.txt'
with open(file_path) as file_object:</code>
</pre>
<p>通过使用绝对路径，可读取系统中任何地方的文件。就目前而言，最简单的做法是，要么将数据文件存储在程序文件所在的目录，要么将其存储在程序文件所在目录下的一个文件夹（如text_files）中。</p>
<blockquote>
<p>
<strong>注意</strong>
 　如果在文件路径中直接使用反斜杠，将引发错误，因为反斜杠用于对字符串中的字符进行转义。例如，对于路径<code>"C:\path\to\file.txt"</code>
 ，其中的<code>\t</code>
 将被解读为制表符。如果一定要使用反斜杠，可对路径中的每个反斜杠都进行转义，如<code>"C:\\path\\to\\file.txt"</code>
 。</p>
</blockquote>
<h3 id="nav_point_220">10.1.3　逐行读取</h3>
<p>读取文件时，常常需要检查其中的每一行：可能要在文件中查找特定的信息，或者要以某种方式修改文件中的文本。例如，你可能要遍历一个包含天气数据的文件，并使用天气描述中包含sunny字样的行。在新闻报道中，你可能会查找包含标签<code>&lt;headline&gt;</code>
 的行，并按特定的格式设置它。</p>
<p>要以每次一行的方式检查文件，可对文件对象使用<code>for</code>
 循环：</p>
<p>
<em>file_reader.py</em>
</p>
<pre class="代码无行号"><code>❶ filename = 'pi_digits.txt'
❷ with open(filename) as file_object:
❸     for line in file_object:
          print(line)</code>
</pre>
<p>在❶处，将要读取的文件的名称赋给变量<code>filename</code>
 。这是使用文件时的一种常见做法。变量<code>filename</code>
 表示的并非实际文件——它只是一个让Python知道到哪里去查找文件的字符串，因此可以轻松地将<code>'pi_digits.txt'</code>
 替换为要使用的另一个文件的名称。调用<code>open()</code>
 后，将一个表示文件及其内容的对象赋给了变量<code>file_object</code>
 （见❷）。这里也使用了关键字<code>with</code>
 ，让Python负责妥善地打开和关闭文件。为查看文件的内容，通过对文件对象执行循环来遍历文件中的每一行（见❸）。</p>
<p>打印每一行时，发现空白行更多了：</p>
<pre>3.1415926535
  8979323846
  2643383279
</pre>
<p>为何会出现这些空白行呢？因为在这个文件中，每行的末尾都有一个看不见的换行符，而函数调用<code>print()</code>
 也会加上一个换行符，因此每行末尾都有两个换行符：一个来自文件，另一个来自函数调用<code>print()</code>
 。要消除这些多余的空白行，可在函数调用<code>print()</code>
 中使用<code>rstrip()</code>
 ：</p>
<pre class="代码无行号"><code>filename = 'pi_digits.txt'
with open(filename) as file_object:
    for line in file_object:
        print(line.rstrip())</code>
</pre>
<p>现在，输出又与文件内容完全相同了：</p>
<pre class="代码无行号"><code>3.1415926535
  8979323846
  2643383279</code>
</pre>
<h3 id="nav_point_221">10.1.4　创建一个包含文件各行内容的列表</h3>
<p>使用关键字<code>with</code>
 时，<code>open()</code>
 返回的文件对象只在<code>with</code>
 代码块内可用。如果要在<code>with</code>
 代码块外访问文件的内容，可在<code>with</code>
 代码块内将文件的各行存储在一个列表中，并在<code>with</code>
 代码块外使用该列表：可以立即处理文件的各个部分，也可以推迟到程序后面再处理。</p>
<p>下面的示例在<code>with</code>
 代码块中将文件pi_digits.txt的各行存储在一个列表中，再在<code>with</code>
 代码块外打印：</p>
<pre class="代码无行号"><code>  filename = 'pi_digits.txt'
  with open(filename) as file_object:
❶     lines = file_object.readlines()
❷ for line in lines:
      print(line.rstrip())</code>
</pre>
<p>❶处的方法<code>readlines()</code>
 从文件中读取每一行，并将其存储在一个列表中。接下来，该列表被赋给变量<code>lines</code>
 。在<code>with</code>
 代码块外，依然可使用这个变量。在❷处，使用一个简单的<code>for</code>
 循环来打印<code>lines</code>
 中的各行。因为列表<code>lines</code>
 的每个元素都对应于文件中的一行，所以输出与文件内容完全一致。</p>
<h3 id="nav_point_222">10.1.5　使用文件的内容</h3>
<p>将文件读取到内存中后，就能以任何方式使用这些数据了。下面以简单的方式使用圆周率的值。首先，创建一个字符串，它包含文件中存储的所有数字，且没有任何空格：</p>
<p>
<em>pi_string.py</em>
</p>
<pre class="代码无行号"><code>filename = 'pi_digits.txt'
  with open(filename) as file_object:
      lines = file_object.readlines()
❶ pi_string = ''
❷ for line in lines:
      pi_string += line.rstrip()
❸ print(pi_string)
  print(len(pi_string))</code>
</pre>
<p>像前一个示例一样，首先打开文件，并将其中所有的行都存储在一个列表中。在❶处，创建了一个变量<code>pi_string</code>
 ，用于指向圆周率的值。接下来，使用一个循环将各行加入<code>pi_string</code>
 ，并删除每行末尾的换行符（见❷）。在❸处，打印这个字符串及其长度：</p>
<pre class="代码无行号"><code>3.1415926535  8979323846  2643383279
36</code>
</pre>
<p>变量<code>pi_string</code>
 指向的字符串包含原来位于每行左边的空格，为删除这些空格，可使用<code>strip()</code>
 而非<code>rstrip()</code>
 ：</p>
<pre class="代码无行号"><code>--snip--
for line in lines:
    pi_string += line.strip()
print(pi_string)
print(len(pi_string))</code>
</pre>
<p>这样就获得了一个字符串，其中包含准确到30位小数的圆周率值。这个字符串长32字符，因为它还包含整数部分的3和小数点：</p>
<pre class="代码无行号"><code>3.141592653589793238462643383279
32</code>
</pre>
<blockquote>
<p>
<strong>注意</strong>
 　读取文本文件时，Python将其中的所有文本都解读为字符串。如果读取的是数，并要将其作为数值使用，就必须使用函数<code>int()</code>
 将其转换为整数或使用函数<code>float()</code>
 将其转换为浮点数。</p>
</blockquote>
<h3 id="nav_point_223">10.1.6　包含一百万位的大型文件</h3>
<p>前面分析的都是一个只有三行的文本文件，但这些代码示例也可处理大得多的文件。如果我们有一个文本文件，其中包含精确到小数点后1 000 000位而不是30位的圆周率值，也可创建一个包含所有这些数字的字符串。为此，无须对前面的程序做任何修改，只要将这个文件传递给它即可。在这里，只打印到小数点后50位，以免终端为显示全部1 000 000位而不断滚动：</p>
<p>
<em>pi_string.py</em>
</p>
<pre class="代码无行号"><code>filename = 'pi_million_digits.txt'
with open(filename) as file_object:
    lines = file_object.readlines()
pi_string = ''
for line in lines:
    pi_string += line.strip()
print(f"{pi_string[:52]}...")
print(len(pi_string))</code>
</pre>
<p>输出表明，创建的字符串确实包含精确到小数点后1 000 000位的圆周率值：</p>
<pre class="代码无行号"><code>3.14159265358979323846264338327950288419716939937510...
1000002</code>
</pre>
<p>对于可处理的数据量，Python没有任何限制。只要系统的内存足够多，你想处理多少数据都可以。</p>
<blockquote>
<p>
<strong>注意</strong>
 　要运行这个程序（以及后面的众多示例），需要从<a href="http://ituring.cn/book/2784">http://ituring.cn/book/2784</a>
 下载相关的资源。</p>
</blockquote>
<h3 id="nav_point_224">10.1.7　圆周率值中包含你的生日吗</h3>
<p>我一直想知道自己的生日是否包含在圆周率值中。下面来扩展刚才编写的程序，以确定某个人的生日是否包含在圆周率值的前1 000 000位中。为此，可将生日表示为一个由数字组成的字符串，再检查这个字符串是否包含在<code>pi_string</code>
 中：</p>
<pre class="代码无行号"><code>  --snip--
  for line in lines:
      pi_string += line.strip()
❶ birthday = input("Enter your birthday, in the form mmddyy: ")
❷ if birthday in pi_string:
      print("Your birthday appears in the first million digits of pi!")
  else:
      print("Your birthday does not appear in the first million digits of pi.")</code>
</pre>
<p>在❶处，提示用户输入生日。在❷处，检查这个字符串是否包含在<code>pi_string</code>
 中。下面来运行一下这个程序：</p>
<pre class="代码无行号"><code>Enter your birthdate, in the form mmddyy: 120372
Your birthday appears in the first million digits of pi!</code>
</pre>
<p>我的生日确实出现在了圆周率值中！读取文件的内容后，能以你能想到的任何方式对其进行分析。</p>
<blockquote>
<p>
<strong>动手试一试</strong>
</p>
<p>
<strong>练习10-1：Python学习笔记</strong>
 　在文本编辑器中新建一个文件，写几句话来总结一下你至此学到的Python知识，其中每一行都以“In Python you can”打头。将这个文件命名为learning_python.txt，并存储到为完成本章练习而编写的程序所在的目录中。编写一个程序，它读取这个文件，并将你所写的内容打印三次：第一次打印时读取整个文件；第二次打印时遍历文件对象；第三次打印时将各行存储在一个列表中，再在<code>with</code>
 代码块外打印它们。</p>
<p>
<strong>练习10-2：C语言学习笔记</strong>
 　可使用方法<code>replace()</code>
 将字符串中的特定单词都替换为另一个单词。下面是一个简单的示例，演示了如何将句子中的<code>'dog'</code>
 替换为<code>'cat'</code>
 ：</p>
<pre class="代码无行号"><code>&gt;&gt;&gt; message = "I really like dogs."
&gt;&gt;&gt; message.replace('dog', 'cat')
'I really like cats.'</code>
</pre>
<p>读取你刚创建的文件learning_python.txt中的每一行，将其中的Python都替换为另一门语言的名称，比如C。将修改后的各行都打印到屏幕上。</p>
</blockquote>
<h2 id="nav_point_225">10.2　写入文件</h2>
<p>保存数据的最简单的方式之一是将其写入文件中。通过将输出写入文件，即便关闭包含程序输出的终端窗口，这些输出也依然存在：可以在程序结束运行后查看这些输出，可以与别人分享输出文件，还可以编写程序来将这些输出读取到内存中并进行处理。</p>
<h3 id="nav_point_226">10.2.1　写入空文件</h3>
<p>要将文本写入文件，你在调用<code>open()</code>
 时需要提供另一个实参，告诉Python你要写入打开的文件。为明白其中的工作原理，我们来将一条简单的消息存储到文件中，而不是将其打印到屏幕上：</p>
<p>
<em>write_message.py</em>
</p>
<pre class="代码无行号"><code>  filename = 'programming.txt'
❶ with open(filename, 'w') as file_object:
❷     file_object.write("I love programming.")</code>
</pre>
<p>在本例中，调用<code>open()</code>
 时提供了两个实参（见❶）。第一个实参也是要打开的文件的名称。第二个实参（<code>'w'</code>
 ）告诉Python，要以<strong>写入模式</strong>
 打开这个文件。打开文件时，可指定<strong>读取模式</strong>
 （<code>'r'</code>
 ）、<strong>写入模式</strong>
 （<code>'w'</code>
 ）、<strong>附加模式</strong>
 （<code>'a'</code>
 ）或<strong>读写模式</strong>
 （<code>'r+'</code>
 ）。如果省略了模式实参，Python将以默认的只读模式打开文件。</p>
<p>如果要写入的文件不存在，函数<code>open()</code>
 将自动创建它。然而，以写入模式（<code>'w'</code>
 ）打开文件时千万要小心，因为如果指定的文件已经存在，Python将在返回文件对象前清空该文件的内容。</p>
<p>在❷处，使用文件对象的方法<code>write()</code>
 将一个字符串写入文件。这个程序没有终端输出，但如果打开文件programming.txt，将看到其中包含如下一行内容：</p>
<p>
<em>programming.txt</em>
</p>
<pre class="代码无行号"><code>I love programming.</code>
</pre>
<p>相比于计算机中的其他文件，这个文件没有什么不同。你可以打开它、在其中输入新文本、复制其内容、将内容粘贴到其中，等等。</p>
<blockquote>
<p>
<strong>注意</strong>
 　Python只能将字符串写入文本文件。要将数值数据存储到文本文件中，必须先使用函数<code>str()</code>
 将其转换为字符串格式。</p>
</blockquote>
<h3 id="nav_point_227">10.2.2　写入多行</h3>
<p>函数<code>write()</code>
 不会在写入的文本末尾添加换行符，因此如果写入多行时没有指定换行符，文件看起来可能不是你希望的那样：</p>
<pre class="代码无行号"><code>filename = 'programming.txt'
with open(filename, 'w') as file_object:
    file_object.write("I love programming.")
    file_object.write("I love creating new games.")</code>
</pre>
<p>如果你打开programming.txt，将发现两行内容挤在一起：</p>
<pre class="代码无行号"><code>I love programming.I love creating new games.</code>
</pre>
<p>要让每个字符串都单独占一行，需要在方法调用<code>write()</code>
 中包含换行符：</p>
<pre class="代码无行号"><code>filename = 'programming.txt'
with open(filename, 'w') as file_object:
    file_object.write("I love programming.\n")
    file_object.write("I love creating new games.\n")</code>
</pre>
<p>现在，输出出现在不同的行中：</p>
<pre class="代码无行号"><code>I love programming.
I love creating new games.</code>
</pre>
<p>像显示到终端的输出一样，还可以使用空格、制表符和空行来设置这些输出的格式。</p>
<h3 id="nav_point_228">10.2.3　附加到文件</h3>
<p>如果要给文件添加内容，而不是覆盖原有的内容，可以以<strong>附加模式</strong>
 打开文件。以附加模式打开文件时，Python不会在返回文件对象前清空文件的内容，而是将写入文件的行添加到文件末尾。如果指定的文件不存在，Python将为你创建一个空文件。</p>
<p>下面来修改write_message.py，在既有文件programming.txt中再添加一些你酷爱编程的原因：</p>
<p>
<em>write_message.py</em>
</p>
<pre class="代码无行号"><code>  filename = 'programming.txt'
❶ with open(filename, 'a') as file_object:
❷     file_object.write("I also love finding meaning in large datasets.\n")
      file_object.write("I love creating apps that can run in a browser.\n")</code>
</pre>
<p>在❶处，打开文件时指定了实参<code>'a'</code>
 ，以便将内容附加到文件末尾，而不是覆盖文件原来的内容。在❷处，又写入了两行，它们被添加到文件programming.txt末尾：</p>
<p>
<em>programming.txt</em>
</p>
<pre class="代码无行号"><code>I love programming.
I love creating new games.
I also love finding meaning in large datasets.
I love creating apps that can run in a browser.</code>
</pre>
<p>最终的结果是，文件原来的内容还在，后面则是刚添加的内容。</p>
<blockquote>
<p>
<strong>动手试一试</strong>
</p>
<p>
<strong>练习10-3：访客</strong>
 　编写一个程序，提示用户输入名字。用户做出响应后，将其名字写入文件guest.txt中。</p>
<p>
<strong>练习10-4：访客名单</strong>
 　编写一个<code>while</code>
 循环，提示用户输入名字。用户输入名字后，在屏幕上打印一句问候语，并将一条到访记录添加到文件guest_book.txt中。确保这个文件中的每条记录都独占一行。</p>
<p>
<strong>练习10-5：调查</strong>
 　编写一个<code>while</code>
 循环，询问用户为何喜欢编程。每当用户输入一个原因后，都将其添加到一个存储所有原因的文件中。</p>
</blockquote>
<h2 id="nav_point_229">10.3　异常</h2>
<p>Python使用称为<strong>异常</strong>
 的特殊对象来管理程序执行期间发生的错误。每当发生让Python不知所措的错误时，它都会创建一个异常对象。如果你编写了处理该异常的代码，程序将继续运行；如果未对异常进行处理，程序将停止并显示traceback，其中包含有关异常的报告。</p>
<p>异常是使用<code>try-except</code>
 代码块处理的。<code>try-except</code>
 代码块让Python执行指定的操作，同时告诉Python发生异常时怎么办。使用<code>try-except</code>
 代码块时，即便出现异常，程序也将继续运行：显示你编写的友好的错误消息，而不是令用户迷惑的traceback。</p>
<h3 id="nav_point_230">10.3.1　处理<code>ZeroDivisionError</code>
 异常</h3>
<p>下面来看一种导致Python引发异常的简单错误。你可能知道，不能用数除以0，但还是让Python这样做：</p>
<p>
<em>division_calculator.py</em>
</p>
<pre class="代码无行号"><code>print(5/0)</code>
</pre>
<p>显然，Python无法这样做，因此你将看到一个traceback：</p>
<pre class="代码无行号"><code>  Traceback (most recent call last):
    File "division_calculator.py", line 1, in &lt;module&gt;
      print(5/0)
❶ ZeroDivisionError: division by zero</code>
</pre>
<p>在上述traceback中，❶处指出的错误<code>ZeroDivisionError</code>
 是个异常对象。Python无法按你的要求做时，就会创建这种对象。在这种情况下，Python将停止运行程序，并指出引发了哪种异常，而我们可根据这些信息对程序进行修改。下面来告诉Python，发生这种错误时怎么办。这样，如果再次发生此类错误，我们就有备无患了。</p>
<h3 id="nav_point_231">10.3.2　使用<code>try-except</code>
 代码块</h3>
<p>当你认为可能会发生错误时，可编写一个<code>try-except</code>
 代码块来处理可能引发的异常。你让Python尝试运行一些代码，并告诉它如果这些代码引发了指定的异常该怎么办。</p>
<p>处理<code>ZeroDivisionError</code>
 异常的<code>try-except</code>
 代码块类似于下面这样：</p>
<pre class="代码无行号"><code>try:
    print(5/0)
except ZeroDivisionError:
    print("You can't divide by zero!")</code>
</pre>
<p>将导致错误的代码行<code>print(5/0)</code>
 放在一个<code>try</code>
 代码块中。如果<code>try</code>
 代码块中的代码运行起来没有问题，Python将跳过<code>except</code>
 代码块；如果<code>try</code>
 代码块中的代码导致了错误，Python将查找与之匹配的<code>except</code>
 代码块并运行其中的代码。</p>
<p>在本例中，<code>try</code>
 代码块中的代码引发了<code>ZeroDivisionError</code>
 异常，因此Python查找指出了该怎么办的<code>except</code>
 代码块，并运行其中的代码。这样，用户看到的是一条友好的错误消息，而不是traceback：</p>
<pre class="代码无行号"><code>You can't divide by zero!</code>
</pre>
<p>如果<code>try-except</code>
 代码块后面还有其他代码，程序将接着运行，因为已经告诉了Python如何处理这种错误。下面来看一个捕获错误后程序继续运行的示例。</p>
<h3 id="nav_point_232">10.3.3　使用异常避免崩溃</h3>
<p>发生错误时，如果程序还有工作尚未完成，妥善地处理错误就尤其重要。这种情况经常会出现在要求用户提供输入的程序中；如果程序能够妥善地处理无效输入，就能再提示用户提供有效输入，而不至于崩溃。</p>
<p>下面来创建一个只执行除法运算的简单计算器：</p>
<p>
<em>division_calculator.py</em>
</p>
<pre class="代码无行号"><code>  print("Give me two numbers, and I'll divide them.")
  print("Enter 'q' to quit.")
  while True:
❶     first_number = input("\nFirst number: ")
      if first_number == 'q':
          break
❷     second_number = input("Second number: ")
      if second_number == 'q':
          break
❸     answer = int(first_number) / int(second_number)
      print(answer)</code>
</pre>
<p>在❶处，程序提示用户输入一个数，并将其赋给变量<code>first_number</code>
 。如果用户输入的不是表示退出的<code>q</code>
 ，就再提示用户输入一个数，并将其赋给变量<code>second_number</code>
 （见❷）。接下来，计算这两个数的商（见❸）。该程序没有采取任何处理错误的措施，因此在执行除数为0的除法运算时，它将崩溃：</p>
<pre class="代码无行号"><code>Give me two numbers, and I'll divide them.
Enter 'q' to quit.
First number: 5
Second number: 0
Traceback (most recent call last):
  File "division_calculator.py", line 9, in &lt;module&gt;
    answer = int(first_number) / int(second_number)
ZeroDivisionError: division by zero</code>
</pre>
<p>程序崩溃可不好，但让用户看到traceback也不是个好主意。不懂技术的用户会被搞糊涂，怀有恶意的用户还会通过traceback获悉你不想他知道的信息。例如，他将知道你的程序文件的名称，还将看到部分不能正确运行的代码。有时候，训练有素的攻击者可根据这些信息判断出可对你的代码发起什么样的攻击。</p>
<h3 id="nav_point_233">10.3.4　<code>else</code>
 代码块</h3>
<p>通过将可能引发错误的代码放在<code>try-except</code>
 代码块中，可提高程序抵御错误的能力。错误是执行除法运算的代码行导致的，因此需要将它放到<code>try-except</code>
 代码块中。这个示例还包含一个<code>else</code>
 代码块。依赖<code>try</code>
 代码块成功执行的代码都应放到<code>else</code>
 代码块中：</p>
<pre class="代码无行号"><code>  --snip--
  while True:
      --snip--
      if second_number == 'q':
          break
❶     try:
          answer = int(first_number) / int(second_number)
❷     except ZeroDivisionError:
          print("You can't divide by 0!")
❸     else:
          print(answer)</code>
</pre>
<p>让Python尝试执行<code>try</code>
 代码块中的除法运算（见❶），这个代码块只包含可能导致错误的代码。依赖<code>try</code>
 代码块成功执行的代码都放在<code>else</code>
 代码块中。在本例中，如果除法运算成功，就使用<code>else</code>
 代码块来打印结果（见❸）。</p>
<p>
<code>except</code>
 代码块告诉Python，出现<code>ZeroDivisionError</code>
 异常时该如何办（见❷）。如果<code>try</code>
 代码块因除零错误而失败，就打印一条友好的消息，告诉用户如何避免这种错误。程序继续运行，用户根本看不到traceback：</p>
<pre class="代码无行号"><code>Give me two numbers, and I'll divide them.
Enter 'q' to quit.
First number: 5
Second number: 0
You can't divide by 0!
First number: 5
Second number: 2
2.5
First number: q</code>
</pre>
<p>
<code>try-except-else</code>
 代码块的工作原理大致如下。Python尝试执行<code>try</code>
 代码块中的代码，只有可能引发异常的代码才需要放在<code>try</code>
 语句中。有时候，有一些仅在<code>try</code>
 代码块成功执行时才需要运行的代码，这些代码应放在<code>else</code>
 代码块中。<code>except</code>
 代码块告诉Python，如果尝试运行<code>try</code>
 代码块中的代码时引发了指定的异常该怎么办。</p>
<p>通过预测可能发生错误的代码，可编写健壮的程序。它们即便面临无效数据或缺少资源，也能继续运行，从而抵御无意的用户错误和恶意的攻击。</p>
<h3 id="nav_point_234">10.3.5　处理<code>FileNotFoundError</code>
 异常</h3>
<p>使用文件时，一种常见的问题是找不到文件：查找的文件可能在其他地方，文件名可能不正确，或者这个文件根本就不存在。对于所有这些情形，都可使用<code>try-except</code>
 代码块以直观的方式处理。</p>
<p>我们来尝试读取一个不存在的文件。下面的程序尝试读取文件alice.txt的内容，但该文件没有存储在alice.py所在的目录中：</p>
<p>
<em>alice.py</em>
</p>
<pre class="代码无行号"><code>filename = 'alice.txt'
with open(filename, encoding='utf-8') as f:
    contents = f.read()</code>
</pre>
<p>相比于本章前面的文件打开方式，这里有两个不同之处。一是使用变量<code>f</code>
 来表示文件对象，这是一种常见的做法。二是给参数<code>encoding</code>
 指定了值，在系统的默认编码与要读取文件使用的编码不一致时，必须这样做。</p>
<p>Python无法读取不存在的文件，因此它引发一个异常：</p>
<pre class="代码无行号"><code>Traceback (most recent call last):
  File "alice.py", line 3, in &lt;module&gt;
    with open(filename, encoding='utf-8') as f:
FileNotFoundError: [Errno 2] No such file or directory: 'alice.txt'</code>
</pre>
<p>上述traceback的最后一行报告了<code>FileNotFoundError</code>
 异常，这是Python找不到要打开的文件时创建的异常。在本例中，这个错误是函数<code>open()</code>
 导致的。因此，要处理这个错误，必须将<code>try</code>
 语句放在包含<code>open()</code>
 的代码行之前：</p>
<pre class="代码无行号"><code>filename = 'alice.txt'
try:
    with open(filename, encoding='utf-8') as f:
        contents = f.read()
except FileNotFoundError:
    print(f"Sorry, the file {filename} does not exist.")</code>
</pre>
<p>在本例中，<code>try</code>
 代码块引发了<code>FileNotFoundError</code>
 异常，因此Python找到与该错误匹配的<code>except</code>
 代码块，并运行其中的代码。最终的结果是显示一条友好的错误消息，而不是traceback：</p>
<pre class="代码无行号"><code>Sorry, the file alice.txt does not exist.</code>
</pre>
<p>如果文件不存在，这个程序就什么都做不了，错误处理代码也意义不大。下面来扩展这个示例，看看在你使用多个文件时，异常处理可提供什么样的帮助。</p>
<h3 id="nav_point_235">10.3.6　分析文本</h3>
<p>你可以分析包含整本书的文本文件。很多经典文学作品都是简单以文本文件的形式提供的，因为它们不受版权限制。本节使用的文本来自古登堡计划，该计划提供了一系列不受版权限制的文学作品。如果你要在编程项目中使用文学文本，这是一个很不错的资源。</p>
<p>下面来提取童话《爱丽丝漫游奇境记》（<em>Alice in Wonderland</em>
 ）的文本，并尝试计算它包含多少个单词。我们将使用方法<code>split()</code>
 ，它能根据一个字符串创建一个单词列表。下面是对只包含童话名<code>"Alice in Wonderland"</code>
 的字符串调用方法<code>split()</code>
 的结果：</p>
<pre class="代码无行号"><code>&gt;&gt;&gt; title = "Alice in Wonderland"
&gt;&gt;&gt; title.split()
['Alice', 'in', 'Wonderland']</code>
</pre>
<p>方法<code>split()</code>
 以空格为分隔符将字符串分拆成多个部分，并将这些部分都存储到一个列表中。结果是一个包含字符串中所有单词的列表，虽然有些单词可能包含标点。为计算《爱丽丝漫游奇境记》包含多少个单词，我们将对整篇小说调用<code>split()</code>
 ，再计算得到的列表包含多少个元素，从而确定整篇童话大致包含多少个单词：</p>
<pre class="代码无行号"><code>  filename = 'alice.txt'
  try:
      with open(filename, encoding='utf-8') as f:
          contents = f.read()
  except FileNotFoundError:
      print(f"Sorry, the file {filename} does not exist.")
  else:
      # 计算该文件大致包含多少个单词。
❶     words = contents.split()
❷     num_words = len(words)
❸     print(f"The file {filename} has about {num_words} words.")</code>
</pre>
<p>我们将文件alice.txt移到了正确的目录中，让<code>try</code>
 代码块能够成功执行。在❶处，对变量<code>contents</code>
 （它现在是一个长长的字符串，包含童话《爱丽丝漫游奇境记》的全部文本）调用方法<code>split()</code>
 ，以生成一个列表，其中包含这部童话中的所有单词。使用<code>len()</code>
 来确定这个列表的长度时，就能知道原始字符串大致包含多少个单词了（见❷）。在❸处，打印一条消息，指出文件包含多少个单词。这些代码都放在<code>else</code>
 代码块中，因为仅当<code>try</code>
 代码块成功执行时才执行它们。输出指出了文件alice.txt包含多少个单词：</p>
<pre class="代码无行号"><code>The file alice.txt has about 29465 words.</code>
</pre>
<p>这个数稍大一点，因为使用的文本文件包含出版商提供的额外信息，但还是成功估算出了童话《爱丽丝漫游奇境记》的篇幅。</p>
<h3 id="nav_point_236">10.3.7　使用多个文件</h3>
<p>下面多分析几本书。这此之前，先将这个程序的大部分代码移到一个名为<code>count_words()</code>
 的函数中。这样，对多本书进行分析时将更容易：</p>
<p>
<em>word_count.py</em>
</p>
<pre class="代码无行号"><code>  def count_words(filename):
❶     """计算一个文件大致包含多少个单词。"""
      try:
          with open(filename, encoding='utf-8') as f:
              contents = f.read()
      except FileNotFoundError:
          print(f"Sorry, the file {filename} does not exist.")
      else:
          words = contents.split()
          num_words = len(words)
          print(f"The file {filename} has about {num_words} words.")
  filename = 'alice.txt'
  count_words(filename)</code>
</pre>
<p>这些代码大多与原来一样，只是移到了函数<code>count_words()</code>
 中，并增加了缩进量。修改程序的同时更新注释是个不错的习惯，因此我们将注释改成文档字符串，并稍微调整了一下措辞（见❶）。</p>
<p>现在可以编写一个简单的循环，计算要分析的任何文本包含多少个单词了。为此，将要分析的文件的名称存储在一个列表中，然后对列表中的每个文件调用<code>count_words()</code>
 。我们将尝试计算《爱丽丝漫游奇境记》《悉达多》（<em>Siddhartha</em>
 ）、《白鲸》（<em>Moby Dick</em>
 ）和《小妇人》（<em>Little Women</em>
 ）分别包含多少个单词，它们都不受版权限制。我故意没有将siddhartha.txt放到word_count.py所在的目录中，从而展示该程序在文件不存在时应对得有多出色：</p>
<pre class="代码无行号"><code>def count_words(filename):
    --snip--
filenames = ['alice.txt', 'siddhartha.txt', 'moby_dick.txt', 'little_women.txt']
for filename in filenames:
    count_words(filename)</code>
</pre>
<p>文件siddhartha.txt不存在，但这丝毫不影响该程序处理其他文件：</p>
<pre class="代码无行号"><code>The file alice.txt has about 29465 words.
Sorry, the file siddhartha.txt does not exist.
The file moby_dick.txt has about 215830 words.
The file little_women.txt has about 189079 words.</code>
</pre>
<p>在本例中，使用<code>try-except</code>
 代码块提供了两个重要的优点：避免用户看到traceback，以及让程序继续分析能够找到的其他文件。如果不捕获因找不到siddhartha.txt而引发的<code>FileNotFoundError</code>
 异常，用户将看到完整的traceback，而程序将在尝试分析《悉达多》后停止运行。它根本不会分析《白鲸》和《小妇人》。</p>
<h3 id="nav_point_237">10.3.8　静默失败</h3>
<p>在前一个示例中，我们告诉用户有一个文件找不到。但并非每次捕获到异常都需要告诉用户，有时候你希望程序在发生异常时保持静默，就像什么都没有发生一样继续运行。要让程序静默失败，可像通常那样编写<code>try</code>
 代码块，但在<code>except</code>
 代码块中明确地告诉Python什么都不要做。Python有一个<code>pass</code>
 语句，可用于让Python在代码块中什么都不要做：</p>
<pre class="代码无行号"><code>  def count_words(filename):
      """计算一个文件大致包含多少个单词。"""
      try:
          --snip--
      except FileNotFoundError:
❶         pass
      else:
          --snip--
  filenames = ['alice.txt', 'siddhartha.txt', 'moby_dick.txt', 'little_women.txt']
  for filename in filenames:
      count_words(filename)</code>
</pre>
<p>相比于前一个程序，这个程序唯一的不同之处是❶处的<code>pass</code>
 语句。现在，出现<code>FileNotFoundError</code>
 异常时，将执行<code>except</code>
 代码块中的代码，但什么都不会发生。这种错误发生时，不会出现traceback，也没有任何输出。用户将看到存在的每个文件包含多少个单词，但没有任何迹象表明有一个文件未找到：</p>
<pre class="代码无行号"><code>The file alice.txt has about 29465 words.
The file moby_dick.txt has about 215830 words.
The file little_women.txt has about 189079 words.</code>
</pre>
<p>
<code>pass</code>
 语句还充当了占位符，提醒你在程序的某个地方什么都没有做，并且以后也许要在这里做些什么。例如，在这个程序中，我们可能决定将找不到的文件的名称写入文件missing_files.txt中。用户看不到这个文件，但我们可以读取它，进而处理所有找不到文件的问题。</p>
<h3 id="nav_point_238">10.3.9　决定报告哪些错误</h3>
<p>该在什么情况下向用户报告错误？又该在什么情况下静默失败呢？如果用户知道要分析哪些文件，他们可能希望在有文件却没有分析时出现一条消息来告知原因。如果用户只想看到结果，并不知道要分析哪些文件，可能就无须在有些文件不存在时告知他们。向用户显示他不想看到的信息可能会降低程序的可用性。Python的错误处理结构让你能够细致地控制与用户分享错误信息的程度，要分享多少信息由你决定。</p>
<p>编写得很好且经过详尽测试的代码不容易出现内部错误，如语法或逻辑错误，但只要程序依赖于外部因素，如用户输入、存在指定的文件、有网络链接，就有可能出现异常。凭借经验可判断该在程序的什么地方包含异常处理块，以及出现错误时该向用户提供多少相关的信息。</p>
<blockquote>
<p>
<strong>动手试一试</strong>
</p>
<p>
<strong>练习10-6：加法运算</strong>
 　提示用户提供数值输入时，常出现的一个问题是，用户提供的是文本而不是数。在此情况下，当你尝试将输入转换为整数时，将引发<code>ValueError</code>
 异常。编写一个程序，提示用户输入两个数，再将其相加并打印结果。在用户输入的任何一个值不是数时都捕获<code>ValueError</code>
 异常，并打印一条友好的错误消息。对你编写的程序进行测试：先输入两个数，再输入一些文本而不是数。</p>
<p>
<strong>练习10-7：加法计算器</strong>
 　将为完成练习10-6而编写的代码放在一个<code>while</code>
 循环中，让用户犯错（输入的是文本而不是数）后能够继续输入数。</p>
<p>
<strong>练习10-8：猫和狗</strong>
 　创建文件cats.txt和dogs.txt，在第一个文件中至少存储三只猫的名字，在第二个文件中至少存储三条狗的名字。编写一个程序，尝试读取这些文件，并将其内容打印到屏幕上。将这些代码放在一个<code>try-except</code>
 代码块中，以便在文件不存在时捕获<code>FileNotFound</code>
 错误，并显示一条友好的消息。将任意一个文件移到另一个地方，并确认<code>except</code>
 代码块中的代码将正确执行。</p>
<p>
<strong>练习10-9：静默的猫和狗</strong>
 　修改你在练习10-8中编写的<code>except</code>
 代码块，让程序在任意文件不存在时静默失败。</p>
<p>
<strong>练习10-10：常见单词</strong>
 　访问古登堡计划，找一些你想分析的图书。下载这些作品的文本文件或将浏览器中的原始文本复制到文本文件中。</p>
<p>可以使用方法<code>count()</code>
 来确定特定的单词或短语在字符串中出现了多少次。例如，下面的代码计算<code>'row'</code>
 在一个字符串中出现了多少次：</p>
<pre class="代码无行号"><code>&gt;&gt;&gt; line = "Row, row, row your boat"
&gt;&gt;&gt; line.count('row')
2
&gt;&gt;&gt; line.lower().count('row')
3</code>
</pre>
<p>请注意，通过使用<code>lower()</code>
 将字符串转换为小写，可捕捉要查找单词的所有格式，而不管其大小写如何。</p>
<p>编写一个程序，它读取你在古登堡计划中获取的文件，并计算单词<code>'the'</code>
 在每个文件中分别出现了多少次。这里计算得到的结果并不准确，因为将诸如<code>'then'</code>
 和<code>'there'</code>
 等单词也计算在内了。请尝试计算<code>'the '</code>
 （包含空格）出现的次数，看看结果相差多少。</p>
</blockquote>
<h2 id="nav_point_239">10.4　存储数据</h2>
<p>很多程序都要求用户输入某种信息，如让用户存储游戏首选项或提供要可视化的数据。不管关注点是什么，程序都把用户提供的信息存储在列表和字典等数据结构中。用户关闭程序时，几乎总是要保存他们提供的信息。一种简单的方式是使用模块<code>json</code>
 来存储数据。</p>
<p>模块<code>json</code>
 让你能够将简单的Python数据结构转储到文件中，并在程序再次运行时加载该文件中的数据。你还可以使用<code>json</code>
 在Python程序之间分享数据。更重要的是，JSON数据格式并非Python专用的，这让你能够将以JSON格式存储的数据与使用其他编程语言的人分享。这是一种轻便而有用的格式，也易于学习。</p>
<blockquote>
<p>
<strong>注意</strong>
 　JSON（JavaScript Object Notation）格式最初是为JavaScript开发的，但随后成了一种常见格式，被包括Python在内的众多语言采用。</p>
</blockquote>
<h3 id="nav_point_240">10.4.1　使用<code>json.dump()</code>
 和<code>json.load()</code>
</h3>
<p>我们来编写一个存储一组数的简短程序，再编写一个将这些数读取到内存中的程序。第一个程序将使用<code>json.dump()</code>
 来存储这组数，而第二个程序将使用<code>json.load()</code>
 。</p>
<p>函数<code>json.dump()</code>
 接受两个实参：要存储的数据，以及可用于存储数据的文件对象。下面演示了如何使用<code>json.dump()</code>
 来存储数字列表：</p>
<p>
<em>number_writer.py</em>
</p>
<pre class="代码无行号"><code>  import json
  numbers = [2, 3, 5, 7, 11, 13]
❶ filename = 'numbers.json'
❷ with open(filename, 'w') as f:
❸     json.dump(numbers, f)</code>
</pre>
<p>先导入模块<code>json</code>
 ，再创建一个数字列表。在❶处，指定了要将该数字列表存储到哪个文件中。通常使用文件扩展名.json来指出文件存储的数据为JSON格式。接下来，以写入模式打开这个文件，让<code>json</code>
 能够将数据写入其中（见❷）。在❸处，使用函数<code>json.dump()</code>
 将数字列表存储到文件numbers.json中。</p>
<p>这个程序没有输出，但可以打开文件numbers.json来看看内容。数据的存储格式与Python中一样：</p>
<pre class="代码无行号"><code>[2, 3, 5, 7, 11, 13]</code>
</pre>
<p>下面再编写一个程序，使用<code>json.load()</code>
 将列表读取到内存中：</p>
<p>
<em>number_reader.py</em>
</p>
<pre class="代码无行号"><code>  import json
❶ filename = 'numbers.json'
❷ with open(filename) as f:
❸     numbers = json.load(f)
  print(numbers)</code>
</pre>
<p>在❶处，确保读取的是前面写入的文件。这次以读取方式打开该文件，因为Python只需要读取它（见❷）。在❸处，使用函数<code>json.load()</code>
 加载存储在numbers.json中的信息，并将其赋给变量<code>numbers</code>
 。最后，打印恢复的数字列表，看看是否与number_writer.py中创建的数字列表相同：</p>
<pre class="代码无行号"><code>[2, 3, 5, 7, 11, 13]</code>
</pre>
<p>这是一种在程序之间共享数据的简单方式。</p>
<h3 id="nav_point_241">10.4.2　保存和读取用户生成的数据</h3>
<p>使用<code>json</code>
 保存用户生成的数据大有裨益，因为如果不以某种方式存储，用户的信息会在程序停止运行时丢失。下面来看一个这样的例子：提示用户首次运行程序时输入自己的名字，并在再次运行程序时记住他。</p>
<p>先来存储用户的名字：</p>
<p>
<em>remember_me.py</em>
</p>
<pre class="代码无行号"><code>  import json
❶ username = input("What is your name? ")
  filename = 'username.json'
  with open(filename, 'w') as f:
❷     json.dump(username, f)
❸     print(f"We'll remember you when you come back, {username}!")</code>
</pre>
<p>在❶处，提示输入用户名并将其赋给一个变量。接下来，调用<code>json.dump()</code>
 ，并将用户名和一个文件对象传递给它，从而将用户名存储到文件中（见❷）。然后，打印一条消息，指出存储了用户输入的信息（见❸）：</p>
<pre class="代码无行号"><code>What is your name? Eric
We'll remember you when you come back, Eric!</code>
</pre>
<p>现在再编写一个程序，向已存储了名字的用户发出问候：</p>
<p>
<em>greet_user.py</em>
</p>
<pre class="代码无行号"><code>  import json
  filename = 'username.json'
  with open(filename) as f:
❶     username = json.load(f)
❷     print(f"Welcome back, {username}!")</code>
</pre>
<p>在❶处，使用<code>json.load()</code>
 将存储在username.json中的信息读取到变量<code>username</code>
 中。恢复用户名后，就可以欢迎用户回来了（见❷）：</p>
<pre class="代码无行号"><code>Welcome back, Eric!</code>
</pre>
<p>需要将这两个程序合并到一个程序（remember_me.py）中。这个程序运行时，将尝试从文件username.json中获取用户名。因此，首先编写一个尝试恢复用户名的<code>try</code>
 代码块。如果这个文件不存在，就在<code>except</code>
 代码块中提示用户输入用户名，并将其存储到username.json中，以便程序再次运行时能够获取：</p>
<p>
<em>remember_me.py</em>
</p>
<pre class="代码无行号"><code>  import json
  # 如果以前存储了用户名，就加载它。
  # 否则，提示用户输入用户名并存储它。
  filename = 'username.json'
  try:
❶     with open(filename) as f:
❷         username = json.load(f)
❸ except FileNotFoundError:
❹     username = input("What is your name? ")
❺     with open(filename, 'w') as f:
          json.dump(username, f)
          print(f"We'll remember you when you come back, {username}!")
  else:
      print(f"Welcome back, {username}!")</code>
</pre>
<p>这里没有任何新代码，只是将前两个示例的代码合并到了一个程序中。在❶处，尝试打开文件username.json。如果该文件存在，就将其中的用户名读取到内存中（见❷），再执行<code>else</code>
 代码块，打印一条欢迎用户回来的消息。用户首次运行该程序时，文件username.json不存在，将引发<code>FileNotFoundError</code>
 异常（见❸）。因此Python将执行<code>except</code>
 代码块，提示用户输入用户名（见❹），再使用<code>json.dump()</code>
 存储该用户名并打印一句问候语（见❺）。</p>
<p>无论执行的是<code>except</code>
 还是<code>else</code>
 代码块，都将显示用户名和合适的问候语。如果这个程序是首次运行，输出将如下：</p>
<pre class="代码无行号"><code>What is your name? Eric
We'll remember you when you come back, Eric!</code>
</pre>
<p>否则，输出将如下：</p>
<pre class="代码无行号"><code>Welcome back, Eric!</code>
</pre>
<p>这是程序之前至少运行了一次时的输出。</p>
<h3 id="nav_point_242">10.4.3　重构</h3>
<p>你经常会遇到这样的情况：代码能够正确地运行，但通过将其划分为一系列完成具体工作的函数，还可以改进。这样的过程称为<strong>重构</strong>
 。重构让代码更清晰、更易于理解、更容易扩展。</p>
<p>要重构remember_me.py，可将其大部分逻辑放到一个或多个函数中。remember_me.py的重点是问候用户，因此将其所有代码都放到一个名为<code>greet_user()</code>
 的函数中：</p>
<p>
<em>remember_me.py</em>
</p>
<pre class="代码无行号"><code>  import json
  def greet_user():
❶     """问候用户，并指出其名字。"""
      filename = 'username.json'
      try:
          with open(filename) as f:
              username = json.load(f)
      except FileNotFoundError:
          username = input("What is your name? ")
          with open(filename, 'w') as f:
              json.dump(username, f)
              print(f"We'll remember you when you come back, {username}!")
      else:
          print(f"Welcome back, {username}!")
  greet_user()</code>
</pre>
<p>考虑到现在使用了一个函数，我们删除原注释，转而使用一个文档字符串来指出程序的作用（见❶）。这个程序更加清晰，但函数<code>greet_user()</code>
 所做的不仅仅是问候用户，还在存储了用户名时获取它、在没有存储用户名时提示用户输入。</p>
<p>下面来重构<code>greet_user()</code>
 ，减少其任务。为此，首先将获取已存储用户名的代码移到另一个函数中：</p>
<pre class="代码无行号"><code>  import json
  def get_stored_username():
❶     """如果存储了用户名，就获取它。"""
      filename = 'username.json'
      try:
          with open(filename) as f:
              username = json.load(f)
      except FileNotFoundError:
❷         return None
      else:
          return username
  def greet_user():
      """问候用户，并指出其名字。"""
      username = get_stored_username()
❸     if username:
          print(f"Welcome back, {username}!")
      else:
          username = input("What is your name? ")
          filename = 'username.json'
          with open(filename, 'w') as f:
              json.dump(username, f)
              print(f"We'll remember you when you come back, {username}!")
  greet_user()</code>
</pre>
<p>新增的函数<code>get_stored_username()</code>
 目标明确，❶处的文档字符串指出了这一点。如果存储了用户名，该函数就获取并返回它；如果文件username.json不存在，该函数就返回<code>None</code>
 （见❷）。这是一种不错的做法：函数要么返回预期的值，要么返回<code>None</code>
 。这让我们能够使用函数的返回值做简单的测试。在❸处，如果成功地获取了用户名，就打印一条欢迎用户回来的消息，否则提示用户输入用户名。</p>
<p>还需要重构<code>greet_user()</code>
 中的另一个代码块，将没有存储用户名时提示用户输入的代码放在一个独立的函数中：</p>
<pre class="代码无行号"><code>import json
def get_stored_username():
    """如果存储了用户名，就获取它。"""
    --snip--
def get_new_username():
    """提示用户输入用户名。"""
    username = input("What is your name? ")
    filename = 'username.json'
    with open(filename, 'w') as f:
        json.dump(username, f)
    return username
def greet_user():
    """问候用户，并指出其名字。"""
    username = get_stored_username()
    if username:
        print(f"Welcome back, {username}!")
    else:
        username = get_new_username()
        print(f"We'll remember you when you come back, {username}!")
greet_user()</code>
</pre>
<p>在remember_me.py的这个最终版本中，每个函数都执行单一而清晰的任务。我们调用<code>greet_user()</code>
 ，它打印一条合适的消息：要么欢迎老用户回来，要么问候新用户。为此，它首先调用<code>get_stored_username()</code>
 ，该函数只负责获取已存储的用户名（如果存储了的话）。最后在必要时调用<code>get_new_username()</code>
 ，该函数只负责获取并存储新用户的用户名。要编写出清晰而易于维护和扩展的代码，这种划分必不可少。</p>
<blockquote>
<p>
<strong>动手试一试</strong>
</p>
<p>
<strong>练习10-11：喜欢的数</strong>
 　编写一个程序，提示用户输入喜欢的数，并使用<code>json.dump()</code>
 将这个数存储到文件中。再编写一个程序，从文件中读取这个值，并打印如下所示的消息。</p>
<p>I know your favorite number! It's _____.</p>
<p>
<strong>练习10-12：记住喜欢的数</strong>
 　将练习10-11中的程序合二为一。如果存储了用户喜欢的数，就向用户显示它，否则提示用户输入喜欢的数并将其存储到文件中。运行这个程序两次，看看它能否像预期的那样工作。</p>
<p>
<strong>练习10-13：验证用户</strong>
 　最后一个remember_me.py版本假设用户要么已输入用户名，要么是首次运行该程序。我们应该修改这个程序，以防当前用户并非上次运行该程序的用户。</p>
<p>为此，在<code>greet_user()</code>
 中打印欢迎用户回来的消息前，询问他用户名是否正确。如果不对，就调用<code>get_new_username()</code>
 让用户输入正确的用户名。</p>
</blockquote>
<h2 id="nav_point_243">10.5　小结</h2>
<p>在本章中，你学习了：如何使用文件；如何一次性读取整个文件，以及如何以每次一行的方式读取文件的内容；如何写入文件，以及如何将文本附加到文件末尾；什么是异常以及如何处理程序可能引发的异常；如何存储Python数据结构，以保存用户提供的信息，避免用户每次运行程序时都需要重新提供。</p>
<p>在第11章中，你将学习高效的代码测试方式。这可帮助你确定代码正确无误，以及发现扩展现有程序时可能引入的bug。</p>
<br style="page-break-after:always"/>
<div></div>
]]></description>
    <pubDate>Fri, 17 Jul 2026 01:45:50 +0000</pubDate>
    <guid isPermaLink="false">10</guid>
</item>
<item>
    <title>Basic Python - 009 - 第 9 章 类.epub</title>
    <description><![CDATA[<p style='color: #888;'>⏱ 预计阅读时间：128 分钟 (25418 字)</p><hr><h1 id="nav_point_190">第 9 章　类</h1>
<blockquote>
<p><img alt="" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/9_Image00001.gif" style="width: 16%" width="16%"/>
 　<strong>面向对象编程</strong>
 是最有效的软件编写方法之一。在面向对象编程中，你编写表示现实世界中的事物和情景的类，并基于这些类来创建对象。编写类时，你定义一大类对象都有的通用行为。基于类创建<strong>对象</strong>
 时，每个对象都自动具备这种通用行为，然后可根据需要赋予每个对象独特的个性。使用面向对象编程可模拟现实情景，其逼真程度达到了令人惊讶的地步。</p>
<p>根据类来创建对象称为<strong>实例化</strong>
 ，这让你能够使用类的实例。在本章中，你将编写一些类并创建其实例。你将指定可在实例中存储什么信息，定义可对这些实例执行哪些操作。你还将编写一些类来扩展既有类的功能，让相似的类能够高效地共享代码。你将把自己编写的类存储在模块中，并在自己的程序文件中导入其他程序员编写的类。</p>
<p>理解面向对象编程有助于你像程序员那样看世界，还可以帮助你真正明白自己编写的代码：不仅是各行代码的作用，还有代码背后更宏大的概念。了解类背后的概念可培养逻辑思维，让你能够通过编写程序来解决遇到的几乎任何问题。</p>
<p>随着面临的挑战日益严峻，类还能让你以及与你合作的其他程序员的生活更轻松。如果你与其他程序员基于同样的逻辑来编写代码，你们就能明白对方所做的工作。你编写的程序将能被众多合作者所理解，每个人都能事半功倍。</p>
</blockquote>
<h2 id="nav_point_191">9.1　创建和使用类</h2>
<p>使用类几乎可以模拟任何东西。下面来编写一个表示小狗的简单类<code>Dog</code>
 ，它表示的不是特定的小狗，而是任何小狗。对于大多数宠物狗，我们都知道些什么呢？它们都有名字和年龄。我们还知道，大多数小狗还会蹲下和打滚。由于大多数小狗都具备上述两项信息（名字和年龄）和两种行为（蹲下和打滚），我们的<code>Dog</code>
 类将包含它们。这个类让Python知道如何创建表示小狗的对象。编写这个类后，我们将使用它来创建表示特定小狗的实例。</p>
<h3 id="nav_point_192">9.1.1　创建<code>Dog</code>
 类</h3>
<p>根据<code>Dog</code>
 类创建的每个实例都将存储名字和年龄，我们赋予了每条小狗蹲下（<code>sit()</code>
 ）和打滚（<code>roll_over()</code>
 ）的能力：</p>
<p>
<em>dog.py</em>
</p>
<pre class="代码无行号"><code>❶ class Dog:
❷     """一次模拟小狗的简单尝试。"""
❸     def __init__(self, name, age):
          """初始化属性name和age。"""
❹         self.name = name
          self.age = age
❺     def sit(self):
          """模拟小狗收到命令时蹲下。"""
          print(f"{self.name} is now sitting.")
      def roll_over(self):
          """模拟小狗收到命令时打滚。"""
          print(f"{self.name} rolled over!")</code>
</pre>
<p>这里需要注意的地方很多，但也不用担心，本章充斥着这样的结构，你有大把的机会熟悉它。❶处定义了一个名为<code>Dog</code>
 的类。根据约定，在Python中，首字母大写的名称指的是类。这个类定义中没有圆括号，因为要从空白创建这个类。❷处编写了一个文档字符串，对这个类的功能做了描述。</p>
<p>
<strong>方法<code>__init__()</code>
</strong>
</p>
<p>类中的函数称为<strong>方法</strong>
 。你在前面学到的有关函数的一切都适用于方法，就目前而言，唯一重要的差别是调用方法的方式。❸处的方法<code>__init__()</code>
 是一个特殊方法，每当你根据<code>Dog</code>
 类创建新实例时，Python都会自动运行它。在这个方法的名称中，开头和末尾各有两个下划线，这是一种约定，旨在避免Python默认方法与普通方法发生名称冲突。务必确保<code>__init__()</code>
 的两边都有两个下划线，否则当你使用类来创建实例时，将不会自动调用这个方法，进而引发难以发现的错误。</p>
<p>我们将方法<code>__init__()</code>
 定义成包含三个形参：<code>self</code>
 、<code>name</code>
 和<code>age</code>
 。在这个方法的定义中，形参<code>self</code>
 必不可少，而且必须位于其他形参的前面。为何必须在方法定义中包含形参<code>self</code>
 呢？因为Python调用这个方法来创建<code>Dog</code>
 实例时，将自动传入实参<code>self</code>
 。每个与实例相关联的方法调用都自动传递实参<code>self</code>
 ，它是一个指向实例本身的引用，让实例能够访问类中的属性和方法。创建<code>Dog</code>
 实例时，Python将调用<code>Dog</code>
 类的方法<code>__init__()</code>
 。我们将通过实参向<code>Dog()</code>
 传递名字和年龄，<code>self</code>
 会自动传递，因此不需要传递它。每当根据<code>Dog</code>
 类创建实例时，都只需给最后两个形参（<code>name</code>
 和<code>age</code>
 ）提供值。</p>
<p>❹处定义的两个变量都有前缀<code>self</code>
 。以<code>self</code>
 为前缀的变量可供类中的所有方法使用，可以通过类的任何实例来访问。<code>self.name = name</code>
 获取与形参<code>name</code>
 相关联的值，并将其赋给变量<code>name</code>
 ，然后该变量被关联到当前创建的实例。<code>self.age = age</code>
 的作用与此类似。像这样可通过实例访问的变量称为<strong>属性</strong>
 。</p>
<p>
<code>Dog</code>
 类还定义了另外两个方法：<code>sit()</code>
 和<code>roll_over()</code>
 （见❺）。这些方法执行时不需要额外的信息，因此它们只有一个形参<code>self</code>
 。我们随后将创建的实例能够访问这些方法，换句话说，它们都会蹲下和打滚。当前，<code>sit()</code>
 和<code>roll_over()</code>
 所做的有限，只是打印一条消息，指出小狗正在蹲下或打滚。但可以扩展这些方法以模拟实际情况：如果这个类包含在一个计算机游戏中，这些方法将包含创建小狗蹲下和打滚动画效果的代码；如果这个类是用于控制机器狗的，这些方法将让机器狗做出蹲下和打滚的动作。</p>
<h3 id="nav_point_193">9.1.2　根据类创建实例</h3>
<p>可将类视为有关如何创建实例的说明。<code>Dog</code>
 类是一系列说明，让Python知道如何创建表示特定小狗的实例。</p>
<p>下面来创建一个表示特定小狗的实例：</p>
<pre class="代码无行号"><code>  class Dog:
      --snip--
❶ my_dog = Dog('Willie', 6)
❷ print(f"My dog's name is {my_dog.name}.")
❸ print(f"My dog is {my_dog.age} years old.")</code>
</pre>
<p>这这里使用的是前一个示例中编写的<code>Dog</code>
 类。在❶处，让Python创建一条名字为<code>'Willie'</code>
 、年龄为<code>6</code>
 的小狗。遇到这行代码时，Python使用实参<code>'Willie'</code>
 和<code>6</code>
 调用<code>Dog</code>
 类的方法<code>__init__()</code>
 。方法<code>__init__()</code>
 创建一个表示特定小狗的实例，并使用提供的值来设置属性<code>name</code>
 和<code>age</code>
 。接下来，Python返回一个表示这条小狗的实例，而我们将这个实例赋给了变量<code>my_dog</code>
 。在这里，命名约定很有用：通常可认为首字母大写的名称（如<code>Dog</code>
 ）指的是类，而小写的名称（如<code>my_dog</code>
 ）指的是根据类创建的实例。</p>
<ol>
<li value="1">
<p>
<strong>访问属性</strong>
</p>
<p>要访问实例的属性，可使用句点表示法。❷处编写了如下代码来访问<code>my_dog</code>
 的属性<code>name</code>
 的值：</p>
<pre class="代码无行号"><code>my_dog.name</code>
</pre>
<p>句点表示法在Python中很常用，这种语法演示了Python如何获悉属性的值。在这里， Python先找到实例<code>my_dog</code>
 ，再查找与该实例相关联的属性<code>name</code>
 。在<code>Dog</code>
 类中引用这个属性时，使用的是<code>self.name</code>
 。在❸处，使用同样的方法来获取属性<code>age</code>
 的值。</p>
<p>输出是有关<code>my_dog</code>
 的摘要：</p>
<pre class="代码无行号"><code>My dog's name is Willie.
My dog is 6 years old.</code>
</pre>
<p> </p>
</li>
<li value="2">
<p>
<strong>调用方法</strong>
</p>
<p>根据<code>Dog</code>
 类创建实例后，就能使用句点表示法来调用<code>Dog</code>
 类中定义的任何方法了。下面来让小狗蹲下和打滚：</p>
<pre class="代码无行号"><code>class Dog:
    --snip--
my_dog = Dog('Willie', 6)
my_dog.sit()
my_dog.roll_over()</code>
</pre>
<p>要调用方法，可指定实例的名称（这里是<code>my_dog</code>
 ）和要调用的方法，并用句点分隔。遇到代码<code>my_dog.sit()</code>
 时，Python在类<code>Dog</code>
 中查找方法<code>sit()</code>
 并运行其代码。Python以同样的方式解读代码<code>my_dog.roll_over()</code>
 。</p>
<p>Willie按我们的命令做了：</p>
<pre class="代码无行号"><code>Willie is now sitting.
Willie rolled over!</code>
</pre>
<p>这种语法很有用。如果给属性和方法指定了合适的描述性名称，如<code>name</code>
 、<code>age</code>
 、<code>sit()</code>
 和<code>roll_over()</code>
 ，即便是从未见过的代码块，我们也能够轻松地推断出它是做什么的。<br/>
 </p>
</li>
<li value="3">
<p>
<strong>创建多个实例</strong>
</p>
<p>可按需求根据类创建任意数量的实例。下面再创建一个名为<code>your_dog</code>
 的小狗实例：</p>
<pre class="代码无行号"><code>class Dog:
    --snip--
my_dog = Dog('Willie', 6)
your_dog = Dog('Lucy', 3)
print(f"My dog's name is {my_dog.name}.")
print(f"My dog is {my_dog.age} years old.")
my_dog.sit()
print(f"\nYour dog's name is {your_dog.name}.")
print(f"Your dog is {your_dog.age} years old.")
your_dog.sit()</code>
</pre>
<p>在本例中创建了两条小狗，分别名为Willie和Lucy。每条小狗都是一个独立的实例，有自己的一组属性，能够执行相同的操作：</p>
<pre class="代码无行号"><code>My dog's name is Willie.
My dog is 6 years old.
Willie is now sitting.
Your dog's name is Lucy.
Your dog is 3 years old.
Lucy is now sitting.</code>
</pre>
<p>即使给第二条小狗指定同样的名字和年龄，Python依然会根据<code>Dog</code>
 类创建另一个实例。你可按需求根据一个类创建任意数量的实例，条件是将每个实例都存储在不同的变量中，或者占用列表或字典的不同位置。</p>
</li>
</ol>
<blockquote>
<p>
<strong>动手试一试</strong>
</p>
<p>
<strong>练习9-1：餐馆</strong>
 　创建一个名为<code>Restaurant</code>
 的类，为其方法<code>__init__()</code>
 设置属性<code>restaurant_name</code>
 和<code>cuisine_type</code>
 。创建一个名为<code>describe_restaurant()</code>
 的方法和一个名为<code>open_restaurant()</code>
 的方法，前者打印前述两项信息，而后者打印一条消息，指出餐馆正在营业。</p>
<p>根据这个类创建一个名为<code>restaurant</code>
 的实例，分别打印其两个属性，再调用前述两个方法。</p>
<p>
<strong>练习9-2：三家餐馆</strong>
 　根据为完成练习9-1而编写的类创建三个实例，并对每个实例调用方法<code>describe_restaurant()</code>
 。</p>
<p>
<strong>练习9-3：用户</strong>
 　创建一个名为<code>User</code>
 的类，其中包含属性<code>first_name</code>
 和<code>last_name</code>
 ，以及用户简介通常会存储的其他几个属性。在类<code>User</code>
 中定义一个名为<code>describe_user()</code>
 的方法，用于打印用户信息摘要。再定义一个名为<code>greet_user()</code>
 的方法，用于向用户发出个性化的问候。</p>
<p>创建多个表示不同用户的实例，并对每个实例调用上述两个方法。</p>
</blockquote>
<h2 id="nav_point_194">9.2　使用类和实例</h2>
<p>可使用类来模拟现实世界中的很多情景。类编写好后，你的大部分时间将花在根据类创建的实例上。你需要执行的一个重要任务是修改实例的属性。可以直接修改实例的属性，也可以编写方法以特定的方式进行修改。</p>
<h3 id="nav_point_195">9.2.1　<code>Car</code>
 类</h3>
<p>下面来编写一个表示汽车的类。它存储了有关汽车的信息，还有一个汇总这些信息的方法：</p>
<p>
<em>car.py</em>
</p>
<pre class="代码无行号"><code>  class Car:
      """一次模拟汽车的简单尝试。"""
❶     def __init__(self, make, model, year):
          """初始化描述汽车的属性。"""
          self.make = make
          self.model = model
          self.year = year
❷     def get_descriptive_name(self):
          """返回整洁的描述性信息。"""
          long_name = f"{self.year} {self.make} {self.model}"
          return long_name.title()
❸ my_new_car = Car('audi', 'a4', 2019)
  print(my_new_car.get_descriptive_name())</code>
</pre>
<p>在❶处，定义了方法<code>__init__()</code>
 。与前面的<code>Dog</code>
 类中一样，这个方法的第一个形参为<code>self</code>
 。该方法还包含另外三个形参：<code>make</code>
 、<code>model</code>
 和<code>year</code>
 。方法<code>__init__()</code>
 接受这些形参的值，并将它们赋给根据这个类创建的实例的属性。创建新的<code>Car</code>
 实例时，需要指定其制造商、型号和生产年份。</p>
<p>在❷处，定义了一个名为<code>get_descriptive_name()</code>
 的方法。它使用属性<code>year</code>
 、<code>make</code>
 和<code>model</code>
 创建一个对汽车进行描述的字符串，让我们无须分别打印每个属性的值。为在这个方法中访问属性的值，使用了<code>self.make</code>
 、<code>self.model</code>
 和<code>self.year</code>
 。在❸处，根据<code>Car</code>
 类创建了一个实例，并将其赋给变量<code>my_new_car</code>
 。接下来，调用方法<code>get_descriptive_name()</code>
 ，指出我们拥有一辆什么样的汽车：</p>
<pre class="代码无行号"><code>2019 Audi A4</code>
</pre>
<p>为了让这个类更有趣，下面给它添加一个随时间变化的属性，用于存储汽车的总里程。</p>
<h3 id="nav_point_196">9.2.2　给属性指定默认值</h3>
<p>创建实例时，有些属性无须通过形参来定义，可在方法<code>__init__()</code>
 中为其指定默认值。</p>
<p>下面来添加一个名为<code>odometer_reading</code>
 的属性，其初始值总是为0。我们还添加了一个名为<code>read_odometer()</code>
 的方法，用于读取汽车的里程表：</p>
<pre class="代码无行号"><code>  class Car:
      def __init__(self, make, model, year):
          """初始化描述汽车的属性。"""
          self.make = make
          self.model = model
          self.year = year
❶         self.odometer_reading = 0
      def get_descriptive_name(self):
          --snip--
❷     def read_odometer(self):
          """打印一条指出汽车里程的消息。"""
          print(f"This car has {self.odometer_reading} miles on it.")
  my_new_car = Car('audi', 'a4', 2019)
  print(my_new_car.get_descriptive_name())
  my_new_car.read_odometer()</code>
</pre>
<p>现在，当Python调用方法<code>__init__()</code>
 来创建新实例时，将像前一个示例一样以属性的方式存储制造商、型号和生产年份。接下来，Python将创建一个名为<code>odometer_reading</code>
 的属性，并将其初始值设置为0（见❶）。在❷处，定义一个名为<code>read_odometer()</code>
 的方法，让你能够轻松地获悉汽车的里程<span class="注释编号">1</span>
 。</p>
<p class="注释内容">
<span class="注释编号下">1</span>
 此处里程的单位为英里（mile），1英里 ≈ 1.6千米。——编者注</p>
<p>一开始汽车的里程为0：</p>
<pre class="代码无行号"><code>2019 Audi A4
This car has 0 miles on it.</code>
</pre>
<p>出售时里程表读数为0的汽车不多，因此需要一种方式来修改该属性的值。</p>
<h3 id="nav_point_197">9.2.3　修改属性的值</h3>
<p>我们能以三种方式修改属性的值：直接通过实例进行修改，通过方法进行设置，以及通过方法进行递增（增加特定的值）。下面依次介绍这些方式。</p>
<ol>
<li value="1">
<p>
<strong>直接修改属性的值</strong>
</p>
<p>要修改属性的值，最简单的方式是通过实例直接访问它。下面的代码直接将里程表读数设置为23：</p>
<pre class="代码无行号"><code>  class Car:
      --snip--
  my_new_car = Car('audi', 'a4', 2019)
  print(my_new_car.get_descriptive_name())
❶ my_new_car.odometer_reading = 23
  my_new_car.read_odometer()</code>
</pre>
<p>在❶处，使用句点表示法直接访问并设置汽车的属性<code>odometer_reading</code>
 。这行代码让Python在实例<code>my_new_car</code>
 中找到属性<code>odometer_reading</code>
 ，并将其值设置为23：</p>
<pre class="代码无行号"><code>2019 Audi A4
This car has 23 miles on it.</code>
</pre>
<p>有时候需要像这样直接访问属性，但其他时候需要编写对属性进行更新的方法。<br/>
 </p>
</li>
<li value="2">
<p>
<strong>通过方法修改属性的值</strong>
</p>
<p>如果有方法能替你更新属性，将大有裨益。这样就无须直接访问属性，而可将值传递给方法，由它在内部进行更新。</p>
<p>下面的示例演示了一个名为<code>update_odometer()</code>
 的方法：</p>
<pre class="代码无行号"><code>  class Car:
      --snip--
❶     def update_odometer(self, mileage):
          """将里程表读数设置为指定的值。"""
          self.odometer_reading = mileage
  my_new_car = Car('audi', 'a4', 2019)
  print(my_new_car.get_descriptive_name())
❷ my_new_car.update_odometer(23)
  my_new_car.read_odometer()</code>
</pre>
<p>对<code>Car</code>
 类所做的唯一修改是在❶处添加了方法<code>update_odometer()</code>
 。这个方法接受一个里程值，并将其赋给<code>self.odometer_reading</code>
 。在❷处，调用<code>update_odometer()</code>
 ，并向它提供了实参23（该实参对应于方法定义中的形参<code>mileage</code>
 ）。它将里程表读数设置为23，而方法<code>read_odometer()</code>
 打印该读数：</p>
<pre class="代码无行号"><code>2019 Audi A4
This car has 23 miles on it.</code>
</pre>
<p>可对方法<code>update_odometer()</code>
 进行扩展，使其在修改里程表读数时做些额外的工作。下面来添加一些逻辑，禁止任何人将里程表读数往回调：</p>
<pre class="代码无行号"><code>  class Car:
      --snip--
      def update_odometer(self, mileage):
          """
          将里程表读数设置为指定的值。
          禁止将里程表读数往回调。
          """
❶         if mileage &gt;= self.odometer_reading:
              self.odometer_reading = mileage
          else:
❷             print("You can't roll back an odometer!")</code>
</pre>
<p>现在，<code>update_odometer()</code>
 在修改属性前检查指定的读数是否合理。如果新指定的里程（<code>mileage</code>
 ）大于或等于原来的里程（<code>self.odometer_reading</code>
 ），就将里程表读数改为新指定的里程（见❶）；否则发出警告，指出不能将里程表往回调（见❷）。<br/>
 </p>
</li>
<li value="3">
<p>
<strong>通过方法对属性的值进行递增</strong>
</p>
<p>有时候需要将属性值递增特定的量，而不是将其设置为全新的值。假设我们购买了一辆二手车，且从购买到登记期间增加了100英里的里程。下面的方法让我们能够传递这个增量，并相应地增大里程表读数：</p>
<pre class="代码无行号"><code>  class Car:
      --snip--
      def update_odometer(self, mileage):
          --snip--
❶     def increment_odometer(self, miles):
          """将里程表读数增加指定的量。"""
          self.odometer_reading += miles
❷ my_used_car = Car('subaru', 'outback', 2015)
  print(my_used_car.get_descriptive_name())
❸ my_used_car.update_odometer(23_500)
  my_used_car.read_odometer()
❹ my_used_car.increment_odometer(100)
  my_used_car.read_odometer()</code>
</pre>
<p>在❶处，新增的方法<code>increment_odometer()</code>
 接受一个单位为英里的数，并将其加入<code>self.odometer_reading</code>
 中。在❷处，创建一辆二手车<code>my_used_car</code>
 。在❸处，调用方法<code>update_odometer()</code>
 并传入<code>23_500</code>
 ，将这辆二手车的里程表读数设置为23 500。在❹处，调用<code>increment_odometer()</code>
 并传入<code>100</code>
 ，以增加从购买到登记期间行驶的100英里：</p>
<pre class="代码无行号"><code>2015 Subaru Outback
This car has 23500 miles on it.
This car has 23600 miles on it.</code>
</pre>
<p>你可以轻松地修改这个方法，以禁止增量为负值，从而防止有人利用它来回调里程表。</p>
<blockquote>
<p>
<strong>注意</strong>
 　你可以使用类似于上面的方法来控制用户修改属性值（如里程表读数）的方式，但能够访问程序的人都可以通过直接访问属性来将里程表修改为任何值。要确保安全，除了进行类似于前面的基本检查外，还需特别注意细节。</p>
</blockquote>
</li>
</ol>
<p>　</p>
<blockquote>
<p>
<strong>动手试一试</strong>
</p>
<p>
<strong>练习9-4：就餐人数</strong>
 　在为完成练习9-1而编写的程序中，添加一个名为<code>number_served</code>
 的属性，并将其默认值设置为0。根据这个类创建一个名为<code>restaurant</code>
 的实例。打印有多少人在这家餐馆就餐过，然后修改这个值并再次打印它。</p>
<p>添加一个名为<code>set_number_served()</code>
 的方法，让你能够设置就餐人数。调用这个方法并向它传递一个值，然后再次打印这个值。</p>
<p>添加一个名为<code>increment_number_served()</code>
 的方法，让你能够将就餐人数递增。调用这个方法并向它传递一个这样的值：你认为这家餐馆每天可能接待的就餐人数。</p>
<p>
<strong>练习9-5：尝试登录次数</strong>
 　在为完成练习9-3而编写的<code>User</code>
 类中，添加一个名为<code>login_attempts</code>
 的属性。编写一个名为<code>increment_login_attempts()</code>
 的方法，将属性<code>login_attempts</code>
 的值加1。再编写一个名为<code>reset_login_attempts()</code>
 的方法，将属性<code>login_attempts</code>
 的值重置为0。</p>
<p>根据<code>User</code>
 类创建一个实例，再调用方法<code>increment_login_attempts()</code>
 多次。打印属性<code>login_attempts</code>
 的值，确认它被正确地递增。然后，调用方法<code>reset_login_attempts()</code>
 ，并再次打印属性<code>login_attempts</code>
 的值，确认它被重置为0。</p>
</blockquote>
<h2 id="nav_point_198">9.3　继承</h2>
<p>编写类时，并非总是要从空白开始。如果要编写的类是另一个现成类的特殊版本，可使用<strong>继承</strong>
 。一个类<strong>继承</strong>
 另一个类时，将自动获得另一个类的所有属性和方法。原有的类称为<strong>父类</strong>
 ，而新类称为<strong>子类</strong>
 。子类继承了父类的所有属性和方法，同时还可以定义自己的属性和方法。</p>
<h3 id="nav_point_199">9.3.1　子类的方法<code>__init__()</code>
</h3>
<p>在既有类的基础上编写新类时，通常要调用父类的方法<code>__init__()</code>
 。这将初始化在父类<code>__init__()</code>
 方法中定义的所有属性，从而让子类包含这些属性。</p>
<p>例如，下面来模拟电动汽车。电动汽车是一种特殊的汽车，因此可在前面创建的<code>Car</code>
 类的基础上创建新类<code>ElectricCar</code>
 。这样就只需为电动汽车特有的属性和行为编写代码。</p>
<p>下面来创建<code>ElectricCar</code>
 类的一个简单版本，它具备<code>Car</code>
 类的所有功能：</p>
<p>
<em>electric_car.py</em>
</p>
<pre class="代码无行号"><code>❶ class Car:
      """一次模拟汽车的简单尝试。"""
      def __init__(self, make, model, year):
          self.make = make
          self.model = model
          self.year = year
          self.odometer_reading = 0
      def get_descriptive_name(self):
          long_name = f"{self.year} {self.make} {self.model}"
          return long_name.title()
      def read_odometer(self):
          print(f"This car has {self.odometer_reading} miles on it.")
      def update_odometer(self, mileage):
          if mileage &gt;= self.odometer_reading:
              self.odometer_reading = mileage
          else:
              print("You can't roll back an odometer!")
      def increment_odometer(self, miles):
          self.odometer_reading += miles
❷ class ElectricCar(Car):
      """电动汽车的独特之处。"""
❸     def __init__(self, make, model, year):
          """初始化父类的属性。"""
❹         super().__init__(make, model, year)
❺ my_tesla = ElectricCar('tesla', 'model s', 2019)
  print(my_tesla.get_descriptive_name())</code>
</pre>
<p>首先是<code>Car</code>
 类的代码（见❶）。创建子类时，父类必须包含在当前文件中，且位于子类前面。在❷处，定义了子类<code>ElectricCar</code>
 。定义子类时，必须在圆括号内指定父类的名称。方法<code>__init__()</code>
 接受创建<code>Car</code>
 实例所需的信息（见❸）。</p>
<p>❹处的<code>super()</code>
 是一个特殊函数，让你能够调用父类的方法。这行代码让Python调用<code>Car</code>
 类的方法<code>__init__()</code>
 ，让<code>ElectricCar</code>
 实例包含这个方法中定义的所有属性。父类也称为<strong>超类</strong>
 （superclass），名称<code>super</code>
 由此而来。</p>
<p>为测试继承能够正确地发挥作用，我们尝试创建一辆电动汽车，但提供的信息与创建普通汽车时相同。在❺处，创建<code>ElectricCar</code>
 类的一个实例，并将其赋给变量<code>my_tesla</code>
 。这行代码调用<code>ElectricCar</code>
 类中定义的方法<code>__init__()</code>
 ，后者让Python调用父类<code>Car</code>
 中定义的方法<code>__init__()</code>
 。我们提供了实参<code>'tesla'</code>
 、<code>'model s'</code>
 和<code>2019</code>
 。</p>
<p>除方法<code>__init___()</code>
 外，电动汽车没有其他特有的属性和方法。当前，我们只想确认电动汽车具备普通汽车的行为：</p>
<pre class="代码无行号"><code>2019 Tesla Model S</code>
</pre>
<p>
<code>ElectricCar</code>
 实例的行为与<code>Car</code>
 实例一样，现在可以开始定义电动汽车特有的属性和方法了。</p>
<h3 id="nav_point_200">9.3.2　给子类定义属性和方法</h3>
<p>让一个类继承另一个类后，就可以添加区分子类和父类所需的新属性和新方法了。</p>
<p>下面来添加一个电动汽车特有的属性（电瓶），以及一个描述该属性的方法。我们将存储电瓶容量，并编写一个打印电瓶描述的方法：</p>
<pre class="代码无行号"><code>  class Car:
      --snip--
  class ElectricCar(Car):
      """电动汽车的独特之处。"""
      def __init__(self, make, model, year):
          """
          初始化父类的属性。
          再初始化电动汽车特有的属性。
          """
          super().__init__(make, model, year)
❶         self.battery_size = 75
❷     def describe_battery(self):
          """打印一条描述电瓶容量的消息。"""
          print(f"This car has a {self.battery_size}-kWh battery.")
  my_tesla = ElectricCar('tesla', 'model s', 2019)
  print(my_tesla.get_descriptive_name())
  my_tesla.describe_battery()</code>
</pre>
<p>在❶处，添加了新属性<code>self.battery_size</code>
 ，并设置其初始值（<code>75</code>
 ）。根据<code>ElectricCar</code>
 类创建的所有实例都将包含该属性，但所有<code>Car</code>
 实例都不包含它。在❷处，还添加了一个名为<code>describe_battery()</code>
 的方法，打印有关电瓶的信息。调用这个方法时，将看到一条电动汽车特有的描述：</p>
<pre class="代码无行号"><code>2019 Tesla Model S
This car has a 75-kWh battery.</code>
</pre>
<p>对于<code>ElectricCar</code>
 类的特殊程度没有任何限制。模拟电动汽车时，可根据所需的准确程度添加任意数量的属性和方法。如果一个属性或方法是任何汽车都有的，而不是电动汽车特有的，就应将其加入到<code>Car</code>
 类而非<code>ElectricCar</code>
 类中。这样，使用<code>Car</code>
 类的人将获得相应的功能，而<code>ElectricCar</code>
 类只包含处理电动汽车特有属性和行为的代码。</p>
<h3 id="nav_point_201">9.3.3　重写父类的方法</h3>
<p>对于父类的方法，只要它不符合子类模拟的实物的行为，都可以进行重写。为此，可在子类中定义一个与要重写的父类方法同名的方法。这样，Python将不会考虑这个父类方法，而只关注你在子类中定义的相应方法。</p>
<p>假设<code>Car</code>
 类有一个名为<code>fill_gas_tank()</code>
 的方法，它对全电动汽车来说毫无意义，因此你可能想重写它。下面演示了一种重写方式：</p>
<pre class="代码无行号"><code>class ElectricCar(Car):
    --snip--
    def fill_gas_tank(self):
        """电动汽车没有油箱。"""
        print("This car doesn't need a gas tank!")</code>
</pre>
<p>现在，如果有人对电动汽车调用方法<code>fill_gas_tank()</code>
 ，Python将忽略<code>Car</code>
 类中的方法<code>fill_gas_tank()</code>
 ，转而运行上述代码。使用继承时，可让子类保留从父类那里继承而来的精华，并剔除不需要的糟粕。</p>
<h3 id="nav_point_202">9.3.4　将实例用作属性</h3>
<p>使用代码模拟实物时，你可能会发现自己给类添加的细节越来越多：属性和方法清单以及文件都越来越长。在这种情况下，可能需要将类的一部分提取出来，作为一个独立的类。可以将大型类拆分成多个协同工作的小类。</p>
<p>例如，不断给<code>ElectricCar</code>
 类添加细节时，我们可能发现其中包含很多专门针对汽车电瓶的属性和方法。在这种情况下，可将这些属性和方法提取出来，放到一个名为<code>Battery</code>
 的类中，并将一个<code>Battery</code>
 实例作为<code>ElectricCar</code>
 类的属性：</p>
<pre class="代码无行号"><code>  class Car:
      --snip--
❶ class Battery:
      """一次模拟电动汽车电瓶的简单尝试。"""
❷     def __init__(self, battery_size=75):
          """初始化电瓶的属性。"""
          self.battery_size = battery_size
❸     def describe_battery(self):
          """打印一条描述电瓶容量的消息。"""
          print(f"This car has a {self.battery_size}-kWh battery.")
  class ElectricCar(Car):
      """电动汽车的独特之处。"""
      def __init__(self, make, model, year):
          """
          初始化父类的属性。
          再初始化电动汽车特有的属性。
          """
          super().__init__(make, model, year)
❹         self.battery = Battery()
  my_tesla = ElectricCar('tesla', 'model s', 2019)
  print(my_tesla.get_descriptive_name())
  my_tesla.battery.describe_battery()</code>
</pre>
<p>❶处定义一个名为<code>Battery</code>
 的新类，它没有继承任何类。❷处的方法<code>__init__()</code>
 除<code>self</code>
 外，还有另一个形参<code>battery_size</code>
 。这个形参是可选的：如果没有给它提供值，电瓶容量将被设置为75。方法<code>describe_battery()</code>
 也移到了这个类中（见❸）。</p>
<p>在<code>ElectricCar</code>
 类中，添加了一个名为<code>self.battery</code>
 的属性（见❹）。这行代码让Python创建一个新的<code>Battery</code>
 实例（因为没有指定容量，所以为默认值75），并将该实例赋给属性<code>self.battery</code>
 。每当方法<code>__init__()</code>
 被调用时，都将执行该操作，因此现在每个<code>ElectricCar</code>
 实例都包含一个自动创建的<code>Battery</code>
 实例。</p>
<p>我们创建一辆电动汽车，并将其赋给变量<code>my_tesla</code>
 。描述电瓶时，需要使用电动汽车的属性<code>battery</code>
 ：</p>
<pre class="代码无行号"><code>my_tesla.battery.describe_battery()</code>
</pre>
<p>这行代码让Python在实例<code>my_tesla</code>
 中查找属性<code>battery</code>
 ，并对存储在该属性中的<code>Battery</code>
 实例调用方法<code>describe_battery()</code>
 。</p>
<p>输出与你在前面看到的相同：</p>
<pre class="代码无行号"><code>2019 Tesla Model S
This car has a 75-kWh battery.</code>
</pre>
<p>这看似做了很多额外的工作，但是现在想多详细地描述电瓶都可以，且不会导致<code>ElectricCar</code>
 类混乱不堪。下面再给<code>Battery</code>
 类添加一个方法，它根据电瓶容量报告汽车的续航里程：</p>
<pre class="代码无行号"><code>  class Car:
      --snip--
  class Battery:
      --snip--
❶     def get_range(self):
          """打印一条消息，指出电瓶的续航里程。"""
          if self.battery_size == 75:
              range = 260
          elif self.battery_size == 100:
              range = 315
          print(f"This car can go about {range} miles on a full charge.")
  class ElectricCar(Car):
      --snip--
  my_tesla = ElectricCar('tesla', 'model s', 2019)
  print(my_tesla.get_descriptive_name())
  my_tesla.battery.describe_battery()
❷ my_tesla.battery.get_range()</code>
</pre>
<p>❶处新增的方法<code>get_range()</code>
 做了一些简单的分析：如果电瓶的容量为75 kWh，就将续航里程设置为260英里；如果容量为100 kWh，就将续航里程设置为315英里，然后报告这个值。为使用这个方法，也需要通过汽车的属性<code>battery</code>
 来调用（见❷）。</p>
<p>输出指出了汽车的续航里程（这取决于电瓶的容量）：</p>
<pre class="代码无行号"><code>2019 Tesla Model S
This car has a 75-kWh battery.
This car can go about 260 miles on a full charge.</code>
</pre>
<h3 id="nav_point_203">9.3.5　模拟实物</h3>
<p>模拟较复杂的物件（如电动汽车）时，需要解决一些有趣的问题。续航里程是电瓶的属性还是汽车的属性呢？如果只描述一辆汽车，将方法<code>get_range()</code>
 放在<code>Battery</code>
 类中也许是合适的，但如果要描述一家汽车制造商的整个产品线，也许应该将方法<code>get_range()</code>
 移到<code>ElectricCar</code>
 类中。在这种情况下，<code>get_range()</code>
 依然根据电瓶容量来确定续航里程，但报告的是一款汽车的续航里程。也可以这样做：仍将方法<code>get_range()</code>
 留在<code>Battery</code>
 类中，但向它传递一个参数，如<code>car_model</code>
 。在这种情况下，方法<code>get_range()</code>
 将根据电瓶容量和汽车型号报告续航里程。</p>
<p>这让你进入了程序员的另一个境界：解决上述问题时，从较高的逻辑层面（而不是语法层面）考虑；考虑的不是Python，而是如何使用代码来表示实物。达到这种境界后，你会经常发现，对现实世界的建模方法没有对错之分。有些方法的效率更高，但要找出效率最高的表示法，需要经过一定的实践。只要代码像你希望的那样运行，就说明你做得很好！即便发现自己不得不多次尝试使用不同的方法来重写类，也不必气馁。要编写出高效、准确的代码，都得经过这样的过程。</p>
<blockquote>
<p>
<strong>动手试一试</strong>
</p>
<p>
<strong>练习9-6：冰激凌小店</strong>
 　冰激凌小店是一种特殊的餐馆。编写一个名为<code>IceCreamStand</code>
 的类，让它继承为完成练习9-1或练习9-4而编写的<code>Restaurant</code>
 类。这两个版本的<code>Restaurant</code>
 类都可以，挑选你更喜欢的那个即可。添加一个名为<code>flavors</code>
 的属性，用于存储一个由各种口味的冰激凌组成的列表。编写一个显示这些冰激凌的方法。创建一个<code>IceCreamStand</code>
 实例，并调用这个方法。</p>
<p>
<strong>练习9-7：管理员</strong>
 　管理员是一种特殊的用户。编写一个名为<code>Admin</code>
 的类，让它继承为完成练习9-3或练习9-5而编写的<code>User</code>
 类。添加一个名为<code>privileges</code>
 的属性，用于存储一个由字符串（如<code>"can add post"</code>
 、<code>"can delete post"</code>
 、<code>"can ban user"</code>
 等）组成的列表。编写一个名为<code>show_privileges()</code>
 的方法，显示管理员的权限。创建一个<code>Admin</code>
 实例，并调用这个方法。</p>
<p>
<strong>练习9-8：权限</strong>
 　编写一个名为<code>Privileges</code>
 的类，它只有一个属性<code>privileges</code>
 ，其中存储了练习9-7所述的字符串列表。将方法<code>show_privileges()</code>
 移到这个类中。在<code>Admin</code>
 类中，将一个<code>Privileges</code>
 实例用作其属性。创建一个<code>Admin</code>
 实例，并使用方法<code>show_privileges()</code>
 来显示其权限。</p>
<p>
<strong>练习9-9：电瓶升级</strong>
 　在本节最后一个electric_car.py版本中，给<code>Battery</code>
 类添加一个名为<code>upgrade_battery()</code>
 的方法。该方法检查电瓶容量，如果不是100，就将其设置为100。创建一辆电瓶容量为默认值的电动汽车，调用方法<code>get_range()</code>
 ，然后对电瓶进行升级，并再次调用<code>get_range()</code>
 。你将看到这辆汽车的续航里程增加了。</p>
</blockquote>
<h2 id="nav_point_204">9.4　导入类</h2>
<p>随着不断给类添加功能，文件可能变得很长，即便妥善地使用了继承亦如此。为遵循Python的总体理念，应让文件尽可能整洁。Python在这方面提供了帮助，允许将类存储在模块中，然后在主程序中导入所需的模块。</p>
<h3 id="nav_point_205">9.4.1　导入单个类</h3>
<p>下面来创建一个只包含<code>Car</code>
 类的模块。这让我们面临一个微妙的命名问题：在本章中已经有一个名为car.py的文件，但这个模块也应命名为car.py，因为它包含表示汽车的代码。我们将这样解决这个命名问题：将<code>Car</code>
 类存储在一个名为car.py的模块中，该模块将覆盖前面使用的文件car.py。从现在开始，使用该模块的程序都必须使用更具体的文件名，如my_car.py。下面是模块car.py，其中只包含<code>Car</code>
 类的代码：</p>
<p>
<em>car.py</em>
</p>
<pre class="代码无行号"><code>❶ """一个可用于表示汽车的类。"""
  class Car:
      """一次模拟汽车的简单尝试。"""
      def __init__(self, make, model, year):
          """初始化描述汽车的属性。"""
          self.make = make
          self.model = model
          self.year = year
          self.odometer_reading = 0
      def get_descriptive_name(self):
          """返回整洁的描述性名称。"""
          long_name = f"{self.year} {self.make} {self.model}"
          return long_name.title()
      def read_odometer(self):
          """打印一条消息，指出汽车的里程。"""
          print(f"This car has {self.odometer_reading} miles on it.")
      def update_odometer(self, mileage):
          """
          将里程表读数设置为指定的值。
          拒绝将里程表往回调。
          """
          if mileage &gt;= self.odometer_reading:
              self.odometer_reading = mileage
          else:
              print("You can't roll back an odometer!")
      def increment_odometer(self, miles):
          """将里程表读数增加指定的量。"""
          self.odometer_reading += miles</code>
</pre>
<p>❶处包含一个模块级文档字符串，对该模块的内容做了简要的描述。你应为自己创建的每个模块编写文档字符串。</p>
<p>下面来创建另一个文件my_car.py，在其中导入<code>Car</code>
 类并创建其实例：</p>
<p>
<em>my_car.py</em>
</p>
<pre class="代码无行号"><code>❶ from car import Car
  my_new_car = Car('audi', 'a4', 2019)
  print(my_new_car.get_descriptive_name())
  my_new_car.odometer_reading = 23
  my_new_car.read_odometer()</code>
</pre>
<p>❶处的<code>import</code>
 语句让Python打开模块<code>car</code>
 并导入其中的<code>Car</code>
 类。这样，我们就可以使用<code>Car</code>
 类，就像它是在这个文件中定义的一样。输出与我们在前面看到的一样：</p>
<pre class="代码无行号"><code>2019 Audi A4
This car has 23 miles on it.</code>
</pre>
<p>导入类是一种有效的编程方式。如果这个程序包含整个<code>Class</code>
 类，它该有多长啊！通过将这个类移到一个模块中并导入该模块，依然可以使用其所有功能，但主程序文件变得整洁而易于阅读了。这还让你能够将大部分逻辑存储在独立的文件中。确定类像你希望的那样工作后，就可以不管这些文件，而专注于主程序的高级逻辑了。</p>
<h3 id="nav_point_206">9.4.2　在一个模块中存储多个类</h3>
<p>虽然同一个模块中的类之间应存在某种相关性，但可根据需要在一个模块中存储任意数量的类。<code>Battery</code>
 类和<code>ElectricCar</code>
 类都可帮助模拟汽车，下面将它们都加入模块car.py中：</p>
<p>
<em>car.py</em>
</p>
<pre class="代码无行号"><code>"""一组用于表示燃油汽车和电动汽车的类。"""
class Car:
    --snip--
class Battery:
    """一次模拟电动汽车电瓶的简单尝试。"""
    def __init__(self, battery_size=75):
        """初始化电瓶的属性。"""
        self.battery_size = battery_size
    def describe_battery(self):
        """打印一条描述电瓶容量的消息。"""
        print(f"This car has a {self.battery_size}-kWh battery.")
    def get_range(self):
        """打印一条描述电瓶续航里程的消息。"""
        if self.battery_size == 75:
            range = 260
        elif self.battery_size == 100:
            range = 315
        print(f"This car can go about {range} miles on a full charge.")
class ElectricCar(Car):
    """模拟电动汽车的独特之处。"""
    def __init__(self, make, model, year):
        """
        初始化父类的属性。
        再初始化电动汽车特有的属性。
        """
        super().__init__(make, model, year)
        self.battery = Battery()</code>
</pre>
<p>现在，可以新建一个名为my_electric_car.py的文件，导入<code>ElectricCar</code>
 类，并创建一辆电动汽车了：</p>
<p>
<em>my_electric_car.py</em>
</p>
<pre class="代码无行号"><code>from car import ElectricCar
my_tesla = ElectricCar('tesla', 'model s', 2019)
print(my_tesla.get_descriptive_name())
my_tesla.battery.describe_battery()
my_tesla.battery.get_range()</code>
</pre>
<p>输出与我们在前面看到的相同，但大部分逻辑隐藏在一个模块中：</p>
<pre class="代码无行号"><code>2019 Tesla Model S
This car has a 75-kWh battery.
This car can go about 260 miles on a full charge.</code>
</pre>
<h3 id="nav_point_207">9.4.3　从一个模块中导入多个类</h3>
<p>可根据需要在程序文件中导入任意数量的类。如果要在同一个程序中创建普通汽车和电动汽车，就需要将<code>Car</code>
 类和<code>ElectricCar</code>
 类都导入：</p>
<p>
<em>my_cars.py</em>
</p>
<pre class="代码无行号"><code>❶ from car import Car, ElectricCar
❷ my_beetle = Car('volkswagen', 'beetle', 2019)
  print(my_beetle.get_descriptive_name())
❸ my_tesla = ElectricCar('tesla', 'roadster', 2019)
  print(my_tesla.get_descriptive_name())</code>
</pre>
<p>在❶处从一个模块中导入多个类时，用逗号分隔了各个类。导入必要的类后，就可根据需要创建每个类的任意数量实例。</p>
<p>在本例中，在❷处创建了一辆大众甲壳虫普通汽车，并在❸处创建了一辆特斯拉Roadster电动汽车：</p>
<pre class="代码无行号"><code>2019 Volkswagen Beetle
2019 Tesla Roadster</code>
</pre>
<h3 id="nav_point_208">9.4.4　导入整个模块</h3>
<p>还可以导入整个模块，再使用句点表示法访问需要的类。这种导入方式很简单，代码也易于阅读。因为创建类实例的代码都包含模块名，所以不会与当前文件使用的任何名称发生冲突。</p>
<p>下面的代码导入整个<code>car</code>
 模块，并创建一辆普通汽车和一辆电动汽车：</p>
<p>
<em>my_cars.py</em>
</p>
<pre class="代码无行号"><code>❶ import car
❷ my_beetle = car.Car('volkswagen', 'beetle', 2019)
  print(my_beetle.get_descriptive_name())
❸ my_tesla = car.ElectricCar('tesla', 'roadster', 2019)
  print(my_tesla.get_descriptive_name())</code>
</pre>
<p>在❶处，导入了整个<code>car</code>
 模块。接下来，使用语法<code>
<i>module_name.ClassName</i>
</code>
 访问需要的类。像前面一样，在❷处创建一辆大众甲壳虫汽车，并在❸处创建一辆特斯拉Roadster汽车。</p>
<h3 id="nav_point_209">9.4.5　导入模块中的所有类</h3>
<p>要导入模块中的每个类，可使用下面的语法：</p>
<pre class="代码无行号"><code>from module_name import *</code>
</pre>
<p>不推荐使用这种导入方式，原因有二。第一，如果只看文件开头的<code>import</code>
 语句，就能清楚地知道程序使用了哪些类，将大有裨益。然而这种导入方式没有明确地指出使用了模块中的哪些类。第二，这种方式还可能引发名称方面的迷惑。如果不小心导入了一个与程序文件中其他东西同名的类，将引发难以诊断的错误。这里之所以介绍这种导入方式，是因为虽然不推荐使用，但你可能在别人编写的代码中见到它。</p>
<p>需要从一个模块中导入很多类时，最好导入整个模块，并使用<code>
<i>module_name.ClassName</i>
</code>
 语法来访问类。这样做时，虽然文件开头并没有列出用到的所有类，但你清楚地知道在程序的哪些地方使用了导入的模块。这也避免了导入模块中的每个类可能引发的名称冲突。</p>
<h3 id="nav_point_210">9.4.6　在一个模块中导入另一个模块</h3>
<p>有时候，需要将类分散到多个模块中，以免模块太大或在同一个模块中存储不相关的类。将类存储在多个模块中时，你可能会发现一个模块中的类依赖于另一个模块中的类。在这种情况下，可在前一个模块中导入必要的类。</p>
<p>下面将<code>Car</code>
 类存储在一个模块中，并将<code>ElectricCar</code>
 类和<code>Battery</code>
 类存储在另一个模块中。将第二个模块命名为electric_car.py（这将覆盖前面创建的文件electric_car.py），并将<code>Battery</code>
 类和<code>ElectricCar</code>
 类复制到这个模块中：</p>
<p>
<em>electric_car.py</em>
</p>
<pre class="代码无行号"><code>  """一组可用于表示电动汽车的类。"""
❶ from car import Car
  class Battery:
      --snip--
  class ElectricCar(Car):
      --snip--</code>
</pre>
<p>
<code>ElectricCar</code>
 类需要访问其父类<code>Car</code>
 ，因此在❶处直接将<code>Car</code>
 类导入该模块中。如果忘记了这行代码，Python将在我们试图创建<code>ElectricCar</code>
 实例时引发错误。还需要更新模块<code>car</code>
 ，使其只包含<code>Car</code>
 类：</p>
<p>
<em>car.py</em>
</p>
<pre class="代码无行号"><code>"""一个可用于表示汽车的类。"""
class Car:
    --snip--</code>
</pre>
<p>现在可以分别从每个模块中导入类，以根据需要创建任何类型的汽车了：</p>
<p>
<em>my_cars.py</em>
</p>
<pre class="代码无行号"><code>❶ from car import Car
  from electric_car import ElectricCar
  my_beetle = Car('volkswagen', 'beetle', 2019)
  print(my_beetle.get_descriptive_name())
  my_tesla = ElectricCar('tesla', 'roadster', 2019)
  print(my_tesla.get_descriptive_name())</code>
</pre>
<p>在❶处，从模块<code>car</code>
 中导入了<code>Car</code>
 类，并从模块<code>electric_car</code>
 中导入<code>ElectricCar</code>
 类。接下来，创建了一辆普通汽车和一辆电动汽车。这两种汽车都被正确地创建出来了：</p>
<pre class="代码无行号"><code>2019 Volkswagen Beetle
2019 Tesla Roadster</code>
</pre>
<h3 id="nav_point_211">9.4.7　使用别名</h3>
<p>第8章说过，使用模块来组织项目代码时，别名大有裨益。导入类时，也可为其指定别名。</p>
<p>例如，要在程序中创建大量电动汽车实例，需要反复输入<code>ElectricCar</code>
 ，非常烦琐。为避免这种烦恼，可在<code>import</code>
 语句中给<code>ElectricCar</code>
 指定一个别名：</p>
<pre class="代码无行号"><code>from electric_car import ElectricCar as EC</code>
</pre>
<p>现在每当需要创建电动汽车实例时，都可使用这个别名：</p>
<pre class="代码无行号"><code>my_tesla = EC('tesla', 'roadster', 2019)</code>
</pre>
<h3 id="nav_point_212">9.4.8　自定义工作流程</h3>
<p>如你所见，在组织大型项目的代码方面，Python提供了很多选项。熟悉所有这些选项很重要，这样你才能确定哪种项目组织方式是最佳的，并能理解别人开发的项目。</p>
<p>一开始应让代码结构尽可能简单。先尽可能在一个文件中完成所有的工作，确定一切都能正确运行后，再将类移到独立的模块中。如果你喜欢模块和文件的交互方式，可在项目开始时就尝试将类存储到模块中。先找出让你能够编写出可行代码的方式，再尝试改进代码。</p>
<blockquote>
<p>
<strong>动手试一试</strong>
</p>
<p>
<strong>练习9-10：导入<code>Restaurant</code>
 类</strong>
 　将最新的<code>Restaurant</code>
 类存储在一个模块中。在另一个文件中，导入<code>Restaurant</code>
 类，创建一个<code>Restaurant</code>
 实例并调用<code>Restaurant</code>
 的一个方法，以确认<code>import</code>
 语句正确无误。</p>
<p>
<strong>练习9-11：导入<code>Admin</code>
 类</strong>
 　以为完成练习9-8而做的工作为基础。将<code>User</code>
 类、<code>Privileges</code>
 类和<code>Admin</code>
 类存储在一个模块中，再创建一个文件，在其中创建一个<code>Admin</code>
 实例并对其调用方法<code>show_privileges()</code>
 ，以确认一切都能正确运行。</p>
<p>
<strong>练习9-12：多个模块</strong>
 　将<code>User</code>
 类存储在一个模块中，并将<code>Privileges</code>
 类和<code>Admin</code>
 类存储在另一个模块中。再创建一个文件，在其中创建一个<code>Admin</code>
 实例并对其调用方法<code>show_privileges()</code>
 ，以确认一切依然能够正确运行。</p>
</blockquote>
<h2 id="nav_point_213">9.5　Python标准库</h2>
<p>
<strong>Python标准库</strong>
 是一组模块，我们安装的Python都包含它。你现在对函数和类的工作原理已有大致的了解，可以开始使用其他程序员编写好的模块了。可以使用标准库中的任何函数和类，只需在程序开头包含一条简单的<code>import</code>
 语句即可。下面来看看模块<code>random</code>
 ，它在你模拟很多现实情况时很有用。</p>
<p>在这个模块中，一个有趣的函数是<code>randint()</code>
 。它将两个整数作为参数，并随机返回一个位于这两个整数之间（含）的整数。下面演示了如何生成一个位于1和6之间的随机整数：</p>
<pre class="代码无行号"><code>&gt;&gt;&gt; from random import randint
&gt;&gt;&gt; randint(1, 6)
3</code>
</pre>
<p>在模块<code>random</code>
 中，另一个有用的函数是<code>choice()</code>
 。它将一个列表或元组作为参数，并随机返回其中的一个元素：</p>
<pre class="代码无行号"><code>&gt;&gt;&gt; from random import choice
&gt;&gt;&gt; players = ['charles', 'martina', 'michael', 'florence', 'eli']
&gt;&gt;&gt; first_up = choice(players)
&gt;&gt;&gt; first_up
'florence'</code>
</pre>
<p>创建与安全相关的应用程序时，请不要使用模块<code>random</code>
 ，但该模块可以很好地用于创建众多有趣的项目。</p>
<blockquote>
<p>
<strong>注意</strong>
 　还可以从其他地方下载外部模块。第二部分的每个项目都需要使用外部模块，届时你将看到很多此类示例。</p>
</blockquote>
<p>　</p>
<blockquote>
<p>
<strong>动手试一试</strong>
</p>
<p>
<strong>练习9-13：骰子</strong>
 　创建一个<code>Die</code>
 类，它包含一个名为<code>sides</code>
 的属性，该属性的默认值为6。编写一个名为<code>roll_die()</code>
 的方法，它打印位于1和骰子面数之间的随机数。创建一个6面的骰子再掷10次。</p>
<p>创建一个10面的骰子和一个20面的骰子，再分别掷10次。</p>
<p>
<strong>练习9-14：彩票</strong>
 　创建一个列表或元组，其中包含10个数和5个字母。从这个列表或元组中随机选择4个数或字母，并打印一条消息，指出只要彩票上是这4个数或字母，就中大奖了。</p>
<p>
<strong>练习9-15：彩票分析</strong>
 　可以使用一个循环来明白前述彩票大奖有多难中奖。为此，创建一个名为<code>my_ticket</code>
 的列表或元组，再编写一个循环，不断地随机选择数或字母，直到中大奖为止。请打印一条消息，报告执行循环多少次才中了大奖。</p>
<p>
<strong>练习9-16：Python Module of the Week</strong>
 　要了解Python标准库，一个很不错的资源是网站Python Module of the Week。请访问该网站并查看其中的目录，找一个你感兴趣的模块进行探索。从模块<code>random</code>
 开始可能是个不错的选择。</p>
</blockquote>
<h2 id="nav_point_214">9.6　类编码风格</h2>
<p>你必须熟悉有些与类相关的编码风格问题，在编写的程序较复杂时尤其如此。</p>
<p>类名应采用<strong>驼峰命名法</strong>
 ，即将类名中的每个单词的首字母都大写，而不使用下划线。实例名和模块名都采用小写格式，并在单词之间加上下划线。</p>
<p>对于每个类，都应紧跟在类定义后面包含一个文档字符串。这种文档字符串简要地描述类的功能，并遵循编写函数的文档字符串时采用的格式约定。每个模块也都应包含一个文档字符串，对其中的类可用于做什么进行描述。</p>
<p>可使用空行来组织代码，但不要滥用。在类中，可使用一个空行来分隔方法；而在模块中，可使用两个空行来分隔类。</p>
<p>需要同时导入标准库中的模块和你编写的模块时，先编写导入标准库模块的<code>import</code>
 语句，再添加一个空行，然后编写导入你自己编写的模块的<code>import</code>
 语句。在包含多条<code>import</code>
 语句的程序中，这种做法让人更容易明白程序使用的各个模块都来自何处。</p>
<h2 id="nav_point_215">9.7　小结</h2>
<p>在本章中，你学习了：如何编写类；如何使用属性在类中存储信息，以及如何编写方法，以让类具备所需的行为；如何编写方法<code>__init__()</code>
 ，以便根据类创建包含所需属性的实例。你见识了如何修改实例的属性，包括直接修改以及通过方法进行修改。你还了解了使用继承可简化相关类的创建工作，以及将一个类的实例用作另一个类的属性可让类更简洁。</p>
<p>你了解到，通过将类存储在模块中，并在需要使用这些类的文件中导入它们，可让项目组织有序。你学习了Python标准库，并见识了一个使用模块<code>random</code>
 的示例。最后，你学习了编写类时应遵循的Python约定。</p>
<p>在第10章中，你将学习如何使用文件，这让你能够保存你在程序中所做的工作，以及你让用户做的工作。你还将学习<strong>异常</strong>
 ，这是一种特殊的Python类，用于帮助你在发生错误时采取相应的措施。</p>
<br style="page-break-after:always"/>
<div></div>
]]></description>
    <pubDate>Wed, 15 Jul 2026 01:21:57 +0000</pubDate>
    <guid isPermaLink="false">9</guid>
</item>
<item>
    <title>Basic Python - 008 - 第 8 章 函数.epub</title>
    <description><![CDATA[<p style='color: #888;'>⏱ 预计阅读时间：133 分钟 (26403 字)</p><hr><h1 id="nav_point_161">第 8 章　函数</h1>
<blockquote>
<p><img alt="" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/8_Image00001.gif" style="width: 16%" width="16%"/>
 　在本章中，你将学习编写<strong>函数</strong>
 。函数是带名字的代码块，用于完成具体的工作。要执行函数定义的特定任务，可<strong>调用</strong>
 该函数。需要在程序中多次执行同一项任务时，无须反复编写完成该任务的代码，只需要调用执行该任务的函数，让Python运行其中的代码即可。你将发现，通过使用函数，程序编写、阅读、测试和修复起来都更加容易。</p>
<p>你还将学习向函数传递信息的方式；学习如何编写主要任务是显示信息的函数，以及旨在处理数据并返回一个或一组值的函数；最后，学习如何将函数存储在称为<strong>模块</strong>
 的独立文件中，让主程序文件的组织更为有序。</p>
</blockquote>
<h2 id="nav_point_162">8.1　定义函数</h2>
<p>下面是一个打印问候语的简单函数，名为<code>greet_user()</code>
 ：</p>
<p>
<em>greeter.py</em>
</p>
<pre class="代码无行号"><code>❶ def greet_user():
❷     """显示简单的问候语。"""
❸     print("Hello!")
❹ greet_user()</code>
</pre>
<p>本例演示了最简单的函数结构。❶处的代码行使用关键字<code>def</code>
 来告诉Python，你要定义一个函数。这是<strong>函数定义</strong>
 ，向Python指出了函数名，还可能在圆括号内指出函数为完成任务需要什么样的信息。在这里，函数名为<code>greet_user()</code>
 ，它不需要任何信息就能完成工作，因此括号是空的（即便如此，括号也必不可少）。最后，定义以冒号结尾。</p>
<p>紧跟在<code>def greet_user():</code>
 后面的所有缩进行构成了函数体。❷处的文本是称为<strong>文档字符串</strong>
 （docstring）的注释，描述了函数是做什么的。文档字符串用三引号括起，Python使用它们来生成有关程序中函数的文档。</p>
<p>代码行<code>print("Hello!")</code>
 （见❸）是函数体内的唯一一行代码，因此<code>greet_user()</code>
 只做一项工作：打印<code>Hello!</code>
 。</p>
<p>要使用这个函数，可调用它。<strong>函数调用</strong>
 让Python执行函数的代码。要<strong>调用</strong>
 函数，可依次指定函数名以及用圆括号括起的必要信息，如❹处所示。由于这个函数不需要任何信息，调用它时只需输入<code>greet_user()</code>
 即可。和预期一样，它打印<code>Hello!</code>
 ：</p>
<pre class="代码无行号"><code>Hello!</code>
</pre>
<h3 id="nav_point_163">8.1.1　向函数传递信息</h3>
<p>只需稍作修改，就可让函数<code>greet_user()</code>
 不仅向用户显示<code>Hello!</code>
 ，还将用户的名字作为抬头。为此，可在函数定义<code>def greet_user()</code>
 的括号内添加<code>username</code>
 。通过在这里添加<code>username</code>
 ，可让函数接受你给<code>username</code>
 指定的任何值。现在，这个函数要求你调用它时给<code>username</code>
 指定一个值。调用<code>greet_user()</code>
 时，可将一个名字传递给它，如下所示：</p>
<pre class="代码无行号"><code>def greet_user(username):
    """显示简单的问候语。"""
    print(f"Hello, {username.title()}!")
greet_user('jesse')</code>
</pre>
<p>代码<code>greet_user('jesse')</code>
 调用函数<code>greet_user()</code>
 ，并向它提供执行函数调用<code>print()</code>
 所需的信息。这个函数接受你传递给它的名字，并向这个人发出问候：</p>
<pre class="代码无行号"><code>Hello, Jesse!</code>
</pre>
<p>同样，<code>greet_user('sarah')</code>
 调用函数<code>greet_user()</code>
 并向它传递<code>'sarah'</code>
 ，从而打印<code>Hello, Sarah!</code>
 。可根据需要调用函数<code>greet_user()</code>
 任意次，调用时无论传入什么名字，都将生成相应的输出。</p>
<h3 id="nav_point_164">8.1.2　实参和形参</h3>
<p>前面定义函数<code>greet_user()</code>
 时，要求给变量<code>username</code>
 指定一个值。调用这个函数并提供这种信息（人名）时，它将打印相应的问候语。</p>
<p>在函数<code>greet_user()</code>
 的定义中，变量<code>username</code>
 是一个<strong>形参</strong>
 （parameter），即函数完成工作所需的信息。在代码<code>greet_user('jesse')</code>
 中，值<code>'jesse'</code>
 是一个<strong>实参</strong>
 （argument），即调用函数时传递给函数的信息。调用函数时，将要让函数使用的信息放在圆括号内。在<code>greet_user('jesse')</code>
 中，将实参<code>'jesse'</code>
 传递给了函数<code>greet_user()</code>
 ，这个值被赋给了形参<code>username</code>
 。</p>
<blockquote>
<p>
<strong>注意</strong>
 　大家有时候会形参、实参不分，因此如果你看到有人将函数定义中的变量称为实参或将函数调用中的变量称为形参，不要大惊小怪。</p>
</blockquote>
<p>　</p>
<blockquote>
<p>
<strong>动手试一试</strong>
</p>
<p>
<strong>练习8-1：消息</strong>
 　编写一个名为<code>display_message()</code>
 的函数，它打印一个句子，指出你在本章学的是什么。调用这个函数，确认显示的消息正确无误。</p>
<p>
<strong>练习8-2：喜欢的图书</strong>
 　编写一个名为<code>favorite_book()</code>
 的函数，其中包含一个名为<code>title</code>
 的形参。这个函数打印一条消息，下面是一个例子。</p>
<p>One of my favorite books is Alice in Wonderland.</p>
<p>调用这个函数，并将一本图书的名称作为实参传递给它。</p>
</blockquote>
<h2 id="nav_point_165">8.2　传递实参</h2>
<p>函数定义中可能包含多个形参，因此函数调用中也可能包含多个实参。向函数传递实参的方式很多：可使用<strong>位置实参</strong>
 ，这要求实参的顺序与形参的顺序相同；也可使用<strong>关键字实参</strong>
 ，其中每个实参都由变量名和值组成；还可使用列表和字典。下面依次介绍这些方式。</p>
<h3 id="nav_point_166">8.2.1　位置实参</h3>
<p>调用函数时，Python必须将函数调用中的每个实参都关联到函数定义中的一个形参。为此，最简单的关联方式是基于实参的顺序。这种关联方式称为<strong>位置实参</strong>
 。</p>
<p>为明白其中的工作原理，来看一个显示宠物信息的函数。这个函数指出一个宠物属于哪种动物以及它叫什么名字，如下所示：</p>
<p>
<em>pets.py</em>
</p>
<pre class="代码无行号"><code>❶ def describe_pet(animal_type, pet_name):
      """显示宠物的信息。"""
      print(f"\nI have a {animal_type}.")
      print(f"My {animal_type}'s name is {pet_name.title()}.")
❷ describe_pet('hamster', 'harry')</code>
</pre>
<p>这个函数的定义表明，它需要一种动物类型和一个名字（见❶）。调用<code>describe_pet()</code>
 时，需要按顺序提供一种动物类型和一个名字。例如，在刚才的函数调用中，实参<code>'hamster'</code>
 被赋给形参<code>animal_type</code>
 ，而实参<code>'harry'</code>
 被赋给形参<code>pet_name</code>
 （见❷）。在函数体内，使用了这两个形参来显示宠物的信息。</p>
<p>输出描述了一只名为Harry的仓鼠：</p>
<pre class="代码无行号"><code>I have a hamster.
My hamster's name is Harry.</code>
</pre>
<ol>
<li value="1">
<p>
<strong>多次调用函数</strong>
</p>
<p>可以根据需要调用函数任意次。要再描述一个宠物，只需再次调用<code>describe_pet()</code>
 即可：</p>
<pre class="代码无行号"><code>def describe_pet(animal_type, pet_name):
    """显示宠物的信息。"""
    print(f"\nI have a {animal_type}.")
    print(f"My {animal_type}'s name is {pet_name.title()}.")
describe_pet('hamster', 'harry')
describe_pet('dog', 'willie')</code>
</pre>
<p>第二次调用<code>describe_pet()</code>
 函数时，向它传递了实参<code>'dog'</code>
 和<code>'willie'</code>
 。与第一次调用时一样，Python将实参<code>'dog'</code>
 关联到形参<code>animal_type</code>
 ，并将实参<code>'willie'</code>
 关联到形参<code>pet_name</code>
 。与前面一样，这个函数完成了任务，但打印的是一条名为Willie的小狗的信息。至此，有一只名为Harry的仓鼠，还有一条名为Willie的小狗：</p>
<pre class="代码无行号"><code>I have a hamster.
My hamster's name is Harry.
I have a dog.
My dog's name is Willie.</code>
</pre>
<p>多次调用函数是一种效率极高的工作方式。只需在函数中编写一次描述宠物的代码，然后每当需要描述新宠物时，都调用该函数并向它提供新宠物的信息。即便描述宠物的代码增加到了10行，依然只需使用一行调用函数的代码，就可描述一个新宠物。</p>
<p>在函数中，可根据需要使用任意数量的位置实参，Python将按顺序将函数调用中的实参关联到函数定义中相应的形参。<br/>
 </p>
</li>
<li value="2">
<p>
<strong>位置实参的顺序很重要</strong>
</p>
<p>使用位置实参来调用函数时，如果实参的顺序不正确，结果可能出乎意料：</p>
<pre class="代码无行号"><code>def describe_pet(animal_type, pet_name):
    """显示宠物的信息。"""
    print(f"\nI have a {animal_type}.")
    print(f"My {animal_type}'s name is {pet_name.title()}.")
describe_pet('harry', 'hamster')</code>
</pre>
<p>在这个函数调用中，先指定名字，再指定动物类型。由于实参<code>'harry'</code>
 在前，这个值将赋给形参<code>animal_type</code>
 。同理，<code>'hamster'</code>
 将赋给形参<code>pet_name</code>
 。结果是有一个名为Hamster的harry：</p>
<pre class="代码无行号"><code>I have a harry.
My harry's name is Hamster.</code>
</pre>
<p>如果你得到的结果像上面一样可笑，请确认函数调用中实参的顺序与函数定义中形参的顺序一致。</p>
</li>
</ol>
<h3 id="nav_point_167">8.2.2　关键字实参</h3>
<p>
<strong>关键字实参</strong>
 是传递给函数的名称值对。因为直接在实参中将名称和值关联起来，所以向函数传递实参时不会混淆（不会得到名为Hamster的harry这样的结果）。关键字实参让你无须考虑函数调用中的实参顺序，还清楚地指出了函数调用中各个值的用途。</p>
<p>下面来重新编写pets.py，在其中使用关键字实参来调用<code>describe_pet()</code>
 ：</p>
<pre class="代码无行号"><code>def describe_pet(animal_type, pet_name):
    """显示宠物的信息。"""
    print(f"\nI have a {animal_type}.")
    print(f"My {animal_type}'s name is {pet_name.title()}.")
describe_pet(animal_type='hamster', pet_name='harry')</code>
</pre>
<p>函数<code>describe_pet()</code>
 还和之前一样，但调用这个函数时，向Python明确地指出了各个实参对应的形参。看到这个函数调用时，Python知道应该将实参<code>'hamster'</code>
 和<code>'harry'</code>
 分别赋给形参<code>animal_type</code>
 和<code>pet_name</code>
 。输出正确无误，指出有一只名为Harry的仓鼠。</p>
<p>关键字实参的顺序无关紧要，因为Python知道各个值该赋给哪个形参。下面两个函数调用是等效的：</p>
<pre class="代码无行号"><code>describe_pet(animal_type='hamster', pet_name='harry')
describe_pet(pet_name='harry', animal_type='hamster')</code>
</pre>
<blockquote>
<p>
<strong>注意</strong>
 　使用关键字实参时，务必准确指定函数定义中的形参名。</p>
</blockquote>
<h3 id="nav_point_168">8.2.3　默认值</h3>
<p>编写函数时，可给每个形参指定<strong>默认值</strong>
 。在调用函数中给形参提供了实参时，Python将使用指定的实参值；否则，将使用形参的默认值。因此，给形参指定默认值后，可在函数调用中省略相应的实参。使用默认值可简化函数调用，还可清楚地指出函数的典型用法。</p>
<p>例如，如果你发现调用<code>describe_pet()</code>
 时，描述的大多是小狗，就可将形参<code>animal_type</code>
 的默认值设置为<code>'dog'</code>
 。这样，调用<code>describe_pet()</code>
 来描述小狗时，就可不提供这种信息：</p>
<pre class="代码无行号"><code>def describe_pet(pet_name, animal_type='dog'):
    """显示宠物的信息。"""
    print(f"\nI have a {animal_type}.")
    print(f"My {animal_type}'s name is {pet_name.title()}.")
describe_pet(pet_name='willie')</code>
</pre>
<p>这里修改了函数<code>describe_pet()</code>
 的定义，在其中给形参<code>animal_type</code>
 指定了默认值<code>'dog'</code>
 。这样，调用这个函数时，如果没有给<code>animal_type</code>
 指定值，Python就将把这个形参设置为<code>'dog'</code>
 ：</p>
<pre class="代码无行号"><code>I have a dog.
My dog's name is Willie.</code>
</pre>
<p>请注意，在这个函数的定义中，修改了形参的排列顺序。因为给<code>animal_type</code>
 指定了默认值，无须通过实参来指定动物类型，所以在函数调用中只包含一个实参——宠物的名字。然而，Python依然将这个实参视为位置实参，因此如果函数调用中只包含宠物的名字，这个实参将关联到函数定义中的第一个形参。这就是需要将<code>pet_name</code>
 放在形参列表开头的原因。</p>
<p>现在，使用这个函数的最简单方式是在函数调用中只提供小狗的名字：</p>
<pre class="代码无行号"><code>describe_pet('willie')</code>
</pre>
<p>这个函数调用的输出与前一个示例相同。只提供了一个实参<code>'willie'</code>
 ，这个实参将关联到函数定义中的第一个形参<code>pet_name</code>
 。由于没有给<code>animal_type</code>
 提供实参，Python将使用默认值<code>'dog'</code>
 。</p>
<p>如果要描述的动物不是小狗，可使用类似于下面的函数调用：</p>
<pre class="代码无行号"><code>describe_pet(pet_name='harry', animal_type='hamster')</code>
</pre>
<p>由于显式地给<code>animal_type</code>
 提供了实参，Python将忽略这个形参的默认值。</p>
<blockquote>
<p>
<strong>注意</strong>
 　使用默认值时，必须先在形参列表中列出没有默认值的形参，再列出有默认值的实参。这让Python依然能够正确地解读位置实参。</p>
</blockquote>
<h3 id="nav_point_169">8.2.4　等效的函数调用</h3>
<p>鉴于可混合使用位置实参、关键字实参和默认值，通常有多种等效的函数调用方式。请看下面对函数<code>describe_pet()</code>
 的定义，其中给一个形参提供了默认值：</p>
<pre class="代码无行号"><code>def describe_pet(pet_name, animal_type='dog'):</code>
</pre>
<p>基于这种定义，在任何情况下都必须给<code>pet_name</code>
 提供实参。指定该实参时可采用位置方式，也可采用关键字方式。如果要描述的动物不是小狗，还必须在函数调用中给<code>animal_type</code>
 提供实参。同样，指定该实参时可以采用位置方式，也可采用关键字方式。</p>
<p>下面对这个函数的所有调用都可行：</p>
<pre class="代码无行号"><code># 一条名为Willie的小狗。
describe_pet('willie')
describe_pet(pet_name='willie')
# 一只名为Harry的仓鼠。
describe_pet('harry', 'hamster')
describe_pet(pet_name='harry', animal_type='hamster')
describe_pet(animal_type='hamster', pet_name='harry')</code>
</pre>
<p>这些函数调用的输出与前面的示例相同。</p>
<blockquote>
<p>
<strong>注意</strong>
 　使用哪种调用方式无关紧要，只要函数调用能生成你期望的输出就行。使用对你来说最容易理解的调用方式即可。</p>
</blockquote>
<h3 id="nav_point_170">8.2.5　避免实参错误</h3>
<p>等你开始使用函数后，如果遇到实参不匹配错误，不要大惊小怪。你提供的实参多于或少于函数完成工作所需的信息时，将出现实参不匹配错误。例如，如果调用函数<code>describe_pet()</code>
 时没有指定任何实参，结果将如何呢？</p>
<pre class="代码无行号"><code>def describe_pet(animal_type, pet_name):
    """显示宠物的信息。"""
    print(f"\nI have a {animal_type}.")
    print(f"My {animal_type}'s name is {pet_name.title()}.")
describe_pet()</code>
</pre>
<p>Python发现该函数调用缺少必要的信息，traceback指出了这一点：</p>
<pre class="代码无行号"><code>  Traceback (most recent call last):
❶   File "pets.py", line 6, in &lt;module&gt;
❷     describe_pet()
❸ TypeError: describe_pet() missing 2 required positional arguments: 'animal_
  type' and 'pet_name'</code>
</pre>
<p>在❶处，traceback指出了问题出在什么地方，让我们能够回过头去找出函数调用中的错误。在❷处，指出了导致问题的函数调用。在❸处，traceback指出该函数调用少了两个实参，并指出了相应形参的名称。如果这个函数存储在一个独立的文件中，我们也许无须打开这个文件并查看函数的代码，就能重新正确地编写函数调用。</p>
<p>Python读取函数的代码并指出需要为哪些形参提供实参，这提供了极大的帮助。这也是应该给变量和函数指定描述性名称的另一个原因：如果这样做了，那么无论对于你，还是可能使用你编写的代码的其他任何人来说，Python提供的错误消息都将更帮助。</p>
<p>如果提供的实参太多，将出现类似的traceback，帮助你确保函数调用和函数定义匹配。</p>
<blockquote>
<p>
<strong>动手试一试</strong>
</p>
<p>
<strong>练习8-3：T恤</strong>
 　编写一个名为<code>make_shirt()</code>
 的函数，它接受一个尺码以及要印到T恤上的字样。这个函数应打印一个句子，概要地说明T恤的尺码和字样。</p>
<p>使用位置实参调用该函数来制作一件T恤，再使用关键字实参来调用这个函数。</p>
<p>
<strong>练习8-4：大号T恤</strong>
 　修改函数<code>make_shirt()</code>
 ，使其在默认情况下制作一件印有“I love Python”字样的大号T恤。调用这个函数来制作：一件印有默认字样的大号T恤，一件印有默认字样的中号T恤，以及一件印有其他字样的T恤（尺码无关紧要）。</p>
<p>
<strong>练习8-5：城市</strong>
 　编写一个名为<code>describe_city()</code>
 的函数，它接受一座城市的名字以及该城市所属的国家。这个函数应打印一个简单的句子，下面是一个例子。</p>
<p>Reykjavik is in Iceland.</p>
<p>给用于存储国家的形参指定默认值。为三座不同的城市调用这个函数，且其中至少有一座城市不属于默认国家。</p>
</blockquote>
<h2 id="nav_point_171">8.3　返回值</h2>
<p>函数并非总是直接显示输出，它还可以处理一些数据，并返回一个或一组值。函数返回的值称为<strong>返回值</strong>
 。在函数中，可使用<code>return</code>
 语句将值返回到调用函数的代码行。返回值让你能够将程序的大部分繁重工作移到函数中去完成，从而简化主程序。</p>
<h3 id="nav_point_172">8.3.1　返回简单值</h3>
<p>下面来看一个函数，它接受名和姓并返回整洁的姓名：</p>
<p>
<em>formatted_name.py</em>
</p>
<pre class="代码无行号"><code>❶ def get_formatted_name(first_name, last_name):
      """返回整洁的姓名。"""
❷     full_name = f"{first_name} {last_name}"
❸     return full_name.title()
❹ musician = get_formatted_name('jimi', 'hendrix')
  print(musician)</code>
</pre>
<p>函数<code>get_formatted_name()</code>
 的定义通过形参接受名和姓（见❶）。它将姓和名合而为一，在中间加上一个空格，并将结果赋给变量<code>full_name</code>
 （见❷）。然后，将<code>full_name</code>
 的值转换为首字母大写格式，并将结果返回到函数调用行（见❸）。</p>
<p>调用返回值的函数时，需要提供一个变量，以便将返回的值赋给它。在这里，将返回值赋给了变量<code>musician</code>
 （见❹）。输出为整洁的姓名：</p>
<pre class="代码无行号"><code>Jimi Hendrix</code>
</pre>
<p>原本只需编写下面的代码就可输出整洁的姓名，相比于此，前面做的工作好像太多了：</p>
<pre class="代码无行号"><code>print("Jimi Hendrix")</code>
</pre>
<p>但在需要分别存储大量名和姓的大型程序中，像<code>get_formatted_name()</code>
 这样的函数非常有用。可以分别存储名和姓，每当需要显示姓名时都调用这个函数。</p>
<h3 id="nav_point_173">8.3.2　让实参变成可选的</h3>
<p>有时候，需要让实参变成可选的，这样使用函数的人就能只在必要时提供额外的信息。可使用默认值来让实参变成可选的。</p>
<p>例如，假设要扩展函数<code>get_formatted_name()</code>
 ，使其同时处理中间名。为此，可将其修改成类似于下面这样：</p>
<pre class="代码无行号"><code>def get_formatted_name(first_name, middle_name, last_name):
    """返回整洁的姓名。"""
    full_name = f"{first_name} {middle_name} {last_name}"
    return full_name.title()
musician = get_formatted_name('john', 'lee', 'hooker')
print(musician)</code>
</pre>
<p>只要同时提供名、中间名和姓，这个函数就能正确运行。它根据这三部分创建一个字符串，在适当的地方加上空格，并将结果转换为首字母大写格式：</p>
<pre class="代码无行号"><code>John Lee Hooker</code>
</pre>
<p>并非所有的人都有中间名，但如果调用这个函数时只提供了名和姓，它将不能正确运行。为了让中间名变成可选的，可给形参<code>middle_name</code>
 指定一个空的默认值，并在用户没有提供中间名时不使用这个形参。为让<code>get_formatted_name()</code>
 在没有提供中间名时依然可行，可将形参<code>middle_name</code>
 的默认值设置为空字符串，并将其移到形参列表的末尾：</p>
<pre class="代码无行号"><code>❶ def get_formatted_name(first_name, last_name, middle_name=''):
      """返回整洁的姓名。"""
❷     if middle_name:
          full_name = f"{first_name} {middle_name} {last_name}"
❸     else:
          full_name = f"{first_name} {last_name}"
      return full_name.title()
  musician = get_formatted_name('jimi', 'hendrix')
  print(musician)
❹ musician = get_formatted_name('john', 'hooker', 'lee')
  print(musician)</code>
</pre>
<p>在本例中，姓名是根据三个可能提供的部分创建的。由于人都有名和姓，因此在函数定义中首先列出了这两个形参。中间名是可选的，因此在函数定义中最后列出该形参，并将其默认值设置为空字符串（见❶）。</p>
<p>在函数体中，检查是否提供了中间名。Python将非空字符串解读为<code>True</code>
 ，因此如果函数调用中提供了中间名，<code>if middle_name</code>
 将为<code>True</code>
 （见❷）。如果提供了中间名，就将名、中间名和姓合并为姓名，再将其修改为首字母大写格式，并返回到函数调用行。在函数调用行，将返回的值赋给变量<code>musician</code>
 ，然后这个变量的值被打印出来。如果没有提供中间名，<code>middle_name</code>
 将为空字符串，导致<code>if</code>
 测试未通过，进而执行<code>else</code>
 代码块（见❸）：只使用名和姓来生成姓名，并将格式设置好的姓名返回给函数调用行。在函数调用行，将返回的值赋给变量<code>musician</code>
 ，然后这个变量的值被打印出来。</p>
<p>调用这个函数时，如果只想指定名和姓，调用起来将非常简单。如果还要指定中间名，就必须确保它是最后一个实参，这样Python才能正确地将位置实参关联到形参（见❹）。</p>
<p>这个修改后的版本不仅适用于只有名和姓的人，而且适用于还有中间名的人：</p>
<pre class="代码无行号"><code>Jimi Hendrix
John Lee Hooker</code>
</pre>
<p>可选值让函数能够处理各种不同的情形，同时确保函数调用尽可能简单。</p>
<h3 id="nav_point_174">8.3.3　返回字典</h3>
<p>函数可返回任何类型的值，包括列表和字典等较复杂的数据结构。例如，下面的函数接受姓名的组成部分，并返回一个表示人的字典：</p>
<p>
<em>person.py</em>
</p>
<pre class="代码无行号"><code>  def build_person(first_name, last_name):
      """返回一个字典，其中包含有关一个人的信息。"""
❶     person = {'first': first_name, 'last': last_name}
❷     return person
  musician = build_person('jimi', 'hendrix')
❸ print(musician)</code>
</pre>
<p>函数<code>build_person()</code>
 接受名和姓，并将这些值放到字典中（见❶）。存储<code>first_name</code>
 的值时，使用的键为<code>'first'</code>
 ，而存储<code>last_name</code>
 的值时，使用的键为<code>'last'</code>
 。最后，返回表示人的整个字典（见❷）。在❸处，打印这个返回的值，此时原来的两项文本信息存储在一个字典中：</p>
<pre class="代码无行号"><code>{'first': 'jimi', 'last': 'hendrix'}</code>
</pre>
<p>这个函数接受简单的文本信息，并将其放在一个更合适的数据结构中，让你不仅能打印这些信息，还能以其他方式处理它们。当前，字符串<code>'jimi'</code>
 和<code>'hendrix'</code>
 被标记为名和姓。你可以轻松地扩展这个函数，使其接受可选值，如中间名、年龄、职业或其他任何要存储的信息。例如，下面的修改让你能存储年龄：</p>
<pre class="代码无行号"><code>def build_person(first_name, last_name, age=None):
    """返回一个字典，其中包含有关一个人的信息。"""
    person = {'first': first_name, 'last': last_name}
    if age:
        person['age'] = age
    return person
musician = build_person('jimi', 'hendrix', age=27)
print(musician)</code>
</pre>
<p>在函数定义中，新增了一个可选形参<code>age</code>
 ，并将其默认值设置为特殊值<code>None</code>
 （表示变量没有值）。可将<code>None</code>
 视为占位值。在条件测试中，<code>None</code>
 相当于<code>False</code>
 。如果函数调用中包含形参<code>age</code>
 的值，这个值将被存储到字典中。在任何情况下，这个函数都会存储人的姓名，但可进行修改，使其同时存储有关人的其他信息。</p>
<h3 id="nav_point_175">8.3.4　结合使用函数和<code>while</code>
 循环</h3>
<p>可将函数同本书前面介绍的任何Python结构结合起来使用。例如，下面将结合使用函数<code>get_formatted_name()</code>
 和<code>while</code>
 循环，以更正式的方式问候用户。下面尝试使用名和姓跟用户打招呼：</p>
<p>
<em>greeter.py</em>
</p>
<pre class="代码无行号"><code>  def get_formatted_name(first_name, last_name):
      """返回整洁的姓名。"""
      full_name = f"{first_name} {last_name}"
      return full_name.title()
  # 这是一个无限循环！
  while True:
❶     print("\nPlease tell me your name:")
      f_name = input("First name: ")
      l_name = input("Last name: ")
      formatted_name = get_formatted_name(f_name, l_name)
      print(f"\nHello, {formatted_name}!")</code>
</pre>
<p>在本例中，使用的是<code>get_formatted_name()</code>
 的简单版本，不涉及中间名。<code>while</code>
 循环让用户输入姓名：依次提示用户输入名和姓（见❶）。</p>
<p>但这个<code>while</code>
 循环存在一个问题：没有定义退出条件。请用户提供一系列输入时，该在什么地方提供退出途径呢？要让用户能够尽可能容易地退出，因此每次提示用户输入时，都应提供退出途径。每次提示用户输入时，都使用<code>break</code>
 语句提供退出循环的简单途径：</p>
<pre class="代码无行号"><code>def get_formatted_name(first_name, last_name):
    """返回整洁的姓名。"""
    full_name = f"{first_name} {last_name}"
    return full_name.title()
while True:
    print("\nPlease tell me your name:")
    print("(enter 'q' at any time to quit)")
    f_name = input("First name: ")
    if f_name == 'q':
        break
    l_name = input("Last name: ")
    if l_name == 'q':
        break
    formatted_name = get_formatted_name(f_name, l_name)
    print(f"\nHello, {formatted_name}!")</code>
</pre>
<p>我们添加了一条消息来告诉用户如何退出，然后在每次提示用户输入时，都检查他输入的是否是退出值。如果是，就退出循环。现在，这个程序将不断地问候，直到用户输入的姓或名为<code>'q'</code>
 ：</p>
<pre class="代码无行号"><code>Please tell me your name:
(enter 'q' at any time to quit)
First name: eric
Last name: matthes
Hello, Eric Matthes!
Please tell me your name:
(enter 'q' at any time to quit)
First name: q</code>
</pre>
<blockquote>
<p>
<strong>动手试一试</strong>
</p>
<p>
<strong>练习8-6：城市名</strong>
 　编写一个名为<code>city_country()</code>
 的函数，它接受城市的名称及其所属的国家。这个函数应返回一个格式类似于下面的字符串：</p>
<pre class="代码无行号"><code>"Santiago, Chile"</code>
</pre>
<p>至少使用三个城市国家对来调用这个函数，并打印它返回的值。</p>
<p>
<strong>练习8-7：专辑</strong>
 　编写一个名为<code>make_album()</code>
 的函数，它创建一个描述音乐专辑的字典。这个函数应接受歌手的名字和专辑名，并返回一个包含这两项信息的字典。使用这个函数创建三个表示不同专辑的字典，并打印每个返回的值，以核实字典正确地存储了专辑的信息。</p>
<p>给函数<code>make_album()</code>
 添加一个默认值为<code>None</code>
 的可选形参，以便存储专辑包含的歌曲数。如果调用这个函数时指定了歌曲数，就将该值添加到表示专辑的字典中。调用这个函数，并至少在一次调用中指定专辑包含的歌曲数。</p>
<p>
<strong>练习8-8：用户的专辑</strong>
 　在为完成练习8-7编写的程序中，编写一个<code>while</code>
 循环，让用户输入专辑的歌手和名称。获取这些信息后，使用它们来调用函数<code>make_album()</code>
 并将创建的字典打印出来。在这个<code>while</code>
 循环中，务必提供退出途径。</p>
</blockquote>
<h2 id="nav_point_176">8.4　传递列表</h2>
<p>你经常会发现，向函数传递列表很有用，其中包含的可能是名字、数或更复杂的对象（如字典）。将列表传递给函数后，函数就能直接访问其内容。下面使用函数来提高处理列表的效率。</p>
<p>假设有一个用户列表，我们要问候其中的每位用户。下面的示例将包含名字的列表传递给一个名为<code>greet_users()</code>
 的函数，这个函数问候列表中的每个人：</p>
<p>
<em>greet_users.py</em>
</p>
<pre class="代码无行号"><code>  def greet_users(names):
      """向列表中的每位用户发出简单的问候。"""
      for name in names:
          msg = f"Hello, {name.title()}!"
          print(msg)
❶ usernames = ['hannah', 'ty', 'margot']
  greet_users(usernames)</code>
</pre>
<p>我们将<code>greet_users()</code>
 定义为接受一个名字列表，并将其赋给形参<code>names</code>
 。这个函数遍历收到的列表，并对其中的每位用户打印一条问候语。❶处定义了一个用户列表<code>usernames</code>
 ，然后调用<code>greet_users()</code>
 并将该列表传递给它：</p>
<pre class="代码无行号"><code>Hello, Hannah!
Hello, Ty!
Hello, Margot!</code>
</pre>
<p>输出完全符合预期。每位用户都看到了一条个性化的问候语。每当需要问候一组用户时，都可调用这个函数。</p>
<h3 id="nav_point_177">8.4.1　在函数中修改列表</h3>
<p>将列表传递给函数后，函数就可对其进行修改。在函数中对这个列表所做的任何修改都是永久性的，这让你能够高效地处理大量数据。</p>
<p>来看一家为用户提交的设计制作3D打印模型的公司。需要打印的设计存储在一个列表中，打印后将移到另一个列表中。下面是在不使用函数的情况下模拟这个过程的代码：</p>
<p>
<em>printing_models.py</em>
</p>
<pre class="代码无行号"><code># 首先创建一个列表，其中包含一些要打印的设计。
unprinted_designs = ['phone case', 'robot pendant', 'dodecahedron']
completed_models = []
# 模拟打印每个设计，直到没有未打印的设计为止。
# 打印每个设计后，都将其移到列表completed_models中。
while unprinted_designs:
    current_design = unprinted_designs.pop()
    print(f"Printing model: {current_design}")
    completed_models.append(current_design)
# 显示打印好的所有模型。
print("\nThe following models have been printed:")
for completed_model in completed_models:
    print(completed_model)</code>
</pre>
<p>这个程序首先创建一个需要打印的设计列表，以及一个名为<code>completed_models</code>
 的空列表，每个设计打印后都将移到其中。只要列表<code>unprinted_designs</code>
 中还有设计，<code>while</code>
 循环就模拟打印设计的过程：从该列表末尾删除一个设计，将其赋给变量<code>current_design</code>
 ，并显示一条消息指出正在打印当前的设计，然后将该设计加入到列表<code>completed_models</code>
 中。循环结束后，显示已打印的所有设计：</p>
<pre class="代码无行号"><code>Printing model: dodecahedron
Printing model: robot pendant
Printing model: phone case
The following models have been printed:
dodecahedron
robot pendant
phone case</code>
</pre>
<p>为重新组织这些代码，可编写两个函数，每个都做一件具体的工作。大部分代码与原来相同，只是效率更高。第一个函数负责处理打印设计的工作，第二个概述打印了哪些设计：</p>
<pre class="代码无行号"><code>❶ def print_models(unprinted_designs, completed_models):
      """
      模拟打印每个设计，直到没有未打印的设计为止。
      打印每个设计后，都将其移到列表completed_models中。
      """
      while unprinted_designs:
          current_design = unprinted_designs.pop()
          print(f"Printing model: {current_design}")
          completed_models.append(current_design)
❷ def show_completed_models(completed_models):
      """显示打印好的所有模型。"""
      print("\nThe following models have been printed:")
      for completed_model in completed_models:
          print(completed_model)
  unprinted_designs = ['phone case', 'robot pendant', 'dodecahedron']
  completed_models = []
  print_models(unprinted_designs, completed_models)
  show_completed_models(completed_models)</code>
</pre>
<p>❶处定义了函数<code>print_models()</code>
 ，它包含两个形参：一个需要打印的设计列表和一个打印好的模型列表。给定这两个列表，该函数模拟打印每个设计的过程：将设计逐个从未打印的设计列表中取出，并加入打印好的模型列表中。❷处定义了函数<code>show_completed_models()</code>
 ，它包含一个形参：打印好的模型列表。给定这个列表，函数<code>show_completed_models()</code>
 显示打印出来的每个模型的名称。</p>
<p>这个程序的输出与未使用函数的版本相同，但组织更为有序。完成大部分工作的代码都移到了两个函数中，让主程序更容易理解。只要看看主程序，就会发现这个程序的功能清晰得多：</p>
<pre class="代码无行号"><code>unprinted_designs = ['phone case', 'robot pendant', 'dodecahedron']
completed_models = []
print_models(unprinted_designs, completed_models)
show_completed_models(completed_models)</code>
</pre>
<p>我们创建了一个未打印的设计列表，还创建了一个空列表，用于存储打印好的模型。接下来，由于已经定义了两个函数，只需调用它们并传入正确的实参即可。我们调用<code>print_models()</code>
 并向它传递两个列表。像预期一样，<code>print_models()</code>
 模拟打印设计的过程。接下来，调用<code>show_completed_models()</code>
 ，并将打印好的模型列表传递给它，让其能够指出打印了哪些模型。描述性的函数名让别人阅读这些代码时也能明白，尽管没有任何注释。</p>
<p>相比于没有使用函数的版本，这个程序更容易扩展和维护。如果以后需要打印其他设计，只需再次调用<code>print_models()</code>
 即可。如果发现需要对打印代码进行修改，只需修改这些代码一次，就能影响所有调用该函数的地方。与必须分别修改程序的多个地方相比，这种修改的效率更高。</p>
<p>该程序还演示了这样一种理念：每个函数都应只负责一项具体的工作。第一个函数打印每个设计，第二个显示打印好的模型。这优于使用一个函数来完成这两项工作。编写函数时，如果发现它执行的任务太多，请尝试将这些代码划分到两个函数中。别忘了，总是可以在一个函数中调用另一个函数，这有助于将复杂的任务划分成一系列步骤。</p>
<h3 id="nav_point_178">8.4.2　禁止函数修改列表</h3>
<p>有时候，需要禁止函数修改列表。例如，假设像前一个示例那样，你有一个未打印的设计列表，并编写了一个函数将这些设计移到打印好的模型列表中。你可能会做出这样的决定：即便打印好了所有设计，也要保留原来的未打印的设计列表，以供备案。但由于你将所有的设计都移出了<code>unprinted_designs</code>
 ，这个列表变成了空的，原来的列表没有了。为解决这个问题，可向函数传递列表的副本而非原件。这样，函数所做的任何修改都只影响副本，而原件丝毫不受影响。</p>
<p>要将列表的副本传递给函数，可以像下面这样做：</p>
<pre class="代码无行号"><code>function_name(list_name_[:])</code>
</pre>
<p>切片表示法<code>[:]</code>
 创建列表的副本。在printing_models.py中，如果不想清空未打印的设计列表，可像下面这样调用<code>print_models()</code>
 ：</p>
<pre class="代码无行号"><code>print_models(unprinted_designs[:], completed_models)</code>
</pre>
<p>这样函数<code>print_models()</code>
 依然能够完成工作，因为它获得了所有未打印的设计的名称，但使用的是列表<code>unprinted_designs</code>
 的副本，而不是列表<code>unprinted_designs</code>
 本身。像以前一样，列表<code>completed_models</code>
 也将包含打印好的模型的名称，但函数所做的修改不会影响到列表<code>unprinted_designs</code>
 。</p>
<p>虽然向函数传递列表的副本可保留原始列表的内容，但除非有充分的理由，否则还是应该将原始列表传递给函数。这是因为让函数使用现成的列表可避免花时间和内存创建副本，从而提高效率，在处理大型列表时尤其如此。</p>
<blockquote>
<p>
<strong>动手试一试</strong>
</p>
<p>
<strong>练习8-9：消息</strong>
 　创建一个列表，其中包含一系列简短的文本消息。将该列表传递给一个名为<code>show_messages()</code>
 的函数，这个函数会打印列表中的每条文本消息。</p>
<p>
<strong>练习8-10：发送消息</strong>
 　在你为完成练习8-9而编写的程序中，编写一个名为<code>send_messages()</code>
 的函数，将每条消息都打印出来并移到一个名为<code>sent_messages</code>
 的列表中。调用函数<code>send_messages()</code>
 ，再将两个列表都打印出来，确认正确地移动了消息。</p>
<p>
<strong>练习8-11：消息归档</strong>
 　修改你为完成练习8-10而编写的程序，在调用函数<code>send_messages()</code>
 时，向它传递消息列表的副本。调用函数<code>send_messages()</code>
 后，将两个列表都打印出来，确认保留了原始列表中的消息。</p>
</blockquote>
<h2 id="nav_point_179">8.5　传递任意数量的实参</h2>
<p>有时候，预先不知道函数需要接受多少个实参，好在Python允许函数从调用语句中收集任意数量的实参。</p>
<p>例如，来看一个制作比萨的函数，它需要接受很多配料，但无法预先确定顾客要多少种配料。下面的函数只有一个形参<code>*toppings</code>
 ，但不管调用语句提供了多少实参，这个形参会将它们统统收入囊中：</p>
<p>
<em>pizza.py</em>
</p>
<pre class="代码无行号"><code>def make_pizza(*toppings):
    """打印顾客点的所有配料。"""
    print(toppings)
make_pizza('pepperoni')
make_pizza('mushrooms', 'green peppers', 'extra cheese')</code>
</pre>
<p>形参名<code>*toppings</code>
 中的星号让Python创建一个名为<code>toppings</code>
 的空元组，并将收到的所有值都封装到这个元组中。函数体内的函数调用<code>print()</code>
 通过生成输出，证明Python能够处理使用一个值来调用函数的情形，也能处理使用三个值来调用函数的情形。它以类似的方式处理不同的调用。注意，Python将实参封装到一个元组中，即便函数只收到一个值：</p>
<pre class="代码无行号"><code>('pepperoni',)
('mushrooms', 'green peppers', 'extra cheese')</code>
</pre>
<p>现在，可以将函数调用<code>print()</code>
 替换为一个循环，遍历配料列表并对顾客点的比萨进行描述：</p>
<pre class="代码无行号"><code>def make_pizza(*toppings):
    """概述要制作的比萨。"""
    print("\nMaking a pizza with the following toppings:")
    for topping in toppings:
        print(f"- {topping}")
make_pizza('pepperoni')
make_pizza('mushrooms', 'green peppers', 'extra cheese')</code>
</pre>
<p>不管收到一个值还是三个值，这个函数都能妥善处理：</p>
<pre class="代码无行号"><code>Making a pizza with the following toppings:
- pepperoni
Making a pizza with the following toppings:
- mushrooms
- green peppers
- extra cheese</code>
</pre>
<p>不管函数收到的实参是多少个，这种语法都管用。</p>
<h3 id="nav_point_180">8.5.1　结合使用位置实参和任意数量实参</h3>
<p>如果要让函数接受不同类型的实参，必须在函数定义中将接纳任意数量实参的形参放在最后。Python先匹配位置实参和关键字实参，再将余下的实参都收集到最后一个形参中。</p>
<p>例如，如果前面的函数还需要一个表示比萨尺寸的形参，必须将其放在形参<code>*toppings</code>
 的前面：</p>
<pre class="代码无行号"><code>def make_pizza(size, *toppings):
    """概述要制作的比萨。"""
    print(f"\nMaking a {size}-inch pizza with the following toppings:")
    for topping in toppings:
        print(f"- {topping}")
make_pizza(16, 'pepperoni')
make_pizza(12, 'mushrooms', 'green peppers', 'extra cheese')</code>
</pre>
<p>基于上述函数定义，Python将收到的第一个值赋给形参<code>size</code>
 ，并将其他所有值都存储在元组<code>toppings</code>
 中。在函数调用中，首先指定表示比萨尺寸的实参，再根据需要指定任意数量的配料。</p>
<p>现在，每个比萨都有了尺寸和一系列配料，而且这些信息按正确的顺序打印出来了——首先是尺寸，然后是配料：</p>
<pre class="代码无行号"><code>Making a 16-inch pizza with the following toppings:
- pepperoni
Making a 12-inch pizza with the following toppings:
- mushrooms
- green peppers
- extra cheese</code>
</pre>
<blockquote>
<p>
<strong>注意</strong>
 　你经常会看到通用形参名<code>*args</code>
 ，它也收集任意数量的位置实参。</p>
</blockquote>
<h3 id="nav_point_181">8.5.2　使用任意数量的关键字实参</h3>
<p>有时候，需要接受任意数量的实参，但预先不知道传递给函数的会是什么样的信息。在这种情况下，可将函数编写成能够接受任意数量的键值对——调用语句提供了多少就接受多少。一个这样的示例是创建用户简介：你知道将收到有关用户的信息，但不确定会是什么样的信息。在下面的示例中，函数<code>build_profile()</code>
 接受名和姓，还接受任意数量的关键字实参：</p>
<p>
<em>user_profile.py</em>
</p>
<pre class="代码无行号"><code>  def build_profile(first, last, **user_info):
      """创建一个字典，其中包含我们知道的有关用户的一切。"""
❶     user_info['first_name'] = first
      user_info['last_name'] = last
      return user_info
  user_profile = build_profile('albert', 'einstein',
                               location='princeton',
                               field='physics')
  print(user_profile)</code>
</pre>
<p>函数<code>build_profile()</code>
 的定义要求提供名和姓，同时允许根据需要提供任意数量的名称值对。形参<code>**user_info</code>
 中的两个星号让Python创建一个名为<code>user_info</code>
 的空字典，并将收到的所有名称值对都放到这个字典中。在这个函数中，可以像访问其他字典那样访问<code>user_info</code>
 中的名称值对。</p>
<p>在<code>build_profile()</code>
 的函数体内，将名和姓加入了字典<code>user_info</code>
 中（见❶），因为总是会从用户那里收到这两项信息，而这两项信息没有放到这个字典中。接下来，将字典<code>user_info</code>
 返回到函数调用行。</p>
<p>我们调用<code>build_profile()</code>
 ，向它传递名（<code>'albert'</code>
 ）、姓（<code>'einstein'</code>
 ）和两个键值对（<code>location='princeton'</code>
 和<code>field='physics'</code>
 ），并将返回的<code>user_info</code>
 赋给变量<code>user_profile</code>
 ，再打印该变量：</p>
<pre class="代码无行号"><code>{'location': 'princeton', 'field': 'physics',
'first_name': 'albert', 'last_name': 'einstein'}</code>
</pre>
<p>在这里，返回的字典包含用户的名和姓，还有求学的地方和所学专业。调用这个函数时，不管额外提供多少个键值对，它都能正确地处理。</p>
<p>编写函数时，能以各种方式混合使用位置实参、关键字实参和任意数量的实参。知道这些实参类型大有裨益，因为阅读别人编写的代码时经常会见到它们。要正确地使用这些类型的实参并知道其使用时机，需要经过一定的练习。就目前而言，牢记使用最简单的方法来完成任务就好了。继续往下阅读，你就会知道在各种情况下哪种方法的效率最高。</p>
<blockquote>
<p>
<strong>注意</strong>
 　你经常会看到形参名<code>**kwargs</code>
 ，它用于收集任意数量的关键字实参。</p>
</blockquote>
<p>　</p>
<blockquote>
<p>
<strong>动手试一试</strong>
</p>
<p>
<strong>练习8-12：三明治</strong>
 　编写一个函数，它接受顾客要在三明治中添加的一系列食材。这个函数只有一个形参（它收集函数调用中提供的所有食材），并打印一条消息，对顾客点的三明治进行概述。调用这个函数三次，每次都提供不同数量的实参。</p>
<p>
<strong>练习8-13：用户简介</strong>
 　复制前面的程序user_profile.py，在其中调用<code>build_profile()</code>
 来创建有关你的简介。调用这个函数时，指定你的名和姓，以及三个描述你的键值对。</p>
<p>
<strong>练习8-14：汽车</strong>
 　编写一个函数，将一辆汽车的信息存储在字典中。这个函数总是接受制造商和型号，还接受任意数量的关键字实参。这样调用该函数：提供必不可少的信息，以及两个名称值对，如颜色和选装配件。这个函数必须能够像下面这样进行调用：</p>
<pre class="代码无行号"><code>car = make_car('subaru', 'outback', color='blue', tow_package=True)</code>
</pre>
<p>打印返回的字典，确认正确地处理了所有的信息。</p>
</blockquote>
<h2 id="nav_point_182">8.6　将函数存储在模块中</h2>
<p>使用函数的优点之一是可将代码块与主程序分离。通过给函数指定描述性名称，可让主程序容易理解得多。你还可以更进一步，将函数存储在称为<strong>模块</strong>
 的独立文件中，再将模块<strong>导入</strong>
 到主程序中。<code>import</code>
 语句允许在当前运行的程序文件中使用模块中的代码。</p>
<p>通过将函数存储在独立的文件中，可隐藏程序代码的细节，将重点放在程序的高层逻辑上。这还能让你在众多不同的程序中重用函数。将函数存储在独立文件中后，可与其他程序员共享这些文件而不是整个程序。知道如何导入函数还能让你使用其他程序员编写的函数库。</p>
<p>导入模块的方法有多种，下面对每种进行简要的介绍。</p>
<h3 id="nav_point_183">8.6.1　导入整个模块</h3>
<p>要让函数是可导入的，得先创建模块。<strong>模块</strong>
 是扩展名为.py的文件，包含要导入到程序中的代码。下面来创建一个包含函数<code>make_pizza()</code>
 的模块。为此，将文件pizza.py中除函数<code>make_pizza()</code>
 之外的其他代码删除：</p>
<p>
<em>pizza.py</em>
</p>
<pre class="代码无行号"><code>def make_pizza(size, *toppings):
    """概述要制作的比萨。"""
    print(f"\nMaking a {size}-inch pizza with the following toppings:")
    for topping in toppings:
        print(f"- {topping}")</code>
</pre>
<p>接下来，在pizza.py所在的目录中创建一个名为making_pizzas.py的文件。这个文件导入刚创建的模块，再调用<code>make_pizza()</code>
 两次：</p>
<p>
<em>making_pizzas.py</em>
</p>
<pre class="代码无行号"><code>  import pizza
❶ pizza.make_pizza(16, 'pepperoni')
  pizza.make_pizza(12, 'mushrooms', 'green peppers', 'extra cheese')</code>
</pre>
<p>Python读取这个文件时，代码行<code>import pizza</code>
 让Python打开文件pizza.py，并将其中的所有函数都复制到这个程序中。你看不到复制的代码，因为在这个程序即将运行时，Python在幕后复制了这些代码。你只需知道，在making_pizzas.py中，可使用pizza.py中定义的所有函数。</p>
<p>要调用被导入模块中的函数，可指定被导入模块的名称<code>pizza</code>
 和函数名<code>make_pizza()</code>
 ，并用句点分隔（见❶）。这些代码的输出与没有导入模块的原始程序相同：</p>
<pre class="代码无行号"><code>Making a 16-inch pizza with the following toppings:
- pepperoni
Making a 12-inch pizza with the following toppings:
- mushrooms
- green peppers
- extra cheese</code>
</pre>
<p>这就是一种导入方法：只需编写一条<code>import</code>
 语句并在其中指定模块名，就可在程序中使用该模块中的所有函数。如果使用这种<code>import</code>
 语句导入了名为module_name.py的整个模块，就可使用下面的语法来使用其中任何一个函数：</p>
<pre class="代码无行号"><code>module_name.function_name()</code>
</pre>
<h3 id="nav_point_184">8.6.2　导入特定的函数</h3>
<p>还可以导入模块中的特定函数，这种导入方法的语法如下：</p>
<pre class="代码无行号"><code>from module_name import function_name</code>
</pre>
<p>通过用逗号分隔函数名，可根据需要从模块中导入任意数量的函数：</p>
<pre class="代码无行号"><code>from module_name import function_0, function_1, function_2</code>
</pre>
<p>对于前面的making_pizzas.py示例，如果只想导入要使用的函数，代码将类似于下面这样：</p>
<pre class="代码无行号"><code>from pizza import make_pizza
make_pizza(16, 'pepperoni')
make_pizza(12, 'mushrooms', 'green peppers', 'extra cheese')</code>
</pre>
<p>使用这种语法时，调用函数时无须使用句点。由于在<code>import</code>
 语句中显式地导入了函数<code>make_pizza()</code>
 ，调用时只需指定其名称即可。</p>
<h3 id="nav_point_185">8.6.3　使用<code>as</code>
 给函数指定别名</h3>
<p>如果要导入函数的名称可能与程序中现有的名称冲突，或者函数的名称太长，可指定简短而独一无二的<strong>别名</strong>
 ：函数的另一个名称，类似于外号。要给函数取这种特殊外号，需要在导入它时指定。</p>
<p>下面给函数<code>make_pizza()</code>
 指定了别名<code>mp()</code>
 。这是在<code>import</code>
 语句中使用<code>make_pizza as mp</code>
 实现的，关键字<code>as</code>
 将函数重命名为指定的别名：</p>
<pre class="代码无行号"><code>from pizza import make_pizza as mp
mp(16, 'pepperoni')
mp(12, 'mushrooms', 'green peppers', 'extra cheese')</code>
</pre>
<p>上面的<code>import</code>
 语句将函数<code>make_pizza()</code>
 重命名为<code>mp()</code>
 。在这个程序中，每当需要调用<code>make_pizza()</code>
 时，都可简写成<code>mp()</code>
 。Python将运行<code>make_pizza()</code>
 中的代码，避免与这个程序可能包含的函数<code>make_pizza()</code>
 混淆。</p>
<p>指定别名的通用语法如下：</p>
<pre class="代码无行号"><code>from module_name import function_name as fn</code>
</pre>
<h3 id="nav_point_186">8.6.4　使用<code>as</code>
 给模块指定别名</h3>
<p>还可以给模块指定别名。通过给模块指定简短的别名（如给模块<code>pizza</code>
 指定别名<code>p</code>
 ），让你能够更轻松地调用模块中的函数。相比于<code>pizza.make_pizza()</code>
 ，<code>p.make_pizza()</code>
 更为简洁：</p>
<pre class="代码无行号"><code>import pizza as p
p.make_pizza(16, 'pepperoni')
p.make_pizza(12, 'mushrooms', 'green peppers', 'extra cheese')</code>
</pre>
<p>上述<code>import</code>
 语句给模块<code>pizza</code>
 指定了别名<code>p</code>
 ，但该模块中所有函数的名称都没变。要调用函数<code>make_pizza()</code>
 ，可编写代码<code>p.make_pizza()</code>
 而非<code>pizza.make_pizza()</code>
 。这样不仅代码更简洁，还让你不用再关注模块名，只专注于描述性的函数名。这些函数名明确指出了函数的功能，对于理解代码而言，比模块名更重要。</p>
<p>给模块指定别名的通用语法如下：</p>
<pre class="代码无行号"><code>import module_name as mn</code>
</pre>
<h3 id="nav_point_187">8.6.5　导入模块中的所有函数</h3>
<p>使用星号（<code>*</code>
 ）运算符可让Python导入模块中的所有函数：</p>
<pre class="代码无行号"><code>from pizza import *
make_pizza(16, 'pepperoni')
make_pizza(12, 'mushrooms', 'green peppers', 'extra cheese')</code>
</pre>
<p>
<code>import</code>
 语句中的星号让Python将模块<code>pizza</code>
 中的每个函数都复制到这个程序文件中。由于导入了每个函数，可通过名称来调用每个函数，而无须使用句点表示法。然而，使用并非自己编写的大型模块时，最好不要采用这种导入方法。这是因为如果模块中有函数的名称与当前项目中使用的名称相同，可能导致意想不到的结果：Python可能遇到多个名称相同的函数或变量，进而覆盖函数，而不是分别导入所有的函数。</p>
<p>最佳的做法是，要么只导入需要使用的函数，要么导入整个模块并使用句点表示法。这让代码更清晰，更容易阅读和理解。这里之所以介绍这种导入方法，只是想让你在阅读别人编写的代码时，能够理解类似于下面的<code>import</code>
 语句：</p>
<pre class="代码无行号"><code>from module_name import *</code>
</pre>
<h2 id="nav_point_188">8.7　函数编写指南</h2>
<p>编写函数时，需要牢记几个细节。应给函数指定描述性名称，且只在其中使用小写字母和下划线。描述性名称可帮助你和别人明白代码想要做什么。给模块命名时也应遵循上述约定。</p>
<p>每个函数都应包含简要地阐述其功能的注释。该注释应紧跟在函数定义后面，并采用文档字符串格式。文档良好的函数让其他程序员只需阅读文档字符串中的描述就能够使用它。他们完全可以相信代码如描述的那样运行，并且只要知道函数的名称、需要的实参以及返回值的类型，就能在自己的程序中使用它。</p>
<p>给形参指定默认值时，等号两边不要有空格：</p>
<pre class="代码无行号"><code>def function_name(parameter_0, parameter_1='default value')</code>
</pre>
<p>对于函数调用中的关键字实参，也应遵循这种约定：</p>
<pre class="代码无行号"><code>function_name(value_0, parameter_1='value')</code>
</pre>
<p>PEP 8建议代码行的长度不要超过79字符，这样只要编辑器窗口适中，就能看到整行代码。如果形参很多，导致函数定义的长度超过了79字符，可在函数定义中输入左括号后按回车键，并在下一行按两次Tab键，从而将形参列表和只缩进一层的函数体区分开来。</p>
<p>大多数编辑器会自动对齐后续参数列表行，使其缩进程度与你给第一个参数列表行指定的缩进程度相同：</p>
<pre class="代码无行号"><code>def function_name(
        parameter_0, parameter_1, parameter_2,
        parameter_3, parameter_4, parameter_5):
function body...</code>
</pre>
<p>如果程序或模块包含多个函数，可使用两个空行将相邻的函数分开，这样将更容易知道前一个函数在什么地方结束，下一个函数从什么地方开始。</p>
<p>所有<code>import</code>
 语句都应放在文件开头。唯一例外的情形是，在文件开头使用了注释来描述整个程序。</p>
<blockquote>
<p>
<strong>动手试一试</strong>
</p>
<p>
<strong>练习8-15：打印模型</strong>
 　将示例printing_models.py中的函数放在一个名为printing_functions.py的文件中。在printing_models.py的开头编写一条<code>import</code>
 语句，并修改该文件以使用导入的函数。</p>
<p>
<strong>练习8-16：导入　</strong>
 选择一个你编写的且只包含一个函数的程序，将该函数放在另一个文件中。在主程序文件中，使用下述各种方法导入这个函数，再调用它：</p>
<pre class="代码无行号"><code>import module_name
from module_name import function_name
from module_name import function_name as fn
import module_name as mn
from module_name import *</code>
</pre>
<p>
<strong>练习8-17：函数编写指南</strong>
 　选择你在本章中编写的三个程序，确保它们遵循了本节介绍的函数编写指南。</p>
</blockquote>
<h2 id="nav_point_189">8.8　小结</h2>
<p>在本章中，你学习了：如何编写函数，以及如何传递实参，让函数能够访问完成其工作所需的信息；如何使用位置实参和关键字实参，以及如何接受任意数量的实参；显示输出的函数和返回值的函数；如何将函数同列表、字典、<code>if</code>
 语句和<code>while</code>
 循环结合起来使用；如何将函数存储在称为<strong>模块</strong>
 的独立文件中，让程序文件更简单、更易于理解。最后，你学习了函数编写指南，遵循这些指南可让程序始终结构良好，并对你和其他人来说易于阅读。</p>
<p>程序员的目标之一是，编写简单的代码来完成任务，而函数有助于你实现这样的目标。它们让你编写好代码块并确定其能够正确运行后，就可置之不理。确定函数能够正确地完成其工作后，你就可以接着投身于下一个编码任务。</p>
<p>函数让你编写代码一次后，想重用它们多少次就重用多少次。需要运行函数中的代码时，只需编写一行函数调用代码，就可让函数完成其工作。需要修改函数的行为时，只需修改一个代码块，而所做的修改将影响调用这个函数的每个地方。</p>
<p>使用函数让程序更容易阅读，而良好的函数名概述了程序各个部分的作用。相对于阅读一系列的代码块，阅读一系列函数调用让你能够更快地明白程序的作用。</p>
<p>函数还让代码更容易测试和调试。如果程序使用一系列的函数来完成其任务，而其中的每个函数都完成一项具体的工作，测试和维护起来将容易得多：可编写分别调用每个函数的程序，并测试每个函数是否在它可能遇到的各种情形下都能正确地运行。经过这样的测试后你就能充满信心，深信每次调用这些函数时，它们都将正确地运行。</p>
<p>在第9章，你将学习编写类。类将函数和数据整洁地封装起来，让你能够灵活而高效地使用它们。</p>
<br style="page-break-after:always"/>
<div></div>
]]></description>
    <pubDate>Mon, 13 Jul 2026 01:48:30 +0000</pubDate>
    <guid isPermaLink="false">8</guid>
</item>
<item>
    <title>Basic Python - 007 - 第 7 章 用户输入和while循环.epub</title>
    <description><![CDATA[<p style='color: #888;'>⏱ 预计阅读时间：74 分钟 (14702 字)</p><hr><h1 id="nav_point_144">第 7 章　用户输入和<code>while</code>
 循环</h1>
<blockquote>
<p><img alt="" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/7_Image00001.gif" style="width: 16%" width="16%"/>
 　大多数程序旨在解决最终用户的问题，为此通常需要从用户那里获取一些信息。例如，假设有人要判断自己是否到了投票年龄。要编写回答这个问题的程序，就需要知道用户的年龄，才能给出答案。因此，这种程序需要让用户<strong>输入</strong>
 年龄，再将其与投票年龄进行比较，以判断用户是否到了投票年龄，从而给出结果。</p>
<p>在本章中，你将学习如何接受用户输入，以便程序进行处理。程序需要一个名字时，你需要提示用户输入该名字；程序需要一个名单时，你需要提示用户输入一系列名字。为此，你将使用函数<code>input()</code>
 。</p>
<p>你还将学习如何让程序不断地运行，以便用户根据需要输入信息，并在程序中使用这些信息。为此，你将使用<code>while</code>
 循环让程序不断运行，直到指定的条件不满足为止。</p>
<p>通过获取用户输入并学会控制程序的运行时间，你就能编写出交互式程序。</p>
</blockquote>
<h2 id="nav_point_145">7.1　函数<code>input()</code>
 的工作原理</h2>
<p>函数<code>input()</code>
 让程序暂停运行，等待用户输入一些文本。获取用户输入后，Python将其赋给一个变量，以方便你使用。</p>
<p>例如，下面的程序让用户输入一些文本，再将这些文本呈现给用户：</p>
<p>
<em>parrot.py</em>
</p>
<pre class="代码无行号"><code>message = input("Tell me something, and I will repeat it back to you: ")
print(message)</code>
</pre>
<p>函数<code>input()</code>
 接受一个参数——要向用户显示的<strong>提示</strong>
 （prompt）或说明，让用户知道该如何做。在本例中，Python运行第一行代码时，用户将看到提示<code>Tell me something, and I will repeat it back to you:</code>
 。程序等待用户输入，并在用户按回车键后继续运行。输入被赋给变量<code>message</code>
 ，接下来的<code>print(message)</code>
 将输入呈现给用户：</p>
<pre class="代码无行号"><code>Tell me something, and I will repeat it back to you: Hello everyone!
Hello everyone!</code>
</pre>
<blockquote>
<p>
<strong>注意</strong>
 　Sublime Text等众多编辑器不能运行提示用户输入的程序。你可以使用Sublime Text来编写提示用户输入的程序，但必须从终端运行它们。详情请参阅1.5节。</p>
</blockquote>
<h3 id="nav_point_146">7.1.1　编写清晰的程序</h3>
<p>每当使用函数<code>input()</code>
 时，都应指定清晰易懂的提示，准确地指出希望用户提供什么样的信息——指出用户应该输入何种信息的任何提示都行，如下所示：</p>
<p>
<em>greeter.py</em>
</p>
<pre class="代码无行号"><code>name = input("Please enter your name: ")
print(f"\nHello, {name}!")</code>
</pre>
<p>通过在提示末尾（这里是冒号后面）包含一个空格，可将提示与用户输入分开，让用户清楚地知道其输入始于何处，如下所示：</p>
<pre class="代码无行号"><code>Please enter your name: Eric
Hello, Eric!</code>
</pre>
<p>有时候，提示可能超过一行。例如，你可能需要指出获取特定输入的原因。在这种情况下，可将提示赋给一个变量，再将该变量传递给函数<code>input()</code>
 。这样，即便提示超过一行，<code>input()</code>
 语句也会非常清晰。</p>
<p>
<em>greeter.py</em>
</p>
<pre class="代码无行号"><code>prompt = "If you tell us who you are, we can personalize the messages you see."
prompt += "\nWhat is your first name? "
name = input(prompt)
print(f"\nHello, {name}!")</code>
</pre>
<p>本例演示了一种创建多行字符串的方式。第一行将消息的前半部分赋给变量<code>prompt</code>
 中。在第二行中，运算符<code>+=</code>
 在前面赋给变量<code>prompt</code>
 的字符串末尾附加一个字符串。</p>
<p>最终的提示占据两行，且问号后面有一个空格，这也是为了使其更加清晰：</p>
<pre class="代码无行号"><code>If you tell us who you are, we can personalize the messages you see.
What is your first name? Eric
Hello, Eric!</code>
</pre>
<h3 id="nav_point_147">7.1.2　使用<code>int()</code>
 来获取数值输入</h3>
<p>使用函数<code>input()</code>
 时，Python将用户输入解读为字符串。请看下面让用户输入年龄的解释器会话：</p>
<pre class="代码无行号"><code>&gt;&gt;&gt; age = input("How old are you? ")
How old are you? 21
&gt;&gt;&gt; age
'21'</code>
</pre>
<p>用户输入的是数21，但我们请求Python提供变量<code>age</code>
 的值时，它返回的是<code>'21'</code>
 ——用户输入数值的字符串表示。我们怎么知道Python将输入解读成了字符串呢？因为这个数用引号括起了。如果只想打印输入，这一点问题都没有；但如果试图将输入作为数来使用，就会引发错误：</p>
<pre class="代码无行号"><code>  &gt;&gt;&gt; age = input("How old are you? ")
  How old are you? 21
❶ &gt;&gt;&gt; age &gt;= 18
  Traceback (most recent call last):
    File "&lt;stdin&gt;", line 1, in &lt;module&gt;
❷ TypeError: unorderable types: str() &gt;= int()</code>
</pre>
<p>试图将输入用于数值比较时（见❶），Python会引发错误，因为它无法将字符串和整数进行比较：不能将赋给<code>age</code>
 的字符串<code>'21'</code>
 与数值<code>18</code>
 进行比较（见❷）。</p>
<p>为解决这个问题，可使用函数<code>int()</code>
 ，它让Python将输入视为数值。函数<code>int()</code>
 将数的字符串表示转换为数值表示，如下所示：</p>
<pre class="代码无行号"><code>  &gt;&gt;&gt; age = input("How old are you? ")
  How old are you? 21
❶ &gt;&gt;&gt; age = int(age)
  &gt;&gt;&gt; age &gt;= 18
  True</code>
</pre>
<p>在本例中，用户根据提示输入<code>21</code>
 后，Python将这个数解读为字符串，但随后<code>int()</code>
 将这个字符串转换成了数值表示（见❶）。这样Python就能运行条件测试了：将变量<code>age</code>
 （它现在表示的是数值21）同<code>18</code>
 进行比较，看它是否大于或等于18。测试结果为<code>True</code>
 。</p>
<p>如何在实际程序中使用函数<code>int()</code>
 呢？请看下面的程序，它判断一个人是否满足坐过山车的身高要求：</p>
<p>
<em>rollercoaster.py</em>
</p>
<pre class="代码无行号"><code>height = input("How tall are you, in inches? ")
height = int(height)
if height &gt;= 48:
    print("\nYou're tall enough to ride!")
else:
    print("\nYou'll be able to ride when you're a little older.")</code>
</pre>
<p>在此程序中，为何可以将<code>height</code>
 同<code>48</code>
 进行比较呢？因为在比较前，<code>height = int(height)</code>
 将输入转换成了数值表示。如果输入的数大于或等于48，就指出用户满足身高条件：</p>
<pre class="代码无行号"><code>How tall are you, in inches? 71
You're tall enough to ride!</code>
</pre>
<p>将数值输入用于计算和比较前，务必将其转换为数值表示。</p>
<h3 id="nav_point_148">7.1.3　求模运算符</h3>
<p>处理数值信息时，<strong>求模运算符</strong>
 （%）是个很有用的工具，它将两个数相除并返回余数：</p>
<pre class="代码无行号"><code>&gt;&gt;&gt; 4 % 3
1
&gt;&gt;&gt; 5 % 3
2
&gt;&gt;&gt; 6 % 3
0
&gt;&gt;&gt; 7 % 3
1</code>
</pre>
<p>求模运算符不会指出一个数是另一个数的多少倍，只指出余数是多少。</p>
<p>如果一个数可被另一个数整除，余数就为0，因此求模运算将返回0。可利用这一点来判断一个数是奇数还是偶数：</p>
<p>
<em>even_or_odd.py</em>
</p>
<pre class="代码无行号"><code>number = input("Enter a number, and I'll tell you if it's even or odd: ")
number = int(number)
if number % 2 == 0:
    print(f"\nThe number {number} is even.")
else:
    print(f"\nThe number {number} is odd.")</code>
</pre>
<p>偶数都能被2整除，因此如果对一个数和2执行求模运算的结果为0，即<code>number % 2 == 0</code>
 ，那么这个数就是偶数；否则就是奇数。</p>
<pre class="代码无行号"><code>Enter a number, and I'll tell you if it's even or odd: 42
The number 42 is even.</code>
</pre>
<blockquote>
<p>
<strong>动手试一试</strong>
</p>
<p>
<strong>练习7-1：汽车租赁</strong>
 　编写一个程序，询问用户要租赁什么样的汽车，并打印一条消息，下面是一个例子。</p>
<p>Let me see if I can find you a Subaru.</p>
<p>
<strong>练习7-2：餐馆订位</strong>
 　编写一个程序，询问用户有多少人用餐。如果超过8位，就打印一条消息，指出没有空桌；否则指出有空桌。</p>
<p>
<strong>练习7-3：10的整数倍</strong>
 　让用户输入一个数，并指出该数是否是10的整数倍。</p>
</blockquote>
<h2 id="nav_point_149">7.2　<code>while</code>
 循环简介</h2>
<p>
<code>for</code>
 循环用于针对集合中的每个元素都执行一个代码块，而<code>while</code>
 循环则不断运行，直到指定的条件不满足为止。</p>
<h3 id="nav_point_150">7.2.1　使用<code>while</code>
 循环</h3>
<p>可使用<code>while</code>
 循环来数数。例如，下面的<code>while</code>
 循环从1数到5：</p>
<p>
<em>counting.py</em>
</p>
<pre class="代码无行号"><code>current_number = 1
while current_number &lt;= 5:
    print(current_number)
    current_number += 1</code>
</pre>
<p>在第一行，将<code>1</code>
 赋给变量<code>current_number</code>
 ，从而指定从1开始数。将接下来的<code>while</code>
 循环设置成：只要<code>current_number</code>
 小于或等于5，就接着运行这个循环。循环中的代码打印<code>current_number</code>
 的值，再使用代码<code>current_number += 1</code>
 （代码<code>current_number = current_number + 1</code>
 的简写）将其值加1。</p>
<p>只要满足条件<code>current_number &lt;= 5</code>
 ，Python就接着运行这个循环。因为1小于5，所以Python打印<code>1</code>
 并将<code>current_number</code>
 加1，使其为2；因为2小于5，所以Python打印<code>2</code>
 并将<code>current_number</code>
 加1，使其为3；依此类推。一旦<code>current_number</code>
 大于5，循环就将停止，整个程序也将结束：</p>
<pre class="代码无行号"><code>1
2
3
4
5</code>
</pre>
<p>你每天使用的程序很可能就包含<code>while</code>
 循环。例如，游戏使用<code>while</code>
 循环，确保在玩家想玩时不断运行，并在玩家想退出时停止运行。如果程序在用户没有让它停止时停止运行，或者在用户要退出时还继续运行，那就太没有意思了。因此，<code>while</code>
 循环很有用。</p>
<h3 id="nav_point_151">7.2.2　让用户选择何时退出</h3>
<p>可以使用<code>while</code>
 循环让程序在用户愿意时不断运行，如下面的程序parrot.py所示。我们在其中定义了一个<strong>退出值</strong>
 ，只要用户输入的不是这个值，程序就将接着运行：</p>
<p>
<em>parrot.py</em>
</p>
<pre class="代码无行号"><code>❶ prompt = "\nTell me something, and I will repeat it back to you:"
  prompt += "\nEnter 'quit' to end the program. "
❷ message = ""
❸ while message != 'quit':
      message = input(prompt)
      print(message)</code>
</pre>
<p>❶处定义了一条提示消息，告诉用户有两个选择：要么输入一条消息，要么输入退出值（这里为<code>'quit'</code>
 ）。接下来，创建变量<code>message</code>
 （见❷），用于记录用户输入的值。我们将变量<code>message</code>
 的初始值设置为空字符串<code>""</code>
 ，让Python首次执行<code>while</code>
 代码行时有可供检查的东西。Python首次执行<code>while</code>
 语句时，需要将<code>message</code>
 的值与<code>'quit'</code>
 进行比较，但此时用户还没有输入。如果没有可供比较的东西，Python将无法继续运行程序。为解决这个问题，必须给变量<code>message</code>
 指定初始值。虽然这个初始值只是一个空字符串，但符合要求，能够让Python执行<code>while</code>
 循环所需的比较。只要<code>message</code>
 的值不是<code>'quit'</code>
 ，这个循环（见❸）就会不断运行。</p>
<p>首次遇到这个循环时，<code>message</code>
 是一个空字符串，因此Python进入该循环。执行到代码行<code>message = input(prompt)</code>
 时，Python显示提示消息，并等待用户输入。不管用户输入是什么，都将赋给变量<code>message</code>
 并打印出来。接下来，Python重新检查<code>while</code>
 语句中的条件。只要用户输入的不是单词<code>'quit'</code>
 ，Python就会再次显示提示消息并等待用户输入。等到用户终于输入<code>'quit'</code>
 后，Python停止执行<code>while</code>
 循环，整个程序也到此结束：</p>
<pre class="代码无行号"><code>Tell me something, and I will repeat it back to you:
Enter 'quit' to end the program. Hello everyone!
Hello everyone!
Tell me something, and I will repeat it back to you:
Enter 'quit' to end the program. Hello again.
Hello again.
Tell me something, and I will repeat it back to you:
Enter 'quit' to end the program. quit
quit</code>
</pre>
<p>这个程序很好，唯一美中不足的是，它将单词<code>'quit'</code>
 也作为一条消息打印了出来。为修复这种问题，只需使用一个简单的<code>if</code>
 测试：</p>
<pre class="代码无行号"><code>prompt = "\nTell me something, and I will repeat it back to you:"
prompt += "\nEnter 'quit' to end the program. "
message = ""
while message != 'quit':
    message = input(prompt)
    if message != 'quit':
        print(message)</code>
</pre>
<p>现在，程序在显示消息前将做简单的检查，仅在消息不是退出值时才打印它：</p>
<pre class="代码无行号"><code>Tell me something, and I will repeat it back to you:
Enter 'quit' to end the program. Hello everyone!
Hello everyone!
Tell me something, and I will repeat it back to you:
Enter 'quit' to end the program. Hello again.
Hello again.
Tell me something, and I will repeat it back to you:
Enter 'quit' to end the program. quit</code>
</pre>
<h3 id="nav_point_152">7.2.3　使用标志</h3>
<p>在前一个示例中，我们让程序在满足指定条件时执行特定的任务。但在更复杂的程序中，很多不同的事件会导致程序停止运行。在这种情况下，该怎么办呢？</p>
<p>例如，有多种事件可能导致游戏结束，如玩家失去所有飞船、时间已用完，或者要保护的城市被全部摧毁。导致程序结束的事件有很多时，如果在一条<code>while</code>
 语句中检查所有这些条件，将既复杂又困难。</p>
<p>在要求很多条件都满足才继续运行的程序中，可定义一个变量，用于判断整个程序是否处于活动状态。这个变量称为<strong>标志</strong>
 （flag），充当程序的交通信号灯。可以让程序在标志为<code>True</code>
 时继续运行，并在任何事件导致标志的值为<code>False</code>
 时让程序停止运行。这样，在<code>while</code>
 语句中就只需检查一个条件：标志的当前值是否为<code>True</code>
 。然后将所有其他测试（是否发生了应将标志设置为<code>False</code>
 的事件）都放在其他地方，从而让程序更整洁。</p>
<p>下面在前一节的程序parrot.py中添加一个标志。将其命名为<code>active</code>
 （你可给它指定任何名称），用于判断程序是否应继续运行：</p>
<pre class="代码无行号"><code>  prompt = "\nTell me something, and I will repeat it back to you:"
  prompt += "\nEnter 'quit' to end the program. "
❶ active = True
❷ while active:
      message = input(prompt)
❸     if message == 'quit':
          active = False
❹     else:
          print(message)</code>
</pre>
<p>将变量<code>active</code>
 设置为<code>True</code>
 （见❶），让程序最初处于活动状态。这样做简化了<code>while</code>
 语句，因为不需要在其中做任何比较——相关的逻辑由程序的其他部分处理。只要变量<code>active</code>
 为<code>True</code>
 ，循环就将继续运行（见❷）。</p>
<p>在<code>while</code>
 循环中，在用户输入后使用一条<code>if</code>
 语句来检查变量<code>message</code>
 的值。如果用户输入的是<code>'quit'</code>
 （见❸），就将变量<code>active</code>
 设置为<code>False</code>
 。这将导致<code>while</code>
 循环不再继续执行。如果用户输入的不是<code>'quit'</code>
 （见❹），就将输入作为一条消息打印出来。</p>
<p>这个程序的输出与前一个示例相同。前一个示例将条件测试直接放在了<code>while</code>
 语句中，而这个程序则使用一个标志来指出程序是否处于活动状态。这样，如果要添加测试（如<code>elif</code>
 语句）以检查是否发生了其他导致<code>active</code>
 变为<code>False</code>
 的事件，就会很容易。在复杂的程序（如很多事件会导致程序停止运行的游戏）中，标志很有用：在任意一个事件导致活动标志变成<code>False</code>
 时，主游戏循环将退出，此时可显示一条游戏结束消息，并让用户选择是否要重新玩。</p>
<h3 id="nav_point_153">7.2.4　使用<code>break</code>
 退出循环</h3>
<p>要立即退出<code>while</code>
 循环，不再运行循环中余下的代码，也不管条件测试的结果如何，可使用<code>break</code>
 语句。<code>break</code>
 语句用于控制程序流程，可用来控制哪些代码行将执行、哪些代码行不执行，从而让程序按你的要求执行你要执行的代码。</p>
<p>例如，来看一个让用户指出他到过哪些地方的程序。在这个程序中，可在用户输入<code>'quit'</code>
 后使用<code>break</code>
 语句立即退出<code>while</code>
 循环：</p>
<p>
<em>cities.py</em>
</p>
<pre class="代码无行号"><code>  prompt = "\nPlease enter the name of a city you have visited:"
  prompt += "\n(Enter 'quit' when you are finished.) "
❶ while True:
      city = input(prompt)
      if city == 'quit':
          break
      else:
          print(f"I'd love to go to {city.title()}!")</code>
</pre>
<p>以<code>while True</code>
 （见❶）打头的循环将不断运行，直到遇到<code>break</code>
 语句。这个程序中的循环不断让用户输入他到过的城市的名字，直到用户输入<code>'quit'</code>
 为止。用户输入<code>'quit'</code>
 后，将执行<code>break</code>
 语句，导致Python退出循环：</p>
<pre class="代码无行号"><code>Please enter the name of a city you have visited:
(Enter 'quit' when you are finished.) New York
I'd love to go to New York!
Please enter the name of a city you have visited:
(Enter 'quit' when you are finished.) San Francisco
I'd love to go to San Francisco!
Please enter the name of a city you have visited:
(Enter 'quit' when you are finished.) quit</code>
</pre>
<blockquote>
<p>
<strong>注意</strong>
 　在任何Python循环中都可使用<code>break</code>
 语句。例如，可使用<code>break</code>
 语句来退出遍历列表或字典的<code>for</code>
 循环。</p>
</blockquote>
<h3 id="nav_point_154">7.2.5　在循环中使用<code>continue</code>
</h3>
<p>要返回循环开头，并根据条件测试结果决定是否继续执行循环，可使用<code>continue</code>
 语句，它不像<code>break</code>
 语句那样不再执行余下的代码并退出整个循环。例如，来看一个从1数到10但只打印其中奇数的循环：</p>
<p>
<em>counting.py</em>
</p>
<pre class="代码无行号"><code>  current_number = 0
  while current_number &lt; 10:
❶     current_number += 1
      if current_number % 2 == 0:
          continue
      print(current_number)</code>
</pre>
<p>首先将<code>current_number</code>
 设置为0，由于它小于10，Python进入<code>while</code>
 循环。进入循环后，以步长1的方式往上数（见❶），因此<code>current_number</code>
 为1。接下来，<code>if</code>
 语句检查<code>current_number</code>
 与2的求模运算结果。如果结果为0（意味着<code>current_number</code>
 可被2整除），就执行<code>continue</code>
 语句，让Python忽略余下的代码，并返回循环的开头。如果当前的数不能被2整除，就执行循环中余下的代码，将这个数打印出来：</p>
<pre class="代码无行号"><code>1
3
5
7
9</code>
</pre>
<h3 id="nav_point_155">7.2.6　避免无限循环</h3>
<p>每个<code>while</code>
 循环都必须有停止运行的途径，这样才不会没完没了地执行下去。例如，下面的循环从1数到5：</p>
<p>
<em>counting.py</em>
</p>
<pre class="代码无行号"><code>x = 1
while x &lt;= 5:
    print(x)
    x += 1</code>
</pre>
<p>但如果像下面这样不小心遗漏了代码行<code>x += 1</code>
 ，这个循环将没完没了地运行：</p>
<pre class="代码无行号"><code># 这个循环将没完没了地运行！
x = 1
while x &lt;= 5:
    print(x)</code>
</pre>
<p>在这里，<code>x</code>
 的初始值为<code>1</code>
 ，但根本不会变。因此条件测试<code>x &lt;= 5</code>
 始终为<code>True</code>
 ，导致<code>while</code>
 循环没完没了地打印<code>1</code>
 ，如下所示：</p>
<pre class="代码无行号"><code>1
1
1
1
--snip--</code>
</pre>
<p>每个程序员都会偶尔因不小心而编写出无限循环，在循环的退出条件比较微妙时尤其如此。如果程序陷入无限循环，可按Ctrl + C，也可关闭显示程序输出的终端窗口。</p>
<p>要避免编写无限循环，务必对每个<code>while</code>
 循环进行测试，确保其按预期那样结束。如果你希望程序在用户输入特定值时结束，可运行程序并输入这样的值。如果在这种情况下程序没有结束，请检查程序处理这个值的方式，确认程序至少有一个这样的地方能让循环条件为<code>False</code>
 ，或者让<code>break</code>
 语句得以执行。</p>
<blockquote>
<p>
<strong>注意</strong>
 　Sublime Text等一些编辑器内嵌了输出窗口，这可能导致难以结束无限循环，不得不通过关闭编辑器来结束。在这种情况下，可在输出窗口中单击鼠标，再按Ctrl + C，这样应该能够结束无限循环。</p>
</blockquote>
<p>　</p>
<blockquote>
<p>
<strong>动手试一试</strong>
</p>
<p>
<strong>练习7-4：比萨配料</strong>
 　编写一个循环，提示用户输入一系列比萨配料，并在用户输入<code>'quit'</code>
 时结束循环。每当用户输入一种配料后，都打印一条消息，指出我们会在比萨中添加这种配料。</p>
<p>
<strong>练习7-5：电影票</strong>
 　有家电影院根据观众的年龄收取不同的票价：不到3岁的观众免费；3～12岁的观众收费10美元；超过12岁的观众收费15美元。请编写一个循环，在其中询问用户的年龄，并指出其票价。</p>
<p>
<strong>练习7-6：三种出路</strong>
 　以不同的方式完成练习7-4或练习7-5，在程序中采取如下做法。</p>
<ul>
<li class="第2级无序列表">在<code>while</code>
 循环中使用条件测试来结束循环。</li>
<li class="第2级无序列表">使用变量<code>active</code>
 来控制循环结束的时机。</li>
<li class="第2级无序列表">使用<code>break</code>
 语句在用户输入<code>'quit'</code>
 时退出循环。</li>
</ul>
<p>
<strong>练习7-7：无限循环</strong>
 　编写一个没完没了的循环，并运行它（要结束该循环，可按Ctrl + C，也可关闭显示输出的窗口）。</p>
</blockquote>
<h2 id="nav_point_156">7.3　使用<code>while</code>
 循环处理列表和字典</h2>
<p>到目前为止，我们每次都只处理了一项用户信息：获取用户的输入，再将输入打印出来或做出应答；循环再次运行时，获悉另一个输入值并做出响应。然而，要记录大量的用户和信息，需要在<code>while</code>
 循环中使用列表和字典。</p>
<p>
<code>for</code>
 循环是一种遍历列表的有效方式，但不应在<code>for</code>
 循环中修改列表，否则将导致Python难以跟踪其中的元素。要在遍历列表的同时对其进行修改，可使用<code>while</code>
 循环。通过将<code>while</code>
 循环同列表和字典结合起来使用，可收集、存储并组织大量输入，供以后查看和显示。</p>
<h3 id="nav_point_157">7.3.1　在列表之间移动元素</h3>
<p>假设有一个列表包含新注册但还未验证的网站用户。验证这些用户后，如何将他们移到另一个已验证用户列表中呢？一种办法是使用一个<code>while</code>
 循环，在验证用户的同时将其从未验证用户列表中提取出来，再将其加入另一个已验证用户列表中。代码可能类似于下面这样：</p>
<p>
<em>confirmed_users.py</em>
</p>
<pre class="代码无行号"><code>  # 首先，创建一个待验证用户列表
  # 和一个用于存储已验证用户的空列表。
❶ unconfirmed_users = ['alice', 'brian', 'candace']
  confirmed_users = []
  # 验证每个用户，直到没有未验证用户为止。
  # 将每个经过验证的用户都移到已验证用户列表中。
❷ while unconfirmed_users:
❸     current_user = unconfirmed_users.pop()
      print(f"Verifying user: {current_user.title()}")
❹     confirmed_users.append(current_user)
  # 显示所有已验证的用户。
  print("\nThe following users have been confirmed:")
  for confirmed_user in confirmed_users:
      print(confirmed_user.title())</code>
</pre>
<p>首先创建一个未验证用户列表（见❶），其中包含用户Alice、Brian和Candace，还创建了一个空列表，用于存储已验证的用户。❷处的<code>while</code>
 循环将不断运行，直到列表<code>unconfirmed_users</code>
 变成空的。在此循环中，❸处的方法<code>pop()</code>
 以每次一个的方式从列表<code>unconfirmed_users</code>
 末尾删除未验证的用户。由于Candace位于列表<code>unconfirmed_users</code>
 末尾，其名字将首先被删除、赋给变量<code>current_user</code>
 并加入列表<code>confirmed_users</code>
 中（见❹）。接下来是Brian，然后是Alice。</p>
<p>为模拟用户验证过程，我们打印一条验证消息并将用户加入已验证用户列表中。未验证用户列表越来越短，而已验证用户列表越来越长。未验证用户列表为空后结束循环，再打印已验证用户列表：</p>
<pre class="代码无行号"><code>Verifying user: Candace
Verifying user: Brian
Verifying user: Alice
The following users have been confirmed:
Candace
Brian
Alice</code>
</pre>
<h3 id="nav_point_158">7.3.2　删除为特定值的所有列表元素</h3>
<p>在第3章中，我们使用函数<code>remove()</code>
 来删除列表中的特定值。这之所以可行，是因为要删除的值只在列表中出现一次。如果要删除列表中所有为特定值的元素，该怎么办呢？</p>
<p>假设你有一个宠物列表，其中包含多个值为<code>'cat'</code>
 的元素。要删除所有这些元素，可不断运行一个<code>while</code>
 循环，直到列表中不再包含值<code>'cat'</code>
 ，如下所示：</p>
<p>
<em>pets.py</em>
</p>
<pre class="代码无行号"><code>pets = ['dog', 'cat', 'dog', 'goldfish', 'cat', 'rabbit', 'cat']
print(pets)
while 'cat' in pets:
    pets.remove('cat')
print(pets)</code>
</pre>
<p>首先创建一个列表，其中包含多个值为<code>'cat'</code>
 的元素。打印这个列表后，Python进入<code>while</code>
 循环，因为它发现<code>'cat'</code>
 在列表中至少出现了一次。进入该循环后，Python删除第一个<code>'cat'</code>
 并返回到<code>while</code>
 代码行，然后发现<code>'cat'</code>
 还包含在列表中，因此再次进入循环。它不断删除<code>'cat'</code>
 ，直到这个值不再包含在列表中，然后退出循环并再次打印列表：</p>
<pre class="代码无行号"><code>['dog', 'cat', 'dog', 'goldfish', 'cat', 'rabbit', 'cat']
['dog', 'dog', 'goldfish', 'rabbit']</code>
</pre>
<h3 id="nav_point_159">7.3.3　使用用户输入来填充字典</h3>
<p>可使用<code>while</code>
 循环提示用户输入任意多的信息。下面创建一个调查程序，其中的循环每次执行时都提示输入被调查者的名字和回答。我们将收集的数据存储在一个字典中，以便将回答同被调查者关联起来：</p>
<p>
<em>mountain_poll.py</em>
</p>
<pre class="代码无行号"><code>  responses = {}
  # 设置一个标志，指出调查是否继续。
  polling_active = True
  while polling_active:
      # 提示输入被调查者的名字和回答。
❶     name = input("\nWhat is your name? ")
      response = input("Which mountain would you like to climb someday? ")
      # 将回答存储在字典中。
❷     responses[name] = response
      # 看看是否还有人要参与调查。
❸     repeat = input("Would you like to let another person respond? (yes/ no) ")
      if repeat == 'no':
          polling_active = False
  # 调查结束，显示结果。
  print("\n--- Poll Results ---")
❹ for name, response in responses.items():
      print(f"{name} would like to climb {response}.")</code>
</pre>
<p>这个程序首先定义了一个空字典（<code>responses</code>
 ），并设置了一个标志（<code>polling_active</code>
 ）用于指出调查是否继续。只要<code>polling_active</code>
 为<code>True</code>
 ，Python就运行<code>while</code>
 循环中的代码。</p>
<p>在这个循环中，提示用户输入其名字及其喜欢爬哪座山（见❶）。将这些信息存储在字典<code>responses</code>
 中（见❷），然后询问用户是否继续调查（见❸）。如果用户输入<code>yes</code>
 ，程序将再次进入<code>while</code>
 循环；如果用户输入<code>no</code>
 ，标志<code>polling_active</code>
 将被设置为<code>False</code>
 ，而<code>while</code>
 循环将就此结束。最后一个代码块（见❹）显示调查结果。</p>
<p>如果运行这个程序，并输入一些名字和回答，输出将类似于下面这样：</p>
<pre class="代码无行号"><code>What is your name? Eric
Which mountain would you like to climb someday? Denali
Would you like to let another person respond? (yes/ no) yes
What is your name? Lynn
Which mountain would you like to climb someday? Devil's Thumb
Would you like to let another person respond? (yes/ no) no
--- Poll Results ---
Eric would like to climb Denali.
Lynn would like to climb Devil's Thumb.</code>
</pre>
<blockquote>
<p>
<strong>动手试一试</strong>
</p>
<p>
<strong>练习7-8：熟食店</strong>
 　创建一个名为<code>sandwich_orders</code>
 的列表，在其中包含各种三明治的名字，再创建一个名为<code>finished_sandwiches</code>
 的空列表。遍历列表<code>sandwich_orders</code>
 ，对于其中的每种三明治，都打印一条消息，如<code>I made your tuna sandwich</code>
 ，并将其移到列表<code>finished_sandwiches</code>
 中。所有三明治都制作好后，打印一条消息，将这些三明治列出来。</p>
<p>
<strong>练习7-9：五香烟熏牛肉卖完了</strong>
 　使用为完成练习7-8而创建的列表<code>sandwich_orders</code>
 ，并确保<code>'pastrami'</code>
 在其中至少出现了三次。在程序开头附近添加这样的代码：打印一条消息，指出熟食店的五香烟熏牛肉（pastrami）卖完了；再使用一个<code>while</code>
 循环将列表<code>sandwich_orders</code>
 中的<code>'pastrami'</code>
 都删除。确认最终的列表<code>finished_sandwiches</code>
 未包含<code>'pastrami'</code>
 。</p>
<p>
<strong>练习7-10：梦想的度假胜地</strong>
 　编写一个程序，调查用户梦想的度假胜地。使用类似于下面的提示，并编写一个打印调查结果的代码块。</p>
<p>If you could visit one place in the world, where would you go?</p>
</blockquote>
<h2 id="nav_point_160">7.4　小结</h2>
<p>在本章中，你学习了：如何在程序中使用<code>input()</code>
 来让用户提供信息；如何处理文本和数的输入，以及如何使用<code>while</code>
 循环让程序按用户的要求不断运行；多种控制<code>while</code>
 循环流程的方式：设置活动标志、使用<code>break</code>
 语句以及使用<code>continue</code>
 语句；如何使用<code>while</code>
 循环在列表之间移动元素，以及如何从列表中删除所有包含特定值的元素；如何结合使用<code>while</code>
 循环和字典。</p>
<p>在第8章中，你将学习<strong>函数</strong>
 。函数让你能够将程序分成多个很小的部分，每部分都负责完成一项具体任务。你可以根据需要调用同一个函数任意次，还可将函数存储在独立的文件中。使用函数可让你编写的代码效率更高、更容易维护和排除故障，还可在众多不同的程序中重用。</p>
<br style="page-break-after:always"/>
<div></div>
]]></description>
    <pubDate>Sat, 11 Jul 2026 01:43:48 +0000</pubDate>
    <guid isPermaLink="false">7</guid>
</item>
<item>
    <title>Basic Python - 006 - 第 6 章 字典.epub</title>
    <description><![CDATA[<p style='color: #888;'>⏱ 预计阅读时间：99 分钟 (19635 字)</p><hr><h1 id="nav_point_124">第 6 章　字典</h1>
<blockquote>
<p><img alt="" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/6_Image00001.gif" style="width: 16%" width="16%"/>
 　在本章中，你将学习能够将相关信息关联起来的Python字典，以及如何访问和修改字典中的信息。字典可存储的信息量几乎不受限制，因此我们会演示如何遍历字典中的数据。另外，你还将学习存储字典的列表、存储列表的字典和存储字典的字典。</p>
<p>理解字典后，就能够更准确地为各种真实物体建模。你可以创建一个表示人的字典，然后想在其中存储多少信息就存储多少信息：姓名、年龄、地址、职业，以及能描述他的任何方面。你还能够存储任意两种相关的信息，如一系列单词及其含义，一系列人名及其喜欢的数，以及一系列山脉及其海拔，等等。</p>
</blockquote>
<h2 id="nav_point_125">6.1　一个简单的字典</h2>
<p>来看一个包含外星人的游戏，这些外星人的颜色和分数各不相同。下面是一个简单的字典，存储了有关特定外星人的信息：</p>
<p>
<em>alien.py</em>
</p>
<pre class="代码无行号"><code>alien_0 = {'color': 'green', 'points': 5}
print(alien_0['color'])
print(alien_0['points'])</code>
</pre>
<p>字典<code>alien_0</code>
 存储了外星人的颜色和分数。最后两行代码访问并显示这些信息，结果如下：</p>
<pre class="代码无行号"><code>green
5</code>
</pre>
<p>与大多数编程概念一样，要熟练使用字典，也需要一段时间的练习。使用字典一段时间后，你就会明白为何它们能够高效地模拟现实世界中的情形。</p>
<h2 id="nav_point_126">6.2　使用字典</h2>
<p>在Python中，<strong>字典</strong>
 是一系列<strong>键值对</strong>
 。每个<strong>键</strong>
 都与一个值相关联，你可使用键来访问相关联的值。与键相关联的值可以是数、字符串、列表乃至字典。事实上，可将任何Python对象用作字典中的值。</p>
<p>在Python中，字典用放在花括号（<code>{}</code>
 ）中的一系列键值对表示，如前面的示例所示：</p>
<pre class="代码无行号"><code>alien_0 = {'color': 'green', 'points': 5}</code>
</pre>
<p>
<strong>键值对</strong>
 是两个相关联的值。指定键时，Python将返回与之相关联的值。键和值之间用冒号分隔，而键值对之间用逗号分隔。在字典中，想存储多少个键值对都可以。</p>
<p>最简单的字典只有一个键值对，如下述修改后的字典<code>alien_0</code>
 所示：</p>
<pre class="代码无行号"><code>alien_0 = {'color': 'green'}</code>
</pre>
<p>这个字典只存储了一项有关<code>alien_0</code>
 的信息，具体地说是这个外星人的颜色。在该字典中，字符串<code>'color'</code>
 是一个键，与之相关联的值为<code>'green'</code>
 。</p>
<h3 id="nav_point_127">6.2.1　访问字典中的值</h3>
<p>要获取与键相关联的值，可依次指定字典名和放在方括号内的键，如下所示：</p>
<p>
<em>alien.py</em>
</p>
<pre class="代码无行号"><code>alien_0 = {'color': 'green'}
print(alien_0['color'])</code>
</pre>
<p>这将返回字典<code>alien_0</code>
 中与键<code>'color'</code>
 相关联的值：</p>
<pre class="代码无行号"><code>green</code>
</pre>
<p>字典中可包含任意数量的键值对。例如，下面是最初的字典<code>alien_0</code>
 ，其中包含两个键值对：</p>
<pre class="代码无行号"><code>alien_0 = {'color': 'green', 'points': 5}</code>
</pre>
<p>现在，你可访问外星人<code>alien_0</code>
 的颜色和分数。如果玩家射杀了这个外星人，就可以使用下面的代码来确定应获得多少分：</p>
<pre class="代码无行号"><code>  alien_0 = {'color': 'green', 'points': 5}
❶ new_points = alien_0['points']
❷ print(f"You just earned {new_points} points!")</code>
</pre>
<p>上述代码首先定义了一个字典。然后，从这个字典中获取与键<code>'points'</code>
 相关联的值（见❶），并将这个值赋给变量<code>new_points</code>
 。接下来，将这个整数转换为字符串，并打印一条消息，指出玩家获得了多少分（见❷）：</p>
<pre class="代码无行号"><code>You just earned 5 points!</code>
</pre>
<p>如果在外星人被射杀时运行这段代码，就将获取该外星人的分数。</p>
<h3 id="nav_point_128">6.2.2　添加键值对</h3>
<p>字典是一种动态结构，可随时在其中添加键值对。要添加键值对，可依次指定字典名、用方括号括起的键和相关联的值。</p>
<p>下面来在字典<code>alien_0</code>
 中添加两项信息：外星人的<img alt="x" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/6_Image00003.gif"/>
 坐标和<img alt="y" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/6_Image00004.gif"/>
 坐标，让我们能够在屏幕的特定位置显示该外星人。我们将这个外星人放在屏幕左边缘，且离屏幕顶部25像素的地方。由于屏幕坐标系的原点通常为左上角，要将该外星人放在屏幕左边缘，可将<img alt="x" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/6_Image00003.gif"/>
 坐标设置为0；要将该外星人放在离屏幕顶部25像素的地方，可将<img alt="y" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/6_Image00004.gif"/>
 坐标设置为25，如下所示：</p>
<p>
<em>alien.py</em>
</p>
<pre class="代码无行号"><code>  alien_0 = {'color': 'green', 'points': 5}
  print(alien_0)
❶ alien_0['x_position'] = 0
❷ alien_0['y_position'] = 25
  print(alien_0)</code>
</pre>
<p>首先定义前面一直在使用的字典，然后打印这个字典，以显示其信息快照。在❶处，我们在这个字典中新增了一个键值对，其中的键为<code>'x_position'</code>
 ，值为0。在❷处重复这样的操作，但使用的键为<code>'y_position'</code>
 。打印修改后的字典时，将看到这两个新增的键值对：</p>
<pre class="代码无行号"><code>{'color': 'green', 'points': 5}
{'color': 'green', 'points': 5, 'y_position': 25, 'x_position': 0}</code>
</pre>
<p>这个字典的最终版本包含四个键值对：原来的两个指定外星人的颜色和分数，而新增的两个指定其位置。</p>
<blockquote>
<p>
<strong>注意</strong>
 　在Python 3.7中，字典中元素的排列顺序与定义时相同。如果将字典打印出来或遍历其元素，将发现元素的排列顺序与添加顺序相同。</p>
</blockquote>
<h3 id="nav_point_129">6.2.3　先创建一个空字典</h3>
<p>在空字典中添加键值对有时候可提供便利，而有时候必须这样做。为此，可先使用一对空花括号定义一个字典，再分行添加各个键值对。例如，下面演示了如何以这种方式创建字典<code>alien_0</code>
 ：</p>
<p>
<em>alien.py</em>
</p>
<pre class="代码无行号"><code>alien_0 = {}
alien_0['color'] = 'green'
alien_0['points'] = 5
print(alien_0)</code>
</pre>
<p>这里首先定义了空字典<code>alien_0</code>
 ，再在其中添加颜色和分数，得到前述示例一直在使用的字典：</p>
<pre class="代码无行号"><code>{'color': 'green', 'points': 5}</code>
</pre>
<p>使用字典来存储用户提供的数据或在编写能自动生成大量键值对的代码时，通常需要先定义一个空字典。</p>
<h3 id="nav_point_130">6.2.4　修改字典中的值</h3>
<p>要修改字典中的值，可依次指定字典名、用方括号括起的键，以及与该键相关联的新值。例如，假设随着游戏的进行，需要将一个外星人从绿色改为黄色：</p>
<p>
<em>alien.py</em>
</p>
<pre class="代码无行号"><code>alien_0 = {'color': 'green'}
print(f"The alien is {alien_0['color']}.")
alien_0['color'] = 'yellow'
print(f"The alien is now {alien_0['color']}.")</code>
</pre>
<p>首先定义一个表示外星人<code>alien_0</code>
 的字典，其中只包含这个外星人的颜色。接下来，将与键<code>'color'</code>
 相关联的值改为<code>'yellow'</code>
 。输出表明，这个外星人确实从绿色变成了黄色：</p>
<pre class="代码无行号"><code>The alien is green.
The alien is now yellow.</code>
</pre>
<p>来看一个更有趣的例子，对一个能够以不同速度移动的外星人进行位置跟踪。为此，我们将存储该外星人的当前速度，并据此确定该外星人将向右移动多远：</p>
<pre class="代码无行号"><code>  alien_0 = {'x_position': 0, 'y_position': 25, 'speed': 'medium'}
  print(f"Original position: {alien_0['x_position']}")
  # 向右移动外星人。
  # 根据当前速度确定将外星人向右移动多远。
❶ if alien_0['speed'] == 'slow':
      x_increment = 1
  elif alien_0['speed'] == 'medium':
      x_increment = 2
  else:
      # 这个外星人的移动速度肯定很快。
      x_increment = 3
  # 新位置为旧位置加上移动距离。
❷ alien_0['x_position'] = alien_0['x_position'] + x_increment
  print(f"New position: {alien_0['x_position']}")</code>
</pre>
<p>首先定义一个外星人，其中包含初始<img alt="x" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/6_Image00003.gif"/>
 坐标和<img alt="y" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/6_Image00004.gif"/>
 坐标，还有速度<code>'medium'</code>
 。出于简化考虑，省略了颜色和分数，但即便包含这些键值对，本例的工作原理也不会有任何变化。我们还打印了<code>x_position</code>
 的初始值，旨在让用户知道这个外星人向右移动了多远。</p>
<p>❶处使用一个<code>if-elif-else</code>
 结构来确定外星人应向右移动多远，并将这个值赋给变量<code>x_increment</code>
 。如果外星人的速度为<code>'slow'</code>
 ，它将向右移动一个单位；如果速度为<code>'medium'</code>
 ，将向右移动两个单位；如果为<code>'fast'</code>
 ，将向右移动三个单位。确定移动距离后，将其与<code>x_position</code>
 的当前值相加（见❷），再将结果关联到字典中的键<code>x_position</code>
 。</p>
<p>因为这是一个速度中等的外星人，所以其位置将向右移两个单位：</p>
<pre class="代码无行号"><code>Original x-position: 0
New x-position: 2</code>
</pre>
<p>这种技术很棒：通过修改外星人字典中的值，可改变外星人的行为。例如，要将这个速度中等的外星人变成速度很快的外星人，可添加如下代码行：</p>
<pre class="代码无行号"><code>alien_0['speed'] = 'fast'</code>
</pre>
<p>这样，再次运行这些代码时，其中的<code>if-elif-else</code>
 结构将把一个更大的值赋给变量<code>x_increment</code>
 。</p>
<h3 id="nav_point_131">6.2.5　删除键值对</h3>
<p>对于字典中不再需要的信息，可使用<code>del</code>
 语句将相应的键值对彻底删除。使用<code>del</code>
 语句时，必须指定字典名和要删除的键。</p>
<p>例如，下面的代码从字典<code>alien_0</code>
 中删除键<code>'points'</code>
 及其值：</p>
<p>
<em>alien.py</em>
</p>
<pre class="代码无行号"><code>  alien_0 = {'color': 'green', 'points': 5}
  print(alien_0)
❶ del alien_0['points']
  print(alien_0)</code>
</pre>
<p>❶处的代码行让Python将键<code>'points'</code>
 从字典<code>alien_0</code>
 中删除，同时删除与这个键相关联的值。输出表明，键<code>'points'</code>
 及其值<code>5</code>
 已从字典中删除，但其他键值对未受影响：</p>
<pre class="代码无行号"><code>{'color': 'green', 'points': 5}
{'color': 'green'}</code>
</pre>
<blockquote>
<p>
<strong>注意</strong>
 　删除的键值对会永远消失。</p>
</blockquote>
<h3 id="nav_point_132">6.2.6　由类似对象组成的字典</h3>
<p>在前面的示例中，字典存储的是一个对象（游戏中的一个外星人）的多种信息，但你也可以使用字典来存储众多对象的同一种信息。例如，假设你要调查很多人，询问他们最喜欢的编程语言，可使用一个字典来存储这种简单调查的结果，如下所示：</p>
<pre class="代码无行号"><code>favorite_languages = {
    'jen': 'python',
    'sarah': 'c',
    'edward': 'ruby',
    'phil': 'python',
    }</code>
</pre>
<p>如你所见，我们将一个较大的字典放在了多行中。每个键都是一个被调查者的名字，而每个值都是被调查者喜欢的语言。确定需要使用多行来定义字典时，要在输入左花括号后按回车键。在下一行缩进四个空格，指定第一个键值对，并在它后面加上一个逗号。此后再按回车键时，文本编辑器将自动缩进后续键值对，且缩进量与第一个键值对相同。</p>
<p>定义好字典后，在最后一个键值对的下一行添加一个右花括号，并缩进四个空格，使其与字典中的键对齐。一种不错的做法是，在最后一个键值对后面也加上逗号，为以后在下一行添加键值对做好准备。</p>
<blockquote>
<p>
<strong>注意</strong>
 　对于较长的列表和字典，大多数编辑器提供了以类似方式设置格式的功能。对于较长的字典，还有其他一些可行的格式设置方式，因此在你的编辑器或其他源代码中，你可能会看到稍微不同的格式设置方式。</p>
</blockquote>
<p>给定被调查者的名字，可使用这个字典轻松地获悉他喜欢的语言：</p>
<p>
<em>favorite_languages.py</em>
</p>
<pre class="代码无行号"><code>  favorite_languages = {
      'jen': 'python',
      'sarah': 'c',
      'edward': 'ruby',
      'phil': 'python',
      }
❶ language = favorite_languages['sarah'].title()
  print(f"Sarah's favorite language is {language}.")</code>
</pre>
<p>为获悉Sarah喜欢的语言，我们使用如下代码：</p>
<pre class="代码无行号"><code>favorite_languages['sarah']</code>
</pre>
<p>在❶处，使用这种语法获取Sarah喜欢的语言，并将其赋给变量<code>language</code>
 。创建这个新变量让函数调用<code>print()</code>
 变得整洁得多。输出指出了Sarah喜欢的语言：</p>
<pre class="代码无行号"><code>Sarah's favorite language is C.</code>
</pre>
<p>这种语法可用来从字典中获取任何人喜欢的语言。</p>
<h3 id="nav_point_133">6.2.7　使用<code>get()</code>
 来访问值</h3>
<p>使用放在方括号内的键从字典中获取感兴趣的值时，可能会引发问题：如果指定的键不存在就会出错。</p>
<p>如果你要求获取外星人的分数，而这个外星人没有分数，结果将如何呢？下面来看一看：</p>
<p>
<em>alien_no_points.py</em>
</p>
<pre class="代码无行号"><code>alien_0 = {'color': 'green', 'speed': 'slow'}
print(alien_0['points'])</code>
</pre>
<p>这将导致Python显示traceback，指出存在键值错误（<code>KeyError</code>
 ）：</p>
<pre class="代码无行号"><code>Traceback (most recent call last):
  File "alien_no_points.py", line 2, in &lt;module&gt;
    print(alien_0['points'])
KeyError: 'points'</code>
</pre>
<p>第10章将详细介绍如何处理类似的错误，但就字典而言，可使用方法<code>get()</code>
 在指定的键不存在时返回一个默认值，从而避免这样的错误。</p>
<p>方法<code>get()</code>
 的第一个参数用于指定键，是必不可少的；第二个参数为指定的键不存在时要返回的值，是可选的：</p>
<pre class="代码无行号"><code>alien_0 = {'color': 'green', 'speed': 'slow'}
point_value = alien_0.get('points', 'No point value assigned.')
print(point_value)</code>
</pre>
<p>如果字典中有键<code>'points'</code>
 ，将获得与之相关联的值；如果没有，将获得指定的默认值。虽然这里没有键<code>'points'</code>
 ，但将获得一条清晰的消息，不会引发错误：</p>
<pre class="代码无行号"><code>No point value assigned.</code>
</pre>
<p>如果指定的键有可能不存在，应考虑使用方法<code>get()</code>
 ，而不要使用方括号表示法。</p>
<blockquote>
<p>
<strong>注意</strong>
 　调用<code>get()</code>
 时，如果没有指定第二个参数且指定的键不存在，Python将返回值<code>None</code>
 。这个特殊值表示没有相应的值。<code>None</code>
 并非错误，而是一个表示所需值不存在的特殊值，第8章将介绍它的其他用途。</p>
</blockquote>
<p>　</p>
<blockquote>
<p>
<strong>动手试一试</strong>
</p>
<p>
<strong>练习6-1：人</strong>
 　使用一个字典来存储一个熟人的信息，包括名、姓、年龄和居住的城市。该字典应包含键<code>first_name</code>
 、<code>last_name</code>
 、<code>age</code>
 和<code>city</code>
 。将存储在该字典中的每项信息都打印出来。</p>
<p>
<strong>练习6-2：喜欢的数</strong>
 　使用一个字典来存储一些人喜欢的数。请想出5个人的名字，并将这些名字用作字典中的键；找出每个人喜欢的一个数，并将这些数作为值存储在字典中。打印每个人的名字和喜欢的数。为了让这个程序更有趣，通过询问朋友确保数据是真实的。</p>
<p>
<strong>练习6-3：词汇表</strong>
 　Python字典可用于模拟现实生活中的字典。为避免混淆，我们将后者称为词汇表。</p>
<ul>
<li class="第2级无序列表">想出你在前面学过的5个编程术语，将其用作词汇表中的键，并将它们的含义作为值存储在词汇表中。</li>
<li class="第2级无序列表">以整洁的方式打印每个术语及其含义。为此，可先打印术语，在它后面加上一个冒号，再打印其含义；也可在一行打印术语，再使用换行符（<code>\n</code>
 ）插入一个空行，然后在下一行以缩进的方式打印其含义。</li>
</ul>
</blockquote>
<h2 id="nav_point_134">6.3　遍历字典</h2>
<p>一个Python字典可能只包含几个键值对，也可能包含数百万个键值对。鉴于字典可能包含大量数据，Python支持对字典进行遍历。字典可用于以各种方式存储信息，因此有多种遍历方式：可遍历字典的所有键值对，也可仅遍历键或值。</p>
<h3 id="nav_point_135">6.3.1　遍历所有键值对</h3>
<p>探索各种遍历方法前，先来看一个新字典，它用于存储有关网站用户的信息。下面的字典存储一名用户的用户名、名和姓：</p>
<pre class="代码无行号"><code>user_0 = {
    'username': 'efermi',
    'first': 'enrico',
    'last': 'fermi',
    }</code>
</pre>
<p>利用本章前面介绍过的知识，可访问<code>user_0</code>
 的任何一项信息，但如果要获悉该用户字典中的所有信息，该如何办呢？可使用<code>for</code>
 循环来遍历这个字典：</p>
<p>
<em>user.py</em>
</p>
<pre class="代码无行号"><code>  user_0 = {
      'username': 'efermi',
      'first': 'enrico',
      'last': 'fermi',
      }
❶ for key, value in user_0.items():
❷     print(f"\nKey: {key}")
❸     print(f"Value: {value}")</code>
</pre>
<p>如❶所示，要编写遍历字典的<code>for</code>
 循环，可声明两个变量，用于存储键值对中的键和值。这两个变量可以使用任意名称。下面的代码使用了简单的变量名，这完全可行：</p>
<pre class="代码无行号"><code>for k, v in user_0.items()</code>
</pre>
<p>
<code>for</code>
 语句的第二部分包含字典名和方法<code>items()</code>
 （见❶），它返回一个键值对列表。接下来，<code>for</code>
 循环依次将每个键值对赋给指定的两个变量。在本例中，使用这两个变量来打印每个键（见❷）及其相关联的值（见❸）。第一个函数调用<code>print()</code>
 中的<code>"\n"</code>
 确保在输出每个键值对前都插入一个空行：</p>
<pre class="代码无行号"><code>Key: username
Value: efermi
Key: first
Value: enrico
Key: last
Value: fermi</code>
</pre>
<p>在6.2.6节的示例favorite_languages.py中，字典存储的是不同人的同一种信息。对于类似这样的字典，遍历所有的键值对很合适。如果遍历字典<code>favorite_languages</code>
 ，将得到其中每个人的姓名和喜欢的编程语言。由于该字典中的键都是人名，值都是语言，因此在循环中使用变量<code>name</code>
 和<code>language</code>
 ，而不是<code>key</code>
 和<code>value</code>
 。这让人更容易明白循环的作用：</p>
<p>
<em>favorite_languages.py</em>
</p>
<pre class="代码无行号"><code>  favorite_languages = {
      'jen': 'python',
      'sarah': 'c',
      'edward': 'ruby',
      'phil': 'python',
      }
❶ for name, language in favorite_languages.items():
❷     print(f"{name.title()}'s favorite language is {language.title()}.")</code>
</pre>
<p>❶处的代码让Python遍历字典中的每个键值对，并将键赋给变量<code>name</code>
 ，将值赋给变量<code>language</code>
 。这些描述性名称能够让人非常轻松地明白函数调用<code>print()</code>
 （见❷）是做什么的。</p>
<p>仅使用几行代码，就将全部调查结果显示出来了：</p>
<pre class="代码无行号"><code>Jen's favorite language is Python.
Sarah's favorite language is C.
Edward's favorite language is Ruby.
Phil's favorite language is Python.</code>
</pre>
<p>即便字典存储的是上千乃至上百万人的调查结果，这种循环也管用。</p>
<h3 id="nav_point_136">6.3.2　遍历字典中的所有键</h3>
<p>在不需要使用字典中的值时，方法<code>keys()</code>
 很有用。下面来遍历字典<code>favorite_languages</code>
 ，并将每个被调查者的名字都打印出来：</p>
<pre class="代码无行号"><code>  favorite_languages = {
      'jen': 'python',
      'sarah': 'c',
      'edward': 'ruby',
      'phil': 'python',
      }
❶ for name in favorite_languages.keys():
      print(name.title())</code>
</pre>
<p>❶处的代码行让Python提取字典<code>favorite_languages</code>
 中的所有键，并依次将它们赋给变量<code>name</code>
 。输出列出了每个被调查者的名字：</p>
<pre class="代码无行号"><code>Jen
Sarah
Edward
Phil</code>
</pre>
<p>遍历字典时，会默认遍历所有的键。因此，如果将上述代码中的：</p>
<pre class="代码无行号"><code>for name in favorite_languages:</code>
</pre>
<p>替换为：</p>
<pre class="代码无行号"><code>for name in favorite_languages.keys():</code>
</pre>
<p>输出将不变。</p>
<p>显式地使用方法<code>keys()</code>
 可让代码更容易理解，你可以选择这样做，但是也可以省略它。</p>
<p>在这种循环中，可使用当前键来访问与之相关联的值。下面来打印两条消息，指出两位朋友喜欢的语言。像前面一样遍历字典中的名字，但在名字为指定朋友的名字时，打印一条消息，指出其喜欢的语言：</p>
<pre class="代码无行号"><code>  favorite_languages = {
      --snip--
      }
❶ friends = ['phil', 'sarah']
  for name in favorite_languages.keys():
      print(f"Hi {name.title()}.")
❷     if name in friends:
❸         language = favorite_languages[name].title()
          print(f"\t{name.title()}, I see you love {language}!")</code>
</pre>
<p>❶处创建了一个列表，其中包含要收到打印消息的朋友。在循环中，打印每个人的名字，并检查当前的名字是否在列表<code>friends</code>
 中（见❷）。如果在，就打印一句特殊的问候语，其中包含这位朋友喜欢的语言。为获悉朋友喜欢的语言，我们使用了字典名，并将变量<code>name</code>
 的当前值作为键（见❸）。</p>
<p>每个人的名字都会被打印，但只对朋友打特殊消息：</p>
<pre class="代码无行号"><code>Hi Jen.
Hi Sarah.
    Sarah, I see you love C!
Hi Edward.
Hi Phil.
    Phil, I see you love Python!</code>
</pre>
<p>还可使用方法<code>keys()</code>
 确定某个人是否接受了调查。下面的代码确定Erin是否接受了调查：</p>
<pre class="代码无行号"><code>  favorite_languages = {
      'jen': 'python',
      'sarah': 'c',
      'edward': 'ruby',
      'phil': 'python',
      }
❶ if 'erin' not in favorite_languages.keys():
      print("Erin, please take our poll!")</code>
</pre>
<p>方法<code>keys()</code>
 并非只能用于遍历：实际上，它返回一个列表，其中包含字典中的所有键。因此❶处的代码行只核实<code>'erin'</code>
 是否包含在这个列表中。因为她并不包含在这个列表中，所以打印一条消息，邀请她参加调查：</p>
<pre class="代码无行号"><code>Erin, please take our poll!</code>
</pre>
<h3 id="nav_point_137">6.3.3　按特定顺序遍历字典中的所有键</h3>
<p>从Python 3.7起，遍历字典时将按插入的顺序返回其中的元素。不过在有些情况下，你可能要按与此不同的顺序遍历字典。</p>
<p>要以特定顺序返回元素，一种办法是在<code>for</code>
 循环中对返回的键进行排序。为此，可使用函数<code>sorted()</code>
 来获得按特定顺序排列的键列表的副本：</p>
<pre class="代码无行号"><code>favorite_languages = {
    'jen': 'python',
    'sarah': 'c',
    'edward': 'ruby',
    'phil': 'python',
    }
for name in sorted(favorite_languages.keys()):
    print(f"{name.title()}, thank you for taking the poll.")</code>
</pre>
<p>这条<code>for</code>
 语句类似于其他<code>for</code>
 语句，不同之处是对方法<code>dictionary.keys()</code>
 的结果调用了函数<code>sorted()</code>
 。这让Python列出字典中的所有键，并在遍历前对这个列表进行排序。输出表明，按顺序显示了所有被调查者的名字：</p>
<pre class="代码无行号"><code>Edward, thank you for taking the poll.
Jen, thank you for taking the poll.
Phil, thank you for taking the poll.
Sarah, thank you for taking the poll.</code>
</pre>
<h3 id="nav_point_138">6.3.4　遍历字典中的所有值</h3>
<p>如果主要对字典包含的值感兴趣，可使用方法<code>values()</code>
 来返回一个值列表，不包含任何键。例如，假设我们想获得一个列表，其中只包含被调查者选择的各种语言，而不包含被调查者的名字，可以这样做：</p>
<pre class="代码无行号"><code>favorite_languages = {
    'jen': 'python',
    'sarah': 'c',
    'edward': 'ruby',
    'phil': 'python',
    }
print("The following languages have been mentioned:")
for language in favorite_languages.values():
    print(language.title())</code>
</pre>
<p>这条<code>for</code>
 语句提取字典中的每个值，并将其依次赋给变量<code>language</code>
 。通过打印这些值，就获得了一个包含被调查者所选择语言的列表：</p>
<pre class="代码无行号"><code>The following languages have been mentioned:
Python
C
Ruby
Python</code>
</pre>
<p>这种做法提取字典中所有的值，而没有考虑是否重复。涉及的值很少时，这也许不是问题，但如果被调查者很多，最终的列表可能包含大量重复项。为剔除重复项，可使用集合（set）。<strong>集合</strong>
 中的每个元素都必须是独一无二的：</p>
<pre class="代码无行号"><code>  favorite_languages = {
      --snip--
      }
  print("The following languages have been mentioned:")
❶ for language in set(favorite_languages.values()):
      print(language.title())</code>
</pre>
<p>通过对包含重复元素的列表调用<code>set()</code>
 ，可让Python找出列表中独一无二的元素，并使用这些元素来创建一个集合。❶处使用<code>set()</code>
 来提取<code>favorite_languages.values()</code>
 中不同的语言。</p>
<p>结果是一个不重复的列表，其中列出了被调查者提及的所有语言：</p>
<pre class="代码无行号"><code>The following languages have been mentioned:
Python
C
Ruby</code>
</pre>
<p>随着你更深入地学习Python，经常会发现它内置的功能可帮助你以希望的方式处理数据。</p>
<blockquote>
<p>
<strong>注意</strong>
 　可使用一对花括号直接创建集合，并在其中用逗号分隔元素：</p>
<pre class="代码无行号"><code>&gt;&gt;&gt; languages = {'python', 'ruby', 'python', 'c'}
&gt;&gt;&gt; languages
{'ruby', 'python', 'c'}</code>
</pre>
<p>集合和字典很容易混淆，因为它们都是用一对花括号定义的。当花括号内没有键值对时，定义的很可能是集合。不同于列表和字典，集合不会以特定的顺序存储元素。</p>
</blockquote>
<p>　</p>
<blockquote>
<p>
<strong>动手试一试</strong>
</p>
<p>
<strong>练习6-4：词汇表2</strong>
 　现在你知道了如何遍历字典，可以整理为完成练习6-3而编写的代码，将其中的一系列函数调用<code>print()</code>
 替换为一个遍历字典中键和值的循环。确定该循环正确无误后，再在词汇表中添加5个Python术语。当你再次运行这个程序时，这些新术语及其含义将自动包含在输出中。</p>
<p>
<strong>练习6-5：河流</strong>
 　创建一个字典，在其中存储三条重要河流及其流经的国家。例如，一个键值对可能是<code>'nile': 'egypt'</code>
 。</p>
<ul>
<li>
<p>使用循环为每条河流打印一条消息，下面是一个例子。</p>
<p>The Nile runs through Egypt.<br/>
 </p>
</li>
<li>
<p>使用循环将该字典中每条河流的名字打印出来。</p>
</li>
<li class="第2级无序列表">使用循环将该字典包含的每个国家的名字打印出来。</li>
</ul>
<p>
<strong>练习6-6：调查</strong>
 　在6.3.1节编写的程序favorite_languages.py中执行以下操作。</p>
<ul>
<li class="第2级无序列表">创建一个应该会接受调查的人员名单，其中有些人已包含在字典中，而其他人未包含在字典中。</li>
<li class="第2级无序列表">遍历这个人员名单。对于已参与调查的人，打印一条消息表示感谢；对于还未参与调查的人，打印一条消息邀请他参加。</li>
</ul>
</blockquote>
<h2 id="nav_point_139">6.4　嵌套</h2>
<p>有时候，需要将一系列字典存储在列表中，或将列表作为值存储在字典中，这称为<strong>嵌套</strong>
 。你可以在列表中嵌套字典、在字典中嵌套列表甚至在字典中嵌套字典。正如下面的示例将演示的，嵌套是一项强大的功能。</p>
<h3 id="nav_point_140">6.4.1　字典列表</h3>
<p>字典<code>alien_0</code>
 包含一个外星人的各种信息，但无法存储第二个外星人的信息，更别说屏幕上全部外星人的信息了。如何管理成群结队的外星人呢？一种办法是创建一个外星人列表，其中每个外星人都是一个字典，包含有关该外星人的各种信息。例如，下面的代码创建一个包含三个外星人的列表：</p>
<p>
<em>aliens.py</em>
</p>
<pre class="代码无行号"><code>  alien_0 = {'color': 'green', 'points': 5}
  alien_1 = {'color': 'yellow', 'points': 10}
  alien_2 = {'color': 'red', 'points': 15}
❶ aliens = [alien_0, alien_1, alien_2]
  for alien in aliens:
      print(alien)</code>
</pre>
<p>首先创建三个字典，其中每个字典都表示一个外星人。然后在❶处将这些字典都存储到一个名为<code>aliens</code>
 的列表中。最后，遍历这个列表，并将每个外星人都打印出来：</p>
<pre class="代码无行号"><code>{'color': 'green', 'points': 5}
{'color': 'yellow', 'points': 10}
{'color': 'red', 'points': 15}</code>
</pre>
<p>更符合现实的情形是，外星人不止三个，且每个外星人都是使用代码自动生成的。在下面的示例中，使用<code>range()</code>
 生成了30个外星人：</p>
<pre class="代码无行号"><code>  # 创建一个用于存储外星人的空列表。
  aliens = []
  # 创建30个绿色的外星人。
❶ for alien_number in range(30):
❷     new_alien = {'color': 'green', 'points': 5, 'speed': 'slow'}
❸     aliens.append(new_alien)
  # 显示前5个外星人。
❹ for alien in aliens[:5]:
      print(alien)
  print("...")
  # 显示创建了多少个外星人。
❺ print(f"Total number of aliens: {len(aliens)}")</code>
</pre>
<p>在本例中，首先创建一个空列表，用于存储接下来将创建的所有外星人。在❶处，<code>range()</code>
 返回一系列数，其唯一的用途是告诉Python要重复这个循环多少次。每次执行这个循环时，都创建一个外星人（见❷），并将其附加到列表<code>aliens</code>
 末尾（见❸）。在❹处，使用一个切片来打印前5个外星人。在❺处，打印列表的长度，以证明确实创建了30个外星人：</p>
<pre class="代码无行号"><code>{'color': 'green', 'points': 5, 'speed': slow'}
{'color': 'green', 'points': 5, 'speed': slow'}
{'color': 'green', 'points': 5, 'speed': slow'}
{'color': 'green', 'points': 5, 'speed': slow'}
{'color': 'green', 'points': 5, 'speed': slow'}
...
Total number of aliens: 30</code>
</pre>
<p>这些外星人都具有相同的特征，但在Python看来，每个外星人都是独立的，这让我们能够独立地修改每个外星人。</p>
<p>在什么情况下需要处理成群结队的外星人呢？想象一下，可能随着游戏的进行，有些外星人会变色且加快移动速度。必要时，可使用<code>for</code>
 循环和<code>if</code>
 语句来修改某些外形人的颜色。例如，要将前三个外星人修改为黄色、速度为中等且值10分，可这样做：</p>
<pre class="代码无行号"><code># 创建一个用于存储外星人的空列表。
aliens = []
# 创建30个绿色的外星人。
for alien_number in range (30):
    new_alien = {'color': 'green', 'points': 5, 'speed': 'slow'}
    aliens.append(new_alien)
for alien in aliens[:3]:
    if alien['color'] == 'green':
        alien['color'] = 'yellow'
        alien['speed'] = 'medium'
        alien['points'] = 10
# 显示前5个外星人。
for alien in aliens[:5]:
    print(alien)
print("...")</code>
</pre>
<p>鉴于要修改前三个外星人，我们遍历一个只包含这些外星人的切片。当前，所有外星人都是绿色的，但情况并非总是如此，因此编写一条<code>if</code>
 语句来确保只修改绿色外星人。如果外星人是绿色的，就将其颜色改为<code>'yellow'</code>
 ，将其速度改为<code>'medium'</code>
 ，并将其分数改为<code>10</code>
 ，如下面的输出所示：</p>
<pre class="代码无行号"><code>{'color': 'yellow', 'points': 10, 'speed': 'medium'}
{'color': 'yellow', 'points': 10, 'speed': 'medium'}
{'color': 'yellow', 'points': 10, 'speed': 'medium'}
{'color': 'green', 'points': 5, 'speed': 'slow'}
{'color': 'green', 'points': 5, 'speed': 'slow'}
...</code>
</pre>
<p>可进一步扩展这个循环，在其中添加一个<code>elif</code>
 代码块，将黄色外星人改为移动速度快且值15分的红色外星人，如下所示（这里只列出了循环，而没有列出整个程序）：</p>
<pre class="代码无行号"><code>for alien in aliens[0:3]:
    if alien['color'] == 'green':
        alien['color'] = 'yellow'
        alien['speed'] = 'medium'
        alien['points'] = 10
    elif alien['color'] == 'yellow':
        alien['color'] = 'red'
        alien['speed'] = 'fast'
        alien['points'] = 15</code>
</pre>
<p>经常需要在列表中包含大量的字典，而其中每个字典都包含特定对象的众多信息。例如，你可能需要为网站的每个用户创建一个字典（就像6.3.1节的user.py中那样），并将这些字典存储在一个名为<code>users</code>
 的列表中。在这个列表中，所有字典的结构都相同，因此你可以遍历这个列表，并以相同的方式处理其中的每个字典。</p>
<h3 id="nav_point_141">6.4.2　在字典中存储列表</h3>
<p>有时候，需要将列表存储在字典中，而不是将字典存储在列表中。例如，你如何描述顾客点的比萨呢？如果使用列表，只能存储要添加的比萨配料；但如果使用字典，就不仅可在其中包含配料列表，还可包含其他有关比萨的描述。</p>
<p>在下面的示例中，存储了比萨的两方面信息：外皮类型和配料列表。配料列表是一个与键<code>'toppings'</code>
 相关联的值。要访问该列表，我们使用字典名和键<code>'toppings'</code>
 ，就像访问字典中的其他值一样。这将返回一个配料列表，而不是单个值：</p>
<p>
<em>pizza.py</em>
</p>
<pre class="代码无行号"><code>  # 存储所点比萨的信息。
❶ pizza = {
      'crust': 'thick',
      'toppings': ['mushrooms', 'extra cheese'],
      }
  # 概述所点的比萨。
❷ print(f"You ordered a {pizza['crust']}-crust pizza "
      "with the following toppings:")
❸ for topping in pizza['toppings']:
      print("\"f+topping)</code>
</pre>
<p>首先创建一个字典，其中存储了有关顾客所点比萨的信息（见❶）。在这个字典中，一个键是<code>'crust'</code>
 ，与之相关联的值是字符串<code>'thick'</code>
 ；下一个键是<code>'toppings'</code>
 ，与之相关联的值是一个列表，其中存储了顾客要求添加的所有配料。制作前，我们概述了顾客所点的比萨（见❷）。如果函数调用<code>print()</code>
 中的字符串很长，可以在合适的位置分行。只需要在每行末尾都加上引号，同时对于除第一行外的其他各行，都在行首加上引号并缩进。这样，Python将自动合并圆括号内的所有字符串。为打印配料，编写一个<code>for</code>
 循环（见❸）。为访问配料列表，使用键<code>'toppings'</code>
 ，这样Python将从字典中提取配料列表。</p>
<p>下面的输出概述了要制作的比萨：</p>
<pre class="代码无行号"><code>You ordered a thick-crust pizza with the following toppings:
    mushrooms
    extra cheese</code>
</pre>
<p>每当需要在字典中将一个键关联到多个值时，都可以在字典中嵌套一个列表。在本章前面有关喜欢的编程语言的示例中，如果将每个人的回答都存储在一个列表中，被调查者就可选择多种喜欢的语言。在这种情况下，当我们遍历字典时，与每个被调查者相关联的都是一个语言列表，而不是一种语言；因此，在遍历该字典的<code>for</code>
 循环中，我们需要再使用一个<code>for</code>
 循环来遍历与被调查者相关联的语言列表：</p>
<p>
<em>favorite_languages.py</em>
</p>
<pre class="代码无行号"><code>❶ favorite_languages = {
         'jen': ['python', 'ruby'],
      'sarah': ['c'],
      'edward': ['ruby', 'go'],
      'phil': ['python', 'haskell'],
      }
❷ for name, languages in favorite_languages.items():
      print(f"\n{name.title()}'s favorite languages are:")
❸     for language in languages:
          print(f"\t{language.title()}")</code>
</pre>
<p>如你所见，现在与每个名字相关联的值都是一个列表（见❶）。请注意，有些人喜欢的语言只有一种，而有些人有多种。遍历字典时（见❷），使用变量<code>languages</code>
 来依次存储对字典中每个值的引用，因为我们知道这些值都是列表。在遍历字典的主循环中，使用了另一个<code>for</code>
 循环（见❸）来遍历每个人喜欢的语言列表。现在，每个人想列出多少种喜欢的语言都可以：</p>
<pre class="代码无行号"><code>Jen's favorite languages are:
    Python
    Ruby
Sarah's favorite languages are:
    C
Edward's favorite languages are:
    Ruby
    Go
Phil's favorite languages are:
    Python
    Haskell</code>
</pre>
<p>为进一步改进这个程序，可在遍历字典的<code>for</code>
 循环开头添加一条<code>if</code>
 语句，通过查看<code>len(languages)</code>
 的值来确定当前的被调查者喜欢的语言是否有多种。如果他喜欢的语言有多种，就像以前一样显示输出；如果只有一种，就相应修改输出的措辞，如显示<code>Sarah's favorite language is C</code>
 。</p>
<blockquote>
<p>
<strong>注意</strong>
 　列表和字典的嵌套层级不应太多。如果嵌套层级比前面的示例多得多，很可能有更简单的解决方案。</p>
</blockquote>
<h3 id="nav_point_142">6.4.3　在字典中存储字典</h3>
<p>可在字典中嵌套字典，但这样做时，代码可能很快复杂起来。例如，如果有多个网站用户，每个都有独特的用户名，可在字典中将用户名作为键，然后将每位用户的信息存储在一个字典中，并将该字典作为与用户名相关联的值。在下面的程序中，存储了每位用户的三项信息：名、姓和居住地。为访问这些信息，我们遍历所有的用户名，并访问与每个用户名相关联的信息字典：</p>
<p>
<em>many_users.py</em>
</p>
<pre class="代码无行号"><code>  users = {
      'aeinstein': {
          'first': 'albert',
          'last': 'einstein',
          'location': 'princeton',
          },
      'mcurie': {
          'first': 'marie',
          'last': 'curie',
          'location': 'paris',
          },
      }
❶ for username, user_info in users.items():
❷     print(f"\nUsername: {username}")
❸     full_name = f"{user_info['first']} {user_info['last']}"
      location = user_info['location']
❹     print(f"\tFull name: {full_name.title()}")
      print(f"\tLocation: {location.title()}")</code>
</pre>
<p>首先定义一个名为<code>users</code>
 的字典，其中包含两个键：用户名<code>'aeinstein'</code>
 和<code>'mcurie'</code>
 。与每个键盘相关联的值都是一个字典，其中包含用户的名、姓和居住地。在❶处，遍历字典<code>users</code>
 ，让Python依次将每个键赋给变量<code>username</code>
 ，并依次将与当前键相关联的字典赋给变量<code>user_info</code>
 。在循环内部的❷处，将用户名打印出来。</p>
<p>在❸处，开始访问内部的字典。变量<code>user_info</code>
 包含用户信息字典，而该字典包含三个键：<code>'first'</code>
 、<code>'last'</code>
 和<code>'location'</code>
 。对于每位用户，都使用这些键来生成整洁的姓名和居住地，然后打印有关用户的简要信息（见❹）：</p>
<pre class="代码无行号"><code>Username: aeinstein
    Full name: Albert Einstein
    Location: Princeton
Username: mcurie
    Full name: Marie Curie
    Location: Paris</code>
</pre>
<p>请注意，表示每位用户的字典都具有相同的结构。虽然Python并没有这样的要求，但这使得嵌套的字典处理起来更容易。倘若表示每位用户的字典都包含不同的键，<code>for</code>
 循环内部的代码将更复杂。</p>
<blockquote>
<p>
<strong>动手试一试</strong>
</p>
<p>
<strong>练习6-7：人们</strong>
 　在为完成练习6-1而编写的程序中，再创建两个表示人的字典，然后将这三个字典都存储在一个名为<code>people</code>
 的列表中。遍历这个列表，将其中每个人的所有信息都打印出来。</p>
<p>
<strong>练习6-8：宠物</strong>
 　创建多个表示宠物的字典，每个字典都包含宠物的类型及其主人的名字。将这些字典存储在一个名为<code>pets</code>
 的列表中，再遍历该列表，并将有关每个宠物的所有信息都打印出来。</p>
<p>
<strong>练习6-9：喜欢的地方</strong>
 　创建一个名为<code>favorite_places</code>
 的字典。在这个字典中，将三个人的名字用作键，并存储每个人喜欢的1～3个地方。为了让这个练习更有趣些，可以让一些朋友说出他们喜欢的几个地方。遍历这个字典，并将其中每个人的名字及其喜欢的地方打印出来。</p>
<p>
<strong>练习6-10：喜欢的数2</strong>
 　修改为完成练习6-2而编写的程序，让每个人都可以有多个喜欢的数，然后将每个人的名字及其喜欢的数打印出来。</p>
<p>
<strong>练习6-11：城市</strong>
 　创建一个名为<code>cities</code>
 的字典，将三个城市名用作键。对于每座城市，都创建一个字典，并在其中包含该城市所属的国家、人口约数以及一个有关该城市的事实。在表示每座城市的字典中，应包含<code>country</code>
 、<code>population</code>
 和<code>fact</code>
 等键。将每座城市的名字以及有关信息都打印出来。</p>
<p>
<strong>练习6-12：扩展</strong>
 　本章的示例足够复杂，能以很多方式进行扩展。请对本章的一个示例进行扩展：添加键和值、调整程序要解决的问题或改进输出的格式。</p>
</blockquote>
<h2 id="nav_point_143">6.5　小结</h2>
<p>在本章中，你学习了：如何定义字典，以及如何使用存储在字典中的信息；如何访问和修改字典中的元素，以及如何遍历字典中的所有信息；如何遍历字典中所有的键值对、所有的键和所有的值；如何在列表中嵌套字典、在字典中嵌套列表以及在字典中嵌套字典。</p>
<p>在下一章中，你将学习<code>while</code>
 循环以及如何从用户那里获取输入。这是激动人心的一章，让你知道如何将程序变成交互性的：能够对用户输入做出响应。</p>
<br style="page-break-after:always"/>
<div></div>
]]></description>
    <pubDate>Fri, 10 Jul 2026 01:54:53 +0000</pubDate>
    <guid isPermaLink="false">6</guid>
</item>
<item>
    <title>Basic Python - 005 - 第 5 章 if语句.epub</title>
    <description><![CDATA[<p style='color: #888;'>⏱ 预计阅读时间：85 分钟 (16826 字)</p><hr><h1 id="nav_point_100">第 5 章　<code>if</code>
 语句</h1>
<blockquote>
<p><img alt="" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/5_Image00001.gif" style="width: 16%" width="16%"/>
 　编程时经常需要检查一系列条件，并据此决定采取什么措施。在Python中，<code>if</code>
 语句让你能够检查程序的当前状态，并采取相应的措施。</p>
<p>在本章中，你将学习条件测试，以检查所关心的任何条件。你将学习简单的<code>if</code>
 语句，以及创建一系列复杂的<code>if</code>
 语句来确定当前到底处于什么情形。接下来，你将把学到的知识应用于列表，编写一个<code>for</code>
 循环，以一种方式处理列表中的大多数元素，并以另一种方式处理包含特定值的元素。</p>
</blockquote>
<h2 id="nav_point_101">5.1　一个简单示例</h2>
<p>下面是一个简短的示例，演示了如何使用<code>if</code>
 语句来正确地处理特殊情形。假设你有一个汽车列表，并想将其中每辆汽车的名称打印出来。对于大多数汽车，应以首字母大写的方式打印其名称，但对于汽车名<code>'bmw'</code>
 ，应以全大写的方式打印。下面的代码遍历这个列表，并以首字母大写的方式打印其中的汽车名，不过对于<code>'bmw'</code>
 ，则以全大写的方式打印：</p>
<p>
<em>cars.py</em>
</p>
<pre class="代码无行号"><code>  cars = ['audi', 'bmw', 'subaru', 'toyota']
  for car in cars:
❶     if car == 'bmw':
          print(car.upper())
      else:
          print(car.title())</code>
</pre>
<p>这个示例中的循环首先检查当前的汽车名是否是<code>'bmw'</code>
 （见❶）。如果是，就以全大写方式打印，否则以首字母大写的方式打印：</p>
<pre class="代码无行号"><code>Audi
BMW
Subaru
Toyota</code>
</pre>
<p>这个示例涵盖了本章将介绍的很多概念。下面先来介绍可用来在程序中检查条件的测试。</p>
<h2 id="nav_point_102">5.2　条件测试</h2>
<p>每条<code>if</code>
 语句的核心都是一个值为<code>True</code>
 或<code>False</code>
 的表达式，这种表达式称为<strong>条件测试</strong>
 。Python根据条件测试的值为<code>True</code>
 还是<code>False</code>
 来决定是否执行<code>if</code>
 语句中的代码。如果条件测试的值为<code>True</code>
 ，Python就执行紧跟在<code>if</code>
 语句后面的代码；如果为<code>False</code>
 ，Python就忽略这些代码。</p>
<h3 id="nav_point_103">5.2.1　检查是否相等</h3>
<p>大多数条件测试将一个变量的当前值同特定值进行比较。最简单的条件测试检查变量的值是否与特定值相等：</p>
<pre class="代码无行号"><code>❶ &gt;&gt;&gt; car = 'bmw'
❷ &gt;&gt;&gt; car == 'bmw'
  True</code>
</pre>
<p>首先使用一个等号将<code>car</code>
 的值设置为<code>'bmw'</code>
 （见❶），这种做法你已经见过很多次。接下来，使用两个等号（<code>==</code>
 ）检查<code>car</code>
 的值是否为<code>'bmw'</code>
 （见❷）。这个<strong>相等运算符</strong>
 在两边的值相等时返回<code>True</code>
 ，否则返回<code>False</code>
 。在本例中，两边的值相等，因此Python返回<code>True</code>
 。</p>
<p>如果变量<code>car</code>
 的值不是<code>'bmw'</code>
 ，上述测试将返回<code>False</code>
 ：</p>
<pre class="代码无行号"><code>❶ &gt;&gt;&gt; car = 'audi'
❷ &gt;&gt;&gt; car == 'bmw'
  False</code>
</pre>
<p>一个等号是陈述，于是❶处的代码可解读为：将变量<code>car</code>
 的值设置为<code>'audi'</code>
 。两个等号则是发问，于是❷处的代码可解读为：变量<code>car</code>
 的值是<code>'bmw'</code>
 吗？大多数编程语言使用等号的方式与这里演示的相同。</p>
<h3 id="nav_point_104">5.2.2　检查是否相等时忽略大小写</h3>
<p>在Python中检查是否相等时区分大小写。例如，两个大小写不同的值被视为不相等：</p>
<pre class="代码无行号"><code>&gt;&gt;&gt; car = 'Audi'
&gt;&gt;&gt; car == 'audi'
False</code>
</pre>
<p>如果大小写很重要，这种行为有其优点。但如果大小写无关紧要，只想检查变量的值，可将变量的值转换为小写，再进行比较：</p>
<pre class="代码无行号"><code>&gt;&gt;&gt; car = 'Audi'
&gt;&gt;&gt; car.lower() == 'audi'
True</code>
</pre>
<p>无论值<code>'Audi'</code>
 的大小写如何，上述测试都将返回<code>True</code>
 ，因为该测试不区分大小写。函数<code>lower()</code>
 不会修改最初赋给变量<code>car</code>
 的值，因此进行这样的比较时不会影响原来的变量：</p>
<pre class="代码无行号"><code>❶ &gt;&gt;&gt; car = 'Audi'
❷ &gt;&gt;&gt; car.lower() == 'audi'
  True
❸ &gt;&gt;&gt; car
  'Audi'</code>
</pre>
<p>在❶处，将首字母大写的字符串<code>'Audi'</code>
 赋给变量<code>car</code>
 。在❷处，获取变量<code>car</code>
 的值并将其转换为小写，再将结果与字符串<code>'audi'</code>
 进行比较。这两个字符串相同，因此Python返回<code>True</code>
 。从❸处的输出可知，方法<code>lower()</code>
 并没有影响关联到变量<code>car</code>
 的值。</p>
<p>网站采用类似的方式让用户输入的数据符合特定的格式。例如，网站可能使用类似的测试来确保用户名是独一无二的，而并非只是与另一个用户名的大小写不同。用户提交新的用户名时，将把它转换为小写，并与所有既有用户名的小写版本进行比较。执行这种检查时，如果已经有用户名<code>'john'</code>
 （不管大小写如何），则用户提交用户名<code>'John'</code>
 时将遭到拒绝。</p>
<h3 id="nav_point_105">5.2.3　检查是否不相等</h3>
<p>要判断两个值是否不等，可结合使用惊叹号和等号（<code>!=</code>
 ），其中的惊叹号表示<strong>不</strong>
 ，其他很多编程语言中也是如此。</p>
<p>下面再使用一条<code>if</code>
 语句来演示如何使用不等运算符。我们将把要求的比萨配料赋给一个变量，再打印一条消息，指出顾客要求的配料是否是意式小银鱼（anchovies）：</p>
<p>
<em>toppings.py</em>
</p>
<pre class="代码无行号"><code>  requested_topping = 'mushrooms'
❸ if requested_topping != 'anchovies':
      print("Hold the anchovies!")</code>
</pre>
<p>❸处的代码行将<code>requested_topping</code>
 的值与<code>'anchovies'</code>
 进行比较。如果这两个值不相等，Python将返回<code>True</code>
 ，进而执行紧跟在<code>if</code>
 语句后面的代码；如果相等，Python将返回<code>False</code>
 ，因此不执行紧跟在<code>if</code>
 语句后面的代码。</p>
<p>因为<code>requested_topping</code>
 的值不是<code>'anchovies'</code>
 ，所以执行函数调用<code>print()</code>
 ：</p>
<pre class="代码无行号"><code>Hold the anchovies!</code>
</pre>
<p>你编写的大多数条件表达式检查两个值是否相等，但有时候检查两个值是否不等的效率更高。</p>
<h3 id="nav_point_106">5.2.4　数值比较</h3>
<p>检查数值非常简单。例如，下面的代码检查一个人是否是18岁：</p>
<pre class="代码无行号"><code>&gt;&gt;&gt; age = 18
&gt;&gt;&gt; age == 18
True</code>
</pre>
<p>还可检查两个数是否不等。例如，下面的代码在提供的答案不正确时打印一条消息：</p>
<p>
<em>magic_number.py</em>
</p>
<pre class="代码无行号"><code>  answer = 17
❶ if answer != 42:
      print("That is not the correct answer. Please try again!")</code>
</pre>
<p>
<code>answer</code>
 的值（<code>17</code>
 ）不是<code>42</code>
 ，❶处的条件得到满足，因此缩进的代码块得以执行：</p>
<pre class="代码无行号"><code>That is not the correct answer. Please try again!</code>
</pre>
<p>条件语句中可包含各种数学比较，如小于、小于等于、大于、大于等于：</p>
<pre class="代码无行号"><code>&gt;&gt;&gt; age = 19
&gt;&gt;&gt; age &lt; 21
True
&gt;&gt;&gt; age &lt;= 21
True
&gt;&gt;&gt; age &gt; 21
False
&gt;&gt;&gt; age &gt;= 21
False</code>
</pre>
<p>在<code>if</code>
 语句中可使用各种数学比较，这让你能够直接检查关心的条件。</p>
<h3 id="nav_point_107">5.2.5　检查多个条件</h3>
<p>你可能想同时检查多个条件。例如，有时候需要在两个条件都为<code>True</code>
 时才执行相应的操作，而有时候只要求一个条件为<code>True</code>
 。在这些情况下，关键字<code>and</code>
 和<code>or</code>
 可助你一臂之力。</p>
<ol>
<li value="1">
<p>
<strong>使用<code>and</code>
 检查多个条件</strong>
</p>
<p>要检查是否两个条件都为<code>True</code>
 ，可使用关键字<code>and</code>
 将两个条件测试合而为一。如果每个测试都通过了，整个表达式就为<code>True</code>
 ；如果至少一个测试没有通过，整个表达式就为<code>False</code>
 。</p>
<p>例如，要检查是否两个人都不小于21岁，可使用下面的测试：</p>
<pre class="代码无行号"><code>❸ &gt;&gt;&gt; age_0 = 22
  &gt;&gt;&gt; age_1 = 18
❷ &gt;&gt;&gt; age_0 &gt;= 21 and age_1 &gt;= 21
  False
❸ &gt;&gt;&gt; age_1 = 22
  &gt;&gt;&gt; age_0 &gt;= 21 and age_1 &gt;= 21
  True</code>
</pre>
<p>在❶处，定义两个用于存储年龄的变量：<code>age_0</code>
 和<code>age_1</code>
 。在❷处，检查这两个变量是否都大于或等于21。左边的测试通过了，但右边的测试没有通过，因此整个条件表达式的结果为<code>False</code>
 。在❸处，将<code>age_1</code>
 改为22，这样<code>age_1</code>
 的值大于21，因此两个测试都通过了，导致整个条件表达式的结果为<code>True</code>
 。</p>
<p>为改善可读性，可将每个测试分别放在一对圆括号内，但并非必须这样做。如果你使用圆括号，测试将类似于下面这样：</p>
<pre class="代码无行号"><code>(age_0 &gt;= 21) and (age_1 &gt;= 21)</code>
</pre>
<p> </p>
</li>
<li value="2">
<p>
<strong>使用or检查多个条件</strong>
</p>
<p>关键字<code>or</code>
 也能够让你检查多个条件，但只要至少一个条件满足，就能通过整个测试。仅当两个测试都没有通过时，使用<code>or</code>
 的表达式才为<code>False</code>
 。</p>
<p>下面再次检查两个人的年龄，但检查的条件是至少一个人的年龄不小于21岁：</p>
<pre class="代码无行号"><code>❶ &gt;&gt;&gt; age_0 = 22
  &gt;&gt;&gt; age_1 = 18
❷ &gt;&gt;&gt; age_0 &gt;= 21 or age_1 &gt;= 21
  True
❸ &gt;&gt;&gt; age_0 = 18
  &gt;&gt;&gt; age_0 &gt;= 21 or age_1 &gt;= 21
  False</code>
</pre>
<p>同样，首先定义两个用于存储年龄的变量（见❶）。❷处对<code>age_0</code>
 的测试通过了，因此整个表达式的结果为<code>True</code>
 。接下来，将<code>age_0</code>
 减小为18。在❸处的测试中，两个测试都没有通过，因此整个表达式的结果为<code>False</code>
 。</p>
</li>
</ol>
<h3 id="nav_point_108">5.2.6　检查特定值是否包含在列表中</h3>
<p>有时候，执行操作前必须检查列表是否包含特定的值。例如，结束用户的注册过程前，可能需要检查他提供的用户名是否已包含在用户名列表中。在地图程序中，可能需要检查用户提交的位置是否包含在已知位置列表中。</p>
<p>要判断特定的值是否已包含在列表中，可使用关键字<code>in</code>
 。下面来看看你可能为比萨店编写的一些代码。这些代码首先创建一个列表，其中包含用户点的比萨配料，然后检查特定的配料是否包含在该列表中。</p>
<pre class="代码无行号"><code>  &gt;&gt;&gt; requested_toppings = ['mushrooms', 'onions', 'pineapple']
❶ &gt;&gt;&gt; 'mushrooms' in requested_toppings
  True
❷ &gt;&gt;&gt; 'pepperoni' in requested_toppings
  False</code>
</pre>
<p>在❶处和❷处，关键字<code>in</code>
 让<code>Python</code>
 检查列表<code>requested_toppings</code>
 是否包含<code>'mushrooms'</code>
 和<code>'pepperoni'</code>
 。这种技术很有用，让你能够在创建一个列表后，轻松地检查其中是否包含特定的值。</p>
<h3 id="nav_point_109">5.2.7　检查特定值是否不包含在列表中</h3>
<p>还有些时候，确定特定的值未包含在列表中很重要。在这种情况下，可使用关键字<code>not in</code>
 。例如，有一个列表，其中包含被禁止在论坛上发表评论的用户，可以在允许用户提交评论前检查他是否被禁言：</p>
<p>
<em>banned_users.py</em>
</p>
<pre class="代码无行号"><code>  banned_users = ['andrew', 'carolina', 'david']
  user = 'marie'
❶ if user not in banned_users:
      print(f"{user.title()}, you can post a response if you wish.")</code>
</pre>
<p>❶处的代码行明白易懂：如果<code>user</code>
 的值未包含在列表<code>banned_users</code>
 中，Python将返回<code>True</code>
 ，进而执行缩进的代码行。</p>
<p>用户<code>'marie'</code>
 未包含在列表<code>banned_users</code>
 中，因此她将看到一条邀请她发表评论的消息：</p>
<pre class="代码无行号"><code>Marie, you can post a response if you wish.</code>
</pre>
<h3 id="nav_point_110">5.2.8　布尔表达式</h3>
<p>随着你对编程的了解越来越深入，将遇到术语<strong>布尔表达式</strong>
 ，它不过是条件测试的别名。与条件表达式一样，布尔表达式的结果要么为<code>True</code>
 ，要么为<code>False</code>
 。</p>
<p>布尔值通常用于记录条件，如游戏是否正在运行，或者用户是否可以编辑网站的特定内容：</p>
<pre class="代码无行号"><code>game_active = True
can_edit = False</code>
</pre>
<p>在跟踪程序状态或程序中重要的条件方面，布尔值提供了一种高效的方式。</p>
<blockquote>
<p>
<strong>动手试一试</strong>
</p>
<p>
<strong>练习5-1：条件测试</strong>
 　编写一系列条件测试，将每个测试以及对其结果的预测和实际结果打印出来。你编写的代码应类似于下面这样：</p>
<pre class="代码无行号"><code>car = 'subaru'
print("Is car == 'subaru'? I predict True.")
print(car == 'subaru')
print("\nIs car == 'audi'? I predict False.")
print(car == 'audi')</code>
</pre>
<ul>
<li class="第2级无序列表">详细研究实际结果，直到你明白它为何为<code>True</code>
 或<code>False</code>
 。</li>
<li class="第2级无序列表">创建至少10个测试，且其中结果分别为<code>True</code>
 和<code>False</code>
 的测试都至少有5个。</li>
</ul>
<p>
<strong>练习5-2：更多条件测试</strong>
 　你并非只能创建10个测试。如果想尝试做更多比较，可再编写一些测试，并将它们加入conditional_tests.py中。对于下面列出的各种情况，至少编写两个结果分别为<code>True</code>
 和<code>False</code>
 的测试。</p>
<ul>
<li class="第2级无序列表">检查两个字符串相等和不等。</li>
<li class="第2级无序列表">使用方法<code>lower()</code>
 的测试。</li>
<li class="第2级无序列表">涉及相等、不等、大于、小于、大于等于和小于等于的数值测试。</li>
<li class="第2级无序列表">使用关键字<code>and</code>
 和<code>or</code>
 的测试。</li>
<li class="第2级无序列表">测试特定的值是否包含在列表中。</li>
<li class="第2级无序列表">测试特定的值是否未包含在列表中。</li>
</ul>
</blockquote>
<h2 id="nav_point_111">5.3　<code>if</code>
 语句</h2>
<p>理解条件测试后，就可以开始编写<code>if</code>
 语句了。<code>if</code>
 语句有很多种，选择使用哪种取决于要测试的条件数。前面讨论条件测试时，列举了多个<code>if</code>
 语句示例，下面更深入地讨论这个主题。</p>
<h3 id="nav_point_112">5.3.1　简单的<code>if</code>
 语句</h3>
<p>最简单的<code>if</code>
 语句只有一个测试和一个操作：</p>
<pre class="代码无行号"><code>if conditional_test:
    do something</code>
</pre>
<p>第一行可包含任何条件测试，而在紧跟在测试后面的缩进代码块中，可执行任何操作。如果条件测试的结果为<code>True</code>
 ，Python就会执行紧跟在<code>if</code>
 语句后面的代码，否则Python将忽略这些代码。</p>
<p>假设有一个表示某人年龄的变量，而你想知道这个人是否符合投票的年龄，可使用如下代码：</p>
<p>
<em>voting.py</em>
</p>
<pre class="代码无行号"><code>  age = 19
❶ if age &gt;= 18:
❷     print("You are old enough to vote!")</code>
</pre>
<p>在❶处，Python检查变量<code>age</code>
 的值是否大于或等于18。答案是肯定的，因此Python执行❷处缩进的函数调用<code>print()</code>
 ：</p>
<pre class="代码无行号"><code>You are old enough to vote!</code>
</pre>
<p>在<code>if</code>
 语句中，缩进的作用与在<code>for</code>
 循环中相同。如果测试通过了，将执行<code>if</code>
 语句后面所有缩进的代码行，否则将忽略它们。</p>
<p>在紧跟<code>if</code>
 语句后面的代码块中，可根据需要包含任意数量的代码行。下面在一个人符合投票年龄时再打印一行输出，问他是否登记了：</p>
<pre class="代码无行号"><code>age = 19
if age &gt;= 18:
    print("You are old enough to vote!")
    print("Have you registered to vote yet?")</code>
</pre>
<p>条件测试通过了，而且两个函数调用<code>print()</code>
 都缩进了，因此它们都将执行：</p>
<pre class="代码无行号"><code>You are old enough to vote!
Have you registered to vote yet?</code>
</pre>
<p>如果<code>age</code>
 的值小于18，这个程序将不会有任何输出。</p>
<h3 id="nav_point_113">5.3.2　<code>if-else</code>
 语句</h3>
<p>我们经常需要在条件测试通过时执行一个操作，在没有通过时执行另一个操作。在这种情况下，可使用Python提供的<code>if-else</code>
 语句。<code>if-else</code>
 语句块类似于简单的<code>if</code>
 语句，但其中的<code>else</code>
 语句让你能够指定条件测试未通过时要执行的操作。</p>
<p>下面的代码在一个人符合投票年龄时显示与前面相同的消息，在不符合时显示一条新消息：</p>
<pre class="代码无行号"><code>  age = 17
❶ if age &gt;= 18:
      print("You are old enough to vote!")
      print("Have you registered to vote yet?")
❷ else:
      print("Sorry, you are too young to vote.")
      print("Please register to vote as soon as you turn 18!")</code>
</pre>
<p>如果❶处的条件测试通过了，就执行第一组缩进的函数调用<code>print()</code>
 。如果测试结果为<code>False</code>
 ，就执行❷处的<code>else</code>
 代码块。这次<code>age</code>
 小于18，条件测试未通过，因此执行<code>else</code>
 代码块中的代码：</p>
<pre class="代码无行号"><code>Sorry, you are too young to vote.
Please register to vote as soon as you turn 18!</code>
</pre>
<p>上述代码之所以可行，是因为只存在两种情形：要么符合投票年龄，要么不符合。<code>if-else</code>
 结构非常适合用于让Python执行两种操作之一的情形。在这样简单的<code>if-else</code>
 结构中，总是会执行两个操作中的一个。</p>
<h3 id="nav_point_114">5.3.3　<code>if-elif-else</code>
 结构</h3>
<p>我们经常需要检查超过两个的情形，为此可使用Python提供的<code>if-elif-else</code>
 结构。Python只执行<code>if-elif-else</code>
 结构中的一个代码块。它依次检查每个条件测试，直到遇到通过了的条件测试。测试通过后，Python将执行紧跟在它后面的代码，并跳过余下的测试。</p>
<p>在现实世界中，很多情况下需要考虑的情形超过两个。例如，来看一个根据年龄段收费的游乐场：</p>
<ul>
<li class="第1级无序列表">4岁以下免费；</li>
<li class="第1级无序列表">4～18岁收费25美元；</li>
<li class="第1级无序列表">18岁（含）以上收费40美元。</li>
</ul>
<p>如果只使用一条<code>if</code>
 语句，该如何确定门票价格呢？下面的代码确定一个人所属的年龄段，并打印一条包含门票价格的消息：</p>
<p>
<em>amusement_park.py</em>
</p>
<pre class="代码无行号"><code>  age = 12
❶ if age &lt; 4:
      print("Your admission cost is $0.")
❷ elif age &lt; 18:
      print("Your admission cost is $25.")
❸ else:
      print("Your admission cost is $40.")</code>
</pre>
<p>❶处的<code>if</code>
 测试检查一个人是否不满4岁。如果是，Python就打印一条合适的消息，并跳过余下测试。❷处的<code>elif</code>
 代码行其实是另一个<code>if</code>
 测试，仅在前面的测试未通过时才会运行。在这里，我们知道这个人不小于4岁，因为第一个测试未通过。如果这个人未满18岁，Python将打印相应的消息，并跳过<code>else</code>
 代码块。如果<code>if</code>
 测试和<code>elif</code>
 测试都未通过，Python将运行❸处<code>else</code>
 代码块中的代码。</p>
<p>在本例中，❶处测试的结果为<code>False</code>
 ，因此不执行其代码块。然而，第二个测试的结果为<code>True</code>
 （12小于18），因此执行其代码块。输出为一个句子，向用户指出门票价格：</p>
<pre class="代码无行号"><code>Your admission cost is $25.</code>
</pre>
<p>只要年龄超过17岁，前两个测试就都不能通过。在这种情况下，将执行<code>else</code>
 代码块，指出门票价格为40美元。</p>
<p>为了让代码更简洁，可不在<code>if-elif-else</code>
 代码块中打印门票价格，而只在其中设置门票价格，并在它后面添加一个简单的函数调用<code>print()</code>
 ：</p>
<pre class="代码无行号"><code>  age = 12
  if age &lt; 4:
❶     price = 0
  elif age &lt; 18:
❷     price = 25
  else:
❸     price = 40
❹ print(f"Your admission cost is ${price}.")</code>
</pre>
<p>❶处、❷处和❸处的代码行像前一个示例那样，根据人的年龄设置变量<code>price</code>
 的值。在<code>if-elif-else</code>
 结构中设置<code>price</code>
 的值后，一条未缩进的函数调用<code>print()</code>
 ❹会根据这个变量的值打印一条消息，指出门票的价格。</p>
<p>这些代码的输出与前一个示例相同，但<code>if-elif-else</code>
 结构的作用更小：它只确定门票价格，而不是在确定门票价格的同时打印一条消息。除效率更高外，这些修订后的代码还更容易修改：要调整输出消息的内容，只需修改一个而不是三个函数调用<code>print()</code>
 。</p>
<h3 id="nav_point_115">5.3.4　使用多个<code>elif</code>
 代码块</h3>
<p>可根据需要使用任意数量的<code>elif</code>
 代码块。例如，假设前述游乐场要给老年人打折，可再添加一个条件测试，判断顾客是否符合打折条件。下面假设对于65岁（含）以上的老人，可半价（即20美元）购买门票：</p>
<pre class="代码无行号"><code>  age = 12
  if age &lt; 4:
      price = 0
  elif age &lt; 18:
      price = 25
❶ elif age &lt; 65:
      price = 40
❷ else:
      price = 20
  print(f"Your admission cost is ${price}.")</code>
</pre>
<p>这些代码大多未变。第二个<code>elif</code>
 代码块（见❶）通过检查确定年龄不到65岁后，才将门票价格设置为全票价格——40美元。请注意，在<code>else</code>
 代码块（见❷）中，必须将所赋的值改为20，因为仅当年龄超过65岁（含）时，才会执行这个代码块。</p>
<h3 id="nav_point_116">5.3.5　省略<code>else</code>
 代码块</h3>
<p>Python并不要求<code>if-elif</code>
 结构后面必须有<code>else</code>
 代码块。在有些情况下，<code>else</code>
 代码块很有用；而在其他一些情况下，使用一条<code>elif</code>
 语句来处理特定的情形更清晰：</p>
<pre class="代码无行号"><code>  age = 12
  if age &lt; 4:
      price = 0
  elif age &lt; 18:
      price = 25
  elif age &lt; 65:
      price = 40
❶ elif age &gt;= 65:
      price = 20
  print(f"Your admission cost is ${price}.")</code>
</pre>
<p>❶处的<code>elif</code>
 代码块在顾客的年龄超过65岁（含）时，将价格设置为20美元。这比使用<code>else</code>
 代码块更清晰些。经过这样的修改后，每个代码块都仅在通过了相应的测试时才会执行。</p>
<p>
<code>else</code>
 是一条包罗万象的语句，只要不满足任何<code>if</code>
 或<code>elif</code>
 中的条件测试，其中的代码就会执行。这可能引入无效甚至恶意的数据。如果知道最终要测试的条件，应考虑使用一个<code>elif</code>
 代码块来代替<code>else</code>
 代码块。这样就可以肯定，仅当满足相应的条件时，代码才会执行。</p>
<h3 id="nav_point_117">5.3.6　测试多个条件</h3>
<p>
<code>if-elif-else</code>
 结构功能强大，但仅适合用于只有一个条件满足的情况：遇到通过了的测试后，Python就跳过余下的测试。这种行为很好，效率很高，让你能够测试一个特定的条件。</p>
<p>然而，有时候必须检查你关心的所有条件。在这种情况下，应使用一系列不包含<code>elif</code>
 和<code>else</code>
 代码块的简单<code>if</code>
 语句。在可能有多个条件为<code>True</code>
 且需要在每个条件为<code>True</code>
 时都采取相应措施时，适合使用这种方法。</p>
<p>下面再来看看前面的比萨店示例。如果顾客点了两种配料，就需要确保在其比萨中包含这些配料：</p>
<p>
<em>toppings.py</em>
</p>
<pre class="代码无行号"><code>❶ requested_toppings = ['mushrooms', 'extra cheese']
❷ if 'mushrooms' in requested_toppings:
      print("Adding mushrooms.")
❸ if 'pepperoni' in requested_toppings:
      print("Adding pepperoni.")
❹ if 'extra cheese' in requested_toppings:
      print("Adding extra cheese.")
  print("\nFinished making your pizza!")</code>
</pre>
<p>首先创建一个列表，其中包含顾客点的配料（见❶）。❷处的<code>if</code>
 语句检查顾客是否点了配料蘑菇（mushrooms）。如果点了，就打印一条确认消息。❸处检查配料辣香肠（pepperoni）的代码也是一个简单的<code>if</code>
 语句，而不是<code>elif</code>
 或<code>else</code>
 语句。因此不管前一个测试是否通过，都将进行这个测试。❹处的代码检查顾客是否要求多加芝士（extra cheese）。不管前两个测试的结果如何，都会执行这些代码。每当这个程序运行时，都会执行这三个独立的测试。</p>
<p>因为本例检查了每个条件，所以将在比萨中添加蘑菇并多加芝士：</p>
<pre class="代码无行号"><code>Adding mushrooms.
Adding extra cheese.
Finished making your pizza!</code>
</pre>
<p>如果像下面这样转而使用<code>if-elif-else</code>
 结构，代码将不能正确运行，因为有一个测试通过后，就会跳过余下的测试：</p>
<pre class="代码无行号"><code>requested_toppings = ['mushrooms', 'extra cheese']
if 'mushrooms' in requested_toppings:
    print("Adding mushrooms.")
elif 'pepperoni' in requested_toppings:
    print("Adding pepperoni.")
elif 'extra cheese' in requested_toppings:
    print("Adding extra cheese.")
print("\nFinished making your pizza!")</code>
</pre>
<p>第一个测试检查列表中是否包含<code>'mushrooms'</code>
 。它通过了，因此将在比萨中添加蘑菇。然而，Python将跳过<code>if-elif-else</code>
 结构中余下的测试，不再检查列表中是否包含<code>'pepperoni'</code>
 和<code>'extra cheese'</code>
 。结果是，将添加顾客点的第一种配料，但不会添加其他配料：</p>
<pre class="代码无行号"><code>Adding mushrooms.
Finished making your pizza!</code>
</pre>
<p>总之，如果只想执行一个代码块，就使用<code>if-elif-else</code>
 结构；如果要执行多个代码块，就使用一系列独立的<code>if</code>
 语句。</p>
<blockquote>
<p>
<strong>动手试一试</strong>
</p>
<p>
<strong>练习5-3：外星人颜色</strong>
 　假设在游戏中刚射杀了一个外星人，请创建一个名为<code>alien_color</code>
 的变量，并将其赋值为<code>'green'</code>
 、<code>'yellow'</code>
 或<code>'red'</code>
 。</p>
<ul>
<li class="第2级无序列表">编写一条<code>if</code>
 语句，检查外星人是否是绿色的。如果是，就打印一条消息，指出玩家获得了5分。</li>
<li class="第2级无序列表">编写这个程序的两个版本，在一个版本中上述测试通过了，而在另一个版本中未通过（未通过测试时没有输出）。</li>
</ul>
<p>
<strong>练习5-4：外星人颜色2</strong>
 　像练习5-3那样设置外星人的颜色，并编写一个<code>if-else</code>
 结构。</p>
<ul>
<li class="第2级无序列表">如果外星人是绿色的，就打印一条消息，指出玩家因射杀该外星人获得了5分。</li>
<li class="第2级无序列表">如果外星人不是绿色的，就打印一条消息，指出玩家获得了10分。</li>
<li class="第2级无序列表">编写这个程序的两个版本，在一个版本中执行<code>if</code>
 代码块，在另一个版本中执行<code>else</code>
 代码块。</li>
</ul>
<p>
<strong>练习5-5：外星人颜色3</strong>
 　将练习5-4中的<code>if-else</code>
 结构改为<code>if-elif-else</code>
 结构。</p>
<ul>
<li class="第2级无序列表">如果外星人是绿色的，就打印一条消息，指出玩家获得了5分。</li>
<li class="第2级无序列表">如果外星人是黄色的，就打印一条消息，指出玩家获得了10分。</li>
<li class="第2级无序列表">如果外星人是红色的，就打印一条消息，指出玩家获得了15分。</li>
<li class="第2级无序列表">编写这个程序的三个版本，分别在外星人为绿色、黄色和红色时打印一条消息。</li>
</ul>
<p>
<strong>练习5-6：人生的不同阶段</strong>
 　设置变量<code>age</code>
 的值，再编写一个<code>if-elif-else</code>
 结构，根据<code>age</code>
 的值判断一个人处于人生的哪个阶段。</p>
<ul>
<li class="第2级无序列表">如果年龄小于2岁，就打印一条消息，指出这个人是婴儿。</li>
<li class="第2级无序列表">如果年龄为2（含）～4岁，就打印一条消息，指出这个人是幼儿。</li>
<li class="第2级无序列表">如果年龄为4（含）～13岁，就打印一条消息，指出这个人是儿童。</li>
<li class="第2级无序列表">如果年龄为13（含）～20岁，就打印一条消息，指出这个人是青少年。</li>
<li class="第2级无序列表">如果年龄为20（含）～65岁，就打印一条消息，指出这个人是成年人。</li>
<li class="第2级无序列表">如果年龄超过65岁（含），就打印一条消息，指出这个人是老年人。</li>
</ul>
<p>
<strong>练习5-7：喜欢的水果</strong>
 　创建一个列表，其中包含你喜欢的水果，再编写一系列独立的<code>if</code>
 语句，检查列表中是否包含特定的水果。</p>
<ul>
<li class="第2级无序列表">将该列表命名为<code>favorite_fruits</code>
 ，并在其中包含三种水果。</li>
<li>
<p>编写5条<code>if</code>
 语句，每条都检查某种水果是否包含在列表中。如果是，就打印一条消息，下面是一个例子。</p>
<p>You really like bananas!</p>
</li>
</ul>
</blockquote>
<h2 id="nav_point_118">5.4　使用<code>if</code>
 语句处理列表</h2>
<p>通过结合使用<code>if</code>
 语句和列表，可完成一些有趣的任务：对列表中特定的值做特殊处理；高效地管理不断变化的情形，如餐馆是否还有特定的食材；证明代码在各种情形下都将按预期那样运行。</p>
<h3 id="nav_point_119">5.4.1　检查特殊元素</h3>
<p>本章开头通过一个简单示例演示了如何处理特殊值<code>'bmw'</code>
 ——它需要采用不同的格式进行打印。现在你对条件测试和<code>if</code>
 语句有了大致的认识，下面就来进一步研究如何检查列表中的特殊值，并对其做合适的处理。</p>
<p>继续使用前面的比萨店示例。这家比萨店在制作比萨时，每添加一种配料都打印一条消息。通过创建一个列表，在其中包含顾客点的配料，并使用一个循环来指出添加到比萨中的配料，能以极高的效率编写这样的代码：</p>
<p>
<em>toppings.py</em>
</p>
<pre class="代码无行号"><code>requested_toppings = ['mushrooms', 'green peppers', 'extra cheese']
for requested_topping in requested_toppings:
    print(f"Adding {requested_topping}.")
print("\nFinished making your pizza!")</code>
</pre>
<p>输出很简单，因为上述代码不过是一个简单的<code>for</code>
 循环：</p>
<pre class="代码无行号"><code>Adding mushrooms.
Adding green peppers.
Adding extra cheese.
Finished making your pizza!</code>
</pre>
<p>然而，如果比萨店的青椒用完了，该如何处理呢？为妥善地处理这种情况，可在<code>for</code>
 循环中包含一条<code>if</code>
 语句：</p>
<pre class="代码无行号"><code>  requested_toppings = ['mushrooms', 'green peppers', 'extra cheese']
  for requested_topping in requested_toppings:
❶     if requested_topping == 'green peppers':
          print("Sorry, we are out of green peppers right now.")
❷     else:
          print(f"Adding {requested_topping}.")
  print("\nFinished making your pizza!")</code>
</pre>
<p>这里在比萨中添加每种配料前都进行检查。❶处的代码检查顾客是否点了青椒。如果是，就显示一条消息，指出不能点青椒的原因。❷处的<code>else</code>
 代码块确保其他配料都将添加到比萨中。</p>
<p>输出表明，已经妥善地处理了顾客点的每种配料：</p>
<pre class="代码无行号"><code>Adding mushrooms.
Sorry, we are out of green peppers right now.
Adding extra cheese.
Finished making your pizza!</code>
</pre>
<h3 id="nav_point_120">5.4.2　确定列表不是空的</h3>
<p>到目前为止，我们对于处理的每个列表都做了一个简单的假设——假设它们都至少包含一个元素。因为马上就要让用户来提供存储在列表中的信息，所以不能再假设循环运行时列表不是空的。有鉴于此，在运行<code>for</code>
 循环前确定列表是否为空很重要。</p>
<p>下面在制作比萨前检查顾客点的配料列表是否为空。如果列表为空，就向顾客确认是否要点原味比萨；如果列表不为空，就像前面的示例那样制作比萨：</p>
<pre class="代码无行号"><code>❶ requested_toppings = []
❷ if requested_toppings:
      for requested_topping in requested_toppings:
          print(f"Adding {requested_topping}.")
      print("\nFinished making your pizza!")
❸ else:
      print("Are you sure you want a plain pizza?")</code>
</pre>
<p>首先创建一个空列表，其中不包含任何配料（见❶）。❷处进行简单的检查，而不是直接执行<code>for</code>
 循环。在<code>if</code>
 语句中将列表名用作条件表达式时，Python将在列表至少包含一个元素时返回<code>True</code>
 ，并在列表为空时返回<code>False</code>
 。如果<code>requested_toppings</code>
 不为空，就运行与前一个示例相同的<code>for</code>
 循环；否则，就打印一条消息，询问顾客是否确实要点不加任何配料的原味比萨（见❸）。</p>
<p>在这里，这个列表为空，因此输出如下——询问顾客是否确实要点原味比萨：</p>
<pre class="代码无行号"><code>Are you sure you want a plain pizza?</code>
</pre>
<p>如果这个列表不为空，输出将显示在比萨中添加的各种配料。</p>
<h3 id="nav_point_121">5.4.3　使用多个列表</h3>
<p>顾客的要求往往五花八门，在比萨配料方面尤其如此。如果顾客要在比萨中添加炸薯条，该怎么办呢？可使用列表和<code>if</code>
 语句来确定能否满足顾客的要求。</p>
<p>来看看在制作比萨前如何拒绝怪异的配料要求。下面的示例定义了两个列表，其中第一个列表包含比萨店供应的配料，而第二个列表包含顾客点的配料。这次对于<code>requested_toppings</code>
 中的每个元素，都检查它是否是比萨店供应的配料，再决定是否在比萨中添加它：</p>
<pre class="代码无行号"><code>❶ available_toppings = ['mushrooms', 'olives', 'green peppers',
                        'pepperoni', 'pineapple', 'extra cheese']
❷ requested_toppings = ['mushrooms', 'french fries', 'extra cheese']
❸ for requested_topping in requested_toppings:
❹     if requested_topping in available_toppings:
          print(f"Adding {requested_topping}.")
❺     else:
          print(f"Sorry, we don't have {requested_topping}.")
  print("\nFinished making your pizza!")</code>
</pre>
<p>❶处定义了一个列表，其中包含比萨店供应的配料。请注意，如果比萨店供应的配料是固定的，也可使用一个元组来存储它们。❷处又创建了一个列表，其中包含顾客点的配料。请注意那个不同寻常的配料——<code>'french fries'</code>
 。在❸处遍历顾客点的配料列表。在这个循环中，对于顾客点的每种配料，都检查它是否包含在供应的配料列表中（见❹）。如果答案是肯定的，就将其加入比萨中，否则将运行<code>else</code>
 代码块（见❺）：打印一条消息，告诉顾客不供应这种配料。</p>
<p>这些代码的输出整洁而翔实：</p>
<pre class="代码无行号"><code>Adding mushrooms.
Sorry, we don't have french fries.
Adding extra cheese.
Finished making your pizza!</code>
</pre>
<p>通过为数不多的几行代码，我们高效地处理了一种真实的情形！</p>
<blockquote>
<p>
<strong>动手试一试</strong>
</p>
<p>
<strong>练习5-8：以特殊方式跟管理员打招呼</strong>
 　创建一个至少包含5个用户名的列表，且其中一个用户名为<code>'admin'</code>
 。想象你要编写代码，在每位用户登录网站后都打印一条问候消息。遍历用户名列表，并向每位用户打印一条问候消息。</p>
<ul>
<li>
<p>如果用户名为<code>'admin'</code>
 ，就打印一条特殊的问候消息，如下所示。</p>
<p>Hello admin, would you like to see a status report?<br/>
 </p>
</li>
<li>
<p>否则，打印一条普通的问候消息，如下所示。</p>
<p>Hello Jaden, thank you for logging in again.<br/>
 </p>
</li>
</ul>
<p>
<strong>练习5-9：处理没有用户的情形</strong>
 　在为完成练习5-8编写的程序中，添加一条<code>if</code>
 语句，检查用户名列表是否为空。</p>
<ul>
<li>
<p>如果为空，就打印如下消息。</p>
<p>We need to find some users!<br/>
 </p>
</li>
<li>
<p>删除列表中的所有用户名，确定将打印正确的消息。</p>
</li>
</ul>
<p>
<strong>练习5-10：检查用户名</strong>
 　按下面的说明编写一个程序，模拟网站如何确保每位用户的用户名都独一无二。</p>
<ul>
<li class="第2级无序列表">创建一个至少包含5个用户名的列表，并将其命名为<code>current_users</code>
 。</li>
<li class="第2级无序列表">再创建一个包含5个用户名的列表，将其命名为<code>new_users</code>
 ，并确保其中有一两个用户名也包含在列表<code>current_users</code>
 中。</li>
<li class="第2级无序列表">遍历列表<code>new_users</code>
 ，对于其中的每个用户名，都检查它是否已被使用。如果是，就打印一条消息，指出需要输入别的用户名；否则，打印一条消息，指出这个用户名未被使用。</li>
<li class="第2级无序列表">确保比较时不区分大小写。换句话说，如果用户名<code>'John'</code>
 已被使用，应拒绝用户名<code>'JOHN'</code>
 。（为此，需要创建列表<code>current_users</code>
 的副本，其中包含当前所有用户名的小写版本。）</li>
</ul>
<p>
<strong>练习5-11：序数</strong>
 　序数表示位置，如1st和2nd。序数大多以th结尾，只有1、2和3例外。</p>
<ul>
<li class="第2级无序列表">在一个列表中存储数字1～9。</li>
<li class="第2级无序列表">遍历这个列表。</li>
<li class="第2级无序列表">在循环中使用一个<code>if-elif-else</code>
 结构，以打印每个数字对应的序数。输出内容应为<code>"1st 2nd 3rd 4th 5th 6th 7th 8th 9th"</code>
 ，但每个序数都独占一行。</li>
</ul>
</blockquote>
<h2 id="nav_point_122">5.5　设置<code>if</code>
 语句的格式</h2>
<p>本章的每个示例都展示了良好的格式设置习惯。在条件测试的格式设置方面，PEP 8提供的唯一建议是，在诸如<code>==</code>
 、<code>&gt;=</code>
 和<code>&lt;=</code>
 等比较运算符两边各添加一个空格。例如：</p>
<pre class="代码无行号"><code>if age &lt; 4:</code>
</pre>
<p>要比</p>
<pre class="代码无行号"><code>if age&lt;4:</code>
</pre>
<p>更好。</p>
<p>这样的空格不会影响Python对代码的解读，而只是让代码阅读起来更容易。</p>
<blockquote>
<p>
<strong>动手试一试</strong>
</p>
<p>
<strong>练习5-12：设置<code>if</code>
 语句的格式　</strong>
 审核你在本章编写的程序，确保正确地设置了条件测试的格式。</p>
<p>
<strong>练习5-13：自己的想法</strong>
 　与刚拿起本书时相比，现在你是一名能力更强的程序员了。鉴于你对如何在程序中模拟现实情形有了更深入的认识，可以考虑使用程序来解决一些问题了。随着编程技能不断提高，你可能想解决一些问题，请将这方面的想法记录下来。想想你可能想编写的游戏、想研究的数据集以及想创建的Web应用程序。</p>
</blockquote>
<h2 id="nav_point_123">5.6　小结</h2>
<p>在本章中，你学习了：如何编写结果要么为<code>True</code>
 要么为<code>False</code>
 的条件测试；如何编写简单的<code>if</code>
 语句、<code>if-else</code>
 语句和<code>if-elif-else</code>
 结构，并且在程序中使用这些结构来测试特定的条件，以确定这些条件是否满足；如何在利用高效的<code>for</code>
 循环的同时，以不同于其他元素的方式对特定的列表元素进行处理。你还再次学习了Python就代码格式提出的建议，从而确保即便编写的程序越来越复杂，其代码依然易于阅读和理解。</p>
<p>在第6章，你将学习Python字典。字典类似于列表，但让你能够将不同的信息关联起来。你将学习如何创建和遍历字典，以及如何将字典同列表和<code>if</code>
 语句结合起来使用。学习字典让你能够模拟更多现实世界的情形。</p>
<br style="page-break-after:always"/>
<div></div>
]]></description>
    <pubDate>Wed, 08 Jul 2026 01:44:51 +0000</pubDate>
    <guid isPermaLink="false">5</guid>
</item>
</channel>
</rss>