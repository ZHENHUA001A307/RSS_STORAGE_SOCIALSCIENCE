<?xml version="1.0" encoding="UTF-8" ?>
<rss version="2.0">
<channel>
    <title>我的每日小说</title>
    <link>https://github.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE</link>
    <description>从 EPUB 自动生成的 RSS 订阅源</description>
        <item>
    <title>Basic Python - 007 - 第 7 章 用户输入和while循环.epub</title>
    <description><![CDATA[<p style='color: #888;'>⏱ 预计阅读时间：74 分钟 (14702 字)</p><hr><h1 id="nav_point_144">第 7 章　用户输入和<code>while</code>
 循环</h1>
<blockquote>
<p><img alt="" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/7_Image00001.gif" style="width: 16%" width="16%"/>
 　大多数程序旨在解决最终用户的问题，为此通常需要从用户那里获取一些信息。例如，假设有人要判断自己是否到了投票年龄。要编写回答这个问题的程序，就需要知道用户的年龄，才能给出答案。因此，这种程序需要让用户<strong>输入</strong>
 年龄，再将其与投票年龄进行比较，以判断用户是否到了投票年龄，从而给出结果。</p>
<p>在本章中，你将学习如何接受用户输入，以便程序进行处理。程序需要一个名字时，你需要提示用户输入该名字；程序需要一个名单时，你需要提示用户输入一系列名字。为此，你将使用函数<code>input()</code>
 。</p>
<p>你还将学习如何让程序不断地运行，以便用户根据需要输入信息，并在程序中使用这些信息。为此，你将使用<code>while</code>
 循环让程序不断运行，直到指定的条件不满足为止。</p>
<p>通过获取用户输入并学会控制程序的运行时间，你就能编写出交互式程序。</p>
</blockquote>
<h2 id="nav_point_145">7.1　函数<code>input()</code>
 的工作原理</h2>
<p>函数<code>input()</code>
 让程序暂停运行，等待用户输入一些文本。获取用户输入后，Python将其赋给一个变量，以方便你使用。</p>
<p>例如，下面的程序让用户输入一些文本，再将这些文本呈现给用户：</p>
<p>
<em>parrot.py</em>
</p>
<pre class="代码无行号"><code>message = input("Tell me something, and I will repeat it back to you: ")
print(message)</code>
</pre>
<p>函数<code>input()</code>
 接受一个参数——要向用户显示的<strong>提示</strong>
 （prompt）或说明，让用户知道该如何做。在本例中，Python运行第一行代码时，用户将看到提示<code>Tell me something, and I will repeat it back to you:</code>
 。程序等待用户输入，并在用户按回车键后继续运行。输入被赋给变量<code>message</code>
 ，接下来的<code>print(message)</code>
 将输入呈现给用户：</p>
<pre class="代码无行号"><code>Tell me something, and I will repeat it back to you: Hello everyone!
Hello everyone!</code>
</pre>
<blockquote>
<p>
<strong>注意</strong>
 　Sublime Text等众多编辑器不能运行提示用户输入的程序。你可以使用Sublime Text来编写提示用户输入的程序，但必须从终端运行它们。详情请参阅1.5节。</p>
</blockquote>
<h3 id="nav_point_146">7.1.1　编写清晰的程序</h3>
<p>每当使用函数<code>input()</code>
 时，都应指定清晰易懂的提示，准确地指出希望用户提供什么样的信息——指出用户应该输入何种信息的任何提示都行，如下所示：</p>
<p>
<em>greeter.py</em>
</p>
<pre class="代码无行号"><code>name = input("Please enter your name: ")
print(f"\nHello, {name}!")</code>
</pre>
<p>通过在提示末尾（这里是冒号后面）包含一个空格，可将提示与用户输入分开，让用户清楚地知道其输入始于何处，如下所示：</p>
<pre class="代码无行号"><code>Please enter your name: Eric
Hello, Eric!</code>
</pre>
<p>有时候，提示可能超过一行。例如，你可能需要指出获取特定输入的原因。在这种情况下，可将提示赋给一个变量，再将该变量传递给函数<code>input()</code>
 。这样，即便提示超过一行，<code>input()</code>
 语句也会非常清晰。</p>
<p>
<em>greeter.py</em>
</p>
<pre class="代码无行号"><code>prompt = "If you tell us who you are, we can personalize the messages you see."
prompt += "\nWhat is your first name? "
name = input(prompt)
print(f"\nHello, {name}!")</code>
</pre>
<p>本例演示了一种创建多行字符串的方式。第一行将消息的前半部分赋给变量<code>prompt</code>
 中。在第二行中，运算符<code>+=</code>
 在前面赋给变量<code>prompt</code>
 的字符串末尾附加一个字符串。</p>
<p>最终的提示占据两行，且问号后面有一个空格，这也是为了使其更加清晰：</p>
<pre class="代码无行号"><code>If you tell us who you are, we can personalize the messages you see.
What is your first name? Eric
Hello, Eric!</code>
</pre>
<h3 id="nav_point_147">7.1.2　使用<code>int()</code>
 来获取数值输入</h3>
<p>使用函数<code>input()</code>
 时，Python将用户输入解读为字符串。请看下面让用户输入年龄的解释器会话：</p>
<pre class="代码无行号"><code>&gt;&gt;&gt; age = input("How old are you? ")
How old are you? 21
&gt;&gt;&gt; age
'21'</code>
</pre>
<p>用户输入的是数21，但我们请求Python提供变量<code>age</code>
 的值时，它返回的是<code>'21'</code>
 ——用户输入数值的字符串表示。我们怎么知道Python将输入解读成了字符串呢？因为这个数用引号括起了。如果只想打印输入，这一点问题都没有；但如果试图将输入作为数来使用，就会引发错误：</p>
<pre class="代码无行号"><code>  &gt;&gt;&gt; age = input("How old are you? ")
  How old are you? 21
❶ &gt;&gt;&gt; age &gt;= 18
  Traceback (most recent call last):
    File "&lt;stdin&gt;", line 1, in &lt;module&gt;
❷ TypeError: unorderable types: str() &gt;= int()</code>
</pre>
<p>试图将输入用于数值比较时（见❶），Python会引发错误，因为它无法将字符串和整数进行比较：不能将赋给<code>age</code>
 的字符串<code>'21'</code>
 与数值<code>18</code>
 进行比较（见❷）。</p>
<p>为解决这个问题，可使用函数<code>int()</code>
 ，它让Python将输入视为数值。函数<code>int()</code>
 将数的字符串表示转换为数值表示，如下所示：</p>
<pre class="代码无行号"><code>  &gt;&gt;&gt; age = input("How old are you? ")
  How old are you? 21
❶ &gt;&gt;&gt; age = int(age)
  &gt;&gt;&gt; age &gt;= 18
  True</code>
</pre>
<p>在本例中，用户根据提示输入<code>21</code>
 后，Python将这个数解读为字符串，但随后<code>int()</code>
 将这个字符串转换成了数值表示（见❶）。这样Python就能运行条件测试了：将变量<code>age</code>
 （它现在表示的是数值21）同<code>18</code>
 进行比较，看它是否大于或等于18。测试结果为<code>True</code>
 。</p>
<p>如何在实际程序中使用函数<code>int()</code>
 呢？请看下面的程序，它判断一个人是否满足坐过山车的身高要求：</p>
<p>
<em>rollercoaster.py</em>
</p>
<pre class="代码无行号"><code>height = input("How tall are you, in inches? ")
height = int(height)
if height &gt;= 48:
    print("\nYou're tall enough to ride!")
else:
    print("\nYou'll be able to ride when you're a little older.")</code>
</pre>
<p>在此程序中，为何可以将<code>height</code>
 同<code>48</code>
 进行比较呢？因为在比较前，<code>height = int(height)</code>
 将输入转换成了数值表示。如果输入的数大于或等于48，就指出用户满足身高条件：</p>
<pre class="代码无行号"><code>How tall are you, in inches? 71
You're tall enough to ride!</code>
</pre>
<p>将数值输入用于计算和比较前，务必将其转换为数值表示。</p>
<h3 id="nav_point_148">7.1.3　求模运算符</h3>
<p>处理数值信息时，<strong>求模运算符</strong>
 （%）是个很有用的工具，它将两个数相除并返回余数：</p>
<pre class="代码无行号"><code>&gt;&gt;&gt; 4 % 3
1
&gt;&gt;&gt; 5 % 3
2
&gt;&gt;&gt; 6 % 3
0
&gt;&gt;&gt; 7 % 3
1</code>
</pre>
<p>求模运算符不会指出一个数是另一个数的多少倍，只指出余数是多少。</p>
<p>如果一个数可被另一个数整除，余数就为0，因此求模运算将返回0。可利用这一点来判断一个数是奇数还是偶数：</p>
<p>
<em>even_or_odd.py</em>
</p>
<pre class="代码无行号"><code>number = input("Enter a number, and I'll tell you if it's even or odd: ")
number = int(number)
if number % 2 == 0:
    print(f"\nThe number {number} is even.")
else:
    print(f"\nThe number {number} is odd.")</code>
</pre>
<p>偶数都能被2整除，因此如果对一个数和2执行求模运算的结果为0，即<code>number % 2 == 0</code>
 ，那么这个数就是偶数；否则就是奇数。</p>
<pre class="代码无行号"><code>Enter a number, and I'll tell you if it's even or odd: 42
The number 42 is even.</code>
</pre>
<blockquote>
<p>
<strong>动手试一试</strong>
</p>
<p>
<strong>练习7-1：汽车租赁</strong>
 　编写一个程序，询问用户要租赁什么样的汽车，并打印一条消息，下面是一个例子。</p>
<p>Let me see if I can find you a Subaru.</p>
<p>
<strong>练习7-2：餐馆订位</strong>
 　编写一个程序，询问用户有多少人用餐。如果超过8位，就打印一条消息，指出没有空桌；否则指出有空桌。</p>
<p>
<strong>练习7-3：10的整数倍</strong>
 　让用户输入一个数，并指出该数是否是10的整数倍。</p>
</blockquote>
<h2 id="nav_point_149">7.2　<code>while</code>
 循环简介</h2>
<p>
<code>for</code>
 循环用于针对集合中的每个元素都执行一个代码块，而<code>while</code>
 循环则不断运行，直到指定的条件不满足为止。</p>
<h3 id="nav_point_150">7.2.1　使用<code>while</code>
 循环</h3>
<p>可使用<code>while</code>
 循环来数数。例如，下面的<code>while</code>
 循环从1数到5：</p>
<p>
<em>counting.py</em>
</p>
<pre class="代码无行号"><code>current_number = 1
while current_number &lt;= 5:
    print(current_number)
    current_number += 1</code>
</pre>
<p>在第一行，将<code>1</code>
 赋给变量<code>current_number</code>
 ，从而指定从1开始数。将接下来的<code>while</code>
 循环设置成：只要<code>current_number</code>
 小于或等于5，就接着运行这个循环。循环中的代码打印<code>current_number</code>
 的值，再使用代码<code>current_number += 1</code>
 （代码<code>current_number = current_number + 1</code>
 的简写）将其值加1。</p>
<p>只要满足条件<code>current_number &lt;= 5</code>
 ，Python就接着运行这个循环。因为1小于5，所以Python打印<code>1</code>
 并将<code>current_number</code>
 加1，使其为2；因为2小于5，所以Python打印<code>2</code>
 并将<code>current_number</code>
 加1，使其为3；依此类推。一旦<code>current_number</code>
 大于5，循环就将停止，整个程序也将结束：</p>
<pre class="代码无行号"><code>1
2
3
4
5</code>
</pre>
<p>你每天使用的程序很可能就包含<code>while</code>
 循环。例如，游戏使用<code>while</code>
 循环，确保在玩家想玩时不断运行，并在玩家想退出时停止运行。如果程序在用户没有让它停止时停止运行，或者在用户要退出时还继续运行，那就太没有意思了。因此，<code>while</code>
 循环很有用。</p>
<h3 id="nav_point_151">7.2.2　让用户选择何时退出</h3>
<p>可以使用<code>while</code>
 循环让程序在用户愿意时不断运行，如下面的程序parrot.py所示。我们在其中定义了一个<strong>退出值</strong>
 ，只要用户输入的不是这个值，程序就将接着运行：</p>
<p>
<em>parrot.py</em>
</p>
<pre class="代码无行号"><code>❶ prompt = "\nTell me something, and I will repeat it back to you:"
  prompt += "\nEnter 'quit' to end the program. "
❷ message = ""
❸ while message != 'quit':
      message = input(prompt)
      print(message)</code>
</pre>
<p>❶处定义了一条提示消息，告诉用户有两个选择：要么输入一条消息，要么输入退出值（这里为<code>'quit'</code>
 ）。接下来，创建变量<code>message</code>
 （见❷），用于记录用户输入的值。我们将变量<code>message</code>
 的初始值设置为空字符串<code>""</code>
 ，让Python首次执行<code>while</code>
 代码行时有可供检查的东西。Python首次执行<code>while</code>
 语句时，需要将<code>message</code>
 的值与<code>'quit'</code>
 进行比较，但此时用户还没有输入。如果没有可供比较的东西，Python将无法继续运行程序。为解决这个问题，必须给变量<code>message</code>
 指定初始值。虽然这个初始值只是一个空字符串，但符合要求，能够让Python执行<code>while</code>
 循环所需的比较。只要<code>message</code>
 的值不是<code>'quit'</code>
 ，这个循环（见❸）就会不断运行。</p>
<p>首次遇到这个循环时，<code>message</code>
 是一个空字符串，因此Python进入该循环。执行到代码行<code>message = input(prompt)</code>
 时，Python显示提示消息，并等待用户输入。不管用户输入是什么，都将赋给变量<code>message</code>
 并打印出来。接下来，Python重新检查<code>while</code>
 语句中的条件。只要用户输入的不是单词<code>'quit'</code>
 ，Python就会再次显示提示消息并等待用户输入。等到用户终于输入<code>'quit'</code>
 后，Python停止执行<code>while</code>
 循环，整个程序也到此结束：</p>
<pre class="代码无行号"><code>Tell me something, and I will repeat it back to you:
Enter 'quit' to end the program. Hello everyone!
Hello everyone!
Tell me something, and I will repeat it back to you:
Enter 'quit' to end the program. Hello again.
Hello again.
Tell me something, and I will repeat it back to you:
Enter 'quit' to end the program. quit
quit</code>
</pre>
<p>这个程序很好，唯一美中不足的是，它将单词<code>'quit'</code>
 也作为一条消息打印了出来。为修复这种问题，只需使用一个简单的<code>if</code>
 测试：</p>
<pre class="代码无行号"><code>prompt = "\nTell me something, and I will repeat it back to you:"
prompt += "\nEnter 'quit' to end the program. "
message = ""
while message != 'quit':
    message = input(prompt)
    if message != 'quit':
        print(message)</code>
</pre>
<p>现在，程序在显示消息前将做简单的检查，仅在消息不是退出值时才打印它：</p>
<pre class="代码无行号"><code>Tell me something, and I will repeat it back to you:
Enter 'quit' to end the program. Hello everyone!
Hello everyone!
Tell me something, and I will repeat it back to you:
Enter 'quit' to end the program. Hello again.
Hello again.
Tell me something, and I will repeat it back to you:
Enter 'quit' to end the program. quit</code>
</pre>
<h3 id="nav_point_152">7.2.3　使用标志</h3>
<p>在前一个示例中，我们让程序在满足指定条件时执行特定的任务。但在更复杂的程序中，很多不同的事件会导致程序停止运行。在这种情况下，该怎么办呢？</p>
<p>例如，有多种事件可能导致游戏结束，如玩家失去所有飞船、时间已用完，或者要保护的城市被全部摧毁。导致程序结束的事件有很多时，如果在一条<code>while</code>
 语句中检查所有这些条件，将既复杂又困难。</p>
<p>在要求很多条件都满足才继续运行的程序中，可定义一个变量，用于判断整个程序是否处于活动状态。这个变量称为<strong>标志</strong>
 （flag），充当程序的交通信号灯。可以让程序在标志为<code>True</code>
 时继续运行，并在任何事件导致标志的值为<code>False</code>
 时让程序停止运行。这样，在<code>while</code>
 语句中就只需检查一个条件：标志的当前值是否为<code>True</code>
 。然后将所有其他测试（是否发生了应将标志设置为<code>False</code>
 的事件）都放在其他地方，从而让程序更整洁。</p>
<p>下面在前一节的程序parrot.py中添加一个标志。将其命名为<code>active</code>
 （你可给它指定任何名称），用于判断程序是否应继续运行：</p>
<pre class="代码无行号"><code>  prompt = "\nTell me something, and I will repeat it back to you:"
  prompt += "\nEnter 'quit' to end the program. "
❶ active = True
❷ while active:
      message = input(prompt)
❸     if message == 'quit':
          active = False
❹     else:
          print(message)</code>
</pre>
<p>将变量<code>active</code>
 设置为<code>True</code>
 （见❶），让程序最初处于活动状态。这样做简化了<code>while</code>
 语句，因为不需要在其中做任何比较——相关的逻辑由程序的其他部分处理。只要变量<code>active</code>
 为<code>True</code>
 ，循环就将继续运行（见❷）。</p>
<p>在<code>while</code>
 循环中，在用户输入后使用一条<code>if</code>
 语句来检查变量<code>message</code>
 的值。如果用户输入的是<code>'quit'</code>
 （见❸），就将变量<code>active</code>
 设置为<code>False</code>
 。这将导致<code>while</code>
 循环不再继续执行。如果用户输入的不是<code>'quit'</code>
 （见❹），就将输入作为一条消息打印出来。</p>
<p>这个程序的输出与前一个示例相同。前一个示例将条件测试直接放在了<code>while</code>
 语句中，而这个程序则使用一个标志来指出程序是否处于活动状态。这样，如果要添加测试（如<code>elif</code>
 语句）以检查是否发生了其他导致<code>active</code>
 变为<code>False</code>
 的事件，就会很容易。在复杂的程序（如很多事件会导致程序停止运行的游戏）中，标志很有用：在任意一个事件导致活动标志变成<code>False</code>
 时，主游戏循环将退出，此时可显示一条游戏结束消息，并让用户选择是否要重新玩。</p>
<h3 id="nav_point_153">7.2.4　使用<code>break</code>
 退出循环</h3>
<p>要立即退出<code>while</code>
 循环，不再运行循环中余下的代码，也不管条件测试的结果如何，可使用<code>break</code>
 语句。<code>break</code>
 语句用于控制程序流程，可用来控制哪些代码行将执行、哪些代码行不执行，从而让程序按你的要求执行你要执行的代码。</p>
<p>例如，来看一个让用户指出他到过哪些地方的程序。在这个程序中，可在用户输入<code>'quit'</code>
 后使用<code>break</code>
 语句立即退出<code>while</code>
 循环：</p>
<p>
<em>cities.py</em>
</p>
<pre class="代码无行号"><code>  prompt = "\nPlease enter the name of a city you have visited:"
  prompt += "\n(Enter 'quit' when you are finished.) "
❶ while True:
      city = input(prompt)
      if city == 'quit':
          break
      else:
          print(f"I'd love to go to {city.title()}!")</code>
</pre>
<p>以<code>while True</code>
 （见❶）打头的循环将不断运行，直到遇到<code>break</code>
 语句。这个程序中的循环不断让用户输入他到过的城市的名字，直到用户输入<code>'quit'</code>
 为止。用户输入<code>'quit'</code>
 后，将执行<code>break</code>
 语句，导致Python退出循环：</p>
<pre class="代码无行号"><code>Please enter the name of a city you have visited:
(Enter 'quit' when you are finished.) New York
I'd love to go to New York!
Please enter the name of a city you have visited:
(Enter 'quit' when you are finished.) San Francisco
I'd love to go to San Francisco!
Please enter the name of a city you have visited:
(Enter 'quit' when you are finished.) quit</code>
</pre>
<blockquote>
<p>
<strong>注意</strong>
 　在任何Python循环中都可使用<code>break</code>
 语句。例如，可使用<code>break</code>
 语句来退出遍历列表或字典的<code>for</code>
 循环。</p>
</blockquote>
<h3 id="nav_point_154">7.2.5　在循环中使用<code>continue</code>
</h3>
<p>要返回循环开头，并根据条件测试结果决定是否继续执行循环，可使用<code>continue</code>
 语句，它不像<code>break</code>
 语句那样不再执行余下的代码并退出整个循环。例如，来看一个从1数到10但只打印其中奇数的循环：</p>
<p>
<em>counting.py</em>
</p>
<pre class="代码无行号"><code>  current_number = 0
  while current_number &lt; 10:
❶     current_number += 1
      if current_number % 2 == 0:
          continue
      print(current_number)</code>
</pre>
<p>首先将<code>current_number</code>
 设置为0，由于它小于10，Python进入<code>while</code>
 循环。进入循环后，以步长1的方式往上数（见❶），因此<code>current_number</code>
 为1。接下来，<code>if</code>
 语句检查<code>current_number</code>
 与2的求模运算结果。如果结果为0（意味着<code>current_number</code>
 可被2整除），就执行<code>continue</code>
 语句，让Python忽略余下的代码，并返回循环的开头。如果当前的数不能被2整除，就执行循环中余下的代码，将这个数打印出来：</p>
<pre class="代码无行号"><code>1
3
5
7
9</code>
</pre>
<h3 id="nav_point_155">7.2.6　避免无限循环</h3>
<p>每个<code>while</code>
 循环都必须有停止运行的途径，这样才不会没完没了地执行下去。例如，下面的循环从1数到5：</p>
<p>
<em>counting.py</em>
</p>
<pre class="代码无行号"><code>x = 1
while x &lt;= 5:
    print(x)
    x += 1</code>
</pre>
<p>但如果像下面这样不小心遗漏了代码行<code>x += 1</code>
 ，这个循环将没完没了地运行：</p>
<pre class="代码无行号"><code># 这个循环将没完没了地运行！
x = 1
while x &lt;= 5:
    print(x)</code>
</pre>
<p>在这里，<code>x</code>
 的初始值为<code>1</code>
 ，但根本不会变。因此条件测试<code>x &lt;= 5</code>
 始终为<code>True</code>
 ，导致<code>while</code>
 循环没完没了地打印<code>1</code>
 ，如下所示：</p>
<pre class="代码无行号"><code>1
1
1
1
--snip--</code>
</pre>
<p>每个程序员都会偶尔因不小心而编写出无限循环，在循环的退出条件比较微妙时尤其如此。如果程序陷入无限循环，可按Ctrl + C，也可关闭显示程序输出的终端窗口。</p>
<p>要避免编写无限循环，务必对每个<code>while</code>
 循环进行测试，确保其按预期那样结束。如果你希望程序在用户输入特定值时结束，可运行程序并输入这样的值。如果在这种情况下程序没有结束，请检查程序处理这个值的方式，确认程序至少有一个这样的地方能让循环条件为<code>False</code>
 ，或者让<code>break</code>
 语句得以执行。</p>
<blockquote>
<p>
<strong>注意</strong>
 　Sublime Text等一些编辑器内嵌了输出窗口，这可能导致难以结束无限循环，不得不通过关闭编辑器来结束。在这种情况下，可在输出窗口中单击鼠标，再按Ctrl + C，这样应该能够结束无限循环。</p>
</blockquote>
<p>　</p>
<blockquote>
<p>
<strong>动手试一试</strong>
</p>
<p>
<strong>练习7-4：比萨配料</strong>
 　编写一个循环，提示用户输入一系列比萨配料，并在用户输入<code>'quit'</code>
 时结束循环。每当用户输入一种配料后，都打印一条消息，指出我们会在比萨中添加这种配料。</p>
<p>
<strong>练习7-5：电影票</strong>
 　有家电影院根据观众的年龄收取不同的票价：不到3岁的观众免费；3～12岁的观众收费10美元；超过12岁的观众收费15美元。请编写一个循环，在其中询问用户的年龄，并指出其票价。</p>
<p>
<strong>练习7-6：三种出路</strong>
 　以不同的方式完成练习7-4或练习7-5，在程序中采取如下做法。</p>
<ul>
<li class="第2级无序列表">在<code>while</code>
 循环中使用条件测试来结束循环。</li>
<li class="第2级无序列表">使用变量<code>active</code>
 来控制循环结束的时机。</li>
<li class="第2级无序列表">使用<code>break</code>
 语句在用户输入<code>'quit'</code>
 时退出循环。</li>
</ul>
<p>
<strong>练习7-7：无限循环</strong>
 　编写一个没完没了的循环，并运行它（要结束该循环，可按Ctrl + C，也可关闭显示输出的窗口）。</p>
</blockquote>
<h2 id="nav_point_156">7.3　使用<code>while</code>
 循环处理列表和字典</h2>
<p>到目前为止，我们每次都只处理了一项用户信息：获取用户的输入，再将输入打印出来或做出应答；循环再次运行时，获悉另一个输入值并做出响应。然而，要记录大量的用户和信息，需要在<code>while</code>
 循环中使用列表和字典。</p>
<p>
<code>for</code>
 循环是一种遍历列表的有效方式，但不应在<code>for</code>
 循环中修改列表，否则将导致Python难以跟踪其中的元素。要在遍历列表的同时对其进行修改，可使用<code>while</code>
 循环。通过将<code>while</code>
 循环同列表和字典结合起来使用，可收集、存储并组织大量输入，供以后查看和显示。</p>
<h3 id="nav_point_157">7.3.1　在列表之间移动元素</h3>
<p>假设有一个列表包含新注册但还未验证的网站用户。验证这些用户后，如何将他们移到另一个已验证用户列表中呢？一种办法是使用一个<code>while</code>
 循环，在验证用户的同时将其从未验证用户列表中提取出来，再将其加入另一个已验证用户列表中。代码可能类似于下面这样：</p>
<p>
<em>confirmed_users.py</em>
</p>
<pre class="代码无行号"><code>  # 首先，创建一个待验证用户列表
  # 和一个用于存储已验证用户的空列表。
❶ unconfirmed_users = ['alice', 'brian', 'candace']
  confirmed_users = []
  # 验证每个用户，直到没有未验证用户为止。
  # 将每个经过验证的用户都移到已验证用户列表中。
❷ while unconfirmed_users:
❸     current_user = unconfirmed_users.pop()
      print(f"Verifying user: {current_user.title()}")
❹     confirmed_users.append(current_user)
  # 显示所有已验证的用户。
  print("\nThe following users have been confirmed:")
  for confirmed_user in confirmed_users:
      print(confirmed_user.title())</code>
</pre>
<p>首先创建一个未验证用户列表（见❶），其中包含用户Alice、Brian和Candace，还创建了一个空列表，用于存储已验证的用户。❷处的<code>while</code>
 循环将不断运行，直到列表<code>unconfirmed_users</code>
 变成空的。在此循环中，❸处的方法<code>pop()</code>
 以每次一个的方式从列表<code>unconfirmed_users</code>
 末尾删除未验证的用户。由于Candace位于列表<code>unconfirmed_users</code>
 末尾，其名字将首先被删除、赋给变量<code>current_user</code>
 并加入列表<code>confirmed_users</code>
 中（见❹）。接下来是Brian，然后是Alice。</p>
<p>为模拟用户验证过程，我们打印一条验证消息并将用户加入已验证用户列表中。未验证用户列表越来越短，而已验证用户列表越来越长。未验证用户列表为空后结束循环，再打印已验证用户列表：</p>
<pre class="代码无行号"><code>Verifying user: Candace
Verifying user: Brian
Verifying user: Alice
The following users have been confirmed:
Candace
Brian
Alice</code>
</pre>
<h3 id="nav_point_158">7.3.2　删除为特定值的所有列表元素</h3>
<p>在第3章中，我们使用函数<code>remove()</code>
 来删除列表中的特定值。这之所以可行，是因为要删除的值只在列表中出现一次。如果要删除列表中所有为特定值的元素，该怎么办呢？</p>
<p>假设你有一个宠物列表，其中包含多个值为<code>'cat'</code>
 的元素。要删除所有这些元素，可不断运行一个<code>while</code>
 循环，直到列表中不再包含值<code>'cat'</code>
 ，如下所示：</p>
<p>
<em>pets.py</em>
</p>
<pre class="代码无行号"><code>pets = ['dog', 'cat', 'dog', 'goldfish', 'cat', 'rabbit', 'cat']
print(pets)
while 'cat' in pets:
    pets.remove('cat')
print(pets)</code>
</pre>
<p>首先创建一个列表，其中包含多个值为<code>'cat'</code>
 的元素。打印这个列表后，Python进入<code>while</code>
 循环，因为它发现<code>'cat'</code>
 在列表中至少出现了一次。进入该循环后，Python删除第一个<code>'cat'</code>
 并返回到<code>while</code>
 代码行，然后发现<code>'cat'</code>
 还包含在列表中，因此再次进入循环。它不断删除<code>'cat'</code>
 ，直到这个值不再包含在列表中，然后退出循环并再次打印列表：</p>
<pre class="代码无行号"><code>['dog', 'cat', 'dog', 'goldfish', 'cat', 'rabbit', 'cat']
['dog', 'dog', 'goldfish', 'rabbit']</code>
</pre>
<h3 id="nav_point_159">7.3.3　使用用户输入来填充字典</h3>
<p>可使用<code>while</code>
 循环提示用户输入任意多的信息。下面创建一个调查程序，其中的循环每次执行时都提示输入被调查者的名字和回答。我们将收集的数据存储在一个字典中，以便将回答同被调查者关联起来：</p>
<p>
<em>mountain_poll.py</em>
</p>
<pre class="代码无行号"><code>  responses = {}
  # 设置一个标志，指出调查是否继续。
  polling_active = True
  while polling_active:
      # 提示输入被调查者的名字和回答。
❶     name = input("\nWhat is your name? ")
      response = input("Which mountain would you like to climb someday? ")
      # 将回答存储在字典中。
❷     responses[name] = response
      # 看看是否还有人要参与调查。
❸     repeat = input("Would you like to let another person respond? (yes/ no) ")
      if repeat == 'no':
          polling_active = False
  # 调查结束，显示结果。
  print("\n--- Poll Results ---")
❹ for name, response in responses.items():
      print(f"{name} would like to climb {response}.")</code>
</pre>
<p>这个程序首先定义了一个空字典（<code>responses</code>
 ），并设置了一个标志（<code>polling_active</code>
 ）用于指出调查是否继续。只要<code>polling_active</code>
 为<code>True</code>
 ，Python就运行<code>while</code>
 循环中的代码。</p>
<p>在这个循环中，提示用户输入其名字及其喜欢爬哪座山（见❶）。将这些信息存储在字典<code>responses</code>
 中（见❷），然后询问用户是否继续调查（见❸）。如果用户输入<code>yes</code>
 ，程序将再次进入<code>while</code>
 循环；如果用户输入<code>no</code>
 ，标志<code>polling_active</code>
 将被设置为<code>False</code>
 ，而<code>while</code>
 循环将就此结束。最后一个代码块（见❹）显示调查结果。</p>
<p>如果运行这个程序，并输入一些名字和回答，输出将类似于下面这样：</p>
<pre class="代码无行号"><code>What is your name? Eric
Which mountain would you like to climb someday? Denali
Would you like to let another person respond? (yes/ no) yes
What is your name? Lynn
Which mountain would you like to climb someday? Devil's Thumb
Would you like to let another person respond? (yes/ no) no
--- Poll Results ---
Eric would like to climb Denali.
Lynn would like to climb Devil's Thumb.</code>
</pre>
<blockquote>
<p>
<strong>动手试一试</strong>
</p>
<p>
<strong>练习7-8：熟食店</strong>
 　创建一个名为<code>sandwich_orders</code>
 的列表，在其中包含各种三明治的名字，再创建一个名为<code>finished_sandwiches</code>
 的空列表。遍历列表<code>sandwich_orders</code>
 ，对于其中的每种三明治，都打印一条消息，如<code>I made your tuna sandwich</code>
 ，并将其移到列表<code>finished_sandwiches</code>
 中。所有三明治都制作好后，打印一条消息，将这些三明治列出来。</p>
<p>
<strong>练习7-9：五香烟熏牛肉卖完了</strong>
 　使用为完成练习7-8而创建的列表<code>sandwich_orders</code>
 ，并确保<code>'pastrami'</code>
 在其中至少出现了三次。在程序开头附近添加这样的代码：打印一条消息，指出熟食店的五香烟熏牛肉（pastrami）卖完了；再使用一个<code>while</code>
 循环将列表<code>sandwich_orders</code>
 中的<code>'pastrami'</code>
 都删除。确认最终的列表<code>finished_sandwiches</code>
 未包含<code>'pastrami'</code>
 。</p>
<p>
<strong>练习7-10：梦想的度假胜地</strong>
 　编写一个程序，调查用户梦想的度假胜地。使用类似于下面的提示，并编写一个打印调查结果的代码块。</p>
<p>If you could visit one place in the world, where would you go?</p>
</blockquote>
<h2 id="nav_point_160">7.4　小结</h2>
<p>在本章中，你学习了：如何在程序中使用<code>input()</code>
 来让用户提供信息；如何处理文本和数的输入，以及如何使用<code>while</code>
 循环让程序按用户的要求不断运行；多种控制<code>while</code>
 循环流程的方式：设置活动标志、使用<code>break</code>
 语句以及使用<code>continue</code>
 语句；如何使用<code>while</code>
 循环在列表之间移动元素，以及如何从列表中删除所有包含特定值的元素；如何结合使用<code>while</code>
 循环和字典。</p>
<p>在第8章中，你将学习<strong>函数</strong>
 。函数让你能够将程序分成多个很小的部分，每部分都负责完成一项具体任务。你可以根据需要调用同一个函数任意次，还可将函数存储在独立的文件中。使用函数可让你编写的代码效率更高、更容易维护和排除故障，还可在众多不同的程序中重用。</p>
<br style="page-break-after:always"/>
<div></div>
]]></description>
    <pubDate>Sat, 11 Jul 2026 01:43:48 +0000</pubDate>
    <guid isPermaLink="false">7</guid>
</item>
<item>
    <title>Basic Python - 006 - 第 6 章 字典.epub</title>
    <description><![CDATA[<p style='color: #888;'>⏱ 预计阅读时间：99 分钟 (19635 字)</p><hr><h1 id="nav_point_124">第 6 章　字典</h1>
<blockquote>
<p><img alt="" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/6_Image00001.gif" style="width: 16%" width="16%"/>
 　在本章中，你将学习能够将相关信息关联起来的Python字典，以及如何访问和修改字典中的信息。字典可存储的信息量几乎不受限制，因此我们会演示如何遍历字典中的数据。另外，你还将学习存储字典的列表、存储列表的字典和存储字典的字典。</p>
<p>理解字典后，就能够更准确地为各种真实物体建模。你可以创建一个表示人的字典，然后想在其中存储多少信息就存储多少信息：姓名、年龄、地址、职业，以及能描述他的任何方面。你还能够存储任意两种相关的信息，如一系列单词及其含义，一系列人名及其喜欢的数，以及一系列山脉及其海拔，等等。</p>
</blockquote>
<h2 id="nav_point_125">6.1　一个简单的字典</h2>
<p>来看一个包含外星人的游戏，这些外星人的颜色和分数各不相同。下面是一个简单的字典，存储了有关特定外星人的信息：</p>
<p>
<em>alien.py</em>
</p>
<pre class="代码无行号"><code>alien_0 = {'color': 'green', 'points': 5}
print(alien_0['color'])
print(alien_0['points'])</code>
</pre>
<p>字典<code>alien_0</code>
 存储了外星人的颜色和分数。最后两行代码访问并显示这些信息，结果如下：</p>
<pre class="代码无行号"><code>green
5</code>
</pre>
<p>与大多数编程概念一样，要熟练使用字典，也需要一段时间的练习。使用字典一段时间后，你就会明白为何它们能够高效地模拟现实世界中的情形。</p>
<h2 id="nav_point_126">6.2　使用字典</h2>
<p>在Python中，<strong>字典</strong>
 是一系列<strong>键值对</strong>
 。每个<strong>键</strong>
 都与一个值相关联，你可使用键来访问相关联的值。与键相关联的值可以是数、字符串、列表乃至字典。事实上，可将任何Python对象用作字典中的值。</p>
<p>在Python中，字典用放在花括号（<code>{}</code>
 ）中的一系列键值对表示，如前面的示例所示：</p>
<pre class="代码无行号"><code>alien_0 = {'color': 'green', 'points': 5}</code>
</pre>
<p>
<strong>键值对</strong>
 是两个相关联的值。指定键时，Python将返回与之相关联的值。键和值之间用冒号分隔，而键值对之间用逗号分隔。在字典中，想存储多少个键值对都可以。</p>
<p>最简单的字典只有一个键值对，如下述修改后的字典<code>alien_0</code>
 所示：</p>
<pre class="代码无行号"><code>alien_0 = {'color': 'green'}</code>
</pre>
<p>这个字典只存储了一项有关<code>alien_0</code>
 的信息，具体地说是这个外星人的颜色。在该字典中，字符串<code>'color'</code>
 是一个键，与之相关联的值为<code>'green'</code>
 。</p>
<h3 id="nav_point_127">6.2.1　访问字典中的值</h3>
<p>要获取与键相关联的值，可依次指定字典名和放在方括号内的键，如下所示：</p>
<p>
<em>alien.py</em>
</p>
<pre class="代码无行号"><code>alien_0 = {'color': 'green'}
print(alien_0['color'])</code>
</pre>
<p>这将返回字典<code>alien_0</code>
 中与键<code>'color'</code>
 相关联的值：</p>
<pre class="代码无行号"><code>green</code>
</pre>
<p>字典中可包含任意数量的键值对。例如，下面是最初的字典<code>alien_0</code>
 ，其中包含两个键值对：</p>
<pre class="代码无行号"><code>alien_0 = {'color': 'green', 'points': 5}</code>
</pre>
<p>现在，你可访问外星人<code>alien_0</code>
 的颜色和分数。如果玩家射杀了这个外星人，就可以使用下面的代码来确定应获得多少分：</p>
<pre class="代码无行号"><code>  alien_0 = {'color': 'green', 'points': 5}
❶ new_points = alien_0['points']
❷ print(f"You just earned {new_points} points!")</code>
</pre>
<p>上述代码首先定义了一个字典。然后，从这个字典中获取与键<code>'points'</code>
 相关联的值（见❶），并将这个值赋给变量<code>new_points</code>
 。接下来，将这个整数转换为字符串，并打印一条消息，指出玩家获得了多少分（见❷）：</p>
<pre class="代码无行号"><code>You just earned 5 points!</code>
</pre>
<p>如果在外星人被射杀时运行这段代码，就将获取该外星人的分数。</p>
<h3 id="nav_point_128">6.2.2　添加键值对</h3>
<p>字典是一种动态结构，可随时在其中添加键值对。要添加键值对，可依次指定字典名、用方括号括起的键和相关联的值。</p>
<p>下面来在字典<code>alien_0</code>
 中添加两项信息：外星人的<img alt="x" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/6_Image00003.gif"/>
 坐标和<img alt="y" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/6_Image00004.gif"/>
 坐标，让我们能够在屏幕的特定位置显示该外星人。我们将这个外星人放在屏幕左边缘，且离屏幕顶部25像素的地方。由于屏幕坐标系的原点通常为左上角，要将该外星人放在屏幕左边缘，可将<img alt="x" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/6_Image00003.gif"/>
 坐标设置为0；要将该外星人放在离屏幕顶部25像素的地方，可将<img alt="y" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/6_Image00004.gif"/>
 坐标设置为25，如下所示：</p>
<p>
<em>alien.py</em>
</p>
<pre class="代码无行号"><code>  alien_0 = {'color': 'green', 'points': 5}
  print(alien_0)
❶ alien_0['x_position'] = 0
❷ alien_0['y_position'] = 25
  print(alien_0)</code>
</pre>
<p>首先定义前面一直在使用的字典，然后打印这个字典，以显示其信息快照。在❶处，我们在这个字典中新增了一个键值对，其中的键为<code>'x_position'</code>
 ，值为0。在❷处重复这样的操作，但使用的键为<code>'y_position'</code>
 。打印修改后的字典时，将看到这两个新增的键值对：</p>
<pre class="代码无行号"><code>{'color': 'green', 'points': 5}
{'color': 'green', 'points': 5, 'y_position': 25, 'x_position': 0}</code>
</pre>
<p>这个字典的最终版本包含四个键值对：原来的两个指定外星人的颜色和分数，而新增的两个指定其位置。</p>
<blockquote>
<p>
<strong>注意</strong>
 　在Python 3.7中，字典中元素的排列顺序与定义时相同。如果将字典打印出来或遍历其元素，将发现元素的排列顺序与添加顺序相同。</p>
</blockquote>
<h3 id="nav_point_129">6.2.3　先创建一个空字典</h3>
<p>在空字典中添加键值对有时候可提供便利，而有时候必须这样做。为此，可先使用一对空花括号定义一个字典，再分行添加各个键值对。例如，下面演示了如何以这种方式创建字典<code>alien_0</code>
 ：</p>
<p>
<em>alien.py</em>
</p>
<pre class="代码无行号"><code>alien_0 = {}
alien_0['color'] = 'green'
alien_0['points'] = 5
print(alien_0)</code>
</pre>
<p>这里首先定义了空字典<code>alien_0</code>
 ，再在其中添加颜色和分数，得到前述示例一直在使用的字典：</p>
<pre class="代码无行号"><code>{'color': 'green', 'points': 5}</code>
</pre>
<p>使用字典来存储用户提供的数据或在编写能自动生成大量键值对的代码时，通常需要先定义一个空字典。</p>
<h3 id="nav_point_130">6.2.4　修改字典中的值</h3>
<p>要修改字典中的值，可依次指定字典名、用方括号括起的键，以及与该键相关联的新值。例如，假设随着游戏的进行，需要将一个外星人从绿色改为黄色：</p>
<p>
<em>alien.py</em>
</p>
<pre class="代码无行号"><code>alien_0 = {'color': 'green'}
print(f"The alien is {alien_0['color']}.")
alien_0['color'] = 'yellow'
print(f"The alien is now {alien_0['color']}.")</code>
</pre>
<p>首先定义一个表示外星人<code>alien_0</code>
 的字典，其中只包含这个外星人的颜色。接下来，将与键<code>'color'</code>
 相关联的值改为<code>'yellow'</code>
 。输出表明，这个外星人确实从绿色变成了黄色：</p>
<pre class="代码无行号"><code>The alien is green.
The alien is now yellow.</code>
</pre>
<p>来看一个更有趣的例子，对一个能够以不同速度移动的外星人进行位置跟踪。为此，我们将存储该外星人的当前速度，并据此确定该外星人将向右移动多远：</p>
<pre class="代码无行号"><code>  alien_0 = {'x_position': 0, 'y_position': 25, 'speed': 'medium'}
  print(f"Original position: {alien_0['x_position']}")
  # 向右移动外星人。
  # 根据当前速度确定将外星人向右移动多远。
❶ if alien_0['speed'] == 'slow':
      x_increment = 1
  elif alien_0['speed'] == 'medium':
      x_increment = 2
  else:
      # 这个外星人的移动速度肯定很快。
      x_increment = 3
  # 新位置为旧位置加上移动距离。
❷ alien_0['x_position'] = alien_0['x_position'] + x_increment
  print(f"New position: {alien_0['x_position']}")</code>
</pre>
<p>首先定义一个外星人，其中包含初始<img alt="x" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/6_Image00003.gif"/>
 坐标和<img alt="y" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/6_Image00004.gif"/>
 坐标，还有速度<code>'medium'</code>
 。出于简化考虑，省略了颜色和分数，但即便包含这些键值对，本例的工作原理也不会有任何变化。我们还打印了<code>x_position</code>
 的初始值，旨在让用户知道这个外星人向右移动了多远。</p>
<p>❶处使用一个<code>if-elif-else</code>
 结构来确定外星人应向右移动多远，并将这个值赋给变量<code>x_increment</code>
 。如果外星人的速度为<code>'slow'</code>
 ，它将向右移动一个单位；如果速度为<code>'medium'</code>
 ，将向右移动两个单位；如果为<code>'fast'</code>
 ，将向右移动三个单位。确定移动距离后，将其与<code>x_position</code>
 的当前值相加（见❷），再将结果关联到字典中的键<code>x_position</code>
 。</p>
<p>因为这是一个速度中等的外星人，所以其位置将向右移两个单位：</p>
<pre class="代码无行号"><code>Original x-position: 0
New x-position: 2</code>
</pre>
<p>这种技术很棒：通过修改外星人字典中的值，可改变外星人的行为。例如，要将这个速度中等的外星人变成速度很快的外星人，可添加如下代码行：</p>
<pre class="代码无行号"><code>alien_0['speed'] = 'fast'</code>
</pre>
<p>这样，再次运行这些代码时，其中的<code>if-elif-else</code>
 结构将把一个更大的值赋给变量<code>x_increment</code>
 。</p>
<h3 id="nav_point_131">6.2.5　删除键值对</h3>
<p>对于字典中不再需要的信息，可使用<code>del</code>
 语句将相应的键值对彻底删除。使用<code>del</code>
 语句时，必须指定字典名和要删除的键。</p>
<p>例如，下面的代码从字典<code>alien_0</code>
 中删除键<code>'points'</code>
 及其值：</p>
<p>
<em>alien.py</em>
</p>
<pre class="代码无行号"><code>  alien_0 = {'color': 'green', 'points': 5}
  print(alien_0)
❶ del alien_0['points']
  print(alien_0)</code>
</pre>
<p>❶处的代码行让Python将键<code>'points'</code>
 从字典<code>alien_0</code>
 中删除，同时删除与这个键相关联的值。输出表明，键<code>'points'</code>
 及其值<code>5</code>
 已从字典中删除，但其他键值对未受影响：</p>
<pre class="代码无行号"><code>{'color': 'green', 'points': 5}
{'color': 'green'}</code>
</pre>
<blockquote>
<p>
<strong>注意</strong>
 　删除的键值对会永远消失。</p>
</blockquote>
<h3 id="nav_point_132">6.2.6　由类似对象组成的字典</h3>
<p>在前面的示例中，字典存储的是一个对象（游戏中的一个外星人）的多种信息，但你也可以使用字典来存储众多对象的同一种信息。例如，假设你要调查很多人，询问他们最喜欢的编程语言，可使用一个字典来存储这种简单调查的结果，如下所示：</p>
<pre class="代码无行号"><code>favorite_languages = {
    'jen': 'python',
    'sarah': 'c',
    'edward': 'ruby',
    'phil': 'python',
    }</code>
</pre>
<p>如你所见，我们将一个较大的字典放在了多行中。每个键都是一个被调查者的名字，而每个值都是被调查者喜欢的语言。确定需要使用多行来定义字典时，要在输入左花括号后按回车键。在下一行缩进四个空格，指定第一个键值对，并在它后面加上一个逗号。此后再按回车键时，文本编辑器将自动缩进后续键值对，且缩进量与第一个键值对相同。</p>
<p>定义好字典后，在最后一个键值对的下一行添加一个右花括号，并缩进四个空格，使其与字典中的键对齐。一种不错的做法是，在最后一个键值对后面也加上逗号，为以后在下一行添加键值对做好准备。</p>
<blockquote>
<p>
<strong>注意</strong>
 　对于较长的列表和字典，大多数编辑器提供了以类似方式设置格式的功能。对于较长的字典，还有其他一些可行的格式设置方式，因此在你的编辑器或其他源代码中，你可能会看到稍微不同的格式设置方式。</p>
</blockquote>
<p>给定被调查者的名字，可使用这个字典轻松地获悉他喜欢的语言：</p>
<p>
<em>favorite_languages.py</em>
</p>
<pre class="代码无行号"><code>  favorite_languages = {
      'jen': 'python',
      'sarah': 'c',
      'edward': 'ruby',
      'phil': 'python',
      }
❶ language = favorite_languages['sarah'].title()
  print(f"Sarah's favorite language is {language}.")</code>
</pre>
<p>为获悉Sarah喜欢的语言，我们使用如下代码：</p>
<pre class="代码无行号"><code>favorite_languages['sarah']</code>
</pre>
<p>在❶处，使用这种语法获取Sarah喜欢的语言，并将其赋给变量<code>language</code>
 。创建这个新变量让函数调用<code>print()</code>
 变得整洁得多。输出指出了Sarah喜欢的语言：</p>
<pre class="代码无行号"><code>Sarah's favorite language is C.</code>
</pre>
<p>这种语法可用来从字典中获取任何人喜欢的语言。</p>
<h3 id="nav_point_133">6.2.7　使用<code>get()</code>
 来访问值</h3>
<p>使用放在方括号内的键从字典中获取感兴趣的值时，可能会引发问题：如果指定的键不存在就会出错。</p>
<p>如果你要求获取外星人的分数，而这个外星人没有分数，结果将如何呢？下面来看一看：</p>
<p>
<em>alien_no_points.py</em>
</p>
<pre class="代码无行号"><code>alien_0 = {'color': 'green', 'speed': 'slow'}
print(alien_0['points'])</code>
</pre>
<p>这将导致Python显示traceback，指出存在键值错误（<code>KeyError</code>
 ）：</p>
<pre class="代码无行号"><code>Traceback (most recent call last):
  File "alien_no_points.py", line 2, in &lt;module&gt;
    print(alien_0['points'])
KeyError: 'points'</code>
</pre>
<p>第10章将详细介绍如何处理类似的错误，但就字典而言，可使用方法<code>get()</code>
 在指定的键不存在时返回一个默认值，从而避免这样的错误。</p>
<p>方法<code>get()</code>
 的第一个参数用于指定键，是必不可少的；第二个参数为指定的键不存在时要返回的值，是可选的：</p>
<pre class="代码无行号"><code>alien_0 = {'color': 'green', 'speed': 'slow'}
point_value = alien_0.get('points', 'No point value assigned.')
print(point_value)</code>
</pre>
<p>如果字典中有键<code>'points'</code>
 ，将获得与之相关联的值；如果没有，将获得指定的默认值。虽然这里没有键<code>'points'</code>
 ，但将获得一条清晰的消息，不会引发错误：</p>
<pre class="代码无行号"><code>No point value assigned.</code>
</pre>
<p>如果指定的键有可能不存在，应考虑使用方法<code>get()</code>
 ，而不要使用方括号表示法。</p>
<blockquote>
<p>
<strong>注意</strong>
 　调用<code>get()</code>
 时，如果没有指定第二个参数且指定的键不存在，Python将返回值<code>None</code>
 。这个特殊值表示没有相应的值。<code>None</code>
 并非错误，而是一个表示所需值不存在的特殊值，第8章将介绍它的其他用途。</p>
</blockquote>
<p>　</p>
<blockquote>
<p>
<strong>动手试一试</strong>
</p>
<p>
<strong>练习6-1：人</strong>
 　使用一个字典来存储一个熟人的信息，包括名、姓、年龄和居住的城市。该字典应包含键<code>first_name</code>
 、<code>last_name</code>
 、<code>age</code>
 和<code>city</code>
 。将存储在该字典中的每项信息都打印出来。</p>
<p>
<strong>练习6-2：喜欢的数</strong>
 　使用一个字典来存储一些人喜欢的数。请想出5个人的名字，并将这些名字用作字典中的键；找出每个人喜欢的一个数，并将这些数作为值存储在字典中。打印每个人的名字和喜欢的数。为了让这个程序更有趣，通过询问朋友确保数据是真实的。</p>
<p>
<strong>练习6-3：词汇表</strong>
 　Python字典可用于模拟现实生活中的字典。为避免混淆，我们将后者称为词汇表。</p>
<ul>
<li class="第2级无序列表">想出你在前面学过的5个编程术语，将其用作词汇表中的键，并将它们的含义作为值存储在词汇表中。</li>
<li class="第2级无序列表">以整洁的方式打印每个术语及其含义。为此，可先打印术语，在它后面加上一个冒号，再打印其含义；也可在一行打印术语，再使用换行符（<code>\n</code>
 ）插入一个空行，然后在下一行以缩进的方式打印其含义。</li>
</ul>
</blockquote>
<h2 id="nav_point_134">6.3　遍历字典</h2>
<p>一个Python字典可能只包含几个键值对，也可能包含数百万个键值对。鉴于字典可能包含大量数据，Python支持对字典进行遍历。字典可用于以各种方式存储信息，因此有多种遍历方式：可遍历字典的所有键值对，也可仅遍历键或值。</p>
<h3 id="nav_point_135">6.3.1　遍历所有键值对</h3>
<p>探索各种遍历方法前，先来看一个新字典，它用于存储有关网站用户的信息。下面的字典存储一名用户的用户名、名和姓：</p>
<pre class="代码无行号"><code>user_0 = {
    'username': 'efermi',
    'first': 'enrico',
    'last': 'fermi',
    }</code>
</pre>
<p>利用本章前面介绍过的知识，可访问<code>user_0</code>
 的任何一项信息，但如果要获悉该用户字典中的所有信息，该如何办呢？可使用<code>for</code>
 循环来遍历这个字典：</p>
<p>
<em>user.py</em>
</p>
<pre class="代码无行号"><code>  user_0 = {
      'username': 'efermi',
      'first': 'enrico',
      'last': 'fermi',
      }
❶ for key, value in user_0.items():
❷     print(f"\nKey: {key}")
❸     print(f"Value: {value}")</code>
</pre>
<p>如❶所示，要编写遍历字典的<code>for</code>
 循环，可声明两个变量，用于存储键值对中的键和值。这两个变量可以使用任意名称。下面的代码使用了简单的变量名，这完全可行：</p>
<pre class="代码无行号"><code>for k, v in user_0.items()</code>
</pre>
<p>
<code>for</code>
 语句的第二部分包含字典名和方法<code>items()</code>
 （见❶），它返回一个键值对列表。接下来，<code>for</code>
 循环依次将每个键值对赋给指定的两个变量。在本例中，使用这两个变量来打印每个键（见❷）及其相关联的值（见❸）。第一个函数调用<code>print()</code>
 中的<code>"\n"</code>
 确保在输出每个键值对前都插入一个空行：</p>
<pre class="代码无行号"><code>Key: username
Value: efermi
Key: first
Value: enrico
Key: last
Value: fermi</code>
</pre>
<p>在6.2.6节的示例favorite_languages.py中，字典存储的是不同人的同一种信息。对于类似这样的字典，遍历所有的键值对很合适。如果遍历字典<code>favorite_languages</code>
 ，将得到其中每个人的姓名和喜欢的编程语言。由于该字典中的键都是人名，值都是语言，因此在循环中使用变量<code>name</code>
 和<code>language</code>
 ，而不是<code>key</code>
 和<code>value</code>
 。这让人更容易明白循环的作用：</p>
<p>
<em>favorite_languages.py</em>
</p>
<pre class="代码无行号"><code>  favorite_languages = {
      'jen': 'python',
      'sarah': 'c',
      'edward': 'ruby',
      'phil': 'python',
      }
❶ for name, language in favorite_languages.items():
❷     print(f"{name.title()}'s favorite language is {language.title()}.")</code>
</pre>
<p>❶处的代码让Python遍历字典中的每个键值对，并将键赋给变量<code>name</code>
 ，将值赋给变量<code>language</code>
 。这些描述性名称能够让人非常轻松地明白函数调用<code>print()</code>
 （见❷）是做什么的。</p>
<p>仅使用几行代码，就将全部调查结果显示出来了：</p>
<pre class="代码无行号"><code>Jen's favorite language is Python.
Sarah's favorite language is C.
Edward's favorite language is Ruby.
Phil's favorite language is Python.</code>
</pre>
<p>即便字典存储的是上千乃至上百万人的调查结果，这种循环也管用。</p>
<h3 id="nav_point_136">6.3.2　遍历字典中的所有键</h3>
<p>在不需要使用字典中的值时，方法<code>keys()</code>
 很有用。下面来遍历字典<code>favorite_languages</code>
 ，并将每个被调查者的名字都打印出来：</p>
<pre class="代码无行号"><code>  favorite_languages = {
      'jen': 'python',
      'sarah': 'c',
      'edward': 'ruby',
      'phil': 'python',
      }
❶ for name in favorite_languages.keys():
      print(name.title())</code>
</pre>
<p>❶处的代码行让Python提取字典<code>favorite_languages</code>
 中的所有键，并依次将它们赋给变量<code>name</code>
 。输出列出了每个被调查者的名字：</p>
<pre class="代码无行号"><code>Jen
Sarah
Edward
Phil</code>
</pre>
<p>遍历字典时，会默认遍历所有的键。因此，如果将上述代码中的：</p>
<pre class="代码无行号"><code>for name in favorite_languages:</code>
</pre>
<p>替换为：</p>
<pre class="代码无行号"><code>for name in favorite_languages.keys():</code>
</pre>
<p>输出将不变。</p>
<p>显式地使用方法<code>keys()</code>
 可让代码更容易理解，你可以选择这样做，但是也可以省略它。</p>
<p>在这种循环中，可使用当前键来访问与之相关联的值。下面来打印两条消息，指出两位朋友喜欢的语言。像前面一样遍历字典中的名字，但在名字为指定朋友的名字时，打印一条消息，指出其喜欢的语言：</p>
<pre class="代码无行号"><code>  favorite_languages = {
      --snip--
      }
❶ friends = ['phil', 'sarah']
  for name in favorite_languages.keys():
      print(f"Hi {name.title()}.")
❷     if name in friends:
❸         language = favorite_languages[name].title()
          print(f"\t{name.title()}, I see you love {language}!")</code>
</pre>
<p>❶处创建了一个列表，其中包含要收到打印消息的朋友。在循环中，打印每个人的名字，并检查当前的名字是否在列表<code>friends</code>
 中（见❷）。如果在，就打印一句特殊的问候语，其中包含这位朋友喜欢的语言。为获悉朋友喜欢的语言，我们使用了字典名，并将变量<code>name</code>
 的当前值作为键（见❸）。</p>
<p>每个人的名字都会被打印，但只对朋友打特殊消息：</p>
<pre class="代码无行号"><code>Hi Jen.
Hi Sarah.
    Sarah, I see you love C!
Hi Edward.
Hi Phil.
    Phil, I see you love Python!</code>
</pre>
<p>还可使用方法<code>keys()</code>
 确定某个人是否接受了调查。下面的代码确定Erin是否接受了调查：</p>
<pre class="代码无行号"><code>  favorite_languages = {
      'jen': 'python',
      'sarah': 'c',
      'edward': 'ruby',
      'phil': 'python',
      }
❶ if 'erin' not in favorite_languages.keys():
      print("Erin, please take our poll!")</code>
</pre>
<p>方法<code>keys()</code>
 并非只能用于遍历：实际上，它返回一个列表，其中包含字典中的所有键。因此❶处的代码行只核实<code>'erin'</code>
 是否包含在这个列表中。因为她并不包含在这个列表中，所以打印一条消息，邀请她参加调查：</p>
<pre class="代码无行号"><code>Erin, please take our poll!</code>
</pre>
<h3 id="nav_point_137">6.3.3　按特定顺序遍历字典中的所有键</h3>
<p>从Python 3.7起，遍历字典时将按插入的顺序返回其中的元素。不过在有些情况下，你可能要按与此不同的顺序遍历字典。</p>
<p>要以特定顺序返回元素，一种办法是在<code>for</code>
 循环中对返回的键进行排序。为此，可使用函数<code>sorted()</code>
 来获得按特定顺序排列的键列表的副本：</p>
<pre class="代码无行号"><code>favorite_languages = {
    'jen': 'python',
    'sarah': 'c',
    'edward': 'ruby',
    'phil': 'python',
    }
for name in sorted(favorite_languages.keys()):
    print(f"{name.title()}, thank you for taking the poll.")</code>
</pre>
<p>这条<code>for</code>
 语句类似于其他<code>for</code>
 语句，不同之处是对方法<code>dictionary.keys()</code>
 的结果调用了函数<code>sorted()</code>
 。这让Python列出字典中的所有键，并在遍历前对这个列表进行排序。输出表明，按顺序显示了所有被调查者的名字：</p>
<pre class="代码无行号"><code>Edward, thank you for taking the poll.
Jen, thank you for taking the poll.
Phil, thank you for taking the poll.
Sarah, thank you for taking the poll.</code>
</pre>
<h3 id="nav_point_138">6.3.4　遍历字典中的所有值</h3>
<p>如果主要对字典包含的值感兴趣，可使用方法<code>values()</code>
 来返回一个值列表，不包含任何键。例如，假设我们想获得一个列表，其中只包含被调查者选择的各种语言，而不包含被调查者的名字，可以这样做：</p>
<pre class="代码无行号"><code>favorite_languages = {
    'jen': 'python',
    'sarah': 'c',
    'edward': 'ruby',
    'phil': 'python',
    }
print("The following languages have been mentioned:")
for language in favorite_languages.values():
    print(language.title())</code>
</pre>
<p>这条<code>for</code>
 语句提取字典中的每个值，并将其依次赋给变量<code>language</code>
 。通过打印这些值，就获得了一个包含被调查者所选择语言的列表：</p>
<pre class="代码无行号"><code>The following languages have been mentioned:
Python
C
Ruby
Python</code>
</pre>
<p>这种做法提取字典中所有的值，而没有考虑是否重复。涉及的值很少时，这也许不是问题，但如果被调查者很多，最终的列表可能包含大量重复项。为剔除重复项，可使用集合（set）。<strong>集合</strong>
 中的每个元素都必须是独一无二的：</p>
<pre class="代码无行号"><code>  favorite_languages = {
      --snip--
      }
  print("The following languages have been mentioned:")
❶ for language in set(favorite_languages.values()):
      print(language.title())</code>
</pre>
<p>通过对包含重复元素的列表调用<code>set()</code>
 ，可让Python找出列表中独一无二的元素，并使用这些元素来创建一个集合。❶处使用<code>set()</code>
 来提取<code>favorite_languages.values()</code>
 中不同的语言。</p>
<p>结果是一个不重复的列表，其中列出了被调查者提及的所有语言：</p>
<pre class="代码无行号"><code>The following languages have been mentioned:
Python
C
Ruby</code>
</pre>
<p>随着你更深入地学习Python，经常会发现它内置的功能可帮助你以希望的方式处理数据。</p>
<blockquote>
<p>
<strong>注意</strong>
 　可使用一对花括号直接创建集合，并在其中用逗号分隔元素：</p>
<pre class="代码无行号"><code>&gt;&gt;&gt; languages = {'python', 'ruby', 'python', 'c'}
&gt;&gt;&gt; languages
{'ruby', 'python', 'c'}</code>
</pre>
<p>集合和字典很容易混淆，因为它们都是用一对花括号定义的。当花括号内没有键值对时，定义的很可能是集合。不同于列表和字典，集合不会以特定的顺序存储元素。</p>
</blockquote>
<p>　</p>
<blockquote>
<p>
<strong>动手试一试</strong>
</p>
<p>
<strong>练习6-4：词汇表2</strong>
 　现在你知道了如何遍历字典，可以整理为完成练习6-3而编写的代码，将其中的一系列函数调用<code>print()</code>
 替换为一个遍历字典中键和值的循环。确定该循环正确无误后，再在词汇表中添加5个Python术语。当你再次运行这个程序时，这些新术语及其含义将自动包含在输出中。</p>
<p>
<strong>练习6-5：河流</strong>
 　创建一个字典，在其中存储三条重要河流及其流经的国家。例如，一个键值对可能是<code>'nile': 'egypt'</code>
 。</p>
<ul>
<li>
<p>使用循环为每条河流打印一条消息，下面是一个例子。</p>
<p>The Nile runs through Egypt.<br/>
 </p>
</li>
<li>
<p>使用循环将该字典中每条河流的名字打印出来。</p>
</li>
<li class="第2级无序列表">使用循环将该字典包含的每个国家的名字打印出来。</li>
</ul>
<p>
<strong>练习6-6：调查</strong>
 　在6.3.1节编写的程序favorite_languages.py中执行以下操作。</p>
<ul>
<li class="第2级无序列表">创建一个应该会接受调查的人员名单，其中有些人已包含在字典中，而其他人未包含在字典中。</li>
<li class="第2级无序列表">遍历这个人员名单。对于已参与调查的人，打印一条消息表示感谢；对于还未参与调查的人，打印一条消息邀请他参加。</li>
</ul>
</blockquote>
<h2 id="nav_point_139">6.4　嵌套</h2>
<p>有时候，需要将一系列字典存储在列表中，或将列表作为值存储在字典中，这称为<strong>嵌套</strong>
 。你可以在列表中嵌套字典、在字典中嵌套列表甚至在字典中嵌套字典。正如下面的示例将演示的，嵌套是一项强大的功能。</p>
<h3 id="nav_point_140">6.4.1　字典列表</h3>
<p>字典<code>alien_0</code>
 包含一个外星人的各种信息，但无法存储第二个外星人的信息，更别说屏幕上全部外星人的信息了。如何管理成群结队的外星人呢？一种办法是创建一个外星人列表，其中每个外星人都是一个字典，包含有关该外星人的各种信息。例如，下面的代码创建一个包含三个外星人的列表：</p>
<p>
<em>aliens.py</em>
</p>
<pre class="代码无行号"><code>  alien_0 = {'color': 'green', 'points': 5}
  alien_1 = {'color': 'yellow', 'points': 10}
  alien_2 = {'color': 'red', 'points': 15}
❶ aliens = [alien_0, alien_1, alien_2]
  for alien in aliens:
      print(alien)</code>
</pre>
<p>首先创建三个字典，其中每个字典都表示一个外星人。然后在❶处将这些字典都存储到一个名为<code>aliens</code>
 的列表中。最后，遍历这个列表，并将每个外星人都打印出来：</p>
<pre class="代码无行号"><code>{'color': 'green', 'points': 5}
{'color': 'yellow', 'points': 10}
{'color': 'red', 'points': 15}</code>
</pre>
<p>更符合现实的情形是，外星人不止三个，且每个外星人都是使用代码自动生成的。在下面的示例中，使用<code>range()</code>
 生成了30个外星人：</p>
<pre class="代码无行号"><code>  # 创建一个用于存储外星人的空列表。
  aliens = []
  # 创建30个绿色的外星人。
❶ for alien_number in range(30):
❷     new_alien = {'color': 'green', 'points': 5, 'speed': 'slow'}
❸     aliens.append(new_alien)
  # 显示前5个外星人。
❹ for alien in aliens[:5]:
      print(alien)
  print("...")
  # 显示创建了多少个外星人。
❺ print(f"Total number of aliens: {len(aliens)}")</code>
</pre>
<p>在本例中，首先创建一个空列表，用于存储接下来将创建的所有外星人。在❶处，<code>range()</code>
 返回一系列数，其唯一的用途是告诉Python要重复这个循环多少次。每次执行这个循环时，都创建一个外星人（见❷），并将其附加到列表<code>aliens</code>
 末尾（见❸）。在❹处，使用一个切片来打印前5个外星人。在❺处，打印列表的长度，以证明确实创建了30个外星人：</p>
<pre class="代码无行号"><code>{'color': 'green', 'points': 5, 'speed': slow'}
{'color': 'green', 'points': 5, 'speed': slow'}
{'color': 'green', 'points': 5, 'speed': slow'}
{'color': 'green', 'points': 5, 'speed': slow'}
{'color': 'green', 'points': 5, 'speed': slow'}
...
Total number of aliens: 30</code>
</pre>
<p>这些外星人都具有相同的特征，但在Python看来，每个外星人都是独立的，这让我们能够独立地修改每个外星人。</p>
<p>在什么情况下需要处理成群结队的外星人呢？想象一下，可能随着游戏的进行，有些外星人会变色且加快移动速度。必要时，可使用<code>for</code>
 循环和<code>if</code>
 语句来修改某些外形人的颜色。例如，要将前三个外星人修改为黄色、速度为中等且值10分，可这样做：</p>
<pre class="代码无行号"><code># 创建一个用于存储外星人的空列表。
aliens = []
# 创建30个绿色的外星人。
for alien_number in range (30):
    new_alien = {'color': 'green', 'points': 5, 'speed': 'slow'}
    aliens.append(new_alien)
for alien in aliens[:3]:
    if alien['color'] == 'green':
        alien['color'] = 'yellow'
        alien['speed'] = 'medium'
        alien['points'] = 10
# 显示前5个外星人。
for alien in aliens[:5]:
    print(alien)
print("...")</code>
</pre>
<p>鉴于要修改前三个外星人，我们遍历一个只包含这些外星人的切片。当前，所有外星人都是绿色的，但情况并非总是如此，因此编写一条<code>if</code>
 语句来确保只修改绿色外星人。如果外星人是绿色的，就将其颜色改为<code>'yellow'</code>
 ，将其速度改为<code>'medium'</code>
 ，并将其分数改为<code>10</code>
 ，如下面的输出所示：</p>
<pre class="代码无行号"><code>{'color': 'yellow', 'points': 10, 'speed': 'medium'}
{'color': 'yellow', 'points': 10, 'speed': 'medium'}
{'color': 'yellow', 'points': 10, 'speed': 'medium'}
{'color': 'green', 'points': 5, 'speed': 'slow'}
{'color': 'green', 'points': 5, 'speed': 'slow'}
...</code>
</pre>
<p>可进一步扩展这个循环，在其中添加一个<code>elif</code>
 代码块，将黄色外星人改为移动速度快且值15分的红色外星人，如下所示（这里只列出了循环，而没有列出整个程序）：</p>
<pre class="代码无行号"><code>for alien in aliens[0:3]:
    if alien['color'] == 'green':
        alien['color'] = 'yellow'
        alien['speed'] = 'medium'
        alien['points'] = 10
    elif alien['color'] == 'yellow':
        alien['color'] = 'red'
        alien['speed'] = 'fast'
        alien['points'] = 15</code>
</pre>
<p>经常需要在列表中包含大量的字典，而其中每个字典都包含特定对象的众多信息。例如，你可能需要为网站的每个用户创建一个字典（就像6.3.1节的user.py中那样），并将这些字典存储在一个名为<code>users</code>
 的列表中。在这个列表中，所有字典的结构都相同，因此你可以遍历这个列表，并以相同的方式处理其中的每个字典。</p>
<h3 id="nav_point_141">6.4.2　在字典中存储列表</h3>
<p>有时候，需要将列表存储在字典中，而不是将字典存储在列表中。例如，你如何描述顾客点的比萨呢？如果使用列表，只能存储要添加的比萨配料；但如果使用字典，就不仅可在其中包含配料列表，还可包含其他有关比萨的描述。</p>
<p>在下面的示例中，存储了比萨的两方面信息：外皮类型和配料列表。配料列表是一个与键<code>'toppings'</code>
 相关联的值。要访问该列表，我们使用字典名和键<code>'toppings'</code>
 ，就像访问字典中的其他值一样。这将返回一个配料列表，而不是单个值：</p>
<p>
<em>pizza.py</em>
</p>
<pre class="代码无行号"><code>  # 存储所点比萨的信息。
❶ pizza = {
      'crust': 'thick',
      'toppings': ['mushrooms', 'extra cheese'],
      }
  # 概述所点的比萨。
❷ print(f"You ordered a {pizza['crust']}-crust pizza "
      "with the following toppings:")
❸ for topping in pizza['toppings']:
      print("\"f+topping)</code>
</pre>
<p>首先创建一个字典，其中存储了有关顾客所点比萨的信息（见❶）。在这个字典中，一个键是<code>'crust'</code>
 ，与之相关联的值是字符串<code>'thick'</code>
 ；下一个键是<code>'toppings'</code>
 ，与之相关联的值是一个列表，其中存储了顾客要求添加的所有配料。制作前，我们概述了顾客所点的比萨（见❷）。如果函数调用<code>print()</code>
 中的字符串很长，可以在合适的位置分行。只需要在每行末尾都加上引号，同时对于除第一行外的其他各行，都在行首加上引号并缩进。这样，Python将自动合并圆括号内的所有字符串。为打印配料，编写一个<code>for</code>
 循环（见❸）。为访问配料列表，使用键<code>'toppings'</code>
 ，这样Python将从字典中提取配料列表。</p>
<p>下面的输出概述了要制作的比萨：</p>
<pre class="代码无行号"><code>You ordered a thick-crust pizza with the following toppings:
    mushrooms
    extra cheese</code>
</pre>
<p>每当需要在字典中将一个键关联到多个值时，都可以在字典中嵌套一个列表。在本章前面有关喜欢的编程语言的示例中，如果将每个人的回答都存储在一个列表中，被调查者就可选择多种喜欢的语言。在这种情况下，当我们遍历字典时，与每个被调查者相关联的都是一个语言列表，而不是一种语言；因此，在遍历该字典的<code>for</code>
 循环中，我们需要再使用一个<code>for</code>
 循环来遍历与被调查者相关联的语言列表：</p>
<p>
<em>favorite_languages.py</em>
</p>
<pre class="代码无行号"><code>❶ favorite_languages = {
         'jen': ['python', 'ruby'],
      'sarah': ['c'],
      'edward': ['ruby', 'go'],
      'phil': ['python', 'haskell'],
      }
❷ for name, languages in favorite_languages.items():
      print(f"\n{name.title()}'s favorite languages are:")
❸     for language in languages:
          print(f"\t{language.title()}")</code>
</pre>
<p>如你所见，现在与每个名字相关联的值都是一个列表（见❶）。请注意，有些人喜欢的语言只有一种，而有些人有多种。遍历字典时（见❷），使用变量<code>languages</code>
 来依次存储对字典中每个值的引用，因为我们知道这些值都是列表。在遍历字典的主循环中，使用了另一个<code>for</code>
 循环（见❸）来遍历每个人喜欢的语言列表。现在，每个人想列出多少种喜欢的语言都可以：</p>
<pre class="代码无行号"><code>Jen's favorite languages are:
    Python
    Ruby
Sarah's favorite languages are:
    C
Edward's favorite languages are:
    Ruby
    Go
Phil's favorite languages are:
    Python
    Haskell</code>
</pre>
<p>为进一步改进这个程序，可在遍历字典的<code>for</code>
 循环开头添加一条<code>if</code>
 语句，通过查看<code>len(languages)</code>
 的值来确定当前的被调查者喜欢的语言是否有多种。如果他喜欢的语言有多种，就像以前一样显示输出；如果只有一种，就相应修改输出的措辞，如显示<code>Sarah's favorite language is C</code>
 。</p>
<blockquote>
<p>
<strong>注意</strong>
 　列表和字典的嵌套层级不应太多。如果嵌套层级比前面的示例多得多，很可能有更简单的解决方案。</p>
</blockquote>
<h3 id="nav_point_142">6.4.3　在字典中存储字典</h3>
<p>可在字典中嵌套字典，但这样做时，代码可能很快复杂起来。例如，如果有多个网站用户，每个都有独特的用户名，可在字典中将用户名作为键，然后将每位用户的信息存储在一个字典中，并将该字典作为与用户名相关联的值。在下面的程序中，存储了每位用户的三项信息：名、姓和居住地。为访问这些信息，我们遍历所有的用户名，并访问与每个用户名相关联的信息字典：</p>
<p>
<em>many_users.py</em>
</p>
<pre class="代码无行号"><code>  users = {
      'aeinstein': {
          'first': 'albert',
          'last': 'einstein',
          'location': 'princeton',
          },
      'mcurie': {
          'first': 'marie',
          'last': 'curie',
          'location': 'paris',
          },
      }
❶ for username, user_info in users.items():
❷     print(f"\nUsername: {username}")
❸     full_name = f"{user_info['first']} {user_info['last']}"
      location = user_info['location']
❹     print(f"\tFull name: {full_name.title()}")
      print(f"\tLocation: {location.title()}")</code>
</pre>
<p>首先定义一个名为<code>users</code>
 的字典，其中包含两个键：用户名<code>'aeinstein'</code>
 和<code>'mcurie'</code>
 。与每个键盘相关联的值都是一个字典，其中包含用户的名、姓和居住地。在❶处，遍历字典<code>users</code>
 ，让Python依次将每个键赋给变量<code>username</code>
 ，并依次将与当前键相关联的字典赋给变量<code>user_info</code>
 。在循环内部的❷处，将用户名打印出来。</p>
<p>在❸处，开始访问内部的字典。变量<code>user_info</code>
 包含用户信息字典，而该字典包含三个键：<code>'first'</code>
 、<code>'last'</code>
 和<code>'location'</code>
 。对于每位用户，都使用这些键来生成整洁的姓名和居住地，然后打印有关用户的简要信息（见❹）：</p>
<pre class="代码无行号"><code>Username: aeinstein
    Full name: Albert Einstein
    Location: Princeton
Username: mcurie
    Full name: Marie Curie
    Location: Paris</code>
</pre>
<p>请注意，表示每位用户的字典都具有相同的结构。虽然Python并没有这样的要求，但这使得嵌套的字典处理起来更容易。倘若表示每位用户的字典都包含不同的键，<code>for</code>
 循环内部的代码将更复杂。</p>
<blockquote>
<p>
<strong>动手试一试</strong>
</p>
<p>
<strong>练习6-7：人们</strong>
 　在为完成练习6-1而编写的程序中，再创建两个表示人的字典，然后将这三个字典都存储在一个名为<code>people</code>
 的列表中。遍历这个列表，将其中每个人的所有信息都打印出来。</p>
<p>
<strong>练习6-8：宠物</strong>
 　创建多个表示宠物的字典，每个字典都包含宠物的类型及其主人的名字。将这些字典存储在一个名为<code>pets</code>
 的列表中，再遍历该列表，并将有关每个宠物的所有信息都打印出来。</p>
<p>
<strong>练习6-9：喜欢的地方</strong>
 　创建一个名为<code>favorite_places</code>
 的字典。在这个字典中，将三个人的名字用作键，并存储每个人喜欢的1～3个地方。为了让这个练习更有趣些，可以让一些朋友说出他们喜欢的几个地方。遍历这个字典，并将其中每个人的名字及其喜欢的地方打印出来。</p>
<p>
<strong>练习6-10：喜欢的数2</strong>
 　修改为完成练习6-2而编写的程序，让每个人都可以有多个喜欢的数，然后将每个人的名字及其喜欢的数打印出来。</p>
<p>
<strong>练习6-11：城市</strong>
 　创建一个名为<code>cities</code>
 的字典，将三个城市名用作键。对于每座城市，都创建一个字典，并在其中包含该城市所属的国家、人口约数以及一个有关该城市的事实。在表示每座城市的字典中，应包含<code>country</code>
 、<code>population</code>
 和<code>fact</code>
 等键。将每座城市的名字以及有关信息都打印出来。</p>
<p>
<strong>练习6-12：扩展</strong>
 　本章的示例足够复杂，能以很多方式进行扩展。请对本章的一个示例进行扩展：添加键和值、调整程序要解决的问题或改进输出的格式。</p>
</blockquote>
<h2 id="nav_point_143">6.5　小结</h2>
<p>在本章中，你学习了：如何定义字典，以及如何使用存储在字典中的信息；如何访问和修改字典中的元素，以及如何遍历字典中的所有信息；如何遍历字典中所有的键值对、所有的键和所有的值；如何在列表中嵌套字典、在字典中嵌套列表以及在字典中嵌套字典。</p>
<p>在下一章中，你将学习<code>while</code>
 循环以及如何从用户那里获取输入。这是激动人心的一章，让你知道如何将程序变成交互性的：能够对用户输入做出响应。</p>
<br style="page-break-after:always"/>
<div></div>
]]></description>
    <pubDate>Fri, 10 Jul 2026 01:54:53 +0000</pubDate>
    <guid isPermaLink="false">6</guid>
</item>
<item>
    <title>Basic Python - 005 - 第 5 章 if语句.epub</title>
    <description><![CDATA[<p style='color: #888;'>⏱ 预计阅读时间：85 分钟 (16826 字)</p><hr><h1 id="nav_point_100">第 5 章　<code>if</code>
 语句</h1>
<blockquote>
<p><img alt="" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/5_Image00001.gif" style="width: 16%" width="16%"/>
 　编程时经常需要检查一系列条件，并据此决定采取什么措施。在Python中，<code>if</code>
 语句让你能够检查程序的当前状态，并采取相应的措施。</p>
<p>在本章中，你将学习条件测试，以检查所关心的任何条件。你将学习简单的<code>if</code>
 语句，以及创建一系列复杂的<code>if</code>
 语句来确定当前到底处于什么情形。接下来，你将把学到的知识应用于列表，编写一个<code>for</code>
 循环，以一种方式处理列表中的大多数元素，并以另一种方式处理包含特定值的元素。</p>
</blockquote>
<h2 id="nav_point_101">5.1　一个简单示例</h2>
<p>下面是一个简短的示例，演示了如何使用<code>if</code>
 语句来正确地处理特殊情形。假设你有一个汽车列表，并想将其中每辆汽车的名称打印出来。对于大多数汽车，应以首字母大写的方式打印其名称，但对于汽车名<code>'bmw'</code>
 ，应以全大写的方式打印。下面的代码遍历这个列表，并以首字母大写的方式打印其中的汽车名，不过对于<code>'bmw'</code>
 ，则以全大写的方式打印：</p>
<p>
<em>cars.py</em>
</p>
<pre class="代码无行号"><code>  cars = ['audi', 'bmw', 'subaru', 'toyota']
  for car in cars:
❶     if car == 'bmw':
          print(car.upper())
      else:
          print(car.title())</code>
</pre>
<p>这个示例中的循环首先检查当前的汽车名是否是<code>'bmw'</code>
 （见❶）。如果是，就以全大写方式打印，否则以首字母大写的方式打印：</p>
<pre class="代码无行号"><code>Audi
BMW
Subaru
Toyota</code>
</pre>
<p>这个示例涵盖了本章将介绍的很多概念。下面先来介绍可用来在程序中检查条件的测试。</p>
<h2 id="nav_point_102">5.2　条件测试</h2>
<p>每条<code>if</code>
 语句的核心都是一个值为<code>True</code>
 或<code>False</code>
 的表达式，这种表达式称为<strong>条件测试</strong>
 。Python根据条件测试的值为<code>True</code>
 还是<code>False</code>
 来决定是否执行<code>if</code>
 语句中的代码。如果条件测试的值为<code>True</code>
 ，Python就执行紧跟在<code>if</code>
 语句后面的代码；如果为<code>False</code>
 ，Python就忽略这些代码。</p>
<h3 id="nav_point_103">5.2.1　检查是否相等</h3>
<p>大多数条件测试将一个变量的当前值同特定值进行比较。最简单的条件测试检查变量的值是否与特定值相等：</p>
<pre class="代码无行号"><code>❶ &gt;&gt;&gt; car = 'bmw'
❷ &gt;&gt;&gt; car == 'bmw'
  True</code>
</pre>
<p>首先使用一个等号将<code>car</code>
 的值设置为<code>'bmw'</code>
 （见❶），这种做法你已经见过很多次。接下来，使用两个等号（<code>==</code>
 ）检查<code>car</code>
 的值是否为<code>'bmw'</code>
 （见❷）。这个<strong>相等运算符</strong>
 在两边的值相等时返回<code>True</code>
 ，否则返回<code>False</code>
 。在本例中，两边的值相等，因此Python返回<code>True</code>
 。</p>
<p>如果变量<code>car</code>
 的值不是<code>'bmw'</code>
 ，上述测试将返回<code>False</code>
 ：</p>
<pre class="代码无行号"><code>❶ &gt;&gt;&gt; car = 'audi'
❷ &gt;&gt;&gt; car == 'bmw'
  False</code>
</pre>
<p>一个等号是陈述，于是❶处的代码可解读为：将变量<code>car</code>
 的值设置为<code>'audi'</code>
 。两个等号则是发问，于是❷处的代码可解读为：变量<code>car</code>
 的值是<code>'bmw'</code>
 吗？大多数编程语言使用等号的方式与这里演示的相同。</p>
<h3 id="nav_point_104">5.2.2　检查是否相等时忽略大小写</h3>
<p>在Python中检查是否相等时区分大小写。例如，两个大小写不同的值被视为不相等：</p>
<pre class="代码无行号"><code>&gt;&gt;&gt; car = 'Audi'
&gt;&gt;&gt; car == 'audi'
False</code>
</pre>
<p>如果大小写很重要，这种行为有其优点。但如果大小写无关紧要，只想检查变量的值，可将变量的值转换为小写，再进行比较：</p>
<pre class="代码无行号"><code>&gt;&gt;&gt; car = 'Audi'
&gt;&gt;&gt; car.lower() == 'audi'
True</code>
</pre>
<p>无论值<code>'Audi'</code>
 的大小写如何，上述测试都将返回<code>True</code>
 ，因为该测试不区分大小写。函数<code>lower()</code>
 不会修改最初赋给变量<code>car</code>
 的值，因此进行这样的比较时不会影响原来的变量：</p>
<pre class="代码无行号"><code>❶ &gt;&gt;&gt; car = 'Audi'
❷ &gt;&gt;&gt; car.lower() == 'audi'
  True
❸ &gt;&gt;&gt; car
  'Audi'</code>
</pre>
<p>在❶处，将首字母大写的字符串<code>'Audi'</code>
 赋给变量<code>car</code>
 。在❷处，获取变量<code>car</code>
 的值并将其转换为小写，再将结果与字符串<code>'audi'</code>
 进行比较。这两个字符串相同，因此Python返回<code>True</code>
 。从❸处的输出可知，方法<code>lower()</code>
 并没有影响关联到变量<code>car</code>
 的值。</p>
<p>网站采用类似的方式让用户输入的数据符合特定的格式。例如，网站可能使用类似的测试来确保用户名是独一无二的，而并非只是与另一个用户名的大小写不同。用户提交新的用户名时，将把它转换为小写，并与所有既有用户名的小写版本进行比较。执行这种检查时，如果已经有用户名<code>'john'</code>
 （不管大小写如何），则用户提交用户名<code>'John'</code>
 时将遭到拒绝。</p>
<h3 id="nav_point_105">5.2.3　检查是否不相等</h3>
<p>要判断两个值是否不等，可结合使用惊叹号和等号（<code>!=</code>
 ），其中的惊叹号表示<strong>不</strong>
 ，其他很多编程语言中也是如此。</p>
<p>下面再使用一条<code>if</code>
 语句来演示如何使用不等运算符。我们将把要求的比萨配料赋给一个变量，再打印一条消息，指出顾客要求的配料是否是意式小银鱼（anchovies）：</p>
<p>
<em>toppings.py</em>
</p>
<pre class="代码无行号"><code>  requested_topping = 'mushrooms'
❸ if requested_topping != 'anchovies':
      print("Hold the anchovies!")</code>
</pre>
<p>❸处的代码行将<code>requested_topping</code>
 的值与<code>'anchovies'</code>
 进行比较。如果这两个值不相等，Python将返回<code>True</code>
 ，进而执行紧跟在<code>if</code>
 语句后面的代码；如果相等，Python将返回<code>False</code>
 ，因此不执行紧跟在<code>if</code>
 语句后面的代码。</p>
<p>因为<code>requested_topping</code>
 的值不是<code>'anchovies'</code>
 ，所以执行函数调用<code>print()</code>
 ：</p>
<pre class="代码无行号"><code>Hold the anchovies!</code>
</pre>
<p>你编写的大多数条件表达式检查两个值是否相等，但有时候检查两个值是否不等的效率更高。</p>
<h3 id="nav_point_106">5.2.4　数值比较</h3>
<p>检查数值非常简单。例如，下面的代码检查一个人是否是18岁：</p>
<pre class="代码无行号"><code>&gt;&gt;&gt; age = 18
&gt;&gt;&gt; age == 18
True</code>
</pre>
<p>还可检查两个数是否不等。例如，下面的代码在提供的答案不正确时打印一条消息：</p>
<p>
<em>magic_number.py</em>
</p>
<pre class="代码无行号"><code>  answer = 17
❶ if answer != 42:
      print("That is not the correct answer. Please try again!")</code>
</pre>
<p>
<code>answer</code>
 的值（<code>17</code>
 ）不是<code>42</code>
 ，❶处的条件得到满足，因此缩进的代码块得以执行：</p>
<pre class="代码无行号"><code>That is not the correct answer. Please try again!</code>
</pre>
<p>条件语句中可包含各种数学比较，如小于、小于等于、大于、大于等于：</p>
<pre class="代码无行号"><code>&gt;&gt;&gt; age = 19
&gt;&gt;&gt; age &lt; 21
True
&gt;&gt;&gt; age &lt;= 21
True
&gt;&gt;&gt; age &gt; 21
False
&gt;&gt;&gt; age &gt;= 21
False</code>
</pre>
<p>在<code>if</code>
 语句中可使用各种数学比较，这让你能够直接检查关心的条件。</p>
<h3 id="nav_point_107">5.2.5　检查多个条件</h3>
<p>你可能想同时检查多个条件。例如，有时候需要在两个条件都为<code>True</code>
 时才执行相应的操作，而有时候只要求一个条件为<code>True</code>
 。在这些情况下，关键字<code>and</code>
 和<code>or</code>
 可助你一臂之力。</p>
<ol>
<li value="1">
<p>
<strong>使用<code>and</code>
 检查多个条件</strong>
</p>
<p>要检查是否两个条件都为<code>True</code>
 ，可使用关键字<code>and</code>
 将两个条件测试合而为一。如果每个测试都通过了，整个表达式就为<code>True</code>
 ；如果至少一个测试没有通过，整个表达式就为<code>False</code>
 。</p>
<p>例如，要检查是否两个人都不小于21岁，可使用下面的测试：</p>
<pre class="代码无行号"><code>❸ &gt;&gt;&gt; age_0 = 22
  &gt;&gt;&gt; age_1 = 18
❷ &gt;&gt;&gt; age_0 &gt;= 21 and age_1 &gt;= 21
  False
❸ &gt;&gt;&gt; age_1 = 22
  &gt;&gt;&gt; age_0 &gt;= 21 and age_1 &gt;= 21
  True</code>
</pre>
<p>在❶处，定义两个用于存储年龄的变量：<code>age_0</code>
 和<code>age_1</code>
 。在❷处，检查这两个变量是否都大于或等于21。左边的测试通过了，但右边的测试没有通过，因此整个条件表达式的结果为<code>False</code>
 。在❸处，将<code>age_1</code>
 改为22，这样<code>age_1</code>
 的值大于21，因此两个测试都通过了，导致整个条件表达式的结果为<code>True</code>
 。</p>
<p>为改善可读性，可将每个测试分别放在一对圆括号内，但并非必须这样做。如果你使用圆括号，测试将类似于下面这样：</p>
<pre class="代码无行号"><code>(age_0 &gt;= 21) and (age_1 &gt;= 21)</code>
</pre>
<p> </p>
</li>
<li value="2">
<p>
<strong>使用or检查多个条件</strong>
</p>
<p>关键字<code>or</code>
 也能够让你检查多个条件，但只要至少一个条件满足，就能通过整个测试。仅当两个测试都没有通过时，使用<code>or</code>
 的表达式才为<code>False</code>
 。</p>
<p>下面再次检查两个人的年龄，但检查的条件是至少一个人的年龄不小于21岁：</p>
<pre class="代码无行号"><code>❶ &gt;&gt;&gt; age_0 = 22
  &gt;&gt;&gt; age_1 = 18
❷ &gt;&gt;&gt; age_0 &gt;= 21 or age_1 &gt;= 21
  True
❸ &gt;&gt;&gt; age_0 = 18
  &gt;&gt;&gt; age_0 &gt;= 21 or age_1 &gt;= 21
  False</code>
</pre>
<p>同样，首先定义两个用于存储年龄的变量（见❶）。❷处对<code>age_0</code>
 的测试通过了，因此整个表达式的结果为<code>True</code>
 。接下来，将<code>age_0</code>
 减小为18。在❸处的测试中，两个测试都没有通过，因此整个表达式的结果为<code>False</code>
 。</p>
</li>
</ol>
<h3 id="nav_point_108">5.2.6　检查特定值是否包含在列表中</h3>
<p>有时候，执行操作前必须检查列表是否包含特定的值。例如，结束用户的注册过程前，可能需要检查他提供的用户名是否已包含在用户名列表中。在地图程序中，可能需要检查用户提交的位置是否包含在已知位置列表中。</p>
<p>要判断特定的值是否已包含在列表中，可使用关键字<code>in</code>
 。下面来看看你可能为比萨店编写的一些代码。这些代码首先创建一个列表，其中包含用户点的比萨配料，然后检查特定的配料是否包含在该列表中。</p>
<pre class="代码无行号"><code>  &gt;&gt;&gt; requested_toppings = ['mushrooms', 'onions', 'pineapple']
❶ &gt;&gt;&gt; 'mushrooms' in requested_toppings
  True
❷ &gt;&gt;&gt; 'pepperoni' in requested_toppings
  False</code>
</pre>
<p>在❶处和❷处，关键字<code>in</code>
 让<code>Python</code>
 检查列表<code>requested_toppings</code>
 是否包含<code>'mushrooms'</code>
 和<code>'pepperoni'</code>
 。这种技术很有用，让你能够在创建一个列表后，轻松地检查其中是否包含特定的值。</p>
<h3 id="nav_point_109">5.2.7　检查特定值是否不包含在列表中</h3>
<p>还有些时候，确定特定的值未包含在列表中很重要。在这种情况下，可使用关键字<code>not in</code>
 。例如，有一个列表，其中包含被禁止在论坛上发表评论的用户，可以在允许用户提交评论前检查他是否被禁言：</p>
<p>
<em>banned_users.py</em>
</p>
<pre class="代码无行号"><code>  banned_users = ['andrew', 'carolina', 'david']
  user = 'marie'
❶ if user not in banned_users:
      print(f"{user.title()}, you can post a response if you wish.")</code>
</pre>
<p>❶处的代码行明白易懂：如果<code>user</code>
 的值未包含在列表<code>banned_users</code>
 中，Python将返回<code>True</code>
 ，进而执行缩进的代码行。</p>
<p>用户<code>'marie'</code>
 未包含在列表<code>banned_users</code>
 中，因此她将看到一条邀请她发表评论的消息：</p>
<pre class="代码无行号"><code>Marie, you can post a response if you wish.</code>
</pre>
<h3 id="nav_point_110">5.2.8　布尔表达式</h3>
<p>随着你对编程的了解越来越深入，将遇到术语<strong>布尔表达式</strong>
 ，它不过是条件测试的别名。与条件表达式一样，布尔表达式的结果要么为<code>True</code>
 ，要么为<code>False</code>
 。</p>
<p>布尔值通常用于记录条件，如游戏是否正在运行，或者用户是否可以编辑网站的特定内容：</p>
<pre class="代码无行号"><code>game_active = True
can_edit = False</code>
</pre>
<p>在跟踪程序状态或程序中重要的条件方面，布尔值提供了一种高效的方式。</p>
<blockquote>
<p>
<strong>动手试一试</strong>
</p>
<p>
<strong>练习5-1：条件测试</strong>
 　编写一系列条件测试，将每个测试以及对其结果的预测和实际结果打印出来。你编写的代码应类似于下面这样：</p>
<pre class="代码无行号"><code>car = 'subaru'
print("Is car == 'subaru'? I predict True.")
print(car == 'subaru')
print("\nIs car == 'audi'? I predict False.")
print(car == 'audi')</code>
</pre>
<ul>
<li class="第2级无序列表">详细研究实际结果，直到你明白它为何为<code>True</code>
 或<code>False</code>
 。</li>
<li class="第2级无序列表">创建至少10个测试，且其中结果分别为<code>True</code>
 和<code>False</code>
 的测试都至少有5个。</li>
</ul>
<p>
<strong>练习5-2：更多条件测试</strong>
 　你并非只能创建10个测试。如果想尝试做更多比较，可再编写一些测试，并将它们加入conditional_tests.py中。对于下面列出的各种情况，至少编写两个结果分别为<code>True</code>
 和<code>False</code>
 的测试。</p>
<ul>
<li class="第2级无序列表">检查两个字符串相等和不等。</li>
<li class="第2级无序列表">使用方法<code>lower()</code>
 的测试。</li>
<li class="第2级无序列表">涉及相等、不等、大于、小于、大于等于和小于等于的数值测试。</li>
<li class="第2级无序列表">使用关键字<code>and</code>
 和<code>or</code>
 的测试。</li>
<li class="第2级无序列表">测试特定的值是否包含在列表中。</li>
<li class="第2级无序列表">测试特定的值是否未包含在列表中。</li>
</ul>
</blockquote>
<h2 id="nav_point_111">5.3　<code>if</code>
 语句</h2>
<p>理解条件测试后，就可以开始编写<code>if</code>
 语句了。<code>if</code>
 语句有很多种，选择使用哪种取决于要测试的条件数。前面讨论条件测试时，列举了多个<code>if</code>
 语句示例，下面更深入地讨论这个主题。</p>
<h3 id="nav_point_112">5.3.1　简单的<code>if</code>
 语句</h3>
<p>最简单的<code>if</code>
 语句只有一个测试和一个操作：</p>
<pre class="代码无行号"><code>if conditional_test:
    do something</code>
</pre>
<p>第一行可包含任何条件测试，而在紧跟在测试后面的缩进代码块中，可执行任何操作。如果条件测试的结果为<code>True</code>
 ，Python就会执行紧跟在<code>if</code>
 语句后面的代码，否则Python将忽略这些代码。</p>
<p>假设有一个表示某人年龄的变量，而你想知道这个人是否符合投票的年龄，可使用如下代码：</p>
<p>
<em>voting.py</em>
</p>
<pre class="代码无行号"><code>  age = 19
❶ if age &gt;= 18:
❷     print("You are old enough to vote!")</code>
</pre>
<p>在❶处，Python检查变量<code>age</code>
 的值是否大于或等于18。答案是肯定的，因此Python执行❷处缩进的函数调用<code>print()</code>
 ：</p>
<pre class="代码无行号"><code>You are old enough to vote!</code>
</pre>
<p>在<code>if</code>
 语句中，缩进的作用与在<code>for</code>
 循环中相同。如果测试通过了，将执行<code>if</code>
 语句后面所有缩进的代码行，否则将忽略它们。</p>
<p>在紧跟<code>if</code>
 语句后面的代码块中，可根据需要包含任意数量的代码行。下面在一个人符合投票年龄时再打印一行输出，问他是否登记了：</p>
<pre class="代码无行号"><code>age = 19
if age &gt;= 18:
    print("You are old enough to vote!")
    print("Have you registered to vote yet?")</code>
</pre>
<p>条件测试通过了，而且两个函数调用<code>print()</code>
 都缩进了，因此它们都将执行：</p>
<pre class="代码无行号"><code>You are old enough to vote!
Have you registered to vote yet?</code>
</pre>
<p>如果<code>age</code>
 的值小于18，这个程序将不会有任何输出。</p>
<h3 id="nav_point_113">5.3.2　<code>if-else</code>
 语句</h3>
<p>我们经常需要在条件测试通过时执行一个操作，在没有通过时执行另一个操作。在这种情况下，可使用Python提供的<code>if-else</code>
 语句。<code>if-else</code>
 语句块类似于简单的<code>if</code>
 语句，但其中的<code>else</code>
 语句让你能够指定条件测试未通过时要执行的操作。</p>
<p>下面的代码在一个人符合投票年龄时显示与前面相同的消息，在不符合时显示一条新消息：</p>
<pre class="代码无行号"><code>  age = 17
❶ if age &gt;= 18:
      print("You are old enough to vote!")
      print("Have you registered to vote yet?")
❷ else:
      print("Sorry, you are too young to vote.")
      print("Please register to vote as soon as you turn 18!")</code>
</pre>
<p>如果❶处的条件测试通过了，就执行第一组缩进的函数调用<code>print()</code>
 。如果测试结果为<code>False</code>
 ，就执行❷处的<code>else</code>
 代码块。这次<code>age</code>
 小于18，条件测试未通过，因此执行<code>else</code>
 代码块中的代码：</p>
<pre class="代码无行号"><code>Sorry, you are too young to vote.
Please register to vote as soon as you turn 18!</code>
</pre>
<p>上述代码之所以可行，是因为只存在两种情形：要么符合投票年龄，要么不符合。<code>if-else</code>
 结构非常适合用于让Python执行两种操作之一的情形。在这样简单的<code>if-else</code>
 结构中，总是会执行两个操作中的一个。</p>
<h3 id="nav_point_114">5.3.3　<code>if-elif-else</code>
 结构</h3>
<p>我们经常需要检查超过两个的情形，为此可使用Python提供的<code>if-elif-else</code>
 结构。Python只执行<code>if-elif-else</code>
 结构中的一个代码块。它依次检查每个条件测试，直到遇到通过了的条件测试。测试通过后，Python将执行紧跟在它后面的代码，并跳过余下的测试。</p>
<p>在现实世界中，很多情况下需要考虑的情形超过两个。例如，来看一个根据年龄段收费的游乐场：</p>
<ul>
<li class="第1级无序列表">4岁以下免费；</li>
<li class="第1级无序列表">4～18岁收费25美元；</li>
<li class="第1级无序列表">18岁（含）以上收费40美元。</li>
</ul>
<p>如果只使用一条<code>if</code>
 语句，该如何确定门票价格呢？下面的代码确定一个人所属的年龄段，并打印一条包含门票价格的消息：</p>
<p>
<em>amusement_park.py</em>
</p>
<pre class="代码无行号"><code>  age = 12
❶ if age &lt; 4:
      print("Your admission cost is $0.")
❷ elif age &lt; 18:
      print("Your admission cost is $25.")
❸ else:
      print("Your admission cost is $40.")</code>
</pre>
<p>❶处的<code>if</code>
 测试检查一个人是否不满4岁。如果是，Python就打印一条合适的消息，并跳过余下测试。❷处的<code>elif</code>
 代码行其实是另一个<code>if</code>
 测试，仅在前面的测试未通过时才会运行。在这里，我们知道这个人不小于4岁，因为第一个测试未通过。如果这个人未满18岁，Python将打印相应的消息，并跳过<code>else</code>
 代码块。如果<code>if</code>
 测试和<code>elif</code>
 测试都未通过，Python将运行❸处<code>else</code>
 代码块中的代码。</p>
<p>在本例中，❶处测试的结果为<code>False</code>
 ，因此不执行其代码块。然而，第二个测试的结果为<code>True</code>
 （12小于18），因此执行其代码块。输出为一个句子，向用户指出门票价格：</p>
<pre class="代码无行号"><code>Your admission cost is $25.</code>
</pre>
<p>只要年龄超过17岁，前两个测试就都不能通过。在这种情况下，将执行<code>else</code>
 代码块，指出门票价格为40美元。</p>
<p>为了让代码更简洁，可不在<code>if-elif-else</code>
 代码块中打印门票价格，而只在其中设置门票价格，并在它后面添加一个简单的函数调用<code>print()</code>
 ：</p>
<pre class="代码无行号"><code>  age = 12
  if age &lt; 4:
❶     price = 0
  elif age &lt; 18:
❷     price = 25
  else:
❸     price = 40
❹ print(f"Your admission cost is ${price}.")</code>
</pre>
<p>❶处、❷处和❸处的代码行像前一个示例那样，根据人的年龄设置变量<code>price</code>
 的值。在<code>if-elif-else</code>
 结构中设置<code>price</code>
 的值后，一条未缩进的函数调用<code>print()</code>
 ❹会根据这个变量的值打印一条消息，指出门票的价格。</p>
<p>这些代码的输出与前一个示例相同，但<code>if-elif-else</code>
 结构的作用更小：它只确定门票价格，而不是在确定门票价格的同时打印一条消息。除效率更高外，这些修订后的代码还更容易修改：要调整输出消息的内容，只需修改一个而不是三个函数调用<code>print()</code>
 。</p>
<h3 id="nav_point_115">5.3.4　使用多个<code>elif</code>
 代码块</h3>
<p>可根据需要使用任意数量的<code>elif</code>
 代码块。例如，假设前述游乐场要给老年人打折，可再添加一个条件测试，判断顾客是否符合打折条件。下面假设对于65岁（含）以上的老人，可半价（即20美元）购买门票：</p>
<pre class="代码无行号"><code>  age = 12
  if age &lt; 4:
      price = 0
  elif age &lt; 18:
      price = 25
❶ elif age &lt; 65:
      price = 40
❷ else:
      price = 20
  print(f"Your admission cost is ${price}.")</code>
</pre>
<p>这些代码大多未变。第二个<code>elif</code>
 代码块（见❶）通过检查确定年龄不到65岁后，才将门票价格设置为全票价格——40美元。请注意，在<code>else</code>
 代码块（见❷）中，必须将所赋的值改为20，因为仅当年龄超过65岁（含）时，才会执行这个代码块。</p>
<h3 id="nav_point_116">5.3.5　省略<code>else</code>
 代码块</h3>
<p>Python并不要求<code>if-elif</code>
 结构后面必须有<code>else</code>
 代码块。在有些情况下，<code>else</code>
 代码块很有用；而在其他一些情况下，使用一条<code>elif</code>
 语句来处理特定的情形更清晰：</p>
<pre class="代码无行号"><code>  age = 12
  if age &lt; 4:
      price = 0
  elif age &lt; 18:
      price = 25
  elif age &lt; 65:
      price = 40
❶ elif age &gt;= 65:
      price = 20
  print(f"Your admission cost is ${price}.")</code>
</pre>
<p>❶处的<code>elif</code>
 代码块在顾客的年龄超过65岁（含）时，将价格设置为20美元。这比使用<code>else</code>
 代码块更清晰些。经过这样的修改后，每个代码块都仅在通过了相应的测试时才会执行。</p>
<p>
<code>else</code>
 是一条包罗万象的语句，只要不满足任何<code>if</code>
 或<code>elif</code>
 中的条件测试，其中的代码就会执行。这可能引入无效甚至恶意的数据。如果知道最终要测试的条件，应考虑使用一个<code>elif</code>
 代码块来代替<code>else</code>
 代码块。这样就可以肯定，仅当满足相应的条件时，代码才会执行。</p>
<h3 id="nav_point_117">5.3.6　测试多个条件</h3>
<p>
<code>if-elif-else</code>
 结构功能强大，但仅适合用于只有一个条件满足的情况：遇到通过了的测试后，Python就跳过余下的测试。这种行为很好，效率很高，让你能够测试一个特定的条件。</p>
<p>然而，有时候必须检查你关心的所有条件。在这种情况下，应使用一系列不包含<code>elif</code>
 和<code>else</code>
 代码块的简单<code>if</code>
 语句。在可能有多个条件为<code>True</code>
 且需要在每个条件为<code>True</code>
 时都采取相应措施时，适合使用这种方法。</p>
<p>下面再来看看前面的比萨店示例。如果顾客点了两种配料，就需要确保在其比萨中包含这些配料：</p>
<p>
<em>toppings.py</em>
</p>
<pre class="代码无行号"><code>❶ requested_toppings = ['mushrooms', 'extra cheese']
❷ if 'mushrooms' in requested_toppings:
      print("Adding mushrooms.")
❸ if 'pepperoni' in requested_toppings:
      print("Adding pepperoni.")
❹ if 'extra cheese' in requested_toppings:
      print("Adding extra cheese.")
  print("\nFinished making your pizza!")</code>
</pre>
<p>首先创建一个列表，其中包含顾客点的配料（见❶）。❷处的<code>if</code>
 语句检查顾客是否点了配料蘑菇（mushrooms）。如果点了，就打印一条确认消息。❸处检查配料辣香肠（pepperoni）的代码也是一个简单的<code>if</code>
 语句，而不是<code>elif</code>
 或<code>else</code>
 语句。因此不管前一个测试是否通过，都将进行这个测试。❹处的代码检查顾客是否要求多加芝士（extra cheese）。不管前两个测试的结果如何，都会执行这些代码。每当这个程序运行时，都会执行这三个独立的测试。</p>
<p>因为本例检查了每个条件，所以将在比萨中添加蘑菇并多加芝士：</p>
<pre class="代码无行号"><code>Adding mushrooms.
Adding extra cheese.
Finished making your pizza!</code>
</pre>
<p>如果像下面这样转而使用<code>if-elif-else</code>
 结构，代码将不能正确运行，因为有一个测试通过后，就会跳过余下的测试：</p>
<pre class="代码无行号"><code>requested_toppings = ['mushrooms', 'extra cheese']
if 'mushrooms' in requested_toppings:
    print("Adding mushrooms.")
elif 'pepperoni' in requested_toppings:
    print("Adding pepperoni.")
elif 'extra cheese' in requested_toppings:
    print("Adding extra cheese.")
print("\nFinished making your pizza!")</code>
</pre>
<p>第一个测试检查列表中是否包含<code>'mushrooms'</code>
 。它通过了，因此将在比萨中添加蘑菇。然而，Python将跳过<code>if-elif-else</code>
 结构中余下的测试，不再检查列表中是否包含<code>'pepperoni'</code>
 和<code>'extra cheese'</code>
 。结果是，将添加顾客点的第一种配料，但不会添加其他配料：</p>
<pre class="代码无行号"><code>Adding mushrooms.
Finished making your pizza!</code>
</pre>
<p>总之，如果只想执行一个代码块，就使用<code>if-elif-else</code>
 结构；如果要执行多个代码块，就使用一系列独立的<code>if</code>
 语句。</p>
<blockquote>
<p>
<strong>动手试一试</strong>
</p>
<p>
<strong>练习5-3：外星人颜色</strong>
 　假设在游戏中刚射杀了一个外星人，请创建一个名为<code>alien_color</code>
 的变量，并将其赋值为<code>'green'</code>
 、<code>'yellow'</code>
 或<code>'red'</code>
 。</p>
<ul>
<li class="第2级无序列表">编写一条<code>if</code>
 语句，检查外星人是否是绿色的。如果是，就打印一条消息，指出玩家获得了5分。</li>
<li class="第2级无序列表">编写这个程序的两个版本，在一个版本中上述测试通过了，而在另一个版本中未通过（未通过测试时没有输出）。</li>
</ul>
<p>
<strong>练习5-4：外星人颜色2</strong>
 　像练习5-3那样设置外星人的颜色，并编写一个<code>if-else</code>
 结构。</p>
<ul>
<li class="第2级无序列表">如果外星人是绿色的，就打印一条消息，指出玩家因射杀该外星人获得了5分。</li>
<li class="第2级无序列表">如果外星人不是绿色的，就打印一条消息，指出玩家获得了10分。</li>
<li class="第2级无序列表">编写这个程序的两个版本，在一个版本中执行<code>if</code>
 代码块，在另一个版本中执行<code>else</code>
 代码块。</li>
</ul>
<p>
<strong>练习5-5：外星人颜色3</strong>
 　将练习5-4中的<code>if-else</code>
 结构改为<code>if-elif-else</code>
 结构。</p>
<ul>
<li class="第2级无序列表">如果外星人是绿色的，就打印一条消息，指出玩家获得了5分。</li>
<li class="第2级无序列表">如果外星人是黄色的，就打印一条消息，指出玩家获得了10分。</li>
<li class="第2级无序列表">如果外星人是红色的，就打印一条消息，指出玩家获得了15分。</li>
<li class="第2级无序列表">编写这个程序的三个版本，分别在外星人为绿色、黄色和红色时打印一条消息。</li>
</ul>
<p>
<strong>练习5-6：人生的不同阶段</strong>
 　设置变量<code>age</code>
 的值，再编写一个<code>if-elif-else</code>
 结构，根据<code>age</code>
 的值判断一个人处于人生的哪个阶段。</p>
<ul>
<li class="第2级无序列表">如果年龄小于2岁，就打印一条消息，指出这个人是婴儿。</li>
<li class="第2级无序列表">如果年龄为2（含）～4岁，就打印一条消息，指出这个人是幼儿。</li>
<li class="第2级无序列表">如果年龄为4（含）～13岁，就打印一条消息，指出这个人是儿童。</li>
<li class="第2级无序列表">如果年龄为13（含）～20岁，就打印一条消息，指出这个人是青少年。</li>
<li class="第2级无序列表">如果年龄为20（含）～65岁，就打印一条消息，指出这个人是成年人。</li>
<li class="第2级无序列表">如果年龄超过65岁（含），就打印一条消息，指出这个人是老年人。</li>
</ul>
<p>
<strong>练习5-7：喜欢的水果</strong>
 　创建一个列表，其中包含你喜欢的水果，再编写一系列独立的<code>if</code>
 语句，检查列表中是否包含特定的水果。</p>
<ul>
<li class="第2级无序列表">将该列表命名为<code>favorite_fruits</code>
 ，并在其中包含三种水果。</li>
<li>
<p>编写5条<code>if</code>
 语句，每条都检查某种水果是否包含在列表中。如果是，就打印一条消息，下面是一个例子。</p>
<p>You really like bananas!</p>
</li>
</ul>
</blockquote>
<h2 id="nav_point_118">5.4　使用<code>if</code>
 语句处理列表</h2>
<p>通过结合使用<code>if</code>
 语句和列表，可完成一些有趣的任务：对列表中特定的值做特殊处理；高效地管理不断变化的情形，如餐馆是否还有特定的食材；证明代码在各种情形下都将按预期那样运行。</p>
<h3 id="nav_point_119">5.4.1　检查特殊元素</h3>
<p>本章开头通过一个简单示例演示了如何处理特殊值<code>'bmw'</code>
 ——它需要采用不同的格式进行打印。现在你对条件测试和<code>if</code>
 语句有了大致的认识，下面就来进一步研究如何检查列表中的特殊值，并对其做合适的处理。</p>
<p>继续使用前面的比萨店示例。这家比萨店在制作比萨时，每添加一种配料都打印一条消息。通过创建一个列表，在其中包含顾客点的配料，并使用一个循环来指出添加到比萨中的配料，能以极高的效率编写这样的代码：</p>
<p>
<em>toppings.py</em>
</p>
<pre class="代码无行号"><code>requested_toppings = ['mushrooms', 'green peppers', 'extra cheese']
for requested_topping in requested_toppings:
    print(f"Adding {requested_topping}.")
print("\nFinished making your pizza!")</code>
</pre>
<p>输出很简单，因为上述代码不过是一个简单的<code>for</code>
 循环：</p>
<pre class="代码无行号"><code>Adding mushrooms.
Adding green peppers.
Adding extra cheese.
Finished making your pizza!</code>
</pre>
<p>然而，如果比萨店的青椒用完了，该如何处理呢？为妥善地处理这种情况，可在<code>for</code>
 循环中包含一条<code>if</code>
 语句：</p>
<pre class="代码无行号"><code>  requested_toppings = ['mushrooms', 'green peppers', 'extra cheese']
  for requested_topping in requested_toppings:
❶     if requested_topping == 'green peppers':
          print("Sorry, we are out of green peppers right now.")
❷     else:
          print(f"Adding {requested_topping}.")
  print("\nFinished making your pizza!")</code>
</pre>
<p>这里在比萨中添加每种配料前都进行检查。❶处的代码检查顾客是否点了青椒。如果是，就显示一条消息，指出不能点青椒的原因。❷处的<code>else</code>
 代码块确保其他配料都将添加到比萨中。</p>
<p>输出表明，已经妥善地处理了顾客点的每种配料：</p>
<pre class="代码无行号"><code>Adding mushrooms.
Sorry, we are out of green peppers right now.
Adding extra cheese.
Finished making your pizza!</code>
</pre>
<h3 id="nav_point_120">5.4.2　确定列表不是空的</h3>
<p>到目前为止，我们对于处理的每个列表都做了一个简单的假设——假设它们都至少包含一个元素。因为马上就要让用户来提供存储在列表中的信息，所以不能再假设循环运行时列表不是空的。有鉴于此，在运行<code>for</code>
 循环前确定列表是否为空很重要。</p>
<p>下面在制作比萨前检查顾客点的配料列表是否为空。如果列表为空，就向顾客确认是否要点原味比萨；如果列表不为空，就像前面的示例那样制作比萨：</p>
<pre class="代码无行号"><code>❶ requested_toppings = []
❷ if requested_toppings:
      for requested_topping in requested_toppings:
          print(f"Adding {requested_topping}.")
      print("\nFinished making your pizza!")
❸ else:
      print("Are you sure you want a plain pizza?")</code>
</pre>
<p>首先创建一个空列表，其中不包含任何配料（见❶）。❷处进行简单的检查，而不是直接执行<code>for</code>
 循环。在<code>if</code>
 语句中将列表名用作条件表达式时，Python将在列表至少包含一个元素时返回<code>True</code>
 ，并在列表为空时返回<code>False</code>
 。如果<code>requested_toppings</code>
 不为空，就运行与前一个示例相同的<code>for</code>
 循环；否则，就打印一条消息，询问顾客是否确实要点不加任何配料的原味比萨（见❸）。</p>
<p>在这里，这个列表为空，因此输出如下——询问顾客是否确实要点原味比萨：</p>
<pre class="代码无行号"><code>Are you sure you want a plain pizza?</code>
</pre>
<p>如果这个列表不为空，输出将显示在比萨中添加的各种配料。</p>
<h3 id="nav_point_121">5.4.3　使用多个列表</h3>
<p>顾客的要求往往五花八门，在比萨配料方面尤其如此。如果顾客要在比萨中添加炸薯条，该怎么办呢？可使用列表和<code>if</code>
 语句来确定能否满足顾客的要求。</p>
<p>来看看在制作比萨前如何拒绝怪异的配料要求。下面的示例定义了两个列表，其中第一个列表包含比萨店供应的配料，而第二个列表包含顾客点的配料。这次对于<code>requested_toppings</code>
 中的每个元素，都检查它是否是比萨店供应的配料，再决定是否在比萨中添加它：</p>
<pre class="代码无行号"><code>❶ available_toppings = ['mushrooms', 'olives', 'green peppers',
                        'pepperoni', 'pineapple', 'extra cheese']
❷ requested_toppings = ['mushrooms', 'french fries', 'extra cheese']
❸ for requested_topping in requested_toppings:
❹     if requested_topping in available_toppings:
          print(f"Adding {requested_topping}.")
❺     else:
          print(f"Sorry, we don't have {requested_topping}.")
  print("\nFinished making your pizza!")</code>
</pre>
<p>❶处定义了一个列表，其中包含比萨店供应的配料。请注意，如果比萨店供应的配料是固定的，也可使用一个元组来存储它们。❷处又创建了一个列表，其中包含顾客点的配料。请注意那个不同寻常的配料——<code>'french fries'</code>
 。在❸处遍历顾客点的配料列表。在这个循环中，对于顾客点的每种配料，都检查它是否包含在供应的配料列表中（见❹）。如果答案是肯定的，就将其加入比萨中，否则将运行<code>else</code>
 代码块（见❺）：打印一条消息，告诉顾客不供应这种配料。</p>
<p>这些代码的输出整洁而翔实：</p>
<pre class="代码无行号"><code>Adding mushrooms.
Sorry, we don't have french fries.
Adding extra cheese.
Finished making your pizza!</code>
</pre>
<p>通过为数不多的几行代码，我们高效地处理了一种真实的情形！</p>
<blockquote>
<p>
<strong>动手试一试</strong>
</p>
<p>
<strong>练习5-8：以特殊方式跟管理员打招呼</strong>
 　创建一个至少包含5个用户名的列表，且其中一个用户名为<code>'admin'</code>
 。想象你要编写代码，在每位用户登录网站后都打印一条问候消息。遍历用户名列表，并向每位用户打印一条问候消息。</p>
<ul>
<li>
<p>如果用户名为<code>'admin'</code>
 ，就打印一条特殊的问候消息，如下所示。</p>
<p>Hello admin, would you like to see a status report?<br/>
 </p>
</li>
<li>
<p>否则，打印一条普通的问候消息，如下所示。</p>
<p>Hello Jaden, thank you for logging in again.<br/>
 </p>
</li>
</ul>
<p>
<strong>练习5-9：处理没有用户的情形</strong>
 　在为完成练习5-8编写的程序中，添加一条<code>if</code>
 语句，检查用户名列表是否为空。</p>
<ul>
<li>
<p>如果为空，就打印如下消息。</p>
<p>We need to find some users!<br/>
 </p>
</li>
<li>
<p>删除列表中的所有用户名，确定将打印正确的消息。</p>
</li>
</ul>
<p>
<strong>练习5-10：检查用户名</strong>
 　按下面的说明编写一个程序，模拟网站如何确保每位用户的用户名都独一无二。</p>
<ul>
<li class="第2级无序列表">创建一个至少包含5个用户名的列表，并将其命名为<code>current_users</code>
 。</li>
<li class="第2级无序列表">再创建一个包含5个用户名的列表，将其命名为<code>new_users</code>
 ，并确保其中有一两个用户名也包含在列表<code>current_users</code>
 中。</li>
<li class="第2级无序列表">遍历列表<code>new_users</code>
 ，对于其中的每个用户名，都检查它是否已被使用。如果是，就打印一条消息，指出需要输入别的用户名；否则，打印一条消息，指出这个用户名未被使用。</li>
<li class="第2级无序列表">确保比较时不区分大小写。换句话说，如果用户名<code>'John'</code>
 已被使用，应拒绝用户名<code>'JOHN'</code>
 。（为此，需要创建列表<code>current_users</code>
 的副本，其中包含当前所有用户名的小写版本。）</li>
</ul>
<p>
<strong>练习5-11：序数</strong>
 　序数表示位置，如1st和2nd。序数大多以th结尾，只有1、2和3例外。</p>
<ul>
<li class="第2级无序列表">在一个列表中存储数字1～9。</li>
<li class="第2级无序列表">遍历这个列表。</li>
<li class="第2级无序列表">在循环中使用一个<code>if-elif-else</code>
 结构，以打印每个数字对应的序数。输出内容应为<code>"1st 2nd 3rd 4th 5th 6th 7th 8th 9th"</code>
 ，但每个序数都独占一行。</li>
</ul>
</blockquote>
<h2 id="nav_point_122">5.5　设置<code>if</code>
 语句的格式</h2>
<p>本章的每个示例都展示了良好的格式设置习惯。在条件测试的格式设置方面，PEP 8提供的唯一建议是，在诸如<code>==</code>
 、<code>&gt;=</code>
 和<code>&lt;=</code>
 等比较运算符两边各添加一个空格。例如：</p>
<pre class="代码无行号"><code>if age &lt; 4:</code>
</pre>
<p>要比</p>
<pre class="代码无行号"><code>if age&lt;4:</code>
</pre>
<p>更好。</p>
<p>这样的空格不会影响Python对代码的解读，而只是让代码阅读起来更容易。</p>
<blockquote>
<p>
<strong>动手试一试</strong>
</p>
<p>
<strong>练习5-12：设置<code>if</code>
 语句的格式　</strong>
 审核你在本章编写的程序，确保正确地设置了条件测试的格式。</p>
<p>
<strong>练习5-13：自己的想法</strong>
 　与刚拿起本书时相比，现在你是一名能力更强的程序员了。鉴于你对如何在程序中模拟现实情形有了更深入的认识，可以考虑使用程序来解决一些问题了。随着编程技能不断提高，你可能想解决一些问题，请将这方面的想法记录下来。想想你可能想编写的游戏、想研究的数据集以及想创建的Web应用程序。</p>
</blockquote>
<h2 id="nav_point_123">5.6　小结</h2>
<p>在本章中，你学习了：如何编写结果要么为<code>True</code>
 要么为<code>False</code>
 的条件测试；如何编写简单的<code>if</code>
 语句、<code>if-else</code>
 语句和<code>if-elif-else</code>
 结构，并且在程序中使用这些结构来测试特定的条件，以确定这些条件是否满足；如何在利用高效的<code>for</code>
 循环的同时，以不同于其他元素的方式对特定的列表元素进行处理。你还再次学习了Python就代码格式提出的建议，从而确保即便编写的程序越来越复杂，其代码依然易于阅读和理解。</p>
<p>在第6章，你将学习Python字典。字典类似于列表，但让你能够将不同的信息关联起来。你将学习如何创建和遍历字典，以及如何将字典同列表和<code>if</code>
 语句结合起来使用。学习字典让你能够模拟更多现实世界的情形。</p>
<br style="page-break-after:always"/>
<div></div>
]]></description>
    <pubDate>Wed, 08 Jul 2026 01:44:51 +0000</pubDate>
    <guid isPermaLink="false">5</guid>
</item>
<item>
    <title>Basic Python - 004 - 第 4 章 操作列表.epub</title>
    <description><![CDATA[<p style='color: #888;'>⏱ 预计阅读时间：97 分钟 (19338 字)</p><hr><h1 id="nav_point_69">第 4 章　操作列表</h1>
<blockquote>
<p><img alt="" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/4_Image00001.gif" style="width: 16%" width="16%"/>
 　在第3章，你学习了如何创建简单的列表，还学习了如何操作列表元素。在本章中，你将学习如何<strong>遍历</strong>
 整个列表，这只需要几行代码，无论列表有多长。循环让你能够对列表的每个元素都采取一个或一系列相同的措施，从而高效地处理任何长度的列表，包括包含数千乃至数百万个元素的列表。</p>
</blockquote>
<h2 id="nav_point_70">4.1　遍历整个列表</h2>
<p>你经常需要遍历列表的所有元素，对每个元素执行相同的操作。例如，在游戏中，可能需要将每个界面元素平移相同的距离；对于包含数字的列表，可能需要对每个元素执行相同的统计运算；在网站中，可能需要显示文章列表中的每个标题。需要对列表中的每个元素都执行相同的操作时，可使用Python中的<code>for</code>
 循环。</p>
<p>假设我们有一个魔术师名单，需要将其中每个魔术师的名字都打印出来。为此，可以分别获取名单中的每个名字，但这种做法会导致多个问题。例如，如果名单很长，将包含大量重复的代码。另外，每当名单的长度发生变化时，都必须修改代码。通过使用<code>for</code>
 循环，可以让Python去处理这些问题。</p>
<p>下面使用<code>for</code>
 循环来打印魔术师名单中的所有名字：</p>
<p>
<em>magicians.py</em>
</p>
<pre class="代码无行号"><code>❶ magicians = ['alice', 'david', 'carolina']
❷ for magician in magicians:
❸     print(magician)</code>
</pre>
<p>首先，像第3章那样定义一个列表（见❶）。接下来，定义一个<code>for</code>
 循环（见❷）。这行代码让Python从列表<code>magicians</code>
 中取出一个名字，并将其与变量<code>magician</code>
 相关联。最后，让Python打印前面赋给变量<code>magician</code>
 的名字（见❸）。这样，对于列表中的每个名字，Python都将重复执行❷处和❸处的代码行。你可以这样解读这些代码：对于列表<code>magicians</code>
 中的每位魔术师，都将其名字打印出来。输出很简单，就是列表中所有的名字：</p>
<pre class="代码无行号"><code>alice
david
carolina</code>
</pre>
<h3 id="nav_point_71">4.1.1　深入研究循环</h3>
<p>循环这种概念很重要，因为它是让计算机自动完成重复工作的常见方式之一。例如，在前面magicians.py中使用的简单循环里，Python将首先读取其中的第一行代码：</p>
<pre class="代码无行号"><code>for magician in magicians:</code>
</pre>
<p>这行代码让Python获取列表<code>magicians</code>
 中的第一个值<code>'alice'</code>
 ，并将其与变量<code>magician</code>
 相关联。接下来，Python读取下一行代码：</p>
<pre class="代码无行号"><code>    print(magician)</code>
</pre>
<p>它让Python打印<code>magician</code>
 的值，依然是<code>'alice'</code>
 。鉴于该列表还包含其他值，Python返回到循环的第一行：</p>
<pre class="代码无行号"><code>for magician in magicians:</code>
</pre>
<p>Python获取列表中的下一个名字'david'，并将其与变量magician相关联，再执行下面这行代码：</p>
<pre class="代码无行号"><code>    print(magician)</code>
</pre>
<p>Python再次打印变量<code>magician</code>
 的值，当前为<code>'david'</code>
 。接下来，Python再次执行整个循环，对列表中的最后一个值<code>'carolina'</code>
 进行处理。至此，列表中没有其他的值了，因此Python接着执行程序的下一行代码。在这个示例中，<code>for</code>
 循环后面没有其他代码，因此程序就此结束。</p>
<p>刚开始使用循环时请牢记，对列表中的每个元素，都将执行循环指定的步骤，而不管列表包含多少个元素。如果列表包含一百万个元素，Python就重复执行指定的步骤一百万次，且通常速度非常快。</p>
<p>另外，编写<code>for</code>
 循环时，可以给依次与列表中每个值相关联的临时变量指定任意名称。然而，选择描述单个列表元素的有意义名称大有裨益。例如，对于小猫列表、小狗列表和一般性列表，像下面这样编写<code>for</code>
 循环的第一行代码是不错的选择：</p>
<pre class="代码无行号"><code>for cat in cats:
for dog in dogs:
for item in list_of_items:</code>
</pre>
<p>这些命名约定有助于你明白<code>for</code>
 循环中将对每个元素执行的操作。使用单数和复数式名称，可帮助你判断代码段处理的是单个列表元素还是整个列表。</p>
<h3 id="nav_point_72">4.1.2　在<code>for</code>
 循环中执行更多操作</h3>
<p>在<code>for</code>
 循环中，可对每个元素执行任何操作。下面来扩展前面的示例，对于每位魔术师，都打印一条消息，指出他的表演太精彩了。</p>
<p>
<em>magicians.py</em>
</p>
<pre class="代码无行号"><code>  magicians = ['alice', 'david', 'carolina']
  for magician in magicians:
❶     print(f"{magician.title()}, that was a great trick!")</code>
</pre>
<p>相比于前一个示例，唯一的不同是为每位魔术师打印了一条以其名字为抬头的消息（见❶）。这个循环第一次迭代时，变量<code>magician</code>
 的值为<code>'alice'</code>
 ，因此Python打印的第一条消息的抬头为<code>'Alice'</code>
 ；第二次迭代时，消息的抬头为<code>'David'</code>
 ；第三次迭代时，抬头为<code>'Carolina'</code>
 。</p>
<p>下面的输出表明，对于列表中的每位魔术师，都打印了一条个性化消息：</p>
<pre class="代码无行号"><code>Alice, that was a great trick!
David, that was a great trick!
Carolina, that was a great trick!</code>
</pre>
<p>在<code>for</code>
 循环中，想包含多少行代码都可以。在代码行<code>for magician in magicians</code>
 后面，每个缩进的代码行都是循环的一部分，将针对列表中的每个值都执行一次。因此，可对列表中的每个值执行任意次数的操作。</p>
<p>下面再添加一行代码，告诉每位魔术师，我们期待他的下一次表演：</p>
<pre class="代码无行号"><code>  magicians = ['alice', 'david', 'carolina']
  for magician in magicians:
      print(f"{magician.title()}, that was a great trick!")
❶     print(f"I can't wait to see your next trick, {magician.title()}.\n")</code>
</pre>
<p>两个函数调用<code>print()</code>
 都缩进了，因此它们都将针对列表中的每位魔术师执行一次。第二个函数调用<code>print()</code>
 中的换行符<code>"\n"</code>
 （见❶）在每次迭代结束后都插入一个空行，从而整洁地将针对各位魔术师的消息编组：</p>
<pre class="代码无行号"><code>Alice, that was a great trick!
I can't wait to see your next trick, Alice.
David, that was a great trick!
I can't wait to see your next trick, David.
Carolina, that was a great trick!
I can't wait to see your next trick, Carolina.</code>
</pre>
<p>在<code>for</code>
 循环中，想包含多少行代码都可以。实际上，你会发现使用<code>for</code>
 循环对每个元素执行众多不同的操作很有用。</p>
<h3 id="nav_point_73">4.1.3　在<code>for</code>
 循环结束后执行一些操作</h3>
<p>
<code>for</code>
 循环结束后怎么办呢？通常，你需要提供总结性输出或接着执行程序必须完成的其他任务。</p>
<p>在<code>for</code>
 循环后面，没有缩进的代码都只执行一次，不会重复执行。下面来打印一条向全体魔术师致谢的消息，感谢他们的精彩表演。想要在打印给各位魔术师的消息后面打印一条给全体魔术师的致谢消息，需要将相应的代码放在<code>for</code>
 循环后面，且不缩进：</p>
<pre class="代码无行号"><code>  magicians = ['alice', 'david', 'carolina']
  for magician in magicians:
      print(f"{magician.title()}, that was a great trick!")
      print(f"I can't wait to see your next trick, {magician.title()}.\n")
❶ print("Thank you, everyone. That was a great magic show!")</code>
</pre>
<p>你在前面看到了，开头两个函数调用<code>print()</code>
 针对列表中的每位魔术师重复执行。然而，第三个函数调用<code>print()</code>
 没有缩进（见❶），因此只执行一次：</p>
<pre class="代码无行号"><code>Alice, that was a great trick!
I can't wait to see your next trick, Alice.
David, that was a great trick!
I can't wait to see your next trick, David.
Carolina, that was a great trick!
I can't wait to see your next trick, Carolina.
Thank you, everyone. That was a great magic show!</code>
</pre>
<p>使用<code>for</code>
 循环处理数据是一种对数据集执行整体操作的不错方式。例如，你可能使用<code>for</code>
 循环来初始化游戏：遍历角色列表，将每个角色显示到屏幕上。然后在循环后面添加一个不缩进的代码块，在屏幕上绘制所有角色后显示一个Play Now按钮。</p>
<h2 id="nav_point_74">4.2　避免缩进错误</h2>
<p>Python根据缩进来判断代码行与前一个代码行的关系。在前面的示例中，向各位魔术师显示消息的代码行是<code>for</code>
 循环的一部分，因为它们缩进了。Python通过使用缩进让代码更易读。简单地说，它要求你使用缩进让代码整洁而结构清晰。在较长的Python程序中，你将看到缩进程度各不相同的代码块，从而对程序的组织结构有大致的认识。</p>
<p>开始编写必须正确缩进的代码时，需要注意一些常见的<strong>缩进错误</strong>
 。例如，程序员有时候会将不需要缩进的代码块缩进，而对于必须缩进的代码块却忘了缩进。查看这样的错误示例有助于你以后避开它们，以及在它们出现在程序中时进行修复。</p>
<p>下面来看一些较为常见的缩进错误。</p>
<h3 id="nav_point_75">4.2.1　忘记缩进</h3>
<p>对于位于<code>for</code>
 语句后面且属于循环组成部分的代码行，一定要缩进。如果忘记缩进， Python会提醒你：</p>
<p>
<em>magicians.py</em>
</p>
<pre class="代码无行号"><code>  magicians = ['alice', 'david', 'carolina']
  for magician in magicians:
❶ print(magician)</code>
</pre>
<p>函数调用<code>print()</code>
 （见❶）应缩进却没有缩进。Python没有找到期望缩进的代码块时，会让你知道哪行代码有问题。</p>
<pre class="代码无行号"><code>File "magicians.py", line 3
  print(magician)
      ^
IndentationError: expected an indented block</code>
</pre>
<p>通常，将紧跟在<code>for</code>
 语句后面的代码行缩进，可消除这种缩进错误。</p>
<h3 id="nav_point_76">4.2.2　忘记缩进额外的代码行</h3>
<p>有时候，循环能够运行且不会报告错误，但结果可能出人意料。试图在循环中执行多项任务，却忘记缩进其中的一些代码行时，就会出现这种情况。</p>
<p>例如，如果忘记缩进循环中的第二行代码（它告诉每位魔术师，我们期待其下次表演），就会出现这种情况：</p>
<pre class="代码无行号"><code>  magicians = ['alice', 'david', 'carolina']
  for magician in magicians:
      print(f"{magician.title()}, that was a great trick!")
❶ print(f"I can't wait to see your next trick, {magician.title()}.\n")</code>
</pre>
<p>第二个函数调用<code>print()</code>
 （见❶）原本需要缩进，但Python发现<code>for</code>
 语句后面有一行代码是缩进的，因此没有报告错误。最终的结果是，对于列表中的每位魔术师，都执行了第一个函数调用<code>print()</code>
 ，因为它缩进了；而第二个函数调用<code>print()</code>
 没有缩进，因此只在循环结束后执行一次。由于变量<code>magician</code>
 的终值为<code>'carolina'</code>
 ，结果只有她收到了消息“looking forward to the next trick”：</p>
<pre class="代码无行号"><code>Alice, that was a great trick!
David, that was a great trick!
Carolina, that was a great trick!
I can't wait to see your next trick, Carolina.</code>
</pre>
<p>这是一个<strong>逻辑错误</strong>
 。从语法上看，这些Python代码是合法的，但由于存在逻辑错误，结果并不符合预期。如果你预期某项操作将针对每个列表元素都执行一次，但它总共只执行了一次，请确定需要将一行还是多行代码缩进。</p>
<h3 id="nav_point_77">4.2.3　不必要的缩进</h3>
<p>如果你不小心缩进了无须缩进的代码行，Python将指出这一点：</p>
<p>
<em>hello_world.py</em>
</p>
<pre class="代码无行号"><code>  message = "Hello Python world!"
❶     print(message)</code>
</pre>
<p>函数调用<code>print()</code>
 （见❶）无须缩进，因为它并非循环的组成部分。因此Python将指出这种错误：</p>
<pre class="代码无行号"><code>  File "hello_world.py", line 2
    print(message)
    ^
IndentationError: unexpected indent</code>
</pre>
<p>为避免意外缩进错误，请只缩进需要缩进的代码。在前面编写的程序中，只有要在<code>for</code>
 循环中对每个元素执行的代码需要缩进。</p>
<h3 id="nav_point_78">4.2.4　循环后不必要的缩进</h3>
<p>如果不小心缩进了应在循环结束后执行的代码，这些代码将针对每个列表元素重复执行。在有些情况下，这可能导致Python报告语法错误，但在大多数情况下，这只会导致逻辑错误。</p>
<p>例如，如果不小心缩进了感谢全体魔术师精彩表演的代码行，结果将如何呢？</p>
<p>
<em>magicians.py</em>
</p>
<pre class="代码无行号"><code>  magicians = ['alice', 'david', 'carolina']
  for magician in magicians:
      print(f"{magician.title()}, that was a great trick!")
      print(f"I can't wait to see your next trick, {magician.title()}.\n")
❶ print("Thank you everyone, that was a great magic show!")</code>
</pre>
<p>由于❶处的代码行缩进了，它将针对列表中的每位魔术师执行一次，如下所示：</p>
<pre class="代码无行号"><code>Alice, that was a great trick!
I can't wait to see your next trick, Alice.
Thank you everyone, that was a great magic show!
David, that was a great trick!
I can't wait to see your next trick, David.
Thank you everyone, that was a great magic show!
Carolina, that was a great trick!
I can't wait to see your next trick, Carolina.
Thank you everyone, that was a great magic show!</code>
</pre>
<p>这也是一个逻辑错误，与4.2.2节的错误类似。Python不知道你的本意，只要代码符合语法，它就会运行。如果原本只应执行一次的操作执行了多次，可能要对执行该操作的代码取消缩进。</p>
<h3 id="nav_point_79">4.2.5　遗漏了冒号</h3>
<p>
<code>for</code>
 语句末尾的冒号告诉Python，下一行是循环的第一行。</p>
<pre class="代码无行号"><code>  magicians = ['alice', 'david', 'carolina']
❶ for magician in magicians
      print(magician)</code>
</pre>
<p>如果不小心遗漏了冒号，如❶所示，将导致语法错误，因为Python不知道你意欲何为。这种错误虽然易于消除，但并不那么容易发现。程序员为找出这样的单字符错误，花费的时间多得令人惊讶。此类错误之所以难以发现，是因为通常在人们的意料之外。</p>
<blockquote>
<p>
<strong>动手试一试</strong>
</p>
<p>
<strong>练习4-1：比萨</strong>
 　想出至少三种你喜欢的比萨，将其名称存储在一个列表中，再使用<code>for</code>
 循环将每种比萨的名称打印出来。</p>
<ul>
<li>
<p>修改这个<code>for</code>
 循环，使其打印包含比萨名称的句子，而不仅仅是比萨的名称。对于每种比萨，都显示一行输出，下面是一个例子。</p>
<p>I like pepperoni pizza.<br/>
 </p>
</li>
<li>
<p>在程序末尾添加一行代码，它不在<code>for</code>
 循环中，指出你有多喜欢比萨。输出应包含针对每种比萨的消息，还有一个总结性句子，下面是一个例子。</p>
<p>I really love pizza!<br/>
 </p>
</li>
</ul>
<p>
<strong>练习4-2：动物</strong>
 　想出至少三种有共同特征的动物，将其名称存储在一个列表中，再使用<code>for</code>
 循环将每种动物的名称打印出来。</p>
<ul>
<li>
<p>修改这个程序，使其针对每种动物都打印一个句子，下面是一个例子。</p>
<p>A dog would make a great pet.<br/>
 </p>
</li>
<li>
<p>在程序末尾添加一行代码，指出这些动物的共同之处，如打印下面这样的句子。</p>
<p>Any of these animals would make a great pet!</p>
</li>
</ul>
</blockquote>
<h2 id="nav_point_80">4.3　创建数值列表</h2>
<p>需要存储一组数的原因有很多。例如，在游戏中，需要跟踪每个角色的位置，还可能需要跟踪玩家的几个最高得分；在数据可视化中，处理的几乎都是由数（如温度、距离、人口数量、经度和纬度等）组成的集合。</p>
<p>列表非常适合用于存储数字集合，而Python提供了很多工具，可帮助你高效地处理数字列表。明白如何有效地使用这些工具后，即便列表包含数百万个元素，你编写的代码也能运行得很好。</p>
<h3 id="nav_point_81">4.3.1　使用函数<code>range()</code>
</h3>
<p>Python函数<code>range()</code>
 让你能够轻松地生成一系列数。例如，可以像下面这样使用函数<code>range()</code>
 来打印一系列数：</p>
<p>
<em>first_numbers.py</em>
</p>
<pre class="代码无行号"><code>for value in range(1, 5):
    print(value)</code>
</pre>
<p>上述代码好像应该打印数1～5，但实际上不会打印5：</p>
<pre class="代码无行号"><code>1
2
3
4</code>
</pre>
<p>在这个示例中，<code>range()</code>
 只打印数1～4。这是编程语言中常见的差一行为的结果。函数<code>range()</code>
 让Python从指定的第一个值开始数，并在到达你指定的第二个值时停止。因为它在第二个值处停止，所以输出不包含该值（这里为5）。</p>
<p>要打印数1～5，需要使用<code>range(1,6)</code>
 ：</p>
<pre class="代码无行号"><code>for value in range(1, 6):
    print(value)</code>
</pre>
<p>这样，输出将从1开始、到5结束：</p>
<pre class="代码无行号"><code>1
2
3
4
5</code>
</pre>
<p>使用<code>range()</code>
 时，如果输出不符合预期，请尝试将指定的值加1或减1。</p>
<p>调用函数<code>range()</code>
 时，也可只指定一个参数，这样它将从0开始。例如，<code>range(6)</code>
 返回数0～5。</p>
<h3 id="nav_point_82">4.3.2　使用<code>range()</code>
 创建数字列表</h3>
<p>要创建数字列表，可使用函数<code>list()</code>
 将<code>range()</code>
 的结果直接转换为列表。如果将<code>range()</code>
 作为<code>list()</code>
 的参数，输出将是一个数字列表。</p>
<p>在前一节的示例中，只是将一系列数打印出来。要将这组数转换为列表，可使用<code>list()</code>
 ：</p>
<pre class="代码无行号"><code>numbers = list(range(1, 6))
print(numbers)</code>
</pre>
<p>结果如下：</p>
<pre class="代码无行号"><code>[1, 2, 3, 4, 5]</code>
</pre>
<p>使用函数<code>range()</code>
 时，还可指定步长。为此，可给这个函数指定第三个参数，Python将根据这个步长来生成数。</p>
<p>例如，下面的代码打印1～10的偶数：</p>
<p>
<em>even_numbers.py</em>
</p>
<pre class="代码无行号"><code>even_numbers = list(range(2, 11, 2))
print(even_numbers)</code>
</pre>
<p>在这个示例中，函数<code>range()</code>
 从2开始数，然后不断加2，直到达到或超过终值（11），因此输出如下：</p>
<pre class="代码无行号"><code>[2, 4, 6, 8, 10]</code>
</pre>
<p>使用函数<code>range()</code>
 几乎能够创建任何需要的数集。例如，如何创建一个列表，其中包含前10个整数（1～10）的平方呢？在Python中，用两个星号（<code>**</code>
 ）表示乘方运算。下面的代码演示了如何将前10个整数的平方加入一个列表中：</p>
<p>
<em>squares.py</em>
</p>
<pre class="代码无行号"><code>❶ squares = []
❷ for value in range(1, 11):
❸     square = value ** 2
❹     squares.append(square)
❺ print(squares)</code>
</pre>
<p>首先，创建一个名为<code>squares</code>
 的空列表（见❶）。接下来，使用函数<code>range()</code>
 让Python遍历1～10的值（见❷）。在循环中，计算当前值的平方，并将结果赋给变量<code>square</code>
 （见❸）。然后，将新计算得到的平方值附加到列表<code>squares</code>
 末尾（见❹）。最后，循环结束后，打印列表<code>squares</code>
 （见❺）：</p>
<pre class="代码无行号"><code>[1, 4, 9, 16, 25, 36, 49, 64, 81, 100]</code>
</pre>
<p>为了让代码更简洁，可不使用临时变量<code>square</code>
 ，而直接将每个计算得到的值附加到列表末尾：</p>
<pre class="代码无行号"><code>  squares = []
  for value in range(1,11):
❶     squares.append(value**2)
  print(squares)</code>
</pre>
<p>❶处的代码与squares.py中❸处和❹处的代码等效。在循环中，计算每个值的平方，并立即将结果附加到列表<code>squares</code>
 的末尾。</p>
<p>创建更复杂的列表时，可使用上述两种方法中的任何一种。有时候，使用临时变量会让代码更易读；而在其他情况下，这样做只会让代码无谓地变长。你首先应该考虑的是，编写清晰易懂且能完成所需功能的代码，等到审核代码时，再考虑采用更高效的方法。</p>
<h3 id="nav_point_83">4.3.3　对数字列表执行简单的统计计算</h3>
<p>有几个专门用于处理数字列表的Python函数。例如，你可以轻松地找出数字列表的最大值、最小值和总和：</p>
<pre class="代码无行号"><code>&gt;&gt;&gt; digits = [1, 2, 3, 4, 5, 6, 7, 8, 9, 0]
&gt;&gt;&gt; min(digits)
0
&gt;&gt;&gt; max(digits)
9
&gt;&gt;&gt; sum(digits)
45</code>
</pre>
<blockquote>
<p>
<strong>注意</strong>
 　考虑到版面，本节使用的数字列表都很短，但这里介绍的知识也适用于包含数百万个数的列表。</p>
</blockquote>
<h3 id="nav_point_84">4.3.4　列表解析</h3>
<p>前面介绍的生成列表<code>squares</code>
 的方式包含三四行代码，而列表解析让你只需编写一行代码就能生成这样的列表。<strong>列表解析</strong>
 将<code>for</code>
 循环和创建新元素的代码合并成一行，并自动附加新元素。面向初学者的书并非都会介绍列表解析，这里之所以介绍列表解析，是因为等你开始阅读他人编写的代码时，很可能会遇到它。</p>
<p>下面的示例使用列表解析创建你在前面看到的平方数列表：</p>
<p>
<em>squares.py</em>
</p>
<pre class="代码无行号"><code>squares = [value**2 for value in range(1, 11)]
print(squares)</code>
</pre>
<p>要使用这种语法，首先指定一个描述性的列表名，如<code>squares</code>
 。然后，指定一个左方括号，并定义一个表达式，用于生成要存储到列表中的值。在这个示例中，表达式为<code>value**2</code>
 ，它计算平方值。接下来，编写一个<code>for</code>
 循环，用于给表达式提供值，再加上右方括号。在这个示例中，<code>for</code>
 循环为<code>for value in range(1,11)</code>
 ，它将值1～10提供给表达式<code>value**2</code>
 。请注意，这里的<code>for</code>
 语句末尾没有冒号。</p>
<p>结果与前面的平方数列表相同：</p>
<pre class="代码无行号"><code>[1, 4, 9, 16, 25, 36, 49, 64, 81, 100]</code>
</pre>
<p>要创建自己的列表解析，需要经过一定的练习，但能够熟练地创建常规列表后，你会发现这样做是完全值得的。当你觉得编写三四行代码来生成列表有点繁复时，就应考虑创建列表解析了。</p>
<blockquote>
<p>
<strong>动手试一试</strong>
</p>
<p>
<strong>练习4-3：数到20</strong>
 　使用一个<code>for</code>
 循环打印数1～20（含）。</p>
<p>
<strong>练习4-4：一百万</strong>
 　创建一个包含数1～1 000 000的列表，再使用一个<code>for</code>
 循环将这些数打印出来。（如果输出的时间太长，按Ctrl + C停止输出或关闭输出窗口。）</p>
<p>
<strong>练习4-5：一百万求和</strong>
 　创建一个包含数1～1 000 000的列表，再使用<code>min()</code>
 和<code>max()</code>
 核实该列表确实是从1开始、到1 000 000结束的。另外，对这个列表调用函数<code>sum()</code>
 ，看看Python将一百万个数相加需要多长时间。</p>
<p>
<strong>练习4-6：奇数</strong>
 　通过给函数<code>range()</code>
 指定第三个参数来创建一个列表，其中包含1～20的奇数，再使用一个<code>for</code>
 循环将这些数打印出来。</p>
<p>
<strong>练习4-7：3的倍数</strong>
 　创建一个列表，其中包含3～30能被3整除的数，再使用一个<code>for</code>
 循环将这个列表中的数打印出来。</p>
<p>
<strong>练习4-8：立方</strong>
 　将同一个数乘三次称为<strong>立方</strong>
 。例如，在Python中，2的立方用<code>2**3</code>
 表示。请创建一个列表，其中包含前10个整数（1～10）的立方，再使用一个<code>for</code>
 循环将这些立方数打印出来。</p>
<p>
<strong>练习4-9：立方解析</strong>
 　使用列表解析生成一个列表，其中包含前10个整数的立方。</p>
</blockquote>
<h2 id="nav_point_85">4.4　使用列表的一部分</h2>
<p>在第3章中，你学习了如何访问单个列表元素。在本章中，你一直在学习如何处理列表的所有元素。你还可以处理列表的部分元素，Python称之为<strong>切片</strong>
 。</p>
<h3 id="nav_point_86">4.4.1　切片</h3>
<p>要创建切片，可指定要使用的第一个元素和最后一个元素的索引。与函数<code>range()</code>
 一样，Python在到达第二个索引之前的元素后停止。要输出列表中的前三个元素，需要指定索引0和3，这将返回索引为0、1和2的元素。</p>
<p>下面的示例处理的是一个运动队成员列表：</p>
<p>
<em>players.py</em>
</p>
<pre class="代码无行号"><code>  players = ['charles', 'martina', 'michael', 'florence', 'eli']
❶ print(players[0:3])</code>
</pre>
<p>❶处的代码打印该列表的一个切片，其中只包含三名队员。输出也是一个列表，其中包含前三名队员：</p>
<pre class="代码无行号"><code>['charles', 'martina', 'michael']</code>
</pre>
<p>你可以生成列表的任意子集。例如，如果要提取列表的第二、第三和第四个元素，可将起始索引指定为<code>1</code>
 ，并将终止索引指定为<code>4</code>
 ：</p>
<pre class="代码无行号"><code>players = ['charles', 'martina', 'michael', 'florence', 'eli']
print(players[1:4])</code>
</pre>
<p>此时，切片始于<code>'martina'</code>
 、终于<code>'florence'</code>
 。</p>
<pre class="代码无行号"><code>['martina', 'michael', 'florence']</code>
</pre>
<p>如果没有指定第一个索引，Python将自动从列表开头开始：</p>
<pre class="代码无行号"><code>players = ['charles', 'martina', 'michael', 'florence', 'eli']
print(players[:4])</code>
</pre>
<p>由于没有指定起始索引，Python从列表开头开始提取：</p>
<pre class="代码无行号"><code>['charles', 'martina', 'michael', 'florence']</code>
</pre>
<p>要让切片终止于列表末尾，也可使用类似的语法。例如，如果要提取从第三个元素到列表末尾的所有元素，可将起始索引指定为<code>2</code>
 ，并省略终止索引：</p>
<pre class="代码无行号"><code>players = ['charles', 'martina', 'michael', 'florence', 'eli']
print(players[2:])</code>
</pre>
<p>Python将返回从第三个元素到列表末尾的所有元素：</p>
<pre class="代码无行号"><code>['michael', 'florence', 'eli']</code>
</pre>
<p>无论列表多长，这种语法都能够让你输出从特定位置到列表末尾的所有元素。上一章说过，负数索引返回离列表末尾相应距离的元素，因此你可以输出列表末尾的任意切片。例如，如果要输出名单上的最后三名队员，可使用切片<code>players[-3:]</code>
 ：</p>
<pre class="代码无行号"><code>players = ['charles', 'martina', 'michael', 'florence', 'eli']
print(players[-3:])</code>
</pre>
<p>上述代码打印最后三名队员的名字，即便队员名单长度发生变化，也依然如此。</p>
<blockquote>
<p>
<strong>注意</strong>
 　可在表示切片的方括号内指定第三个值。这个值告诉Python在指定范围内每隔多少元素提取一个。</p>
</blockquote>
<h3 id="nav_point_87">4.4.2　遍历切片</h3>
<p>如果要遍历列表的部分元素，可在<code>for</code>
 循环中使用切片。下面的示例遍历前三名队员，并打印他们的名字：</p>
<pre class="代码无行号"><code>  players = ['charles', 'martina', 'michael', 'florence', 'eli']
  print("Here are the first three players on my team:")
❶ for player in players[:3]:
      print(player.title())</code>
</pre>
<p>❶处的代码没有遍历整个队员列表，而只遍历前三名队员：</p>
<pre class="代码无行号"><code>Here are the first three players on my team:
Charles
Martina
Michael</code>
</pre>
<p>在很多情况下，切片都很有用。例如，编写游戏时，你可以在玩家退出游戏时将其最终得分加入一个列表中，然后将该列表按降序排列以获取三个最高得分，再创建一个只包含前三个得分的切片；处理数据时，可使用切片来进行批量处理；编写Web应用程序时，可使用切片来分页显示信息，并在每页显示数量合适的信息。</p>
<h3 id="nav_point_88">4.4.3　复制列表</h3>
<p>我们经常需要根据既有列表创建全新的列表。下面来介绍复制列表的工作原理，以及复制列表可提供极大帮助的一种情形。</p>
<p>要复制列表，可创建一个包含整个列表的切片，方法是同时省略起始索引和终止索引（<code>[:]</code>
 ）。这让Python创建一个始于第一个元素、终止于最后一个元素的切片，即整个列表的副本。</p>
<p>例如，假设有一个列表包含你最喜欢的四种食品，而你想再创建一个列表，并在其中包含一位朋友喜欢的所有食品。不过，你喜欢的食品，这位朋友也都喜欢，因此可通过复制来创建这个列表：</p>
<p>
<em>foods.py</em>
</p>
<pre class="代码无行号"><code>❶ my_foods = ['pizza', 'falafel', 'carrot cake']
❷ friend_foods = my_foods[:]
  print("My favorite foods are:")
  print(my_foods)
  print("\nMy friend's favorite foods are:")
  print(friend_foods)</code>
</pre>
<p>首先，创建一个你喜欢的食品列表，名为<code>my_foods</code>
 （见❶）。然后创建一个名为<code>friend_foods</code>
 的新列表（见❷）。在不指定任何索引的情况下从列表<code>my_foods</code>
 中提取一个切片，从而创建这个列表的副本，并将该副本赋给变量<code>friend_foods</code>
 。打印这两个列表后，我们发现其包含的食品相同：</p>
<pre class="代码无行号"><code>My favorite foods are:
['pizza', 'falafel', 'carrot cake']
My friend's favorite foods are:
['pizza', 'falafel', 'carrot cake']</code>
</pre>
<p>为核实确实有两个列表，下面在每个列表中都添加一种食品，并核实每个列表都记录了相应人员喜欢的食品：</p>
<pre class="代码无行号"><code>  my_foods = ['pizza', 'falafel', 'carrot cake']
❶ friend_foods = my_foods[:]
❷ my_foods.append('cannoli')
❸ friend_foods.append('ice cream')
  print("My favorite foods are:")
  print(my_foods)
  print("\nMy friend's favorite foods are:")
  print(friend_foods)</code>
</pre>
<p>与前一个示例一样，首先将<code>my_foods</code>
 的元素复制到新列表<code>friend_foods</code>
 中（见❶）。接下来，在每个列表中都添加一种食品：在列表<code>my_foods</code>
 中添加<code>'cannoli'</code>
 （见❷），而在<code>friend_foods</code>
 中添加<code>'ice cream'</code>
 （见❸）。最后，打印这两个列表，核实这两种食品分别包含在正确的列表中。</p>
<pre class="代码无行号"><code>  My favorite foods are:
❹ ['pizza', 'falafel', 'carrot cake', 'cannoli']
  My friend's favorite foods are:
❺ ['pizza', 'falafel', 'carrot cake', 'ice cream']</code>
</pre>
<p>❹处的输出表明，<code>'cannoli'</code>
 包含在你喜欢的食品列表中，而<code>'ice cream'</code>
 不在。❺处的输出表明，<code>'ice cream'</code>
 包含在你朋友喜欢的食品列表中，而<code>'cannoli'</code>
 不在。如果只是将<code>my_foods</code>
 赋给<code>friend_foods</code>
 ，就不能得到两个列表。例如，下面演示了在不使用切片的情况下复制列表的情况：</p>
<pre class="代码无行号"><code>  my_foods = ['pizza', 'falafel', 'carrot cake']
  # 这行不通:
❶ friend_foods = my_foods
  my_foods.append('cannoli')
  friend_foods.append('ice cream')
  print("My favorite foods are:")
  print(my_foods)
  print("\nMy friend's favorite foods are:")
  print(friend_foods)</code>
</pre>
<p>这里将<code>my_foods</code>
 赋给<code>friend_foods</code>
 ，而不是将<code>my_foods</code>
 的副本赋给<code>friend_foods</code>
 （见❶）。这种语法实际上是让Python将新变量<code>friend_foods</code>
 关联到已与<code>my_foods</code>
 相关联的列表，因此这两个变量指向同一个列表。有鉴于此，当我们将<code>'cannoli'</code>
 添加到<code>my_foods</code>
 中时，它也将出现在<code>friend_foods</code>
 中。同样，虽然<code>'ice cream'</code>
 好像只被加入到了<code>friend_foods</code>
 中，但它也将出现在这两个列表中。</p>
<p>输出表明，两个列表是相同的，这并非我们想要的结果：</p>
<pre class="代码无行号"><code>My favorite foods are:
['pizza', 'falafel', 'carrot cake', 'cannoli', 'ice cream']
My friend's favorite foods are:
['pizza', 'falafel', 'carrot cake', 'cannoli', 'ice cream']</code>
</pre>
<blockquote>
<p>
<strong>注意</strong>
 　暂时不要考虑这个示例中的细节。当试图使用列表的副本时结果出乎意料，基本上都要确认你是否像第一个示例那样使用切片复制了列表。</p>
</blockquote>
<p>　</p>
<blockquote>
<p>
<strong>动手试一试</strong>
</p>
<p>
<strong>练习4-10：切片</strong>
 　选择你在本章编写的一个程序，在末尾添加几行代码，以完成如下任务。</p>
<ul>
<li class="第2级无序列表">打印消息“The first three items in the list are:”，再使用切片来打印列表的前三个元素。</li>
<li class="第2级无序列表">打印消息“Three items from the middle of the list are:”，再使用切片来打印列表的中间三个元素。</li>
<li class="第2级无序列表">打印消息“The last three items in the list are:”，再使用切片来打印列表的末尾三个元素。</li>
</ul>
<p>
<strong>练习4-11：你的比萨，我的比萨</strong>
 　在你为完成练习4-1而编写的程序中，创建比萨列表的副本，并将其赋给变量<code>friend_pizzas</code>
 ，再完成如下任务。</p>
<ul>
<li class="第2级无序列表">在原来的比萨列表中添加一种比萨。</li>
<li class="第2级无序列表">在列表<code>friend_pizzas</code>
 中添加另一种比萨。</li>
<li class="第2级无序列表">核实有两个不同的列表。为此，打印消息“My favorite pizzas are:”，再使用一个<code>for</code>
 循环来打印第一个列表；打印消息“My friend's favorite pizzas are:”，再使用一个<code>for</code>
 循环来打印第二个列表。核实新增的比萨被添加到了正确的列表中。</li>
</ul>
<p>
<strong>练习4-12：使用多个循环</strong>
 　在本节中，为节省篇幅，程序foods.py的每个版本都没有使用<code>for</code>
 循环来打印列表。请选择一个版本的foods.py，在其中编写两个<code>for</code>
 循环，将各个食品列表打印出来。</p>
</blockquote>
<h2 id="nav_point_89">4.5　元组</h2>
<p>列表非常适合用于存储在程序运行期间可能变化的数据集。列表是可以修改的，这对处理网站的用户列表或游戏中的角色列表至关重要。然而，有时候你需要创建一系列不可修改的元素，元组可以满足这种需求。Python将不能修改的值称为<strong>不可变的</strong>
 ，而不可变的列表被称为<strong>元组</strong>
 。</p>
<h3 id="nav_point_90">4.5.1　定义元组</h3>
<p>元组看起来很像列表，但使用圆括号而非中括号来标识。定义元组后，就可使用索引来访问其元素，就像访问列表元素一样。</p>
<p>例如，如果有一个大小不应改变的矩形，可将其长度和宽度存储在一个元组中，从而确保它们是不能修改的：</p>
<p>
<em>dimensions.py</em>
</p>
<pre class="代码无行号"><code>❶ dimensions = (200, 50)
❷ print(dimensions[0])
  print(dimensions[1])</code>
</pre>
<p>首先定义元组<code>dimensions</code>
 （见❶），为此使用了圆括号而不是方括号。接下来，分别打印该元组的各个元素，使用的语法与访问列表元素时使用的语法相同（见❷）：</p>
<pre class="代码无行号"><code>200
50</code>
</pre>
<p>下面来尝试修改元组<code>dimensions</code>
 的一个元素，看看结果如何：</p>
<pre class="代码无行号"><code>  dimensions = (200, 50)
❶ dimensions[0] = 250</code>
</pre>
<p>❶处的代码试图修改第一个元素的值，导致Python返回类型错误消息。由于试图修改元组的操作是被禁止的，因此Python指出不能给元组的元素赋值：</p>
<pre class="代码无行号"><code>Traceback (most recent call last):
  File "dimensions.py", line 2, in &lt;module&gt;
    dimensions[0] = 250
TypeError: 'tuple' object does not support item assignment</code>
</pre>
<p>这很好，因为我们希望Python在代码试图修改矩形的尺寸时引发错误。</p>
<blockquote>
<p>
<strong>注意</strong>
 　严格地说，元组是由逗号标识的，圆括号只是让元组看起来更整洁、更清晰。如果你要定义只包含一个元素的元组，必须在这个元素后面加上逗号：</p>
<pre class="代码无行号"><code>my_t = (3,)</code>
</pre>
<p>创建只包含一个元素的元组通常没有意义，但自动生成的元组有可能只有一个元素。</p>
</blockquote>
<h3 id="nav_point_91">4.5.2　遍历元组中的所有值</h3>
<p>像列表一样，也可以使用<code>for</code>
 循环来遍历元组中的所有值：</p>
<pre class="代码无行号"><code>dimensions = (200, 50)
for dimension in dimensions:
    print(dimension)</code>
</pre>
<p>就像遍历列表时一样，Python返回元组中所有的元素：</p>
<pre class="代码无行号"><code>200
50</code>
</pre>
<h3 id="nav_point_92">4.5.3　修改元组变量</h3>
<p>虽然不能修改元组的元素，但可以给存储元组的变量赋值。因此，如果要修改前述矩形的尺寸，可重新定义整个元组：</p>
<pre class="代码无行号"><code>❶ dimensions = (200, 50)
  print("Original dimensions:")
  for dimension in dimensions:
      print(dimension)
❷ dimensions = (400, 100)
❸ print("\nModified dimensions:")
  for dimension in dimensions:
      print(dimension)</code>
</pre>
<p>首先定义一个元组，并将其存储的尺寸打印出来（见❶）。接下来，将一个新元组关联到变量<code>dimensions</code>
 （见❷）。然后，打印新的尺寸（见❸）。这次，Python不会引发任何错误，因为给元组变量重新赋值是合法的：</p>
<pre class="代码无行号"><code>Original dimensions:
200
50
Modified dimensions:
400
100</code>
</pre>
<p>相比于列表，元组是更简单的数据结构。如果需要存储的一组值在程序的整个生命周期内都不变，就可以使用元组。</p>
<blockquote>
<p>
<strong>动手试一试</strong>
</p>
<p>
<strong>练习4-13：自助餐</strong>
 　有一家自助式餐馆，只提供五种简单的食品。请想出五种简单的食品，并将其存储在一个元组中。</p>
<ul>
<li class="第2级无序列表">使用一个<code>for</code>
 循环将该餐馆提供的五种食品都打印出来。</li>
<li class="第2级无序列表">尝试修改其中的一个元素，核实Python确实会拒绝你这样做。</li>
<li class="第2级无序列表">餐馆调整了菜单，替换了它提供的其中两种食品。请编写一个这样的代码块：给元组变量赋值，并使用一个<code>for</code>
 循环将新元组的每个元素都打印出来。</li>
</ul>
</blockquote>
<h2 id="nav_point_93">4.6　设置代码格式</h2>
<p>随着你编写的程序越来越长，有必要了解一些代码格式设置约定。请花时间让你的代码尽可能易于阅读。这有助于你掌握程序是做什么的，也可以帮助他人理解你编写的代码。</p>
<p>为确保所有人编写的代码结构都大致一致，Python程序员会遵循一些格式设置约定。学会编写整洁的Python后，就能明白他人编写的Python代码的整体结构——只要他们和你遵循相同的指南。要成为专业程序员，应从现在开始就遵循这些指南，以养成良好的习惯。</p>
<h3 id="nav_point_94">4.6.1　格式设置指南</h3>
<p>要提出Python语言修改建议，需要编写<strong>Python改进提案</strong>
 （Python Enhancement Proposal，PEP）。<strong>PEP 8</strong>
 是最古老的PEP之一，向Python程序员提供了代码格式设置指南。PEP 8的篇幅很长，但基本上与复杂的编码结构相关。</p>
<p>Python格式设置指南的编写者深知，代码被阅读的次数比编写的次数多。代码编写出来后，调试时需要阅读；给程序添加新功能时，需要花很长的时间阅读；与其他程序员分享代码时，这些程序员也会阅读。</p>
<p>如果一定要在让代码易于编写和易于阅读之间做出选择，Python程序员几乎总是选择后者。下面的指南可帮助你从一开始就编写出清晰的代码。</p>
<h3 id="nav_point_95">4.6.2　缩进</h3>
<p>PEP 8建议每级缩进都使用四个空格。这既可提高可读性，又留下了足够的多级缩进空间。</p>
<p>在字处理文档中，大家常常使用制表符而不是空格来缩进。对于字处理文档来说，这样做的效果很好，但混合使用制表符和空格会让Python解释器感到迷惑。每款文本编辑器都提供了一种设置，可将你输入的制表符转换为指定数量的空格。你在编写代码时绝对应该使用制表符键，但一定要对编辑器进行设置，使其在文档中插入空格而不是制表符。</p>
<p>在程序中混合使用制表符和空格可能导致极难排查的问题。如果混合使用了制表符和空格，可将文件中的所有制表符都转换为空格，大多数编辑器提供了这样的功能。</p>
<h3 id="nav_point_96">4.6.3　行长</h3>
<p>很多Python程序员建议每行不超过80字符。最初制定这样的指南时，在大多数计算机中，终端窗口每行只能容纳79字符。当前，计算机屏幕每行可容纳的字符数多得多，为何还要使用79字符的标准行长呢？这里有别的原因。专业程序员通常会在同一个屏幕上打开多个文件，使用标准行长可以让他们在屏幕上并排打开两三个文件时同时看到各个文件的完整行。PEP 8还建议注释的行长不应超过72字符，因为有些工具为大型项目自动生成文档时，会在每行注释开头添加格式化字符。</p>
<p>PEP 8中有关行长的指南并非不可逾越的红线，有些小组将最大行长设置为99字符。在学习期间，你不用过多考虑代码的行长，但别忘了，协作编写程序时，大家几乎都遵守PEP 8指南。在大多数编辑器中，可以设置一个视觉标志（通常是一条竖线），让你知道不能越过的界线在什么地方。</p>
<blockquote>
<p>
<strong>注意</strong>
 　附录B介绍了如何配置文本编辑器，使其在你按制表符键时插入四个空格，并且显示一条垂直参考线，帮助你遵守行长不超过79字符的约定。</p>
</blockquote>
<h3 id="nav_point_97">4.6.4　空行</h3>
<p>要将程序的不同部分分开，可使用空行。你应该使用空行来组织程序文件，但也不能滥用。只要按本书的示例展示的那样做，就能掌握其中的平衡。例如，如果你有五行创建列表的代码，还有三行处理该列表的代码，那么用一个空行将这两部分隔开是合适的。然而，你不应使用三四个空行将其隔开。</p>
<p>空行不会影响代码的运行，但会影响代码的可读性。Python解释器根据水平缩进情况来解读代码，但不关心垂直间距。</p>
<h3 id="nav_point_98">4.6.5　其他格式设置指南</h3>
<p>PEP 8还有很多其他的格式设置建议，但这些指南针对的程序大多比目前为止本书提到的程序复杂。等介绍更复杂的Python结构时，我们再来分享相关的PEP 8指南。</p>
<blockquote>
<p>
<strong>动手试一试</strong>
</p>
<p>
<strong>练习4-14：PEP 8</strong>
 　请访问Python网站并搜索“PEP 8 — Style Guide for Python Code”，阅读PEP 8格式设置指南。当前，这些指南适用的情况不多，但可以大致浏览一下。</p>
<p>
<strong>练习4-15：代码审核</strong>
 　从本章编写的程序中选择三个，根据PEP 8指南对它们进行修改。</p>
<ul>
<li class="第2级无序列表">每级缩进都使用四个空格。对你使用的文本编辑器进行设置，使其在你按Tab键时插入四个空格。如果你还没有这样做，现在就去做吧（有关如何设置，请参阅附录B）。</li>
<li class="第2级无序列表">每行都不要超过80字符。对你使用的编辑器进行设置，使其在第80个字符处显示一条垂直参考线。</li>
<li class="第2级无序列表">不要在程序文件中过多使用空行。</li>
</ul>
</blockquote>
<h2 id="nav_point_99">4.7　小结</h2>
<p>在本章中，你学习了：如何高效地处理列表中的元素；如何使用<code>for</code>
 循环遍历列表，Python如何根据缩进来确定程序的结构，以及如何避免一些常见的缩进错误；如何创建简单的数字列表，以及可对数字列表执行的一些操作；如何通过切片来使用列表的一部分和复制列表。你还学习了元组（它对不应变化的值提供了一定程度的保护），以及在代码变得越来越复杂时如何设置格式，使其易于阅读。</p>
<p>在第5章中，你将学习如何使用<code>if</code>
 语句在不同的条件下采取不同的措施；如何将一组较复杂的条件测试组合起来，并在满足特定条件时采取相应的措施。你还将学习如何在遍历列表时，通过使用<code>if</code>
 语句对特定元素采取特定的措施。</p>
<br style="page-break-after:always"/>
<div></div>
]]></description>
    <pubDate>Mon, 06 Jul 2026 02:08:22 +0000</pubDate>
    <guid isPermaLink="false">4</guid>
</item>
<item>
    <title>Basic Python - 003 - 第 3 章 列表简介.epub</title>
    <description><![CDATA[<p style='color: #888;'>⏱ 预计阅读时间：60 分钟 (11982 字)</p><hr><h1 id="nav_point_53">第 3 章　列表简介</h1>
<blockquote>
<p><img alt="" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/3_Image00001.gif" style="width: 16%" width="16%"/>
 　在本章和下一章中，你将学习列表是什么以及如何使用列表元素。列表让你能够在一个地方存储成组的信息，其中可以只包含几个元素，也可以包含数百万个元素。列表是新手可直接使用的最强大的Python功能之一，它融合了众多重要的编程概念。</p>
</blockquote>
<h2 id="nav_point_54">3.1　列表是什么</h2>
<p>
<strong>列表</strong>
 由一系列按特定顺序排列的元素组成。你可以创建包含字母表中所有字母、数字0～9或所有家庭成员姓名的列表；也可以将任何东西加入列表中，其中的元素之间可以没有任何关系。列表通常包含多个元素，因此给列表指定一个表示复数的名称（如<code>letters</code>
 、<code>digits</code>
 或<code>names</code>
 ）是个不错的主意。</p>
<p>在Python中，用方括号（<code>[]</code>
 ）表示列表，并用逗号分隔其中的元素。下面是一个简单的列表示例，其中包含几种自行车：</p>
<p>
<em>bicycles.py</em>
</p>
<pre class="代码无行号"><code>bicycles = ['trek', 'cannondale', 'redline', 'specialized']
print(bicycles)</code>
</pre>
<p>如果让Python将列表打印出来，Python将打印列表的内部表示，包括方括号：</p>
<pre class="代码无行号"><code>['trek', 'cannondale', 'redline', 'specialized']</code>
</pre>
<p>鉴于这不是你要让用户看到的输出，下面来学习如何访问列表元素。</p>
<h3 id="nav_point_55">3.1.1　访问列表元素</h3>
<p>列表是有序集合，因此要访问列表的任意元素，只需将该元素的位置（<strong>索引</strong>
 ）告诉Python即可。要访问列表元素，可指出列表的名称，再指出元素的索引，并将后者放在方括号内。</p>
<p>例如，下面的代码从列表<code>bicycles</code>
 中提取第一款自行车：</p>
<pre class="代码无行号"><code>  bicycles = ['trek', 'cannondale', 'redline', 'specialized']
❶ print(bicycles[0])</code>
</pre>
<p>❶处演示了访问列表元素的语法。当你请求获取列表元素时，Python只返回该元素，而不包括方括号：</p>
<pre class="代码无行号"><code>trek</code>
</pre>
<p>这正是你要让用户看到的结果——整洁、干净的输出。</p>
<p>你还可以对任意列表元素调用第2章介绍的字符串方法。例如，可使用方法<code>title()</code>
 让元素<code>'trek'</code>
 的格式更整洁：</p>
<pre class="代码无行号"><code>bicycles = ['trek', 'cannondale', 'redline', 'specialized']
print(bicycles[0].title())</code>
</pre>
<p>这个示例的输出与前一个示例相同，只是首字母T是大写的。</p>
<h3 id="nav_point_56">3.1.2　索引从0而不是1开始</h3>
<p>在Python中，第一个列表元素的索引为0，而不是1。多数编程语言是如此规定的，这与列表操作的底层实现相关。如果结果出乎意料，请看看你是否犯了简单的差一错误。</p>
<p>第二个列表元素的索引为1。根据这种简单的计数方式，要访问列表的任何元素，都可将其位置减1，并将结果作为索引。例如，要访问第四个列表元素，可使用索引3。</p>
<p>下面的代码访问索引<code>1</code>
 和索引<code>3</code>
 处的自行车：</p>
<pre class="代码无行号"><code>bicycles = ['trek', 'cannondale', 'redline', 'specialized']
print(bicycles[1])
print(bicycles[3])</code>
</pre>
<p>这些代码返回列表中的第二个和第四个元素：</p>
<pre class="代码无行号"><code>cannondale
specialized</code>
</pre>
<p>Python为访问最后一个列表元素提供了一种特殊语法。通过将索引指定为<code>-1</code>
 ，可让Python返回最后一个列表元素：</p>
<pre class="代码无行号"><code>bicycles = ['trek', 'cannondale', 'redline', 'specialized']
print(bicycles[-1])</code>
</pre>
<p>这段代码返回<code>'specialized'</code>
 。这种语法很有用，因为你经常需要在不知道列表长度的情况下访问最后的元素。这种约定也适用于其他负数索引。例如，索引<code>-2</code>
 返回倒数第二个列表元素，索引<code>-3</code>
 返回倒数第三个列表元素，依此类推。</p>
<h3 id="nav_point_57">3.1.3　使用列表中的各个值</h3>
<p>你可以像使用其他变量一样使用列表中的各个值。例如，可以使用f字符串根据列表中的值来创建消息。</p>
<p>下面尝试从列表中提取第一款自行车，并使用这个值创建一条消息：</p>
<pre class="代码无行号"><code>  bicycles = ['trek', 'cannondale', 'redline', 'specialized']
❶ message = f"My first bicycle was a {bicycles[0].title()}."
  print(message)</code>
</pre>
<p>我们使用<code>bicycles[0]</code>
 的值生成了一个句子，并将其赋给变量<code>message</code>
 （见❶）。输出是一个简单的句子，其中包含列表中的第一款自行车：</p>
<pre class="代码无行号"><code>My first bicycle was a Trek.</code>
</pre>
<blockquote>
<p>
<strong>动手试一试</strong>
</p>
<p>请尝试编写一些简短的程序来完成下面的练习，以获得一些使用Python列表的第一手经验。你可能需要为每章创建一个文件夹，以整洁有序的方式存储为完成各章的练习而编写的程序。</p>
<p>
<strong>练习3-1：姓名</strong>
 　将一些朋友的姓名存储在一个列表中，并将其命名为<code>names</code>
 。依次访问该列表中的每个元素，从而将每个朋友的姓名打印出来。</p>
<p>
<strong>练习3-2：问候语</strong>
 　继续使用练习3-1中的列表，但不打印每个朋友的姓名，而为每人打印一条消息。每条消息都包含相同的问候语，但抬头为相应朋友的姓名。</p>
<p>
<strong>练习3-3：自己的列表</strong>
 　想想你喜欢的通勤方式，如骑摩托车或开汽车，并创建一个包含多种通勤方式的列表。根据该列表打印一系列有关这些通勤方式的宣言，下面是一个例子。</p>
<blockquote>
<p>I would like to own a Honda motorcycle.</p>
</blockquote>
</blockquote>
<h2 id="nav_point_58">3.2　修改、添加和删除元素</h2>
<p>你创建的大多数列表将是动态的，这意味着列表创建后，将随着程序的运行增删元素。例如，你创建一个游戏，要求玩家射杀从天而降的外星人。为此，可在开始时将一些外星人存储在列表中，然后每当有外星人被射杀时，都将其从列表中删除，而每次有新的外星人出现在屏幕上时，都将其添加到列表中。在整个游戏运行期间，外星人列表的长度将不断变化。</p>
<h3 id="nav_point_59">3.2.1　修改列表元素</h3>
<p>修改列表元素的语法与访问列表元素的语法类似。要修改列表元素，可指定列表名和要修改的元素的索引，再指定该元素的新值。</p>
<p>例如，假设有一个摩托车列表，其中的第一个元素为<code>'honda'</code>
 ，如何修改它的值呢？</p>
<p>
<em>motorcycles.py</em>
</p>
<pre class="代码无行号"><code>❶ motorcycles = ['honda', 'yamaha', 'suzuki']
  print(motorcycles)
❷ motorcycles[0] = 'ducati'
  print(motorcycles)</code>
</pre>
<p>首先定义一个摩托车列表，其中的第一个元素为<code>'honda'</code>
 （见❶）。接下来，将第一个元素的值改为<code>'ducati'</code>
 （见❷）。输出表明，第一个元素的值确实变了，但其他列表元素的值没变：</p>
<pre class="代码无行号"><code>['honda', 'yamaha', 'suzuki']
['ducati', 'yamaha', 'suzuki']</code>
</pre>
<p>你可以修改任意列表元素的值，而不仅仅是第一个元素的值。</p>
<h3 id="nav_point_60">3.2.2　在列表中添加元素</h3>
<p>你可能出于众多原因要在列表中添加新元素。例如，你可能希望游戏中出现新的外星人、添加可视化数据或给网站添加新注册的用户。Python提供了多种在既有列表中添加新数据的方式。</p>
<ol>
<li value="1">
<p>
<strong>在列表末尾添加元素</strong>
</p>
<p>在列表中添加新元素时，最简单的方式是将元素<strong>附加</strong>
 （append）到列表。给列表附加元素时，它将添加到列表末尾。继续使用前一个示例中的列表，在其末尾添加新元素<code>'ducati'</code>
 ：</p>
<pre class="代码无行号"><code>  motorcycles = ['honda', 'yamaha', 'suzuki']
  print(motorcycles)
❶ motorcycles.append('ducati')
  print(motorcycles)</code>
</pre>
<p>方法<code>append()</code>
 将元素<code>'ducati'</code>
 添加到列表末尾（见❶），而不影响列表中的其他所有元素：</p>
<pre class="代码无行号"><code>['honda', 'yamaha', 'suzuki']
['honda', 'yamaha', 'suzuki', 'ducati']</code>
</pre>
<p>方法<code>append()</code>
 让动态地创建列表易如反掌。例如，你可以先创建一个空列表，再使用一系列函数调用<code>append()</code>
 来添加元素。下面来创建一个空列表，再在其中添加元素<code>'honda'</code>
 、<code>'yamaha'</code>
 和<code>'suzuki'</code>
 ：</p>
<pre class="代码无行号"><code>motorcycles = []
motorcycles.append('honda')
motorcycles.append('yamaha')
motorcycles.append('suzuki')
print(motorcycles)</code>
</pre>
<p>最终的列表与前述示例中的列表完全相同：</p>
<pre class="代码无行号"><code>['honda', 'yamaha', 'suzuki']</code>
</pre>
<p>这种创建列表的方式极其常见，因为经常要等程序运行后，你才知道用户要在程序中存储哪些数据。为控制用户，可首先创建一个空列表，用于存储用户将要输入的值，然后将用户提供的每个新值附加到列表中。<br/>
 </p>
</li>
<li value="2">
<p>
<strong>在列表中插入元素</strong>
</p>
<p>使用方法<code>insert()</code>
 可在列表的任何位置添加新元素。为此，你需要指定新元素的索引和值。</p>
<pre class="代码无行号"><code>  motorcycles = ['honda', 'yamaha', 'suzuki']
❶ motorcycles.insert(0, 'ducati')
  print(motorcycles)</code>
</pre>
<p>在这个示例中，值<code>'ducati'</code>
 被插入到了列表开头（见❶）。方法<code>insert()</code>
 在索引<code>0</code>
 处添加空间，并将值<code>'ducati'</code>
 存储到这个地方。这种操作将列表中既有的每个元素都右移一个位置：</p>
<pre class="代码无行号"><code>['ducati', 'honda', 'yamaha', 'suzuki']</code>
</pre>
</li>
</ol>
<h3 id="nav_point_61">3.2.3　从列表中删除元素</h3>
<p>你经常需要从列表中删除一个或多个元素。例如，玩家将空中的一个外星人射杀后，你很可能要将其从存活的外星人列表中删除；当用户在你创建的Web应用中注销账户时，你就需要将该用户从活动用户列表中删除。你可以根据位置或值来删除列表中的元素。</p>
<ol>
<li value="1">
<p>
<strong>使用<code>del</code>
 语句删除元素</strong>
</p>
<p>如果知道要删除的元素在列表中的位置，可使用<code>del</code>
 语句。</p>
<pre class="代码无行号"><code>  motorcycles = ['honda', 'yamaha', 'suzuki']
  print(motorcycles)
❶ del motorcycles[0]
  print(motorcycles)</code>
</pre>
<p>❶处的代码使用<code>del</code>
 删除了列表<code>motorcycles</code>
 中的第一个元素<code>'honda'</code>
 ：</p>
<pre class="代码无行号"><code>['honda', 'yamaha', 'suzuki']
['yamaha', 'suzuki']</code>
</pre>
<p>使用<code>del</code>
 可删除任意位置处的列表元素，条件是知道其索引。例如，下面演示了如何删除前述列表中的第二个元素<code>'yamaha'</code>
 ：</p>
<pre class="代码无行号"><code>motorcycles = ['honda', 'yamaha', 'suzuki']
print(motorcycles)
del motorcycles[1]
print(motorcycles)</code>
</pre>
<p>下面的输出表明，已经将第二款摩托车从列表中删除了：</p>
<pre class="代码无行号"><code>['honda', 'yamaha', 'suzuki']
['honda', 'suzuki']</code>
</pre>
<p>在这两个示例中，使用<code>del</code>
 语句将值从列表中删除后，你就无法再访问它了。</p>
</li>
<li value="2">
<p>
<strong>使用方法<code>pop()</code>
 删除元素</strong>
</p>
<p>有时候，你要将元素从列表中删除，并接着使用它的值。例如，你可能需要获取刚被射杀的外星人的<img alt="x" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/3_Image00003.gif"/>
 坐标和<img alt="y" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/3_Image00004.gif"/>
 坐标，以便在相应的位置显示爆炸效果；在Web应用程序中，你可能要将用户从活跃成员列表中删除，并将其加入到非活跃成员列表中。</p>
<p>方法<code>pop()</code>
 删除列表末尾的元素，并让你能够接着使用它。术语<strong>弹出</strong>
 （pop）源自这样的类比：列表就像一个栈，而删除列表末尾的元素相当于弹出栈顶元素。</p>
<p>下面来从列表<code>motorcycles</code>
 中弹出一款摩托车：</p>
<pre class="代码无行号"><code>❶ motorcycles = ['honda', 'yamaha', 'suzuki']
  print(motorcycles)
❷ popped_motorcycle = motorcycles.pop()
❸ print(motorcycles)
❹ print(popped_motorcycle)</code>
</pre>
<p>首先定义并打印列表<code>motorcycles</code>
 （见❶）。接下来，从这个列表中弹出一个值，并将其赋给变量<code>popped_motorcycle</code>
 中（见❷）。然后打印这个列表，以核实从中删除了一个值（见❸）。最后打印弹出的值，以证明我们依然能够访问被删除的值（见❹）。</p>
<p>输出表明，列表末尾的值<code>'suzuki'</code>
 已删除，它现在被赋给了变量<code>popped_motorcycle</code>
 ：</p>
<pre class="代码无行号"><code>['honda', 'yamaha', 'suzuki']
['honda', 'yamaha']
suzuki</code>
</pre>
<p>方法<code>pop()</code>
 有什么用处呢？假设列表中的摩托车是按购买时间存储的，就可使用方法<code>pop()</code>
 打印一条消息，指出最后购买的是哪款摩托车：</p>
<pre class="代码无行号"><code>motorcycles = ['honda', 'yamaha', 'suzuki']
last_owned = motorcycles.pop()
print(f"The last motorcycle I owned was a {last_owned.title()}.")</code>
</pre>
<p>输出是一个简单的句子，指出了最新购买的是哪款摩托车：</p>
<pre class="代码无行号"><code>The last motorcycle I owned was a Suzuki.</code>
</pre>
<p> </p>
</li>
<li value="3">
<p>
<strong>弹出列表中任何位置处的元素</strong>
</p>
<p>实际上，可以使用<code>pop()</code>
 来删除列表中任意位置的元素，只需在圆括号中指定要删除元素的索引即可。</p>
<pre class="代码无行号"><code>  motorcycles = ['honda', 'yamaha', 'suzuki']
❶ first_owned = motorcycles.pop(0)
❷ print(f"The first motorcycle I owned was a {first_owned.title()}.")</code>
</pre>
<p>首先弹出列表中的第一款摩托车（见❶），然后打印一条有关这辆摩托车的消息（见❷）。输出是一个简单的句子，描述了我购买的第一辆摩托车：</p>
<pre class="代码无行号"><code>The first motorcycle I owned was a Honda.</code>
</pre>
<p>别忘了，每当你使用<code>pop()</code>
 时，被弹出的元素就不再在列表中了。</p>
<p>如果你不确定该使用<code>del</code>
 语句还是<code>pop()</code>
 方法，下面是一个简单的判断标准：如果你要从列表中删除一个元素，且不再以任何方式使用它，就使用<code>del</code>
 语句；如果你要在删除元素后还能继续使用它，就使用方法<code>pop()</code>
 。<br/>
 </p>
</li>
<li value="4">
<p>
<strong>根据值删除元素</strong>
</p>
<p>有时候，你不知道要从列表中删除的值所处的位置。如果只知道要删除的元素的值，可使用方法<code>remove()</code>
 。</p>
<p>例如，假设要从列表<code>motorcycles</code>
 中删除值<code>'ducati'</code>
 。</p>
<pre class="代码无行号"><code>  motorcycles = ['honda', 'yamaha', 'suzuki', 'ducati']
  print(motorcycles)
❶ motorcycles.remove('ducati')
  print(motorcycles)</code>
</pre>
<p>❶处的代码让Python确定<code>'ducati'</code>
 出现在列表的什么地方，并将该元素删除：</p>
<pre class="代码无行号"><code>['honda', 'yamaha', 'suzuki', 'ducati']
['honda', 'yamaha', 'suzuki']</code>
</pre>
<p>使用<code>remove()</code>
 从列表中删除元素时，也可接着使用它的值。下面删除值<code>'ducati'</code>
 并打印一条消息，指出要将其从列表中删除的原因：</p>
<pre class="代码无行号"><code>❶ motorcycles = ['honda', 'yamaha', 'suzuki', 'ducati']
  print(motorcycles)
❷ too_expensive = 'ducati'
❸ motorcycles.remove(too_expensive)
  print(motorcycles)
❹ print(f"\nA {too_expensive.title()} is too expensive for me.")</code>
</pre>
<p>定义列表（见❶）后，将值<code>'ducati'</code>
 赋给变量<code>too_expensive</code>
 （见❷）。接下来，使用这个变量来告诉Python将哪个值从列表中删除（见❸）。最后，值<code>'ducati'</code>
 已经从列表中删除，但可通过变量<code>too_expensive</code>
 来访问它（见❹）。这让我们能够打印一条消息，指出将<code>'ducati'</code>
 从列表<code>motorcycles</code>
 中删除的原因：</p>
<pre class="代码无行号"><code>['honda', 'yamaha', 'suzuki', 'ducati']
['honda', 'yamaha', 'suzuki']
A Ducati is too expensive for me.</code>
</pre>
<blockquote>
<p>
<strong>注意</strong>
 　方法<code>remove()</code>
 只删除第一个指定的值。如果要删除的值可能在列表中出现多次，就需要使用循环来确保将每个值都删除。这将在第7章中介绍。</p>
</blockquote>
<p>　</p>
<blockquote>
<p>
<strong>动手试一试</strong>
</p>
<p>下面的练习比第2章的练习要复杂些，但让你有机会以前面介绍过的各种方式使用列表。</p>
<p>
<strong>练习3-4：嘉宾名单</strong>
 　如果你可以邀请任何人一起共进晚餐（无论是在世的还是故去的），你会邀请哪些人？请创建一个列表，其中包含至少三个你想邀请的人，然后使用这个列表打印消息，邀请这些人来与你共进晚餐。</p>
<p>
<strong>练习3-5：修改嘉宾名单</strong>
 　你刚得知有位嘉宾无法赴约，因此需要另外邀请一位嘉宾。</p>
<ul>
<li class="第3级无序列表">以完成练习3-4时编写的程序为基础，在程序末尾添加一条<code>print</code>
 语句，指出哪位嘉宾无法赴约。</li>
<li class="第3级无序列表">修改嘉宾名单，将无法赴约的嘉宾的姓名替换为新邀请的嘉宾的姓名。</li>
<li class="第3级无序列表">再次打印一系列消息，向名单中的每位嘉宾发出邀请。</li>
</ul>
<p>
<strong>练习3-6：添加嘉宾</strong>
 　你刚找到了一个更大的餐桌，可容纳更多的嘉宾。请想想你还想邀请哪三位嘉宾。</p>
<ul>
<li class="第3级无序列表">以完成练习3-4或练习3-5时编写的程序为基础，在程序末尾添加一条<code>print</code>
 语句，指出你找到了一个更大的餐桌。</li>
<li class="第3级无序列表">使用<code>insert()</code>
 将一位新嘉宾添加到名单开头。</li>
<li class="第3级无序列表">使用<code>insert()</code>
 将另一位新嘉宾添加到名单中间。</li>
<li class="第3级无序列表">使用<code>append()</code>
 将最后一位新嘉宾添加到名单末尾。</li>
<li class="第3级无序列表">打印一系列消息，向名单中的每位嘉宾发出邀请。</li>
</ul>
<p>
<strong>练习3-7：缩减名单</strong>
 　你刚得知新购买的餐桌无法及时送达，因此只能邀请两位嘉宾。</p>
<ul>
<li class="第3级无序列表">以完成练习3-6时编写的程序为基础，在程序末尾添加一行代码，打印一条你只能邀请两位嘉宾共进晚餐的消息。</li>
<li class="第3级无序列表">使用<code>pop()</code>
 不断地删除名单中的嘉宾，直到只有两位嘉宾为止。每次从名单中弹出一位嘉宾时，都打印一条消息，让该嘉宾知悉你很抱歉，无法邀请他来共进晚餐。</li>
<li class="第3级无序列表">对于余下两位嘉宾中的每一位，都打印一条消息，指出他依然在受邀人之列。</li>
<li class="第3级无序列表">使用<code>del</code>
 将最后两位嘉宾从名单中删除，让名单变成空的。打印该名单，核实程序结束时名单确实是空的。</li>
</ul>
</blockquote>
</li>
</ol>
<h2 id="nav_point_62">3.3　组织列表</h2>
<p>在你创建的列表中，元素的排列顺序常常是无法预测的，因为你并非总能控制用户提供数据的顺序。这虽然在大多数情况下是不可避免的，但你经常需要以特定的顺序呈现信息。有时候，你希望保留列表元素最初的排列顺序，而有时候又需要调整排列顺序。Python提供了很多组织列表的方式，可根据具体情况选用。</p>
<h3 id="nav_point_63">3.3.1　使用方法<code>sort()</code>
 对列表永久排序</h3>
<p>Python方法<code>sort()</code>
 让你能够较为轻松地对列表进行排序。假设你有一个汽车列表，并要让其中的汽车按字母顺序排列。为简化这项任务，假设该列表中的所有值都是小写的。</p>
<p>
<em>cars.py</em>
</p>
<pre class="代码无行号"><code>  cars = ['bmw', 'audi', 'toyota', 'subaru']
❶ cars.sort()
  print(cars)</code>
</pre>
<p>方法<code>sort()</code>
 （见❶）永久性地修改列表元素的排列顺序。现在，汽车是按字母顺序排列的，再也无法恢复到原来的排列顺序：</p>
<pre class="代码无行号"><code>['audi', 'bmw', 'subaru', 'toyota']</code>
</pre>
<p>还可以按与字母顺序相反的顺序排列列表元素，只需向<code>sort()</code>
 方法传递参数<code>reverse=True</code>
 即可。下面的示例将汽车列表按与字母顺序相反的顺序排列：</p>
<pre class="代码无行号"><code>cars = ['bmw', 'audi', 'toyota', 'subaru']
cars.sort(reverse=True)
print(cars)</code>
</pre>
<p>同样，对列表元素排列顺序的修改是永久性的：</p>
<pre class="代码无行号"><code>['toyota', 'subaru', 'bmw', 'audi']</code>
</pre>
<h3 id="nav_point_64">3.3.2　使用函数<code>sorted()</code>
 对列表临时排序</h3>
<p>要保留列表元素原来的排列顺序，同时以特定的顺序呈现它们，可使用函数<code>sorted()</code>
 。函数<code>sorted()</code>
 让你能够按特定顺序显示列表元素，同时不影响它们在列表中的原始排列顺序。</p>
<p>下面尝试来对汽车列表调用这个函数。</p>
<pre class="代码无行号"><code>  cars = ['bmw', 'audi', 'toyota', 'subaru']
❶ print("Here is the original list:")
  print(cars)
❷ print("\nHere is the sorted list:")
  print(sorted(cars))
❸ print("\nHere is the original list again:")
  print(cars)</code>
</pre>
<p>首先按原始顺序打印列表（见❶），再按字母顺序显示该列表（见❷）。以特定顺序显示列表后，我们进行核实，确认列表元素的排列顺序与以前相同（见❸）。</p>
<pre class="代码无行号"><code>  Here is the original list:
  ['bmw', 'audi', 'toyota', 'subaru']
  Here is the sorted list:
  ['audi', 'bmw', 'subaru', 'toyota']
❹ Here is the original list again:
  ['bmw', 'audi', 'toyota', 'subaru']</code>
</pre>
<p>注意，调用函数<code>sorted()</code>
 后，列表元素的排列顺序并没有变（见❹）。如果要按与字母顺序相反的顺序显示列表，也可向函数<code>sorted()</code>
 传递参数<code>reverse=True</code>
 。</p>
<blockquote>
<p>
<strong>注意</strong>
 　在并非所有的值都是小写时，按字母顺序排列列表要复杂些。决定排列顺序时，有多种解读大写字母的方式，要指定准确的排列顺序，可能比我们这里所做的要复杂。然而，大多数排序方式是以本节介绍的知识为基础的。</p>
</blockquote>
<h3 id="nav_point_65">3.3.3　倒着打印列表</h3>
<p>要反转列表元素的排列顺序，可使用方法<code>reverse()</code>
 。假设汽车列表是按购买时间排列的，可轻松地按相反的顺序排列其中的汽车：</p>
<pre class="代码无行号"><code>cars = ['bmw', 'audi', 'toyota', 'subaru']
print(cars)
cars.reverse()
print(cars)</code>
</pre>
<p>注意，<code>reverse()</code>
 不是按与字母顺序相反的顺序排列列表元素，而只是反转列表元素的排列顺序：</p>
<pre class="代码无行号"><code>['bmw', 'audi', 'toyota', 'subaru']
['subaru', 'toyota', 'audi', 'bmw']</code>
</pre>
<p>方法<code>reverse()</code>
 永久性地修改列表元素的排列顺序，但可随时恢复到原来的排列顺序，只需对列表再次调用<code>reverse()</code>
 即可。</p>
<h3 id="nav_point_66">3.3.4　确定列表的长度</h3>
<p>使用函数<code>len()</code>
 可快速获悉列表的长度。在下面的示例中，列表包含四个元素，因此其长度为<code>4</code>
 ：</p>
<pre class="代码无行号"><code>&gt;&gt;&gt; cars = ['bmw', 'audi', 'toyota', 'subaru']
&gt;&gt;&gt; len(cars)
4</code>
</pre>
<p>需要完成如下任务时，<code>len()</code>
 很有用：明确还有多少个外星人未被射杀，确定需要管理多少项可视化数据，计算网站有多少注册用户，等等。</p>
<blockquote>
<p>
<strong>注意</strong>
 　Python计算列表元素数时从1开始，因此确定列表长度时，你应该不会遇到差一错误。</p>
</blockquote>
<p>　</p>
<blockquote>
<p>
<strong>动手试一试</strong>
</p>
<p>
<strong>练习3-8：放眼世界</strong>
 　想出至少5个你渴望去旅游的地方。</p>
<p>将这些地方存储在一个列表中，并确保其中的元素不是按字母顺序排列的。</p>
<p>按原始排列顺序打印该列表。不要考虑输出是否整洁的问题，只管打印原始Python列表。</p>
<ul>
<li class="第2级无序列表">使用<code>sorted()</code>
 按字母顺序打印这个列表，同时不要修改它。</li>
<li class="第2级无序列表">再次打印该列表，核实排列顺序未变。</li>
<li class="第2级无序列表">使用<code>sorted()</code>
 按与字母顺序相反的顺序打印这个列表，同时不要修改它。</li>
<li class="第2级无序列表">再次打印该列表，核实排列顺序未变。</li>
<li class="第2级无序列表">使用<code>reverse()</code>
 修改列表元素的排列顺序。打印该列表，核实排列顺序确实变了。</li>
<li class="第2级无序列表">使用<code>reverse()</code>
 再次修改列表元素的排列顺序。打印该列表，核实已恢复到原来的排列顺序。</li>
<li class="第2级无序列表">使用<code>sort()</code>
 修改该列表，使其元素按字母顺序排列。打印该列表，核实排列顺序确实变了。</li>
<li class="第2级无序列表">使用<code>sort()</code>
 修改该列表，使其元素按与字母顺序相反的顺序排列。打印该列表，核实排列顺序确实变了。</li>
</ul>
<p>
<strong>练习3-9：晚餐嘉宾</strong>
 　在完成练习3-4~练习3-7时编写的程序之一中，使用<code>len()</code>
 打印一条消息，指出你邀请了多少位嘉宾来共进晚餐。</p>
<p>
<strong>练习3-10：尝试使用各个函数</strong>
 　想想可存储到列表中的东西，如山川、河流、国家、城市、语言或你喜欢的任何东西。编写一个程序，在其中创建一个包含这些元素的列表，然后，对于本章介绍的每个函数，都至少使用一次来处理这个列表。</p>
</blockquote>
<h2 id="nav_point_67">3.4　使用列表时避免索引错误</h2>
<p>刚开始使用列表时，经常会遇到一种错误。假设你有一个包含三个元素的列表，却要求获取第四个元素：</p>
<p>
<em>motorcycles.py</em>
</p>
<pre class="代码无行号"><code>motorcycles = ['honda', 'yamaha', 'suzuki']
print(motorcycles[3])</code>
</pre>
<p>这将导致<strong>索引错误</strong>
 ：</p>
<pre class="代码无行号"><code>Traceback (most recent call last):
  File "motorcycles.py", line 2, in &lt;module&gt;
    print(motorcycles[3])
IndexError: list index out of range</code>
</pre>
<p>Python试图向你提供位于索引<code>3</code>
 处的元素，但它搜索列表<code>motorcycles</code>
 时，却发现索引<code>3</code>
 处没有元素。鉴于列表索引差一的特征，这种错误很常见。有些人从1开始数，因此以为第三个元素的索引为3。然而在Python中，第三个元素的索引为2，因为索引是从0开始的。</p>
<p>索引错误意味着Python在指定索引处找不到元素。程序发生索引错误时，请尝试将指定的索引减1，然后再次运行程序，看看结果是否正确。</p>
<p>别忘了，每当需要访问最后一个列表元素时，都可使用索引<code>-1</code>
 。这在任何情况下都行之有效，即便你最后一次访问列表后，其长度发生了变化：</p>
<pre class="代码无行号"><code>motorcycles = ['honda', 'yamaha', 'suzuki']
print(motorcycles[-1])</code>
</pre>
<p>索引<code>-1</code>
 总是返回最后一个列表元素，这里为值<code>'suzuki'</code>
 ：</p>
<pre class="代码无行号"><code>'suzuki'</code>
</pre>
<p>仅当列表为空时，这种访问最后一个元素的方式才会导致错误：</p>
<pre class="代码无行号"><code>motorcycles = []
print(motorcycles[-1])</code>
</pre>
<p>列表<code>motorcycles</code>
 不包含任何元素，因此Python返回一条索引错误消息：</p>
<pre class="代码无行号"><code>Traceback (most recent call last):
  File "motorcyles.py", line 3, in &lt;module&gt;
    print(motorcycles[-1])
IndexError: list index out of range</code>
</pre>
<blockquote>
<p>
<strong>注意</strong>
 　发生索引错误却找不到解决办法时，请尝试将列表或其长度打印出来。列表可能与你以为的截然不同，在程序对其进行了动态处理时尤其如此。通过查看列表或其包含的元素数，可帮助你找出这种逻辑错误。</p>
</blockquote>
<p>　</p>
<blockquote>
<p>
<strong>动手试一试</strong>
</p>
<p>
<strong>练习3-11：有意引发错误</strong>
 　如果你还没有在程序中遇到过索引错误，就尝试引发一个这种错误。在你的一个程序中，修改其中的索引，以引发索引错误。关闭程序前，务必消除这个错误。</p>
</blockquote>
<h2 id="nav_point_68">3.5　小结</h2>
<p>在本章中，你学习了：列表是什么以及如何使用其中的元素；如何定义列表以及如何增删元素；如何对列表进行永久性排序，以及如何为展示列表而进行临时排序；如何确定列表的长度，以及在使用列表时如何避免索引错误。</p>
<p>在第4章，你将学习如何以更高效的方式处理列表元素。通过使用为数不多的几行代码来遍历列表元素，你就能高效地处理它们，即便列表包含数千乃至数百万个元素。</p>
<br style="page-break-after:always"/>
<div></div>
]]></description>
    <pubDate>Sat, 04 Jul 2026 01:55:23 +0000</pubDate>
    <guid isPermaLink="false">3</guid>
</item>
<item>
    <title>Basic Python - 002 - 第 2 章 变量和简单数据类型.epub</title>
    <description><![CDATA[<p style='color: #888;'>⏱ 预计阅读时间：67 分钟 (13277 字)</p><hr><h1 id="nav_point_29">第 2 章　变量和简单数据类型</h1>
<blockquote>
<p><img alt="" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/2_Image00001.gif" style="width: 16%" width="16%"/>
 　在本章中，你将学习可在Python程序中使用的各种数据，还将学习如何在程序中使用变量来表示这些数据。</p>
</blockquote>
<h2 id="nav_point_30">2.1　运行hello_world.py时发生的情况</h2>
<p>运行hello_world.py时，Python都做了些什么呢？下面来深入研究一下。实际上，即便是运行简单的程序，Python所做的工作也相当多：</p>
<p>
<em>hello_world.py</em>
</p>
<pre class="代码无行号"><code>print("Hello Python world!")</code>
</pre>
<p>运行上述代码时，你将看到如下输出：</p>
<pre class="代码无行号"><code>Hello Python world!</code>
</pre>
<p>运行文件hello_world.py时，末尾的.py指出这是一个Python程序，因此编辑器将使用<strong>Python解释器</strong>
 来运行它。Python解释器读取整个程序，确定其中每个单词的含义。例如，看到后面跟着圆括号的单词<code>print</code>
 时，解释器就将圆括号中的内容打印到屏幕。</p>
<p>编写程序时，编辑器会以各种方式突出程序的不同部分。例如，它知道<code>print()</code>
 是一个函数的名称，因此将其显示为某种颜色；它知道<code>"Hello Python world!"</code>
 不是Python代码，因此将其显示为另一种颜色。这种功能称为<strong>语法高亮</strong>
 ，在你刚开始编写程序时很有帮助。</p>
<h2 id="nav_point_31">2.2　变量</h2>
<p>下面来尝试在hello_world.py中使用一个变量。在这个文件开头添加一行代码，并对第二行代码进行修改，如下所示：</p>
<p>
<em>hello_world.py</em>
</p>
<pre class="代码无行号"><code>message = "Hello Python world!"
print(message)</code>
</pre>
<p>运行这个程序，看看结果如何。你会发现，输出与以前相同：</p>
<pre class="代码无行号"><code>Hello Python world!</code>
</pre>
<p>我们添加了一个名为<code>message</code>
 的<strong>变量</strong>
 。每个变量都指向一个<strong>值</strong>
 ——与该变量相关联的信息。在这里，指向的值为文本<code>"Hello Python world!"</code>
 。</p>
<p>添加变量导致Python解释器需要做更多工作。处理第一行代码时，它将变量<code>message</code>
 与文本<code>"Hello Python world!"</code>
 关联起来；处理第二行代码时，它将与变量<code>message</code>
 关联的值打印到屏幕。</p>
<p>下面来进一步扩展这个程序：修改hello_world.py，使其再打印一条消息。为此，在hello_world.py中添加一个空行，再添加下面两行代码：</p>
<pre class="代码无行号"><code>message = "Hello Python world!"
print(message)
message = "Hello Python Crash Course world!"
print(message)</code>
</pre>
<p>现在如果运行这个程序，将看到两行输出：</p>
<pre class="代码无行号"><code>Hello Python world!
Hello Python Crash Course world!</code>
</pre>
<p>在程序中可随时修改变量的值，而Python将始终记录变量的最新值。</p>
<h3 id="nav_point_32">2.2.1　变量的命名和使用</h3>
<p>在Python中使用变量时，需要遵守一些规则和指南。违反这些规则将引发错误，而指南旨在让你编写的代码更容易阅读和理解。请务必牢记下述有关变量的规则。</p>
<ul>
<li class="第1级无序列表">变量名只能包含字母、数字和下划线。变量名能以字母或下划线打头，但不能以数字打头。例如，可将变量命名为<code>message_1</code>
 ，但不能将其命名为<code>1_message</code>
 。</li>
<li class="第1级无序列表">变量名不能包含空格，但能使用下划线来分隔其中的单词。例如，变量名<code>greeting_message</code>
 可行，但变量名<code>greeting message</code>
 会引发错误。</li>
<li class="第1级无序列表">不要将Python关键字和函数名用作变量名，即不要使用Python保留用于特殊用途的单词，如<code>print</code>
 （请参见附录A.4）。</li>
<li class="第1级无序列表">变量名应既简短又具有描述性。例如，<code>name</code>
 比<code>n</code>
 好，<code>student_name</code>
 比<code>s_n</code>
 好，<code>name_length</code>
 比<code>length_of_persons_name</code>
 好。</li>
<li class="第1级无序列表">慎用小写字母<code>l</code>
 和大写字母<code>O</code>
 ，因为它们可能被人错看成数字<code>1</code>
 和<code>0</code>
 。</li>
</ul>
<p>要创建良好的变量名，需要经过一定的实践，在程序复杂而有趣时尤其如此。随着编写的程序越来越多，并开始阅读别人编写的代码，你将越来越善于创建有意义的变量名。</p>
<blockquote>
<p>
<strong>注意</strong>
 　就目前而言，应使用小写的Python变量名。虽然在变量名中使用大写字母不会导致错误，但是大写字母在变量名中有特殊含义，这将在本书后面讨论。</p>
</blockquote>
<h3 id="nav_point_33">2.2.2　使用变量时避免命名错误</h3>
<p>程序员都会犯错，而且大多数程序员每天都会犯错。虽然优秀的程序员也会犯错，但他们也知道如何高效地消除错误。下面来看一种你可能犯的错误，并学习如何消除它。</p>
<p>我们将有意地编写一些引发错误的代码。请输入下面的代码，包括其中拼写不正确、以粗体显示的单词<code>mesage</code>
 ：</p>
<pre class="代码无行号"><code>message = "Hello Python Crash Course reader!"
print(mesage)</code>
</pre>
<p>程序存在错误时，Python解释器将竭尽所能地帮助你找出问题所在。程序无法成功运行时，解释器将提供一个traceback。traceback是一条记录，指出了解释器尝试运行代码时，在什么地方陷入了困境。下面是你不小心错误地拼写了变量名时，Python解释器提供的traceback：</p>
<pre class="代码无行号"><code>  Traceback (most recent call last):
❶   File "hello_world.py", line 2, in &lt;module&gt;
❷     print(mesage)
❸ NameError: name 'mesage' is not defined</code>
</pre>
<p>解释器指出，文件hello_world.py的第二行存在错误（见❶）。它列出了这行代码，旨在帮助你快速找出错误（见❷），还指出了它发现的是什么样的错误（见❸）。在这里，解释器发现了一个<strong>名称错误</strong>
 ，并报告打印的变量<code>mesage</code>
 未定义：Python无法识别你提供的变量名。名称错误通常意味着两种情况：要么是使用变量前忘记给它赋值，要么是输入变量名时拼写不正确。</p>
<p>在这个示例中，第二行的变量名<code>message</code>
 遗漏了字母s。Python解释器不会对代码做拼写检查，但要求变量名的拼写一致。例如，如果在代码的另一个地方也将<code>message</code>
 错误地拼写成了<code>mesage</code>
 ，结果将如何呢？</p>
<pre class="代码无行号"><code>mesage = "Hello Python Crash Course reader!"
print(mesage)</code>
</pre>
<p>在这种情况下，程序将成功运行！</p>
<pre class="代码无行号"><code>Hello Python Crash Course reader!</code>
</pre>
<p>编程语言要求严格，但并不关心拼写是否正确。因此，创建变量名和编写代码时，无须考虑英语中的拼写和语法规则。</p>
<p>很多编程错误都简单，只是在程序的某一行输错了一个字符。为找出这种错误而花费很长时间的大有人在。很多程序员天资聪颖、经验丰富，却为找出这种细微的错误花费数小时。你可能觉得这很好笑，但别忘了，在你的编程生涯中，经常会有同样的遭遇。</p>
<h3 id="nav_point_34">2.2.3　变量是标签</h3>
<p>变量常被描述为可用于存储值的盒子。在你刚接触变量时，这种定义可能很有帮助，但它并没有准确描述Python内部表示变量的方式。一种好得多的定义是，变量是可以赋给值的标签，也可以说变量指向特定的值。</p>
<p>刚学习编程时，这种差别可能意义不大，但越早知道越好。你迟早会遇到变量的行为出乎意料的情形，此时如果对变量的工作原理有准确的认识，将有助于搞清楚代码是如何运行的。</p>
<blockquote>
<p>
<strong>注意</strong>
 　要理解新的编程概念，最佳的方式是尝试在程序中使用它们。如果你在完成本书的练习时陷入了困境，请尝试做点其他的事情。如果这样做后依然无法摆脱困境，请复习相关内容。如果这样做后情况依然如故，请参阅附录C的建议。</p>
</blockquote>
<p>　</p>
<blockquote>
<p>
<strong>动手试一试</strong>
</p>
<p>在完成下面的每个练习时，都编写一个独立的程序。保存每个程序时，使用符合标准Python约定的文件名：使用小写字母和下划线，如simple_message.py和simple_messages.py。</p>
<p>
<strong>练习2-1：简单消息</strong>
 　将一条消息赋给变量，并将其打印出来。</p>
<p>
<strong>练习2-2：多条简单消息</strong>
 　将一条消息赋给变量，并将其打印出来；再将变量的值修改为一条新消息，并将其打印出来。</p>
</blockquote>
<h2 id="nav_point_35">2.3　字符串</h2>
<p>大多数程序定义并收集某种数据，然后使用它们来做些有意义的事情。有鉴于此，对数据进行分类大有裨益。我们将介绍的第一种数据类型是字符串。字符串虽然看似简单，但能够以很多不同的方式使用。</p>
<p>
<strong>字符串</strong>
 就是一系列字符。在Python中，用引号括起的都是字符串，其中的引号可以是单引号，也可以是双引号，如下所示：</p>
<pre class="代码无行号"><code>"This is a string."
'This is also a string.'</code>
</pre>
<p>这种灵活性让你能够在字符串中包含引号和撇号：</p>
<pre class="代码无行号"><code>'I told my friend, "Python is my favorite language!"'
"The language 'Python' is named after Monty Python, not the snake."
"One of Python's strengths is its diverse and supportive community."</code>
</pre>
<p>下面来看一些使用字符串的方式。</p>
<h3 id="nav_point_36">2.3.1　使用方法修改字符串的大小写</h3>
<p>对于字符串，可执行的最简单的操作之一是修改其中单词的大小写。请看下面的代码，并尝试判断其作用：</p>
<p>
<em>name.py</em>
</p>
<pre class="代码无行号"><code>name = "ada lovelace"
print(name.title())</code>
</pre>
<p>将这个文件保存为name.py，再运行它。你将看到如下输出：</p>
<pre class="代码无行号"><code>Ada Lovelace</code>
</pre>
<p>在这个示例中，变量<code>name</code>
 指向小写的字符串<code>"ada lovelace"</code>
 。在函数调用<code>print()</code>
 中，方法<code>title()</code>
 出现在这个变量的后面。<strong>方法</strong>
 是Python可对数据执行的操作。在<code>name.title()</code>
 中，<code>name</code>
 后面的句点（<code>.</code>
 ）让Python对变量<code>name</code>
 执行方法<code>title()</code>
 指定的操作。每个方法后面都跟着一对圆括号，这是因为方法通常需要额外的信息来完成其工作。这种信息是在圆括号内提供的。函数<code>title()</code>
 不需要额外的信息，因此它后面的圆括号是空的。</p>
<p>方法<code>title()</code>
 以首字母大写的方式显示每个单词，即将每个单词的首字母都改为大写。这很有用，因为你经常需要将名字视为信息。例如，你可能希望程序将值<code>Ada</code>
 、<code>ADA</code>
 和<code>ada</code>
 视为同一个名字，并将它们都显示为<code>Ada</code>
 。</p>
<p>还有其他几个很有用的大小写处理方法。例如，要将字符串改为全部大写或全部小写，可以像下面这样做：</p>
<pre class="代码无行号"><code>name = "Ada Lovelace"
print(name.upper())
print(name.lower())</code>
</pre>
<p>这些代码的输出如下：</p>
<pre class="代码无行号"><code>ADA LOVELACE
ada lovelace</code>
</pre>
<p>存储数据时，方法<code>lower()</code>
 很有用。很多时候，你无法依靠用户来提供正确的大小写，因此需要将字符串先转换为小写，再存储它们。以后需要显示这些信息时，再将其转换为最合适的大小写方式。</p>
<h3 id="nav_point_37">2.3.2　在字符串中使用变量</h3>
<p>在有些情况下，你可能想在字符串中使用变量的值。例如，你可能想使用两个变量分别表示名和姓，然后合并这两个值以显示姓名：</p>
<p>
<em>full_name.py</em>
</p>
<pre class="代码无行号"><code>  first_name = "ada"
  last_name = "lovelace"
❶ full_name = f"{first_name} {last_name}"
  print(full_name)</code>
</pre>
<p>要在字符串中插入变量的值，可在前引号前加上字母<code>f</code>
 （见❶），再将要插入的变量放在花括号内。这样，当Python显示字符串时，将把每个变量都替换为其值。</p>
<p>这种字符串名为<strong>f字符串</strong>
 。f是format（设置格式）的简写，因为Python通过把花括号内的变量替换为其值来设置字符串的格式。上述代码的输出如下：</p>
<pre class="代码无行号"><code>ada lovelace</code>
</pre>
<p>使用f字符串可完成很多任务，如利用与变量关联的信息来创建完整的消息，如下所示：</p>
<pre class="代码无行号"><code>  first_name = "ada"
  last_name = "lovelace"
  full_name = f"{first_name} {last_name}"
❶ print(f"Hello, {full_name.title()}!")</code>
</pre>
<p>在这里，一个问候用户的句子中使用了完整的姓名（见❶），并使用方法<code>title()</code>
 来将姓名设置为合适的格式。这些代码显示一条格式良好的简单问候语：</p>
<pre class="代码无行号"><code>Hello, Ada Lovelace!</code>
</pre>
<p>还可以使用f字符串来创建消息，再把整条消息赋给变量：</p>
<pre class="代码无行号"><code>  first_name = "ada"
  last_name = "lovelace"
  full_name = f"{first_name} {last_name}"
❶ message = f"Hello, {full_name.title()}!"
❷ print(message)</code>
</pre>
<p>上述代码也显示消息<code>Hello, Ada Lovelace!</code>
 ，但将这条消息赋给了一个变量（见❶），这让最后的函数调用<code>print()</code>
 变得简单得多（见❷）。</p>
<blockquote>
<p>
<strong>注意</strong>
 　f字符串是Python 3.6引入的。如果你使用的是Python 3.5或更早的版本，需要使用<code>format()</code>
 方法，而非这种f语法。要使用方法<code>format()</code>
 ，可在圆括号内列出要在字符串中使用的变量。对于每个变量，都通过一对花括号来引用。这样将按顺序将这些花括号替换为圆括号内列出的变量的值，如下所示：</p>
<pre class="代码无行号"><code>full_name = "{} {}".format(first_name, last_name)</code>
</pre>
</blockquote>
<h3 id="nav_point_38">2.3.3　使用制表符或换行符来添加空白</h3>
<p>在编程中，<strong>空白</strong>
 泛指任何非打印字符，如空格、制表符和换行符。你可以使用空白来组织输出，让用户阅读起来更容易。</p>
<p>要在字符串中添加制表符，可使用字符组合<code>\t</code>
 ，如下述代码的❶处所示：</p>
<pre class="代码无行号"><code>  &gt;&gt;&gt; print("Python")
  Python
❶ &gt;&gt;&gt; print("\tPython")
      Python</code>
</pre>
<p>要在字符串中添加换行符，可使用字符组合<code>\n</code>
 ：</p>
<pre class="代码无行号"><code>&gt;&gt;&gt; print("Languages:\nPython\nC\nJavaScript")
Languages:
Python
C
JavaScript</code>
</pre>
<p>还可在同一个字符串中同时包含制表符和换行符。字符串<code>"\n\t"</code>
 让Python换到下一行，并在下一行开头添加一个制表符。下面的示例演示了如何使用一个单行字符串来生成四行输出：</p>
<pre class="代码无行号"><code>&gt;&gt;&gt; print("Languages:\n\tPython\n\tC\n\tJavaScript")
Languages:
    Python
    C
    JavaScript</code>
</pre>
<p>在接下来的两章中，你将使用为数不多的几行代码来生成很多行输出，届时制表符和换行符将提供极大的帮助。</p>
<h3 id="nav_point_39">2.3.4　删除空白</h3>
<p>在程序中，额外的空白可能令人迷惑。对程序员来说，<code>'python'</code>
 和<code>'python '</code>
 看起来几乎没什么两样，但对程序来说，它们却是两个不同的字符串。Python能够发现<code>'python '</code>
 中额外的空白，并认为它意义重大——除非你告诉它不是这样的。</p>
<p>空白很重要，因为你经常需要比较两个字符串是否相同。一个重要的示例是，在用户登录网站时检查其用户名。不过在非常简单的情形下，额外的空格也可能令人迷惑。所幸，在Python中删除用户输入数据中的多余空白易如反掌。</p>
<p>Python能够找出字符串开头和末尾多余的空白。要确保字符串末尾没有空白，可使用方法<code>rstrip()</code>
 。</p>
<pre class="代码无行号"><code>❶ &gt;&gt;&gt; favorite_language = 'python '
❷ &gt;&gt;&gt; favorite_language
  'python '
❸ &gt;&gt;&gt; favorite_language.rstrip()
  'python'
❹ &gt;&gt;&gt; favorite_language
  'python '</code>
</pre>
<p>与变量<code>favorite_language</code>
 相关联的字符串末尾有多余的空白（见❶）。你在终端会话中向Python询问这个变量的值时，可看到末尾的空格（见❷）。对变量<code>favorite_language</code>
 调用方法<code>rstrip()</code>
 后（见❸），这个多余的空格被删除了。然而，这种删除只是暂时的，接下来再次询问<code>favorite_language</code>
 的值时，你会发现这个字符串与输入时一样，依然包含多余的空白（见❹）。</p>
<p>要永久删除这个字符串中的空白，必须将删除操作的结果关联到变量：</p>
<pre class="代码无行号"><code>  &gt;&gt;&gt; favorite_language = 'python '
❶ &gt;&gt;&gt; favorite_language = favorite_language.rstrip()
  &gt;&gt;&gt; favorite_language
  'python'</code>
</pre>
<p>为删除这个字符串中的空白，要将其末尾的空白剔除，再将结果关联到原来的变量（见❶）。在编程中，经常需要修改变量的值，再将新值关联到原来的变量。这就是变量的值可能随程序的运行或用户输入数据而发生变化的原因所在。</p>
<p>你还可以剔除字符串开头的空白，或者同时剔除字符串两边的空白。为此，可分别使用方法<code>lstrip()</code>
 和<code>strip()</code>
 ：</p>
<pre class="代码无行号"><code>❶ &gt;&gt;&gt; favorite_language = ' python '
❷ &gt;&gt;&gt; favorite_language.rstrip()
  ' python'
❸ &gt;&gt;&gt; favorite_language.lstrip()
  'python '
❹ &gt;&gt;&gt; favorite_language.strip()
  'python'</code>
</pre>
<p>在这个示例中，我们首先创建了一个开头和末尾都有空白的字符串（见❶）。接下来，分别删除末尾（见❷）、开头（见❸）和两边（见❹）的空白。尝试使用这些剥除函数有助于你熟悉字符串操作。在实际程序中，这些剥除函数最常用于在存储用户输入前对其进行清理。</p>
<h3 id="nav_point_40">2.3.5　使用字符串时避免语法错误</h3>
<p>
<strong>语法错误</strong>
 是一种你时不时会遇到的错误。程序中包含非法的Python代码时，就会导致语法错误。例如，在用单引号括起的字符串中，如果包含撇号，就将导致错误。这是因为这会导致Python将第一个单引号和撇号之间的内容视为一个字符串，进而将余下的文本视为Python代码，从而引发错误。</p>
<p>下面演示了如何正确地使用单引号和双引号。请将该程序保存为apostrophe.py，再运行它：</p>
<p>
<em>apostrophe.py</em>
</p>
<pre class="代码无行号"><code>message = "One of Python's strengths is its diverse community."
print(message)</code>
</pre>
<p>撇号位于两个双引号之间，因此Python解释器能够正确地理解这个字符串：</p>
<pre class="代码无行号"><code>One of Python's strengths is its diverse community.</code>
</pre>
<p>然而，如果使用单引号，Python将无法正确地确定字符串的结束位置：</p>
<pre class="代码无行号"><code>message = 'One of Python's strengths is its diverse community.'
print(message)</code>
</pre>
<p>你将看到如下输出：</p>
<pre class="代码无行号"><code>  File "apostrophe.py", line 1
    message = 'One of Python's strengths is its diverse community.'
                            ^❶
SyntaxError: invalid syntax</code>
</pre>
<p>从上述输出可知，错误发生在第二个单引号后面（见❶）。这种语法错误表明，在解释器看来，其中的有些内容不是有效的Python代码。错误的原因各种各样，我将指出一些常见的原因。学习编写Python代码时，你可能会经常遇到语法错误。语法错误也是最不具体的错误类型，因此可能难以找出并修复。受困于非常棘手的错误时，请参阅附录C提供的建议。</p>
<blockquote>
<p>
<strong>注意</strong>
 　编写程序时，编辑器的语法高亮功能可帮助你快速找出某些语法错误。看到Python代码以普通句子的颜色显示，或者普通句子以Python代码的颜色显示时，就可能意味着文件中存在引号不匹配的情况。</p>
</blockquote>
<p>　</p>
<blockquote>
<p>
<strong>动手试一试</strong>
</p>
<p>完成下面的每个练习时，都编写一个独立的程序，并将其保存为名称类似于name_cases.py的文件。如果遇到了困难，请休息一会儿或参阅附录C提供的建议。</p>
<p>
<strong>练习2-3：个性化消息</strong>
 　用变量表示一个人的名字，并向其显示一条消息。显示的消息应非常简单，下面是一个例子。</p>
<p>Hello Eric, would you like to learn some Python today?</p>
<p>
<strong>练习2-4：调整名字的大小写</strong>
 　用变量表示一个人的名字，再以小写、大写和首字母大写的方式显示这个人名。</p>
<p>
<strong>练习2-5：名言</strong>
 　找一句你钦佩的名人说的名言，将其姓名和名言打印出来。输出应类似于下面这样（包括引号）。</p>
<blockquote>
<p>Albert Einstein once said, “A person who never made a mistake never tried anything new.”</p>
</blockquote>
<p>
<strong>练习2-6：名言2</strong>
 　重复练习2-5，但用变量<code>famous_person</code>
 表示名人的姓名，再创建要显示的消息并将其赋给变量<code>message</code>
 ，然后打印这条消息。</p>
<p>
<strong>练习2-7：剔除人名中的空白</strong>
 　用变量表示一个人的名字，并在其开头和末尾都包含一些空白字符。务必至少使用字符组合<code>"\t"</code>
 和<code>"\n"</code>
 各一次。</p>
<p>打印这个人名，显示其开头和末尾的空白。然后，分别使用剔除函数<code>lstrip()</code>
 、<code>rstrip()</code>
 和<code>strip()</code>
 对人名进行处理，并将结果打印出来。</p>
</blockquote>
<h2 id="nav_point_41">2.4　数</h2>
<p>在编程中，经常使用数来记录得分、表示可视化数据、存储Web应用信息，等等。Python能根据数的用法以不同的方式处理它们。鉴于整数使用起来最简单，下面就先来看看Python是如何管理它们的。</p>
<h3 id="nav_point_42">2.4.1　整数</h3>
<p>在Python中，可对整数执行加（<code>+</code>
 ）减（<code>-</code>
 ）乘（<code>*</code>
 ）除（<code>/</code>
 ）运算。</p>
<pre class="代码无行号"><code>&gt;&gt;&gt; 2 + 3
5
&gt;&gt;&gt; 3 - 2
1
&gt;&gt;&gt; 2 * 3
6
&gt;&gt;&gt; 3 / 2
1.5</code>
</pre>
<p>在终端会话中，Python直接返回运算结果。Python使用两个乘号表示乘方运算：</p>
<pre class="代码无行号"><code>&gt;&gt;&gt; 3 ** 2
9
&gt;&gt;&gt; 3 ** 3
27
&gt;&gt;&gt; 10 ** 6
1000000</code>
</pre>
<p>Python还支持运算次序，因此可在同一个表达式中使用多种运算。还可以使用圆括号来修改运算次序，让Python按你指定的次序执行运算，如下所示：</p>
<pre class="代码无行号"><code>&gt;&gt;&gt; 2 + 3*4
14
&gt;&gt;&gt; (2 + 3) * 4
20</code>
</pre>
<p>在这些示例中，空格不影响Python计算表达式的方式。它们的存在旨在让你在阅读代码时能迅速确定先执行哪些运算。</p>
<h3 id="nav_point_43">2.4.2　浮点数</h3>
<p>Python将所有带小数点的数称为<strong>浮点数</strong>
 。大多数编程语言使用了这个术语，它指出了这样一个事实：小数点可出现在数的任何位置。每种编程语言都必须细心设计，以妥善地处理浮点数，确保不管小数点出现在什么位置，数的行为都是正常的。</p>
<p>从很大程度上说，使用浮点数时无须考虑其行为。你只需输入要使用的数，Python通常会按你期望的方式处理它们：</p>
<pre class="代码无行号"><code>&gt;&gt;&gt; 0.1 + 0.1
0.2
&gt;&gt;&gt; 0.2 + 0.2
0.4
&gt;&gt;&gt; 2 * 0.1
0.2
&gt;&gt;&gt; 2 * 0.2
0.4</code>
</pre>
<p>但需要注意的是，结果包含的小数位数可能是不确定的：</p>
<pre class="代码无行号"><code>&gt;&gt;&gt; 0.2 + 0.1
0.30000000000000004
&gt;&gt;&gt; 3 * 0.1
0.30000000000000004</code>
</pre>
<p>所有语言都存在这种问题，没有什么可担心的。Python会尽力找到一种精确表示结果的方法，但鉴于计算机内部表示数的方式，这在有些情况下很难。就现在而言，暂时忽略多余的小数位数即可。在第二部分的项目中，你将在需要时学习处理多余小数位的方式。</p>
<h3 id="nav_point_44">2.4.3　整数和浮点数</h3>
<p>将任意两个数相除时，结果总是浮点数，即便这两个数都是整数且能整除：</p>
<pre class="代码无行号"><code>&gt;&gt;&gt; 4/2
2.0</code>
</pre>
<p>在其他任何运算中，如果一个操作数是整数，另一个操作数是浮点数，结果也总是浮点数：</p>
<pre class="代码无行号"><code>&gt;&gt;&gt; 1 + 2.0
3.0
&gt;&gt;&gt; 2 * 3.0
6.0
&gt;&gt;&gt; 3.0 ** 2
9.0</code>
</pre>
<p>无论是哪种运算，只要有操作数是浮点数，Python默认得到的总是浮点数，即便结果原本为整数也是如此。</p>
<h3 id="nav_point_45">2.4.4　数中的下划线</h3>
<p>书写很大的数时，可使用下划线将其中的数字分组，使其更清晰易读：</p>
<pre class="代码无行号"><code>&gt;&gt;&gt; universe_age = 14_000_000_000</code>
</pre>
<p>当你打印这种使用下划线定义的数时，Python不会打印其中的下划线：</p>
<pre class="代码无行号"><code>&gt;&gt;&gt; print(universe_age)
14000000000</code>
</pre>
<p>这是因为存储这种数时，Python会忽略其中的下划线。将数字分组时，即便不是将每三位分成一组，也不会影响最终的值。在Python看来，<code>1000</code>
 与<code>1_000</code>
 没什么不同，<code>1_000</code>
 与<code>10_00</code>
 也没什么不同。这种表示法适用于整数和浮点数，但只有Python 3.6和更高的版本支持。</p>
<h3 id="nav_point_46">2.4.5　同时给多个变量赋值</h3>
<p>可在一行代码中给多个变量赋值，这有助于缩短程序并提高其可读性。这种做法最常用于将一系列数赋给一组变量。</p>
<p>例如，下面演示了如何将变量<code>x</code>
 、<code>y</code>
 和<code>z</code>
 都初始化为零：</p>
<pre class="代码无行号"><code>&gt;&gt;&gt; x, y, z = 0, 0, 0</code>
</pre>
<p>这样做时，需要用逗号将变量名分开；对于要赋给变量的值，也需同样处理。Python将按顺序将每个值赋给对应的变量。只要变量和值的个数相同，Python就能正确地将它们关联起来。</p>
<h3 id="nav_point_47">2.4.6　常量</h3>
<p>
<strong>常量</strong>
 类似于变量，但其值在程序的整个生命周期内保持不变。Python没有内置的常量类型，但Python程序员会使用全大写来指出应将某个变量视为常量，其值应始终不变：</p>
<pre class="代码无行号"><code>MAX_CONNECTIONS = 5000</code>
</pre>
<p>在代码中，要指出应将特定的变量视为常量，可将其字母全部大写。</p>
<blockquote>
<p>
<strong>动手试一试</strong>
</p>
<p>
<strong>练习2-8：数字8</strong>
 　编写四个表达式，分别使用加法、减法、乘法和除法运算，但结果都是数字8。为使用函数调用<code>print()</code>
 来显示结果，务必将这些表达式用圆括号括起来。也就是说，你应该编写四行类似于下面的代码：</p>
<pre class="代码无行号"><code>print(5+3)</code>
</pre>
<p>输出应为四行，其中每行都只包含数字8。</p>
<p>
<strong>练习2-9：最喜欢的数</strong>
 　用一个变量来表示你最喜欢的数，再使用这个变量创建一条消息，指出你最喜欢的数是什么，然后将这条消息打印出来。</p>
</blockquote>
<h2 id="nav_point_48">2.5　注释</h2>
<p>在大多数编程语言中，注释是一项很有用的功能。本书前面编写的程序中都只包含Python代码，但随着程序越来越大、越来越复杂，就应在其中添加说明，对你解决问题的方法进行大致的阐述。<strong>注释</strong>
 让你能够使用自然语言在程序中添加说明。</p>
<h3 id="nav_point_49">2.5.1　如何编写注释</h3>
<p>在Python中，注释用井号（<code>#</code>
 ）标识。井号后面的内容都会被Python解释器忽略，如下所示：</p>
<p>
<em>comment.py</em>
</p>
<pre class="代码无行号"><code># 向大家问好。
print("Hello Python people!")</code>
</pre>
<p>Python解释器将忽略第一行，只执行第二行。</p>
<pre class="代码无行号"><code>Hello Python people!</code>
</pre>
<h3 id="nav_point_50">2.5.2　该编写什么样的注释</h3>
<p>编写注释的主要目的是阐述代码要做什么，以及是如何做的。在开发项目期间，你对各个部分如何协同工作了如指掌，但过段时间后，有些细节你可能不记得了。当然，你总是可以通过研究代码来确定各个部分的工作原理，但通过编写注释以清晰的自然语言对解决方案进行概述，可节省很多时间。</p>
<p>要成为专业程序员或与其他程序员合作，就必须编写有意义的注释。当前，大多数软件是合作编写的，编写者可能是同一家公司的多名员工，也可能是众多致力于同一个开源项目的人员。训练有素的程序员都希望代码中包含注释，因此你最好从现在开始就在程序中添加描述性注释。作为新手，最值得养成的习惯之一就是在代码中编写清晰、简洁的注释。</p>
<p>如果不确定是否要编写注释，就问问自己：在找到合理的解决方案之前，考虑了多个解决方案吗？如果答案是肯定的，就编写注释对你的解决方案进行说明吧。相比回过头去再添加注释，删除多余的注释要容易得多。从现在开始，本书的示例都将使用注释来阐述代码的工作原理。</p>
<blockquote>
<p>
<strong>动手试一试</strong>
</p>
<p>
<strong>练习2-10：添加注释</strong>
 　选择你编写的两个程序，在每个程序中至少添加一条注释。如果程序太简单，实在没有什么需要说明的，就在程序文件开头加上你的姓名和当前日期，再用一句话阐述程序的功能。</p>
</blockquote>
<h2 id="nav_point_51">2.6　Python之禅</h2>
<p>经验丰富的程序员倡导尽可能避繁就简。Python社区的理念都包含在Tim Peters撰写的“Python之禅”中。要获悉这些有关编写优秀Python代码的指导原则，只需在解释器中执行命令<code>import this</code>
 。这里不打算赘述整个“Python之禅”，而只与大家分享其中的几条原则，让你明白为何它们对Python新手来说至关重要。</p>
<pre class="代码无行号"><code>&gt;&gt;&gt; import this
The Zen of Python, by Tim Peters
Beautiful is better than ugly.</code>
</pre>
<p>Python程序员笃信代码可以编写得漂亮而优雅。编程是要解决问题的，设计良好、高效而漂亮的解决方案都会让程序员心生敬意。随着你对Python的认识越来越深入，并使用它来编写越来越多的代码，有一天也许会有人站在你后面惊呼：“哇，代码编写得真是漂亮！”</p>
<pre class="代码无行号"><code>Simple is better than complex.</code>
</pre>
<p>如果有两个解决方案，一个简单、一个复杂，但都行之有效，就选择简单的解决方案吧。这样，你编写的代码将更容易维护，你或他人以后改进这些代码时也会更容易。</p>
<pre class="代码无行号"><code>Complex is better than complicated.</code>
</pre>
<p>现实是复杂的，有时候可能没有简单的解决方案。在这种情况下，就选择最简单可行的解决方案吧。</p>
<pre class="代码无行号"><code>Readability counts.</code>
</pre>
<p>即便是复杂的代码，也要让它易于理解。开发的项目涉及复杂代码时，一定要为这些代码编写有益的注释。</p>
<pre class="代码无行号"><code>There should be one-- and preferably only one --obvious way to do it.</code>
</pre>
<p>如果让两名Python程序员去解决同一个问题，他们提供的解决方案应大致相同。这并不是说编程没有创意空间，而是恰恰相反！然而，大部分编程工作是使用常见解决方案来解决简单的小问题，但这些小问题都包含在更庞大、更有创意空间的项目中。在你的程序中，各种具体细节对其他Python程序员来说都应易于理解。</p>
<pre class="代码无行号"><code>Now is better than never.</code>
</pre>
<p>你可以用余生来学习Python和编程的纷繁难懂之处，但这样你什么项目都完不成。不要企图编写完美无缺的代码，而是要先编写行之有效的代码，再决定是对其做进一步改进，还是转而去编写新代码。</p>
<p>等你进入下一章，开始研究更复杂的主题时，务必牢记这种简约而清晰的理念。如此，经验丰富的程序员定将对你编写的代码心生敬意，进而乐意向你提供反馈，并与你合作开发有趣的项目。</p>
<blockquote>
<p>
<strong>动手试一试</strong>
</p>
<p>
<strong>练习2-11：Python之禅</strong>
 　在Python终端会话中执行命令<code>import this</code>
 ，并粗略地浏览一下其他的指导原则。</p>
</blockquote>
<h2 id="nav_point_52">2.7　小结</h2>
<p>在本章中，你学习了：如何使用变量；如何创建描述性变量名以及如何消除名称错误和语法错误；字符串是什么，以及如何使用小写、大写和首字母大写方式显示字符串；使用空白来显示整洁的输出，以及如何剔除字符串中多余的空白；如何使用整数和浮点数；一些使用数值数据的方式。你还学习了如何编写说明性注释，让代码对你和其他人来说更容易理解。最后，你了解了让代码尽可能简单的理念。</p>
<p>在第3章，你将学习如何在被称为<strong>列表</strong>
 的变量中存储一系列信息，以及如何通过遍历列表来操作其中的信息。</p>
<br style="page-break-after:always"/>
<div></div>
]]></description>
    <pubDate>Fri, 03 Jul 2026 04:14:43 +0000</pubDate>
    <guid isPermaLink="false">2</guid>
</item>
<item>
    <title>Basic Python - 001 - 第一部分 基础知识.epub</title>
    <description><![CDATA[<p style='color: #888;'>⏱ 预计阅读时间：52 分钟 (10219 字)</p><hr><h1 id="nav_point_11">第一部分　基础知识</h1>
<p>本书的第一部分介绍编写Python程序所需要熟悉的基本概念，其中很多适用于所有编程语言，因此它们在你的整个程序员生涯中都很有用。</p>
<p>第1章介绍在计算机中安装Python，并运行第一个程序——在屏幕上打印“Hello Python world!”。</p>
<p>第2章论述如何在变量中存储信息以及如何使用文本和数字。</p>
<p>第3章和第4章介绍列表。使用列表能够在一个变量中存储任意数量的信息，从而高效地处理数据：只需几行代码，你就能够处理数百、数千乃至数百万个值。</p>
<p>第5章讲解使用<code>if</code>
 语句来编写这样的代码：在特定条件满足时采取一种措施，而在该条件不满足时采取另一种措施。</p>
<p>第6章演示如何使用Python字典，将不同的信息关联起来。与列表一样，你也可以根据需要在字典中存储任意数量的信息。</p>
<p>第7章讲解如何从用户那里获取输入，让程序变成交互式的。你还将学习<code>while</code>
 循环，它不断地运行代码块，直到指定的条件不再满足为止。</p>
<p>第8章介绍编写函数。函数是执行特定任务的被命名的代码块，你可以根据需要随时运行它。</p>
<p>第9章介绍类，它让你能够模拟实物，如小狗、小猫、人、汽车、火箭等，让你的代码能够表示任何真实或抽象的东西。</p>
<p>第10章介绍如何使用文件，以及如何处理错误以免程序意外地崩溃。你需要在程序关闭前保存数据，并在程序再次运行时读取它们。你将学习Python异常，它们让你能够未雨绸缪，从而让程序妥善地处理错误。</p>
<p>第11章讲解为代码编写测试，以核实程序是否像你期望的那样工作。这样，扩展程序时，你就不用担心引入新的bug了。要想脱离初级程序员的阵容，跻身于中级程序员的行列，测试代码是你必须掌握的基本技能之一。</p>
<br style="page-break-after:always"/>
<div></div>
<h1 id="nav_point_12">第 1 章　起步</h1>
<blockquote>
<p><img alt="" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/1_Image00001.gif" style="width: 16%" width="16%"/>
 　在本章中，你将运行自己的第一个程序——hello_world.py。为此，你首先需要检查自己的计算机是否安装了较新版本的Python；如果没有，就要安装它。你还要安装一个文本编辑器，用于编写和运行Python程序。你输入Python代码时，这个文本编辑器能够识别它们并突出显示不同的部分，让你能够轻松地了解代码的结构。</p>
</blockquote>
<h2 id="nav_point_13">1.1　搭建编程环境</h2>
<p>在不同的操作系统中，Python存在细微的差别，因此有几点你需要牢记在心。本节将确保你的系统正确安装Python。</p>
<h3 id="nav_point_14">1.1.1　Python版本</h3>
<p>每种编程语言都会随着新概念和新技术的推出而不断发展，Python开发者也在一直致力于丰富和强化其功能。本书编写期间的最新版本为Python 3.7，但只要你安装了Python 3.6或更高的版本，就能运行本书中的所有代码。在本节中，你将核实系统是否安装了Python，以及是否需要安装更新的版本。附录A提供了详尽的指南，指导你在各种主流操作系统中安装最新版本的Python。</p>
<p>有些较老的Python项目依然使用Python 2，但你应该使用Python 3。如果你的系统安装了Python 2，很可能是为了支持系统需要的一些旧程序。你应保留它，并安装更新的版本以便学习本书。</p>
<h3 id="nav_point_15">1.1.2　运行Python代码片段</h3>
<p>Python自带一个在终端窗口中运行的解释器，让你无须保存并运行整个程序就能尝试运行Python代码片段。</p>
<p>本书将以如下方式列出代码片段：</p>
<pre class="代码无行号"><code>❶ &gt;&gt;&gt; print("Hello Python interpreter!")
  Hello Python interpreter!</code>
</pre>
<p>提示符<code>&gt;&gt;&gt;</code>
 表明正在使用终端窗口，而加粗的文本表示需要你输入之后按回车键来执行的代码。本书的大多数示例是独立的小程序，你将在编辑器中执行它们，因为大多数代码也是这样编写出来的。然而，为高效地演示一些基本概念，需要在Python终端会话中执行一系列代码片段。只要代码清单中包含三个右尖括号（如❶所示），就意味着代码是在终端会话中执行的，而输出也是来自终端会话的。稍后将演示如何在Python解释器中编写代码。</p>
<p>此外，你还要安装一款文本编辑器，并使用它来完成学习编程的标准操作——编写一个简单的Hello World程序。长期以来，编程界都认为刚接触一门新语言时，如果首先使用它来编写一个在屏幕上显示消息“Hello world!”的程序，将给你带来好运。这种程序虽然简单，却有其用途：如果它能够在你的系统上正确运行，那么你编写的任何Python程序也都将正确运行。</p>
<h3 id="nav_point_16">1.1.3　Sublime Text简介</h3>
<p>Sublime Text是一款简单的文本编辑器，可以在任何现代操作系统中安装。你几乎能直接在Sublime Text中执行所有程序。在Sublime Text中执行程序时，代码将在其内嵌的终端会话中运行，让你能够轻松地看到输出。</p>
<p>Sublime Text是一款适合初学者的编辑器，但很多专业编程人员也在使用它。在学习Python的过程中熟练掌握Sublime Text之后，可继续使用它来编写复杂的大型项目。Sublime Text的许可条件非常宽松，可以一直免费使用，但如果你喜欢它并想长期使用，其开发者会要求你购买许可证。</p>
<p>附录B介绍了其他几种文本编辑器，如果你想知道还有哪些编辑器可供使用，现在就应该读一读。如果你想马上动手编程，可先使用Sublime Text，等有了一些编程经验后再考虑使用其他编辑器。本章稍后将引导你在当前使用的操作系统中安装Sublime Text。</p>
<h2 id="nav_point_17">1.2　在不同操作系统中搭建Python编程环境</h2>
<p>Python是一种跨平台的编程语言，这意味着它能够运行在所有主流操作系统中。在所有安装了Python的现代计算机上，都能够运行你编写的任何Python程序。然而，在不同的操作系统中，安装Python的方法存在细微的差别。</p>
<p>在本节中，你将学习如何在自己的系统中安装Python。首先要检查系统是否安装了较新的Python版本，如果没有，就进行安装；然后是安装Sublime Text。在不同的操作系统中搭建Python编程环境时，只有这两步存在差别。</p>
<p>接下来，你将运行Hello World程序，并排除各种故障。我将详细介绍如何在各种操作系统中完成这些任务，让你能够搭建一个对初学者友好的Python编程环境。</p>
<h3 id="nav_point_18">1.2.1　在Windows系统中搭建Python编程环境</h3>
<p>Windows系统并非都默认安装了Python，因此你可能需要安装它，再安装Sublime Text。</p>
<ol>
<li value="1">
<p>
<strong>安装Python</strong>
</p>
<p>首先，检查你的系统是否安装了Python。为此，在“开始”菜单中输入<code>command</code>
 并按回车以打开一个命令窗口；也可以按住Shift键并右击桌面，选择“在此处打开命令窗口”<span class="注释编号">1</span>
 。在终端窗口中输入<code>python</code>
 （全部小写）并按回车：如果出现了Python提示符（<code>&gt;&gt;&gt;</code>
 ），就说明系统安装了Python；如果出现一条错误消息，指出<code>python</code>
 是无法识别的命令，就说明没有安装Python。</p>
<p>如果出现后一种情况或者安装的Python版本低于3.6，就需要下载Windows Python安装程序。为此，请访问Python官方网站主页。将鼠标指向Download链接，你将看到一个用于下载最新版本Python的按钮。单击该按钮，这将根据你的系统自动下载正确的安装程序。下载安装程序后，运行它。请务必选中复选框Add Python（版本号）to PATH（例如图1-1），这让你能够更轻松地配置系统。</p>
<p class="p-img"><img alt="" src="https://raw.githubusercontent.com/ZHENHUA001A307/RSS_STORAGE_SOCIALSCIENCE/main/image/1_Image00002.gif" style="width: 80%" width="80%"/>
</p>
<p>
<strong>图1-1　确保选中复选框Add Python（版本号） to PATH</strong>
<br/>
 </p>
</li>
<li value="2">
<p>
<strong>在终端会话中运行Python</strong>
</p>
<p>打开一个命令窗口，并在其中执行命令<code>python</code>
 。如果出现了Python提示符（<code>&gt;&gt;&gt;</code>
 ），就说明Windows找到了你刚安装的Python版本。</p>
<pre class="代码无行号"><code>C:\&gt; python
Python 3.7.2 (v3.7.2:9a3ffc0492, Dec 23 2018, 23:09:28) [MSC v.1916 64 bit
(AMD64)] on win32
Type "help", "copyright", "credits" or "license" for more information.
&gt;&gt;&gt;</code>
</pre>
<blockquote>
<p>
<strong>注意</strong>
 　如果没有看到类似的输出，请参阅附录A中更详尽的安装说明。</p>
</blockquote>
<p>在Python会话中执行下面的命令，并确认看到了输出“Hello Python interpreter!”。</p>
<pre class="代码无行号"><code>&gt;&gt;&gt; print("Hello Python interpreter!")
Hello Python interpreter!
&gt;&gt;&gt;</code>
</pre>
<p>每当要运行Python代码片段时，都请打开一个命令窗口并启动Python终端会话。要关闭该终端会话，可按Ctrl + Z、再按回车键，也可执行命令<code>exit()</code>
 。<br/>
 </p>
</li>
<li value="3">
<p>
<strong>安装Sublime Text</strong>
</p>
<p>要下载Sublime Text安装程序，可访问Sublime Text网站主页，单击Download链接，并查找Windows安装程序。下载安装程序后运行它，并接受所有的默认设置。</p>
</li>
</ol>
<p class="注释内容">
<span class="注释编号下">1</span>
 在Windows 10系统中，可如此打开PowerShell窗口。——编者注</p>
<h3 id="nav_point_19">1.2.2　在macOS系统中搭建Python编程环境</h3>
<p>大多数macOS系统默认安装了Python。确定安装了Python后，你还需安装Sublime Text，并确保其配置正确无误。</p>
<ol>
<li value="1">
<p>
<strong>检查是否安装了Python 3</strong>
</p>
<p>在文件夹Applications/Utilities中，选择Terminal，打开一个终端窗口；也可以按Command + 空格键，再输入<code>terminal</code>
 并按回车。为确定是否安装了Python，请执行命令<code>python</code>
 （请注意，其中的<code>p</code>
 是小写的），这也将在终端窗口中启动Python，让你能够输入Python命令。输出类似于下面这样，它指出了安装的Python版本；最后的<code>&gt;&gt;&gt;</code>
 是提示符，让你能够输入Python命令。</p>
<pre class="代码无行号"><code>$ python
Python 2.7.15 (default, Aug 17 2018, 22:39:05)
[GCC 4.2.1 Compatible Apple LLVM 9.1.0 (clang-902.0.39.2)] on darwin
Type "help", "copyright", "credits", or "license" for more information.
&gt;&gt;&gt;</code>
</pre>
<p>上述输出表明，当前计算机默认使用的Python版本为Python 2.7.15。看到上述输出后，如果要退出Python并返回到终端窗口，可按Ctrl + D或执行命令<code>exit()</code>
 。</p>
<p>要检查系统是否安装了Python 3，可尝试执行命令<code>python3</code>
 。可能会出现一条错误消息，这意味着没有安装任何Python 3版本。如果输出指出安装了Python 3.6或更高的版本，可以直接跳过下一小节。如果系统没有安装Python 3，就需要手动安装它。注意，请将本书中所有的命令<code>python</code>
 都替换为命令<code>python3</code>
 ，这样才能使用Python 3（而不是Python 2）。Python 2和Python 3的差别非常大，如果你使用Python 2来运行本书的代码，肯定会遇到麻烦。</p>
<p>如果系统默认安装的是低于Python 3.6的版本，请按下一小节的说明安装最新版本。<br/>
 </p>
</li>
<li value="2">
<p>
<strong>安装最新的Python版本</strong>
</p>
<p>要下载Python安装程序，可访问Python网站主页。将鼠标指向Download链接，你将看到一个用于下载最新版本Python的按钮。单击该按钮，这将根据你的系统自动下载正确的安装程序。下载安装程序后，运行它。</p>
<p>运行安装程序后，在终端提示符下执行如下命令：</p>
<pre class="代码无行号"><code>$  python3 --version
Python 3.7.2</code>
</pre>
<p>输出应该类似于上面这样。如果确实如此，就可以开始尝试使用Python了，但请务必将本书中的每个命令<code>python</code>
 都替换为<code>python3</code>
 。<br/>
 </p>
</li>
<li value="3">
<p>
<strong>在终端会话中运行Python代码</strong>
</p>
<p>现在可以打开终端窗口并执行命令<code>python3</code>
 ，再尝试运行Python代码片段。请在终端会话中输入如下代码行并按回车：</p>
<pre class="代码无行号"><code>&gt;&gt;&gt; print("Hello Python interpreter!")
Hello Python interpreter!
&gt;&gt;&gt;</code>
</pre>
<p>消息将直接输出到当前终端窗口中。别忘了，要关闭Python解释器，可按Ctrl + D或执行命令<code>exit()</code>
 。<br/>
 </p>
</li>
<li value="4">
<p>
<strong>安装Sublime Text</strong>
</p>
<p>要安装编辑器Sublime Text，需要下载安装程序。为此，可访问Sublime Text网站主页，单击链接Download，并查找macOS安装程序。下载安装程序后运行它，再将Sublime Text图标拖放到文件夹Applications中。</p>
</li>
</ol>
<h3 id="nav_point_20">1.2.3　在Linux系统中搭建Python编程环境</h3>
<p>Linux系统是为编程而设计的，因此大多数Linux计算机默认安装了Python。编写和维护Linux的人认为，你很可能会使用这种系统进行编程，他们也鼓励你这样做。因此，要在这种系统中编程，你几乎不用安装什么软件，只需要修改一些设置。</p>
<ol>
<li value="1">
<p>
<strong>检查Python版本</strong>
</p>
<p>在你的系统中运行应用程序Terminal（如果你使用的是Ubuntu，可按Ctrl + Alt + T），打开一个终端窗口。为确定安装的是哪个Python版本，请执行命令<code>python3</code>
 （请注意，其中的<code>p</code>
 是小写的）。如果安装了Python，这个命令将启动Python解释器。输出类似于下面这样，它指出了安装的Python版本；最后的<code>&gt;&gt;&gt;</code>
 是提示符，让你能够输入Python命令。</p>
<pre class="代码无行号"><code>$ python3
Python 3.7.2 (default, Dec 27 2018, 04:01:51)
[GCC 7.3.0] on linux
Type "help", "copyright", "credits" or "license" for more information.
&gt;&gt;&gt;</code>
</pre>
<p>上述输出表明，当前计算机默认使用的Python版本为Python 3.7.2。看到上述输出后，如果要退出Python并返回到终端窗口，可按Ctrl + D或执行命令<code>exit()</code>
 。务必将本书中的每个命令<code>python</code>
 都替换为<code>python3</code>
 。</p>
<p>要运行本书的代码，必须使用Python 3.6或更高的版本。如果系统安装的是低于Python 3.6的版本，请参阅附录A，了解如何安装最新版。</p>
</li>
<li value="2">
<p>
<strong>在终端会话中运行Python代码</strong>
</p>
<p>现在可打开终端窗口并执行命令<code>python3</code>
 ，再尝试运行Python代码片段。检查Python版本时，你就这样做过。下面再次这样做，然后在终端会话中输入如下代码并按回车：</p>
<pre class="代码无行号"><code>&gt;&gt;&gt; print("Hello Python interpreter!")
Hello Python interpreter!
&gt;&gt;&gt;</code>
</pre>
<p>消息将直接打印到当前终端窗口中。别忘了，要关闭Python解释器，可按Ctrl + D或执行命令<code>exit()</code>
 。</p>
</li>
<li value="3">
<p>
<strong>安装Sublime Text</strong>
</p>
<p>在Linux系统中，可通过Ubuntu Software Center来安装Sublime Text。为此，单击菜单中的Ubuntu Software图标并查找Sublime Text，再通过单击来安装它。</p>
</li>
</ol>
<h2 id="nav_point_21">1.3　运行Hello World程序</h2>
<p>安装较新版本的Python和Sublime Text后，就可以编写并运行你的第一个Python程序了。这样做之前，需要设置Sublime Text，确保它使用系统中正确的Python版本。然后，就可以编写并运行Hello World程序了。</p>
<h3 id="nav_point_22">1.3.1　配置Sublime Text以使用正确的Python版本</h3>
<p>如果在你的系统中执行命令<code>python</code>
 时启动的是Python 3，就无须做任何配置，直接跳到下一节即可。如果需要执行命令<code>python3</code>
 来启动Python，就需要配置Sublime Text，使其使用正确的Python版本来运行你编写的程序。</p>
<p>为此，单击Sublime Text图标以启动它，也可在搜索栏中输入Sublime Text来找到它再启动。选择菜单Tools ▶ Build System ▶ New Build System，新建一个配置文件。删除该文件中的所有内容，再输入如下内容：</p>
<p>
<em>Python3</em>
</p>
<p>
<em>.sublime-build</em>
</p>
<pre class="代码无行号"><code>{
    "cmd": ["python3", "-u", "$file"],
}</code>
</pre>
<p>这段代码让Sublime Text使用命令<code>python3</code>
 来运行Python程序。将这个文件保存到Sublime Text默认打开的文件夹中，并将其命名为Python3.sublime-build。</p>
<h3 id="nav_point_23">1.3.2　运行程序hello_world.py</h3>
<p>编写第一个程序前，在系统中创建一个名为python_work的文件夹，用于存储你开发的项目。文件名和文件夹名称最好使用小写字母，并使用下划线代替空格，因为Python采用了这些命名约定。</p>
<p>启动Sublime Text，再选择菜单File ▶ Save As将Sublime Text创建的空文件存储到文件夹python_work中，并将其命名为hello_world.py。文件扩展名.py告诉Sublime Text，文件中的代码是使用Python编写的，这能让它知道如何运行这个程序，并以有帮助的方式突出其中的代码。</p>
<p>保存这个文件后，在其中输入如下代码行：</p>
<p>
<em>hello_world.py</em>
</p>
<pre class="代码无行号"><code>print("Hello Python world!")</code>
</pre>
<p>在你的系统中，如果能使用命令<code>python</code>
 来启动Python 3，可以选择菜单Tools ▶ Build或按Ctrl + B（在macOS系统中为Command + B）来运行程序。如果需要像前一节那样配置Sublime Text，请选择菜单Tools ▶ Build System ▶ Python 3来运行这个程序。从此以后，你就可以选择菜单Tools ▶ Build或按Ctrl + B（或Command + B）来运行程序了。</p>
<p>在Sublime Text的底部，将出现一个终端窗口，其中包含如下输出：</p>
<pre class="代码无行号"><code>Hello Python world!
[Finished in 0.1s]</code>
</pre>
<p>如果看不到上述输出，可能是因为这个程序出了点问题。请检查你输入的每个字符。是否不小心将<code>print</code>
 的首字母大写了？是否遗漏了引号或圆括号？编程语言的语法非常严格，只要不满足要求，就会报错。如果你无法运行这个程序，请参阅下一节的建议。</p>
<h2 id="nav_point_24">1.4　解决安装问题</h2>
<p>如果无法运行程序hello_world.py，可尝试如下几个解决方法，这些通用方法适用于所有编程问题。</p>
<ul>
<li class="第1级无序列表">程序存在严重错误时，Python将显示traceback，即错误报告。Python会仔细研究文件，试图找出其中的问题。trackback可能会提供线索，让你知道是什么问题让程序无法运行。</li>
<li class="第1级无序列表">离开计算机，先休息一会儿再尝试。别忘了，语法在编程中非常重要，即便是少一个冒号、引号不匹配或括号不匹配，都可能导致程序无法正确运行。请再次阅读本章的相关内容，并重新审视你编写的代码，看看能否找出错误。</li>
<li class="第1级无序列表">推倒重来。你也许不需要卸载任何软件，但删除文件hello_world.py并重新创建它也许是合理的选择。</li>
<li class="第1级无序列表">让别人在你的计算机或其他计算机上按本章的步骤重做一遍，并仔细观察。你可能遗漏了一小步，而别人刚好没有遗漏。</li>
<li class="第1级无序列表">请懂Python的人帮忙。当你有这样的想法时，可能发现在你认识的人当中就有人使用Python。</li>
<li class="第1级无序列表">本章的安装说明在本书主页上：ituring.cn/book/2784。对你来说，在线版也许更合适，因为可以复制并粘贴其中的代码。</li>
<li class="第1级无序列表">到网上寻求帮助。附录C提供了很多在线资源，如论坛或在线聊天网站，你可以在这些地方请求解决过相同问题的人提供解决方案。</li>
</ul>
<p>不要担心这会打扰经验丰富的程序员。每个程序员都遇到过问题，大多数程序员很乐意帮助你正确地设置系统。只要能清晰地说明你要做什么、尝试了哪些方法及其结果，就很可能有人能够帮到你。正如前言中指出的，Python社区对初学者非常友好。</p>
<p>任何现代计算机都能够运行Python。前期的问题可能令人沮丧，但很值得你花时间去解决。能够运行hello_world.py后，你就可以开始学习Python了，而且编程工作会更有趣，也更令人愉快。</p>
<h2 id="nav_point_25">1.5　从终端运行Python程序</h2>
<p>你编写的大多数程序将直接在文本编辑器中运行，但有时候从终端运行程序很有用。例如，你可能想直接运行既有的程序。</p>
<p>在任何安装了Python的系统上都可以这样做，前提是你知道如何进入程序文件所在的目录。为尝试这样做，请确保将文件hello_world.py存储到了桌面的文件夹python_work中。</p>
<h3 id="nav_point_26">1.5.1　在Windows系统中从终端运行Python程序</h3>
<p>在命令窗口中，可以使用终端命令<code>cd</code>
 （表示change directory，即<strong>切换目录</strong>
 ）在文件系统中导航。使用命令<code>dir</code>
 （表示directory，即<strong>目录</strong>
 ）可以显示当前目录中的所有文件。</p>
<p>为运行程序hello_world.py，请打开一个新的终端窗口，并执行下面的命令：</p>
<pre class="代码无行号"><code>❶ C:\&gt; cd Desktop\python_work
❷ C:\Desktop\python_work&gt; dir
  hello_world.py
❸ C:\Desktop\python_work&gt; python hello_world.py
  Hello Python world!</code>
</pre>
<p>这里使用了命令<code>cd</code>
 来切换到文件夹Desktop\python_work（见❶）。接下来，使用命令<code>dir</code>
 来确认这个文件夹中包含文件hello_world.py（见❷）。最后，使用命令<code>python hello_world.py</code>
 来运行这个文件（见❸）。</p>
<p>大多数程序可直接从编辑器运行，但待解决的问题比较复杂时，你编写的程序可能需要从终端运行。</p>
<h3 id="nav_point_27">1.5.2　在Linux和macOS系统中从终端运行Python程序</h3>
<p>在Linux和macOS系统中，从终端运行Python程序的方式相同。在终端会话中，可以使用终端命令<code>cd</code>
 （表示change directory，即<strong>切换目录</strong>
 ）在文件系统中导航。使用命令<code>ls</code>
 （表示list，即<strong>列表</strong>
 ）可以显示当前目录中所有未隐藏的文件。</p>
<p>为运行程序hello_world.py，请打开一个新的终端窗口，并执行下面的命令：</p>
<pre class="代码无行号"><code>❶ ~$ cd Desktop/python_work/
❷ ~/Desktop/python_work$ ls
  hello_world.py
❸ ~/Desktop/python_work$ python hello_world.py
  Hello Python world!</code>
</pre>
<p>这里使用了命令<code>cd</code>
 来切换到文件夹Desktop/python_work（见❶）。接下来，使用命令<code>ls</code>
 来确认这个文件夹中包含文件hello_world.py（见❷）。最后，使用命令<code>python hello_world.py</code>
 来运行这个文件（见❸）。</p>
<p>就这么简单。要运行Python程序，只需使用命令<code>python</code>
 （或<code>python3</code>
 ）即可。</p>
<blockquote>
<p>
<strong>动手试一试</strong>
</p>
<p>本章的练习都是探索性的，但从第2章开始将要求你用那一章学到的知识来解决问题。</p>
<p>
<strong>练习1-1：python.org</strong>
 　浏览Python主页，寻找你感兴趣的主题。你对Python越熟悉，这个网站对你来说就越有用。</p>
<p>
<strong>练习1-2：输入错误</strong>
 　打开你刚创建的文件hello_world.py，在代码中添加一个输入错误，再运行这个程序。输入错误会引发错误吗？你能理解显示的错误消息吗？你能添加一个不会导致错误的输入错误吗？你凭什么认为它不会导致错误？</p>
<p>
<strong>练习1-3：无穷的技艺</strong>
 　如果你有无穷多种编程技艺，你打算开发什么样的程序呢？你就要开始学习编程了。如果心中有目标，就能立即将新学到的技能付诸应用，现在正是草拟目标的大好时机。将想法记录下来是个不错的习惯，这样每当需要开始新项目时，都可参考它们。现在请花点时间描绘三个你想创建的程序。</p>
</blockquote>
<h2 id="nav_point_28">1.6　小结</h2>
<p>在本章中，你大致了解了Python，并在自己的系统中安装了Python。你还安装了一个文本编辑器，以简化Python代码的编写工作。你学习了如何在终端会话中运行Python代码片段，并运行了第一个程序——hello_world.py。你还大致了解了如何解决安装问题。</p>
<p>在下一章，你将学习如何在Python程序中使用各种数据和变量。</p>
<br style="page-break-after:always"/>
<div></div>
]]></description>
    <pubDate>Fri, 03 Jul 2026 04:02:45 +0000</pubDate>
    <guid isPermaLink="false">1</guid>
</item>
</channel>
</rss>